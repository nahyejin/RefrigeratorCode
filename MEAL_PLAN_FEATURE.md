# 식단·조리·유통기한 기능

> 이 문서는 **지금 어떻게 동작하는지**를 적는다.
> "언제 무엇을 왜 바꿨나" 는 `CHANGELOG.md` 에 있다.

---

## 1. 유통기한 — 무엇을 "곧 상한다" 고 보는가

`frontend/src/utils/expiry.ts` 한 곳에 기준이 있다. 화면마다 다른 숫자를 쓰면
같은 재료가 어디선 급하고 어디선 안 급해 보인다.

| 상태 | 조건 | 식단에 쓰나 | 어디에 쓰나 |
|---|---|---|---|
| **곧 상해요** | 남은 날 `5일 이내` ~ 지난 지 `14일 이내` | **쓴다 (먼저)** | 알림 배너 · 식단 우선순위 |
| (표시 안 함) | 남은 날 `6일 이상`, 기한 안 적음 | **쓴다** | — |
| **정리하세요** | 지난 지 `14일 넘음` | 기본으로 **안 씀** | 안내 · 눌러서 도로 넣을 수 있음 |

읽는 법은 하나다 — **냉장고에 있는 건 기본적으로 다 쓴다.** 예외는 두 주 넘게
지난 것뿐이고, 그것도 사용자가 누르면 들어간다. "14일 미만 지난 재료는 어떻게
되나" 는 이 표의 첫 줄이다: 쓰고, 오히려 **먼저** 쓴다.

```ts
export const SOON_DAYS = 5;         // 이번 주에 먹어야 하는 것
export const STALE_AFTER_DAYS = 14; // 이 넘으면 "아마 이미 없는 것"
```

**왜 5일**: 장은 보통 주 1회 본다. 5일이면 "이번 주에 써야 하는 것" 과 거의 같다.
더 길게 잡으면 냉장고 절반이 목록에 올라와 정작 급한 게 묻힌다.

**왜 14일에서 자르나**: 두 주 넘게 지난 재료는 냉장고에 실제로 있는 게 아니라
**앱에서 안 지운 것**일 가능성이 훨씬 높다. 실제로 `소고기 152일 지났어요` 가
식단 기준으로 쓰이고 있었고, 그 결과 `코스트코 고기 할인` 같은 글이 식단에
올라왔다. 그렇다고 조용히 숨기면 그 줄은 냉장고 목록에 영영 남는다 —
**식단 기준에서만 빼고, "정리하세요" 로 따로 말한다.**

한동안 이 규칙이 **말과 다르게 돌고 있었다.** 안내에는 "식단에 쓰지 않았어요" 라고
적혀 있었지만 매칭 질의(`my_ingredients`)에는 냉장고 재료를 통째로 보냈고, 오래된
재료는 우선순위(`applied_expiry_ingredients`)에서만 빠졌다. 지금은 `planIngredients`
한 목록이 **매칭과 우선순위를 함께** 정한다.

### 이번 주에 쓸 재료를 직접 고르기

`/plan` 의 ① 영역에서 재료 칩을 누르면 이번 주 식단에서 뺐다 넣었다 할 수 있다.
**뺀 재료가 들어간 요리는 후보에서 통째로 빠진다** — 질의에서 이름만 빼면 매칭률만
낮아질 뿐, 그 재료를 쓰는 요리는 그대로 올라온다.

- 빼는 쪽을 상태로 담는다(`off`). 기본이 "다 쓴다" 이므로 예외만 기억하면 되고,
  새 재료를 넣었을 때 자동으로 포함된다. 반대로 담았다면 새 재료가 매번 빠진 채로
  시작한다
- 뺀 재료도 목록에서 지우지 않고 취소선만 긋는다. 지우면 잘못 눌렀을 때
  되돌릴 자리가 사라진다
- 냉장고에서 지워지는 것은 **아니다.** 이번 주 식단에서만 빠진다

### 날짜를 어디서 가져오나

1. 사용자가 적었거나 포장지에서 읽힌 `expiry` — **정확하다. 무조건 우선**
2. 없으면 `estimatedExpiry` (구매일 + 재료 종류로 짐작)
3. 그것도 없으면 `estimateExpiry()` 로 그 자리에서 짐작
4. 넷 다 없으면 **목록에서 빠진다** (없는 날짜를 지어내지 않는다)

짐작값에는 화면에 **`약`** 을 붙인다. 정직하게 말하지 않으면 멀쩡한 재료를
버리게 만든다.

### 유통기한이 하나도 없으면

**식단은 정상으로 짜인다.** 냉장고 재료 전체(`my_ingredients`)로 매칭한다.
다만 "무엇을 먼저 써야 하는지" 를 못 정하므로 순서는 매칭률만으로 정해지고,
화면에는 이렇게 말한다:

> 냉장고 재료 12개로 만들 수 있는 것들이에요.
> 유통기한을 넣어 두면 **곧 상하는 것부터** 골라 드려요.

---

## 2. 이번 주 식단 추천 (`/plan`)

**내일부터 7일.** 오늘을 넣으면 이미 저녁이 지난 경우가 많아 첫 칸이 버려진다.
수요일에 짜면 목~수가 된다.

### 두 가지 방식

| | 무엇이 다른가 | 크레딧 |
|---|---|---|
| **기본** | 매칭률로 줄 세우고 겹치는 요리를 걸러 냄 | **안 씀** |
| **AI** | `"담백하게"`, `"아이 먹을 것 위주로"` 같은 **말을 받음** | `plan` = 2 |

기본 방식(다시 짜기·다른 요리로 바꾸기·요일 옮기기)은 **전부 무료**다.
미리 뽑아 둔 재료를 냉장고와 맞춰 보는 것뿐이라 얼마든지 눌러도 된다.
화면에도 그렇게 적어 둔다 — 안 적으면 "AI 가 짜 주는 것" 으로 오해해서
크레딧이 닳는 줄 알고 아껴 쓰게 된다.

### 겹치는 요리를 거르는 규칙

매칭률만 보고 위에서부터 자르면 **비슷한 요리가 줄줄이 나온다**
(김치찌개 → 김치볶음밥 → 김치전). 한 주 식단으로는 쓸 수 없다.

```
1차: 이미 뽑은 요리와 재료가 절반 넘게 겹치면 건너뛴다
2차: 그래도 7칸을 못 채우면 남은 후보로 채운다
```

2차가 필요한 이유: 후보가 적을 때 7일 중 3일이 비어 버렸다.
**비슷한 요리가 하루 더 들어오는 게, 그 날 칸이 텅 비어 있는 것보다 낫다.**

### 입구가 둘인 이유 — 노란색은 거짓말을 하면 안 된다

요리 캘린더의 식단 입구는 **두 버튼**이다.

| 버튼 | 옷 | 하는 일 |
|---|---|---|
| `이번 주 식단 추천` | 평범한 흰 버튼 | `/plan` 으로 간다. **무료** |
| `조건 넣어 추천` | 노란 그라디언트 + 반짝임 + `AI` 배지 | `/plan?ai=1` 로 가서 **조건 칸을 맨 위에 연다** |

한때 이 자리에 버튼이 하나였고, 그 하나가 노란색·AI 배지·반짝임을 다 달고
있었다. 그런데 눌러 봐야 그냥 페이지가 열렸다. **시각 신호가 거짓말을 하면
정작 크레딧을 쓰는 자리에서도 사람이 안 멈춘다** — 노란색이 "여기서 돈이 나간다"
를 뜻하지 않게 되기 때문이다.

두 버튼은 **다른 화면**을 연다. 같은 화면을 열면 나눈 의미가 없다.

| | `/plan` | `/plan?ai=1` |
|---|---|---|
| 제목 | 이번 주 식단 추천 | **AI 식단 추천** |
| 들어가자마자 | 바로 무료 추천 | **조건 묻는 시트** |
| 다시 하기 | `↻ 다시 추천`(무료) | 무료 + **`조건 바꿔서 다시`**(크레딧) |

조건 시트에는 자주 적는 조건 여섯을 알약으로 둔다(`아이 먹을 것 위주로`,
`다이어트 · 담백하게`, `간단한 걸로`, `국물 요리는 빼고`, `손님상에 올릴 것`,
`고기 없이`). 빈 칸만 주고 "적어 보세요" 하면 대부분 그냥 넘긴다 —
눌러서 고를 수 있으면 **무엇을 말할 수 있는 자리인지**가 그 자리에서 보인다.

시트는 **닫을 수 있다**(`그냥 무료 추천 볼게요`). 크레딧이 없거나 마음이
바뀌었을 때 화면이 막히면 안 된다.

**AI 버튼도 누르는 순간 크레딧을 쓰지는 않는다.** 냉장고에 뭐가 있는지 보기도
전에, "아이 먹을 것 위주로" 같은 조건을 적기도 전에 돈이 나가면 결과가 마음에
안 들 때 그대로 손해다. 데려다 주기만 하고, 크레딧은 그 칸에서 조건을 적고
누를 때 나간다. 데려온 자리라는 걸 알 수 있게 그 칸에 노란 테두리를 잠깐 두르고
입력창에 커서를 놓는다.

### AI 식단 (`POST /api/plan/suggest`)

**후보는 우리가 고르고 LLM 은 배치만 한다.**

```
① 사용자 요청을 찾을 수 있는 말로 바꾼다   (backend/plan_intent.py)
② 냉장고 재료 + 그 말로 SQL 검색 → 후보 60개
③ 후보 목록 + 냉장고 재료 + 곧 상하는 재료 + 요청 + 해석 지침을 프롬프트에
④ LLM 은 번호만 고른다  {"plan":[{"n":3,"why":"양파가 이틀 남았어요"}]}
⑤ 그 번호를 실제 레시피로 되돌림
```

레시피를 지어내게 두면 우리 DB 에 없는 요리를 추천하게 되고, 그러면
**조리 순서도 쿠팡 링크도 붙일 수 없다.**

크레딧은 **호출 전에** 깎는다. 호출해 놓고 나중에 깎으면 한도를 넘겨 쓰게 된다.

#### ①이 왜 필요했나 — 요청이 후보 선정에 안 쓰였다

한동안 `request_text` 는 **프롬프트에만** 들어가고 후보를 고르는 데는 전혀 안
쓰였다. 재료 매칭만으로 뽑은 40개를 넘겼으니, 그 안에 아이가 먹을 만한 게
없으면 LLM 이 아무리 잘해도 나올 수가 없었다. "아이 먹을 것 위주로" 라고 적어도
`전골 10가지`, `백반집 한 상` 이 나왔다.

`plan_intent.read(text)` 가 `(찾을 말, 해석 지침, 뺄 말)` 셋을 돌려준다.

| 적은 말 | 찾을 말 | 지침 |
|---|---|---|
| 아이 | 아이·아이들·유아·어린이·키즈·아기 | 맵지 않고 자극이 적어야 |
| 다이어트 | 다이어트·저칼로리·저지방·포만감·저당 | 튀김·설탕이 많은 것은 피한다 |
| 담백 | 담백·슴슴·순한·부드러운 | 맵고 짠 것을 피한다 |
| 간단 | 초간단·간단·10분·15분·쉬운 | 손이 적게 가는 쪽 |
| 손님상 | 손님·집들이·잔치상·파티 | 보기 좋고 양이 넉넉한 쪽 |

**왜 사전으로 두나**: 앱의 레시피 필터가 이미 같은 분류(효능·대상·TPO·난이도·
스타일)를 쓴다. 버튼으로 고르든 글로 적든 **같은 말로 찾아야** 결과가 어긋나지
않는다.

**뺄 말도 읽는다**: `국물 요리는 빼고` 처럼 부정이 두 어절 뒤에 와도 잡는다
(트리거 뒤 **12글자** 안에서 `빼/말고/없이/제외/싫` 을 찾는다).
`국물 좋아하는데 매운 건 싫어` 는 국물을 빼지 않는다 — 문장 전체를 보면
이런 것이 잘못 걸리고, 바로 뒤만 보면 `요리는` 같은 말에 막힌다.

**걸러 내지 않고 순서만 바꾼다**: `want_hit`(제목·본문에 그 말이 있나)로
정렬만 올린다. 딱 맞는 게 적을 때 걸러 버리면 후보가 0이 되어 아무것도 못 짠다.

```sql
ORDER BY (want_hit > 0) DESC, avoid_hit ASC, want_hit DESC, hit DESC, id DESC
```

실측 — 같은 냉장고, 요청만 바꿔서:

| 요청 | 상위 후보 |
|---|---|
| (없음) | 전골 10가지 · 백반집 한 상 10가지 · 반찬 20가지 |
| 아이 먹을 것 위주로 | 아기등갈비 · 아기 목살 수육 · 아이죽 · 유아간식 |
| 다이어트 중인데 담백하게 | 혈당 걱정 없는 케이크 · 다이어트 샌드위치 · 연두부계란탕 |
| 국물 요리는 빼고 간단한 걸로 | 초간단 호박죽 · 초간단 샌드위치 |

### 계획 반영

`[이번 주 식단 계획 반영하기]` → 요리 캘린더에 남는다.
짜고 끝나면 아무 데도 안 남아서 다음 날 "뭐 해 먹기로 했더라" 를 다시 묻게 된다.

- 저장 자리: `localStorage.cookmatch_meal_plan` (**기기 저장**)
- 서버에 안 두는 이유: 계획은 그날이 지나면 값어치가 없고, 무엇보다
  **비회원도 식단을 짜야 하는데** 서버에 두면 로그인 벽 뒤로 들어간다
- **완료 기록과 섞지 않는다.** 캘린더의 기존 항목은 실제로 만든 것이고 계획은
  아직 아니다 — 월 보기에서 계획은 `날짜 뒤에 깔리는 노란 원`, 완료는 `채워진 점`
- 식단을 고치면(다시 짜기·바꾸기·요일 옮기기·AI) **반영 표시가 풀린다**

#### 하루에 여러 끼

`planByDate()` 는 `Map<string, PlannedMeal[]>` 다. 한때 `Map<string, PlannedMeal>`
이라 하루에 하나만 남았고, 그래서 요일을 옮기는 일이 **두 칸 맞바꾸기** 밖에 될
수 없었다 — 토요일 것을 일요일로 옮기면 멀쩡하던 일요일 요리가 토요일로 밀려났다.

- `Slot { date, meals: Meal[] }` / `Meal { recipe, on }` — 담을지는 **끼니마다**
- `moveTo(from, at, to)` 는 **옮긴다.** 맞바꾸지 않는다
- 옮기고 나면 빈 날이 생긴다. 그것도 정상이라고 화면에 적는다(`이 날은 비워 뒀어요`)

#### 덮어쓰기 전에 묻는다

며칠에 걸쳐 고쳐 둔 계획이 버튼 한 번에 사라지는데 사라졌다는 사실조차 안 보였다.
겹치는 날이 있을 때만 창을 띄우고 셋 중에 고르게 한다.

| 고르기 | 하는 일 |
|---|---|
| 새로 짠 것으로 바꾸기 | `savePlan(meals, 'overwrite')` — 겹치는 날을 새 요리로 |
| 비어 있는 날에만 넣기 | `savePlan(meals, 'fill')` — 이미 정해 둔 날은 그대로 |
| 그만두기 | 아무것도 안 한다 |

---

## 3. 장보기 목록

식단에서 **나온다.** 체크한 날의 요리에 필요한데 냉장고에 없는 재료다.
여러 요리에 쓰이는 것부터 놓는다.

쿠팡 링크는 기존 3단 폴백(`resolveCoupangUrl`)을 쓴다 —
`coupang_ads.csv` → 재료 사전 `coupang_link` → 파트너스 검색 딥링크.

---

## 4. 요리 모드 (`CookModeSheet`)

레시피 카드를 누르면 **원문으로 안 가고** 앱 안에서 조리 순서를 본다.
요리하는 중에 블로그 원문을 여는 건 사실상 못 할 짓이다 — 손에 물이 묻어 있고,
위아래로 한참 스크롤해야 하고, 중간에 광고와 잡담이 섞여 있다.

- `GET /api/recipes/<id>/cook` — 재료(분량 포함) + 조리 순서만
- 목록 API 에 안 싣는 이유: 조리 순서는 한 건에 수백 자다. 목록에 실으면
  20건만 불러도 응답이 몇 배가 되는데 대부분은 열어 보지도 않는다
- **원문 링크는 항상 크게 둔다.** 요약은 요약이다
- 조리 순서가 없는 글(영상으로만 설명한 유튜브 등)은 지어내지 않고 링크로 보낸다

### 소리로 읽어 주기

손이 젖어 있으면 화면을 못 만진다. 브라우저 내장 음성(`speechSynthesis`)이라
**API 비용이 없고** 앱에서도 그대로 된다.

- 단계마다 발화를 따로 만들고 `onend` 로 잇는다. 전체를 한 덩어리로 넘기면
  **지금 어느 단계인지 알 수 없어** 화면에서 짚어 줄 수가 없다
- 읽고 있는 단계를 노랗게 짚어 준다
- 시트를 닫거나 원문으로 나가면 반드시 멈춘다

---

## 5. 조리 순서는 어디서 오나

매일 새벽 3시 배치(`ingredient_management/llm_ingredient_extraction.py`)가
**한 번의 LLM 호출로 세 가지를 동시에** 한다:

1. 이 글이 요리 글이 맞나 (아니면 삭제)
2. 무슨 재료가 필요한가 → `used_ingredients` (사전 대표어)
3. 조리 순서와 분량 → `cook_steps`, `ingredients_detail`

### 분량을 지키는 규칙

프롬프트에 **종류별 예시를 다 박는다.** 처음엔 `"불·시간·양"` 이라고만 쓰고
예시를 `"중불에서 5분"` 하나만 들었더니, 모델이 그걸 좁게 해석해
**시간·불세기만 수치로 보고 분량은 전부 버렸다**(보존율 27%).

지금은 분량 / 시간·불세기 / 크기·두께 / 온도·개수를 각각 예시로 들고,
틀린 예도 함께 보여 준다. 보존율 95%.

품질은 `scripts/check_cook_steps_quality.py` 로 **숫자로** 잰다.
프롬프트를 만질 때마다 전후를 비교할 것 — 한두 건을 눈으로 보고 "괜찮네" 로
넘어가면 이런 편향을 못 잡는다.
