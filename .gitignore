node_modules/
__pycache__/
.env
.env.*
backend/.env
backend/.env.*
env.*
venv/
.DS_Store
*.log
logs/
.env.local
.env.development.local
.env.test.local
.env.production.local
chromedriver-win64/
*.db
ingredient_management/llm_full_run_*.csv

# LLM 배치가 매 실행마다 남기는 미리보기 결과물 (재생성 가능)
ingredient_management/llm_used_ingredients_preview_*.csv
ingredient_management/renormalize_preview_*.csv
llm_ingredients.log

# 계정 병합 백업 — 비밀번호 해시가 들어 있어 절대 커밋하지 않는다
scratch_backups/

# 사전 반영 배치 로그
dictionary_sync.log

# Firebase 서비스 계정 키(푸시 발송용 비밀키) — 절대 커밋하지 않는다
# (google-services.json / GoogleService-Info.plist 는 비밀이 아니라 앱에 들어가는 설정 파일이라 커밋해도 됨)
backend/firebase-service-account*.json
*-firebase-adminsdk-*.json

# 스토어 스크린샷용으로 데모 영상에서 뽑은 원본 프레임(store/make_store_assets.py가 다시 만든다)
store/.frames/
