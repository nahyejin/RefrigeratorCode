# AI 사용량(크레딧) 정책 · 어드민 설계

카메라 사진 인식과 요리 챗봇이 쓰는 LLM 호출에 **사용자별 한도**를 두기 위한
설계. 2026-09-02 작성.

> 이 문서는 **왜 그렇게 정했는지**를 남기는 것이 목적이다. 숫자만 바꾸고 싶으면
> 맨 아래 "숫자를 바꾸고 싶을 때" 절을 본다.

---

## 1. 지금 상태 (고치기 전)

`backend/chat_service.py`

```python
_rate_count = 0                      # 프로세스 전역 변수
def _daily_limit():
    return int(os.getenv('LLM_DAILY_LIMIT', '250'))
```

이 하나가 챗봇(`/api/chat`)과 사진 인식(`/api/ingredients/recognize`)에
**함께** 걸려 있다. 문제는 넷이다.

| 문제 | 결과 |
|---|---|
| 사용자별이 아니라 **서버 전체 공용** | 한 명이 250번 쓰면 나머지 전원 차단 |
| 프로세스 **메모리**에만 존재 | 배포·재시작 때마다 0으로 리셋 |
| 인스턴스가 늘면 각자 셈 | 2개면 실제 한도는 500 |
| **기록이 없음** | 누가 얼마나 썼는지 확인할 방법이 전혀 없음 |

지금 사용자가 9명이라 안 터지고 있을 뿐이다. **광고로 유입을 늘리기 전에
반드시 고쳐야 한다.**

---

## 2. 주별 + 일별 이중 한도

### 왜 일별이 아니라 주별인가

사용 패턴이 고르지 않다. 냉장고 재료를 등록하는 일은 **장 본 날 하루에 몰린다.**
일별로만 걸면 그날 한도가 터지고, **첫 사용 경험이 차단으로 끝난다.** 신규
사용자를 잃는 가장 나쁜 지점이다. 반대로 나머지 6일은 한도가 남아도 안 쓴다.

### 왜 주별만으로는 안 되는가

가입 첫날 한 주치를 다 태우고 6일을 못 쓰는 일이 생긴다. 실수나 버그로 인한
폭주도 한 주 한도를 통째로 날린다.

### 결론

**주별 한도 + 일별 상한(버스트 캡)** 을 함께 둔다.

| 구분 | 주 한도 | 일 상한 | 비고 |
|---|---|---|---|
| 비회원 | 10 | 6 | 체험용. 가입 유인이 목적 |
| 회원 (free) | 30 | 15 | 기본값 |
| 회원 (plus) | 150 | 50 | 어드민이 수동 지정 |

> **2026-09-03 조정.** 전에는 무료가 주 100 이었다. 아래 "숫자의 근거" 대로
> 다시 세어 보니 보통 사용자가 쓰는 양의 **6~9배**라, 아무도 한도를 느끼지 못했다.
> 한도를 못 느끼면 유료로 올릴 명분이 없다. 지금 줄이는 또 다른 이유는,
> 사람들이 100 에 익숙해진 뒤에 줄이면 **쓰던 걸 뺏는 게 되기 때문**이다.
> 아직 익숙해진 사용자가 없을 때 정해 두는 편이 낫다.

**리셋: 매주 월요일 00:00 KST.**
가입일 기준 롤링 7일이 아니라 **고정 요일**로 둔다. 사용자가 "언제 리셋되는지"를
예측할 수 있어야 문의가 안 생긴다.

---

## 3. 크레딧 환산 — 왜 사진이 "장수 × N" 이 아닌가

처음에는 사진 장수만큼 크레딧을 매기려 했다. **틀린 계산이었다.**

측정값 (`gemini-3.5-flash-lite`, `countTokens` 실측):

| 요청 | 토큰 | **API 호출 수** |
|---|---|---|
| 프롬프트만 | 519 ~ 695 | — |
| 프롬프트 + 사진 1장 | 1,759 | **1회** |
| 프롬프트 + 사진 2장 | 2,823 | **1회** |
| 프롬프트 + 사진 5장 | 6,015 | **1회** |

핵심은 마지막 열이다. **무료 티어의 한도는 토큰이 아니라 "요청 수"로 센다.**
사진을 5장 넣든 1장 넣든 한 요청의 `parts` 에 함께 실려 나가므로 **실제로 깎이는
한도는 똑같이 1**이다.

그러니 장수만큼 크레딧을 매기면, **우리가 권장해야 할 행동(여러 장을 한 번에
올려 호출을 아끼는 것)에 벌금을 매기는** 꼴이 된다.

### 확정 환산표

| 행동 | 크레딧 | 왜 |
|---|---|---|
| 챗봇 질문 1회 | **1** | 기준 단위 |
| 사진 인식 1회 (1~5장) | **2** | 실제 호출은 1회지만 토큰이 2~4배. 나중에 유료 티어로 넘어가면 토큰이 비용이 되므로 미리 반영해 둔다 |

사용자에게는 크레딧이라는 말을 굳이 쓰지 않는다. 화면에는 **`이번 주 62 / 100`**
하나로만 보인다.

### 30이라는 숫자의 근거

**실제로 요리하는 사람의 한 주**에서 거꾸로 셌다.

| | 빈도 | 크레딧 |
|---|---|---|
| 요리하며 챗봇에 묻기 | 주 3~4회 × 2~3번 | 9~12 |
| 장 보고 영수증 찍기 | 주 1회 × 1~2장 | 2~4 |
| **보통 사용자 합계** | | **11~16 / 주** |

무료 30 은 그 **두 배**다.

- 평범하게 쓰면 절대 안 걸린다. 걸리는 사람은 **매일 요리하거나 사진을 자주 찍는
  사람**이고, 그 사람이 곧 유료로 넘어갈 사람이다
- 일 상한 15 는 주 한도의 절반이다. 하루에 몰아 써서 주 초에 다 태우고
  "남은 6일 동안 아무것도 못 하는" 상태를 막는다
- 첫 주 세팅 시나리오 — 영수증 2장 + 재료 사진 3회 + 챗봇 8회
  = `2×2 + 3×2 + 8×1` = **18 크레딧**. 세팅을 마치고도 12가 남는다
- plus 150 은 무료의 5배다. 400 은 과했다 — **아무도 도달 못 하는 한도는
  없는 한도와 같다.** 그러면 유료를 사도 뭐가 좋아졌는지 체감이 안 된다

숫자는 전부 환경변수로 덮을 수 있다(`QUOTA_FREE_WEEKLY` 등). 실제 사용량이
쌓이면 어드민 대시보드의 주별 크레딧을 보고 다시 조정한다 — **감이 아니라
그 값을 보고.**

### 전체 수용량은 되는가

관측값: 재료 추출 배치가 **하루 약 5,300 요청**에서 일일 한도에 걸렸다
(2026-09-01, 5,280건 중 마지막 36건이 한도 소진으로 스킵).
챗봇/인식은 별도 키(`GEMINI_API_KEY_CHAT`)를 쓰므로 그 키의 상한은 아직
관측되지 않았지만, 같은 계정 등급이라면 비슷한 규모로 본다.

일 상한 40 크레딧(= 최대 40 요청)을 **끝까지 다 쓰는** 사용자가 하루에 몇 명까지
가능한지:

| 하루 여유 요청 | 최대 헤비 유저 |
|---|---|
| 1,000 | 25명 |
| 5,000 | 125명 |

실제로는 대부분이 하루 몇 크레딧만 쓰므로 훨씬 여유롭다. **정확한 시점은
`llm_usage` 원장을 보고 판단한다** — 그게 이 설계에서 원장을 먼저 만드는 이유다.

---

## 4. 데이터 모델

### `llm_usage` — 원장 (호출 1건 = 1행)

```sql
CREATE TABLE llm_usage (
  id            BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id       INT NULL,                 -- 비회원이면 NULL
  device_id     VARCHAR(64) NULL,         -- 비회원 식별용 (클라이언트 생성 UUID)
  kind          VARCHAR(16) NOT NULL,     -- 'chat' | 'vision'
  credits       INT NOT NULL,             -- 우리가 정한 환산값
  detail        VARCHAR(64) NULL,         -- vision 이면 'mode/장수'
  model         VARCHAR(48) NULL,         -- 어느 모델을 썼는지
  images        INT NULL,                 -- 사진 장수
  prompt_tokens INT NULL,                 -- ↓ 실제로 쓴 양 (응답의 usageMetadata)
  output_tokens INT NULL,
  total_tokens  INT NULL,
  created_at    DATETIME NOT NULL,
  INDEX idx_user_time (user_id, created_at),
  INDEX idx_device_time (device_id, created_at),
  INDEX idx_time (created_at)
);
```

**카운터가 아니라 원장을 둔다.** 카운터만 두면 "주별을 월별로 바꾸자", "지난달에
누가 많이 썼지" 가 전부 불가능하다. 원장이 있으면 집계 기준을 언제든 바꿀 수
있고, 어드민 통계가 전부 여기 한 곳에서 나온다.

### 크레딧과 토큰을 **둘 다** 남긴다

`credits` 는 우리가 정한 환산값이고, `*_tokens` 는 **실제로 쓴 양**이다.
크레딧만 남기면 "사진 2크레딧이 적정한가", "유료 티어로 넘어가면 원가가 얼마인가"
를 감으로밖에 못 정한다.

Gemini 응답의 `usageMetadata` 를 그대로 받아 적는다. 호출 함수의 반환값을
바꾸면 호출부를 전부 고쳐야 해서, 같은 스레드에서만 보이는 자리에 흘려 두고
(`note_gemini_usage`) 호출이 끝난 쪽에서 원장 행에 붙인다(`attach_tokens`).
기록이 실패해도 사용자 요청은 성공시킨다.

실측 예 (사진 2장, `food-multi`):

| kind | credits | images | prompt | output | total |
|---|---|---|---|---|---|
| vision | 2 | 2 | 2,841 | 18 | 2,859 |

**환산이 맞는지 확인하는 쿼리** — 크레딧당 실제 토큰이 종류별로 얼마나 차이 나는지:

```sql
SELECT kind,
       COUNT(*) n,
       ROUND(AVG(total_tokens))            avg_tokens,
       ROUND(AVG(total_tokens / credits))  tokens_per_credit
FROM llm_usage
WHERE total_tokens IS NOT NULL
GROUP BY kind;
```

`tokens_per_credit` 이 종류별로 크게 벌어지면 `CREDITS_VISION` 을 조정한다.
**감으로 바꾸지 말고 이 쿼리를 먼저 본다.**

### `user_quota` — 플랜과 수동 조정

```sql
CREATE TABLE user_quota (
  user_id       INT PRIMARY KEY,
  plan          VARCHAR(16) NOT NULL DEFAULT 'free',   -- free | plus
  weekly_limit  INT NULL,        -- NULL 이면 plan 기본값
  daily_cap     INT NULL,        -- NULL 이면 plan 기본값
  note          VARCHAR(255) NULL,   -- 왜 올려줬는지 (어드민 메모)
  updated_by    INT NULL,
  updated_at    DATETIME NOT NULL
);
```

행이 없으면 free 기본값을 쓴다. **한도를 올려준 이유를 `note` 에 반드시 적는다** —
안 적으면 몇 달 뒤에 왜 이 사람만 400인지 아무도 모른다.

### `users.is_admin`

```sql
ALTER TABLE users ADD COLUMN is_admin TINYINT(1) NOT NULL DEFAULT 0;
```

---

## 5. 화면 표시

세 곳에 나오되 **성격이 다르다.**

| 위치 | 무엇을 | 왜 |
|---|---|---|
| 카메라 / 챗봇 아이콘 | **남은 게 20% 이하일 때만** 작은 배지 | 많이 남았을 때 숫자를 들이밀면 "아껴 써야 하나" 싶어 오히려 안 쓴다. 부족할 때만 알린다 |
| 시트 / 챗 패널 헤더 | `이번 주 62 / 100` 한 줄 | 기능을 쓰려고 연 시점이 알려주기 가장 좋은 때다 |
| 마이페이지 | 게이지 + 리셋 시각 + 추가 요청 버튼 | 여기가 "내 계정 상태"를 보는 자리 |

한도를 다 썼을 때의 문구는 **막다른 길로 끝내지 않는다.**

> 이번 주 한도를 다 쓰셨어요. 월요일에 다시 채워져요.
> 더 필요하시면 알려 주세요 → \[추가 요청\]

### 만들면서 걸린 것 둘

**배지 자리.** 아이콘 우상단은 이미 `.ai-fab-badge`("AI")가 쓰고 있다. 거기에
겹쳐 놓았더니 DOM 에는 있는데 화면에는 안 보였다. 사용량 배지는 **좌하단**에 둔다.

**CORS.** 비회원을 세는 `X-Device-Id` 헤더를 `allow_headers` 에 안 넣으면
브라우저가 preflight 에서 막아 요청 자체가 안 나간다. 서버에서 직접 호출하면
CORS 가 없어 멀쩡히 되므로 원인을 찾기 어렵다.

### 표시되는 값은 "더 빠듯한 쪽"

배지 숫자는 `min(주간 잔여, 일간 잔여)` 다. 사용자가 **실제로 막히는 쪽**을
보여줘야 하기 때문이다. 주간이 2 남았어도 오늘 상한을 다 썼으면 0 이 맞다.

---

## 6. 유료 전환 — 지금은 만들지 않는다

전환율이 낮을 것이 뻔한데, 결제를 붙이는 순간 PG 계약 · 환불 정책 ·
세금계산서 · 정기결제 실패 처리가 전부 딸려온다. **지금 단계에서 감당할 이유가
없다.**

대신 이렇게 시작한다.

1. DB에는 `plan` / `weekly_limit` / `daily_cap` 을 **미리** 넣어 둔다.
2. 화면에는 "더 필요하신가요?" → **요청 폼** 만 둔다 (결제 아님).
3. 어드민이 그 요청을 보고 **수동으로 한도를 올려 준다.**

구현(`usage_requests` 표):

- 마이페이지 게이지의 `더 필요해요` → 사유(선택) → 접수. **로그인한 사용자만**
  받는다 (비회원은 "가입하면 더 쓸 수 있다" 가 답이라 요청받을 이유가 없다)
- **대기 중인 요청은 사람당 하나.** 여러 번 눌러도 새로 만들지 않는다 —
  관리자가 처리할 것은 "사람" 이지 "클릭 횟수" 가 아니다. 접수된 뒤에는 버튼
  대신 "요청이 접수됐어요" 로 바뀐다
- 어드민 `요청` 탭에서 처리함/거절/되돌리기
- **한도를 올리는 것과 요청을 닫는 것은 분리했다.** "올려 줬다" 와 "요청을
  닫았다" 는 다른 일이고, 섞으면 올려주지 않고 닫는 경우를 표현할 수 없다

이러면 결제 없이도 사실상 유료 계정을 운영할 수 있고, 무엇보다 **실제 수요가
있는지를 먼저 측정**할 수 있다. 요청이 꾸준히 들어오면 그때 결제를 붙인다.

> ⚠️ 수동 조정은 사용자가 늘면 감당이 안 된다. 다만 **그 시점이 오면 이미
> 결제를 붙일 근거가 생긴 것**이므로, 지금 자동화를 미리 만드는 건 순서가 틀렸다.
> 요청이 주 5건을 넘기 시작하면 그때 다시 본다.

---

## 7. 어드민

### 별도 앱을 만들지 않는다 — 다만 떼기 쉽게 만든다

지금 사용자가 9명이다. 별도 어드민 앱은 과하다.
**기존 프론트에 `/admin` 라우트 하나 + 서버에서 `is_admin` 검사**면 충분하다.

**분리를 부르는 건 사용자 수가 아니라 관리자 수와 위험도다.** 관리자가 1명이면
사용자가 10만이어도 `/admin` 하나로 버틴다. 관리자가 여러 명이 되고, 되돌리기
어려운 작업(환불·계정 삭제)을 남이 하게 되면 그때 분리 + 감사 로그가 필요해진다.

일반적인 단계:

| 단계 | 구조 | 갈아타는 계기 |
|---|---|---|
| 1 | 같은 앱의 `/admin` + 역할 검사 | 대부분 여기서 시작해 오래 버틴다 |
| 2 | 어드민만 별도 프론트 (`admin.도메인`), 백엔드는 공용 | 관리자가 여러 명 · 어드민 코드가 사용자 번들에 섞이는 부담 · IP 제한/SSO |
| 3 | 어드민 백엔드까지 분리 + 감사 로그 + 권한 세분화 | 결제·환불처럼 위험한 작업 |
| 4 | 아예 안 만들고 Retool·Django Admin 등에 DB 연결 | 스타트업에서 흔하다 |

**지금 1단계로 두되, 2단계로 넘어갈 때 쉽도록 세 가지를 지킨다:**

1. 어드민 API 는 전부 `/api/admin/*` (`backend/admin_api.py` 한 파일)
   → 나중에 그 경로만 IP 제한 뒤로 넣거나 프론트를 분리할 수 있다
2. 어드민 화면은 **`lazy()` 로만** 불러온다 → 일반 사용자 번들에 안 실린다
   (실측: `Admin-*.js` 13KB 가 별도 청크로 떨어진다)
3. 관리자 행동은 처음부터 남긴다 — `user_quota.updated_by` + `note`

**권한 검사는 모든 엔드포인트에서 서버가 한다.** 프론트에서 메뉴를 숨기는 건
편의일 뿐 보안이 아니다. 토큰만 믿지 않고 **DB 의 `is_admin` 을 매번 확인**한다 —
권한을 뺏은 뒤에도 이미 발급된 토큰이 30일간 살아 있기 때문이다.

### 화면 셋

1. **사용자 목록** — 가입일 / 제공자 / 탈퇴일 / 재료 수 / 식구 그룹 /
   이번 주 사용량 / 플랜. 검색 · 정렬
2. **사용자 상세** — 한도 수동 조정(+메모), 플랜 변경, 최근 사용 이력
3. **대시보드** — 일별 가입·탈퇴, 사용량 합계, 쿠팡 클릭, 사전 미매칭 상위

### 지금 users 테이블 점검 결과 (2026-09-02 실측)

| 항목 | 상태 |
|---|---|
| 가입 · 제공자 | ✅ 9명 (구글 2 / 카카오 4 / 로컬 2 / 네이버 1) |
| 탈퇴 일자 `deleted_at` | ✅ 1명 |
| 식구 그룹 | ✅ `households` 3개, 6명 연결 |
| 재료 수 | ⚠️ `user_ingredients`(176행 / 5명)를 **조인해야** 나온다 |
| 탈퇴자 원래 이메일 | ⚠️ 탈퇴 시 `deleted+{id}+{email}` 로 덮어써서 원본을 못 본다 |
| 사용량 | ❌ 테이블이 없다 |
| 관리자 구분 | ❌ `is_admin` 컬럼이 없다 |

탈퇴 이메일을 덮어쓰는 건 **의도된 동작**이다(같은 이메일로 재가입할 수 있어야
해서 유니크 키를 풀어 주는 것). 어드민 목록에서는 `deleted+{id}+` 접두어를
떼어 보여주면 된다 — 원본을 따로 저장하지는 않는다.

---

## 8. 비회원 처리

지금 앱은 **비회원도 쓸 수 있다.** 계정 단위 한도를 걸 수 없으므로 클라이언트가
만든 `device_id`(localStorage UUID)로 센다.

캐시를 지우면 초기화된다 — **완벽할 수 없고, 완벽할 필요도 없다.**
비회원 한도는 남용 방지가 아니라 **가입 유인**으로 설계한다.

> 비회원 주 15 → "가입하면 100"

광고 유입 → 체험 → 가입 동선과 정확히 맞는다.

---

## 9. 진행 순서

| # | 할 일 | 상태 |
|---|---|---|
| 1 | `llm_usage` · `user_quota` 테이블, 서버에서 사용자별 차감, 토큰 실측 기록 | ✅ 2026-09-02 |
| 2 | `/api/usage` 엔드포인트 + 화면 표시 3곳 | ✅ 2026-09-02 |
| 3 | 어드민 `/admin` (목록 · 상세 · 대시보드) | ✅ 2026-09-02 |
| 4 | 추가 요청 폼 | ✅ 2026-09-02 |
| 5 | 쿠팡 링크 · 앱 배포 · 인스타 광고 | |

1번은 **배포 전 필수**다. 나머지는 순서를 바꿔도 된다.

---

## 숫자를 바꾸고 싶을 때

정책 숫자는 코드 한 곳(`backend/usage_quota.py` 의 `PLANS`)에 모아 둔다.
환경변수로도 덮을 수 있다.

| 환경변수 | 기본값 | 뜻 |
|---|---|---|
| `QUOTA_FREE_WEEKLY` | 100 | 회원 주 한도 |
| `QUOTA_FREE_DAILY` | 40 | 회원 일 상한 |
| `QUOTA_GUEST_WEEKLY` | 15 | 비회원 주 한도 |
| `QUOTA_GUEST_DAILY` | 10 | 비회원 일 상한 |
| `QUOTA_PLUS_WEEKLY` | 400 | plus 주 한도 |
| `QUOTA_PLUS_DAILY` | 100 | plus 일 상한 |
| `CREDITS_CHAT` | 1 | 챗봇 1회 |
| `CREDITS_VISION` | 2 | 사진 인식 1회 |
| `QUOTA_DAILY_BURN_DAYS` | 7 | 잔액을 며칠에 나눠 쓰게 볼 것인가 (↓) |

**일 상한은 하한이다 (2026-09-05).** 표의 일 상한만 걸면 잔액과 따로 논다 —
크레딧을 300개 준 계정이 하루 15개에 막혀 다 쓰는 데 20일이 걸렸다(관리자 계정에서
실제로 겪었다). 준 걸 못 쓰게 하는 건 준 게 아니다. 그래서 실제 상한은

    max(플랜 기본값, 어드민이 넣은 숫자, ⌈잔액 ÷ QUOTA_DAILY_BURN_DAYS⌉)

7일인 이유는 크레딧이 **매주 월요일에 채워지기 때문**이다. 리셋 주기와 같은
길이로 두면 "한 주 안에는 가진 걸 다 쓸 수 있다" 가 된다. `max` 라서 잔액이
적은 사람의 상한을 깎지는 않는다 — 상한은 몰아쓰기 방지용이지 벌금이 아니다.
어드민이 `user_quota.daily_cap` 에 넣은 숫자는 **올리는 데만** 쓴다. 내려서 묶는
용도로 두었더니, 어드민 화면이 그 칸에 기본값을 미리 채워 두는 바람에 크레딧을
지급하면서 저장한 계정에 `15` 가 그대로 박혀 이 규칙이 무력해졌다 — 그리고 그
계정이 바로 크레딧을 많이 받은 계정이다.

⚠️ 숫자를 바꾸기 전에 **`llm_usage` 를 먼저 본다.** 감으로 바꾸지 말 것.

```sql
-- 이번 주 사용자별 사용량
SELECT user_id, device_id, SUM(credits) c, COUNT(*) n
FROM llm_usage
WHERE created_at >= (CURDATE() - INTERVAL WEEKDAY(CURDATE()) DAY)
GROUP BY user_id, device_id ORDER BY c DESC;

-- 일별 총량 (수용량 여유 확인)
SELECT DATE(created_at) d, kind, SUM(credits) c, COUNT(*) n
FROM llm_usage GROUP BY d, kind ORDER BY d DESC LIMIT 30;
```
