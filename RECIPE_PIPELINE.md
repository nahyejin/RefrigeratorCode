# 레시피가 앱에 나오기까지 — 파이프라인과 자동 배치

레시피 한 건이 **크롤링 → 재료 추출 → 공개**까지 어떤 길을 지나는지, 그리고
그 길에서 실제로 났던 사고와 그 대책을 적는다.

작성: 2026-09-06

---

## 1. 전체 흐름

```
 매일 22:00   CookMatch-WeeklyCrawler        (run_crawlers_scheduled.bat)
              네이버 블로그 · 유튜브 크롤링
                     │  제목 · 본문 · 썸네일 1장 저장
                     │  규칙 기반으로 재료 한 번 훑음  ← 임시값
                     │  재료 3개 이하는 저장 자체를 안 함
                     ▼
              DB(recipes) 에 들어감 — 이 시점에는 아직 앱에 안 보인다
                     │
                     │  (같은 실행 끝에 신규분 LLM 450건까지 바로 처리)
                     ▼
 매일 05:00   CookMatch-DailyLLMIngredients  (run_llm_ingredients_daily.bat)
              본문을 AI 에게 읽혀 다시 뽑음 — 하루 5,280건, **최신 글부터**
                     │  재료 · 조리 순서(cook_steps) · 요리명(recipe_name)
                     │  요리 글이 아니면 그 자리에서 삭제
                     │  llm_ingredients_at = NOW()  ← 공개 표식
                     ▼
              앱에 나타난다 (목록 · 검색 · 요즘인기 · AI 식단 후보)

 매일 06:30   CookMatch-DictionarySync       (apply_dictionary_additions_daily.bat)
              어드민에서 승인한 사전을 CSV 로 옮기고 커밋 · 푸시
              + 대체 재료 표(52,003쌍) 재생성
```

세 작업 모두 **개발 컴퓨터의 윈도우 작업 스케줄러**가 돌린다. 서버가 아니라
그 컴퓨터가 켜져 있어야 돈다. 작업 이름이 `Weekly` 지만 **실제로는 매일** 돈다.

---

## 2. `used_ingredients` 를 쓰는 두 주체

한 칸을 둘이 같이 쓴다. **여기가 이 파이프라인의 가장 위험한 지점이다.**

| | 누가 | 언제 | 방식 |
|---|---|---|---|
| 규칙 기반 | `ingredient_management/update_used_ingredients_batch.py` | 크롤링 직후 + 매일 크롤러 끝에 | 사전에 있는 이름을 본문에서 **글자로 찾는다** |
| AI | `ingredient_management/llm_ingredient_extraction.py` | 매일 05:00 | 본문을 읽혀 **재료 목록을 받는다** |

### 규칙 기반이 왜 임시값인가

글자로 찾으므로 뜻을 모른다. 실제로 나왔던 것들:

- **`가지`** 는 채소이면서 "종류"를 세는 말이다 → `"여러 가지 방법"`,
  `"밑반찬 10가지"`, `"가지고 있는"` 이 전부 가지로 잡혔다.
  44,205건 중 4,309건, 그 중 3,116건은 제목에 가지가 아예 없었다.
  `믹스커피말차라떼` 의 재료가 `얼음,우유,원두,가지,말차` 였다.
- **짧은 이름이 긴 이름에 덤으로 붙었다.** 사전에 긴 쪽도 짧은 쪽도 다 있는데
  `if 이름 in 줄` 로 각각 넣다 보니 `고추장` 한 줄이 `고추장` 과 `고추` 를
  둘 다 만들었다. 같은 본문을 AI 가 읽었을 때와 견주면:

  | 짧은 쪽 | 어디서 | 규칙 기반 | AI |
  |---|---|---|---|
  | 고추 | 고추장·고춧가루·청양고추 | 26.4% | 2.6% |
  | 참치 | 참치액·참치액젓 | 11.9% | 1.5% |
  | 버섯 | 표고버섯·새송이버섯 | 8.4% | 0.6% |
  | 올리브 | 올리브유 | 7.6% | 0.7% |
  | 매실 | 매실청 | 5.5% | 0.3% |
  | 다리 | 닭다리·앞다리살·코다리 | 4.5% | 0.03% |

  가장 심한 건 **식용유**였다. 사전에서 `기름`·`오일` 이 식용유의 동의어라
  **참기름·들기름·올리브오일이 전부 식용유를 하나씩 더 만들었다** — 표본
  2,000건 중 437건.

**고침(2026-09-06).** 매칭에서 **긴 이름이 이긴다** — 자리(위치)까지 보고 더
긴 이름에 완전히 덮이는 것은 버린다. 한 줄에 `고추장 1T, 고추 2개` 처럼
진짜로 둘 다 있으면 자리가 달라 둘 다 남는다. `가지` 는 수량 표현 뒤와
`가지고`·`가지런히` 자리만 떨어뜨린다(`"간단한 가지무침"` 의 `한` 처럼 낱말
끝 글자가 우연히 수량사와 같은 경우를 살리려고, 수량사는 그 앞이 한글이 아닐
때만 인정한다).

검증: 같은 블록에 옛/새 매칭만 갈아 끼워 최신 2,000건 — 재료 평균
**9.49 → 7.92개**, **새로 생긴 재료 0건**(순수하게 덜어내기만 한다).

그래도 임시값인 것은 변함없다. 그래서 아래 4절의 게이트를 둔다.

---

## 3. 사고 기록 — 9일치 AI 결과가 통째로 덮인 일 (2026-09-05)

### 무슨 일이 있었나

8/26~9/3 에 AI 가 누적 44,868건의 재료를 한 바퀴 다시 뽑아 **사이클을 끝냈다**
(9/3 에 남은 것이 563건뿐이었다). 그런데 9/6 에 DB 를 보니 78% 가 규칙 기반
값이었다. 9/3 배치의 미리보기 CSV 에는 제대로 뽑힌 값이 남아 있는데 DB 에는
옛 값이 있었다:

```
id=170349  "I Put CHEESE CAKE Inside Japanese Monaka"
  CSV(9/3 AI): 달걀,말차가루,생크림,설탕,소금,옥수수전분,크림치즈,팥앙금,화이트초콜릿
  DB:          가지,검정깨,말차,치즈            ← 규칙 기반 값
```

### 원인 — 셋이 겹쳤다

1. **`used_ingredients` 한 칸을 둘이 공유한다.** 나중에 쓴 쪽이 이기고, 어느
   쪽이 쓴 값인지 구분할 방법이 없었다.
2. **안전장치가 잘못된 플래그를 봤다.** 규칙 기반 배치에는 방어 코드가 있었다:
   ```sql
   -- ⚠️ 중요: LLM이 이미 처리한 행은 건드리지 않는다
   WHERE COALESCE(llm_ingredients_done, 0) = 0
   ```
   의도는 맞았는데 `llm_ingredients_done` 은 **뜻이 두 개**다 —
   "AI 가 값을 넣어 뒀다"(규칙 기반이 읽은 뜻)와 "다시 처리할 필요가
   없다"(재예약이 쓴 뜻). `requeue_recipes_for_llm.py --write` 가 두 번째
   뜻으로 44,868건을 0 으로 돌리자, 규칙 기반은 첫 번째 뜻으로 읽어
   "아무도 손 안 댔네" 라고 판단했다.
3. **"신규분 임시 채움" 이라면서 실제로는 전량 재계산이다.**
   `run_all_crawlers.py` 가 부르는 배치에는 `--limit` 이 없다. 조건만 풀리면
   즉시 카탈로그 전체를 훑는다.

### 방아쇠와 타임라인

```
8/26~9/3   AI 사이클 44,868건 완주 (9/3 03:04, 남은 것이 563건뿐)
9/3 07:08  "레시피 아님" 판정 추가 → 전량 재판정 예약 (done 전부 0)
9/3 13:09  크롤러 뒤 규칙 기반 배치 시작 → 대상이 전량이 됨. 완료 로그 없음
9/4 13:06  또 시작, 또 완료 로그 없음
9/5 13:50 ─ 18:13  ★ 4시간 22분 만에 완주 → 카탈로그 전체를 덮어씀
9/5 03:00  그날 아침 AI 가 처리한 id ~41,927 은 done=1 이라 살아남음
9/6 03:00  AI 가 41,927~88,062 를 다시 처리 (그래서 이 구간도 깨끗)
```

**증거 두 가지**

1. **소요 시간.** 같은 배치가 9/2 에는 12:48~12:53 **5분**, 9/5 에는
   13:50~18:13 **4시간 22분**. 평소엔 처리할 게 거의 없다가 갑자기 전량이
   됐다는 뜻이다.
2. **데이터가 정확히 그 경계로 갈린다.**

   | id 구간 | 상태 | 가지 | 고추 | 식용유 |
   |---|---|---|---|---|
   | ≤88,062 | 9/5·9/6 AI 재처리 완료 | 2.8% | 2.8% | 21.6% |
   | 88,063~170,349 | **덮어써짐** | 11.7% | 26.5% | 37.3% |

### 대책 — 재예약이 못 건드리는 표식

`recipes.llm_ingredients_at DATETIME NULL` 을 새로 뒀다. **AI 가 실제로 값을
쓴 시각**이고, 재처리 예약은 이 값을 건드리지 않는다.

```sql
-- 전 (재예약 한 번이면 전량이 대상이 된다)
WHERE COALESCE(llm_ingredients_done, 0) = 0

-- 후 (AI 가 값을 쓴 적 없는 행만)
WHERE llm_ingredients_at IS NULL
```

`llm_ingredients_done` 은 그대로 둔다 — 그건 "다시 볼 차례인가" 를 세는
값이고, 재판정 예약은 계속 그 값을 쓴다. **두 뜻을 두 칸으로 나눈 것**이
이 대책의 전부다.

### 복구

미리보기 CSV(`ingredient_management/llm_used_ingredients_preview_*.csv`) 에
**44,367건의 AI 원본**이 남아 있었다. `renormalize_used_ingredients.py` 는
그 원본을 사전으로 다시 정규화하는 결정론적 단계라, **AI 호출 0회**로 되돌릴
수 있다.

```bash
python -u ingredient_management/renormalize_used_ingredients.py --restore --commit
python scripts/mark_llm_restored.py --write
```

- `--restore` : `llm_ingredients_at IS NULL` 인 행만 대상(표식이 있는 행은
  더 나중 프롬프트로 뽑은 값이라 8월 원본으로 되돌리면 오히려 후퇴한다)
- `mark_llm_restored.py` : 값이 우연히 같아 UPDATE 를 건너뛴 행에도 표식만
  채운다. 표식이 없으면 앱이 숨기고 규칙 기반이 다시 덮는다

**결과 (2026-09-06, 약 40분)**

| 재료 | 복구 전 | 복구 후 |
|---|---|---|
| 고추 | 9,067 | 1,136 |
| 참치 | 4,097 | 842 |
| 가지 | 4,309 | 1,367 |
| 버섯 | 2,880 | 578 |
| 올리브 | 2,590 | 334 |
| 매실 | 1,874 | 167 |
| 다리 | 1,548 | 70 |

제목에 `가지` 가 없는데 재료에 가지가 든 것 **3,116건 → 495건.**
되돌린 것 32,927건 + 표식만 채운 것 509건.

곁들여 고친 것: `load_raw_by_id()` 가 파일명으로 정렬해서
`_5400_20260826` 이 `_5280_20260906` 보다 뒤에 왔다 — **옛 원본이 새 원본을
덮고** 있었다. 만든 시각(mtime) 기준으로 바꿨다.

---

## 4. 다 채워지기 전에는 앱에 안 띄운다

크롤러는 AI 를 기다리지 않고 규칙 기반 임시 재료를 붙여 먼저 넣는다. 그 값이
그대로 보이니 사람이 카드에서 **본문에 없는 재료**를 봤고, 매칭률도 그만큼
틀렸다.

조회에 `llm_ingredients_at IS NOT NULL` 을 건다:

| 거는 곳 | 왜 |
|---|---|
| `/api/recipes/filter` (냉장고 요리) | 매칭률이 재료로 계산된다 |
| `/api/recipes/popular` (요즘인기) | 최근 글만 보여 주는 화면이라 특히 크게 걸린다 — 어제 크롤링된 글이 곧바로 1위에 오르면 틀린 재료가 가장 잘 보이는 자리에 놓인다 |
| `/api/recipes` (목록) · `/api/recipes/search` | 목록과 개수가 어긋나면 "총 N건인데 N개가 안 보인다" 가 된다 |
| AI 식단 후보 | 재료를 그대로 믿고 일주일 장바구니를 짠다 |

**빼는 곳**: 저장한 레시피 · 완료한 요리 · 즐겨찾기 · 상세 페이지.
이미 본 것이 사라지면 안 된다.

적용 직후 실측: `total 44,258 → 43,326` (932건, 2.1% 가 배치 대기).

---

## 5. 처리 순서 — 최신 글부터

AI 배치의 기본 순서는 `ORDER BY id ASC`(오래된 것부터)였다. 앱은 최신순으로
보여 주는데 배치는 반대쪽 끝부터 갉아먹고 있었다:

```
처리된 것:  id 1,285 ~ 88,045
가장 최신:  id 170,704
→ 최신 500건 중 조리 순서(cook_steps)가 있는 것: 0건
```

`run_llm_ingredients_daily.bat` 에 `--order recent` 를 붙였다. 밀린 것을 다
끝내는 데 걸리는 시간은 같고(하루 5,280건), **보이는 쪽부터** 맞춰진다.
다 따라잡은 뒤에는 매일 새로 크롤링된 것이 다음 날 바로 처리된다.

---

## 6. 어디를 봐야 하나

어드민 → **운영** 탭에 `레시피가 앱에 나오기까지` 카드가 있다.

| 숫자 | 뜻 | 이상 신호 |
|---|---|---|
| 앱에 보이는 것 | `llm_ingredients_at` 이 있는 글 | — |
| 아직 숨는 것 | 재료가 임시값이라 뺀 글 | **며칠째 늘기만 하면** 05:00 배치를 보라 |
| AI 가 다시 볼 차례 | `llm_ingredients_done = 0` | 하루 5,280건씩 줄어야 한다 |
| 조리 순서 있는 것 | `cook_steps` 가 찬 글 | 재판정이 도는 동안 늘어난다 |

로그:

| 파일 | 무엇 |
|---|---|
| `llm_ingredients.log` | 05:00 AI 배치 — 처리/변경/삭제 건수, 도달한 id |
| `scheduled_run.log` | 크롤러 + 규칙 기반 배치 |
| `dictionary_sync.log` | 06:30 사전 · 대체표 · 프리미엄 목록 |

**규칙 기반 배치가 5분을 크게 넘기면 의심하라.** 그건 대상이 신규분이 아니라
전량이 됐다는 뜻이다(3절의 사고가 정확히 그렇게 드러났다).

---

## 7. 손으로 돌릴 때

```bash
# 규칙 기반 재계산 (AI 가 손대지 않은 행만)
python -u ingredient_management/update_used_ingredients_batch.py --dry-run
python -u ingredient_management/update_used_ingredients_batch.py

# AI 재추출 — 특정 id 만
python -u ingredient_management/llm_ingredient_extraction.py --ids 170349 --commit

# 전량 재판정 예약 (프롬프트를 고쳤을 때)
python scripts/requeue_recipes_for_llm.py            # 미리보기
python scripts/requeue_recipes_for_llm.py --write

# 사전을 보강한 뒤 기존 결과에 소급 (AI 호출 0회)
python -u ingredient_management/renormalize_used_ingredients.py
python -u ingredient_management/renormalize_used_ingredients.py --commit

# 사고 복구 — 규칙 기반에 덮인 행을 AI 원본으로 되돌리기
python -u ingredient_management/renormalize_used_ingredients.py --restore --commit
python scripts/mark_llm_restored.py --write

# 어드민 운영 탭이 읽는 상태 기록
python scripts/report_ops_status.py --write
```

> **미리보기 CSV 를 지우지 말 것.**
> `ingredient_management/llm_used_ingredients_preview_*.csv` 에 AI 원본이
> 남아 있다. 이 사고를 AI 호출 0회로 되돌릴 수 있었던 이유가 그것뿐이다.

---

## 8. 지켜야 할 것

- **본문 이미지는 1개 url 만 저장한다.**
- **재료 3개 이하는 크롤링 단계에서 탈락시킨다.**
- **상위어 통합과 동의어 처리를 없애지 않는다.**
- **주류 브랜드(진로·참이슬·카스·테라)는 사전에 넣지 않는다.**
- **사전 CSV 커밋에 `git add -A` 를 쓰지 않는다** — `scratch_backups/` 에
  비밀번호 해시가 있다. 배치는 사전 CSV 세 개만 지정해서 커밋한다.
