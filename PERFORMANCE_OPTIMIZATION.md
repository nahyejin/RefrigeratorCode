# 성능 최적화 가이드

> **적용 상태 (2026-08-26 실측)**
> - ✅ FULLTEXT 인덱스 `ft_title_content` (title, content) — **이미 적용됨**
> - ✅ 백엔드 코드 최적화 — 적용됨
> - ⬜ 캐싱 / 동의어 최적화 — 미적용
>
> 아래 "적용 방법"은 신규 환경 구축 시 참고용입니다.
> 운영 DB 에는 이미 인덱스가 있으므로 다시 만들 필요가 없습니다.

## 문제점
필터 조건을 걸면 데이터 로드 시간이 너무 오래 걸리는 문제가 있었습니다.

## 원인
1. **LIKE '%keyword%' 패턴**: 인덱스를 사용할 수 없어 전체 테이블 스캔 발생
2. **동의어 증가**: 동의어가 많아질수록 OR 조건이 많아져 쿼리 복잡도 증가
3. **COUNT 쿼리 비효율**: match_rate 필터가 없어도 서브쿼리 사용

## 해결 방법

### 1. FULLTEXT 인덱스 추가 (필수)
Railway MySQL에서 다음 SQL을 실행하세요:

```sql
-- FULLTEXT 인덱스 추가
ALTER TABLE recipes 
ADD FULLTEXT INDEX ft_title_content (title, content);

-- 인덱스 확인
SHOW INDEX FROM recipes WHERE Key_name = 'ft_title_content';
```

**성능 향상**: 10배 이상 빠름

### 2. 백엔드 코드 최적화 (완료)
- FULLTEXT 인덱스를 사용하도록 코드 수정
- 인덱스가 없으면 자동으로 LIKE로 폴백
- COUNT 쿼리 최적화 (match_rate 필터가 없으면 서브쿼리 제거)

### 3. 추가 최적화 옵션

#### A. 동의어 최적화
- 실제로 많이 사용되는 동의어만 포함
- 사용 빈도가 낮은 동의어 제거

#### B. 캐싱
- 자주 검색되는 키워드 결과를 Redis나 메모리에 캐싱
- TTL 설정 (예: 1시간)

#### C. 페이지네이션 최적화
- 첫 페이지만 빠르게 로드
- 나머지 페이지는 필요할 때 로드

## 적용 방법

1. **FULLTEXT 인덱스 추가** (가장 중요!)
   ```bash
   # Railway MySQL에 접속하여 optimize_recipes_fulltext.sql 실행
   ```

2. **백엔드 재시작**
   ```bash
   # Railway에서 서비스 재시작
   ```

3. **테스트**
   - 필터 적용 시 로드 시간 확인
   - 백엔드 로그에서 "[필터링] FULLTEXT 검색 사용" 메시지 확인

## 예상 성능 개선

- **인덱스 없음 (LIKE)**: 25,000건 스캔 → 약 2-5초
- **FULLTEXT 인덱스**: 인덱스 사용 → 약 0.2-0.5초
- **개선율**: 약 **10배 이상 빠름**

## 주의사항

- FULLTEXT 인덱스는 InnoDB에서만 지원 (MyISAM도 지원하지만 InnoDB 권장)
- 인덱스 생성 시간: 데이터 양에 따라 다름 (25,000건 기준 약 1-2분)
- 인덱스 크기: 약 10-20MB 추가 공간 필요

