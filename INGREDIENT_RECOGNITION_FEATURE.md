# 사진으로 재료 담기 (영수증 인식)

내 냉장고에서 영수증이나 재료 사진을 올리면 재료를 읽어 담아 주는 기능입니다.
최신 변경(2026-08-31) 기준으로 정리했습니다 — 이후 프롬프트나 흐름을 고치면
이 문서도 같이 고쳐 주세요.

| 구성 | 파일 |
|---|---|
| 인식 모듈 | `backend/ingredient_vision.py` |
| 엔드포인트 | `backend/app.py` 의 `POST /api/ingredients/recognize` |
| 촬영/선택 시트 | `frontend/src/components/CameraCaptureSheet.tsx` |
| 검토/수정 시트 | `frontend/src/components/IngredientRecognitionSheet.tsx` |
| 화면 연결 | `frontend/src/pages/MyFridge.tsx` |
| 업로드 전 축소 | `frontend/src/utils/imageUtils.ts` 의 `shrinkImageForUpload()` |

---

## 지금 열려 있는 것 / 아직 닫아 둔 것

| 입구 | 상태 | 보내는 mode |
|---|---|---|
| 카메라 → 영수증 | **동작** | `receipt` |
| 앨범/파일에서 선택 | **동작** (여러 장) | `file` |
| 카메라 → 음식 1개 | 준비 중 안내만 | — |
| 카메라 → 음식 여러 개 | 준비 중 안내만 | — |

음식 사진 프롬프트(`_FOOD_PROMPT`)는 서버에 이미 있습니다. 인식 품질을 아직
확인하지 않아 화면에서만 막아 둔 상태이고, `MyFridge.tsx` 의
`mode === 'food-single' || mode === 'food-multi'` 분기만 풀면 열립니다.

> 앨범 경로(`file`)는 무엇을 골랐는지 알 수 없으므로 **모델이 영수증인지 음식
> 사진인지 스스로 판단**합니다(`_AUTO_PROMPT`). 그래서 앨범으로 음식 사진을
> 고르면 실제로는 인식됩니다 — 자동 판별의 자연스러운 결과입니다.

---

## 한 번의 요청이 하는 일

1. 프론트가 사진을 **긴 변 1600px JPEG** 으로 줄여서 `multipart/form-data` 로
   보낸다 (`image` 여러 개 + `mode`).
2. 서버가 장수·용량·형식을 검사하고, 챗봇과 **같은 하루 한도**를 1 소모한다.
3. Gemini 멀티모달을 **1회** 호출한다. 여러 장이어도 한 요청의 `parts` 에
   이미지를 여러 개 넣으므로 호출은 한 번이다.
4. 모델이 돌려준 이름을 **재료 사전으로 정규화**한다.
5. 프론트가 결과를 보여주고 사용자가 검토·수정한 뒤 담는다.

### 응답 형태

```json
{
  "purchase_date": "2026-08-29",
  "ingredients": [
    { "name": "게맛살", "raw": "크래미", "expiry": null },
    { "name": "우유",   "raw": "우유",   "expiry": "2026-09-12" }
  ],
  "unmatched": [
    { "raw": "다시다 감칠맛", "expiry": null }
  ]
}
```

- `name` 은 **재료 사전의 대표어**다. 이 이름 그대로 냉장고에 담기고 레시피
  매칭에 쓰인다.
- `raw` 는 사진에서 읽힌 표기. 화면에서 "사진에서 읽은 표기: 크래미" 처럼
  보여 준다 — 사용자가 "내가 산 게 아닌 이름이 왜 뜨지?" 하고 의심할 여지를
  없애기 위해서다.
- `unmatched` 는 읽히긴 했지만 사전에 없어 그대로는 담을 수 없는 것. 버리지
  않고 돌려줘서, 사용자가 화면에서 맞는 재료를 직접 지정할 수 있게 한다.

---

## 사전에 없던 이름을 모아 둔다 (`ingredient_dictionary_misses`)

사진 인식 품질은 모델보다 **사전이 얼마나 두꺼운지**에 더 크게 좌우된다.
화면에서 회색으로 뜨는 "사전에 없어요" 항목이 곧 사전의 구멍인데, 그냥
흘려보내면 무엇이 부족한지 다시 알 방법이 없다. 그래서 `unmatched` 로 나간
이름을 서버가 표 하나에 쌓아 둔다 (`backend/app.py` 의
`_record_dictionary_misses`).

| 열 | 뜻 |
|---|---|
| `raw_name` | 사진에서 읽힌 표기 (유니크 키) |
| `hit_count` | 몇 번이나 걸렸는지 — 많을수록 사전에 보탤 값어치가 크다 |
| `last_mode` | 어느 모드에서 (`receipt` / `file` / `food-*`) |
| `first_seen` / `last_seen` | 처음·마지막으로 걸린 시각 |

**개인 정보는 담지 않는다** — 누가 찍었는지(user_id)는 저장하지 않고 이름만
모은다. 기록에 실패해도 인식 자체는 성공시킨다(예외를 삼킨다).

꺼내 보는 법:

```bash
python scripts/show_dictionary_misses.py            # 지금도 안 잡히는 것만
python scripts/show_dictionary_misses.py --all      # 그 사이 해결된 것도 함께
python scripts/show_dictionary_misses.py --csv misses.csv
```

이 스크립트는 목록을 내면서 **지금 사전으로 다시 조회해 본 결과**를 함께
보여 준다. 기록된 뒤에 사전을 보강했다면 이제는 잡히는 이름이 있는데, 그건
이미 해결된 것이므로 구분해 줘야 헛수고를 안 한다.

여기서 자주 걸리는 이름을 골라 사전 CSV 에 동의어로 넣고
`scripts/sync_ingredient_dict.py --write` 로 백엔드 사본까지 맞추면 된다.

⚠️ **여기 올라온 걸 다 넣는 게 아니다.** 안 넣기로 한 것들:

| 걸린 이름 | 왜 안 넣나 |
|---|---|
| `된장찌개` `비빔냉면` `간장계란밥` | **요리 이름**. 재료가 아니라 사전에서 일부러 뺀 것(대분류 `요리이름`). 넣으면 냉장고에 "된장찌개" 가 재료로 담긴다 |
| `진로` `참이슬` `카스` `테라` | **주류 브랜드**. 소주/맥주로 흡수하자고 제안했으나 넣지 않기로 결정(2026-09-01). 다시 제안하지 말 것 |

영수증에는 재료가 아닌 게 잔뜩 찍힌다. 이 표는 "안 잡힌 것 = 사전의 구멍" 이
아니라 **후보 목록**일 뿐이라는 뜻이다.

---

## 왜 사전 정규화를 서버에서 하나

레시피의 `used_ingredients` 도 **같은 사전**
(`frontend/public/ingredient_profile_dict_with_substitutes.csv`)의 대표어로
저장된다. 여기서 다른 기준을 쓰면 **인식해서 담아 준 재료가 정작 레시피와
매칭되지 않는다.** 그래서 `backend/ingredient_dictionary.py` 의
`get_alias_to_canonical()` / `resolve_canonical()` 을 그대로 쓴다 — 레시피 재료
배치도 같은 모듈을 쓴다.

LLM 은 "이미지에서 이름을 읽는" 것까지만 하고, 사전 매칭은 결정론적으로
처리한다 — 모델이 사전에 없는 재료를 지어내 담는 걸 막기 위해서다.

사전은 프로세스 수명 동안 **한 번만** 읽는다(2,900행 CSV 파싱이 매 요청마다
돌면 느리다). 사전을 고쳤으면 서버를 다시 띄워야 반영된다.

### ⚠️ 사전 CSV 사본이 두 개다 (배포 제약)

원본은 `frontend/public/` 에 있다. 프론트가 브라우저에서 직접 받아 쓰기 때문이다.
그런데 **백엔드 배포에는 `frontend/` 가 올라가지 않는다.** 실측으로 확인했다 —
배포에서 `재료 사전을 불러오지 못했어요`(503)가 났다. (앞서 겪은 import 실패도
같은 뿌리였다: `ingredient_management/` 도 배포에 없었다.)

그래서 `backend/ingredient_profile_dict_with_substitutes.csv` 에 사본을 하나 더 둔다.
`ingredient_dictionary` 는 이 순서로 찾는다:

```
INGREDIENT_DICT_CSV(환경변수) → frontend/public/ → backend/ → 저장소 루트
```

**사전을 고쳤으면 반드시 사본도 맞춰야 한다:**

```bash
python scripts/sync_ingredient_dict.py --write
```

안 맞추면 화면(프론트)과 서버(사진 인식)가 서로 다른 사전을 쓰게 된다. 조용히
어긋나면 찾기가 매우 어려워서, 서버가 뜰 때 두 파일이 다르면 로그에 경고를 찍는다.

> **주의**: 사전 정규화는 `backend/ingredient_dictionary.py` 한 곳에만 있다.
> 표준 라이브러리(csv)만 쓰고 다른 모듈에 의존하지 않는다.
>
> 원래는 `ingredient_management/llm_ingredient_extraction.py` 의 것을 가져다
> 썼는데, 그건 배치 스크립트라 pandas 와 다른 배치 모듈(`backend.backend.*` 까지)
> 을 함께 끌고 온다. 배포 환경에서 그 사슬이 끊기면서 서버가 500/503 을 냈고,
> 로그로 원인을 좁히기도 어려웠다. 그래서 **의존이 가장 적은 곳으로 내렸다.**
> 배치 쪽(`llm_ingredient_extraction`)이 이제 이 모듈을 가져다 쓴다.
>
> **웹 서버가 `ingredient_management` 를 import 하게 만들지 마세요.**

---

## 날짜 인식

재료마다 구매일자를 적게 돼 있어서, 사진에서 읽을 수 있는 건 읽는다.

| 우선순위 | 상황 | 처리 |
|---|---|---|
| 1 | 영수증 | 거래일시를 `purchase_date` 로 |
| 2 | 포장지 사진 | 찍혀 있는 유통기한/소비기한을 그 재료의 `expiry` 로 |
| 3 | 둘 다 없음 | **업로드 시점(오늘)** 을 구매일자로 |

읽은 날짜는 그대로 믿지 않는다.

- `_clean_purchase_date()`: 미래이거나 1년보다 오래되면 버린다(오독으로 봄).
- `_clean_expiry(value, purchase_date)`: 과거이거나 10년 뒤면 버린다.
  그리고 **구매일자와 같거나 그보다 이르면 버린다.**
- `YYYY-MM-DD` 형식이 아니면 버린다.

> ⚠️ **"구매일자와 같으면 버린다" 는 실제 사고에서 나온 규칙이다.**
> 영수증에는 유통기한이 없는데, 모델이 영수증에 큼직하게 찍힌 **거래일시를 각
> 재료의 유통기한으로** 적어 보내는 경우가 있다. 그 날짜는 대개 오늘이라
> "과거는 버린다" 검사를 그냥 통과했고, 그대로 담기면서 방금 넣은 재료가
> 곧바로 `D-day` / `지남` 으로 빨갛게 떴다.
>
> 그래서 `recognize()` 는 구매일자를 **먼저** 정리하고 그 값을 기준으로 유통기한을
> 거른다. 프론트(`IngredientRecognitionSheet.usableExpiry`)에도 같은 검사가 있다.
> 버려도 손해가 없다 — 구매일 + 재료 특성으로 짐작한 값으로 돌아갈 뿐이다.
>
> 이 검사는 **사진에서 읽은 값에만** 건다. 사용자가 직접 고른 날짜는
> (마감 임박 식품처럼 오늘이 기한인 경우가 있으므로) 그대로 존중한다.

포장지에서 **실제로 읽은** 유통기한은 `expiry` 로 넣는다. 구매일 + 카테고리로
짐작하는 `estimatedExpiry` 보다 정확하므로 짐작값 대신 쓴다.

### 응답 파서

`_parse_response()` 는 모델이 **옛 형식(이름만 담긴 배열)** 으로 답해도
받아들인다. 프롬프트를 객체 형식으로 바꿔도 가끔 배열로 돌아오는 경우가 있어,
그때 통째로 실패하지 않게 하기 위해서다. 코드펜스(```json)도 벗겨 낸다.

---

## 검토·수정 UI (`IngredientRecognitionSheet`)

**인식 결과를 바로 담지 않는다.** OCR 은 반드시 틀린다 — 영수증의 할인 항목을
재료로 읽거나, 흐릿한 글자를 엉뚱한 재료로 읽는다. 사용자가 모르는 사이에 없는
재료가 들어가면 추천 레시피 품질이 통째로 어긋난다.

- 항목마다 `재료명 고치기` → 내 냉장고의 재료 추가 입력과 **같은 방식의 검색
  드롭다운**이 열린다. 사전(동의어 포함)에서 찾아 대표어만 후보로 보여주고,
  고르면 확정된다. **직접 입력은 받지 않는다** — 사전에 없는 말을 담으면 레시피
  매칭에서 빠지기 때문이다.
  (문구가 그냥 `고치기` 였을 때 "무엇을 고치는지 모르겠다" 는 지적을 받았다.
  같은 줄에 보관함 셀렉트도 있어서 더 헷갈렸다.)
- `unmatched` 항목은 회색으로 표시하고 `재료명 고르기` 로 사용자가 맞는 재료를
  지정할 수 있다. 지정하기 전까지는 체크가 꺼져 있어 담기지 않는다.
- 한 행은 **반드시 한 줄**이다: `[✓] 재료명 … [유통기한] [보관함] [재료명 고치기]`.
  잠깐 두 줄(이름 / 조작 버튼)로 나눠 봤는데 항목이 10개 넘는 영수증에서 목록이
  두 배로 길어져 훑어보기가 나빠졌다. 대신 **이름 칸만** 줄어들게 두고
  (`flex:1; min-width:0` + 말줄임) 조작 버튼들은 `flexShrink:0` 으로 고정한다.
  실측 이름 칸 폭: 430px 폰 149px / 375px 폰 94px / 351px 폰 70px.
  → 여기에 칸을 하나 더 붙이려면 이름이 몇 글자밖에 안 남는다는 뜻이다.

#### 구매일자와 유통기한

**규칙은 하나다 — 사용자가 넣은 유통기한이 언제나 이긴다.**
구매일자는 기한을 모를 때 짐작하기 위한 근거일 뿐이다.

| 그 행의 상태 | 냉장고에 담길 때 |
|---|---|
| 유통기한이 있음 (포장지에서 읽었거나 사용자가 넣음) | `expiry` 로 그대로 저장. 짐작하지 않는다 |
| 유통기한이 없음 | 구매일 + 재료 카테고리로 `estimatedExpiry` 를 짐작 (`약 D-21` 처럼 "약" 을 붙여 표시) |

- **유통기한**: 행마다. 기존 배지 자리를 그대로 버튼으로 쓴다 — 값이 있으면
  `~09.12`(노랑), 없으면 `기한 +`(점선). 새 칸을 만들지 않아 한 줄이 유지된다.
  모달에서 "잘 모르겠어요" 를 고르면 지워지고 짐작값으로 돌아간다.
- **구매일자**: 모드에 따라 위치가 다르다.

| 모드 | 어디서 | 왜 |
|---|---|---|
| 영수증 / 앨범(자동판별) | 목록 **위에서 한 번**, 전체에 일괄 적용 | 영수증 한 장은 한 번에 산 목록이다 |
| 음식 1개 / 음식 여러 개 | **행마다** (`09.01` 칩) | 재료마다 산 날이 다를 수 있다. 한 값으로 묶으면 짐작이 통째로 어긋난다 |

영수증에서 읽었는지 오늘 날짜인지는 옆에 문구로 밝히고, 구매일자가 무엇에
쓰이는지도 한 줄로 적어 둔다(둘 다 날짜라 "어느 쪽이 이기지?" 하고 헷갈린다).

> ⚠️ 날짜 모달(`IngredientDateModal`)은 반드시 `nested` 로 띄운다.
> 기본 z-index 가 시트와 같은 `--z-modal`(600) 인데, 바텀시트는 Portal 로 body
> 직속에 붙어 **더 나중에 그려진다.** 같은 층위면 나중 것이 위로 올라가므로
> 날짜 버튼을 눌러도 아무 반응이 없는 것처럼 보이고, 시트를 닫는 순간에야
> 달력이 나타난다. 실제로 그렇게 보고됐다.
- 포장지에서 읽은 유통기한은 항목 옆에 배지(`~09.12`)로 보여 준다.
- 고칠 게 없으면 `N개 반영하기` 한 번으로 끝난다.
- 이미 냉장고에 있는 재료는 담을 때 건너뛴다.

---

## 업로드 제한

| 항목 | 값 | 넘으면 |
|---|---|---|
| 장수 | 5장 | 413 |
| 장당 용량 | 8MB | 413 |
| 합계 용량 | 16MB | 413 |
| 형식 | jpeg / png / webp / heic / heif | 415 |

프론트가 업로드 직전에 **항상 JPEG 으로 다시 뽑으므로** 실제로 사용자가 고를 수
있는 형식은 이보다 넓다(브라우저가 그릴 수 있으면 된다). 아이폰 HEIC 은 크롬이
디코딩하지 못할 수 있는데, 그 경우 원본이 그대로 가고 서버·Gemini 둘 다 HEIC 을
받으므로 문제없다.

> **"사진 찍기"는 웹에서 없앨 수 없다** (여러 번 시도했고, 결론은 이렇다).
>
> 파일 선택을 누르면 OS 가 자기 메뉴(사진 보관함 / 사진 찍기 / 파일 선택)를
> 띄운다. 웹은 `accept` 로 "어떤 파일을 받을지"만 알려줄 뿐, "촬영 항목을 빼라"
> 고 지정하는 표준이 없다. `accept` 를 형식 목록으로 좁혀도 사라지지 않는다.
> `capture` 속성은 촬영을 **강제**하는 것이지 그 반대가 아니다.
>
> 유일하게 없앨 수 있는 방법은 `accept` 를 아예 빼는 것인데, 그러면 iOS 가 '파일'
> 앱을 바로 열어 **'사진 보관함'도 함께 사라진다.** 갤러리에서 고르는 게 주 경로라
> 그 대가가 더 크다고 보고 그대로 두었다.
>
> **대신 그쪽으로 찍어도 모드를 물어본다.** 거기서 찍으면 타일을 거치지 않아
> 무엇을 찍었는지 모른 채 처리되는 게 진짜 문제였다(타일마다 프롬프트가 다르다 —
> `_RECEIPT_PROMPT` / `_FOOD_PROMPT`).
>
> 촬영으로 들어온 파일은 **만들어진 시각이 방금**이다(앨범 사진은 대개 예전 날짜).
> `looksJustTaken()` 이 이를 단서로 잡아, 바로 처리하지 않고 타일 화면으로 돌아가
> `방금 찍은 사진, 무엇인가요?` 를 묻고 타일을 깜빡인다. **다시 찍게 하지 않는다** —
> 받은 사진을 들고 있다가 고른 모드로 보낸다. 고르기 싫으면 하단의
> `모르겠어요 · 알아서 인식해 주세요` 로 자동 판별(`mode=file`)을 탈 수 있다.
>
> 판별은 시각 기반이라 완벽하지 않다(방금 찍은 사진을 앨범에서 골라도 물어본다).
> 다만 그 경우에도 한 번 더 묻는 것뿐이라 손해가 없다.

---

## 호출 한도

챗봇과 **같은 하루 한도**를 공유한다(`chat_service._consume_quota()`).
같은 무료 키를 쓰기 때문이다. **여러 장을 올려도 1회만 소모한다.**

키는 이 순서로 찾는다:

```
GEMINI_API_KEY_VISION  →  GEMINI_API_KEY_CHAT  →  GEMINI_API_KEY
```

`GEMINI_API_KEY_VISION` 에 별도 키를 넣으면 이미지 인식만 그 키를 쓴다.
안 넣으면 챗봇/재료 추출 배치와 하루 한도를 나눠 쓰게 된다.

모델은 `GEMINI_VISION_MODEL` → `GEMINI_MODEL` → `gemini-3.5-flash-lite` 순.

---

## 오류 응답

| 상황 | 코드 | 사용자에게 보이는 문구 |
|---|---|---|
| 이미지 없음 | 400 | 이미지가 없습니다. |
| 5장 초과 / 용량 초과 | 413 | 사진은 한 번에 5장까지 올릴 수 있어요. |
| 지원 안 하는 형식 | 415 | 지원하지 않는 이미지 형식입니다. |
| 하루 한도 소진 | 429 | 오늘 무료 한도(N회)를 다 썼어요. |
| 모듈 로드 실패 | 503 | 이미지 인식이 아직 준비되지 않았어요. |
| **재료 사전 못 찾음** | **503** | **재료 사전을 불러오지 못했어요.** |
| 그 밖의 오류 | 502 | 이미지를 읽지 못했어요. (+ `detail` 에 예외 클래스명) |

원인을 **응답만 보고 구분할 수 있게** 나눠 두었다. 배포 로그를 볼 수 없는 상황에서
"사전 파일이 없다" 와 "LLM 호출이 실패했다" 를 구별하지 못해 한참 헤맸기 때문이다.
실제로 이 구분 덕분에 사전 CSV 가 배포에 없다는 걸 한 번에 찾았다.
503/502 는 서버 로그에 전체 트레이스백도 남긴다.

---

## 실측 (2026-08-31)

거래일시가 찍힌 테스트 영수증(9개 품목):

| 영수증 원문 | 인식 | 사전 정규화 |
|---|---|---|
| CJ 백설 물엿 700g | 물엿 | 물엿 |
| 하선정 까나리액젓 500ml | 까나리액젓 | 액젓 |
| 무항생제 대파 1단 | 대파 | 대파 |
| 한성 크래미 180g | 크래미 | 게맛살 |
| 오뚜기 케찹 500g | 케찹 | 케첩 |
| 돼지고기 목살 500g | 돼지고기 목살 | 목살 |
| 청양고추 100g | 청양고추 | 청양고추 |
| CJ 다시다 감칠맛 100g | 다시다 감칠맛 | 다시다 |
| 비닐봉투 | (모델이 제외) | — |

- 식재료 8개 전부 매핑, 구매일자 `2026-08-29` 정확히 인식
- 영수증 2장을 한 번에 → 재료 12개, 전부 매핑, **호출 1회**
- 우유팩/두부팩 목업 → 유통기한 `2026-09-12` / `2026-09-05` 둘 다 정확

> 8개 중 6개(물엿·게맛살·케첩·목살·대파·다시다)는 **2026-08-31 사전 감사·보강
> 덕분에** 잡힌 것이다. 그 전 사전이었다면 8개 중 2개만 잡혔다. 인식 품질은
> 모델보다 **사전이 얼마나 두꺼운지**에 더 크게 좌우된다.

---

## 알려진 한계

- 음식 사진 인식 품질은 아직 확인하지 않았다(카메라 타일은 막아 둔 상태).
- 영수증 날짜 표기가 다양해서(`거래일시`, `영수증일자`, 점포별 양식) 못 읽는
  경우가 있을 수 있다. 그때는 오늘 날짜가 들어가고, 사용자가 화면에서 고칠 수
  있다.
- 사전에 없는 재료는 사용자가 직접 지정해야 담긴다. 자주 걸리는 이름이 보이면
  사전에 추가하는 편이 낫다(무엇이 자주 걸리는지는
  `scripts/show_dictionary_misses.py`, 추가 방법은 `PROJECT_OVERVIEW.md` 의
  재료 사전 절).
