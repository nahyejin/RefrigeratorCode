# -*- coding: utf-8 -*-
"""**레시피 카드를 내보낼 조건.** 이 파일이 그 정의의 유일한 자리다.

왜 한 곳에 모으나
-----------------
크롤러는 AI 를 기다리지 않는다 — 본문을 규칙 기반으로 훑어 재료를 먼저 붙여
넣는다. 그 값에는 본문에 없는 재료가 섞인다("여러 가지 방법" 의 `가지`,
"참기름" 에 딸려 오는 `식용유`). 그래서 **AI 가 값을 쓴 글만** 내보낸다.

이 조건이 여러 파일에 문자열로 흩어져 있었다. 목록은 `app.py` 가 `RECIPE_READY`
상수로 갖고 있었는데, `chat_service.py` 는 자기 쿼리를 따로 지으면서 `WHERE` 를
`1=1` 로 시작했다 — **챗봇만 아직 AI 가 안 본 글을 추천하고 있었다.** 발견
당시 781건(최근 7일 252건)이 그 상태였다. 조건이 한 곳에 있었으면 안 생겼을 일이라
여기로 모은다. 목록이 하나 더 생기면 이것을 가져다 쓰면 된다.

일부러 이 조건을 안 거는 곳
---------------------------
**이미 사용자가 본 것**은 사라지면 안 된다. 아래는 조건을 걸지 않는다.
    기록한 레시피 · 완료한 요리 · 즐겨찾기 · 가족이 공유한 것
    레시피 상세와 조리 순서 (`/api/recipes/<id>/cook`)

앞으로 — 조건을 더 좁힐 수 있나
-------------------------------
지금 카드는 두 종류다. 재료만 있는 것과, 조리 순서·요리명까지 있는 것.
조리 순서·요리명·재료 상세는 **9월에 들어온 기능**이고, 8월 사이클 결과를
복원한 33,596건은 재료만 갖고 있다. 그 글들은 재처리 대기열에 들어 있어
하루 5,000건씩 채워지는 중이다 (2026-09-08 기준 남은 33,994건, 약 6.4일).

대기열이 비면 `recipe_name IS NOT NULL` 을 조건에 더할 수 있다. 요리명은 새
배치가 **100% 채우므로**(9/6·9/7 실측) "새 기준으로 처리됐다" 는 표식이 된다.

**조리 순서(`cook_steps`)로는 가르면 안 된다.** 본문에 만드는 과정이 없어
정상적으로 빈 글이 섞여 있다 — 그것으로 거르면 멀쩡한 레시피가 사라진다.
"""

# 목록·검색·추천에서 쓰는 조건. `WHERE` 없이 조건만 담는다.
RECIPE_READY = "llm_ingredients_at IS NOT NULL"

# `WHERE ...` 로 시작하는 절이 필요할 때.
WHERE_READY = "WHERE " + RECIPE_READY
