# CHANGELOG

쿡매치(CookMatch) 프로젝트 변경 이력. Cursor AI에서 Claude Code로 작업 환경을 이관한 2026-08-24부터 기록.

## 2026-08-24

### 프로젝트 구조 정리
- 옛 CRA 프론트엔드 잔재 삭제 (루트 `src/`, `package.json`, `vercel.json`, `main.py`) — 실제 배포는 `frontend/`에서 진행되므로 영향 없음
- `backend/App.tsx`, 루트 `check_db.py`, 중복 `export_recipes_all.ipynb` 등 죽은 코드/중복 파일 삭제
- 로컬 SQLite 흔적(`posts.db`, `recipes.db`, `refrigerator.db`) 삭제 — 현재는 MySQL(Railway) 사용
- `crawler.log`, `chromedriver-win64/`, `__pycache__/` 전체를 git 추적 해제 (로컬엔 유지, `.gitignore` 반영)
- 루트에 흩어진 디버그/점검용 스크립트 18개를 `scripts/`로 이동
- `utils/requirements.txt.txt`(오타성 파일명)를 `ingredient_management/requirements.txt`로 이동

### 요리 챗봇 기능 추가
- `backend/chat_service.py`: Gemini/Groq LLM으로 대화 의도를 파악해 DB에서 레시피를 검색하는 `/api/chat` 엔드포인트 신설. 일일 호출 한도(기본 250회) 적용
- 프론트에 전역 플로팅 챗 위젯(`RecipeChatWidget.tsx`) 추가 — 냉장고 재료(localStorage) + 대화 기록을 세션 단위로 유지하며 추천 레시피 카드 표시
- Gemini 기본 모델을 `gemini-2.0-flash`(단종됨) → `gemini-3.5-flash-lite`로 교체
- 레시피 썸네일에 기존 `getProxiedImageUrl` 유틸 적용 — 네이버 이미지 핫링크 차단(403) 우회
- 로컬 개발 CORS 이슈 해결 (프론트 dev 서버 포트를 화이트리스트에 있는 5178로 통일)

### 챗봇 FAB를 AI 기능처럼 리디자인
- 텍스트만 있던 "챗" 버튼을 스파클 아이콘 + 회전하는 그라디언트 링 + 펄스 글로우 + "AI" 배지로 교체
- 채팅창 헤더와 어시스턴트 말풍선에도 같은 스파클 아바타 적용해 AI 브랜딩 통일

### 챗봇 대화 히스토리 관리 + 재료 매칭 우선 검색으로 재설계
- `RecipeChatWidget.tsx`: 채팅창을 닫았다 다시 열면 항상 새 대화로 시작하도록 변경. 지난 대화는 localStorage에 스레드 단위로 저장(최근 30일 보관)되고, 헤더의 "지난 대화" 버튼으로 목록을 열어 이어보거나 삭제 가능
- `chat_service.py`: 기존엔 LLM이 뽑은 키워드가 title/content LIKE **강제 필터**라서, 냉장고 재료와 매칭률이 높은 레시피도 키워드가 안 맞으면 통째로 걸러졌음. 이제 냉장고 재료가 있으면 키워드는 `HAVING` 절의 소프트 조건(키워드 매칭 OR 매칭률 25% 이상)으로 바뀌어, 매칭률 높은 레시피가 우선 노출됨
- LLM 프롬프트에 `ignore_fridge` 필드 추가: 사용자가 "재료 상관없이" 등으로 명시적으로 요청할 때만 냉장고 매칭을 끄고 순수 키워드 검색으로 전환
- 레시피 카드의 "재료 매칭률 N%"를 일반 텍스트에서, 기존 `RecipeCard.tsx`와 동일한 톤(짙은 배지 + 노란색 굵은 글씨)의 배지로 변경

### 재료 추출 파이프라인을 LLM 기반으로 재설계 (진행 중)
- `ingredient_management/llm_ingredient_extraction.py` 신설: Gemini로 본문에서 실제 사용 재료를 "이해"해서 뽑아내고,
  정규화는 100% 기존 사전(`ingredient_profile_dict_with_substitutes.csv`)의 결정론적 매칭으로만 처리
  (LLM 역할을 추출까지만 최소화, 매칭은 LLM 관여 없음). 출력 포맷은 기존과 동일(콤마구분/공백없음/정렬)하게 유지해
  프론트 pill 매칭·백엔드 매칭률 SQL을 전혀 건드리지 않음
- 사전에 없는 LLM 추출 후보는 버리되 `unmapped_ingredients` 컬럼에 기록 (추후 사전 보강용)
- 20건 샘플 검증 결과, 기존 룰베이스의 실제 버그들(예: "청양고추"를 "고추"+"청양고추"로 중복 집계, "만두"/"쌈무" 같은 요리명이 재료로 오인식, 블록 탐색 범위가 좁아 뒷부분 재료 누락)이 다수 수정됨을 확인
- 전체 44,121건에 대해 `--commit` 모드로 백그라운드 배치 실행 중 (모델: gemini-3.5-flash-lite, 분당 140건, 동시 7개)
- **버그 수정**: 최초 버전은 전체 결과를 다 모은 뒤에야 CSV/DB에 쓰는 구조라 중간에 죽으면 진행 상황이 전부 유실되는
  문제가 있었음 → 결과가 나오는 즉시 CSV에 flush, 30건마다 DB 커밋하도록 수정 후 재시작
- **실측 결과**: 이 API 키는 무료 티어라 `gemini-3.5-flash-lite`가 분당 15건으로 하드 제한됨. 분당 140건으로
  돌렸다가 에러율 27%를 확인하고 분당 13건(안전 마진)으로 재시작. 44,121건 전체 기준 약 2~2.5일 소요 예상

### git 추적 충돌 발견 및 정리
- 세션 도중 내가 만들지 않은 커밋 2개(`대거변동 260825`, `업데이트 20260825_12:37`)가 발견됨 — 다른 도구(Cursor 등
  자동 커밋으로 추정)가 같은 저장소에 동시에 작업 중인 것으로 보임. 이 커밋들이 `.gitignore`를 이전 버전으로
  되돌리고 pycache/chromedriver/crawler.log를 다시 추적시킴 → 재정리하고 배치 산출물 CSV도 gitignore에 추가
- **주의**: 다른 자동 커밋 도구가 계속 켜져 있으면 다시 충돌할 수 있음. Cursor 등 확인 필요

### 챗봇 UI 세부 디자인 개선
- FAB/헤더/말풍선의 스파클 아이콘 → 김이 나는 냄비 아이콘으로 교체 (SVG가 flex 컨테이너 안에서
  width 0으로 찌그러지던 렌더링 버그도 같이 수정)
- 지난 대화·새 대화 버튼을 아이콘 버튼(38px)으로 확대, 새 대화는 + 아이콘으로 통일
- 안내 문구를 기능 설명 → 사용자 편익 중심으로 변경, placeholder도 "맛"에서 "먹고 싶은 걸"로 범위 확장
- 채팅창 타이틀 강조(굵게/크게), 창 높이 70vh/560px → 84vh/700px로 확대
- GitHub main에 푸시 완료 (Vercel 자동 배포 트리거됨)

### 배포 환경변수 설정
- Railway 백엔드에 `GEMINI_API_KEY` 환경변수 추가 완료 (운영 환경 챗봇 활성화)

### 크롤러 자동화 안정화 (Task Scheduler 등록 준비)
- `run_all_crawlers.py`가 계속 실패하던 원인 파악: ChromeDriver 버전 자동 감지가
  PowerShell/wmic 서브프로세스에 의존하는데, 실패해도 감지된 버전을 실제로 쓰지
  않는 버그가 있어 최신 Chrome보다 오래된/새로운 ChromeDriver를 받아와 세션 생성이
  계속 실패했음. 레지스트리 기반 감지로 교체하고 감지된 버전을 실제로 전달하도록 수정
- 크롤러 파이프라인에 LLM 일일 증분 추출 단계 연결 (룰베이스 폴백 유지 + 무료 한도 내 LLM 보정)
- 의존성 누락(dnspython, tqdm), 미사용 import(mysql.connector) 정리
- 매일 오전 7시 자동 실행할 `run_crawlers_scheduled.bat` 준비 (Windows 작업 스케줄러 등록 예정)

### LLM 배치 처리 + 하루 500건 한도 대응
- `llm_ingredient_extraction.py`에 `--batch-size` 옵션 추가: 레시피 여러 건을 호출
  1번에 묶어 처리 (기본 8건) → 일일 호출 횟수 한도를 훨씬 적게 사용, 챗봇과 공존 가능
- `--pending-only` 옵션 추가: DB의 `llm_ingredients_done` 플래그로 "이미 처리됨"과
  "신규/미처리"를 구분해, 크롤러가 매일 신규 수집분만 무료 한도 내에서 처리하고
  오늘 못 채운 건 자동으로 다음날로 넘어가도록 함
- 44,121건 기존 백로그는 유료 티어로 별도 처리 예정 (실측 토큰 기반 예상 비용 약 9천~1만 8천원)

### UI 디자인 정리
- 실제 화면 4곳(내 냉장고/냉장고 요리/요즘 인기/마이페이지)을 스크린샷으로 검토해
  아쉬운 지점 정리 후 우선순위대로 수정:
  - AI FAB와 PWA 설치 유도 배너가 z-index 문제로 겹쳐 보이던 것 수정
  - 내 냉장고 재료 태그가 진회색+흰 글씨로 뭉쳐 보이던 것을 연회색+진회색으로 가볍게 변경
  - "부족 재료" pill이 연회색 배경에 흰 글씨라 실질적으로 안 보이던 대비 문제 수정
    (앱 전역 10곳의 범례도 함께 정리)
  - 냉장고요리 페이지 필터 버튼 4개가 전부 같은 톤이라 위계가 없던 것을 아이콘+진한
    "필터" 버튼으로 구분
  - 스플래시 화면을 베이지/일러스트 톤에서 실제 CookMatch 아이콘 기반 화이트/블랙/
    옐로우 톤으로 재구성 (본편과 첫인상 통일)

### 무료 티어 실측: 하루 500건이 진짜 병목
- 분당 13건(안전 마진)으로도 대량 에러 발생 → 원인은 분당 제한이 아니라
  `GenerateRequestsPerDayPerProjectPerModel-FreeTier` **하루 500건** 한도였음
- 44,121건 ÷ 500건/일 ≈ 88일. 게다가 운영 챗봇도 같은 키의 같은 일일 한도를 공유해서
  무료 티어로는 배치와 실사용자 챗봇을 동시에 운영할 수 없음 → 유료 티어 전환 필요, 사용자 결정 대기 중
- 오늘 처리분: 948건 시도, 성공 160건(DB 반영), 나머지는 일일 한도 초과로 실패

### 데이터 안전장치 추가 (중요)
- **버그 발견**: LLM이 빈 배열을 반환하면(파싱 실패/모델 이상 응답 등) 기존에 있던 재료 목록을
  조용히 빈 값으로 덮어써버리는 문제가 있었음. 160건 중 2건에서 실제 발생(id=1326 '초계라면'은
  진짜 레시피인데 빈 값으로 덮어써짐 → 기존 값으로 복구 완료. id=1295는 레시피가 아닌 홍보 영상이라
  원래도 룰베이스가 오탐한 것이었고 빈 값이 맞아서 그대로 둠)
- **수정**: 기존 `used_ingredients`가 있는데 LLM 결과가 비어 있으면 DB에 반영하지 않고 CSV에만
  `NEEDS_REVIEW`로 표시 (DB에는 절대 안 들어가므로 사용자에게 노출되는 값은 항상 기존 값 그대로 유지됨)

### 챗봇 FAB 로고/타이틀 변경
- FAB/헤더/말풍선 아이콘을 냄비 모티프에서 쿡매치 메인 아이콘(집 지붕 + 밥그릇) 모티프로 교체
- 타이틀 "AI 요리 챗" → "쿡매치 AI" (챗봇 느낌을 줄이고 브랜드를 앞세움)

### 레시피 아닌 본문 자동 삭제 로직 추가
- 룰베이스(old)와 LLM(new)이 둘 다 "재료 없음"에 동의하는 행만 레시피가 아닌 것으로 보고
  `recipes` 테이블에서 행 자체를 삭제 (예: 홍보 영상, 안내 영상 등)
- old에는 재료가 있었는데 new만 비어 있는 경우(불일치)는 삭제하지 않음 — LLM 쪽의 일시적
  오류일 수 있어 위 데이터 안전장치와 동일하게 NEEDS_REVIEW로만 표시하고 기존 값 보존
- 수동 확인한 사례 2건 처리: id=1326(진짜 레시피, 오류로 비워졌던 것)은 기존 값 복구,
  id=1295(레시피 아닌 통영 축제 홍보 영상)는 행 자체 삭제
- `ingredient_management/llm_extracted_158_review.csv`: 성공 처리된 158건의 링크 +
  LLM 추출 재료(원본/정규화 후) 검토용 파일 생성

## 2026-08-25

### 스티키 필터 영역 (냉장고 요리 페이지)
- `RecipeList.tsx`: 검색창 + 정렬/필터 바 + 재료 pill 범례("부족 재료 ... 총 000건")를
  스크롤해도 상단에 고정되도록 sticky 처리 (top: 56px, GNB 바로 아래)
- 범례가 화면 밖으로 사라져서 pill 색상의 의미를 잊게 되는 문제 해결

### 챗 패널 모바일 키보드 대응
- `RecipeChatWidget.tsx`: `visualViewport` API로 키보드 높이를 감지해 패널 위치/높이를 재계산.
  키보드가 올라와도 패널 상단이 잘리지 않음
- 메시지 목록/지난 대화 목록에 `minHeight: 0` 추가 — flex 자식이라 스크롤이 안 걸리던 문제 해결
- 패널 타이틀 글꼴/굵기를 앱 공통 톤(18px / 700)에 맞춤

### 내 정보 수정 진입 경로 + 닉네임 마스킹 문제
- `TopNavBar.tsx`: GNB의 닉네임을 버튼으로 바꿔 클릭 시 `/my-page?openEdit=true`로 이동
- `MyPage.tsx`: `openEdit=true` 쿼리 파라미터를 감지해 "내 정보 수정" 모달 자동 오픈
- 버튼 문구 "적용" → "변경 적용"
- **닉네임이 `9208****`로 보이던 원인**: 프론트/백엔드 어디에도 마스킹 코드는 없었고,
  네이버 로그인 API(`/v1/nid/me`)가 별명(nickname)을 마스킹된 형태로 내려주는데 그 값을
  그대로 저장한 것이었음 (DB의 users.nickname 실제 값 자체가 `9208****`)
- `app.py` 네이버 콜백 수정: 닉네임이 비어 있거나 `*`를 포함하면 이메일 로컬 파트를 대신 사용
  → 이후 신규 네이버 가입자는 마스킹된 닉네임이 저장되지 않음
- 기존에 저장된 값은 "내 정보 수정"에서 직접 변경 필요

### Sticky 영역 여백 + 다른 목록 페이지에도 동일 적용
- `RecipeList.tsx`: sticky 영역에 `paddingBottom: 10` 추가. 기존엔 범례의 `marginBottom`이
  margin collapsing으로 sticky 컨테이너 밖으로 빠져나가서, 스크롤 시 카드가 "부족 재료 / 대체 가능 /
  보유 재료" 글자에 바짝 붙어 지나갔음. 이제 범례 아래 흰 여백이 항상 유지됨
- 같은 형식의 범례를 쓰는 나머지 목록 페이지에도 동일하게 sticky 적용
  (정렬/필터 바 + 범례가 함께 상단 고정, top: 56px)
  - `IngredientDetail.tsx` — 요즘인기 페이지 하단 검색 → `/ingredient/:name`으로 이동하는 페이지
  - `RecordedRecipeListPage.tsx` — 내가 기록한 레시피
  - `CompletedRecipeListPage.tsx` — 내가 완료한 레시피
- `Popular.tsx`와 `MyPage.tsx`의 범례는 제외 — 이쪽은 섹션(유튜브 인기 / 네이버 인기 / 특별한 날 /
  즐겨찾기 / 기록 / 완료)마다 각각 붙어 있는 가로 스크롤 캐러셀 헤더라, sticky로 만들면 범례 여러 개가
  서로 밀어내며 겹침. 세로로 길게 스크롤되는 목록 페이지에만 적용하는 것이 맞음

### 챗봇 FAB를 노란 말풍선 버튼으로 리디자인
- 검정 원 + 쿡매치 마크(집 지붕+밥그릇) + 회전 그라디언트 링 조합이 무겁고 안 예쁘다는 피드백 반영
- 브랜드 옐로우(#FFD600) 원형 버튼 + 어두운 말풍선 아이콘(안쪽 점 3개는 노란색으로 뚫음)으로 교체
- 회전하는 conic-gradient 링 제거 (노란 버튼 위에서는 색이 뭉개짐). 은은한 노란 글로우 펄스는 유지
- "AI" 배지를 노란 배경/검정 글씨 → 검정 배경/노란 글씨로 반전 (노란 버튼 위 대비 확보)
- 누를 때 살짝 눌리는 `:active` 스케일 피드백 추가
- 아이콘 클래스를 FAB 전용(`.ai-fab-bubble`)으로 분리 — 패널 헤더/말풍선 아바타의 쿡매치 마크는
  기존 그대로(어두운 원 + 노란 마크) 유지되도록 함

### 챗 패널 아바타/헤더 정리 + FAB 애니메이션 복원
- 패널 헤더 아바타와 답변 말풍선 아바타를 FAB와 동일한 디자인(노란 원 + 어두운 말풍선)으로 통일.
  기존의 검정 원 + 쿡매치 마크(집 지붕+밥그릇) 조합은 FAB와 따로 놀아서 제거
- 사용하지 않게 된 `CookMatchMarkIcon` 컴포넌트와 관련 CSS 삭제
- 헤더의 부제 "오늘 뭐 먹을지, 냉장고 재료로 같이 찾아볼게요 🍳" 제거.
  좁은 화면(앱)에서 말줄임으로 잘리는 데다, 본문 첫 안내 문구
  "뭐 먹을지 고민될 때, 냉장고 재료로 딱 맞는 레시피를 찾아드릴게요."와 내용이 중복됨
- FAB 애니메이션 복원/강화: 회전 링을 뺀 대신
  - 노란 그라데이션이 버튼 위를 천천히 흐르는 `ai-fab-shimmer` (4.2s 무한 반복)
  - 주기적으로 스쳐 지나가는 하이라이트 `ai-fab-sheen`
  - 기존 노란 글로우 펄스 유지
  - 말풍선 아이콘은 하이라이트 위에 올려 흐려지지 않게 처리

### 챗봇 예시 질문 확대 + 입력창 UI 개선
- 첫 진입 시 보여주는 예시 질문을 3개 → 8개로 확대. 맛/냉장고 위주였던 걸
  대상·목적·상황까지 넓힘
  - 기존: 매운 거 / 있는 재료로 / 재료 상관없이 간단한 거
  - 추가: 아이가 잘 먹는 반찬 / 다이어트 중인데 뭐 먹지 / 캠핑 가서 해먹기 좋은 요리 /
    술안주 / 15분이면 되는 자취 요리
- 입력창이 흰 배경에 연회색(gray-100)이라 입력 가능한 영역인지 구분이 안 된다는 피드백 반영
  - 흰 배경 + 진한 테두리(#D8D8DE) + 라운드 필 형태로 변경해 필드 경계를 명확히 함
  - 포커스 시 브랜드 옐로우 테두리 + 글로우 링 표시
  - 입력 영역 전체 배경을 살짝 톤다운(#FAFAFB)해 대화 영역과 시각적으로 분리
  - "보내기" 버튼을 검정 → 브랜드 옐로우로 변경 (비활성 시 회색), 높이를 입력창과 40px로 맞춤

### 챗 패널 스크롤 문제 해결
- 측정해보니 스크롤 자체는 동작하고 있었음(scrollHeight 760 > clientHeight 512). 문제는 세 가지였음
- **뒤 페이지로 스크롤 전파**: 패널 안을 드래그하다 끝에 닿으면 뒤 페이지가 대신 밀려서
  "패널이 안 움직인다"고 느껴짐 → 스크롤 영역에 `overscroll-behavior: contain` 적용
- **뒤 페이지 스크롤 미잠금**: 패널이 열려 있는 동안 `document.body`를 `overflow: hidden`으로 잠그고
  닫힐 때 원복
- **답변 도착 시 맨 아래로 점프**: 긴 답변이 오면 곧장 최하단으로 스크롤해서 답변 앞부분이 안 보였고,
  사용자 입장에선 내용이 잘려 보임 → 이제 '마지막 질문'이 상단에 오도록 스크롤해서
  답변을 처음부터 읽을 수 있고, 아래에 더 있다는 것도 카드가 걸쳐 보여서 인지됨
- 스크롤바를 얇게(6px) 보이도록 스타일링해 스크롤 가능 영역임을 시각적으로 표시

## 2026-08-25 (UI 리팩터링)

### 1단계: 죽은 CSS 정리 + 촌스러움의 실제 원인 2건 제거
- **정정**: "폰트가 맑은고딕으로 떨어진다"는 최초 진단은 틀렸음. 브라우저 실측 결과
  body/h2/button/input 모두 Pretendard로 정상 렌더되고 있었음. 맑은고딕 규칙은
  `pill-label`/`tag-label`/`filter-pill`/`small-pill` 등 **사용처가 0인 클래스**만
  겨냥하고 있어 한 번도 적용된 적이 없었음
- **모든 글자의 흰색 윤곽선 제거** (진짜 원인 ①): `body`에 `text-shadow: 0 0 1px #fff`가
  4겹 걸려 있어 앱 전체 글자가 뿌옇게 번져 보였음. 이 규칙을 되돌리려고 코드 곳곳에
  `textShadow: 'none'` 인라인 우회가 22군데나 생겨 있었음 → 원인을 제거하고 우회 22곳도 함께 삭제
- **페이지 스크롤바를 보이게 변경** (진짜 원인 ②): `html/body/#root`의 스크롤바 thumb이
  `background: transparent`로 되어 있어 스크롤바가 아예 안 보였음. "스크롤이 자꾸 안 보인다"는
  체감의 직접 원인 → 은은하지만 보이는 thumb(검정 18%, hover 30%)으로 변경, Firefox용
  `scrollbar-width/color`도 추가
- **죽은 CSS 규칙 약 80줄 삭제**: `.text-[11px]`, `.bg-[#D1D1D1]`처럼 대괄호를
  이스케이프하지 않은 셀렉터가 목록에 섞이면 CSS 규칙 **전체가 무효**가 됨.
  pill 폰트 강제 블록(Noto Sans KR / font-weight 200 / 12~14px !important)이 전부 이 경우라
  적용된 적 없는 죽은 코드였음. 남겨두면 나중에 해당 클래스를 쓰는 순간 예상 못한 스타일이
  튀어나오는 지뢰라 제거

### 2단계: 디자인 토큰 도입 + 재료 pill/범례 단일화
- **수치 정정**: 앞서 "고유 색상 252종"이라고 했던 건 SVG 에셋 파일(`share_black.svg` 등)이
  섞여 부풀려진 값이었음. 실제 코드(tsx/ts/css) 기준 정확한 수치는 **92종**
- `src/index.css`의 `:root`에 디자인 토큰 정의 (ink 5단계 / line 2단계 / surface 2단계 /
  브랜드 5종 / 상태 3종 / z-index 6단계). `tailwind.config.js`에도 같은 팔레트를 반영해
  `text-ink-500`, `bg-brand-soft` 같은 클래스로도 쓸 수 있게 함
- 흩어진 색상 값 **506곳을 팔레트로 정규화** → 고유 색상 **92종 → 42종**.
  (`#222`/`#111`/`#1A1A1A` → `--ink-900`, `#E5E7EB`/`#E5E5E5` → `--line-200` 등.
  브랜드·플랫폼(네이버 초록/유튜브 빨강)·상태·캘린더 색은 의미가 있어 병합하지 않음)
- **쿠팡 광고 버튼 톤다운**: 채도 높은 주황 꽉 찬 버튼이라 카드에서 가장 눈에 띄었고,
  정작 주인공인 레시피보다 광고가 강조돼 있었음 → 흰 배경 + 옅은 주황 테두리 +
  주황 글씨의 외곽선 스타일로 변경
- **재료 pill 원색 노랑 제거**: 카드마다 보유 재료 pill이 4~6개씩 원색 노랑(#FFD600)으로
  꽉 차 있어 화면이 노란 덩어리가 되고, 브랜드 컬러가 "데이터 상태" 표시에 소모되고 있었음
  → 연한 노랑 배경(#FFF1B8) + 진한 노랑 글자(#6B5200)로 낮춤.
  부족 재료는 더 옅게(#F0F0F3) 물러나게 하고, 드물게 나오는 대체 가능만 진한 색 유지
- **pill 색과 범례를 단일 소스로 통합**: `src/styles/ingredientPill.ts` 신설.
  기존엔 범례 마크업이 6개 파일에 10곳 복붙돼 있어 pill 색을 바꿔도 범례는 안 따라오는
  구조였음 → `IngredientLegend` 컴포넌트로 추출해 10곳 모두 교체.
  이제 색을 바꾸려면 `ingredientPill.ts` 한 파일만 고치면 됨

### 3단계: z-index 체계화 + 모달 스택 맥락 탈출(Portal)
- z-index 값이 20종(1 ~ 10060)이 규칙 없이 흩어져 있었음. 새 요소를 넣을 때마다 숫자를
  키우는 식(9999 → 10001 → 10030 → 10060)으로 대응해 온 흔적 → **역할 기반 7단계**로 정리
  (`--z-sticky 100` / `--z-nav 200` / `--z-dropdown 300` / `--z-fab 400` /
  `--z-overlay 500` / `--z-modal 600` / `--z-toast 700`). 54곳 치환.
  카드 안 아이콘 같은 지역 스택(1~30)은 의미가 다르므로 그대로 둠
- **모달이 헤더/하단 네비 아래에 깔리던 버그 수정** (히트 테스트로 발견):
  필터 모달이 `position: sticky` 인 필터바 **안에서** 렌더되고 있었음. sticky는 새 스택 맥락을
  만들기 때문에, 모달의 z-index를 아무리 올려도 그 조상의 층위(100) 안에서만 높을 뿐
  바깥의 헤더·네비(200)보다 위로 갈 수 없었음
  → `Portal` 컴포넌트 신설(`createPortal`로 `document.body` 직속 렌더)하고
  FilterModal / IngredientDetailModal / RecipeSortBar의 매칭도·임박재료 모달에 적용.
  수정 후 히트 테스트로 헤더·네비·FAB 위치 모두 모달 딤이 덮는 것 확인

### 4단계: 남은 중복 제거
- **런타임 `<style>` 주입 제거**: 로딩 점 애니메이션 CSS가 5개 페이지 파일에 각각 복붙되어 있고,
  모듈 최상위에서 `document.createElement('style')` 로 `<head>` 에 주입하고 있었음
  (페이지를 옮겨 다니면 동일한 `<style>` 태그가 계속 쌓임) → `index.css` 로 옮기고 주입 코드 삭제.
  점 색상도 `var(--brand)` 로, z-index 도 `var(--z-overlay)` 로 토큰화
- **Toast / RecipeToast 통합**: 두 컴포넌트가 거의 같은 스타일 정의를 각자 들고 있었음
  → `RecipeToast` 가 `Toast` 에 위임하도록 변경(여러 줄 표시 옵션만 다름)
- `Toast` 를 `Portal` 로 감쌈 — sticky/transform 조상 안에서 호출돼도 층위에 갇히지 않도록
- 페이지에 인라인으로 복붙돼 있던 토스트 마크업 중 형태가 동일한 2곳을 `<Toast />` 로 교체

### 5단계: 화면 정리
- **`WelcomeModal` 에 딤 배경 추가**: 흰 카드만 화면 가운데 떠 있어서 모달인지 페이지 일부인지
  구분이 안 되는 상태였음(딤 배경이 처음부터 없었음) → 반투명 배경 추가 + 배경 클릭 시 닫힘 +
  `Portal` 적용
- `RegisterPromptModal` 도 `Portal` 적용 — 어디서 호출되든 층위 문제가 생기지 않도록

## 2026-08-25 (UX 재설계)

### A단계: 타이포 스케일 상향 + 터치 영역 확대
- **실측 진단**: 3개 주요 화면의 텍스트 642개를 측정한 결과
  - 전체의 **65%가 13px 미만**, 가장 많이 쓰인 크기가 11px(248곳), 7px도 20곳 존재
  - 터치 대상은 내냉장고 143개 중 **134개가 44px 미만**(Apple HIG / Material 권장 최소 미달)
  - "글씨가 작다 / 조작 영역이 좁다 / 옛날 앱 같다"는 체감의 직접 원인이었음
- 타이포 스케일 7단계 정의(`--text-caption 12` ~ `--text-2xl 26`, 본문 15px 기준)하고
  `tailwind.config.js` 의 `fontSize` 도 한 단계씩 상향(xs 12→13, sm 14→15, base 16)
- 인라인 `fontSize` 와 Tailwind `text-[Npx]` **214곳을 스케일에 맞춰 상향**
  - 결과: 13px 미만 텍스트 **65% → 3%**, 7px/10px 등 판독 곤란한 크기 소멸
  - 크기 종류도 11종 → 8종(12/13/15/16/18/22/26)으로 정리됨
- 재료 pill 을 11px/높이 22 → 13px/높이 26 으로, 정렬·필터 바 컨트롤을 높이 28 → 40 으로 확대
- 작은 아이콘 버튼용 `.tap-44` 유틸 추가 — 보이는 크기는 그대로 두고 투명한 히트 영역만
  44px로 넓혀 레이아웃 변화 없이 터치 정확도를 올리는 방식

### B단계: 공통 컴포넌트(프리미티브) 추출
- 버튼이 페이지마다 인라인으로 각각 작성돼 있었음(MyPage 18개, RecipeSortBar 10개,
  RecipeChatWidget 9개 …). 높이·색·radius가 제각각이고 28~30px 짜리가 많아 터치가 어려웠음
- `src/components/ui/` 신설 — C단계(화면 재설계)가 이 위에서 이뤄진다
  - `Button` : primary / secondary / outline / ghost / danger × sm / md / lg.
    **md·lg 는 항상 44px 이상**이라 터치 규격이 자동으로 지켜짐. 누를 때 축소 피드백 포함
  - `Chip` : 상태 없는 태그·필터 칩 (선택 상태 지원).
    재료 pill 은 보유/부족/대체 상태가 있어 `styles/ingredientPill.ts` 를 계속 사용
  - `Input` : 높이 44px, 테두리 + 브랜드 컬러 포커스 링.
    글자를 16px 로 고정 — iOS 는 16px 미만 입력창에 포커스하면 화면을 자동 확대해버림
  - `Sheet` : 바텀시트. 화면 가운데 모달은 한 손 조작 시 손이 닿기 어렵고 내용이 길면 잘림 →
    아래에서 올라와 엄지 근처에 액션이 오도록. Portal + 뒤 스크롤 잠금 + 손잡이 포함
- 기존 컴포넌트를 새 프리미티브에 위임해 실제로 쓰이게 연결
  - `PrimaryButton` → `ui/Button`
  - `TagPill` → `ui/Chip` (예전엔 폰트를 'Noto Sans KR' 로 따로 지정해 앱 전체 톤과 달랐음)
- `prefers-reduced-motion` 대응 추가 — 움직임 최소화 설정을 켠 사용자에겐 애니메이션을 줄임

### C단계(1차): 재료 pill을 화면의 주인공으로
- **방향 정정**: 처음엔 카드 정보가 많으니 보유 재료 pill을 빼자고 제안했으나,
  이 앱의 핵심 가치가 "보유 재료로 효율적으로 냉털이 + 부족하면 대체제 추천" 이라는 점을
  확인. pill 은 장식이 아니라 **답 그 자체**이므로 빼는 게 아니라 더 잘 읽히게 하는 방향으로 전환
- **pill 가로 스크롤 → 줄바꿈**: 기존엔 `flexWrap: nowrap` + `overflowX: auto` 라
  카드마다 재료가 잘려 있었고, 몇 개가 부족한지 보려면 카드마다 옆으로 밀어야 했음.
  핵심 정보가 스크롤 뒤에 숨어 있던 셈 → 줄바꿈으로 한눈에 보이게 하고,
  8개를 넘으면 점선 `+N` 칩으로 접음(탭하면 펼침).
  `pills` 배열이 이미 [부족 → 대체가능 → 보유] 순이라 접혀도 부족한 재료가 먼저 보임
- **매칭률 뱃지**: 한때 "1개만 더" 를 앞세우고 매칭률을 보조로 내렸다가 되돌림.
  재료 매칭률은 정렬 기준(재료매칭률순)이자 필터 기준(매칭도 30~100%)인 이 서비스의
  핵심 지표라 앞세우는 것이 맞음 → **"재료 매칭률 83% · 1개 부족"** 형태로,
  매칭률을 16px 굵게 강조하고 부족 개수를 보조로 덧붙임.
  부족 개수는 이미 계산돼 있던 `lackingIngredients`(대체 가능한 재료 제외) 를 사용
- 대체 가능 줄을 어두운 라벨 + 나열에서, 옅은 배경 칩 형태로 정리

### C단계(2차): 부족 재료를 눈에 띄게
- 부족 재료 pill이 가장 옅은 회색 채움이라 배경으로 물러나 있었음. 냉털이 판단에서
  실제로 행동을 부르는 건 부족한 쪽이므로 눈에 띄어야 한다는 판단
- 부족 재료를 **흰 배경 + 점선 테두리**로 변경 — "채워야 할 빈 칸"으로 읽히게.
  새 색을 추가하지 않고 테두리 형태만으로 구분해 팔레트가 늘어나지 않음
  - 보유 = 연노랑 채움(채워짐) / 부족 = 점선 빈 칸(채워야 함) / 대체 가능 = 진한 채움(드묾)
- 글자색도 #8A8A93 → #5A5A63 으로 올려 가독성 확보
- `PILL_COLORS` 에 `borderStyle` 을 추가하고 pill·범례가 모두 이를 따르게 함 →
  범례 견본도 자동으로 점선으로 바뀜 (단일 소스의 이점)

### C단계(3차): 필터 바텀시트 전환 + 카드 액션/광고 재배치
- **필터를 바텀시트로**: 화면 가운데 뜨는 모달이라 한 손 조작 시 상단 항목에 엄지가 닿지 않고
  내용이 길면 위아래가 잘렸음 → B단계에서 만든 `Sheet` 로 교체.
  적용 버튼이 엄지 근처로 내려오고, 손잡이·뒤 스크롤 잠금·슬라이드 업 애니메이션 포함
- **카드 액션 아이콘 재배치**: 즐겨찾기/완료/공유/기록 4개가 썸네일 위에 상시 노출돼
  음식 사진(레시피 선택의 주된 판단 근거)을 가리고 있었음. 4개 중 3개(완료·기록·공유)는
  요리한 *뒤* 에 쓰는 동작인데도 목록을 훑는 내내 떠 있던 것
  → 썸네일에는 **즐겨찾기 1개만** 남기고, 나머지는 좋아요/댓글 줄 오른쪽으로 이동.
  줄 높이를 그대로 써서 카드가 길어지지 않고 터치 영역은 36px 확보
- **쿠팡 구매 동선을 부족 재료 pill 로 이동**:
  - 기존엔 부족 재료가 여러 개여도 CTA 버튼은 **1개만 골라서** 보여줘서, 사용자가 사려는
    재료가 다르면 쓸모가 없었음. 게다가 모든 카드에 전체폭 버튼 + 2줄 고지문이 붙어
    20개 목록이면 광고 블록이 20개
  - 이제 점선(부족) pill 을 누르면 그 재료 구매로 연결(`+` 표시). 방금 부족 재료를
    "채워야 할 빈 칸"으로 만들었으므로 **"빈 칸을 누르면 채운다"** 는 동선이 자연스럽게 이어짐.
    사용자가 원하는 재료를 직접 고를 수 있고 클릭 의도도 높아짐
  - 링크 생성 로직을 `utils/coupangLink.ts` 로 분리 (기존엔 `CoupangProductAd` 안에만 있었음)
  - 별도 CTA 블록은 **부족 재료가 1~2개인 카드에만** 노출 — "하나만 사면 완성" 구간에서
    구매 전환이 실제로 일어나고, 5개 부족한 카드의 구매 버튼은 사실상 무의미하기 때문
- **미확인 사항**: 파트너스 고지문을 카드마다 반복하지 않고 목록 하단 1회로 줄이는 건
  쿠팡 파트너스 약관상 노출 요건 확인이 필요해 보류. 현재는 CTA 가 노출되는 카드에 함께 표시

### D단계: 모션 · 마감
- **로딩을 스켈레톤으로 교체** (`RecipeCardSkeleton` 신설, RecipeList / IngredientDetail 적용):
  예전엔 화면 한가운데 점 3개 스피너만 떠서 무엇이 로딩 중인지 알 수 없었고,
  로딩이 끝나는 순간 화면이 통째로 바뀌어 이동이 크게 느껴졌음
  → 실제 카드와 같은 모양(썸네일·제목·통계·pill)의 뼈대로 자리를 미리 잡아
  체감 대기시간을 줄이고 내용이 채워질 때 레이아웃이 흔들리지 않게 함.
  API 를 6초 지연시켜 로딩 상태를 잡은 뒤 스켈레톤 32개 렌더 확인
- **썸네일 페이드인 + `loading="lazy"`**: 이미지가 갑자기 튀어나오지 않고 부드럽게 나타나며,
  화면 밖 이미지는 늦게 불러와 초기 로딩 부담을 줄임
- **카드 누름 반응 추가**: 탭했을 때 아무 반응이 없으면 "안 눌렸나?" 하고 다시 누르게 됨
  → `:active` 에서 살짝 축소 + 투명도 변화. 기존의 hover translateY 는 모바일에서
  의미가 없어 제거하고 누름 반응으로 대체
- **접근성: 포커스 표시 복원**: 예전엔 `outline: none !important` 를 전역으로 걸어
  키보드 사용자가 현재 포커스 위치를 알 수 없었음 → `:focus-visible` 로 복원해
  마우스 클릭 시에는 안 보이고 키보드 이동 시에만 브랜드 컬러 아웃라인이 표시되도록 함

### 쿠팡 CTA 노출 기준 조정
- 부족 재료 1~2개 → **1~3개** 로 완화. 실제 목록에서 세어보니 1~2개 조건에서는
  해당하는 카드가 거의 없어 광고 노출이 과하게 줄어들었음
- (참고) 목록 하단으로 옮기자고 했던 것은 **광고가 아니라 고지 문구**임.
  고지문은 재료명이 들어가지 않는 일반 안내 문장이라 특정 재료와 매칭할 필요가 없음.
  구매 버튼과 pill 탭 구매 동선은 카드에 그대로 유지. 약관 확인 전까지는 현행 유지

### 쿠팡 클릭 측정 추가
- 광고 자리를 더 늘리기 전에 판단 근거부터 만들기로 함. 확인해보니 이 앱에는
  분석 도구가 **설치돼 있지 않았음** (`pwaAnalytics.ts` 에 gtag 호출 코드는 있으나
  `index.html` 에 GA 스크립트가 없어 어디서도 import되지 않는 죽은 파일),
  쿠팡 클릭도 전혀 기록되지 않고 있었음 → "카드 CTA vs pill 탭 중 뭐가 나은가"를
  누구도 답할 수 없는 상태였음
- `POST /api/track/coupang-click` 신설 + `coupang_clicks` 테이블 (없으면 자동 생성)
  - 기록 항목: 경로(`pill` / `card_cta`), 재료명, 그 카드의 부족 재료 개수, 레시피 id, 화면 경로
  - 측정 실패가 구매 동선을 막으면 안 되므로 어떤 예외에도 200 반환
- 프론트 `utils/trackCoupangClick.ts`: 클릭 직후 쿠팡으로 이동하므로 일반 fetch 는
  중간에 취소될 수 있어 `navigator.sendBeacon` 사용 (미지원 시 `keepalive` fetch 로 폴백)
- 2주 정도 쌓이면 "부족 1~3개 기준이 맞는지", "pill 탭이 실제로 쓰이는지",
  "하단 광고가 필요한지"를 추측이 아니라 숫자로 판단할 수 있음
- **하단 집계 광고는 만들지 않기로 함**: 50개 목록의 맨 아래는 도달률이 낮고,
  가장 자주 부족한 재료는 대개 설탕·간장 같은 기본 양념이라 구매 전환이 낮음.
  사용자가 "이게 없네" 하고 인지한 그 자리(부족 pill)에서 누르는 동선이 맥락상 우위

### 피드백 반영 1차: 애니메이션 회복 + 카드 세부
- **FAB 애니메이션이 사라진 것처럼 보이던 문제**: 측정해보니 shimmer/sheen 은 실제로
  돌고 있었으나(playState: running), **노란 버튼 위에 노란 그라데이션**이라 대비가 낮아
  움직임이 인지되지 않았음. 회전 링을 제거한 뒤 생긴 문제
  → sheen 을 더 또렷하게(피크 opacity 0.85 → 1), 글로우 세기 상향(0.5 → 0.75),
  버튼이 숨쉬듯 미세하게 커졌다 작아지는 `ai-fab-breathe` 추가.
  누를 때는 애니메이션을 일시정지시켜 축소 반응과 충돌하지 않게 함
- **로딩에 "Loading..." 표시 추가**: 스켈레톤만 있으면 "멈춘 화면"처럼 보인다는 피드백 →
  브랜드 컬러 링 스피너 + `Loading...` 문구를 목록 상단에 배치 (`role="status"`)
- **좋아요·댓글·조회수 줄바꿈 방지**: 한 줄로 고정하고 넘치면 말줄임 →
  카드 높이가 들쭉날쭉해지던 문제 해결
- **완료·기록·공유 버튼**: 아이콘이 작고 간격만 넓다는 피드백 →
  아이콘 18 → 22px 로 키우고 버튼 간 간격 제거(2 → 0), 오른쪽 여백 정리

### 피드백 반영 2차: 로딩 표시 통일 + 섹션 헤더 정리
- **로딩 표시가 화면마다 3종류로 달랐음** (탭을 옮길 때마다 다른 로딩이 떠 완성도가 떨어져 보임)
  - 내냉장고: 점 3개 + 한글 "로딩 중..."
  - 요즘인기 / 완료목록: 점 3개만 (문구 없음)
  - 냉장고요리 / 검색결과: 링 스피너 + "Loading..."
  → `LoadingIndicator` 컴포넌트 하나로 통일하고 **모든 화면에 적용**
- 링 스피너를 버리고 **점 3개가 순서대로 튀는 형태**로 되돌림. 점 크기도 12 → 13px 로 키우고
  간격·높이를 늘려 눈에 잘 띄게 함. 문구는 `Loading...` 으로 통일
- **섹션 헤더 통일** (`SectionHeader` 신설): 예전엔 섹션마다 마크업이 달랐음 —
  ⭐ 이모지를 제목 문자열에 섞어 넣은 것, 이미지 아이콘을 쓴 것이 섞여 있고
  정렬을 맞추려 `position: relative; top: 6px` 같은 임시 보정이 들어가 있었음
  → 이모지 제거(의미 있는 플랫폼 로고만 아이콘으로), 제목 16 → 18px, 설명문 지원
- **섹션 구분선 완화**: 2px 진한 회색 → 1px 옅은 선. 화면을 토막 내는 느낌 제거 (7곳)
- **요즘인기 카드 가로폭** 280 → 300px (좁아서 내용이 답답하다는 피드백)
- **재료 pill 접힘 기준** 8개 → 6개. 카드마다 3줄까지 늘어나 목록이 무거웠음

### 피드백 반영 3차: 팝업 규격 통일
- **팝업 6종의 수치가 전부 달랐음** — "다 제각각이라 촌스럽다"의 실제 원인
  | | 딤 | 모서리 | 폭 | 여백 |
  |---|---|---|---|---|
  | RegisterPrompt | 30% | rounded-xl | 320px | p-6 |
  | Welcome | (딤 없음) | rounded-lg | 가변 | px-5 py-4 |
  | IngredientDetail | 40% | rounded-2xl | 370px | p-8 |
  | IngredientDate | 40% | rounded-xl | 320px | p-6 |
  닫기 버튼도 `×` 텍스트인 곳, 아예 없는 곳이 섞여 있었음
- `ui/Dialog` 신설 — 확인/안내 팝업의 공통 규격
  (딤 40% / 모서리 20 / 폭 min(340, 100vw-40) / 여백 24 / 등장 애니메이션 /
  ESC 로 닫기 / 뒤 스크롤 잠금 / Portal / 하단 버튼은 `ui/Button` 사용)
- `RegisterPromptModal`, `WelcomeModal` 을 `Dialog` 로 완전 이관
- `IngredientDateModal`, `IngredientDetailModal` 은 내부 구조(달력·상세)가 복잡해
  전면 이관 대신 **겉 규격만 Dialog 와 동일**하게 맞춤 (모서리·폭·여백·그림자)
- 닫기 버튼 통일: `×` 텍스트 → 40×40 터치 영역의 SVG 아이콘 버튼 (`aria-label="닫기"`)
- 딤 농도를 40% 로 통일 (6개 파일)

### 피드백 반영 4차: 상단 GNB · 하단 네비 정리
- **상단 GNB**
  - 로고가 16px(`h-4`)로 지나치게 작아 브랜드가 눈에 안 들어왔음 → 20px 로 키우고
    44px 터치 영역의 버튼으로 감쌈
  - 닉네임·로그아웃 버튼에 `padding: 0` 이라 실제 터치 영역이 글자 높이뿐이었음 →
    높이 36px + 좌우 여백 확보, 공통 `NavTextButton` 으로 통일
  - **하단 구분선이 없어 스크롤 시 본문과 헤더가 섞여 보이던 문제** → `borderBottom` 추가
  - 닉네임이 길면 GNB 를 밀어내던 문제 → 최대 폭 + 말줄임
- **하단 네비게이션**
  - 실제로 쓰이지 않는 `STYLES` 상수 객체와 `CONSTANTS`(`NAV_HEIGHT: 16` 을 rem 으로 써
    실제 렌더와 무관했음)가 남아 있어 값을 어디서 고쳐야 할지 알 수 없었음 → 제거
  - 상단 구분선을 nav 에 붙이지 않고 **별도 `<div>` 를 절대배치**해 그리고 있었고
    그 div 만 `z-index: 30` 이라 다른 요소에 가려질 수 있었음 → nav 자체의 `borderTop` 으로 변경
  - **iPhone 홈 인디케이터 영역(safe-area) 대응 없어 하단이 가려지던 문제** →
    `env(safe-area-inset-bottom)` 반영
  - 탭 버튼을 `flex: 1` 로 균등 분배해 터치 영역을 화면 폭 전체로 확보
    (기존 `justify-around` 는 버튼 사이 빈 공간이 눌리지 않았음)
  - 활성 탭 표시를 `text-black/font-bold` → 토큰(`--ink-900` / `--ink-400`)으로 통일

### 피드백 반영 5차: 챗 패널을 팝업 공통 규격에 맞춤
- FAB 챗 패널만 다른 규격을 쓰고 있었음 (모서리 16 / 그림자 `0 8px 32px`) →
  `Dialog`·`Sheet` 와 동일하게 모서리 20 / `0 16px 48px` 로 통일
- 헤더 구분선을 `border-gray-100` → 토큰(`--line-200`)으로
- 닫기 버튼을 `×` 텍스트(38px) → 앱 공통 규격인 40×40 SVG 아이콘 버튼으로 교체.
  헤더의 원형 아이콘 버튼(지난 대화 / 새 대화)도 38 → 40px, 색을 토큰으로 통일
- 닫기 버튼 색이 옅어 잘 보이지 않아 `--ink-400` → `--ink-500` 로 한 단계 진하게
  (Dialog / 챗 패널 / 달력 3곳 공통)
- 챗 패널은 바텀시트로 바꾸지 않고 **떠 있는 패널 형태를 유지**함 —
  모바일 키보드 대응(`visualViewport` 로 높이·위치 재계산)이 이 구조에 맞춰져 있어
  시트로 바꾸면 그 처리를 다시 만들어야 하고, 얻는 것보다 잃는 게 큼

### 쿠팡 파트너스 고지문을 목록 하단 1회로
- 약관 확인 결과 페이지당 1회 노출로 진행하기로 함
- 기존엔 구매 버튼이 뜨는 **카드마다** 12px 2줄짜리 고지문이 반복돼,
  목록을 스크롤하는 내내 같은 문장이 계속 지나가며 화면을 무겁게 만들었음
  (재료명이 들어가지 않는 고정 안내 문장이라 카드마다 있을 이유가 없었음)
- `CoupangDisclaimer` 컴포넌트 신설 → `CoupangProductAd` 내부에서 제거하고
  광고가 노출되는 목록 화면 6곳(냉장고요리 / 검색결과 / 요즘인기 / 마이페이지 /
  기록한 레시피 / 완료한 레시피) 하단에 각 1회 배치
- 검증: 카드 50개가 렌더된 목록에서 고지문 노출 **1회** 확인

### 피드백 반영 6차: 애니메이션 정지 원인 해결 + 팝업 나가기 동선 통일
- **로딩·FAB 애니메이션이 동시에 멈춰 있던 원인 발견**: B단계에서 넣은
  `@media (prefers-reduced-motion: reduce)` 블록이 `*` 셀렉터로 **모든 애니메이션을
  0.01ms/1회로 죽이고 있었음**. 기기에서 "동작 줄이기"(iOS 손쉬운 사용 / Windows 애니메이션 표시)를
  켜면 로딩 점과 FAB 가 정지한 채로 보임 → 앱이 고장 난 것처럼 느껴짐
  - 로컬 측정에서 계속 "running" 으로 나왔던 건 테스트 브라우저가 그 설정을 물려받지 않았기 때문.
    `reducedMotion: 'reduce'` 로 재현해 확인함
  - → 장식적 움직임만 줄이고 **상태를 알리는 표시(로딩 점 / FAB / 스켈레톤)는 예외**로 둠.
    움직임 최소화 시 점은 튀는 대신 밝기만 변하도록 별도 keyframes 제공
- **팝업 나가기 동선 통일**: 우상단 X 가 있는 곳/없는 곳, 하단 취소 버튼이 있는 곳/없는 곳이
  섞여 있어 나갈 방법을 찾기 어려웠음
  - `ui/CloseButton` 신설 — 44px 터치 영역 + 연회색 원형 배경 + 굵은 SVG(22px, stroke 2.4).
    기존 `×` 문자는 폰트에 따라 크기·두께가 달라지고 얇아 잘 보이지 않았음
  - `Dialog` / `Sheet` / 챗 패널이 모두 이 버튼을 사용
  - `Dialog` 는 `actions` 가 비어 있으면 하단에 **취소 버튼을 자동으로** 넣음
  - `Sheet` 는 `footer` 가 없으면 하단에 **닫기 버튼을 자동으로** 넣음
  - 필터 시트 하단을 `취소` + `적용` 두 버튼으로 변경 (기존엔 적용만 있어 되돌릴 방법이 없었음)
- X 아이콘이 빈 원으로만 보이던 문제: flex 컨테이너 안에서 SVG 가 0px 로 찌그러지는 현상 →
  크기를 인라인으로 고정 (예전 FAB 아이콘과 동일한 원인)

### 피드백 반영 7차: 남아 있던 닫기 버튼 전부 교체
- 6차에서 `Dialog`/`Sheet`/챗 패널만 공통 버튼으로 바꿨고, **자체 마크업을 쓰는 팝업들은
  그대로 텍스트 `×` 였음** → 팝업마다 X 모양이 다른 상태가 계속됨
- 아래 8곳을 모두 `ui/CloseButton` 으로 교체
  - `RecipeSortBar` 매칭도 설정 / 임박 재료 설정 (텍스트 `×` 였음)
  - `CustomCalendar` 기간선택 (직접 만든 18px SVG — 원만 보이고 X 가 안 보였음)
  - `IngredientDetailModal`, `IngredientDateModal`
  - `MyPage` 내 정보 수정 (텍스트 `×`)
  - `RecordedRecipeListPage`, `CompletedRecipeListPage` 의 매칭률·임박재료 모달 (각 2곳)
  - `HomeInstallPrompt` 홈 화면 추가 안내
- 교체하지 않은 `×`: `FilterModal` 의 재료 칩 제거, `IngredientInput` 의 재료 삭제 —
  팝업 닫기가 아니라 항목 삭제 용도라 성격이 다름
- 검증: 매칭도 팝업에서 버튼 40×40 / SVG 22×22 / 배경 `#F5F5F7` 로 다른 팝업과 동일하게 렌더됨

### 피드백 반영 8차: 닫기 버튼 모양 · 팝업 헤더 높이 · 시트 드래그 닫기
- **닫기 버튼을 원형 → 둥근 모서리 사각형으로** (동그란 배경이 투박하다는 피드백)
  36×36, `borderRadius: 10`, 옅은 테두리 + 연회색 배경. 아이콘 20px
  - 실측 시 버튼 폭이 40px 로 나왔는데, 전역 `button { padding: 0.6em 1.2em }` 규칙이
    먹고 있었기 때문 → `padding: 0` + `boxSizing: border-box` 로 고정
- **팝업 제목 크기가 제각각이었음**: 달력 15px / 날짜입력 15px / 매칭도·임박재료 15px /
  기록·완료 모달 15px / 내 정보 수정 18px / 시트 18px → **전부 17px** 로 통일 (9곳)
- `ui/PopupHeader` 신설 — 높이 52px 고정 + 제목 17px + 우상단 닫기.
  기존엔 `minHeight: 56 + paddingTop 18` 같은 임의 값이 섞여 팝업마다 상단 높이가 달랐음.
  `Sheet` 가 이 헤더를 사용
- **바텀시트를 아래로 밀어서 닫기** 추가: 아래에서 올라온 시트는 아래로 미는 게 자연스러운
  닫기 동작인데 지원되지 않아 X 나 취소를 눌러야만 했음
  - 손가락을 그대로 따라 내려가고, 90px 이상 내리면 닫힘. 덜 내리면 스르르 제자리로 복귀
  - 내리는 동안 뒤 딤도 함께 옅어져 닫히는 느낌을 줌
  - 검증: 터치 이벤트로 160px 드래그 시 `translateY(160px)` 적용 → 손 뗀 뒤 닫힘 확인

### 피드백 반영 9차: 필터 시트 확장
- **시트가 중간 높이로만 열려 선택지가 안 보이던 문제**. 원인이 두 가지였음
  1. 시트 기본 높이가 낮았음 → 필터는 `82dvh` 로 넉넉히 열도록 지정
  2. **시트 안에 또 다른 고정 스크롤 영역**이 있었음 — 테마 선택란이 모바일에서
     `maxHeight: 100px` 로 묶여 있어 두 줄도 안 보였고, 시트 안에서 다시 스크롤해야 하는
     이중 스크롤 구조였음 → `46dvh` 로 확대
- **위로 끌어 펼치기 추가**: 시트를 위로 밀면 `95dvh` 로 확장. 손잡이를 탭해도 펼침/접힘 전환
  (드래그가 어려운 경우 대비). 펼친 상태에서 아래로 내리면 기본 높이로,
  기본 높이에서 내리면 닫힘 — 3단계 동작
- 최대로 펼쳐진 상태에서 위로 더 끌면 고무줄처럼 조금만 따라오게 해 한계를 알 수 있게 함
- 검증: 기본 82% → 펼침 95% 로 높이 변화 확인

### 피드백 반영 10차: 내냉장고 재료 설정 동선 개선
- **설정 버튼이 너무 작았음**: `⚙︎` 문자에 padding 4px 뿐이라 실제 터치 영역이 23px 남짓.
  유통기한을 자주 고치는 동선이라 32×32 아이콘 버튼(시계 모양)으로 교체
- **날짜 입력이 어색했던 부분 개선** (`IngredientDateModal`)
  - `yyyy-mm-dd` 를 손으로 다 입력하거나 달력을 열어야만 했음 →
    **빠른 선택 칩 추가** (유통기한: 3일 뒤 / 1주일 뒤 / 2주일 뒤 / 1개월 뒤,
    구매시점: 오늘 / 어제 / 3일 전 / 1주일 전)
  - 제목("일자를 선택하세요")과 바로 아래 질문("유통기한은 언제까지 인가요?")이 같은 말을
    두 번 하고 있었음 → 제목은 `유통기한` / `구매 시점` 으로 정리
  - 확인 버튼이 **파란색**이라 앱의 노랑·검정 체계와 따로 놀았음 → 공통 `Button` 사용
  - "잘 모르겠어요" 를 구매시점에만 주던 것을 유통기한에도 제공
- **재료 상세 팝업이 좌우로 잘리던 문제 수정 (제가 만든 회귀)**
  - 3차에서 팝업 규격을 통일하며 폭을 370 → 340px 로 줄였는데, 이 팝업은 내용이 넓어
    좌우가 잘려 "보관 공간"이 "산 공간"으로 보이고 닫기 버튼도 화면 밖으로 나갔음
  - 근본 원인은 **유통기한 버튼이 200px 고정 × 2개 + gap 32px = 432px** 로
    팝업 폭을 넘고 있었던 것 → 버튼을 `flex: 1` 가변폭으로, 아이콘 64 → 52px,
    팝업 폭 `min(360px, 100vw - 24px)` 로 조정
  - 유통기한 선택 버튼 색이 주황(#FF9800)/하늘색(#4FC3F7)이라 브랜드와 무관했음 →
    "있어요"는 브랜드 옐로우(주 선택), "없어요"는 중립 톤으로 위계 부여
  - 제목이 닫기 버튼과 겹쳐 잘려 보이던 것 → 좌우 여백 확보 + 공통 제목 규격(17px)

### 피드백 반영 11차: 입력창 포커스 스타일 통일
- **포커스 표시가 화면마다 제각각이었음**
  - 노란 테두리 + 노란 글로우: 챗 입력창(`.ai-chat-input`), 공통 `ui/Input`
  - 파란 링: `focus:ring-blue-*` (4곳)
  - 초록 링: `focus:ring-green-*`
  - 회색 테두리만: `focus:border-gray-400` (냉장고요리 검색창 등 3곳)
  - 아무 표시 없음: `focus:outline-none` 만 있는 곳
- 노란 링은 **브랜드 컬러를 "지금 입력 중"이라는 흔한 상태에 써버려** 과하게 튀었음
  → 테두리를 진하게(`--ink-700`) 하고 아주 옅은 회색 링(`rgba(26,26,30,0.07)`)만 두는
  절제된 방식으로 통일. 키보드로 이동해 들어온 경우(`:focus-visible`)엔 조금 더 또렷하게(0.14)
- `input` / `textarea` / `select` 전체에 적용되는 공통 규칙으로 만들고,
  컴포넌트에 흩어져 있던 색상 포커스 클래스를 6개 파일에서 제거
- 검증: 냉장고요리 검색창 / 내냉장고 재료 입력 / 챗 입력창 세 곳 모두
  `border: rgb(26,26,30)` + 동일한 링으로 렌더되는 것 확인

### LLM 재료 추출 매일 자동 실행 등록
- **어제 LLM 단계가 실행되지 않았음을 확인**: 로그에 `used_ingredients LLM 처리 시작` 이 0번
  찍혔고, 완료 문구도 코드상 문구가 아닌 옛 문구였음 → 어제 돌던 크롤러는 LLM 단계를 붙이기
  **전 버전의 스크립트를 메모리에 올린 상태**였음 (파이썬은 실행 중 파일 수정이 반영되지 않음)
- DB 실측: 전체 44,656건 중 LLM 처리 완료 **0건**.
  (수동 테스트로 처리했던 160건은 `llm_ingredients_done` 컬럼 추가 *전* 이라 표시가 남지 않음)
- **크롤러에서 분리**: 크롤러는 주 1회(월요일)만 도는데 LLM 처리는 매일 돌려야 하므로
  `run_llm_ingredients_daily.bat` 신설 + 작업 스케줄러 `CookMatch-DailyLLMIngredients` 등록
  (매일 새벽 3시, 절전 상태면 깨워서 실행)
- **묶음 8 → 12건**: 무료 티어 하루 500회 호출 중 450회만 사용(챗봇 몫 확보) →
  하루 450 × 12 = **5,400건**, 44,656건 기준 **약 9일** 소요
  (20건 이상으로 키우면 한 번에 보내는 본문이 길어져 LLM 이 뒷부분을 대충 처리하는 경향이 있어
   12건을 균형점으로 잡음)
- 소량(24건) 사전 검증 후 등록: 처리 24건 / 오류 0건 확인. 오늘분도 즉시 실행 시작

### ⚠️ 룰베이스 배치가 LLM 결과를 덮어쓰던 문제 수정
- **발견한 충돌**: 룰베이스 배치(`update_used_ingredients_batch.py`)가 조건 없이
  `SELECT ... FROM recipes ORDER BY id` 로 **전체 행**을 돌면서 `used_ingredients` 를
  덮어쓰고 있었음. LLM 추출도 **같은 열**을 쓰기 때문에,
  **주 1회 크롤러가 돌 때마다 그동안 LLM 이 며칠에 걸쳐 채운 결과가 룰베이스 값으로 되돌아감**
  - 게다가 `llm_ingredients_done` 은 1 로 남아 있어 LLM 이 다시 처리하지도 않음
    → 9일짜리 작업이 월요일마다 조용히 일부 무효화되는 구조였음
- 수정: 룰베이스 배치의 대상을 `WHERE COALESCE(llm_ingredients_done, 0) = 0` 으로 제한.
  룰베이스는 **아직 LLM 이 손대지 않은 신규 수집분의 임시 채움** 역할만 하도록 명확히 함
- 검증: 전체 44,518건 중 LLM 처리 완료 876건이 배치 대상에서 제외되어
  43,642건만 대상이 되는 것 확인

### 데이터 흐름 정리 (질문 답변 겸 기록)
- LLM 결과는 **새 열이 아니라 기존 `used_ingredients` 열을 덮어씀** →
  화면의 재료 pill 은 자동으로 LLM 결과로 바뀜 (UI 는 이 열 하나만 읽음).
  `llm_ingredients_done` 은 재료 값이 아니라 "이 행을 LLM 으로 처리했는지" 표시하는 깃발
- 이어서 처리 / 중복 없음: `--pending-only` 가 `done = 0` 인 행만 가져오고 성공 시 1 로 바꿈
  - 정상 추출 → 값 덮어쓰기 + `done=1`
  - 일시적 오류 → `done` 을 건드리지 않음 → 다음 실행에서 자동 재시도
  - LLM 결과만 비고 기존 값은 있음 → **값 보존** + `done=1` (무료 한도 낭비 방지)
  - 룰베이스도 비어 있고 LLM 도 비어 있음(둘 다 "재료 없음"에 동의) → **행 삭제**
    (레시피가 아닌 홍보성 본문으로 판단)

### 문서 전면 점검 및 갱신 (2026-08-26)
실제 코드·DB 와 대조해 문서를 최신 상태로 맞춤. 발견한 불일치는 아래와 같음.

**`DATABASE_SCHEMA.md` — 전면 교체 (실제와 거의 전부 달랐음)**
- `users`: 문서엔 `username`/`password_hash`/`phone`/`is_active` → 실제는
  `email`/`nickname`/`provider`/`provider_id`/`password`/`deleted_at` (소셜 로그인 구조)
- `user_ingredients`: 문서엔 `ingredient_name`/`category`/`quantity`/`unit`/`notes` →
  실제는 `name`/`storage_box`/`expiry_date`/`purchase_date`/`saved_at`
- 문서엔 `action_type` 을 가진 `user_recipes` 단일 테이블 → 실제는 즐겨찾기/기록/완료
  **3개 테이블로 분리**되어 있음
- 문서에 아예 없던 테이블: `recipes`(핵심!), `coupang_clicks`,
  `youtube_channel_cache`, `youtube_channel_meta`
- → 운영 DB 에서 직접 뽑아 다시 작성하고, `used_ingredients` 와 `llm_ingredients_done`
  의 관계·주의사항을 명시

**`PROJECT_OVERVIEW.md`**
- 재료 추출이 룰베이스/LLM **두 단계이고 주기가 다르다**는 점이 문서에 없었음 → 표로 추가
- 자동 실행 작업 2종(주간 크롤러 / 매일 LLM) 명시
- `llm_ingredients_done` 조건이 빠지면 룰베이스가 LLM 결과를 덮어쓴다는 경고 추가

**`DEVELOPMENT_GUIDE.md`**
- 개발 서버 포트가 **5173 으로 적혀 있었으나 실제는 5178** (`vite.config.ts`) → 수정
- 이번에 만든 **UI 체계 전체를 문서화**: 토큰 위치, `components/ui/` 7종,
  지켜야 할 규칙 6가지(팝업은 Dialog/Sheet, sticky 안에서 모달 렌더 금지,
  로딩은 하나만, reduced-motion 에서 `*` 금지, 포커스 개별 지정 금지, SVG 크기 고정)

**`README.md`**
- 폴더 구조도가 실제와 달랐음: 존재하지 않는 루트 `package.json`,
  `database/DATABASE_SCHEMA.md`, `data/*.csv` 등이 적혀 있고,
  실제로 있는 `ingredient_management/`, `scripts/`, `logs/`,
  `frontend/src/ui`·`styles/` 는 빠져 있었음 → 실제 구조로 교체
- CORS 예시에 5178 추가

**`MOBILE_APP_GUIDE.md`**
- "PWA 를 완성해야 한다"고 적혀 있었으나 실제로는 `manifest.json`·`sw.js` 가 이미 있음.
  다만 **서비스 워커 등록 코드가 어디에도 없어 실제로는 동작하지 않는 상태** →
  현재 상태를 표로 정리하고, 등록 시 캐시 무효화 주의사항 함께 기재

**`PERFORMANCE_OPTIMIZATION.md`**
- "FULLTEXT 인덱스 추가 (필수)" 로 남아 있었으나 실측 결과 `ft_title_content`
  (title, content) 가 **이미 적용돼 있음** → 적용 상태 표시 추가

### 피드백 반영 12차: 남은 UI 건 정리
- **챗 입력창 테두리가 굵고 노랗게 보이던 원인 발견 (제가 만든 버그)**
  - D단계에서 넣은 접근성 규칙 `input:focus-visible { outline: 2px solid var(--brand-strong);
    border-radius: 6px }` 이 **입력 요소까지 포함**하고 있었음
  - 챗 입력창은 패널이 열릴 때 자동 포커스되므로 **열자마자 노란 2px 외곽선**이 둘러졌고,
    `border-radius: 6px` 강제 때문에 알약 모양(9999px) 모서리까지 각지게 바뀌어 있었음
  - → 입력 요소를 규칙에서 제외(입력창은 `input:focus` 의 진한 테두리 + 옅은 링이 담당),
    버튼·링크용 외곽선 색도 노랑 → 중립 톤으로 변경. 모서리 강제도 제거
  - 검증: 모서리 `9999px` 복구, 노란 외곽선 사라진 것 확인
- **로딩이 화면마다 여전히 달랐던 이유**: 11차에서 컴포넌트는 통일했지만
  내냉장고·요즘인기는 `dotsOnly` 옵션을 써서 **점만 나오고 문구가 없었음** → 옵션 제거해 통일
- **로딩 중에 쿠팡 고지문이 함께 뜨던 문제**: `CoupangDisclaimer` 가 조건 없이 렌더되고 있었음
  → `{!loading && ...}` 으로 감싸 로딩이 끝난 뒤에만 표시 (4개 화면)
- **요즘인기 날짜 팝업의 연/월 스크롤바가 안 보이던 문제**: 드롭다운에 Firefox 전용
  `scrollbarWidth`/`scrollbarColor` 만 지정돼 있고 WebKit 규칙이 없었음.
  이미 있던 `.custom-scrollbar` 유틸을 두 드롭다운에 적용
- **재료 상세 팝업의 이모지 제거**: 얼음·눈송이·온도계 이모지풍 PNG 3장이 앱의 다른 요소와
  그림체가 달라 촌스러웠음 → **선으로 그린 SVG 아이콘**으로 교체(색은 토큰을 따름).
  선택 상태도 파란 ring(`ring-blue-400 bg-blue-50`) → 브랜드 톤(연노랑 배경 + 진한 테두리)으로

### 날짜 입력 팝업 여백이 없던 원인 수정 (제가 만든 버그)
- 증상: 유통기한·구매시점 팝업만 좌우·상하 여백이 전혀 없어 콘텐츠가 팝업을 뚫고 나오는 것처럼 보임
- 원인: 3차에서 팝업 규격을 통일할 때 `style` 속성을 **추가**했는데, 그 태그에는
  이미 `style={{ fontFamily: ... }}` 가 있었음.
  **JSX 는 같은 속성이 두 번 오면 뒤의 것이 앞의 것을 통째로 덮어쓴다.**
  결과적으로 `padding` / `width` / `borderRadius` / `boxShadow` 가 전부 무시되고
  `fontFamily` 만 적용된 상태였음 (측정값: `padding: 0px`, 카드 폭 286px)
- 수정: 두 style 을 하나로 합치고 `boxSizing: border-box`,
  `maxHeight: calc(100dvh - 64px)` + 세로 스크롤 추가 (긴 내용에서도 잘리지 않도록)
- 같은 실수가 다른 곳에도 있는지 **전체 tsx 를 검사** — 중복 `style` 태그 0건 확인
- 검증: 카드 폭 286 → 340px, `padding: 24px 20px 20px` 정상 적용 확인

### 내냉장고 재료 pill 동작 통합
- **문제**: pill 하나에 동작이 3개였음 —
  이름 탭(토스트로 날짜 표시) / 시계 버튼(설정 팝업) / X(삭제).
  시계 버튼은 pill 과 떨어져 있어 무엇인지 알기 어렵고, 이름 탭으로 뜨는 토스트는
  날짜를 "보여주기만" 하고 고칠 수는 없어 설정 팝업의 열화판이었음.
  (유통기한을 확인하는 이유는 대개 고치려는 것)
- **수정**: pill 을 누르면 **바로 설정 팝업**이 열리도록 합치고 시계 버튼 제거 →
  동작 3개 → 2개(탭 / X). 쓰이지 않게 된 `onInfoClick` prop 과 `handleTagInfo` 도 정리
  (`infoToast` 자체는 재료 저장 안내에도 쓰이므로 유지)
- **D-day 표시 추가**: 유통기한이 설정된 재료에만 `D-3` 형태로 표시.
  3일 이하·지난 재료는 붉은 톤으로 강조 → 임박 재료를 pill 을 눌러보지 않아도 한눈에 파악
- **날짜가 없는 재료에는 아무 표시도 넣지 않음**: 실측 결과 내 냉장고 172건 중
  유통기한 설정 **0건**, 구매일 **3건(2%)** 뿐이라, 빈 재료에 "+ 날짜" 같은 자리표시를 두면
  거의 모든 pill 에 붙어 방금 없앤 시계 아이콘과 같은 문제가 반복됨
- 부수 효과: 아이콘이 빠지면서 pill 이 짧아져 한 줄에 들어가는 재료 수가 늘어남

> 남은 과제: 날짜 입력률이 사실상 0% 인 근본 원인은 버튼 크기가 아니라
> **입력 시점에 날짜를 받지 않는 흐름**임. 영수증 촬영 → OCR 로 구매일·품목을 한 번에
> 얻는 방향이 유력하나 규모가 커 별건으로 논의 예정.

### 재료 상세 팝업 여백 + 뒤로가기 버튼 통일
- **재료 상세 팝업도 `style` 속성이 두 번이었음** (날짜 팝업과 동일한 원인).
  앞서 만든 중복 검사 스크립트가 이 건을 놓쳤는데, 정규식이 속성값 안의 화살표 함수(`=>`)를
  태그 종료(`>`)로 잘못 인식해 태그를 일찍 끊었기 때문
  → 중괄호 깊이를 세면서 **최상위 속성만** 검사하도록 스크립트를 고쳐 전체 재검사.
  실제 중복 **0건** 확인 (앞서 3건으로 나온 것은 prop 값 안의 중첩 JSX 속성을 함께 센 오탐)
- 두 style 을 합치고 `maxHeight` + 세로 스크롤 추가. 검증: 카드 폭 340px,
  `padding: 24px 20px 20px` 정상 적용

**뒤로가기 버튼 통일 (`ui/BackButton` 신설)**
- 닫기(X)는 둥근 사각형으로 통일해 놓았는데 뒤로가기만 제각각이라 같은 팝업 안에서
  두 버튼이 다른 물건처럼 보였음
  - `IngredientDateModal`: PNG 이미지(13px) + padding 4px
  - 기록/완료 목록, 재료 상세 페이지 헤더: PNG 이미지(13px)
  - 챗 패널 "지난 대화": `←` **문자** (18px)
- → `CloseButton` 과 완전히 같은 규격(36×36 둥근 사각형 + 20px SVG)으로 통일. 5곳 교체
- 쓰이지 않게 된 `뒤로가기.png` import(Popular), `backIcon`·`button` 스타일 상수 제거

### 내냉장고 재료 팝업: 키보드 가림 + 맥락 부족 수정
- **팝업이 키보드에 가리던 문제**: 재료명을 입력하고 "입력"을 누르면 팝업이 뜨는데,
  입력창에 포커스가 남아 있어 모바일 키보드가 그대로 떠 팝업을 덮고 있었음
  → 팝업을 열기 전에 `document.activeElement.blur()` 로 포커스를 풀어 키보드를 내림 (2곳)
- **팝업이 무엇을 하는 창인지 알기 어렵던 문제**: 제목이 "재료의 상세정보를 선택해 주세요"
  하나뿐이라 **어떤 재료**에 대한 것인지, **새로 넣는 것인지 이미 있는 걸 고치는 것인지**
  구분되지 않았음
  → 재료 이름을 제목으로 올리고(`돼지고기`), 그 아래에
  `보관 정보를 수정합니다` / `어디에 보관할지 알려주세요` 로 신규·수정 여부를 밝힘

### 내냉장고 저장 버튼 제거 + 자동 저장 신뢰성 개선
재료가 바뀔 때마다 이미 자동 저장이 돌고 있었고, 디스크 모양 저장 버튼은 그 위에 얹힌
수동 백업이었음. 자동 저장에 누락이 생기는 구조라 버튼이 필요했던 것 → 원인 3가지를 고치고 버튼 제거.

- **① 전체 삭제가 서버에 반영되지 않던 버그 (가장 심각)**
  ```js
  const hasData = frozen.length > 0 || fridge.length > 0 || room.length > 0;
  if (hasData) { ...저장... }   // ← 빈 배열이면 저장 자체를 건너뜀
  ```
  재료를 전부 지워도 저장을 하지 않아 서버엔 예전 목록이 남았고,
  **다음 로그인 때 지운 재료가 되살아났음** → 빈 상태도 정상 상태이므로 그대로 저장
- **② 저장 요청 순서 역전**: 연속으로 변경하면 요청이 겹치고, 늦게 출발한 요청이 먼저
  도착하면 옛 데이터가 최신을 덮어썼음 → 600ms 디바운스로 마지막 상태만 보내고,
  일련번호로 **오래된 응답은 무시**하도록 함
- **③ 실패해도 아무도 모름**: 자동 저장 실패 시 `console.error` 만 찍고 끝나서
  저장이 안 됐는데도 성공한 줄 알았음 (수동 저장 버튼이 생긴 원인)
  → 저장 상태를 화면에 표시(`저장 중… / 저장됨 / 저장하지 못했어요`)하고
  실패 시 **다시 시도** 버튼 제공
- 저장 버튼과 관련 온보딩 가이드 단계("저장버튼을 눌러 재료 정보를 저장할 수 있어요") 제거
- 검증: 저장 버튼 사라짐 확인, 냉동칸 재료를 전부 지운 뒤 저장값이 `frozen: 0` 으로
  반영되는 것 확인 (예전에는 저장 자체를 건너뛰던 경우)

### 피드백 반영 13차: 이모지 제거 · 드롭다운 떨림 · 포커스 톤 · 안내 문구
- **③ 섹션 표식 이모지 통일** (`ui/SectionIcon` 신설): 화면마다 표현이 달랐음 —
  내냉장고는 이모지(🧊 ❄️ 🌡️), 요즘인기는 ⭐ 를 제목 문자열에 섞어 넣고,
  마이페이지만 SVG 를 쓰고 있었음. 이모지는 기기·OS 마다 그림체와 크기가 달라
  같은 화면 안에서도 톤이 안 맞음 → 선으로 그린 SVG 7종으로 통일(색은 글자색을 따름)
- **④ 재료 입력 드롭다운 떨림 원인 발견·제거**: 스크롤바를 강제로 보이게 하려고
  드롭다운이 열릴 때 `scrollTop` 을 `3 → 0 → 2 → 0 → 1 → 0` 으로 튕기고,
  그것을 50ms·150ms·300ms 로 **세 번 반복 실행**하고 있었음 (총 15회 스크롤 조작)
  → 스크롤바는 CSS 로 표시하면 되므로 이 조작을 전부 제거하고 `.custom-scrollbar` 로 통일
- **② 입력창 포커스 톤 완화**: 진한 회색 테두리 + 링이 "굵어지고 칙칙하다"는 피드백 →
  테두리를 `--ink-400` 으로 한 단계만 진해지게 하고 링도 0.07 → 0.05 로 낮춤
  (키보드 탐색 시에는 여전히 더 또렷하게 유지)
- **⑤ '특별한 날 특별한 음식' 안내 문구 교체**: "값비싼 재료가 들어간 레시피를 모았어요" 는
  비싼 재료를 사게 하려는 의도가 그대로 드러나 보였음 →
  "기념일이나 손님상에 어울리는 레시피예요" 로 사용자가 얻는 것을 말하도록 변경

### 피드백 반영 14차: 전역 GNB 통일 (①)
- **탭마다 GNB 가 다르게 보이던 원인**: 일부 화면이 공통 GNB(`TopNavBar`) 위에
  **자기만의 고정 헤더를 하나 더** 그리고 있었음. `AppRouter` 가 이미 주요 경로에
  `TopNavBar` 를 렌더하는데, 아래 화면들이 `position: fixed; top: 0` 헤더를 또 만들어
  **정확히 같은 자리에 두 개가 겹쳐 있었음** (둘 다 높이 56px, 같은 z-index)
  - `MyPage`: 로고·닉네임·로그아웃까지 **공통 GNB 를 통째로 복제**.
    게다가 복제본은 `maxWidth: 400px` 이라 넓은 화면에서 정렬이 달라 보였음
  - `IngredientDetail`, `RecordedRecipeListPage`, `CompletedRecipeListPage`:
    뒤로가기(+필터) 전용 헤더가 GNB 와 겹침
- 수정
  - `MyPage` 의 복제 GNB 제거 → 공통 GNB 하나만 사용
  - 나머지 3개 화면은 고정 헤더를 없애고 **뒤로가기·필터를 본문 제목 줄로 이동**.
    (헤더를 top:56 으로 내리는 대신 제목 줄에 합쳐 세로 공간 56px 절약)
  - 헤더가 사라진 만큼 본문 상단 여백 88 → 72px 로 조정
- 검증: 5개 주요 화면 모두 고정 헤더 **1개**, 높이 56px, 동일한 하단 구분선으로 통일 확인

### 피드백 반영 15차: 빈 영역 · 세로 간격 · 스크롤 어려움 (⑥)
- **화면이 벙벙해 보이던 원인**: 가로형 카드에 쿠팡 광고 자리를 **항상 62px 예약**해 두고
  있었음(`HORIZONTAL_COUPANG_SLOT_MIN_HEIGHT_PX`). 그런데 `CoupangProductAd` 는
  링크가 없으면 아무것도 렌더하지 않으므로, 광고가 거의 안 뜨는 상태에서는
  **카드마다 빈 공간만 남아** 있었음 → 슬롯 높이 예약 제거, 내용이 있을 때만 자리 차지
- **세로 간격이 들쭉날쭉하던 원인**: 요즘인기 섹션 간격이 실측 `6 / 4 / 4 / 48px` 로
  제각각이었음(`marginTop: 4, marginBottom: 0` 등이 섞임) → **32px 로 통일**
- **스크롤이 어렵던 원인 (가장 큰 문제)**: 가로 캐러셀의 카드 래퍼에
  `touchAction: 'pan-x'` 만 지정돼 있어, **카드 위에서 시작한 세로 스와이프가 아무 데도
  전달되지 않아 페이지 스크롤이 막혔음.** 요즘인기 화면은 높이 278~286px 짜리 가로
  스크롤 영역이 3개나 되어 화면 대부분이 "스크롤 안 되는 구역" 이었음
  → `touchAction: 'pan-x pan-y'` 로 변경해 세로 스와이프는 페이지로 넘어가게 함
- 검증: 섹션 간격 `32/32/32/32`, 카드 래퍼 `touchAction: pan-x pan-y` 확인

### 피드백 반영 16차: 필터 시트 재설계 (⑦)
- **문제**: 조건이 `■ 채널선택` / `■ 꼭 포함할 재료` / `■ 꼭 제외할 재료` / `■ 테마 선택`
  네 덩어리로 나열돼 있었지만, 사이 구분이 얇은 회색 선 하나뿐이라
  **각 조건이 AND 로 함께 걸린다는 사실이 전혀 드러나지 않았음.**
  "채널을 고르면 테마가 무시되는 건가?" 같은 오해가 생길 수 있는 구조
- `components/ui/FilterGroup.tsx` 신설
  - `FilterGroup`: 번호 배지(①②③) + 제목 + 보조 설명. 묶음의 시작·끝이 눈에 보이게 함
  - `AndDivider`: 선 — **"그리고"** — 선. AND 결합을 **글자로 명시**. 구분선만으로는
    "또는" 인지 "그리고" 인지 알 수 없어서 텍스트로 못박음
- **적용 중인 검색어를 시트 맨 위에 읽기 전용 칩으로 표시**
  - 시트 안에 검색 입력창을 새로 넣지 않은 이유: 화면 상단 검색창과 필터가 **같은 값**
    (`includeKeyword`)을 조작하므로, 입력창을 하나 더 두면 한 값을 고치는 곳이 두 군데가 됨
  - 맨 위에 둔 이유: 검색어는 시트를 열기 **전에 이미 정한 조건**이라,
    "이 검색어에 더해서 좁힌다" 는 흐름이 먼저 읽혀야 함. 여기서는 확인과 해제(×)만 가능
- **채널 선택을 체크박스 → 칩 토글로 변경**: 16px 체크박스는 손가락으로 정확히 누르기
  어려웠음. 38px 높이 칩으로 바꾸고 선택 시 배경 반전 + `aria-pressed` 부여
- **재료 포함/제외를 한 그룹으로 통합**: 예전엔 별도 섹션이라 서로 무관해 보였으나
  실제로는 "재료 조건" 하나임. 그룹 안에서 소제목으로만 구분
- **입력창 규격 통일**: 12px 글자 / 30px 높이 → **16px 글자 / 44px 높이**.
  16px 미만은 iOS 에서 포커스 시 화면이 확대되는 문제도 있었음
- **테마 키워드 버튼**: 20px 남짓(`py-0.5`)이라 오탭이 잦았음 → 34px / 13px 로 확대,
  선택 상태를 노란 계열 대신 잉크 계열 반전으로 통일
- **`■` 문자 표식 제거**: 테마 대분류/중분류도 굵기·색으로만 층위를 구분하도록 변경
- 검증(390×800 실측): 시트 안 `■` 0개, "그리고" 구분선 3개, 검색어 칩 표시,
  입력창 44px/16px, 테마 버튼 34px/13px, 채널 칩 `aria-pressed=true` + 배경 반전,
  적용 시 시트 정상 종료

### 자동 배치가 한 번도 실행되지 않던 문제 수정
- 예약 작업 상태를 확인해보니 `CookMatch-DailyLLMIngredients` / `CookMatch-WeeklyCrawler`
  둘 다 **등록 이후 한 번도 성공하지 못한 상태**였음 (LastTaskResult=1, 즉시 종료).
  DB의 LLM 처리량도 5,260건에서 멈춰 있었음
- 원인: 두 `.bat` 파일이 **LF 개행으로 저장**돼 있었음. cmd.exe 는 배치 파일이 CRLF 인
  것을 전제로 파싱하기 때문에 캐럿(`^`) 줄바꿈이 깨져
  `'ng-only'은(는) 내부 또는 외부 명령이 아닙니다` 오류로 즉시 실패했음
  (`--pending-only` 인자가 중간에서 잘림). 예약 등록 자체는 정상이었기 때문에
  작업 상태만 봐서는 "Ready" 로 보여 놓치기 쉬운 문제였음
- 조치
  - 두 배치 파일을 CRLF 로 재저장
  - LLM 배치는 캐럿 줄바꿈에 의존하지 않도록 **인자를 한 줄로** 합침
  - `.gitattributes` 신설: `*.bat`, `*.cmd`, `*.ps1` 은 항상 CRLF 로 체크아웃되게 고정
    (git 이 개행을 바꾸면서 같은 문제가 재발하는 것을 막음)
- 검증: 예약 작업을 수동 실행해 45초 만에 100건 처리, 오류 0건 확인.
  다음 자동 실행은 매일 03:00

### 유통기한 추정 (⑧)
- **왜 필요했나**: 구매일자를 받아 저장은 하고 있었지만 그 값으로 아무것도 하지 않았다.
  사용자 입장에서는 "적어서 뭐 하려고?" 인 입력이었고, 실제로 유통기한을 직접 넣는
  사용자가 드물어 임박 재료 기능도 사실상 비어 있었음
- `utils/shelfLife.ts` 신설 — 재료 사전의 **세분류 → 소분류 → 중분류** 순으로 찾는
  보관 일수 표. 재료가 2,900개가 넘어 하나씩 채우는 것은 불가능하고, 채워도
  "삼겹살 3일 / 목살 3일" 처럼 같은 값이 반복되므로 카테고리 층위에 값을 붙였다
  - 세분류(가장 구체적): 잎채소 5일 / 뿌리채소 21일 / 육류 3일 / 생선 2일 / 달걀 30일 …
  - 보관 방법(냉동·냉장·실온)별로 값을 따로 둠. 육류·수산의 실온처럼 **권하지 않는
    조합은 값을 비워** 추정하지 않는다
  - **카테고리를 못 찾으면 추정하지 않는다.** 임의의 기본값을 씌우면 실제로는 한 달 가는
    재료에 D-7 이 붙어 멀쩡한 재료를 버리게 되므로, 틀릴 바엔 표시하지 않는 쪽을 택함
- 구매일자 팝업에 안내 문구 추가 — "구매일자를 입력하면 쿡매치가 재료 종류와 보관
  방법에 맞춰 대략적인 유통기한을 짐작해 계산해 드려요. 실제 표기된 기한과는 다를 수
  있어요." 무엇을 위해 묻는지 밝히지 않으면 그냥 귀찮은 입력이 된다
- 표시: 직접 넣은 유통기한은 `D-3`, 짐작한 값은 **`약 D-3`**. 추정값은 급해도 붉은색
  대신 옅은 주황으로 한 단계 낮춤 — 추정을 확정처럼 보여주면 안 되기 때문
- 저장 구조: `estimatedExpiry` 를 별도 필드로 둬서 사용자가 직접 넣은 `expiry` 와 섞지
  않음. 카테고리 표가 로드되면 **기존에 구매일만 있던 재료도 한 번 훑어 채운다**
- 임박 재료 정렬·목록(`RecipeSortBar`, `RecipeList`)에도 추정값을 포함.
  직접 넣은 것만 세면 임박 재료가 거의 항상 비어 기능이 동작하지 않았음
- 검증: 삼겹살 → 냉장보관 → 구매일 오늘 → `약 D-3` 표시 확인 (육류·냉장 3일)

### 피드백 반영 17차: 남은 이모지 · 영역 구분 · 마이페이지 구조
- **요즘인기에 남아 있던 이모지 제거**: 앞서 섹션 제목을 정리했지만 `📈 인기 급상승 요리
  TOP 10`, `📈 인기 급상승 테마 TOP 10`, `🔍️ 특정 재료·테마 등 키워드로 찾아보기` 세 곳은
  `SectionHeader` 를 쓰지 않고 생 `<h2>` 로 되어 있어 빠져 있었음 → 전부 `SectionHeader`
  + `SectionIcon` 으로 교체. `SectionIcon` 에 `trending`(오름세), `search`(돋보기) 추가
- **레시피 카드 순위의 🥇🥈🥉 제거**: 기기마다 그림체·크기가 달라 글자와 높이가 안 맞았음.
  순위 숫자 자체가 이미 정보라 장식을 덧붙이지 않음
- **제목 크기 통일**: 생 `<h2>` 들이 16px, `SectionHeader` 는 18px 이라 같은 층위의 제목이
  두 가지 크기로 섞여 있었음 → 전부 18px
- **"영역이 어디서 시작되는지 모르겠다" 의 실제 원인 세 가지**
  1. 구분선을 **제목 바로 아래**에 그어 놔서, 선이 "섹션의 시작" 이 아니라
     "제목과 내용의 분리" 를 뜻하고 있었음 → 제거
  2. 화면이 전부 흰색이라 섹션 사이 32px 여백이 그냥 빈 공간으로 보임
  3. `인기 급상승` 은 한 섹션 안에 두 덩어리가 **gap 48** 로 들어 있어,
     섹션 사이(32)보다 섹션 **안쪽**이 더 벌어진 역전 상태였음
  → `components/ui/SectionBand.tsx` 신설. 화면 좌우 끝까지 닿는 8px 옅은 회색 띠를
    섹션 맨 앞에 둔다(사이가 아니라 맨 앞에 두는 이유: 조건부로 안 보이는 섹션이 있어도
    구분이 어긋나지 않음). `인기 급상승` 은 두 개의 독립 섹션으로 분리
- **상단 타이틀 여백**: `header` 와 그 안 `h2` 가 각각 `marginBottom: 32` 를 걸어 64px 이
  비어 있었음 → 20px
- **빈 목록이 카드 한 장 높이(280px)를 그대로 비워 두던 문제**: 마이페이지처럼 세 목록이
  모두 비어 있으면 840px, 화면 한 장을 넘는 빈 공간이 생겼음 → 안내 문구 크기만큼만
  차지하게 하고 옅은 배경을 깔아 "비어 있는 목록" 임이 보이게 함 (마이페이지 전체 높이
  1,551 → 999px)

### 마이페이지 구조 재설계
- **문제**: 이 화면의 목적은 "내가 즐겨찾고·기록하고·완료한 레시피 보기" 인데
  - 위 130px + 아래 70px 을 비우고 가운데에 닉네임·이메일·버튼만 세로로 쌓아 둬서
    화면을 열면 **200px 짜리 빈 구역**이 먼저 보였고
  - 브랜드 강조색(노랑)이 하필 **'내 정보 수정'** 에만 칠해져 있어,
    가장 눈에 띄는 것이 이 화면에서 가장 덜 중요한 기능이었음 (위계 역전)
- 프로필을 **한 줄 카드**로 접음. 닉네임·이메일은 왼쪽, `정보 수정` 은 오른쪽 아웃라인
  버튼으로. 노란색은 비로그인 상태의 `로그인` 처럼 실제로 유도할 행동에만 남김
- **요약 줄 추가**: 즐겨찾기/기록/완료 개수를 세 칸으로 보여 주고 각각 전체 목록으로
  이동. 예전엔 스크롤을 끝까지 내려야 각 목록에 뭐가 몇 개 있는지 알 수 있었음
- **전체보기 `☰` → `전체보기 ›`**: 햄버거 기호는 '메뉴' 를 뜻해 '이 목록 전부 보기' 와
  뜻이 맞지 않고, 22px 글자 하나라 무슨 버튼인지 알 수 없었음
- 섹션 제목 3개를 `SectionHeader` + `SectionIcon`(favorite/recorded/completed)으로 통일

### 특별한 날 섹션 설명 문구
- `기념일이나 손님상에 어울리는 레시피예요` → `평소엔 잘 안 쓰는 재료로 만드는 요리예요`
  → 다시 `특별한 재료로 만드는 요리예요` (자주 쓰는 재료가 섞인 경우도 있어
  "평소엔 잘 안 쓰는" 이 사실과 어긋났음. 단정할 수 있는 만큼만 말한다)
- 기념일·손님상으로 못박으니 "그런 날" 이 아닌 사람에게는 남의 이야기가 됐음.
  실제로는 아이에게 잘 먹이고 싶은 날처럼 아무 날에도 보게 되는 목록이라,
  상황을 한정하지 말고 재료의 성격만 말하도록 바꿈

### 피드백 반영 18차: 요즘인기 기간 컨트롤 통일
- **문제**: 기간 컨트롤(최근 7일/30일/기간선택)은 아래 **모든 섹션에 함께 적용되는
  화면 전체 조건**인데, 화면에서는 아래 내용과 무관한 별도 영역처럼 보였음
- 실측으로 확인한 원인
  - **규격이 다른 탭과 어긋남**: 냉장고요리 탭의 같은 성격 컨트롤은 높이 **40px**,
    선택 시 **검정(#1A1A1E)** 인데, 요즘인기의 기간 칩은 높이 **28px**,
    선택 시 **회색(#6A6A73)** 이었음 → 이 화면만 다른 앱처럼 보이던 실제 정체
  - **라벨이 없고 오른쪽 끝에 정렬**돼 있어 무엇을 제어하는지 알 수 없었음
  - 17차에서 넣은 구분 밴드가 **첫 섹션 앞에도** 붙어, 컨트롤과 콘텐츠 사이를 끊었음
- 조치
  - 칩 규격을 냉장고요리와 동일하게(높이 40 / radius 6 / 선택 시 잉크색)
  - `기간` 라벨을 붙이고 왼쪽 정렬
  - 구분 밴드는 **첫 번째로 그려지는 섹션에는 붙이지 않도록** 함.
    밴드는 "앞 내용과 나뉜다" 는 뜻이라 맨 위에 오면 컨트롤을 갈라놓음
- 부수 정리: `특별한 날` 목록 계산이 JSX 안 즉시실행 함수에 들어 있어 (a) 렌더마다
  다시 돌고 (b) 바깥에서 이 섹션이 그려지는지 알 수 없었음 → `useMemo` 로 끌어올림
  (첫 섹션 판정에 필요했음)

### 로그인 세션 · 모달 층위 버그 수정

#### 소셜 로그인 시 닉네임 변경이 "유효하지 않은 토큰입니다" 로 실패하던 문제
- **먼저 확인한 것**: 백엔드는 소셜 로그인 사용자의 닉네임 변경을 **막지 않는다.**
  `provider='kakao'` 로 서명한 토큰으로 `/api/auth/update-profile` 을 직접 호출해
  200 응답을 확인함. (비밀번호만 `provider == 'local'` 일 때 반영되고, 프론트도
  소셜 로그인이면 비밀번호 입력을 이미 숨기고 있음)
- **실제 원인**: 토큰 자체가 서버에서 거부되고 있었음
  - `JWT_SECRET_KEY` 가 없으면 `app.secret_key = secrets.token_hex(32)` 로 떨어지는데,
    이 값은 **서버가 기동할 때마다 새로 만들어진다.** 즉 백엔드를 재시작하면
    이미 발급한 로그인 토큰이 전부 무효가 됨
  - 그런데 프론트는 토큰을 서버에 확인하지 않고 payload 만 base64 로 풀어 화면을 그린다.
    → **죽은 토큰을 들고도 로그인 상태로 보이고**, 인증이 필요한 동작에서만 실패
  - 사용자 입장에서는 "소셜 로그인이라 닉네임을 못 바꾸나?" 로 보였음
- 조치
  - `GET /api/auth/me` 신설 — 토큰 유효성 확인 + 최신 사용자 정보 반환.
    계정이 삭제된 경우(탈퇴)도 401 로 처리
  - 앱을 열 때 이 엔드포인트로 세션을 확인하고, 401 이면 조용히 로그아웃 처리.
    네트워크 오류는 로그아웃 사유로 삼지 않음(끊겼다고 로그아웃시키면 더 불편)
  - 프로필 저장에서 401 을 받으면 백엔드 문구 대신
    **"로그인이 만료되었어요. 다시 로그인해 주세요."** 를 띄우고 로그인 화면으로 보냄
  - `JWT_SECRET_KEY` 가 비어 있으면 서버 기동 시 경고 출력 (원인을 찾기 매우 어려운 형태라서)

#### 회원탈퇴 확인 팝업이 '내 정보 수정' 뒤에 숨던 문제
- 원인: `z-[calc(var(--z-modal) + 1)]` — **Tailwind 의 임의값(`[]`)은 공백을 포함할 수 없다.**
  공백 때문에 클래스가 아예 생성되지 않아 z-index 가 `auto` 였고,
  부모인 '내 정보 수정' 모달(z-index 600) 뒤로 밀렸음
  - 실측: 확인 팝업을 띄운 뒤 화면 한가운데를 hit-test 하면
    확인 팝업이 아니라 **정보수정 모달의 입력칸**이 잡혔음 (= 눌리지도 않는 상태)
- 조치
  - z 토큰에 `--z-modal-dim-nested: 640`, `--z-modal-nested: 650` 추가
    (토스트 700 아래라 토스트는 여전히 최상단)
  - 공용 `Dialog` 에 `nested` 옵션 추가 — 같은 z-index 끼리 DOM 순서로 이기는 방식은
    부모 모달이 어디에 렌더되느냐에 따라 깨지므로, 한 층 위로 올려 순서와 무관하게 만듦
  - 회원탈퇴 확인 팝업을 `<Dialog nested>` 로 교체 (규격도 함께 통일)
- 전수 점검: 나머지 고정 오버레이 12곳은 모두 z 토큰을 정상적으로 쓰고 있었고,
  공백이 들어간 Tailwind 임의값도 이 한 곳뿐이었음

#### 운영 API 주소가 프론트 자신을 가리키던 문제
- `frontend/.env.production` 의 `VITE_API_BASE_URL` 이 **프론트 자신의 주소**
  (`https://refrigerator-code.vercel.app`)로 되어 있었음. `vercel.json` 이 모든 경로를
  `index.html` 로 돌려보내므로 이 값으로 빌드되면 모든 API 호출이 HTML 을 받는다
  (실측: 해당 주소로 `POST /api/auth/update-profile` → **405**, JSON 아님.
  올바른 Railway 주소 → 401 JSON)
- Railway 주소로 수정. 점 없는 `env.production` 은 Vite 가 읽지 않는 문서용 사본이라
  이 사실을 파일 주석에 남김

### 쿠팡 광고 구조 재설계 (파트너스 정책 반영)
파트너스 이용 가이드(2024.12)를 확인한 뒤, 처음 잡았던 계획 중 **두 가지를 바꿨다.**

#### 바꾼 것 ① 고지 문구를 목록 **위**로 (원래 계획: 페이지 최하단 유지)
- 가이드 STEP 7 "경제적 이해관계 표시": 문구는 **"게시물의 제목 또는 첫 부분에 기재"**,
  "소비자가 쉽게 인식할 수 있도록", "모든 게시물에" 게재
- 기존에는 화면 맨 아래 1회였는데, 스크롤을 끝까지 내리지 않으면 보이지 않아
  "쉽게 인식" 요건을 만족한다고 보기 어려웠음 → 광고가 나오기 **전**, 목록 위쪽으로 이동
  (요즘인기 / 냉장고요리 / 즐겨찾기·기록·완료 목록 / 재료 상세 전부)
- 광고 카드와 구매 시트 안에도 짧은 문구를 함께 넣어, 그 화면만 보고 지나가도 보이게 함

#### 바꾼 것 ② 부족 재료 pill 을 눌러도 **바로 쿠팡으로 보내지 않는다**
- 가이드 STEP 7 "자동실행 금지": 광고 클릭은 **"사용자가 그 클릭을 의도했을 경우에만
  발생할 것"** 을 전제로 하고, 의도에 반하는 클릭 유도를 금지
- pill 은 화면에서 "이 레시피에 없는 재료" 를 알려주는 **정보 표시**로 보인다.
  누르는 사람은 재료 설명이나 대체재를 기대하지 광고 클릭을 의도했다고 보기 어려움
  → `CoupangAdSheet` 신설. pill 을 누르면 **광고를 보여주기만** 하고,
    쿠팡으로 나가는 클릭은 사용자가 시트 안에서 직접 하게 함
- (직전 대화에서 "pill 은 이미 광고를 띄우고 있다" 고 적었는데 사실이 아니었음.
  실제로는 `window.open` 으로 곧바로 이동하고 있었다)

#### 계획대로 진행한 것
- **레시피 카드 하단 CTA 제거**: 부족 재료 pill 과 같은 재료·같은 목적지라 중복이었고,
  CTA 유무에 따라 카드 높이가 달라져 가로 캐러셀의 고정 높이를 쓸 수 없었음
- **`CoupangAdCard` 신설** — 레시피 카드와 **같은 규격(300×280)** 의 광고 카드를
  목록 사이에 끼운다
  - 바로 앞 레시피의 부족 재료가 **1~3개**일 때만 (0개는 광고할 재료가 없고,
    4개 이상은 "몇 개만 사면 완성" 이 아니라 구매로 이어지지 않음)
  - 재료 선택은 레시피 id 기반이라 다시 그려도 결과가 바뀌지 않음
    (매번 달라지면 스크롤할 때마다 광고가 바뀌어 어수선함)
  - 첫 2장 뒤부터, 광고 사이 최소 4장 간격
  - 연결할 상품이 없으면 카드를 아예 렌더하지 않음 → 빈 카드가 남지 않음
  - 광고임을 카드 첫 줄에 명시(`광고` 배지 + `쿠팡 파트너스`)
- **마이페이지는 광고 카드를 끄기**(`showAds={false}`) — 즐겨찾기·기록·완료는 사용자가
  직접 모아 둔 목록이라 광고가 섞이면 목록의 성격이 흐려짐
- 클릭 측정에 `feed_card` 경로 추가 (기존 `pill` / `card_cta` 와 비교 가능)

#### 정책상 함께 확인한 것
- **커버형 배너 금지**: 광고 위에 "밀어서 확인" 류 문구나 조작 연상 요소를 덧씌우지 않음.
  캐러셀 좌우 화살표는 광고 이전부터 있던 목록 이동 장치이고 모든 카드에 동일 적용되며,
  세로 위치가 달라 광고 카드의 구매 버튼을 덮지 않음
- **플로팅 배너 금지**: 쿠팡 광고 중 화면에 고정(`position: fixed`)되는 것은 없음
- **지식재산권**: 쿠팡 로고/BI 이미지는 쓰지 않고 상호명만 글자로 표기

#### 곁들여 고친 카드 높이 문제
- 가로 카드의 `대체 가능` 줄이 길어지면 카드가 세로로 늘어나, 실측 높이가
  **228~324px** 로 벌어져 있었음. 목록이 잡아 둔 슬롯은 280px 이라
  **324px 짜리 카드는 아래가 잘려 나가고 있었음**
- 가로 목록은 훑어보는 자리이므로 대체재는 대표 1개만 보이고 나머지는 `외 N개` 로 표시
  (세로 목록·상세는 그대로 전부 표시)
- 결과: 가로 카드 높이 **228~280px**, 슬롯을 넘는 카드 없음 → 잘림 해소

### 피드백 반영 19차: 고지문 위치 재조정 · 기간 컨트롤 · 매칭률 설명

#### 고지문을 페이지 상단 → **광고 소재 자체**로
- 18차에서 정책(가이드: "제목 또는 첫 부분")에 맞춰 페이지 맨 위로 올렸는데,
  **광고가 하나도 없는 화면에서도 문구가 먼저 보여 서비스 전체가 광고처럼 읽혔다.**
  광고 없는 목록 위에 "수수료를 제공받습니다" 가 떠 있는 것은 정확하지도 않다
- → 광고가 실제로 나오는 자리마다 붙인다: 광고 카드 / 구매 안내 시트 / 하단 배너.
  각각의 **첫 부분**(구매 버튼보다 위)에 두므로 "누르기 전에 알 수 있어야 한다" 는
  취지는 그대로 지켜진다
- 문장은 `CoupangDisclaimer` 한 곳에서만 관리 (세 자리가 따로 문구를 갖고 있지 않게)
- `BottomCoupangAd`(다이나믹 배너)에도 문구를 붙임 — 예전엔 페이지 단위 고지문에
  기대고 있어서, 상단 고지문을 걷어내면 이 배너만 고지 없이 남을 뻔했음

#### 기간 컨트롤이 어중간해 보이던 문제
- 좌측 정렬만으로는 **가운데 정렬된 제목과 축이 어긋나** 어중간했고,
  바로 아래 섹션과 붙어 어디까지가 컨트롤인지 불분명했음
- → 옅은 판(`--surface-sub` + 1px 테두리) 위에 얹어 한 덩어리로 묶고,
  아래 섹션과의 간격을 20 → **36px** 로 넓힘. 칩 높이는 40 → 38

#### 매칭률이 어떻게 계산되는지 밝히기
- 매칭률은 이 앱의 핵심 지표인데 **어떻게 나온 숫자인지 어디에서도 말하지 않고 있었음**
- 넣을 자리로 스플래시 화면을 검토했으나, 첫 실행에 한 번 스쳐 지나가는 화면이라
  정작 궁금해지는 시점에는 이미 사라진 뒤다 → **`재료 매칭도 설정` 팝업 안**에 배치.
  사용자가 이 숫자를 만지러 오는 바로 그 자리라 맥락이 맞다
- 문구: "매칭률은 레시피 본문에서 뽑아낸 재료와 내 냉장고 재료를 비교해 계산해요.
  재료는 AI가 본문을 읽고 정리하기 때문에, 글쓴이가 적지 않은 재료는 빠질 수 있어요."
  (한계까지 함께 밝혀야 매칭률이 어긋날 때 사용자가 납득할 수 있다)

### 피드백 반영 20차: 카드 높이 통일 · 내부 표현 제거 · 매칭도 팝업 정리

#### 가로 카드 높이를 전부 같게
- 광고 카드에 고지 문구가 들어가면서 광고 카드(280px)와 레시피 카드(228~280px)의
  높이가 서로 달라 보였음. 그런데 원인은 광고 카드가 아니라 **레시피 카드 쪽**이었다 —
  재료 pill 이 몇 줄로 감기느냐에 따라 카드마다 높이가 제각각이었고,
  목록이 잡아 둔 슬롯(280px)과도 어긋나 있었음
- `RecipeCard` 에 `fixedHeight` 를 추가해 **가로 목록에서는 슬롯 높이와 카드 높이를 일치**시킴.
  내용이 적은 카드는 아래가 남지만, 높이가 들쭉날쭉한 것보다 훨씬 정돈돼 보인다
- 검증: 실측 123장 전부 **280px 단일값** (이전 228~280), 광고 카드도 280px

#### 광고 카드에서 "앞 레시피에 부족한 재료" 문구 제거
- 이 문장은 우리가 그 재료를 고른 **내부 규칙**을 드러낸 것이지 사용자에게 필요한 정보가 아님
- 사용자에게 필요한 건 "무엇에 대한 광고인가" 하나뿐 → 재료명만 남기고,
  위아래 여백을 똑같이 나눠 카드 가운데에 배치 (아래로만 밀면 사이가 텅 비어 보였음)

#### 재료 매칭도 팝업이 답답하던 문제
- 실측한 원인
  - **라벨이 하나도 없었음**: `30 % ~ 100 %` 두 칸이 무엇을 정하는지,
    아래 선택지가 무엇을 고르는 건지 화면에 적혀 있지 않았음
  - **두 설정이 붙어 있었음**: 슬라이더와 선택지 사이 **0px**,
    선택지와 적용 버튼 사이 **8px** → 서로 다른 설정이 한 덩어리로 보였음
  - **기본 라디오 버튼**: 지름 13px 남짓이라 누르기 어렵고, 브라우저 기본 파란색이라
    앱의 다른 컨트롤과 색이 따로 놀았음
- 조치
  - `매칭률 범위` / `부족해도 되는 재료` 소제목 추가
  - 두 설정 사이에 구분선 + 상하 22px 여백
  - 라디오 → 38px 칩 토글(앱의 다른 칩과 동일 규격), 선택 시 잉크색 반전
  - 적용 버튼 위 간격 8 → 24px, 높이 48px 로 통일
- 검증: 요소 간 간격 `15/16/10/14/22/22/9/24` (이전 `16/16/0/8/8`)

### 피드백 반영 21차: 임박 재료 설정 팝업

#### 추정 유통기한이 반영된다는 사실을 밝힘
- 질문: "짐작한 유통기한 데이터도 '유통기한 임박순' 계산에 들어가나?" → **들어간다.**
  18차에서 `RecipeSortBar` / `RecipeList` 의 임박 재료 목록에 `estimatedExpiry` 를 포함시켰음
  (직접 넣은 것만 세면 임박 재료가 거의 항상 비어 기능이 동작하지 않았기 때문)
- 그런데 화면에는 그 사실이 적혀 있지 않아, 넣은 적 없는 재료가 목록에 떠 있으면
  "내가 언제 이걸 입력했지?" 가 됨 → 팝업 상단에 도움말 추가
  "유통기한을 직접 넣은 재료뿐 아니라 **구매일만 넣어 둔 재료**도 재료 종류와 보관 방법으로
   기한을 짐작해 함께 계산해요. 짐작한 값은 **약 D-00** 으로 표시돼요."
- 도움말 상자는 매칭도 팝업과 같은 규격(`STYLES.helpBox`)으로 통일

#### 날짜 표기를 D-표기로
- `구매일 오래된순` 탭이 `2026.08.12` 같은 **절대 날짜**를 그대로 보여주고 있었음.
  며칠 지났는지 머릿속으로 빼야 해서 한눈에 안 들어옴 → 두 탭 모두 D-표기로 통일
  (구매일은 지난 날짜라 `D+10` 처럼 나옴)
- **기한이 지난 재료는 `지남` 으로 못박음**: 같은 `D+1` 이라도 유통기한 탭에서는
  "1일 지남", 구매일 탭에서는 "산 지 1일" 이라 뜻이 정반대여서 혼동됨
- `getDDay` 가 `yyyy.mm.dd`(점 표기)를 확실히 읽도록 대시로 정규화.
  브라우저에 따라 Invalid Date 가 되어 날짜가 그대로 노출될 수 있었음
- 검증(실측): 임박순 `지남 / D-2 / 약 D-2 / 약 D-6 / 약 D-20 / 약 D-115`,
  구매일순 `D+10 / D+8 / D+5 / D+3` — 정렬과 표기 모두 정상

#### AND/OR 선택을 칩 토글로
- 매칭도 팝업과 같은 문제(13px 기본 라디오 + 브라우저 기본 파란색)가 남아 있었음
  → 38px 칩 토글로 교체하고 `선택한 재료를` 소제목 추가.
  문구도 `모두 포함(AND)` → `모두 포함` 으로 줄임 (AND/OR 표기는 개발 용어)

### 피드백 반영 22차: 내냉장고 키보드 · 고지문 정리

#### 재료 선택 후에도 키보드가 안 내려가던 문제
- 이전에 "팝업을 열기 전에 포커스를 풀어 키보드를 내린다" 는 처리를 넣었는데도
  증상이 그대로였음. 코드를 보니 `blur()` 바로 **다음 줄에서 `inputRef.current?.focus()`**
  를 불러 **방금 내린 키보드를 도로 올리고 있었다.**
  (다음 재료를 이어서 입력하라고 넣은 코드인데, 팝업이 뜨는 흐름에서는 키보드가 팝업을 가림)
- 조치
  - `handleSelect` 끝의 `focus()` 제거 — 다음 입력을 위한 포커스는 팝업을 닫은 뒤에 준다
  - 드롭다운 항목의 `onClick` → `onMouseDown` 으로 변경.
    `onClick` 은 입력창의 `onBlur` 뒤에 실행돼 순서 경합이 생긴다
- 검증(실측): 선택 전 `document.activeElement = INPUT` → 선택 후 `BODY`,
  팝업 정상 표시 (포커스가 풀렸으므로 모바일에서 키보드가 내려감)

#### 광고가 없는데 고지문만 남던 문제
- 내냉장고 화면 아래에 "이 광고는 쿠팡 파트너스 활동의 일환으로…" 만 덩그러니 있었음
- 원인: 이 자리는 **다이나믹 배너**(`BottomCoupangAd`) 자리인데,
  `VITE_COUPANG_AD_UNIT_ID` 가 설정돼 있지 않아 배너 자체는 아무것도 그리지 않는다.
  그런데 고지 문구는 감싸는 쪽이 그리고 있어서 문구만 남았음
- → 광고 단위 ID 가 없으면 고지문을 포함해 통째로 렌더하지 않도록 수정.
  (광고가 없으면 고지할 것도 없다)

#### 고지 문구 줄바꿈
- 폭에 따라 아무 데서나 감기던 것을 쉼표 뒤에서 끊어 두 줄로 고정
  `이 광고는 쿠팡 파트너스 활동의 일환으로,` / `이에 따른 일정액의 수수료를 제공받습니다.`
- 문구가 `CoupangDisclaimer` 한 곳에만 있으므로 광고 카드·구매 시트·배너에 모두 반영됨

### 필터 시트: '꼭 제외할 재료' pill 간격
- 선택한 재료 pill 이 입력창에 바짝 붙어 나왔음. `꼭 포함할 재료` 입력창에는 `mb-2` 가
  있는데 `꼭 제외할 재료` 쪽만 빠져 있었음 → 동일하게 맞춤
- 검증(실측): 입력창~pill 간격 포함 8px / 제외 8px (이전 8 / 0)

### 피드백 반영 23차: 바텀시트 드래그 민감도 · 상단 여백

#### 시트가 너무 쉽게 닫히거나 붕 뜨던 문제
- 원인: 터치 핸들러가 **시트 전체**(`div[role="dialog"]`)에 걸려 있었음.
  그래서 본문 목록을 스크롤하려는 손짓까지 "시트를 끄는 동작" 으로 읽혀,
  필터처럼 내용이 긴 시트에서 목록을 내리려다 시트가 통째로 닫히거나 위로 떴음
- 조치: 드래그를 **두 갈래로 분리**
  - 손잡이·헤더에서 시작 → 언제나 끌기 (펼치기/닫기 모두)
  - 본문에서 시작 → **맨 위까지 스크롤된 상태에서 아래로 당길 때만** 끌기.
    위로 올리는 손짓은 "내용을 더 보겠다" 는 뜻이므로 스크롤에 양보
  - 14px 데드존 추가 — 손 떨림이나 스크롤 시작 동작에 시트가 따라 움직이지 않게
- 함께 고친 잠재 버그: 닫기 판정이 `dragY`(state)를 보고 있었는데,
  터치 이벤트가 한 프레임에 몰리면 리렌더 전이라 옛 값(0)을 읽어
  **빠르게 쓸어내려도 안 닫히는** 경우가 있었음 → 판정은 ref, 화면 표시는 state 로 분리
- 검증(iPhone 13 에뮬레이션, 실측)
  - 본문을 스크롤한 상태에서 아래로 쓸기 → 시트 유지 ✅
  - 손잡이에서 아래로 끌기 → 닫힘 ✅
  - 손잡이에서 위로 끌기 → 높이 544 → 631px 로 펼쳐짐 ✅

#### 시트 첫 항목이 헤더 구분선에 붙던 문제
- 본문 영역의 `padding` 이 `0 20px 16px` 로 **위쪽이 0** 이라,
  `채널 · 고르지 않으면 전체` 같은 첫 항목이 헤더 아래 선에 바짝 붙어 있었음
- → `18px 20px 16px` 로 위 여백 확보 (모든 바텀시트에 함께 적용)

### 필터: 같은 재료를 포함·제외 양쪽에 넣을 수 있던 문제
- `가지` 를 `꼭 포함할 재료` 에 넣고 `꼭 제외할 재료` 에도 넣을 수 있었음.
  그러면 조회 조건이 `가지가 있고 AND 가지가 없고` 가 되어 **결과가 항상 0건**이 되는데,
  사용자는 왜 아무것도 안 나오는지 알 길이 없었음
- 조치: 애초에 고를 수 없게 막는다
  - 자동완성 후보에서 **반대쪽에 이미 고른 재료도 함께 제외**
  - 자동완성 후보가 없을 때 입력값이 그대로 들어가던 Enter 경로도 차단하고,
    `'가지' 은(는) 제외 재료로 골라 뒀어요. 한 재료를 포함과 제외에 함께 넣으면
    결과가 하나도 나오지 않아요.` 안내를 3.5초간 표시
- 검증(실측): 포함에 `가지` 선택 → 제외 후보 목록에서 `가지` 사라짐(후보 0개),
  Enter 로 강제 추가 시도 → 안내 표시 + pill 추가 안 됨

### 쿠팡 광고 대상 재료 순위 추출
- `coupang_ads.csv` 를 **레시피 사용 빈도 기준**으로 다시 만듦 (44,471건 집계)
- 대상에서 뺀 것
  - 앱이 새 사용자에게 기본으로 채워 주는 재료 31종(소금·설탕·간장·마늘·양파 등)
    — 대부분 이미 갖고 있어 "부족 재료" 가 될 일이 드물다
  - 물·얼음처럼 살 대상이 아닌 것
- 상위 120종 + 기존 항목 = 194행. **이미 채워 둔 URL 은 덮어쓰지 않음**
- 참고용 컬럼 `recipe_count`, `rank` 추가 — 어느 재료부터 채우면 되는지 보이게.
  파서는 컬럼을 헤더 이름으로 찾으므로 컬럼이 늘어도 동작에 영향 없음
- `coupang_url` 이 빈 행은 파서가 건너뛰므로, 링크를 안 채운 재료는 광고가 뜨지 않음
- 광고 CSV 캐시 버전 1.0 → 1.1 (브라우저에 남은 옛 목록 무효화)
- 상위 10: 다진마늘 11,678 / 파 11,540 / 고추 10,332 / 참깨 6,913 / 액젓 6,223 /
  청양고추 5,929 / 가지 4,758 / 황다랑어 4,633 / 육수 4,585 / 오이 4,306

### AI 챗봇 검색 품질 개선 (A + B)

#### A. 키워드를 정렬에 반영 (가장 큰 원인)
- 예전: 냉장고에 재료가 있으면 정렬이 `match_rate DESC` 뿐이었고, 키워드는
  `HAVING (match_rate >= 25 OR 키워드일치)` 의 **OR 조건**이었음.
  매칭률 25% 이상이 13,869건이라 사실상 전부 통과했고, 정렬에 키워드가 안 들어가
  **"매운 거" 라고 말하든 말든 결과가 거의 같았음**
- 바꾼 것
  1. 키워드가 있으면 **관련도 0인 글은 제외** (OR → 필수 조건)
  2. 어디에서 걸렸는지에 따라 점수 차등: **제목 3 / 재료 2 / 본문 1**
     (제목에 있으면 그 주제를 다룬 글, 본문에 스친 것은 "안 맵게 하려면" 일 수도 있음)
  3. 정렬을 `관련도 × 10 + 매칭률` 로 — 주제가 맞으면서 만들 수 있는 것이 위로

#### B. 낱말 1개 → 여러 개 + 동의어 확장
- 예전에는 LLM 이 낱말 하나만 뽑아서, `매운` 으로 찾으면
  `얼큰`(1,606건) `칼칼`(3,317건) `청양`(9,710건) 이 쓰인 글을 통째로 놓쳤음
- 프롬프트를 바꿔 **1~5개**를 뽑게 하고, 같은 뜻으로 글에 쓰일 만한 말을 함께
  달라고 지시 (`매운 거` → 매운/매콤/얼큰/칼칼/청양고추).
  **LLM 호출 수는 그대로** — 한 번의 응답에서 배열로 받는다
- 옛 형식(`keyword` 단수)으로 답하는 경우도 함께 받아 준다

#### 속도 대책
- 본문(content)은 평균 2,000자라 LIKE 비용이 큼. 실측:
  낱말 5개를 전부 본문까지 보면 **4.93초**, 제목·재료만 보면 **0.32초**
- → **첫 번째 낱말만 본문까지** 보고 나머지는 제목·재료에서만 찾는다.
  첫 낱말이 가장 중요한 말이라 손해가 적음. 실측 **1.75~2.42초**

#### 결과 (실측, 냉장고 재료 12종 기준)
| | 상위 8건 중 관련 |
|---|---|
| 예전 방식 | **1건** |
| 새 방식 (낱말 1개) | 7건 |
| 새 방식 (동의어 5개) | **8건** |
| "국물 요리" | **8건** |

#### ⚠️ 확인된 운영 문제: Gemini 무료 한도를 배치와 챗봇이 나눠 씀
- 종단 테스트 중 `/api/chat` 이 LLM 실패 경로로 빠지는 것을 발견.
  Gemini 를 직접 호출해 보니 **429 — 무료 티어 일일 500회 초과**
- 원인: 재료 추출 배치가 하루 450회를 쓰도록 잡혀 있어 챗봇 몫이 50회뿐이고,
  그마저 소진된 상태. **둘이 같은 API 키를 쓴다**
- 검색 로직 자체는 `_search_recipes` 직접 호출로 검증했으나,
  LLM 이 낱말을 뽑아 주는 구간은 한도가 회복되는 내일 재검증 필요

### 내냉장고: 사진 인식 준비 중 안내 추가
- 검색창 옆에 카메라 버튼 추가 (영수증 버튼과 같은 40×40 규격).
  누르면 `재료 사진을 찍으면 알아서 인식해 담아 주는 기능을 준비하고 있어요.` 안내
- 영수증 버튼도 `alert` → 같은 토스트로 통일.
  `alert` 는 OS 창이라 앱 밖의 일처럼 보이고 확인을 눌러야만 사라짐
- 안내에 `showToast` 를 쓰지 않은 이유: 그 토스트는 '삭제 취소' 버튼이 함께 붙는
  형태라 단순 안내에 맞지 않음 → `showPrepNotice` 신설(3초 후 자동 사라짐)

### 문서 갱신: 요리 챗봇 · LLM 키 분리
- `PROJECT_OVERVIEW.md` 에 **요리 챗봇** 절 신설
  - 동작 순서(① 대화+재료 → ② LLM 이 낱말만 고름 → ③ 서버가 MySQL 검색 → ④ 응답)
  - **LLM 은 레시피 본문을 읽지 않는다**는 점을 명시. 답변은 검색 전에 만들어지므로
    "이 레시피는 이래서 추천해요" 같은 말은 구조상 불가능
  - 검색 규칙(제목 3 / 재료 2 / 본문 1, 관련도 필수 조건, `관련도×10 + 매칭률` 정렬)
  - 낱말 확장(1~5개, LLM 호출 수는 그대로)
  - 속도 실측표와, FULLTEXT 인덱스가 ngram 파서 없이 만들어져 한국어에 쓸 수 없다는 사실
- `ENVIRONMENT_SETUP.md` 에 **LLM 키 분리** 절 신설
  - 무료 한도 500회는 **API 키 단위**라 배치와 챗봇이 공유하면 챗봇 몫이 50회뿐
  - `GEMINI_API_KEY_CHAT` 을 넣으면 챗봇만 그 키를 씀 (없으면 기존처럼 공용)
  - 키 발급 절차 — **반드시 `Create API key in new project`** 로 만들어야 별도 한도를 받음
- `chat_service.py` 가 `GEMINI_API_KEY_CHAT` / `GROQ_API_KEY_CHAT` 을 먼저 보도록 수정.
  설정하지 않으면 동작이 전과 같으므로 지금 당장 넣지 않아도 문제 없음

### 챗봇 전용 LLM 키 적용 확인 · 내냉장고 입력 영역 정리

#### 챗봇 전용 키가 실제로 동작함을 확인
- `GEMINI_API_KEY_CHAT` 신규 키(새 프로젝트로 발급) → **정상 응답**
  (기존 `GEMINI_API_KEY` 는 배치가 다 써서 여전히 429 — 의도한 분리가 그대로 작동)
- **종단 검증**(격리 프로세스에서 `handle_chat` 직접 호출)
  - "매운 거 먹고 싶어" → 낱말 `['매운','매콤','얼큰','칼칼','고추장']`,
    결과 4건 전부 매운 요리 (매운 감자조림 / 매운 고추장 감자조림 …)
  - "따뜻한 국물 요리 추천해줘" → 낱말 `['국물','찌개','탕','전골','국']`,
    결과 4건 전부 국물 요리
  - 어제 한도 소진으로 확인하지 못했던 **LLM 의 동의어 확장 구간까지 검증 완료**

#### 영수증·카메라 버튼의 톤이 어긋나던 문제
- 영수증만 PNG 이미지(`영수증.png`)였고 카메라는 선으로 그린 SVG 라,
  나란히 두니 선 굵기와 농도가 달라 보였음
- → 영수증도 같은 규격(`stroke-width: 1.7`)의 선 아이콘으로 교체. 이제 둘 다 SVG
- 쓰이지 않게 된 `영수증.png` import 제거

#### 안내 토스트 글자가 잘리던 문제
- `white-space: nowrap` + 말줄임이라 안내문이 통째로 잘려 무슨 말인지 알 수 없었음
- → 줄바꿈 허용(`wordBreak: keep-all`), 폭을 `min(320px, 100vw - 40px)` 로.
  실측: 높이 120px, 잘림 없음

#### 입력창이 눌려 플레이스홀더조차 안 보이던 문제
- 한 줄 폭 360px 에 입력창 250 + 입력버튼 70 + 아이콘 40×2 + 여백 = **424px** 이라
  입력창이 밀려 있었음 (카메라 버튼이 추가되면서 더 심해짐)
- → 입력창을 `flex: 1 1 auto` 로 바꿔 남는 폭을 다 갖게 하고,
  입력 버튼 여백 18→14px, 아이콘 버튼은 `flexShrink: 0` 으로 고정
- 문구도 `추가할 재료명을 입력하세요` → `재료명을 입력하세요` 로 축약
- 검증(실측): 입력창 내부 폭 148px / 문구 필요 폭 120px → **다 보임**

#### 자동완성 목록에 더 있다는 것이 안 보이던 문제
- 모바일은 스크롤바가 **만졌을 때만** 잠깐 나타나는 방식이라, 손대기 전에는
  아래에 더 있다는 사실을 알 수 없었음 (CSS 로 상시 표시를 시도해도 OS 방식이 이김)
- → 목록 오른쪽 아래에 `72개 더 ⌄` 알약 표시. 끝까지 내리면 사라짐
- 만드는 과정에서 걸린 것 두 가지
  - 목록에 `position: absolute` 가 남아 있어 감싼 요소의 높이가 0이 되고
    표시가 목록 **위쪽**에 붙었음 → `relative` 로 변경
  - 목록에 `z-[10]` 이 걸려 있어 표시가 목록 **뒤로 깔려** 보이지 않았음 → `zIndex: 11`
  - 처음엔 흐려지는 띠(gradient)도 깔았는데 마지막 항목 글자가 씻겨 나가 읽기 어려워짐
    → 띠는 걷어내고 알약 표시만 남김. 위치도 가운데 → 오른쪽으로 옮겨 글자를 가리지 않게

### 챗봇 결과 다양성 · 기본 재료에 D-표기 예시

#### 같은 요리만 늘어서던 문제
- "매운 거 먹고 싶어" 결과 8건이 **전부 감자조림**이었음.
  레시피 글은 같은 요리를 여러 사람이 쓴 경우가 많아, 관련도와 매칭률이 비슷한 것들이
  한 요리로 몰린다. 같은 요리만 나오면 "추천" 으로서 값이 없다
- 조치: **한 요리는 최대 2건까지**(`MAX_PER_DISH = 2`)
  - 제목에서 요리 이름을 알아내는 데 재료 사전의 `대분류 = 요리이름` 994개를 씀.
    제목이 `매운 감자조림 만드는법 고기없이도 짱맛! 고추장 양념 …` 처럼 검색어 범벅이라
    사전 없이 글자만 비교해서는 같은 요리를 골라내기 어려움
  - **긴 이름부터** 대조해 `감자조림` 을 `조림` 으로 잘못 잡지 않게 함
  - 넘치는 것은 버리지 않고 뒤로 미뤄, 개수가 모자라면 그것으로 채움
    (결과가 줄어드는 것보다 겹치더라도 채우는 편이 낫다)
  - SQL 은 `SEARCH_LIMIT × 5` 로 넉넉히 가져와 걸러냄
    (WHERE/HAVING 스캔이 비용의 대부분이라 LIMIT 을 늘려도 속도 차이 없음 — 실측 2.2~2.5초로 동일)
  - 사전 파일을 못 찾아도 검색이 멈추지 않도록, 그때는 다양성 필터만 건너뜀
- 검증(실측)
  - `매운` → 감자조림 2 / 알감자조림 1 / 찜닭 1 / 고추장찌개 1 / 달걀조림 1 / 닭볶음탕 1 / 기타 1
  - `국물` → 닭볶음탕 2 / 김치찌개 2 / 닭도리탕 1 / 참치김치찌개 1 / 계란탕 1 / 기타 1
  - 실제 챗봇 화면에서도 감자조림 2건 뒤로 알감자조림·고추장찜닭·감자 고추장찌개가 이어짐

#### 기본 재료 두 가지에 구매일 예시
- 처음 앱을 열면 기본 재료 31종이 담기는데 날짜가 하나도 없어,
  **D-표기가 어떤 기능인지 알 수 없었음**
- `달걀`, `감자` 두 가지에만 구매일(= 앱을 처음 연 날)을 넣음 → `약 D-30`, `약 D-21`
- 전부 넣지 않은 이유: 화면이 D-표기로 뒤덮이고, 앱을 오래 안 열면 전부 `지남` 으로
  바뀌어 고장난 것처럼 보임
- 유통기한이 아니라 **구매일**을 넣은 이유: 사용자가 직접 넣지도 않은 유통기한을 채우면
  거짓 정보가 됨. 구매일은 사실이고, 거기서 짐작한 값은 `약 D-30` 처럼 추정임을 밝혀 표시됨
- `달걀`(냉장 30일)·`감자`(21일)를 고른 이유: 한동안 의미 있는 숫자가 보임.
  `우유`(7일) 같은 것은 일주일 만에 `지남` 이 되어 예시로 부적절
- 검증(첫 방문 상태 실측): `달걀 약 D-30`, `감자 약 D-21` 두 개만 표기됨

## 2026-08-27

### AI 챗봇: 광범위 질문 처리 + 조미료 매칭 제외

#### 문제 제기
"있는 재료로 뭐 해먹을 수 있어?" 처럼 넓게 물으면 챗봇이 냉장고 재료를 제대로
반영하지 못하고 "멍청하게" 대답한다는 지적. 원인을 짚어 보니:
- LLM 은 레시피를 직접 고르지 않고 검색용 `keywords` 만 뽑는다(`_build_prompt`).
  그런데 `keywords` 가 하나라도 있으면 `_search_recipes` 의 `HAVING relevance > 0`
  이 강제로 걸려서, 그 낱말과 무관한 관련도 필터링이 우선시되고 정작 냉장고
  매칭(`match_rate`)은 뒷전으로 밀린다.
- 프롬프트엔 "매운 거"/"국물" 같은 **구체적 취향 요청**의 낱말 확장 예시만 있고,
  이런 **광범위 요청**(keywords 를 반드시 비워야 하는 경우)에 대한 규칙·예시가
  없어서, 모델이 "재료"/"추천" 같은 낱말을 지어내 위 필터를 걸어 버릴 위험이 있었음.
- `reply` 는 검색 **전에** 쓰여진다(LLM 은 레시피 본문을 안 읽는 구조). 그래서
  "이 레시피는 이래서 추천해요" 식 답변은 애초에 구조적으로 불가능함.
- 매칭률이 재료 **개수 비율**이라, 소금·후추 같은 조미료도 새우·소고기 같은
  메인 재료와 똑같은 가중치를 가진다 — "재료 우선순위를 모르는 것 같다"는
  두 번째 지적의 실체.

프롬프트만으로는 세 번째(구조적 reply 한계)와 네 번째(매칭률 가중치)는 해결이
안 돼서, 아이디어를 여러 개 늘어놓고 사용자와 상의해 아래 3가지 조합으로 진행:
**A2(패턴으로 광범위 질문 감지) + B4(검색 후 템플릿 답변) + C6(조미료 매칭 제외, 가벼운 버전)**.

#### 조치
1. **`_is_broad_fridge_request()` 신설** — "뭐 해먹", "뭐 만들", "추천해줘" 류의,
   취향·재료·요리명 없는 순수 광범위 질문을 정규식으로 감지한다. 오탐(구체적
   요청을 광범위로 오판)이 더 위험하므로 패턴을 일부러 좁게 잡았다 — 여기 안
   걸리는 표현은 그냥 기존처럼 LLM 이 처리한다(기능 후퇴 없음).
2. 광범위 질문으로 판정되면 **LLM 호출 자체를 건너뛰고** `keywords=[]` 로 확정해
   순수 냉장고 매칭(`match_rate DESC`) 검색으로 보낸다. 호출을 안 하니 하루
   LLM 한도(`_consume_quota`)도 쓰지 않는다.
3. 검색이 끝나 **결과를 알고 난 뒤**, 서버가 템플릿으로 답변 문구를 짓는다
   ("냉장고에 있는 재료 기준으로 만들 수 있는 레시피 N개를 찾았어요..."). LLM 경로의
   "검색 전에 답변부터 써야 하는" 구조적 한계를, 이 경로에 한해 결과를 본 뒤
   문구를 만드는 방식으로 우회했다.
4. **`COMMON_SEASONINGS`**(소금·후추·설탕·식용유·참기름·들기름·맛술·미림·식초·물)
   10종을 매칭률 계산의 분모·분자 양쪽에서 제외했다(`_search_recipes`, SQL에서
   `REPLACE` 체인으로 해당 재료 토큰만 지운 문자열로 계산). 고추장·된장·고춧가루처럼
   요리 정체성을 결정하는 양념은 일부러 빼지 않음 — "매운" 검색이 고추장 매칭에
   기대는 부분이 있어서, 여기까지 빼면 기존 다양성 검증(8/26)이 흔들릴 수 있음.

#### 검증 (실측, 운영 DB 대상 — 격리 프로세스에서 `handle_chat` 직접 호출)
| 입력 | 보유 재료 | is_broad | LLM 한도 소모 | 결과 |
|---|---|---|---|---|
| "있는 재료로 뭐 해먹을 수 있어?" | 돼지고기,양파,대파,소금,후추,식용유,고추장 | True | 0 (250→250) | 8건, 템플릿 답변 |
| "추천해줘" | 배추,두부,멸치,소금 | True | 0 (250→250) | 8건, 매칭률 100/67/60/50/50% 로 분산 |
| "매운 거 먹고 싶어" | 감자,고추장,대파 | False | 1 (250→249) | 기존처럼 LLM 경유, keywords: 매운/매콤/얼큰/칼칼 |
| "돼지고기로 뭐 해먹을 수 있어?" | 돼지고기,김치 | False | 1 (249→248) | LLM 경유. "감자,소금,후추"만 필요한 레시피가 100% 매칭으로 올라와 조미료 제외 효과 확인 |

#### 범위
챗봇(`backend/chat_service.py`)에만 적용. 냉장고요리·요즘인기 등 다른 화면의
매칭률(`frontend/src/utils/recipeUtils.ts`)은 그대로다. 앱 전체로 확장(사전의
`중분류`를 이용한 정식 가중치 등)은 이번 범위 밖 — 필요하면 별도로 진행.

### AI 챗봇: 답변에 "어떤 재료를 봤는지" 밝히기 + 대화 이어받기

#### 문제 제기
위 개선 이후에도 "다이어트 중인데 뭐먹지?" 같은 질문에 "냉장고에 있는 재료들로
가볍게 만들기 좋은 요리들을 골라보겠습니다" 처럼 뭉뚱그린 답만 온다는 지적.
어떤 재료를 근거로 골랐는지, 어떤 기준으로 정렬했는지가 답변에 없어서였다.
(이 질문 자체는 `_is_broad_fridge_request` 에 안 걸려 정상적으로 LLM 경유 —
"다이어트" 라는 주제가 있는 요청이라 keywords 를 비우면 오히려 손해라 의도한 동작.
문제는 LLM 경로의 reply 라는 게 드러남)
추가로, 이어지는 대화에서 "그중 A는 빼고 B는 넣어서 다시 줘" 처럼 이전 요청을
수정하는 후속 질문에 자연스럽게 이어 답했으면 한다는 요청. (사용자별 하루
질문 한도는 필요하다고 언급했으나, 테스트 단계라 이번엔 반영하지 않음 — 명시적 보류)

#### 조치
1. **`_top_matched_ingredients()` 신설** — 검색 결과 상위권에 실제로 겹친 보유
   재료를 빈도순으로 추린다. `is_broad`/LLM 두 경로 모두, 검색이 끝나 결과를
   안 뒤에 이 재료 이름들을 답변에 붙인다 — "가지고 계신 재료 중 OO, OO 위주로
   골랐고, 매칭률 높은 순으로 정렬했어요." LLM 경로는 LLM이 쓴 문장 뒤에
   덧붙이는 방식이라(LLM은 검색 전에 답변을 써서 이 사실을 알 수 없음), 기존
   장점(자연스러운 톤)은 유지하면서 구체성만 서버가 보강한다.
   - 방금 사용자가 "빼달라"고 한 재료(`exclude_ingredients`)는 이 집계에서도
     제외한다. 안 그러면 "두부 말고 계란으로 줘" 라고 했는데 "두부 위주로
     골랐다"고 답하는 모순이 생김(실제로 테스트 중 발견해서 수정).
2. **대화 이어받기 프롬프트 규칙 추가** — `_build_prompt` 에 "직전에 네(도우미)
   답변이 있으면 새 메시지를 이전 요청에 대한 수정으로 보고, 사용자가 취소하지
   않은 조건은 이어서 반영해라" 규칙 추가. 프론트(`RecipeChatWidget.tsx`)가
   이미 전체 대화(`nextMessages`)를 매 요청마다 보내고 있어서(세션 내 기억
   자체는 기존 구조로 이미 가능), 이번 조치는 그 대화를 "매 턴 새 질문처럼"
   보지 말고 이어서 보라는 프롬프트 지시만 추가한 것.
   - **실측 중 발견한 버그**: 규칙 설명에 든 예시 문장에 구체적 재료명("두부",
     "계란")을 그대로 썼더니, 사용자가 진짜로 "두부 요리 추천해줘" 라고 **첫
     메시지**를 보냈는데도 모델이 예시 속 가상의 "이전 요청"을 실제 대화로
     착각해 엉뚱하게 계란 레시피를 찾아버림(이전 대화가 전혀 없었는데도).
     예시의 재료명을 A/B 같은 자리표시자로 바꾸고, "지금이 첫 메시지면 이
     규칙은 해당 없다"는 문장을 명시적으로 추가해 해결. → **프롬프트에 넣는
     예시가 실제 사용자 입력과 겹치는 낱말을 쓰면, 특히 싼 모델일수록 예시를
     실제 맥락으로 착각할 수 있다**는 걸 확인함.

#### 검증 (실측, 운영 DB 대상 직접 호출)
| 시나리오 | 확인 내용 |
|---|---|
| "다이어트 중인데 뭐먹지?" (닭가슴살,브로콜리,계란,양파 보유) | keywords: 다이어트/저칼로리/식단/건강식. 답변 끝에 "가지고 계신 재료 중 닭가슴살, 양파, 브로콜리 위주로 골랐고, 매칭률 높은 순으로 정렬했어요." 추가됨 |
| "두부 요리 추천해줘" (첫 메시지, 두부/계란/양파/김치 보유) | 버그 수정 전: 계란 레시피로 엉뚱하게 답함. 수정 후: keywords 전부 두부 관련, 그라운딩 문구도 "두부, 김치, 양파" |
| → 이어서 "두부 말고 계란으로 다시 줘" | keywords가 계란/달걀 관련으로 정확히 전환. 그라운딩 문구에서 "두부"가 (요청대로) 빠지고 "양파, 김치"만 남음 |
| 기존 4개 시나리오(광범위 2건 + 구체적 2건) 재검증 | 전부 그대로 통과, 답변에 그라운딩 문구만 자연스럽게 추가됨 — 회귀 없음 |

#### 반영하지 않은 것
사용자별 하루 질문 한도(예: 10회) — 테스트 단계라 이번엔 보류. 실제 운영 전환
시점에 별도로 반영 필요.

### 잡다한 개선: 유통기한 퀵버튼 · 토스트 줄바꿈 · 임박재료 탭 통합 · 챗봇 유통기한 필터

- `IngredientDateModal`: 유통기한 퀵버튼에 3개월/6개월/1년 뒤 추가(기존 3일/1주일/2주일/1개월)
- `RecipeToast`: `multiline` + 좁은 `maxWidth`(260px) 조합 때문에 "레시피를 즐겨찾기에
  추가했습니다!" 같은 한 줄짜리 문구도 줄바꿈돼 두 줄로 보이던 문제 수정
- 냉장고요리 "임박 재료" 모달의 "유통기한 임박순"/"구매일 오래된순" 두 탭을 하나로 통합.
  구매일을 넣으면 그 자리에서 `estimatedExpiry`가 함께 계산·저장되므로(`MyFridge.tsx`),
  구매일이 있는 재료는 이미 유통기한 임박순 목록에도 다 들어와 있었음 — 사실상 같은 걸
  두 기준으로 보여주기만 했던 탭이었다. `expirySortType` 관련 상태·prop을 `RecipeSortBar`와
  이를 쓰는 5개 페이지에서 전부 정리(`RecipeList`/`Popular`/`IngredientDetail`/
  `RecordedRecipeListPage`/`CompletedRecipeListPage`)
- `RecipeChatWidget`: 챗봇에 보내는 냉장고 재료 목록에서 유통기한(직접 입력 또는
  구매일로 짐작한 값)이 지난 재료를 제외 — 못 쓸 가능성이 높은 재료를 "보유 재료"로
  추천에 반영하지 않도록. 매칭률 계산(`recipeUtils.ts`)에는 적용하지 않음 — 요청 범위가
  "LLM 돌릴 때"로 한정돼 있어 앱 전체 매칭 로직까지는 건드리지 않았다

### 재료 매칭률 가중치 (조미료 낮게 / 나머지 재료 높게) — 앱 전체 적용, 성능 제약 발견

#### 요청과 시도
지난번 챗봇 개선 때 "조미료는 매칭 계산에서 아예 뺀다"(0 또는 1의 하드 제외, 10종
하드코딩)로 임시 처리했던 것을, 이번엔 "정식 버전"으로 — 재료 사전의 기존 분류
(`대분류=재료`, `중분류=양념/조미료`, 287개)를 그대로 써서 **완전 제외가 아니라
가중치(SEASONING_WEIGHT=0.3 vs CORE_WEIGHT=1.0)** 로 반영하고, 챗봇뿐 아니라
냉장고요리·요즘인기 등 **앱 전체 매칭률 표시**에도 적용해 달라는 요청.

#### 막힌 지점 (실측)
287개 전체를 챗봇 검색 SQL에 그대로 쓰니 **응답이 25~27초**까지 걸렸다(정상은
2초 안팎). 원인을 좁히려고 두 가지 SQL 방식을 다 실측했다:
- 조미료만 지운 문자열을 만드는 REPLACE 체인(287번 중첩) → 27초
- 조미료 287개 각각 FIND_IN_SET 으로 세어 합산 → 25초

**두 방식이 거의 같은 시간이 나왔다** — 즉 REPLACE 냐 FIND_IN_SET 이냐가 문제가
아니라, **행마다 287개 낱말을 대조하는 연산 자체가 44,504건짜리 테이블(냉장고
우선 검색은 키워드로 좁혀지지 않아 대부분을 훑음)에서 감당이 안 되는 비용**이었다.
목록 크기를 줄여 가며 재보니: 50개 5.4초, 20개 3.1초, 15개 2.9초, 10개 2.1초로
**목록 크기에 거의 선형으로 비례**했다.

#### 조치
287개 사전 전체 대신, 실측으로 성능이 확인된 **15종을 직접 골라 하드코딩**
(`backend/chat_service.py`의 `_load_seasoning_set()`): 소금·후추·설탕·식용유·
참기름·들기름·맛술·미림·식초·물·간장·올리고당·굴소스·다시다·미원. 가중치는
SEASONING_WEIGHT=0.3 / CORE_WEIGHT=1.0 로 적용(완전 제외가 아니라 낮게만).
- `frontend/src/utils/recipeUtils.ts`의 `calculateMatchRate()`(냉장고요리·
  요즘인기·마이페이지 등 카드에 표시되는 매칭률)에도 **같은 15종·같은 가중치**를
  똑같이 반영 — 챗봇과 앱 다른 화면의 매칭률이 서로 다른 기준으로 보이면 안 되므로,
  두 파일의 목록은 반드시 같이 맞춰야 한다(주석으로 상호 참조 남김)
- 프론트는 재료 하나당 계산이라 287개를 다 써도 성능에 문제가 없다. 그런데도 15개로
  맞춘 이유는 순전히 챗봇 SQL의 한계 때문 — 두 곳의 숫자가 갈리는 게 더 나쁘다고 판단

#### 검증 (실측, 운영 DB 대상)
| 시나리오 | 결과 |
|---|---|
| "매운 거 먹고 싶어" (감자,고추장,대파 보유) | "매운 고추장 감자조림"(감자,고추,고추장) → 67%(2/3, 고추만 없음). "매콤한 감자조림"(재료 12개, 조미료 다수) → 26%로 낮게 — 조미료 비중이 큰 레시피가 과대평가되지 않음 |
| "돼지고기로 뭐 해먹을 수 있어?" (돼지고기,김치 보유) | "목살 김치찜"(김치,돼지고기만 필요) → 100%, "삼겹살 제육볶음"(재료 5개, 조미료 섞임) → 40% — 실제 부담과 매칭률이 더 잘 맞음 |
| 응답 시간 | 5개 시나리오 1.9~3.6초 — 기존 수준(2초 안팎) 유지 확인 |
| 프론트 타입체크(`tsc --noEmit`) | 에러 0건 |

#### 남겨둔 것 (범위 밖)
- **`backend/app.py`의 `/api/recipes/filter`** — 냉장고요리·요즘인기의 서버 쪽
  정렬(`sort_by=match_rate`)에 쓰이는 **세 번째** 매칭률 구현. 이미 REGEXP 배치 등으로
  한 차례 성능 최적화가 되어 있는 코드라, 같은 가중치를 넣으려면 여기서도 동일한
  성능 검증이 필요하다 — 이번 범위에서는 손대지 않았다. 그 결과 **카드에 표시되는
  매칭률(가중치 적용됨)과 서버가 정렬한 순서(가중치 미적용)가 서로 다른 기준일 수
  있다** — 목록이 카드 자신의 매칭률 내림차순과 정확히 일치하지 않게 보일 가능성.
  다음에 이어서 처리 필요
- 287개 전체를 챗봇 SQL에서도 쓰려면, 지금처럼 매 요청마다 실시간 계산하는 대신
  `recipes` 테이블에 가중치 적용 값을 **미리 계산해 컬럼으로 저장**(배치로 한 번
  돌리고 이후 크롤러/LLM 배치 때 갱신)하는 구조 변경이 필요 — 더 큰 작업이라 이번엔
  하지 않음

### 요즘인기: "특별한 날" 레시피 액션 실패 + 즐겨찾기/완료/기록 취소 확인 누락

#### 문제 제기
"냉장고요리에서는 즐겨찾기가 잘 되는데 요즘인기에서는 마이페이지에 반영이 안
된다", "완료·기록도 요즘인기에서는 작동을 안 한다", "즐겨찾기 취소할 때 완료
버튼처럼 확인 팝업이 떠야 하는데 없다"는 세 가지 지적.

#### 원인 (실측으로 확인)
`Popular.tsx`의 `handleRecipeAction`이 클릭된 레시피를 찾을 때
`youtubeRecipes.find(...) || naverRecipes.find(...)` 두 배열만 봤는데, 화면엔
**세 번째 배열 `premiumRecipes`**("특별한 날 특별한 음식" 섹션)로 렌더링되는
카드도 있었다. 그 섹션 카드에서 버튼을 누르면 `recipe`가 `undefined`가 되어
버튼 하나로 통합된 로직의 조건문을 통과하지 못하고 **에러도 토스트도 없이
조용히 아무 일도 안 일어남**.
- 실측: 완료 버튼 26개(요즘인기 전체)를 인덱스별로 눌러 재현 — 특별한 날
  섹션에 해당하는 6개(20~25번)만 전부 실패, 나머지 유튜브/네이버 섹션은 정상
- "요즘인기에서만" 이라는 제보와 정확히 일치 — 냉장고요리(`RecipeList.tsx`)는
  레시피 배열이 하나뿐이라 이 문제 자체가 생길 수 없는 구조였음

즐겨찾기 취소 확인 팝업은 실제로 마이페이지(`MyPage.tsx`, `IngredientDetail.tsx`)
에는 이미 있었다(즐겨찾기 포함). 냉장고요리·요즘인기 두 페이지만 애초에 이
패턴이 없어서, 완료·기록·즐겨찾기 전부 눌러서 켜진 걸 다시 누르면 확인 없이
바로 꺼졌다.

#### 조치
- `Popular.tsx`: 레시피 조회 4곳(favorite/done/write/share) 전부
  `|| premiumRecipes.find(r => r.id === id)` 추가
- `RecipeList.tsx`, `Popular.tsx` 둘 다에 마이페이지와 같은 취소 확인 팝업
  ("레시피 OO를 취소하시겠어요? [아니요][네]") 추가. `Popular.tsx`는 즐겨찾기/
  완료/기록 세 곳에 거의 똑같이 반복되던 취소 로직(로컬스토리지 삭제 + 로그인
  시 DB 동기화 + 상태 갱신 + 토스트)을 `performRemoval()` 하나로 묶어서 확인
  팝업의 "네"에서 호출하도록 정리

#### 검증 (실측)
- 요즘인기 "특별한 날" 섹션 카드에서 완료(id 169608 저장 확인)·즐겨찾기 둘 다
  정상 동작 확인
- 즐겨찾기 추가 → 재클릭 시 즉시 삭제되지 않고 확인 팝업이 뜨며, 이 시점엔
  로컬스토리지에 그대로 남아 있음을 확인 → "네" 클릭 시 정상 삭제됨. 냉장고요리
  쪽도 완료 기준으로 동일하게 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### app.py `/api/recipes/filter` 에도 재료 가중치 적용 (남겨뒀던 세 번째 구현체)

앞서 "범위 밖으로 남겨둔 것"으로 적어둔 `/api/recipes/filter`(냉장고요리·요즘인기
서버 정렬용) 에도 챗봇·`recipeUtils.ts`와 같은 가중치를 적용했다. 카드에 보이는
매칭률과 서버가 정렬한 순서가 다른 기준이면 안 된다는 판단.

이 엔드포인트는 사용자의 냉장고 재료(최대 40개, 작음)만 반복하므로 챗봇이
겪은 "287개 조미료를 테이블 전체에 대조" 같은 병목은 없다. 다만 실측해보니:
- 가중치 적용 전 기준: 2.0초
- 그대로 적용: 5.1초 — `match_rate_expr`의 바깥쪽 `CASE WHEN 분모=0` 검사가
  무거운 조미료 제외 문자열을 두 번 평가하게 만들었음
- 그 중복 검사를 `used_ingredients IS NULL OR ''` (원래 있던 가벼운 체크)로
  바꿔 한 번만 평가하도록 최적화 → **3.5초**

2.0초 대비 3.5초는 냉장고에 재료가 있는 모든 사용자가 냉장고요리·요즘인기를
열 때마다 매번 겪는 지연이라 사용자에게 트레이드오프를 그대로 알리고 선택을
물었다 — **일관성 우선(+1.5초 감수)** 으로 결정, 적용함.

더 줄이려면(범위 밖으로 남김): `recipes` 테이블에 가중치 적용 값을 미리 계산해
컬럼으로 저장하는 구조 변경이 필요 — 위 챗봇 항목과 동일한 근본 해법.

### 서비스워커가 배포된 새 코드를 계속 못 받게 막고 있던 문제 (중요)

#### 문제 제기
요즘인기의 즐겨찾기/완료/기록 버그를 고쳐서 배포했다고 안내했는데, 사용자는
"여전히 마이페이지에 반영이 안 된다"고 재현. 그런데 로컬에서 직접 재현해보면
정상 동작함 — 코드 문제가 아니라 **사용자 브라우저가 새로 배포된 코드를 아예
받지 못하고 있을 가능성**을 의심하고 `public/sw.js`를 확인했다.

#### 원인
`sw.js`의 캐시 전략이 **정적 리소스(HTML 포함)를 "캐시 우선"** 으로 서빙하고
있었다:
- `CACHE_NAME = 'cookmatch-v1.0.0'` 가 프로젝트 시작 이후 한 번도 안 바뀜 →
  `activate` 핸들러의 "이전 캐시 정리" 로직이 조건부(`cacheName !== CACHE_NAME`)라,
  이름이 안 바뀌면 정리도 안 걸림
- `/`(진입 HTML)를 포함한 모든 정적 요청을 캐시에 있으면 **네트워크를 아예 안
  보고** 캐시된 것을 그대로 반환. 한 번 방문(또는 PWA로 설치)한 브라우저는
  Vercel에 새 빌드가 올라가도 계속 예전 `index.html`(예전 빌드의 JS/CSS 해시
  경로를 가리킴)을 받아서, **이번 세션에서 커밋·배포한 수정 사항이 그 브라우저엔
  하나도 반영되지 않고 있었을 가능성**이 있다
- 참고로 `frontend/public/workbox-config.js`(`StaleWhileRevalidate` 등 더 나은
  전략 포함)와 `npm run generate-sw` 스크립트가 따로 있었지만, 실제 배포에 쓰는
  `npm run build`(`vite build`)는 이 스크립트를 안 부른다 — 즉 이 설정은 애초에
  한 번도 실제 배포에 반영된 적이 없는 죽은 설정이었고, 배포되는 건 항상 순수
  `public/sw.js` 원본이었다

#### 조치
- `CACHE_NAME`을 `v1.0.1`로 올려 기존 캐시를 정리하도록 함(당장의 staleness 해소)
- HTML 문서 요청(`mode: 'navigate'` 또는 `destination: 'document'`)만 **네트워크
  우선**으로 전략 변경 — 온라인이면 항상 최신 문서를 받고, 오프라인일 때만 캐시로
  폴백. 해시된 JS/CSS/이미지는 그대로 캐시 우선 유지(Vite가 내용이 바뀌면 파일명도
  바꾸므로 안전)

#### 남겨둔 것
`CACHE_NAME`을 배포마다 손으로 올려야 하는 구조는 여전히 남아 있다(까먹기 쉬움).
근본적으로는 `workbox-config.js`를 실제 빌드 파이프라인에 연결하거나, Vite PWA
플러그인(`vite-plugin-pwa`)으로 교체해 빌드마다 자동으로 캐시가 무효화되게
하는 게 맞다 — 이번엔 당장의 문제만 고치고 범위 밖으로 남김.

#### 사용자에게 필요한 조치
이미 접속해 있던 브라우저/PWA는 새 서비스워커를 받을 때까지 여전히 예전 코드를
쓸 수 있다. 확인하려면: 브라우저 탭을 완전히 닫았다가 새로 열기(모바일 PWA는
완전히 종료 후 재실행), 그래도 안 되면 사이트 데이터/캐시를 지우고 재접속.

### 로그인 화면 "최근 로그인" 배지 + 사용법 가이드 정합성 + 챗봇 대화 기록 30개 제한

서비스워커 수정 이후 이어서 처리한 자잘한 UX 정리 세 가지. 각각 별도 커밋으로
반영했으나 이 CHANGELOG에는 한 번에 기록한다.

#### "최근 로그인" 배지
카카오/구글/네이버가 나란히 있으면 다음에 로그인할 때 지난번에 뭘 눌렀는지
헷갈리기 쉽다.
- `AuthContext.tsx`의 `loginWithToken()`에서 로그인 성공 시 이 기기의
  localStorage에 방금 쓴 방법(`google`/`kakao`/`naver`/`email`)을 저장
- `Login.tsx`: 저장된 값과 일치하는 SSO 버튼 우상단에 "최근 로그인" 배지 표시
- 계정 정보가 아니라 **기기별** 저장이라 로그아웃해도 지워지지 않음(다음에 이
  기기에서 로그인할 때 다시 참고할 수 있어야 하므로)

#### 사용법 가이드 정합성
- MyFridge 가이드 문구 "설정 아이콘(⚙️)을 누르면"이 예전 UI를 가리키고 있었음
  — 지금은 재료 배지를 직접 누르면 설정이 열리도록 이미 바뀌어 있었다(별도
  아이콘 없음). 문구를 "이 재료 배지를 누르면"으로 수정
- 냉장고요리 가이드(즐겨찾기/완료/기록/공유): 완료·기록·공유 버튼이 썸네일
  위에서 통계 줄 아래로 옮겨진 이전 UI 개편 때 `RecipeCard`의 해당 버튼들에서
  `data-guide-target`이 함께 옮겨지지 않고 빠져 있었음. 가이드가 대상 요소를
  못 찾으면 `GuideOverlay`가 **직전 단계의 하이라이트 위치를 그대로 남겨둬서**,
  노란 박스가 엉뚱한 자리에 떠 있는 것처럼 보였다(사용자가 "하이라이팅 표시가
  영역과 안 맞는다"고 직접 보고, 본인이 "UI가 변경돼서 그런 것 같다"고 원인까지
  맞혔다) — 실제 버튼에 `data-guide-target` 추가 + 대상을 못 찾았을 때
  `targetRect`를 확실히 `null`로 비우도록 `GuideOverlay.tsx` 수정
- 같은 가이드에서 아이콘 안내를 "즐겨찾기(☆)", "기록(⟎)"처럼 글자로 대신하고
  있었음(⟎는 폰트에 따라 깨져 보임) — 카드에 실제로 쓰는 아이콘을 그대로
  렌더링하도록 교체, 안 쓰이던 아이콘 import 6개 정리

#### 챗봇 대화 기록 30개 제한
지난 대화 보관 개수를 100개 → 30개로 축소하고, 목록 하단에 "최근 대화 30개까지만
보관돼요. 그 이전 대화는 자동으로 지워져요." 안내 문구 추가.

### 임박재료 버튼 시계 이모지 → 검정 선 아이콘

`⏱`(컬러 이모지)가 앱 전반의 검정 테두리 선 아이콘들 사이에서 혼자 튀어 보인다는
지적. 매칭도 버튼의 `%`처럼 같은 톤(stroke `#1A1A1E`, strokeWidth 1.8)으로 맞추기
위해 `RecipeSortBar.tsx`의 이모지 span을 시계 모양 SVG로 교체.

### 냉장고 재료 앱 재진입 시 새로고침 + 로그인 화면 가족 공유 안내

가족끼리 같은 계정으로 냉장고를 공유하는 상황(여러 기기, 동일 로그인)에 대한
후속 조치 두 가지.

#### 앱 재진입 시 DB 새로고침
`MyFridge.tsx`는 로그인/마운트 시점에만 서버에서 재료를 불러오고 그 뒤로는
실시간 동기화가 없어서, 다른 기기(가족 등)가 그새 바꾼 내용이 화면에 반영되지
않을 수 있었다. `visibilitychange`/`focus`로 앱이 다시 화면에 보이는 시점에
`loadIngredientsFromDB()`를 다시 호출하도록 추가. 저장 중이거나(`isSaving`)
이 기기에서 아직 저장되지 않은 변경사항이 있으면(`hasChanges`) 덮어써서 잃을
수 있으므로 그 경우엔 새로고침을 건너뛴다.

#### 남겨둔 것
이 새로고침은 "다시 열었을 때" 시점 동기화일 뿐, 두 기기가 **동시에** 다른
내용을 수정하면 여전히 나중에 저장하는 쪽이 이긴다(`save_user_ingredients`가
DELETE 후 재삽입하는 전체 덮어쓰기 방식이라 병합/충돌 감지가 없음). 사용자에게
이 위험을 그대로 설명했고, 좁은 범위로 고치는 대신 **가구(household)/초대 코드
기반 그룹 기능**을 별도로 구축하기로 결정 — 다음 항목 참고.

#### 로그인 화면 가족 공유 안내
냉장고 재료·즐겨찾기가 계정 기준으로 저장된다는 점을 이용해, 함께 냉장고를 쓰는
가족과 이용하려면 한 계정으로 같이 로그인하면 된다는 안내 문구를 로그인 화면에
추가(SSO 버튼 아래, 작은 회색 텍스트).

### 내냉장고: "영수증 인식"/"사진으로 재료 인식" 버튼을 카메라 버튼 하나로 통합

#### 문제 제기
내냉장고 페이지에 영수증 인식, 사진으로 재료 인식 버튼이 따로 있었는데 — 이미
찍어보기 전에는 어느 쪽인지 사용자가 먼저 골라야 하는 구조라 버튼이 두 개일
필요가 없다는 지적. 뭘 찍을지 먼저 고르게 하고, 그 대신 "카메라" 입구는 하나로
모으는 편이 낫다는 방향으로 재설계 요청. 더불어 "지금 갖고 있는 사진"(앨범)과
"파일"에서 추가하는 경로도 필요하다는 요청, 그리고 이 촬영 화면에 아직 실제
이미지 인식(OCR) 기능이 없다는 걸 사용자가 알 수 있도록 "준비 중" 배지를 눈에
띄게 넣어 달라는 요청.

참고: 두 버튼 모두 이전부터 이미 `showPrepNotice()`(3초 안내 토스트)만 띄우는
순수 placeholder였다 — 이번 작업은 "없던 placeholder를 새로 만드는" 게 아니라,
이미 준비 중이던 자리를 더 쓸모 있는 흐름(선택 시트 + 실제 파일 입력 + 안내)으로
확장하는 것이었다.

#### 조치
- `frontend/src/components/CameraCaptureSheet.tsx` 신규: 카메라 버튼을 누르면
  뜨는 바텀시트. [영수증] / [음식(재료 1개)] / [음식(재료 여러 개)] 세 가지
  주 선택지 + [사진 앨범에서 선택] / [파일에서 추가] 보조 버튼으로 구성
  - 촬영/선택은 표준 `<input type="file" accept="image/*">` 세 개로 처리.
    카메라 3개 옵션은 `capture="environment"`를 붙여 모바일에서 후면 카메라가
    바로 열리게 하고, 앨범/파일 옵션은 `capture` 없이 일반 선택 다이얼로그를 연다
  - 상단에 살짝 기울어진(`rotate(-4deg)`) 노란 배지로
    "✨ AI 이미지 자동인식 기능을 준비하고 있어요" 표시 — 개발 중인 영역임을
    한눈에 알 수 있게
  - `isStandaloneAppMode()`가 아닐 때(=PWA로 설치되지 않은 상태)만, 하단에
    "홈 화면에 추가해두면 더 빠르게 올 수 있어요" 안내 + (안드로이드/크롬은
    `showInstallPrompt()` 바로 호출 버튼, iOS는 "공유 → 홈 화면에 추가" 안내
    텍스트)를 보여준다
- `MyFridge.tsx`: 기존 두 아이콘 버튼을 제거하고 카메라 아이콘 버튼 하나로 교체,
  클릭 시 `CameraCaptureSheet`를 열고 파일이 선택되면 모드별 안내 문구로
  `showPrepNotice()`를 호출(기존 placeholder 토스트 패턴 그대로 재사용)

#### 명확히 하지 않은 것 — 홈 화면 "위젯"
사용자는 이 카메라가 "핸드폰 위젯으로 떠 있거나 바탕화면에 늘 떠 있는" 수준의
접근성을 원했다. 이건 지금의 웹/PWA 구조로는 만들 수 없다:
- 다른 앱 위에 떠 있는 플로팅 버튼은 안드로이드에만 있는 특수 권한 기능이고
  최근 OS 버전일수록 더 제한적이며, iOS엔 아예 없다
- 진짜 홈 화면 위젯(iOS WidgetKit, Android App Widget)은 네이티브 앱 코드가
  있어야 하고, 이 프로젝트는 `frontend/capacitor-setup.md`에 정리된 대로
  Capacitor로 감싸 앱스토어에 낼 계획은 있지만 아직 그 단계는 아니다

지금 만든 "홈 화면에 추가" 안내는 이미 있는 PWA 설치 유도(`HomeInstallPrompt.tsx`,
`pwa.ts`의 `showInstallPrompt()`)를 재사용한, 현재 구조에서 정직하게 줄 수 있는
가장 가까운 대안이다. 실제 OCR(영수증/음식 이미지 인식) 자체는 LLM 재료 추출
배치 작업이 끝난 뒤로 여전히 범위 밖.

#### 검증
- 프론트 타입체크(`tsc --noEmit`) 에러 0건
- 로컬 브라우저에서 확인: 카메라 버튼 클릭 → 시트가 뜨고 배지·3개 선택지·
  앨범/파일 버튼·설치 안내가 모두 정상 렌더링, 닫기 버튼으로 정상 닫힘

### 가족 그룹(초대 코드) 기능 신규 구축

#### 배경
로그인 화면에 "한 계정으로 같이 로그인하면 가족과 냉장고를 공유할 수 있다"는
안내를 넣어뒀지만(바로 앞 항목), 이 방식은 **같은 로그인 정보를 여러 사람이
공유**해야 하고, `save_user_ingredients`가 DELETE 후 재삽입하는 전체 덮어쓰기
방식이라 두 기기가 동시에 재료를 다르게 고치면 나중에 저장하는 쪽이 이기며
먼저 저장된 내용을 통째로 잃는 위험이 있었다. 앱 출시까지 고려하면 결국
계정은 각자 쓰고 냉장고만 공유하는 구조가 필요하다는 판단으로, 별도
가족 그룹/초대 코드 기능을 새로 만들었다.

구현 전 정한 것(사용자 결정):
- 한 계정은 최대 1개 그룹에만 속한다(다중 그룹 소속 없음)
- 그룹 참여 시 참여자가 갖고 있던 개인 재료는 그룹 재료로 합친다(중복은 하나로)
- 그룹원은 모두 동등한 권한(방장 개념 없음) — 누구나 코드 재발급·나가기 가능
- 즐겨찾기/완료/기록은 계정별 개인 기록으로 그대로 둔다 — 그룹과 무관.
  "OO님이 즐겨찾기함" 배지 같은 표시는 이번 범위에 넣지 않음(아래 참고)

#### 스키마/구현 방식
테이블 구조를 크게 바꾸는 대신 **저장 위치 리다이렉션** 방식을 택했다:
- `households` 테이블 신규(`id`, `invite_code` UNIQUE, `storage_user_id`,
  `created_by`, `created_at`). `storage_user_id`는 "이 그룹의 재료가 실제로
  저장되는 계정"을 가리킬 뿐, 권한상 의미는 없다(모두 동등이므로)
- `users` 테이블에 `household_id` 컬럼 추가(기존 `ensure_users_table()`류
  마이그레이션 패턴과 동일하게, `INFORMATION_SCHEMA.COLUMNS` 확인 후
  `ALTER TABLE`로 추가)
- `get_user_ingredients`/`save_user_ingredients`가 요청한 `user_id`로 바로
  조회/저장하는 대신, `resolve_ingredient_storage_user_id()`로 "이 사람이
  속한 그룹의 storage_user_id(없으면 자기 자신)"를 구해 그 id로 조회/저장
  하도록 수정. **덕분에 `user_ingredients` 테이블 구조도, 프론트에서 이
  두 엔드포인트를 부르는 코드(`MyFridge.tsx`)도 전혀 바꿀 필요가 없었다** —
  그룹에 속한 여러 계정이 같은 행을 자연스럽게 공유하게 됨
- 신규 엔드포인트 5개: `GET /api/households/me`(내 그룹 정보),
  `POST /api/households`(그룹 생성), `POST /api/households/join`(초대 코드로
  참여 — 참여자 개인 재료와 그룹 재료를 이름+보관위치 기준으로 병합, 유통기한은
  있는 쪽을 우선하고 둘 다 있으면 더 임박한 날짜를 남김), `POST
  /api/households/leave`(그룹 나가기 — 그룹의 공유 재료는 그대로 두고
  `household_id`만 NULL로), `POST /api/households/regenerate-code`(초대
  코드 재발급, 그룹원 누구나 가능)
- 초대 코드는 0/O, 1/I/L처럼 헷갈리는 문자를 뺀 8자리(`secrets.choice` 기반)
- 프론트: `frontend/src/components/HouseholdSection.tsx` 신규 — 그룹
  생성/참여/초대 코드 표시·복사/멤버 목록/코드 재발급/나가기 UI. 참여·나가기는
  `Dialog` 공통 컴포넌트로 입력창/확인 팝업 처리. 마이페이지 프로필 카드
  바로 아래(요약 탭 위)에 배치(`MyPage.tsx`)

#### 검증 (실측, 프로덕션 DB에 임시 테스트 계정 2개로 직접 확인 후 정리)
- 계정 A(감자 유통기한 09-10, 계란)와 계정 B(감자 유통기한 09-20, 양파)가
  각자 재료를 저장한 상태에서 A가 그룹 생성 → B가 초대 코드로 참여
- 참여 직후 두 계정 모두 `GET .../ingredients` 응답이 동일: 감자(더 임박한
  09-10 유지), 계란, 양파 — 중복 병합과 리다이렉션 동시 확인
- B가 초대 코드 재발급 → 예전 코드로 참여 시도 시 "유효하지 않은 초대
  코드입니다" 정상 거부
- B가 그룹 나가기 → B는 그룹 참여 전 개인 재료(감자 09-20, 양파)로 복귀,
  A는 그룹의 병합된 재료 그대로 유지 확인
- 프론트 UI 실브라우저 테스트: 그룹 만들기 → 초대 코드/멤버 배지 표시 →
  나가기 확인 팝업 → 나가기 → "그룹 만들기/참여하기" 초기 화면으로 정상 복귀
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

#### 남겨둔 것
- **그룹 나가기 시 개인 재료 복원의 정합성**: 나가면 `household_id`만
  지우고 참여 전 개인 `user_ingredients` 행은 손대지 않으므로, 그룹에서
  활동한 기간 동안 바뀐 내용은 반영되지 않은 "참여 시점 그대로"가 복원된다.
  가끔 있는 일(그룹 나가기 자체가 드묾)이라 지금은 그대로 두고, 필요해지면
  나가는 시점에 그룹 재료 스냅샷을 개인 쪽에 복사하는 방식을 추가하면 된다
- **그룹을 만든 사람(`storage_user_id`)이 나가는 경우**: 그 사람의
  `household_id`만 지워질 뿐 그룹은 그대로 남고, 남은 멤버는 계속 그 사람의
  옛 계정 아래 저장된 행을 공유한다 — 데이터가 사라지진 않지만 "누구 이름
  아래 저장되어 있는지"가 사용자에게는 안 보이는 내부 구현이라 문제 없음
- **즐겨찾기/완료/기록의 그룹 공유 표시**("OO님이 즐겨찾기함" 배지): 이번엔
  구현하지 않음. `user_favorite_recipes` 등은 계정별로 그대로 두는 게
  맞다는 결정은 확정했지만, 그룹 화면에서 조회·배지로 보여주는 프론트 작업은
  `MyPage.tsx`/`RecipeList.tsx`/`Popular.tsx` 세 곳의 카드 렌더링을 모두
  건드려야 해서 범위 밖으로 남김 — 필요해지면 `GET
  /api/households/me/<action>-recipes` 같은 엔드포인트로 멤버별 목록을
  모아 오는 것부터 시작하면 된다

### 가족 그룹 후속: 그룹 나가기 재료 처리 수정 + 배지 + 초대 링크 + 참여 옵션

바로 위 항목을 쓴 뒤 사용자가 "그룹 나가면 재료는 어떻게 되는 거냐"고
구체적으로 캐물으면서, 그때 남겨뒀던 문제 두 가지(나가기 시 재료 복원,
즐겨찾기 배지)를 실제로 해결했다. 여기에 카메라 버튼/시트 재설계, 초대
링크 공유, 참여 시 옵션 선택까지 같이 처리했다.

#### 그룹 나가기 시 재료 처리 — 실제로 고침
기존엔 나가면 `household_id`만 지우고 끝이라, 그룹에서 활동하며 바뀐
내용은 전혀 반영 안 된 **가입 전 옛 개인 재료**가 그대로 복원됐다. 사용자
지적("지금 그룹 재료를 내 개인 공간으로 복사해와야 하는 거 아냐?")이
맞았다 — `leave_household()`를 다시 짜서, 나가는 시점 **그룹의 현재
재료 스냅샷**을 그대로 내 개인 저장공간에 복사하도록 바꿨다
(`_copy_ingredients()`).
- 내가 그룹의 저장 계정(`storage_user_id`)이 아니었던 경우: 그룹 재료를
  내 `user_id` 쪽으로 복사
- 내가 저장 계정이었던 경우(그룹 만든 사람): 남은 멤버가 있으면 그 중
  한 명에게 저장 위치를 옮겨준다(데이터를 그 사람 앞으로 복사 +
  `households.storage_user_id` 갱신) — 그래야 내가 나간 뒤에도 남은
  멤버들이 계속 같은 데이터를 본다. 남은 멤버가 없으면(사실상 혼자
  남은 그룹) 옮길 곳이 없으므로 그대로 둔다
- 즐겨찾기·완료·기록은 애초에 그룹과 무관한 개인 기록이라 나가도 전혀
  바뀌지 않는다 — 이 부분은 처음부터 맞게 설계돼 있었다

#### "OO님도 즐겨찾기함" 배지 — 실제로 구현
- `users` 테이블에 `share_recipe_actions`(기본 1) 컬럼 추가. 그룹
  참여자가 "내 즐겨찾기·완료·기록을 그룹원에게 보여줄지"를 직접 고를 수
  있게 하기 위함(뒤에서 설명)
- `GET /api/households/me/recipe-actions` 신규: 그룹원 중
  `share_recipe_actions=1`인 사람들의 즐겨찾기/완료/기록 `recipe_id`와
  누가 했는지(닉네임)를 모아서 준다. 실제 레코드를 합치는 게 아니라
  조회 시점에만 모으는 것이라, 계정별 개인 기록이라는 원칙은 그대로
  유지된다
- `RecipeCard.tsx`에 `attributionLabel?: string` prop 추가 — 있으면
  제목 아래 작은 배지로 표시. `VirtualizedHorizontalRecipeList.tsx`에
  `getAttributionLabel` prop을 추가해 각 카드에 전달
- `MyPage.tsx`: 그룹 활동 데이터를 불러와(`loadHouseholdActions`), **내가
  이미 갖고 있는 즐겨찾기/기록/완료 카드 중** 다른 그룹원도 같은 행동을
  한 경우에만 "OO님도 즐겨찾기함" 배지를 붙인다. 내가 하지 않은 것까지
  목록에 새로 끌어오지는 않음 — "내가 즐겨찾는 레시피" 라는 제목의 뜻이
  바뀌지 않게 하기 위함(남겨둔 것 참고)

#### 참여 시 옵션 선택 + 초대 링크
"그룹만들기 눌러도 뭐가 어떻게 되는지 잘 안 와닿는다"는 지적에 두 가지로
대응:
- **그룹 만들기 버튼에 확인 팝업 추가**: 누르기 전에 "초대 코드가
  만들어지고, 그 코드로 가족이 참여할 수 있다"는 걸 먼저 설명하고 확인을
  받는다(`HouseholdSection.tsx`)
- **참여 시 옵션 선택**: 초대 코드 입력 팝업(그리고 아래 초대 링크로
  들어왔을 때도 동일하게)에 체크박스 두 개 추가 — "지금 내 재료를 그룹에
  합치기"(끄면 내 재료는 그대로 두고 그룹의 기존 재료만 보게 됨), "내
  즐겨찾기·완료·기록을 그룹원에게 보여주기"(끄면 위 배지에서 내 이름이
  빠짐). `POST /api/households/join`이 `merge_ingredients`,
  `share_recipe_actions` 두 값을 받도록 확장
- **초대 링크**: 초대 코드 옆에 "초대 보내기" 버튼 추가 —
  `navigator.share()`(모바일에서 카카오톡 등 공유 시트가 그대로 뜸)를
  우선 쓰고, 지원 안 하면 링크를 클립보드에 복사. 링크
  형식은 `/join-household?code=XXXX`
- **`frontend/src/pages/JoinHousehold.tsx` 신규**: 이 링크로 들어왔을 때
  보여주는 화면. 로그인 안 돼 있으면 코드를
  `localStorage`(`utils/householdInvite.ts`)에 잠깐 저장해 두고 로그인/
  회원가입으로 보낸 뒤, 로그인이 끝나면(`AuthSuccess.tsx`, `Login.tsx`,
  `Signup.tsx` 세 곳의 로그인 성공 처리에 `getPostLoginRedirectPath()`
  추가) 자동으로 이 페이지로 돌아와 이어서 참여할 수 있다. 로그인 상태면
  위와 같은 옵션 체크박스와 함께 바로 참여할 수 있다

#### 그룹 멤버 목록의 닉네임이 안 바뀌던 문제
멤버 목록(`info.members`)은 `/api/households/me`를 부를 때마다 DB에서
새로 읽어오므로 서버 쪽은 항상 최신인데, `HouseholdSection`의 조회
`useEffect`가 마운트 시 한 번만 실행돼서 **내가 마이페이지를 떠나지
않고 내 닉네임을 바꾸면** 배지가 그대로 남아 있었다. `useAuth()`의
`user?.nickname`을 이 effect의 의존성에 추가해서, 내 닉네임이 바뀌는
순간 다시 불러오게 했다. (다른 멤버의 닉네임 변경은 여전히 페이지를
새로 열어야 반영됨 — 실시간 동기화는 범위 밖)

#### 카메라 버튼/시트 재설계
- 영수증/음식 1개/음식 여러 개 선택지가 설명문처럼 가로로 긴 줄이라
  "눌러서 찍는다"는 게 잘 안 느껴진다는 지적 → 큰 정사각 아이콘 타일
  3개를 나란히 두는 방식으로 교체(`CameraCaptureSheet.tsx`)
- "사진 앨범에서 선택"과 "파일에서 추가"가 나뉘어 있을 필요가 없다는
  지적 — 애초에 `capture` 속성 없는 파일 입력 하나면 모바일에서도
  앨범/파일 앱을 그대로 보여주므로, 버튼 하나("사진 앨범/파일에서
  선택")로 합침
- 내냉장고의 카메라 버튼 자체에도 AI 챗봇 FAB과 같은 시각 언어(노란
  셰이머 애니메이션 + "AI" 배지)를 입혀서, "AI가 나중에 관여할 자리"라는
  느낌을 챗봇 버튼과 통일했다(`MyFridge.tsx`, 기존 `.ai-fab-*` CSS
  클래스 재사용)

#### 그 밖의 자잘한 수정
- `RecipeSortBar.tsx`의 "임박 재료" 버튼이 좁은 화면에서 아이콘+글자가
  두 줄로 보이는 문제 — 버튼에 `display:inline-flex`를 명시해 한 줄로
  고정(`white-space:nowrap`만으로는 인라인 요소 사이 줄바꿈을 완전히
  막지 못했음)

#### 검증 (실측, 프로덕션 DB에 임시 테스트 계정으로 직접 확인 후 정리)
- 재료 없이 그룹 참여(`merge_ingredients:false`) → 개인 재료 안 건드리고
  그룹 재료만 보임 확인
- B가 즐겨찾기 후 `share_recipe_actions:false`로 참여 → 그룹 배지 조회
  결과에서 B가 빠짐, A가 같은 레시피 즐겨찾기하면 A만 표시됨 확인
- 초대 링크로 로그인된 새 계정 진입 → 옵션 선택 후 참여 → 마이페이지에
  멤버 배지 2명 정상 표시 확인
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### 카메라 시트 드래그로 닫기 + "합치기 안 하면 재료 사라지는지" 안전장치

#### 카메라 시트도 필터처럼 끌어서 닫기
"필터" 팝업과 같은 바텀시트 모양인데 끌어서 닫는 동작이 없다는 지적. 직접
드래그 로직을 새로 짜는 대신, 필터가 이미 쓰고 있는 공용 `components/ui/Sheet.tsx`
(손잡이/헤더에서 끌면 언제나 반응, 본문에서는 맨 위로 스크롤된 상태에서
아래로 당길 때만 반응 — 목록 스크롤과 안 헷갈리게 이미 구분해 둔 로직)로
`CameraCaptureSheet.tsx`를 새로 짰다. 직접 그리던 배경 딤·둥근 모서리·닫기
버튼을 다 걷어내고 `Sheet`에 맡겨서, 다른 시트들과 여닫는 손맛이 완전히
같아졌다.

#### 그룹 참여 시 "재료 합치기"를 껐을 때의 실제 보장
"합치기를 안 하면 내 재료가 지워지고 백업도 안 되는 거 아니냐"는 질문에
실제 구현을 다시 보니 — 참여 시점에는 안전했지만(개인 재료 행은 건드리지
않음), **나중에 그룹을 나갈 때** 무조건 "그 시점 그룹 재료"로 덮어써 버리는
코드였다. 즉 합친 적 없는 사람도 나갈 때는 자기 옛 재료를 잃고 있었다 —
지적이 맞았다.

`users.ingredients_merged` 컬럼을 추가해 "참여할 때 실제로 합쳤는지"를
기록하고, `leave_household()`가 이 값을 보고 갈린다:
- 합쳤던 사람(`ingredients_merged=1`): 기존과 동일하게 나가는 시점의 그룹
  재료 스냅샷을 그대로 들고 나간다
- 합친 적 없는 사람(합치기를 꺼두고 참여): 참여 후 자기 `user_ingredients`
  행을 한 번도 안 건드렸으므로, **참여 전 재료가 그대로 보존돼 있다가
  그대로 돌아온다** — 그룹의 최신 상태로 덮어쓰지 않는다

참여 다이얼로그(`HouseholdSection.tsx`, `JoinHousehold.tsx`)의 체크박스
설명도 "꺼두면 삭제되지 않고 보존되며, 나갈 때 그대로 돌려받는다"는 실제
동작 그대로 명시하도록 다시 썼다.

#### 검증 (실측)
- B가 `merge_ingredients:false`로 참여 → 그룹에 있는 동안 A가 그룹 재료를
  바꿔도 → B가 나가면 **참여 전 B의 원래 재료**를 그대로 돌려받음 확인
  (그룹의 바뀐 최신 상태가 아님)
- C가 `merge_ingredients:true`로 참여 → 나가면 그 시점 그룹 재료(합쳐진
  전체 목록)를 그대로 가져감 확인 — 기존 동작 그대로 유지됨
- 프론트 타입체크, 백엔드 `python -m py_compile` 에러 0건

### 즐겨찾기/완료/기록 — "겹치는 것만 배지" → "그룹원 전체 피드"로 확장

#### 문제 제기
바로 앞에서 만든 배지 기능은 "내가 이미 갖고 있는 항목 중 다른 멤버도 같은
걸 했으면 배지만 붙는" 정도였다 — B가 즐겨찾기했지만 나는 안 한 레시피는
내 화면에 아예 안 보였다. 사용자가 원한 건 "B가 한 건 나도 볼 수 있어야
한다"는, 그룹원 전체를 하나의 피드로 보여주는 것이었고, 같은 레시피를
둘 이상이 했으면 카드 하나에 배지 여러 개(이름 여러 개)가 붙어야 한다는
것도 확인했다.

#### 조치
`user_favorite_recipes`/`user_completed_recipes`/`user_recorded_recipes`
자체를 합치는 게 아니라(계정별 개인 기록 원칙은 그대로), **조회할 때**
그룹원 전체(+나는 내 `share_recipe_actions` 설정과 무관하게 항상 포함)의
레코드를 레시피 단위로 모아서 하나의 피드를 만든다.

- 기존에 있던 "겹치는 것만 배지" 용 엔드포인트(`GET
  /api/households/me/recipe-actions`, recipe_id→닉네임 목록만 주던 것)를
  걷어내고, 실제 레시피 전체 데이터를 주는 3개로 교체: `GET
  /api/households/me/{favorite,completed,recorded}-recipes`. 쿼리는
  `recipes` 테이블과 각 액션 테이블을 레시피 id로 GROUP BY 해서, 같은
  레시피에 여러 명이 액션했으면 `GROUP_CONCAT`으로 닉네임을 모아
  `acted_by` 배열로 얹어 한 장만 반환한다
- `MyPage.tsx`: 그룹에 속해 있으면 이 3개 엔드포인트로 받은 목록을
  "내가 즐겨찾는 레시피" 등 미리보기 영역에 그대로 쓰고, 섹션 제목도
  "우리 가족이 즐겨찾는 레시피"로 바뀐다. 카드의 `attributionLabel`은
  이제 서버가 준 `acted_by`에서 내 닉네임을 뺀 나머지로 만든다(여러 명이면
  "엄마·아빠님도 즐겨찾기함"처럼 이어 붙임)
- **중요**: 액션 상태(카드 위 하트/체크 버튼이 채워져 보이는지)는 여전히
  `localStorage`(`my_favorite_recipes` 등, 내 계정 것만)로만 판단한다.
  화면에 표시하는 목록(`recipes` prop)과 "내가 눌렀는지"를 판단하는 값을
  분리해 둔 덕분에, 그룹 피드에 다른 사람이 한 항목이 섞여 들어와도 그게
  내 것으로 잘못 표시되는 일은 없다 — `favoriteRecipes` 등 개인 목록
  상태는 그대로 두고, 화면에 뭘 뿌릴지만 `isInHousehold` 에 따라
  `displayFavoriteRecipes` 등으로 분기했다
- `HouseholdSection`에 `onChange` 콜백을 추가해서, 그룹 생성·참여·나가기가
  성공하면 마이페이지가 이 세 피드를 즉시 다시 불러오게 했다(전에는
  페이지를 새로 열어야 반영됐다)

#### 남겨둔 것
"전체보기"(`/mypage/favorite` 등, `IngredientDetail.tsx` 재사용) 페이지는
이번에 안 건드려서 여전히 내 개인 목록만 보여준다 — 미리보기 영역과
개수/내용이 안 맞을 수 있다. 필요해지면 그 페이지도 같은 방식으로
확장하면 된다.

#### 검증 (실측)
- A가 레시피 X 즐겨찾기, B가 레시피 X·Y 즐겨찾기(그룹) → A 화면에 X
  (배지: "B님도 즐겨찾기함")와 **A가 한 적 없는 Y**가 모두 나타나고, Y에도
  "B님도 즐겨찾기함" 배지 정상 표시 확인
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### 카메라 시트 마무리 + 그룹 UX 다듬기(토글, 공유 상태 표시, 문구 단축)

#### 카메라 시트
- 상단에 이미 × 닫기 버튼이 있는데 하단에 또 "닫기" 버튼이 있어서 중복이라는
  지적 — `Sheet`에 `hideFooter` 옵션을 추가하고 카메라 시트에 적용해 하단
  버튼을 없앴다(다른 시트는 그대로 기본 동작 유지)
- "위젯 관련 설명이 왜 없냐"는 재질문에 실제로는 PWA 설치 안내만 있고 정작
  "위젯"이라는 단어가 없어 오해를 줬다는 걸 확인 — `isStandaloneAppMode()`
  여부와 무관하게 항상 보이는 문구를 추가: "📱 바탕화면에 바로 찍는 위젯도
  준비 중이에요. 앱 출시 후 지원될 예정이에요." (PWA 설치 안내와는 별개로,
  이미 설치한 사람에게도 위젯 계획 자체는 알려야 하므로)

#### 그룹원의 공유 상태를 눈에 보이게
"재료는 처음 합류할 때만 영향 주지만(그 뒤엔 안 보임), 즐겨찾기 공유 여부는
계속 남의 화면에 영향을 주니 상태가 보여야 한다"는 지적. 멤버 목록의 각
닉네임 배지 앞에 작은 점을 추가(🟢 공유 중 / ⚪ 비공개) — `GET
/api/households/me`가 멤버별 `share_recipe_actions` 값을 함께 내려주도록
확장하고, 배지 아래 범례 한 줄을 추가했다.

#### 체크박스 → 토글 스위치, 다이얼로그 문구 단축
- `components/ui/Toggle.tsx` 신규(On/Off 스위치). 그룹 참여(`HouseholdSection`,
  `JoinHousehold`)의 두 체크박스를 이걸로 교체 — 지금 상태가 켜져 있는지
  꺼져 있는지 체크박스보다 한눈에 들어온다
- **그룹 만들기에도 같은 두 토글 추가**(이전엔 참여할 때만 물어보고 만들 때는
  선택지가 없었다): "지금 내 재료를 그룹 재료로 쓰기"(끄면 빈 상태로
  시작 — 재료를 지운다는 뜻이라 "파기"로 명확히 이해하고 고르게 함),
  "즐겨찾기·완료·기록 그룹에 공유". 둘 다 기본 켜짐. `POST
  /api/households`가 `keep_ingredients`/`share_recipe_actions`를 받도록
  확장 — false면 생성 시점에 내 `user_ingredients`를 비운다(그룹의 저장
  계정이 곧 내 계정이므로, "가져가지 않기"는 곧 지금 재료를 없애는 것과
  같다 — 참여자의 "보존" 옵션과는 다른 의미라 문구도 다르게 씀)
- 그룹 만들기/나가기 확인 팝업 문구를 절반 이하로 줄임(4줄 → 1~2줄)

#### 검증 (실측)
- `POST /api/households` `keep_ingredients:false`로 생성 → 생성 직후 내
  재료가 실제로 비워짐 확인, `share_recipe_actions:false`로 생성 →
  `/api/households/me` 멤버 목록에 반영됨 확인
- 프론트 UI에서 토글 클릭 → 상태 전환 → 그대로 그룹 생성 API 호출에 반영되는
  것까지 실제 클릭으로 확인
- 프론트 타입체크, 백엔드 `python -m py_compile` 에러 0건

### 요리 캘린더 신규 (완료 레시피를 날짜별로 보기)

#### 배경
"완료한 레시피를 날짜별로 볼 수 있는 달력이 있으면 좋겠다, 그룹이면 누가
언제 뭘 완료했는지도 보이면 좋겠다"는 아이디어. 일/주/월 전환, 그룹
요약까지 포함된 꽤 큰 기능이라 다른 작업과 묶지 않고 별도로 진행했다.

**어디에 넣을지**: 하단 탭을 새로 만드는 대신 마이페이지 안에 넣기로
했다 — 하단 탭 4개(내냉장고/냉장고요리/요즘인기/마이페이지)는 이미 자주
쓰는 핵심 동선이고, 캘린더는 "완료한 레시피"를 다르게 보여주는 것뿐이라
그 섹션에서 들어가는 하위 화면이 자연스럽다. 이름은 "요리 캘린더",
아이콘은 이모지 대신 앱 전체가 쓰는 검정 선 아이콘 스타일로 새로 그렸다.

#### 조치
- 백엔드 `GET /api/households/me/completed-calendar` 신규(쿼리:
  `start`, `end`, YYYY-MM-DD). `completed-recipes`(레시피 단위로 묶어서
  한 장씩 주는 것)와 달리, 여기서는 **(사용자, 레시피, 날짜) 조합별로
  한 행씩** 그대로 준다 — 달력은 날짜가 핵심이라 뭉개면 안 된다. 그룹
  멤버 중 `share_recipe_actions=1`인 사람(+나는 항상 포함)만 대상
- `frontend/src/pages/CookingCalendar.tsx` 신규(`/mypage/cooking-calendar`):
  - 상단 퀵버튼으로 [일] [주] [월] 전환, ‹ › 로 이전/다음 이동, "오늘" 버튼
  - 월 보기: 표준 달력 그리드, 완료 기록이 있는 날짜에 개수 배지. 날짜를
    누르면 그 날의 일 보기로 바로 이동
  - 주 보기: 7일을 세로로 나열, 그 날 완료한 레시피 제목들을 미리 보여줌
  - 일 보기: 그 날 완료한 레시피 카드 목록. 그룹이면 카드마다 "OO님이
    완료" 표시
  - 상단 요약 박스: 지금 보고 있는 범위(달/주/일에 맞춰) 기준으로 총
    횟수 + 그룹이면 인원별 횟수("총 5회 · 엄마 3회 · 나 2회")
  - 항상 anchorDate가 속한 달 전체를 한 번에 불러와 두고, 일/주 보기
    전환 시에는 같은 달 안에서는 다시 불러오지 않는다(클라이언트에서
    필터링만)
- `MyPage.tsx`의 "완료한 레시피" 섹션 제목 옆에 캘린더 아이콘 버튼 추가 —
  이게 유일한 진입점

#### 검증 (실측)
- 그룹 상태에서 A와 B가 같은 날 서로 다른 레시피를 완료 → 월 보기에 그
  날짜 배지 "2" 표시, 그 날짜 클릭 시 일 보기에 두 카드 모두 "OO님이
  완료" 정상 표시, 주 보기에서도 같은 날짜 칸에 두 레시피 제목과 배지
  "2" 정상 표시, 요약 박스에 "총 2회 · CalA 1회 · CalB 1회" 정상 계산 확인
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

#### 남겨둔 것
"완료 레시피 축약판을 홈 화면 위젯으로도"는 아이디어로만 남겨 둔다 —
`capacitor-setup.md`의 위젯 계획과 마찬가지로 네이티브 앱 출시 이후에나
가능한 별도 작업이다.

### 카메라 시트 문구 정리 + 그룹 재료 합치기 정책 + 마이너 UX 수정 6건

#### 카메라 시트
- "위젯 준비 중" 문구가 한 줄로 길게 이어져 좁은 화면에서 마지막 글자만
  다음 줄로 밀려 어색하게 보였다 — 문장을 둘로 나누고 `<br/>`로 줄바꿈
  위치를 직접 지정
- "홈 화면에 추가하면 더 빠르다"는 PWA 설치 안내는 이 시트가 아니어도 다른
  화면(`HomeInstallPrompt`)에서 이미 하고 있어 중복이라는 지적 — 이 시트
  에서는 위젯 준비 중이라는 안내만 남기고 설치 유도 블록은 제거

#### 그룹 멤버 배지 재설계
"닉네임 옆 초록/흰 점이 뭔지 헷갈린다"는 지적(로그인 중인 사람 표시인 줄
알았다고 함) — 점 + 별도 범례 조합을 걷어내고, 멤버를 한 줄씩(닉네임 —
"즐겨찾기·완료·기록 공유 중"/"비공개") 텍스트로 바로 풀어 썼다. 해석이
필요 없게 만드는 게 목적이라 범례도 같이 없앴다.

#### 그룹 재료 합치기 정책(기존 그룹원이 막을 수 있게)
"새로 들어오는 사람이 원치 않는데 재료가 섞이면 어떡하냐"는 지적이 정확한
설계 공백이었다 — 지금까지는 참여자 개인의 선택(토글)만으로 합쳐졌고,
기존 그룹원은 이를 막을 방법이 없었다.
- `households.allow_ingredient_merge`(기본 1) 신규. 그룹원 누구나 켜고 끌
  수 있다("모두 동등" 원칙 유지) — `HouseholdSection`에 토글 추가
- `POST /api/households/settings`로 이 값을 바꾼다
- `join_household()`: 그룹이 이 값을 꺼 뒀으면, 참여자가 무엇을 선택했든
  서버가 강제로 "합치지 않음"으로 처리한다(`merge_denied_by_policy` 응답
  플래그) — 참여자의 선택보다 그룹의 정책이 우선한다
- `HouseholdSection.tsx`/`JoinHousehold.tsx`: 이 플래그가 오면 "이 그룹은
  재료 합치기를 막아 둬서 내 재료가 보존됐다"고 정확히 안내

#### 나가기 확인창 문구가 실제 동작과 다르던 문제
참여 시 토글 설명("꺼두면 나갈 때 내 재료를 돌려받아요")과 나가기 확인창
문구("그룹 재료를 복사해서 가져가요")가 서로 다른 얘기를 하고 있었다 —
실제로는 `ingredients_merged` 값에 따라 둘 중 하나가 맞는 상황이라, 확인창
문구를 그 값에 따라 갈리도록 고쳤다. `GET /api/households/me`가
`my_ingredients_merged`(내 계정 기준)를 함께 내려주도록 확장

#### 그 밖의 수정
- "우리 가족이 즐겨찾는/기록한/완료한 레시피" → "우리 식구가 ~"로 문구 변경
  (식구=같이 밥 먹는 사이라는 의미가 더 맞다는 의견)
- 마이페이지 "우리 식구가 기록한 레시피" 제목 옆 아이콘이 문서/메모
  모양이었는데, 실제 카드의 '기록' 버튼(`기록하기버튼.png`)은 책갈피(리본)
  모양이라 서로 달라 인지 오류가 있었다 — `SectionIcon.tsx`의 `recorded`
  케이스를 책갈피 모양으로 교체해 실제 버튼과 통일

#### 검증 (실측)
- A가 그룹의 `allow_ingredient_merge`를 끔 → B가 `merge_ingredients:true`로
  참여해도 서버가 강제로 막고 `merge_denied_by_policy:true` 반환, B의 개인
  재료가 DB에 그대로 보존돼 있음을 직접 조회로 확인
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

#### 남겨둔 것 — 진행 여부 확인 필요
"마이페이지에서 그룹원 아이디를 누르면 그 사람의 기록/완료/즐겨찾기 개수를
요약해 보여주는 프로필 팝업 + '공유 요청하기' 버튼으로 상대에게 앱이
꺼져 있어도 알림이 가서 수락/거절하게 하자"는 아이디어는 이번에 반영하지
않았다. 앱이 꺼져 있을 때도 알림을 보내려면 웹 푸시 알림(서비스워커 +
VAPID 키 + 구독 저장 + 발송 백엔드)이라는 별도의 큰 인프라가 필요해서,
범위와 우선순위를 먼저 사용자와 확인한 뒤 진행하기로 함.

### 요리 캘린더를 하단 탭으로 승격 + 인원별 색 구분 + 월 목표

#### 배경
"캘린더 들어가는 입구가 마이페이지 안에 너무 숨어 있다"는 지적과, 신혼부부
집안일 분담을 통계로 보여주고 싶다는 원래 취지, 여기에 "돈 아끼려고 집밥
해먹는 사람들을 위한 월간 목표 달성률"까지 아이디어가 붙으면서 — 이 기능은
마이페이지의 부속 화면이 아니라 그 자체로 하나의 탭이 맞다고 판단해서
승격시켰다.

#### 조치
- **하단 탭으로 이동**: `내냉장고 / 냉장고요리 / 요즘인기 / 요리 캘린더 /
  마이페이지` 순서로 마이페이지 바로 앞에 추가(`BottomNavBar.tsx`). PNG
  아이콘 세트가 없어 인라인 SVG로 새로 그렸는데, 처음에 비활성 상태를
  흰색 선으로 그렸다가 — 다른 탭의 `_white.png` 파일들이 실제로는 흰
  배경 위 **회색/검정 선** 아이콘이라는 걸(직접 이미지를 열어 확인) 뒤늦게
  깨닫고 회색(`#9A9AA2`)으로 고쳤다(안 고쳤으면 탭 안 눌렀을 때 아이콘이
  안 보이는 상태로 배포될 뻔함). 경로도 `/mypage/cooking-calendar` →
  `/cooking-calendar`로 옮기고, 마이페이지 쪽의 숨어 있던 아이콘 입구는
  제거
- **헤더를 다른 탭과 같은 규격으로**: 직접 그리던 뒤로가기 버튼 + "요리
  캘린더" 제목 줄을 없애고, 다른 탭처럼 공용 GNB(`TopNavBar`, 로고+
  닉네임) 아래에 본문이 오는 여백(`paddingTop: 72`)만 맞췄다 — 탭 이름은
  하단 탭 라벨로 이미 보이므로 페이지 안에 또 적을 필요가 없었음
- **"오늘" 버튼 제거**: 어디에 쓰는 건지 불명확하다는 지적 — 뺐다. 대신
  일 보기에서 각 레시피 옆에 **완료한 시각**(예: "14:32")을 보여주도록
  해서, 그냥 날짜만 나열하던 것보다 그 날 안에서 언제 요리했는지도
  드러나게 했다
- **인원별 색 구분**: 월/주 보기의 숫자 배지가 "총 몇 번"만 말해줄 뿐 누가
  했는지 안 보였다는 지적 — 그룹원마다 고정 색(파랑/주황/초록 등 6색
  팔레트, id 순서로 배정)을 주고, 날짜 칸에는 그 날 완료한 사람 수만큼
  작은 색 점(2번 이상이면 점 안에 숫자)을 찍는다. 요약 박스에는
  "🔵 TabA 1회  🟠 TabB 2회"처럼 이름+색 범례를 같이 보여준다. 일 보기의
  레시피 카드에도 같은 색 점 + 닉네임을 붙여 색이 전체 화면에서 일관되게
  같은 사람을 가리키게 했다
- **이번 달 목표 + 달성률**: `users.monthly_cooking_goal`(기본값 12 —
  "설정 안 해도 되게 기본값이 있으면 좋겠다"는 요청 반영, 대략 주 3회) 신규.
  화면 상단에 진행바 + "N회 / 목표회 달성 (%)"로 보여주고, 목표 숫자는
  눌러서 바로 수정 가능(`POST /api/users/<id>/monthly-goal`). 지금은
  본인 목표만 편집하고, 그룹 요약 박스의 인원별 횟수와 나란히 보여 자연스럽게
  비교가 되게 했다

#### 검증 (실측)
- 그룹 상태에서 A 1회 + B 2회(같은 날) 완료 → 월 보기 그 날짜 칸에 파랑
  점(무표시=1회) + 주황 점("2") 정상 표시, 요약 박스에 "총 3회 · TabA 1회 ·
  TabB 2회" 정상 계산, 일 보기 진입 시 각 카드에 "05:51 · TabA" 형식으로
  시각+이름 정상 표시 확인
- 목표 수정(12→20) → 진행바와 퍼센트가 그 자리에서 재계산되는 것 확인
- 하단 탭 순서·라벨 정상 표시, 탭 클릭 시 라우팅 정상 확인
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

#### 남겨둔 것 — 다음 단계로 미룸
아이디어로 나온 것 중 이번엔 반영하지 않은 것들. 전부 "데이터를 어떻게
모델링할지"부터 결정해야 해서 별도로 논의 후 진행하는 게 맞다고 판단:
- **연도별 통계**: 지금은 달 단위 조회만 있음. 연간 뷰 자체는 쿼리
  범위만 넓히면 되지만, 어떤 지표를 보여줄지(월별 추이 그래프? 연간
  총합만?) 화면 설계가 더 필요함
- **절약 금액 추정**("총 평균 얼마 아꼈는지")**: 지금 DB엔 재료 가격
  데이터가 전혀 없다 — 레시피별 재료 목록은 있지만 가격표는 없음. "완료
  1회당 평균 X원"처럼 대략치를 낼지, 실제 쿠팡 연동 가격을 붙일지부터
  정해야 계산 로직을 짤 수 있음
- **모두가 요청 없이 앱을 켜면 뜨는 알림 팝업**(공유 요청 등): 바로 위
  항목과 이어지는 것으로, 요청/수락 데이터를 어디에 쌓고 언제 팝업을
  띄울지(마이페이지 진입 시? 앱 전체 진입 시?) 설계가 필요함

### 요리 캘린더 탭 아이콘이 실제로는 안 보이던 버그 + 목표 위계 재정리

#### 하단 탭 아이콘이 배포판에서 안 보이던 문제(실제 버그)
"아이콘이 없다"는 보고를 받고 확인해보니 — `BottomNavBar.tsx`의 렌더
부분이 여전히 모든 탭에 대해 무조건 `<img src={isActive ? nav.icon :
nav.iconInactive}>` 를 그리고 있었다. 요리 캘린더 항목에는 PNG 대신
`renderIcon` 함수만 넣어 뒀는데, 그 값을 실제로 쓰는 분기를 빼먹어서
`nav.icon`/`nav.iconInactive`가 `undefined`인 채로 `<img>`가 렌더링되고
있었다 — 캐시나 배포 지연 문제가 아니라 순수한 로직 버그였다.
`'renderIcon' in nav` 로 분기해 실제로 SVG를 그리도록 수정. 로컬에서
`hasSvg` 를 직접 확인해 재발 방지 확인.

#### 목표 위계 재정리
"주별로 볼 때랑 월별로 볼 때 목표가 똑같이 나오는 게 이상하다, 월 단위
목표면 일/주/월 선택 버튼보다 위에 있어야 하지 않냐"는 지적이 맞았다 —
목표 카드를 일/주/월 전환 버튼 **위**, 화면에서 가장 먼저 오는 자리로
옮기고, "이번 달 목표" 대신 **"{n}월 목표"**로 실제 몇 월인지 못 박아
표기했다. 이렇게 하면 일/주 보기로 바꿔도 같은 카드가 계속 보이는 게
당연하게 읽힌다 — 애초에 월 단위 개념이라고 라벨이 말해주기 때문.

#### 그룹 설정 문구 개선
"재료 합치기 허용" 토글의 설명이 뭘 의미하는지 애매하다는 지적 — 사용자가
직접 제안한 문구("꺼두면 누가 참여하든 그 사람이 갖고 있던 재료는 합쳐지지
않고 그대로 보존돼요")로 교체해 끔의 실제 결과를 명확히 했다.

#### 검증 (실측)
- 로컬 브라우저에서 하단 탭의 요리 캘린더 아이콘이 `<svg>`로 렌더링되고
  비활성 색(`#9A9AA2`, 회색)이 실제로 보이는지 확인
- 월 목표 카드가 일/주/월 전환 버튼 위에 위치, "주" 보기로 바꿔도 "8월
  목표" 카드가 그대로(값도 동일하게) 유지되는 것 확인

### 공유 요청 기능 신규 + 캘린더/그룹 UX 8건 정리

#### 요리 캘린더
- **하단 탭 아이콘 활성/비활성 모양이 서로 다르던 문제**: 활성 상태에만
  격자 눈금을 그려 넣어서, 다른 탭들(같은 모양에 채움/선만 다름)과 달리
  모양 자체가 바뀌어 보였다 — 눈금을 빼고 둥근 사각 + 위쪽 손잡이 2개라는
  같은 모양을 채움/선으로만 구분하도록 정리
- **목표 카드**: "{n}월 목표" → "{연도}년 {n}월 목표"로 연도까지 표기.
  기본값 12회 → **20회**로 상향(이미 만들어진 계정들도 아직 아무도 손대지
  않은 기본값이라 일괄 20으로 올림 — DB에 직접 확인 후 5개 계정 업데이트).
  "매월 1일에 진행률이 다시 0%부터 시작돼요. 목표는 계정별 개인 설정이라
  그룹원마다 따로 정할 수 있어요(공동 목표 아님)" 안내 문구 추가 — "이거
  가족 공동 목표냐"는 질문에 대한 답이자, 매달 초기화되는 게 목표 숫자가
  아니라 달성 퍼센트라는 것도 명확히 함
- **목표 수정 UX**: "목표 12회 수정"처럼 값과 버튼이 붙어 있던 걸 "목표
  12회" + 별도 [목표수정] 버튼으로 분리. 입력창에 포커스를 잃으면(Enter
  포함) 자동 저장되도록 `onBlur` 추가 — 전에는 저장 버튼을 안 누르고 다른
  곳을 누르면 입력값이 반영도 취소도 안 된 채 남아 있었음

#### 그룹 설정 문구 재정리
"재료 합치기 허용을 꺼도 그 사람 재료가 그냥 없어지는 거 아니냐, 우리
재료는 안 바뀐다는 걸 분명히 해야 한다"는 지적 — 관련된 문구 3곳
(그룹 설정 토글, 참여 다이얼로그, 초대 링크 참여 페이지) 모두 "참여자의
기존 재료는 지워지지 않고 보존되고, 대신 우리 그룹의 기존 재료를 보게
된다 — 우리 쪽 재료는 전혀 바뀌지 않는다"는 세 가지 사실을 한 문장에
다 담도록 다시 썼다.

#### 마이페이지: 배지 간소화 + 모두 보기/나의 것만 보기 토글
- 카드 배지를 "OO님도 즐겨찾기함"(문장) → "OO"(닉네임만)로 줄였다. 세
  영역이 한 화면에 나열되면 문장형 배지가 계속 반복돼 오히려 안 읽힌다는
  지적 반영 — 어느 영역인지는 섹션 제목이 이미 말해 주므로 배지는 누가
  했는지만 짧게 보여주면 된다
- 즐겨찾기·기록·완료 세 영역 전체를 아우르는 **[우리 식구 모두 보기] /
  [나의 것만 보기]** 토글을 그 위에 추가(기본은 모두 보기). "이 화면이
  그룹원 전체 걸 보여주는 공간이 맞다"는 걸 사용자가 직접 확인하고 원하면
  개인 목록으로 되돌릴 수 있게 했다 — 배지가 안 보인다는 문의가 실제로는
  "이 화면 자체가 뭘 보여주는 건지" 에 대한 혼란과 겹쳐 있을 수 있어서,
  토글로 그 경계를 명시적으로 만들었다

#### 공유 요청 기능 신규 (앱을 켰을 때 뜨는 팝업 방식)
전에 "웹 푸시까지는 큰 인프라라 필요하다"고 남겨 뒀던 것을, "앱이 완전히
꺼져 있을 때 말고, 앱을 켰을 때 팝업으로 물어보는 정도"로 범위를 좁혀서
이번에 구현했다.
- `household_share_requests` 테이블 신규(requester_id, target_id, status).
- `GET /api/households/members/<id>/stats`: 같은 그룹원의 즐겨찾기/기록/
  완료 **개수만** 준다(목록 아님) — 요청 팝업에 "이 정도 활동이 있다"는
  요약용
- `POST /api/households/share-requests`: 비공개 멤버에게 공유 요청 보내기.
  이미 공유 중이거나 이미 보낸 pending 요청이 있으면 막음
- `GET /api/households/share-requests/pending`: 내가 응답해야 할 요청 목록
- `POST /api/households/share-requests/<id>/respond`: 수락하면 내
  `share_recipe_actions`를 켠다
- `HouseholdSection.tsx`: 비공개인 다른 멤버 행을 누르면 그 사람의 활동
  개수 요약 + [공유 요청하기] 팝업이 뜬다
- `MyPage.tsx`: 마이페이지 진입 시 대기 중인 요청이 있으면 "OO님이
  공유해 달라고 요청했어요 [거절]/[수락]" 팝업이 뜬다(배경 클릭으로
  안 닫힘 — 응답해야 넘어가게)

#### 검증 (실측)
- A가 B(비공개)의 통계 조회 → 정확한 개수 확인 → A가 요청 전송 → B가
  대기 목록에서 확인 → B가 수락 → B의 `share_recipe_actions`가 실제로
  켜지고 대기 목록에서 사라짐, 이미 공유 중인 상태에서 재요청 시 정상
  거부되는 것까지 curl로 전 과정 확인
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

#### 답변만 하고 코드는 그대로 둔 것
- **캘린더/마이페이지에서 그룹원 데이터가 안 보인다는 문제**: 코드 자체는
  이미 여러 번 실제 계정 2개로 직접 재현해 정상 동작을 확인한 상태다.
  실제 원인일 가능성이 높은 건 (1) 상대방의 `share_recipe_actions`가
  꺼져 있는 경우(마이페이지 "우리 식구 모두 보기" 토글 옆, 그룹 멤버 목록의
  🟢/⚪ 표시로 확인 가능해짐 — 이번 업데이트로 그 확인이 더 쉬워졌다) 또는
  (2) 상대방이 아직 아무 즐겨찾기/완료/기록도 하지 않은 경우. 위 배지
  상태 표시로 먼저 확인해 보고, 그래도 재현되면 구체적으로 알려주면 좋겠다
- **초대 코드가 링크랑 따로 있는 이유**: 둘 다 이미 구현돼 있다 — "초대
  코드로 참여하기" 버튼으로 8자리 코드를 직접 입력하는 것도 완전히 별도로
  동작하는 방법이고, "초대 보내기"가 만드는 링크는 그 코드를 URL에 담아
  자동으로 입력해 주는 편의 기능일 뿐이다. 코드만 따로 있는 이유는 전화로
  불러주거나 문자로 텍스트만 보낼 때처럼, 클릭 가능한 링크 렌더링이 안 되는
  상황에서도 쓸 수 있게 하기 위함
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### 절약액 추정 + 모바일 월 캘린더 가로 넘침 수정 + 목표 게이지 인원별 색 분할

#### 절약액 추정
"이 정도 요리하면 외식/배달 대비 얼마 아꼈는지 보고 싶다"는 요청. 재료
가격 데이터가 DB에 없어 정확한 계산은 불가능하지만, 끼니당 대략적인 평균
절약액(외식/배달 대비 한 끼 8,000원으로 추정 — `ESTIMATED_SAVINGS_PER_MEAL`
상수)에 이번 달 완료 횟수를 곱하는 방식으로 대략치는 낼 수 있었다. 목표
카드에 "💰 이번 달 예상 절약액 약 64,000원 (한 끼 8,000원 추정 × 8회)"
형태로 표시 — 실제 계산이 아니라 추정치라는 걸 문구에 명시해 오해를 막는다.

#### 크롬 개발자도구 모바일 에뮬레이션에서 월 캘린더가 화면을 넘어가던 문제
CSS Grid의 잘 알려진 함정 — `gridTemplateColumns: 'repeat(7, 1fr)'`의
`1fr`은 기본적으로 `minmax(auto, 1fr)`로 취급돼서, 칸 안의 내용(요일 배지
여러 개)이 그 칸의 몫보다 넓어지면 **grid 자체가 줄어들지 않고 넘쳐버린다**.
`repeat(7, minmax(0, 1fr))`로 바꿔서 칸이 필요하면 실제로 좁아지도록 수정.
크롬 F12 모바일 에뮬레이션(375px)에서 `document.documentElement.scrollWidth`가
`window.innerWidth`와 같아지는 것으로 확인.

#### 목표 게이지를 인원별 색으로 분할
"A가 7회, B가 3회 했으면 게이지도 그 비율만큼 각자 색으로 나뉘어 채워지고,
아래 범례에 색-이름-횟수가 있어야 한다"는 요청. 완료 횟수가 많은 사람
순서대로 막대 앞부분부터 채우도록 다시 짰다:
- `monthlyByUser`(이 달 전체, 지금 보고 있는 일/주/월 뷰와 무관 — 목표는
  달 단위 개념이므로) 를 내림차순 정렬해 각자 몫(`count/목표 × 100%`)만큼
  이어 붙이고, 합이 100%를 넘으면 시각적으로만 잘라낸다(`goalSegments`)
- 각 구간은 같은 페이지 다른 곳(달력 점, 요약 범례)과 같은
  `colorForUser()` 색을 그대로 써서, 화면 전체에서 "이 색 = 이 사람"이
  일관되게 유지된다
- 게이지 아래에 "🔵 GaugeA 5회 · 🟠 GaugeB 3회" 범례 추가. 혼자(그룹 아님)면
  전과 같은 단색 막대 그대로 유지
- "N회/목표회 달성" 의 N도 지금 보고 있는 뷰가 아니라 이 달 전체 합계로
  고정(이전에는 "내 것만"이었는데, 게이지가 그룹 합계를 보여주는 이상 이
  숫자도 같은 기준이어야 앞뒤가 맞음)

#### 검증 (실측)
- A 5회 + B 3회(그룹, 목표 20회) → 게이지 파랑 25%(5/20) + 주황
  15%(3/20) = 40%, "8회 / 20회 달성 (40%)" 정상 계산, 범례 "GaugeA 5회 ·
  GaugeB 3회" 순서(내림차순) 정상 확인
- 절약액 "8,000원 × 8회 = 64,000원" 계산 정확성 확인
- 크롬 모바일 에뮬레이션(375px)에서 캘린더 페이지 가로 스크롤 없음(scrollWidth
  == innerWidth) 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

## 2026-08-28

### 이번 달 목표를 "개인 설정"에서 "그룹 공동 목표"로 정정
전날 목표를 계정별 개인 설정(`users.monthly_cooking_goal`)으로 구현했는데,
"그룹 전체에 대한 목표인거야!"라는 정정을 받고 그룹이 있을 땐 그룹 전체가
공유하는 하나의 값으로 바꿨다.

- `households` 테이블에 `monthly_cooking_goal INT DEFAULT 20` 컬럼 추가
  (`ensure_households_table()`), 그룹이 있으면 이 값을 쓰고 없으면(혼자)
  기존 `users.monthly_cooking_goal`을 그대로 폴백으로 씀
- `POST /api/households/goal` 신설 — 그룹원 누구나 그룹 공동 목표를 바꿀 수
  있음("모두 동등" 원칙, 재료 합치기 허용 토글과 같은 방식)
- `GET /api/households/me/completed-calendar` 응답에 `group_goal` /
  `my_personal_goal` / `household_size` 필드 추가. 프론트는
  `isInHousehold ? groupGoal : personalGoal` 로 어느 쪽을 쓸지 결정
- `CookingCalendar.tsx`의 목표 카드 안내 문구도 "그룹원마다 따로 정할 수
  있어요(공동 목표 아님)" → "이 목표는 그룹 전체가 공유하는 값이라 누가
  바꿔도 모두에게 적용돼요"로 정정
- 검증: 계정 A/B로 그룹을 만들고 A가 목표를 15회로 바꾸면 B의 캘린더
  조회에도 즉시 15회로 반영되는 것을 API 레벨에서 확인

### 절약액 계산에 쓰는 "식구 수" 직접 설정 기능 추가
"그룹에 연동된 계정 수를 식구 수로 가정해 곱해주지만, 아이가 있는 집은
계정 없이도 같이 먹으니 그 수를 늘릴 수 있어야 한다"는 요청. 연동 계정
수와는 별개로 그룹(또는 혼자면 개인)이 직접 조정하는 `family_size` 값을
추가했다.

- `households.family_size INT NULL`, `users.family_size INT NULL` 컬럼
  추가. NULL이면(설정 안 함) 그룹은 연동 계정 수, 혼자면 1을 기본값으로
  대신 씀 — `family_size_is_custom` 필드로 프론트에 직접 설정 여부를 알림
- `POST /api/households/family-size`(그룹, 누구나 변경 가능), `POST
  /api/users/<id>/family-size`(혼자) 신설. 1~20 범위 검증
- 절약액 계산을 `이번 달 완료 횟수 × 8,000원 × family_size`로 변경하고,
  캡션 옆에 작은 인라인 수정 버튼("N명" 클릭 → 숫자 입력, 포커스 아웃 시
  저장)을 둬서 목표 수정과 같은 방식으로 조정 가능하게 함
- 검증: A/B 그룹에서 A가 family_size를 4로 바꾸면 B의 캘린더 조회에도
  4로 반영, 값을 null로 리셋하면 다시 연동 계정 수(2)로 자동 폴백되는 것
  확인. 혼자 계정에서도 기본값 1 → 직접 3으로 변경 → 재조회 시 3 유지 확인.
  `family_size=25`(범위 밖) 요청 시 400 확인

### 요리 캘린더: 목표 카드와 달력 카드 사이 시각적 구분 + 일/주/월 카드 분리
"목표 영역과 달력 영역이 너무 붙어 있어서 모바일에서 한눈에 구분이 안
된다"는 지적. 일/주/월 전환 버튼부터 달력 그리드까지를 흰 배경 +
테두리가 있는 별도 카드로 묶어, 회색 배경의 목표 카드와 명확히 분리되도록
했다(두 카드 사이 14px 여백 추가).

### 요리 캘린더 하단 탭 아이콘 색상 수정
다른 탭(마이페이지 등)의 실제 PNG 아이콘을 직접 열어서 확인해 보니
"_white"라는 파일명과 달리 실제로는 흰 배경 위 **검정** 선 아이콘이었고,
활성/비활성 구분은 색이 아니라 "채움(활성) vs 얇은 선(비활성)"으로만
이뤄지고 있었다. 캘린더 아이콘만 회색(#9A9AA2)으로 그려져 있던 걸 검정
(#1A1A1E)으로 맞추고, 채운 상태가 "그냥 검정 사각형"처럼 안 보이도록
안쪽에 흰색 헤더 띠 + 손잡이를 파낸 것처럼 그려 넣었다.

### 마이페이지: "가족" → "식구"로 용어 통일
캘린더 등 최근에 쓴 문구는 "식구"(食口 — 함께 밥 먹는 사이, 혈연이 아니어도
쓸 수 있는 말)로 통일했는데, 마이페이지의 그룹 관리 카드는 예전 표현인
"가족 그룹"이 그대로 남아 있어 같은 기능을 가리키는 두 용어가 섞여 있었다.
`HouseholdSection.tsx`, `JoinHousehold.tsx`의 사용자 노출 문구를 모두
"식구 그룹"으로 통일(초대 문구, 다이얼로그 제목, 안내 문구 등). 검색·추천
키워드처럼 이 기능과 무관하게 "가족"을 쓰는 곳(`RecipeList.tsx` 등)은
그대로 둠.

### 마이페이지: "우리 식구 모두 보기" 토글 위치 & 구분선 정정
- **위계 순서 오류**: 즐겨찾기/기록/완료 개수를 보여주는 숫자 카드가 그
  개수를 어느 기준(모두 vs 나만)으로 셀지 정하는 토글보다 **위에** 있어서
  원인(토글)과 결과(숫자)의 순서가 거꾸로였다. 토글을 숫자 카드 위로
  옮겼다.
- **구분선 오해**: 숫자 카드 바로 아래 `SectionBand`(면 구분선)가 있어서,
  사실은 이어지는 이야기(요약 숫자 → 그 아래 상세 목록)인데 마치 완전히
  다른 얘기가 시작되는 것처럼 보였다. 이 구분선을 없애고 여백(20px)만
  남겼다 — 즐겨찾기→기록→완료 목록 사이의 `SectionBand`는 실제로 서로
  다른 섹션이라 그대로 뒀다.

### 마이페이지: "식구 그룹" 카드 접기/펼치기
그룹에 속하면 초대 코드·멤버 목록·재료 합치기 토글·코드 재발급/나가기
버튼까지 카드가 계속 길어져 마이페이지 첫 화면을 다 차지했다. 제목과 한
줄 설명("같은 초대 코드로 들어온 식구와 냉장고 재료를...")만 항상 보이게
하고, 나머지는 제목 오른쪽 화살표 버튼으로 펼쳤다 접었다 하게 했다.
기본은 접힌 상태. 그룹이 없을 때는 원래도 설명 한 줄 + 버튼 2개뿐이라
접을 이유가 없어 그대로 뒀다.

- 구현 중 `{expanded && (<div/> {...} <div/>)}` 처럼 여러 형제 JSX
  엘리먼트를 감싸는 걸 빠뜨려 `<>...</>` 프래그먼트 없이 나열해 버렸고,
  이게 `tsc --noEmit`은 통과하지만 Vite(esbuild/babel)에서는 실제
  구문 오류로 걸렸다 — 브라우저에서 직접 열어 콘솔 에러로 잡아내 수정.
  타입체크만으로는 못 잡는 오류가 있다는 걸 다시 확인한 사례라 기록해 둠

#### 검증 (실측)
- 계정 A/B로 그룹 생성 후 A가 그룹 목표(15회)·식구 수(4명)를 바꾸면 B의
  캘린더 조회에 즉시 반영되는 것을 API로 확인, 혼자 계정에서 개인 목표/
  식구 수가 그룹과 완전히 분리되어 동작하는 것도 확인
- 프론트에서 목표 카드가 "목표 15회", 절약액 "32,000원(= 8,000×2회×2명)"
  → 식구 수를 "4명"으로 수정하니 "64,000원(= 8,000×2회×4명)"으로 즉시
  재계산되는 것 확인
- 마이페이지에서 토글이 숫자 카드 위로 이동하고 그 사이에 구분선이 없어진
  것, "식구 그룹" 문구로 통일된 것을 실제 화면에서 확인
- "식구 그룹" 카드가 기본 접힘 상태로 제목+설명만 보이다가, 화살표를
  누르면 초대 코드/멤버/토글/버튼이 펼쳐지는 것을 실제 브라우저에서 확인
- 테스트로 만든 `@test.local` 계정 전부(가입한 유저, 완료/즐겨찾기 기록,
  빈 households row 포함) 확인 후 프로덕션 DB에서 직접 삭제
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### "B 계정으로 완료해도 캘린더에 안 보인다" 원인 발견 — 프로덕션 DB 직접 조회로 확인
반복해서 들어온 문제 제기라, 이번엔 추측 대신 프로덕션 DB를 직접 조회해
원인을 찾았다. household 11(구글/네이버/카카오 로그인 계정 3개로 구성된
실사용자 본인의 테스트 그룹)에서 계정 하나(`920803hj@gmail.com`, 구글
로그인)만 `share_recipe_actions = 0`(비공개)이었다 — 의도한 프라이버시
설정이 정확히 동작한 것이었지, 버그가 아니었다.

다만 이 과정에서 실제 기능 공백을 하나 찾았다: **본인이 스스로 자기
공유를 다시 켤 방법이 없었다.** 기존에는 "다른 그룹원이 공유 요청을
보내고 내가 수락"하는 경로만 있어서, 요청을 받기 전까지는 본인 의지로
켤 수 없었다. 이게 실제로 이 문제가 반복해서 제기된 이유로 보인다 —
꺼진 걸 본인이 알아도 스스로 고칠 방법이 없었으니까.

- `POST /api/households/my-sharing` 신설 — 내 `share_recipe_actions`를
  내가 직접 켜고 끔 (그룹 여부 무관하게 허용, 나중에 그룹을 만들 수도
  있으니)
- `HouseholdSection.tsx`의 멤버 목록에서, 본인 행은 눌러서 바로 토글되게
  변경(라벨도 "즐겨찾기·완료·기록 공유 중 · 클릭해서 끄기"처럼 명확하게).
  전에는 본인 행이 아예 클릭 불가능한 `<div>`였다
- 검증: A/B 그룹에서 B가 공유 꺼진 채 완료 → A의 캘린더에 정말 안 보이는
  것 재현 → B가 새 self-toggle로 켜기 → A의 캘린더에 즉시 반영되는 것을
  API와 실제 브라우저(멤버 목록 클릭) 양쪽에서 확인
- 실사용자의 진짜 계정(`920803hj@gmail.com`)은 자동 UPDATE로 건드리지
  않음 — 직접 DB를 고치는 건 자동화 분류기가 막았고, 본인 데이터라도
  동의 없이 조용히 바꾸지 않는 게 맞다고 판단해 앱에서 새 스위치로 직접
  켜도록 안내함

### 그룹 설정 카드: 제목 변경 + 펼치기 버튼을 눈에 띄게
"식구 그룹" 카드의 펼치기/접기가 작은 화살표 아이콘 하나뿐이라 눈에 잘
안 띈다는 지적. 텍스트(펼치기/접기) + 화살표가 있는 알약 버튼으로 바꾸고,
카드 제목도 "식구 그룹" → "그룹 설정"으로 바꿨다(초대 문구·다이얼로그
제목처럼 그룹 자체를 가리키는 곳은 "식구" 표현을 그대로 둠 — 이건 설정
화면이라는 성격을 더 잘 나타내는 이름으로).

### 마이페이지: 그룹 설정과 레시피 활용 내역 사이 구분선 추가
"내 정보/그룹 설정 영역과, 그 아래 즐겨찾기·기록·완료 내역 영역 사이에도
구분선이 있어야 한다"는 요청. `HouseholdSection` 카드와 "우리 식구 모두
보기" 토글 사이에 `SectionBand`를 추가했다. 화면 최상위(좌우 패딩 0)에
들어가는 자리라 다른 곳(paddingLeft/Right 14px 안에서 bleed=14로 상쇄)과
달리 `bleed=0`으로 줘야 밴드가 화면 밖으로 새지 않는다는 걸 확인하고
반영. 반대로 토글 바로 아래(즐겨찾기 목록 시작 지점)는 전 업데이트에서
이미 구분선을 없앴으므로 그대로 뒀다 — 토글이 세 목록 전체를 포괄한다는
게 위쪽 구분선 하나로 명확해졌다.

### 요리 캘린더: 목표 카드 접기/펼치기 + "식구수 수정" 버튼 명확화
"목표 카드가 절약액 줄까지는 보이고, 그 아래는 접고 펼 수 있으면 좋겠다
(캘린더가 한 화면에 안 보인다)"는 요청. 목표·달성률·절약액까지는 항상
보이게 두고, 인원별 색 범례와 하단 안내 문구만 "자세히 보기/접기" 알약
버튼으로 접었다 폈다 하게 했다(기본 접힘). 또 식구 수를 숫자 자체를
눌러야만 수정할 수 있어 발견하기 어렵다는 지적에 맞춰, "목표수정"과
같은 패턴으로 눈에 띄는 "식구수 수정" 버튼을 따로 뒀다.

### 요리 캘린더 탭 아이콘: 이모지처럼 알록달록해 보이는 문제 재수정
전에 채운(활성) 상태에 흰색 헤더 띠 + 손잡이를 "파내" 넣은 게 오히려
"이모지 같다"는 지적을 받았다. 다른 탭 PNG(`navigator_popularity_black/
white.png` 등)를 다시 열어 비교해 보니, 두 상태의 차이는 순수하게
"같은 도형을 얇은 선으로 그리느냐 vs 그 도형을 통째로 단색으로
채우느냐" 뿐이고 색이 여러 개 섞이거나 도형 안을 파내는 디테일은 전혀
없었다. 같은 도형(몸통 사각형 + 위쪽 손잡이 2개)을 비활성은 얇은 선
(strokeWidth 1.3 — 참고 PNG들의 선 굵기 비율에 맞춤, 전엔 1.7로 다른
탭보다 두꺼웠다)으로, 활성은 그 도형 그대로 단색 채움으로만 다시 그렸다.

#### 검증 (실측)
- 프로덕션 DB 직접 조회로 실사용자의 household 11에서 계정 하나
  (`920803hj@gmail.com`)만 `share_recipe_actions=0`인 것 확인 — 반복
  제기된 "B 계정 기록이 캘린더에 안 보임" 문제의 실제 원인
- A/B 테스트 계정으로 위 시나리오 재현(B 비공개로 완료 → A 캘린더에
  안 보임) → 새 self-toggle 엔드포인트/UI로 B가 직접 켬 → A 캘린더에
  "총 1회, VtB 1회"로 즉시 반영되는 것 확인
- 마이페이지에서 "그룹 설정" 제목, 눈에 띄는 "펼치기/접기" 버튼, 본인 행
  클릭 시 "공유 중 ↔ 비공개" 즉시 토글되는 것을 실제 브라우저에서 확인
- 요리 캘린더에서 목표 카드가 절약액 줄까지 기본 노출되고, "자세히 보기"
  클릭 시 범례·안내 문구가 펼쳐지는 것, "식구수 수정" 버튼으로 2명 →
  5명 변경 시 절약액이 16,000원 → 40,000원으로 재계산되는 것 확인
- 캘린더 탭 아이콘이 비활성일 때 strokeWidth 1.3의 얇은 선, 활성일 때
  다른 색 섞임 없는 단색(#1A1A1E) 채움인 것을 렌더된 SVG로 직접 확인
- 테스트로 만든 `@test.local` 계정 전부 확인 후 프로덕션 DB에서 직접 삭제
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### 위 업데이트에 대한 후속 피드백 6건 정리

#### 캘린더 탭 아이콘 — 비활성 선 더 얇게
strokeWidth 1.3도 다른 탭보다 두껍다는 지적. 1로 낮춤.

#### 목표/식구 수 수정에 명시적 [적용] 버튼 추가
"수정 버튼을 누르면 입력창으로 바뀌는데, 그 다음 어떻게 적용되는지 모르겠다"
(포커스를 잃으면 자동 저장되는 방식이라 눈에 보이는 커밋 동작이 없었음).
목표수정·식구수 수정 둘 다 같은 패턴으로 바꿨다: 입력창 옆에 **[적용]**
버튼을 두고, 그 버튼을 누르거나 입력창에서 Enter를 눌러야만 저장되게
했다. 기존 `onBlur`로 조용히 저장하던 동작은 제거 — 포커스를 잃는 것만으로
값이 바뀌면 실수로 딴 곳을 눌렀을 때도 저장돼 버린다.

#### 그룹 설정: "클릭해서 끄기"를 실제 버튼으로 분리
본인 행에서 상태 텍스트("공유 중")와 액션("클릭해서 끄기")이 같은 줄에
글자로만 붙어 있어 뭐가 상태고 뭐가 버튼인지 헷갈린다는 지적. 상태 텍스트는
그대로 두고, 오른쪽에 별도의 **[켜기]/[끄기]** 버튼 엘리먼트를 뒀다.

#### "그룹 나가기" 확인 팝업 줄바꿈 정리
`Dialog`의 본문에 `word-break: keep-all`은 이미 걸려 있었지만, 원인 문장이
길게 이어 붙어 있어(예: "...가져가요(그룹에는 그대로 남아요). 즐겨찾기...")
좁은 팝업 폭에서 괄호 앞뒤나 문장 중간의 애매한 지점에서 줄이 꺾였다.
문장을 의미 단위로 쪼개 `<br/>`로 명시적으로 줄바꿈하도록 바꿨다(예:
"지금 그룹 재료를 복사해서 가져가요." / "그룹에는 그대로 남아요." / (빈 줄) /
"즐겨찾기·완료·기록은 원래 그대로예요.").

#### 마이페이지 상단 구분선 — "반영 안 됐다"는 재확인 결과
지난 업데이트에서 그룹 설정과 레시피 활동 내역 사이에 넣은 구분선은
코드상 정상 렌더되고 있는 것을 다시 확인했다(`bleed=0`이 필요한 이유까지
포함). 실제 문제 제기는 이 구분선이 아니라 **그 아래, 즐겨찾기·기록·완료
세 목록 사이의 구분선**이었다 — 이어지는 후속 항목 참고.

#### 즐겨찾기/기록/완료 사이 구분선이 너무 굵어서 별개 영역처럼 보임
세 목록 모두 "우리 식구 모두 보기" 토글이 지배하는 하나의 이야기인데, 그
사이 구분선이 화면 최상단(설정↔활동 내역 사이)과 똑같이 굵은 회색 면이라
"완전히 무관한 영역"처럼 읽힌다는 지적. `SectionBand`에 `subtle` prop을
추가해 얇은 1px 선 모드를 만들고, 이 세 목록 사이에만 적용했다. 정말 다른
주제로 넘어가는 지점(설정 → 활동 내역)은 기존 굵은 면 그대로 둬서, 이제
"완전히 다른 얘기"와 "같은 얘기의 하위 구분"이 시각적으로 구분된다.

#### 기능 문서화 — `HOUSEHOLD_FEATURE.md` 신설
"나중에 기능 이해하는 데 어려움 없도록 변경 내용을 md 파일에 정리해 달라"는
요청. `CHANGELOG.md`는 시간순 변경 히스토리라 "지금 이 기능이 어떻게
동작하는지"를 알려면 여러 날짜의 항목을 다시 읽어야 했다. 그 대신 현재
동작 방식만 정리한 `HOUSEHOLD_FEATURE.md`를 새로 만들었다 — 그룹 생성/참여/
탈퇴, 재료 공유(리다이렉션 방식), 즐겨찾기·완료·기록 공유 판정과 자주
헷갈리는 포인트("B 계정이 캘린더에 안 보이는" 문제의 원인), 그룹 공동
목표 vs 개인 목표, 식구 수와 절약액 계산, 화면 레이아웃 위계, 엔드포인트
전체 목록, 테스트 시 주의사항까지 포함. `DATABASE_SCHEMA.md`에도
`households`/`household_share_requests` 테이블과 `users`의 새 컬럼들을
반영하고 두 문서를 서로 링크했다. `README.md`/`PROJECT_OVERVIEW.md`의
문서 목록에도 추가.

#### 검증 (실측)
- 캘린더 탭 비활성 아이콘의 렌더된 SVG `stroke-width`가 1인 것을 직접 확인
- 목표수정·식구수 수정 모두: 버튼 클릭 → 입력창+[적용] 노출 → 값 변경 후
  [적용] 클릭 → 실제 반영(목표 20→25회, 식구 수 2→4명, 절약액 재계산)
  확인. 입력창 포커스만 잃었을 때는 저장되지 않는 것도 확인
- 그룹 설정 멤버 목록에서 본인 행의 [끄기]/[켜기] 버튼이 상태 텍스트와
  분리되어 있고, 클릭 시 토스트("비공개로 바꿨어요" 등)와 함께 즉시
  반영되는 것 확인
- "그룹 나가기" 팝업의 실제 DOM 텍스트를 읽어 줄바꿈이 문장 경계에서
  일어나는 것 확인
- 마이페이지의 구분선 3개를 실제 DOM에서 조회 — 설정↔활동 내역 사이는
  8px 굵은 면, 즐겨찾기↔기록↔완료 사이 2개는 1px 얇은 선인 것 확인
- 테스트로 만든 `@test.local` 계정 전부 확인 후 프로덕션 DB에서 직접 삭제
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### 한 끼 절약 추정액도 식구 수처럼 직접 설정 가능하게
"식구 수는 수정 가능한데 한 끼 추정액(8,000원)은 못 고친다"는 지적. 완전히
같은 패턴으로 만들었다 — `family_size`를 그대로 본떠 `savings_per_meal`을
추가했다.

- `households.savings_per_meal`, `users.savings_per_meal`(둘 다 INT NULL)
  컬럼 추가. NULL이면 `ESTIMATED_SAVINGS_PER_MEAL_DEFAULT`(8,000원)를 대신
  씀
- `POST /api/households/savings-per-meal`(그룹원 누구나), `POST
  /api/users/<id>/savings-per-meal`(혼자) 신설. 0~100,000원 범위 검증
- **그룹에 속해 있으면 이 값도 그룹 전체가 공유한다** — 목표·식구 수와
  같은 원칙("모두 동등"). 한 사람이 바꾸면 그룹원 전원의 캘린더에 즉시
  반영된다
- 캘린더 응답에 `savings_per_meal` / `savings_per_meal_is_custom` 필드
  추가
- 목표 카드의 절약액 계산 줄에 식구 수와 나란히 "추정액 수정" 버튼을 추가
  — 같은 [입력창 + 적용] 패턴
- `HOUSEHOLD_FEATURE.md`, `DATABASE_SCHEMA.md`에 새 컬럼/엔드포인트 반영

#### 검증 (실측)
- A/B 그룹에서 A가 한 끼 추정액을 12,000원으로 바꾸면 B의 캘린더 조회에도
  즉시 12,000원으로 반영되는 것을 API로 확인(직접 설정 전엔 둘 다 기본값
  8,000원인 것도 확인)
- 프론트에서 "추정액 수정" 버튼 → 9,000원 입력 → [적용] → 절약액이
  "9,000원 × 1회 × 2명 = 18,000원"으로 즉시 재계산되는 것 확인
- 테스트로 만든 `@test.local` 계정 전부 확인 후 프로덕션 DB에서 직접 삭제
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### 절약액 표현 2건 추가 정리
- **"1인당" 명시**: "외식·배달 대비 한 끼 8,000원 추정 × 17회 × 식구
  3명"은 8,000원이 1인 기준인지 전체 기준인지 애매했다(식구 수를 곱하는
  이상 1인 기준이어야 계산이 맞음). "외식·배달 대비 **1인** 한 끼"로
  최소한만 고쳐 문장이 길어지지 않게 하면서 애매함을 없앴다.
- **"지금까지" 명시**: "이번 달 예상 절약액"이 "목표를 달성하면 받을 예상
  절약액"인지 "지금까지 완료한 횟수 기준 절약액"인지 헷갈린다는 지적.
  실제 계산은 항상 후자(완료 횟수 `monthlyTotal` 기준)라 "이번 달" →
  "**지금까지**"로 헤드라인 자체를 바꿔 오해 소지를 없앴다.

#### 검증 (실측)
- 완료 1회·식구 3명·기본 8,000원 계정에서 "💰 지금까지 예상 절약액 약
  24,000원" / "외식·배달 대비 1인 한 끼 8,000원 추정 × 1회 × 식구 3명"
  문구를 실제 화면에서 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### 마이페이지 "전체보기"가 "우리 식구 모두 보기" 상태를 무시하던 버그 수정
"'우리 식구 모두 보기'로 본 다음 '전체보기 >'를 누르면 페이지가 넘어가면서
'나의 것만 보기'로 필터가 바뀐다"는 제보. 원인을 찾아보니 `전체보기` 목적지
화면(`IngredientDetail.tsx`, `/mypage/{favorite,recorded,completed}`)이
애초에 마이페이지의 화면 상태를 전혀 모르고 **항상 내 localStorage만
직접 읽게** 짜여 있었다 — "우리 식구 모두 보기"로 보고 있던 그룹 combined
목록은 마이페이지 컴포넌트 안의 state일 뿐이라, 다른 라우트로 넘어가는
순간 사라지고 없었다.

- `navigate(to, { state: { recipes: displayXxxRecipes, isHouseholdView:
  showAllHousehold } })`로, 마이페이지가 지금 보여주고 있던 배열(그룹
  combined든 개인 것만이든)을 라우터 state로 그대로 넘긴다
- `IngredientDetail.tsx`는 `location.state?.recipes`가 있으면 그걸 쓰고,
  없을 때만(직접 URL 진입/새로고침) 예전처럼 localStorage를 읽는다
- 그룹 combined로 넘어온 경우 각 카드에 `acted_by` 기반 배지("OO·OO")도
  붙도록 `VirtualizedRecipeList`에 `getAttributionLabel` prop을 추가(그동안
  `VirtualizedHorizontalRecipeList`에만 있었음), 화면 제목도 "우리 식구가
  ..."로 맞춤
- "전체삭제" 버튼은 내 localStorage만 지우는 동작이라, 여러 사람 항목이
  섞인 "모두 보기" 목록에서는 화면과 실제 결과가 어긋나 이 모드에서는
  숨김

### 그룹 설정 멤버 목록: 상태 텍스트와 액션 버튼 분리 (2건째)
지난 업데이트에서 본인 행의 "클릭해서 끄기"는 이미 별도 버튼으로 뺐는데,
**다른 사람 행**의 "비공개 · 요청하기 >"는 그대로 남아 있었다. "이게
한 줄로 붙어 있으니 '비공개 요청하기'라는 하나의 버튼처럼 보인다(실제
의미와 정반대라 헷갈림)"는 지적. 본인 행과 같은 패턴으로 상태 텍스트만
남기고 오른쪽에 별도 **[공개 요청]** 버튼을 뺐다.

### 요리 캘린더 목표 카드: 인원별 범례를 다시 항상 보이게
지난 업데이트에서 "자세히 보기" 접힘 영역 안에 인원별 색 범례(누가 몇 회
했는지)까지 같이 넣었는데, "이건 게이지 옆에 항상 붙어 있어야지 숨기면
안 된다 — 계산 공식이면 몰라도"라는 지적. 인원별 범례를 게이지 바로
아래(항상 보이는 자리)로 다시 옮기고, "자세히 보기"에는 안내 문구(매월
1일 초기화 등)만 남겼다.

### 공유 요청 팝업을 마이페이지 전용에서 앱 전역으로
"다른 그룹원의 공유 요청 팝업이 마이페이지에 들어가야만 뜬다. 앱에 다시
들어오는 순간 어느 탭에 있든 떠야 하고, 팝업에서 수락을 누르면 바로
반영돼야 하는데 안 된다(마이페이지 가서 직접 [켜기]를 또 눌러야 함)"는
제보. 두 가지가 섞인 문제였다.

- **탭 제한**: 요청 확인 로직이 `MyPage.tsx` 안에서만 돌고 있었다 —
  전용 컴포넌트 `ShareRequestPopup.tsx`를 새로 만들어 `AppRouter.tsx`에
  `<HomeInstallPrompt/>`/`<RecipeChatWidget/>`와 같은 자리(전역, 모든
  라우트 위)에 마운트했다. 확인 시점은 로그인 상태가 잡히는 순간과 앱이
  다시 보이게 될 때(`visibilitychange`/`focus`) — "다시 들어오는 순간"
  요구를 이 두 시점으로 커버한다.
- **수락해도 반영 안 됨**: 실제로는 서버 값(`share_recipe_actions`)은
  수락 즉시 바뀌고 있었다 — 화면(그룹 설정 카드)이 이미 마운트돼 있으면
  그 결과를 알 방법이 없어서 계속 "비공개"로 보였을 뿐이었다. 수락 시
  `window.dispatchEvent(new CustomEvent('household-share-updated'))`로
  전역에 알리고, `HouseholdSection`(멤버 목록)과 `MyPage`(그룹 활동 피드)
  양쪽이 이 이벤트를 들어서 즉시 다시 불러오게 했다.
- **이 과정에서 만든 버그를 바로 잡음**: `MyPage.tsx`의 이벤트 리스너
  `useEffect`를 처음에 의존성 배열 `[]`로 짰더니, `loadHouseholdRecipeFeeds`
  가 `useCallback`으로 감싸지 않은 일반 함수라 "마운트 시점의"(아직 인증이
  안 잡혔을 수 있는) `isLoggedIn`을 가둔 첫 렌더의 함수가 계속 쓰이는
  stale closure가 생겼다 — 이벤트가 올 때마다 "로그인 안 됨" 분기로 빠져
  `isInHousehold`를 엉뚱하게 `false`로 초기화해 버려서, 수락 직후
  "우리 식구 모두 보기" 토글과 그룹 활동 피드가 통째로 사라지고 개인
  localStorage 목록으로 잘못 되돌아갔다. 실제 브라우저 테스트로 이 회귀를
  직접 잡아냈고, 의존성 배열에 `[isLoggedIn, authUser?.id]`를 넣어 고쳤다.

#### 검증 (실측)
- A/B 그룹에서 A가 즐겨찾기한 레시피와 B가 즐겨찾기한(서로 다른) 레시피가
  A의 마이페이지 "우리 식구 모두 보기"에 둘 다 뜨는 것, "전체보기"를 눌러도
  제목이 "우리 식구가 즐겨찾는 레시피"로 유지되고 총 2건(B 배지 포함)이
  그대로 보이는 것을 실제 브라우저에서 확인(수정 전에는 "나의 것만"으로
  되돌아가며 1건만 보였음)
  - B가 공유를 끈 상태에서 A가 공유 요청 → B가 `/my-fridge`(마이페이지가
  아닌 탭)에 있어도 팝업이 뜨는 것 확인
- B가 마이페이지에 그룹 설정을 펼쳐 둔 채로("비공개" 표시) 같은 화면 위
  팝업에서 [수락]을 누르면, 페이지 이동/새로고침 없이 몇 초 안에 "공유
  중 · 끄기"로 바뀌는 것을 확인 — 처음 구현에서는 이 자리에서 "우리 식구
  모두 보기" 토글 자체가 사라지는 회귀가 발생했었고, 의존성 배열 수정 후
  재현되지 않는 것까지 확인
- 테스트로 만든 `@test.local` 계정 전부(가입한 유저, 완료/즐겨찾기 기록,
  빈 households row 포함) 확인 후 프로덕션 DB에서 직접 삭제
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### 카카오톡으로 초대 보내면 링크가 두 번 가던 버그
"초대 보내기 → 카카오톡 전송" 하면 링크가 두 번 들어간다는 제보.
`handleShareInvite`가 `navigator.share({ title, text, url })`를 호출하고
있었는데, `text` 문자열 안에 이미 초대 링크를 넣어 뒀으면서(`text =
"...\n${url}"`) **별도의 `url` 필드에도 같은 링크를 또** 넘기고 있었다.
Web Share API는 `text`와 `url`을 각 공유 대상 앱이 알아서 조합하게
맡기는데, 카카오톡 공유 시트는 이 둘을 이어 붙여서 메시지를 만들기 때문에
결과적으로 같은 링크가 두 번(문장 안에 한 번 + 시트가 붙인 것 한 번)
들어갔다. `url` 필드를 빼고 `text` 하나로만 공유하도록 고쳤다 — 이미
`text` 안에 링크가 온전히 들어 있으므로 정보 손실은 없다.

#### 검증 (실측)
- `navigator.share` 호출부에서 `url` 필드를 제거한 코드로 수정, 프론트
  타입체크(`tsc --noEmit`) 에러 0건 확인. Web Share API는 실제 모바일
  브라우저의 사용자 제스처와 카카오톡 앱 연동이 필요해 자동화 브라우저로
  직접 재현·검증하기 어려운 영역이라, 원인(공유 대상이 `text`+`url`을
  이어 붙이는 동작)을 코드로 확인하고 중복 원인을 제거하는 방식으로 수정함

#### 후속 수정 — 진짜 원인은 `url`이 아니라 `title`이었다
`url` 필드를 뺐는데도 "여전히 두 번 온다"는 재확인. 이번엔 사용자가 결정적
단서를 줬다 — 초대 보내기를 누르면 뜨는 카카오톡 쪽 전송 팝업 자체가
**"메시지 보내기 2/2"** 라고 표시되고 있었다. 이건 링크가 텍스트 안에서
중복 표시되는 게 아니라, 애초에 **전송 항목이 2개로 잡혀서 실제로 메시지가
두 번 전송되는 것**이라는 뜻이다.

Android의 공유 인텐트는 `navigator.share()`의 `title`을 `EXTRA_SUBJECT`,
`text`를 `EXTRA_TEXT`라는 서로 다른 값으로 각각 담아 전달한다. 카카오톡
공유 대상이 이 둘을 하나의 메시지로 합치지 않고 별도의 전송 항목 두 개로
처리해("2/2") 메시지를 두 번 보낸 것으로 보인다. `title: '쿡매치 식구
그룹 초대'`를 완전히 빼고 `text` 하나만 넘기도록 고쳤다 — 초대 문구와
링크는 이미 `text` 안에 다 들어 있어 정보 손실은 없다.

#### 검증 (실측)
- `navigator.share` 호출에서 `title` 필드를 제거, `text` 하나만 남김.
  프론트 타입체크(`tsc --noEmit`) 에러 0건 확인. 역시 실제 검증은 카카오톡
  연동이 필요해 자동화 브라우저로는 어렵고, 사용자가 알려준 "2/2" 표시라는
  구체적 증상으로 원인(제목/본문이 별도 전송 항목으로 분리됨)을 특정해
  수정함 — 실제 기기에서 한 번 더 확인 필요

### 재료 합치기 토글 안내 문구 3건 단순화
"'꺼두면 참여자의 기존 재료는 지워지지 않고 그대로 보존되고, 대신 우리
그룹의 기존 재료를 함께 보게 돼요. 우리 쪽 재료는 전혀 바뀌지 않아요.'
이거 말이 너무 어렵다 — '이걸 켜면 초대되는 사용자의 냉장고 재료도 함께
합쳐져요' 정도 뉘앙스로 한 줄이면 되지 않나"는 지적. 3군데 있던 같은 계열
안내 문구를 전부 한 줄로 줄였다:
- 그룹 정책 토글(`HouseholdSection.tsx`, 새로 참여하는 사람의 재료 합치기
  허용): "이걸 켜두면 새로 참여하는 사람의 냉장고 재료도 함께 합쳐져요."
- 참여자 본인 토글(그룹 생성/`HouseholdSection.tsx`·참여/`JoinHousehold.tsx`
  양쪽, 내 재료를 그룹 재료에 합치기): "이걸 켜면 내 냉장고 재료가 그룹
  재료와 합쳐져요."

꺼졌을 때의 되돌리기 보장("돌려받아요" 등)까지 문구에 다 담으려다 오히려
길고 어려워졌던 것 — 토글은 On 상태의 의미만 짧게 말해도 충분하다는 판단.

#### 검증 (실측)
- 프론트 타입체크(`tsc --noEmit`) 에러 0건 확인. 순수 문구 변경이라 로직
  영향 없음

### "홈 화면에 추가" 팝업 대비 문제 2건
"7일간 보지 않기 버튼이 너무 안 보이고, 반대로 우상단 닫기(×) 버튼은 너무
흰색이라 튄다"는 지적. 이 팝업(`HomeInstallPrompt.tsx`)만 짙은 배경
(`rgba(20,20,20,0.94)`)을 쓰는 유일한 팝업인데, 두 버튼 다 이 어두운
배경을 고려하지 않은 색이었다.

- **닫기(×) 버튼**: 앱 공통 `CloseButton`은 밝은 배경(연한 회색) 전제로
  만들어져 있어, 이 팝업 위에서는 하얀 사각형이 튀어 보였다. `CloseButton`에
  `dark` prop을 추가해(다른 어두운 팝업이 생기면 재사용 가능) 반투명 흰색
  배경(`rgba(255,255,255,0.12)`) + 옅은 흰색 아이콘으로, 카드와 자연스럽게
  어울리면서도 여전히 클릭 가능한 버튼으로 보이게 했다.
- **"7일간 보지 않기" 버튼**: 배경 `#6B5200`(어두운 갈색) 위에 글자
  `#1A1A1E`(거의 검정)를 올려 둬서, 버튼 자체가 카드 배경과 거의 구분이
  안 되고 글자도 안 읽혔다. 옆의 "이번엔 닫기"(`#3A3A42` 배경 + 밝은
  회색 글자, 또렷하게 잘 보임)와 대비되던 부분. 브랜드색(`--brand`,
  `#FFD600`)을 옅게 깐 배경(`rgba(255,214,0,0.16)`) + 밝은 브랜드색
  글자로 바꿔 확실한 대비를 냈다.

#### 검증 (실측)
- 온보딩 가이드를 건너뛰게 세션 플래그를 직접 세팅해 팝업을 띄운 뒤,
  실제 렌더된 두 버튼의 `getComputedStyle`을 읽어 확인: 닫기 버튼은
  `rgba(255,255,255,0.12)` 배경 + `rgba(255,255,255,0.85)` 아이콘,
  "7일간 보지 않기" 버튼은 `rgba(255,214,0,0.16)` 배경 + `rgb(255,214,0)`
  글자로 렌더되는 것을 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### 절약액 계산식 문구에 "절약" 빠진 오타
"외식·배달 대비 1인 한 끼 8,000원 추정 × 4회 × 식구 3명" — "8,000원
추정"이라 8,000원이라는 금액 자체가 추정치라는 뜻처럼 읽혔는데, 실제
의도는 "한 끼에 8,000원을 절약한다고 추정"이다. "절약" 한 글자가 빠진
오타였다 — "8,000원 절약 추정"으로 수정. (참고: 같은 대화에서 나온
"0원 절약 추정인 거지" 언급은 실제 0원 표시 버그가 아니라 이 오타를
가리킨 것으로 확인 — 직접 재현 테스트로 96,000원(8,000×4×3)이 정상
계산되는 것 확인함.)

#### 검증 (실측)
- 프론트 타입체크(`tsc --noEmit`) 에러 0건. 순수 문구 변경 1건

### 목표/식구수/추정액 [적용] 버튼이 찌그러진 원으로 보이던 버그
"인풋박스 활성화되면 옆에 적용 버튼이 있는데 찌그러진 원모양이다"는 지적.
`width: 20, height: 20, borderRadius: '50%'`로 정사각형 원을 그리려 했는데,
`padding: 0`을 안 줘서 앱 전역 `button { padding: 0.6em 1.2em }` 규칙이
그대로 적용돼 버튼이 가로로 늘어나 있었다(가로 패딩 1.2em이 세로 0.6em보다
커서 정사각형이 아니게 됨 — `Button.tsx`에 같은 문제를 겪은 이력이 있는
주석이 있었는데 이 버튼엔 반영이 안 돼 있었다). 원 모양을 억지로 고치는
대신 모서리 둥근 사각형(`padding: '0 8px', borderRadius: 6`)으로 바꿨다 —
사용자가 요청한 대로.

#### 검증 (실측)
- 실제 브라우저에서 버튼을 렌더해 `getBoundingClientRect()`/
  `getComputedStyle()`로 확인: 26.2×22px, `border-radius: 6px` — 찌그러진
  원이 아니라 의도한 모서리 둥근 사각형으로 렌더되는 것 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### 요리 캘린더 + 마이페이지에 당겨서 새로고침 추가
"인스타그램처럼 당겨서 새로고침이 왜 안 되냐"는 질문. 확인해 보니
`index.css`에 `overscroll-behavior: none`이 앱 전체에 걸려 있어 브라우저
기본 당겨서 새로고침 자체를 꺼 둔 상태였다 — 그 기본 동작은 페이지를
통째로 하드 리로드시켜 SPA 상태(로그인, 스크롤 위치, 입력 중이던 값)를
날려버리기 때문에 의도적으로 막아 둔 것으로 보인다. 대신 커스텀 당겨서
새로고침 컴포넌트(`PullToRefresh.tsx`)를 새로 만들어, 터치 제스처만
감지해서 지정된 데이터 재조회 콜백만 다시 실행하게 했다(페이지 자체는
그대로).

- 다른 그룹원의 행동으로 화면 내용이 바뀔 수 있는 두 화면에만 적용:
  **요리 캘린더**(`onRefresh={loadCalendar}`), **마이페이지**
  (`onRefresh={refreshMyPage}` — 내 로컬 목록 + 그룹 활동 피드 둘 다
  재조회). 내 냉장고/냉장고요리는 내가 직접 바꾼 것만 반영되면 되고
  이미 즉시 반영되고 있어 제외
- 구현: 페이지가 맨 위로 스크롤돼 있을 때만 당기기 제스처로 인식(그 외엔
  평범한 스크롤), 고무줄 저항(0.5배)을 줘서 자연스럽게, 64px 이상
  당기고 손을 떼면 상단에 스피너를 보여주며 새로고침, 끝나면 원위치로
  애니메이션
- **실제 기기 테스트로 잡은 버그**: 처음엔 손을 뗄 때(`touchend`)
  `setPullDistance(current => { ...; setRefreshing(true); ... })`처럼
  한 state의 함수형 업데이터 안에서 다른 state의 setter를 바로 호출했는데,
  이게 React의 "Cannot update a component while rendering a different
  component" 경고를 유발했다. 실제 터치 시퀀스를 시뮬레이션해서 콘솔에서
  직접 재현·확인한 뒤, 최신 pull 거리를 ref에도 미러링해 두고 `touchend`
  에서는 그 ref만 읽어 판단하도록 고쳐서 두 state가 서로의 업데이터 안에서
  체이닝되지 않게 정리했다

#### 검증 (실측)
- 새 탭에서 두 화면 모두 합성 터치 이벤트(touchstart/move/end)로 실제
  당기기 제스처를 재현: 당기는 동안 인디케이터/콘텐츠가 정확히
  `거리 × 0.5`만큼 내려가고, 64px 이상에서 손을 떼면 요리 캘린더는
  `/api/households/me/completed-calendar`를 다시 호출하는 것을 네트워크
  로그로 확인, 끝나면 두 화면 다 `translateY(0px)`로 복귀하는 것 확인
- 콘솔 경고 재현 전/후 비교: 수정 전 코드로는 매 새로고침마다 "Cannot
  update a component..." 경고가 재현됐고, ref 기반으로 고친 뒤 완전히
  새 탭에서 같은 시퀀스를 반복해도 콘솔에 에러/경고가 전혀 없는 것을 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### 운영 스크립트 확인 + 임시 스케줄 변경
- **LLM 재료 추출기 정상 실행 확인**: `llm_ingredients.log`에서 오늘
  (2026-08-29) 03:00:01에 시작해 03:37:59에 정상 종료(exit=0)한 것을
  확인 — 5,400건 처리, 5,190건 재료 집합 변경, LLM 오류 12건(정상 범위),
  DB 반영 완료
- **크롤러 스케줄 임시 변경**: 원래 주 1회(월요일 07:00)이던 Windows
  작업 스케줄러 작업("CookMatch-WeeklyCrawler")의 트리거를 당분간 매일
  07:00로 변경. 스크립트(`run_crawlers_scheduled.bat`)와 다른 설정(실행
  권한, 배터리 조건 등)은 그대로 두고 트리거만 `MSFT_TaskWeeklyTrigger`
  (월요일만)에서 `MSFT_TaskDailyTrigger`(매일)로 교체. `README.md`에
  임시 변경임을 표시해 둠 — 원래대로 되돌리려면 다시 주간 트리거로
  바꿔야 함

#### 검증 (실측)
- `Get-ScheduledTask`로 변경 후 트리거를 다시 조회해
  `MSFT_TaskDailyTrigger`, `DaysInterval=1`, 시작 시각 07:00인 것을 확인,
  액션(실행 파일 경로)이 그대로 보존된 것도 확인

### 냉장고요리 페이지: 20개(1페이지)만 뜨고 그 이상은 멈추던 성능 버그
"1페이지(20건)만 뜨고 그 이상은 로드가 안 되고 멈춰 있다, 40개(2페이지)
뜨는 데도 한참 걸린다"는 제보. 서브에이전트로 원인을 찾아본 결과, 프론트·
백엔드 양쪽에 겹쳐 있던 문제였다 — 페이지가 쌓일수록 매 페이지가 이전
페이지보다 점점 더 느려지는(총 계산량이 페이지 수의 제곱에 비례하는)
구조라, "처음 몇 페이지는 그럭저럭 되다가 사실상 멈춘 것처럼 보인다".

- **프론트(`RecipeList.tsx`)**: 백그라운드에서 페이지가 하나씩 도착할
  때마다(20개씩), 그 시점까지 쌓인 레시피 **전체**에 대해
  `calculateMatchRate`(동의어 사전 대조 포함)를 처음부터 다시 계산하고,
  `maxLack` 필터에서도 같은 계산을 한 번 더 중복 실행하고 있었다
  (`useEffect` 의존성이 `cachedFilteredRecipes` 배열 전체였음). 레시피
  id별로 계산 결과를 캐시해 두고(내 재료가 바뀔 때만 캐시를 비움), 새로
  도착한 레시피만 계산하도록 고쳤다 — `maxLack` 필터도 이 캐시된 결과를
  그대로 재사용하게 해서 중복 계산도 없앴다.
- **백엔드(`/api/recipes/filter`)**: 기본 정렬(매칭률순)일 때 COUNT
  쿼리가 `match_rate` 계산식을 WHERE에 맞는 행 전체에 대해 실행하는
  무거운 서브쿼리였는데, 이 무거운 COUNT를 **페이지마다 매번** 다시
  실행하고 있었다. 정작 프론트는 1페이지 응답의 `total`만 실제로 쓰고
  2페이지 이후 응답의 `total`은 아예 무시하고 있어서(코드 확인함),
  `page > 1`이면 이 COUNT 쿼리 자체를 건너뛰도록 고쳤다.

#### 검증 (실측)
- 백엔드 API를 페이지 1/2/3/5/10에 대해 직접 호출해 응답 시간 측정:
  수정 전 구조에서 우려됐던 "페이지가 늘어날수록 점점 느려짐" 패턴 없이
  각각 약 2.0~2.6초로 고르게 나오는 것 확인(수정 전엔 매 페이지가 COUNT
  서브쿼리를 다시 돌려 페이지가 늘어날수록 서버 부하가 누적됐을 것으로
  추정)
- 실제 브라우저(재료 29개 등록, 매칭 구간 30~100%, 총 1,194페이지 대상)
  에서 페이지가 끊기지 않고 계속 진행되는 것을 확인 — 약 20초에 7페이지,
  약 40초에 14페이지까지 꾸준히(페이지당 평균 약 2.5~3초 유지, 점점
  느려지는 패턴 없음) 진행됐고 콘솔에 에러 없음
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

#### 남겨둔 것
- 백엔드 SELECT 쿼리 자체는 `match_rate` 계산식으로 정렬하기 위해
  WHERE에 맞는 행 전체를 계산·정렬한 뒤 LIMIT/OFFSET을 적용하는 구조라,
  페이지당 약 2초 정도는 이 설계상 남아 있다(더 줄이려면 필터+재료
  조합별로 정렬된 결과를 서버에 잠시 캐싱해 두는 등 더 큰 구조 변경이
  필요함 — 이번엔 "페이지가 늘어날수록 점점 느려지다 멈추는" 것만
  고쳤고, "각 페이지 자체의 절대 속도"는 다음 기회에)
- "다른 탭에 가 있어도 냉장고요리 페이지가 계속 백그라운드로 로드되게"
  해 달라는 요청은 별개 논의로 남겨 둠 — 지금은 이 페이지가 마운트돼
  있는 동안만 백그라운드 로딩이 진행되고, 다른 탭으로 이동하면(컴포넌트가
  언마운트되면) 같이 멈춘다. 이걸 마운트 여부와 무관하게 계속 돌게
  하려면 이 로딩 로직을 페이지 컴포넌트 밖(앱 전역 상태)으로 옮기는
  구조 변경이 필요해서, 이번 성능 버그 수정과는 분리해 별도로 논의하기로 함

### 요리 캘린더 비로그인 안내 문구 구체화
비로그인 상태에서 뜨는 문구가 "로그인 후 볼 수 있어요."뿐이라 로그인하면
뭘 얻는지 와닿지 않는다는 지적. 이 화면은 냉장고/레시피 목록과 달리 대신
보여줄 로컬(localStorage) 데이터가 아예 없다 — 완료 기록·목표·절약액이
전부 서버 계정에 묶인 데이터라서다(바로 위 "요리 캘린더 로그인 정책"
질문에 대한 답과 같은 이유). 그래서 "왜 로그인해야 하는지" 자체를 문구로
구체적으로 설명하는 쪽으로 보완했다: "로그인하면 요리 캘린더를 쓸 수
있어요" + "완료한 요리 이력을 날짜별로 관리하고, 절약액을 확인하고,
이번 달 목표도 설정할 수 있어요."

#### 검증 (실측)
- 실제 브라우저에서 로그아웃 상태로 `/cooking-calendar` 진입 시 새
  문구가 그대로 렌더되는 것을 확인

## 2026-08-29

### 이모지 → 검은 테두리 SVG 아이콘으로 교체
`SectionIcon.tsx`가 이미 명시하고 있던 "이모지는 기기·OS마다 다르게
보여서 앱 전체를 선으로 그린 SVG로 통일한다"는 기존 원칙을, 요리
캘린더 로그인 안내 문구에 📅를 새로 추가하면서 어긴 것을 계기로 재확인.
- `CookingCalendar.tsx`: 📅 → `BottomNavBar.tsx`에서 새로 export한
  `CALENDAR_ICON_PATHS` 재사용, 💰 → 동전 더미 SVG, ✓/✎ 유니코드 기호 →
  `CheckIcon`/`PencilIcon` 컴포넌트로 교체
- `MyPage.tsx`: 비밀번호 표시/숨김 토글의 👁️/👁️‍🗨️ → `EyeIcon`/`EyeOffIcon`
  컴포넌트로 교체 (표시 중일 때 뜬 눈이 되도록 방향 재확인)
- 나머지 17개 파일에 남아있는 기존 이모지는 이번엔 범위 밖으로 남겨둠
  (한 번에 전부 바꾸기엔 범위가 커서 사용자 확인 후 진행 예정)

### DB 서버 시간대(UTC) 문제 근본 수정
`SELECT * FROM users`로 직접 조회했을 때 `created_at`이 실제 한국
시간보다 정확하지 않다는 지적으로 원인 확인. Railway MySQL 서버 자체
시계가 UTC(`SELECT NOW()`와 로컬 KST를 직접 대조해 확인)로 돌아가는데,
`NOW()`/`CURRENT_TIMESTAMP`로 채워지는 모든 타임스탬프 컬럼에 시간대
보정이 전혀 없었던 게 원인. 이 때문에 요리 캘린더의 날짜별 완료 기록
묶기(`DATE(created_at)`)도 자정~오전9시(KST) 완료 건이 하루 전 날짜로
잘못 묶이는 실제 버그가 있었다.

임시로 그 캘린더 쿼리 하나에만 `DATE_ADD(..., INTERVAL 9 HOUR)`를 붙였다가,
다른 곳에도 같은 문제가 있을 수 있어 근본적으로 고치는 쪽으로 변경:
- `backend/app.py`의 `get_db()`에 `init_command="SET time_zone = '+09:00'"`
  추가 — DB 커넥션 자체를 KST로 고정해서, 이 앱이 쓰는 모든 커넥션에서
  `NOW()`/`CURRENT_TIMESTAMP`가 항상 정확한 한국 시간을 반환하도록 함.
  캘린더 쿼리의 임시 보정 코드는 필요 없어져서 원복
- 이미 UTC로 쌓여있던 과거 데이터 11개 테이블·컬럼(`users.created_at`/
  `updated_at`, `households.created_at`, `household_share_requests.
  created_at`/`responded_at`, `user_completed_recipes.created_at`,
  `user_favorite_recipes.created_at`, `user_ingredients.created_at`/
  `updated_at`, `user_recorded_recipes.created_at`, `coupang_clicks.
  created_at`, `youtube_channel_cache.created_at`, `youtube_channel_meta.
  created_at`)을 일회성으로 `DATE_ADD(컬럼, INTERVAL 9 HOUR)`로 보정해
  새로 쌓이는 데이터와 같은 기준(KST)으로 맞춤 (사용자 승인 후 실행)
- 참고: `recipes.collected_at`은 크롤러가 한국에 있는 로컬 PC에서
  Python으로 직접 시각을 찍어 넣는 값이라 이 문제와 무관 — 그대로 둠

#### 검증 (실측)
- `SELECT NOW()`를 새 커넥션 설정으로 재조회 → 세션 타임존 `+09:00`,
  실제 한국 시각과 일치하는 값 반환 확인
- 마이그레이션 전/후 각 테이블의 MIN/MAX/COUNT를 기록해 행 손실 없이
  정확히 9시간씩만 이동했는지 대조 확인 (예: `users.created_at` 최솟값
  `2025-12-27 03:57:48` → `2025-12-27 12:57:48`)
- `python -m py_compile app.py` 에러 0건

### AI 챗봇: 후속 질문("감자는 빼줘") 무시 버그 수정
사용자가 "너가 준 재료 중에 감자는 빼줘" 처럼 직전 답변을 전제로 한
후속 요청을 해도, 답변 문구는 "감자를 빼고 골랐다"고 하면서 실제로는
직전과 똑같은(감자 포함) 레시피를 그대로 반환하는 문제.

원인(직접 백엔드를 띄워 실제 대화로 재현해서 확인): LLM은 제외 요청
자체는 정확히 `exclude_ingredients: ["감자"]`로 정확히 파싱했지만,
같은 프롬프트 지시 때문에 나머지 냉장고 재료를 전부 `include_ingredients`에
채워 넣어 "네 재료가 전부 동시에 들어간 레시피"라는 지나치게 좁은 조건이
되어 검색 결과가 0건이 됐고, 결과가 0건일 때 조건을 완화하는 로직이
`exclude_ingredients`까지 포함해 모든 조건을 날려버리면서 원래(제외 반영
안 된) 냉장고 검색으로 조용히 되돌아가고 있었다.
- `chat_service.py`의 완화(fallback) 체인을 `keywords` → `include_ingredients`
  → 냉장고 우선순위 순으로 한 단계씩만 완화하도록 고치고, `exclude_ingredients`는
  정말 마지막 단계가 아니면 항상 유지하도록 수정
- 프롬프트에 "사용자가 명시하지 않은 재료를 include_ingredients에 채워
  넣지 마라"는 지시 추가
- 세션별로 직전 턴에 실제로 검색한 조건과 실제로 보여준 레시피(제목+재료)를
  서버가 기억해뒀다가 다음 프롬프트에 사실 그대로 다시 넣어줌 — 답변
  문장에 뭐가 있었는지가 아니라, 실제 검색 결과를 근거로 후속 요청을
  해석하게 함

#### 검증 (실측)
- 실제 백엔드(포트 5000, 실제 Gemini 키·DB)로 2턴 대화 재현: "감자는
  빼줘" 후속 요청 → 8개 결과 전부 감자 없음, 답변 문구도 일치하는 것 확인
- 같은 세션에서 "라면 끓이는 법" 같은 무관한 새 주제로 전환 → 이전
  조건에 얽매이지 않고 새로 검색되는 것 확인(과도한 완화로 이어지지 않음)

### AI 챗봇: 답변에서 재료 언급 시 조미료보다 일반 재료, 유통기한 임박 재료 우선
챗봇이 답변 문구에서 구체적인 재료 이름을 몇 개 골라 언급할 때, 소금·후추
같은 조미료가 섞여 나오거나 유통기한과 무관하게 아무 재료나 골라 언급하는
문제. 기존 매칭률 계산에 이미 있던 조미료/일반재료 가중치 분류
(`_load_seasoning_set()`)를 그대로 재사용해서, 답변 문구가 재료를
언급할 때만 다음 우선순위를 따르도록 프롬프트에 추가:
1. 조미료보다 일반 식재료를 우선 언급
2. 그중에서도 유통기한이 임박한 재료를 우선 언급
3. 단, 사용자가 "다이어트식으로 추천해줘" 처럼 구체적인 목표를 명시하면
   그 목표가 유통기한보다 우선
4. 유통기한 임박이 실제로 답변에 영향을 준 경우에만 그 사실을 답변에
   명시("유통기한이 임박한 OO를 먼저 고려해서 답변드렸어요") — 그래야
   사용자가 "유통기한 신경쓰지 말고 답해줘"처럼 다음 질문으로 되돌릴 수 있음

프론트에서 유통기한 데이터 자체가 애초에 백엔드로 전달되지 않고 있어서
(`RecipeChatWidget.tsx`가 재료 이름만 뽑아 보내고 있었음), `ingredient_expiry`
필드를 새로 추가해 함께 전송하도록 수정. `keywords`/`include_ingredients`/
`exclude_ingredients` 같은 실제 검색 조건에는 영향 없고, 오직 답변 문구의
재료 언급 방식에만 적용됨.

#### 검증 (실측)
- 조미료+유통기한이 제각각인 냉장고(삼겹살 D-1, 닭가슴살 D-6, 두부 D-10,
  감자 D-15, 양파 D-12, 대파 D-8, 소금/후추)로 실제 대화 재현
- 목표 없는 질문 → 삼겹살(가장 임박)/닭가슴살/두부 언급 + "유통기한이
  임박한 삼겹살을 먼저 고려해서 답변드렸어요" 문구 확인
- "다이어트식으로 추천해줘" → 임박도는 낮지만 다이어트에 맞는 닭가슴살을
  언급, 유통기한 문구는 붙지 않는 것 확인(유통기한이 결정 요인이 아니었으므로)
- 위 "후속 질문 무시 버그" 수정 재검증 — 회귀 없음 확인
- `python -m py_compile`, `npx tsc --noEmit` 에러 0건

### AI 챗봇: 지식성 질문에도 무관한 레시피 카드가 붙던 문제 수정
다이어트 대화 중 "너가 추천하는 다이어트용 양념은 뭐야?" 같은 질문을 하면,
`reply`는 "식초, 알룰로스"처럼 구체적으로 답하는데 그 아래 뜨는 레시피 카드는
그 답변과 전혀 무관한 것들이 뜨는 문제. 원인: 이런 지식/조언성 질문도 매번
`keywords`/`include_ingredients`를 뽑아 레시피 검색을 강행했는데, LLM이 `reply`
문장과 검색어를 서로 독립적으로 지어내서 둘이 어긋났다.

LLM 출력에 `recipe_search`(boolean, 기본 true) 필드를 추가: 사용자 메시지가
레시피를 찾아달라는 게 아니라 재료·양념·손질법·대체재에 대한 지식성 질문이면
false로 내려주고, 이때는 `_search_recipes` 자체를 건너뛰어 레시피 카드를 아예
안 붙인다. `recipe_search`가 true인 턴은 `reply`가 언급한 구체적 이름이
`keywords`/`include_ingredients` 중 하나와 실제로 겹쳐야 한다는 규칙을 프롬프트에
추가해 이중으로 막았다. `recipe_search=false` 턴은 세션의 "직전 검색 결과"를
갱신하지 않아, 지식성 질문이 중간에 끼어도 그 다음 "감자는 빼줘" 같은 팔로우업이
여전히 그 이전 진짜 검색 결과를 가리킬 수 있게 했다.

#### 검증 (실측)
- 다이어트 양념 질문 재현 → 수정 후 `recipe_search: false`, `keywords: []`,
  카드 없이 직접 답변만 오는 것 확인
- 일반화 확인: "계란 대신 뭐 쓸 수 있어?"(대체재), "대파 손질법"(손질법) 모두
  `recipe_search: false`로 직접 답변, "매콤한 두부 요리 추천해줘"는 그대로
  `recipe_search: true`에 답변과 일치하는 키워드로 검색되는 것 확인
- "감자는 빼줘" 후속 요청 재검증(8개 전부 감자 없음), 유통기한 우선순위
  답변 2가지 케이스 재검증 — 전부 회귀 없음
- `python -m py_compile` 에러 0건
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### AI 챗봇: 답변을 한 글자씩 타이핑되듯 표시
`RecipeChatWidget.tsx`: LLM 응답이 한 번에 말풍선으로 뜨던 것을, 실제로는
비스트리밍 API 응답을 받아둔 채 클라이언트에서 일정 간격(24ms마다 2자)으로
글자 수를 늘려가며 보여주는 방식으로 흉내냈다. 타이핑이 끝나기 전에는 레시피
카드도 숨겨서, 문장이 다 나오고 나서 카드가 뜨도록 했다.

### 요리 캘린더: 로그인 안 했을 때 하단 탭이 사라지던 버그 수정
`CookingCalendar.tsx`의 로그인 유도 화면이 하단 전역 탭바(`BottomNavBar`)
없이 렌더링되어, 로그인 안 한 사용자가 요리 캘린더 탭에 들어오면 다른 탭으로
이동할 방법이 없었다. 같은 화면에서 달력 아이콘 이미지도 제거.

### 로그인/회원가입 화면에 뒤로가기 버튼 추가
두 화면 모두 뒤로가기 수단이 없었다. 이 앱의 기존 관례(`paddingTop:72` +
상대 위치 배치)는 세로 중앙 정렬 레이아웃에서는 화면 높이에 따라 버튼이
헤더에 가려지는 문제가 있어서(짧은 페이지일수록 더 심함), 대신 전역 헤더
바로 아래 고정 위치(`position:fixed, top:64, zIndex:201`)로 배치했다.

### 로그인 화면의 잘못된 가족 공유 안내 문구 삭제
"한 계정으로 같이 로그인하시면 돼요"라는 문구가 실제 기능(각자 계정으로
가입 후 식구 그룹 초대로 연결)과 달라 삭제.

### 쿠팡 광고 카드: 세로 목록(냉장고요리)에는 아예 안 붙어있던 버그 수정
부족 재료 1~3개인 레시피 카드 뒤에 쿠팡 광고 카드를 끼우는 로직
(`getLackingIngredients`/`pickAdIngredient`/`CoupangAdCard`)이 가로 캐러셀
컴포넌트(`VirtualizedHorizontalRecipeList`, 요즘인기 페이지)에만 구현돼 있었고,
냉장고요리 페이지가 쓰는 세로 목록 컴포넌트(`VirtualizedRecipeList`)에는
연결된 적이 없었다. 같은 규칙(3번째 카드부터, 광고 사이 최소 4개 간격)으로
`VirtualizedRecipeList.tsx`에도 동일하게 구현.

마이페이지(즐겨찾기/기록/완료)는 "사용자가 직접 모아둔 목록 사이에 광고를
끼우면 성격이 흐려진다"는 판단으로 예전에 `showAds={false}`로 꺼뒀었는데,
실제 트래픽이 몰리는 페이지라 판단해 다시 켜기로 함 — 세 섹션 모두
`showAds={false}` 제거(기본값 true).

#### 검증 (실측)
- 로컬(`localhost:5178`)에서 요즘인기 → 유튜브 인기 레시피 캐러셀을
  4번째 카드까지 스크롤 → "오징어" 쿠팡 광고 카드 정상 노출 확인
- 냉장고요리 페이지에서 "부족해도 되는 재료: 3개까지" 필터 + 최신순 정렬로
  1~3개 부족 카드들을 확인 → 3번째 카드(2개 부족) 뒤에 "스팸" 광고,
  이후 4개 간격으로 "버터"/"면"/"가지"/"액젓"/"원두" 광고가 규칙대로
  삽입되는 것을 페이지 텍스트로 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건
