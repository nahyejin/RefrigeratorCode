# CHANGELOG

쿡매치(CookMatch) 프로젝트 변경 이력. Cursor AI에서 Claude Code로 작업 환경을 이관한 2026-08-24부터 기록.

## 2026-08-24

### 프로젝트 구조 정리
- 옛 CRA 프론트엔드 잔재 삭제 (루트 `src/`, `package.json`, `vercel.json`, `main.py`) — 실제 배포는 `frontend/`에서 진행되므로 영향 없음
- `backend/App.tsx`, 루트 `check_db.py`, 중복 `export_recipes_all.ipynb` 등 죽은 코드/중복 파일 삭제
- 로컬 SQLite 흔적(`posts.db`, `recipes.db`, `refrigerator.db`) 삭제 — 현재는 MySQL(Railway) 사용
- `crawler.log`, `chromedriver-win64/`, `__pycache__/` 전체를 git 추적 해제 (로컬엔 유지, `.gitignore` 반영)
- 루트에 흩어진 디버그/점검용 스크립트 18개를 `scripts/`로 이동
- `utils/requirements.txt.txt`(오타성 파일명)를 `ingredient_management/requirements.txt`로 이동

### 요리 챗봇 기능 추가
- `backend/chat_service.py`: Gemini/Groq LLM으로 대화 의도를 파악해 DB에서 레시피를 검색하는 `/api/chat` 엔드포인트 신설. 일일 호출 한도(기본 250회) 적용
- 프론트에 전역 플로팅 챗 위젯(`RecipeChatWidget.tsx`) 추가 — 냉장고 재료(localStorage) + 대화 기록을 세션 단위로 유지하며 추천 레시피 카드 표시
- Gemini 기본 모델을 `gemini-2.0-flash`(단종됨) → `gemini-3.5-flash-lite`로 교체
- 레시피 썸네일에 기존 `getProxiedImageUrl` 유틸 적용 — 네이버 이미지 핫링크 차단(403) 우회
- 로컬 개발 CORS 이슈 해결 (프론트 dev 서버 포트를 화이트리스트에 있는 5178로 통일)

### 챗봇 FAB를 AI 기능처럼 리디자인
- 텍스트만 있던 "챗" 버튼을 스파클 아이콘 + 회전하는 그라디언트 링 + 펄스 글로우 + "AI" 배지로 교체
- 채팅창 헤더와 어시스턴트 말풍선에도 같은 스파클 아바타 적용해 AI 브랜딩 통일

### 챗봇 대화 히스토리 관리 + 재료 매칭 우선 검색으로 재설계
- `RecipeChatWidget.tsx`: 채팅창을 닫았다 다시 열면 항상 새 대화로 시작하도록 변경. 지난 대화는 localStorage에 스레드 단위로 저장(최근 30일 보관)되고, 헤더의 "지난 대화" 버튼으로 목록을 열어 이어보거나 삭제 가능
- `chat_service.py`: 기존엔 LLM이 뽑은 키워드가 title/content LIKE **강제 필터**라서, 냉장고 재료와 매칭률이 높은 레시피도 키워드가 안 맞으면 통째로 걸러졌음. 이제 냉장고 재료가 있으면 키워드는 `HAVING` 절의 소프트 조건(키워드 매칭 OR 매칭률 25% 이상)으로 바뀌어, 매칭률 높은 레시피가 우선 노출됨
- LLM 프롬프트에 `ignore_fridge` 필드 추가: 사용자가 "재료 상관없이" 등으로 명시적으로 요청할 때만 냉장고 매칭을 끄고 순수 키워드 검색으로 전환
- 레시피 카드의 "재료 매칭률 N%"를 일반 텍스트에서, 기존 `RecipeCard.tsx`와 동일한 톤(짙은 배지 + 노란색 굵은 글씨)의 배지로 변경

### 재료 추출 파이프라인을 LLM 기반으로 재설계 (진행 중)
- `ingredient_management/llm_ingredient_extraction.py` 신설: Gemini로 본문에서 실제 사용 재료를 "이해"해서 뽑아내고,
  정규화는 100% 기존 사전(`ingredient_profile_dict_with_substitutes.csv`)의 결정론적 매칭으로만 처리
  (LLM 역할을 추출까지만 최소화, 매칭은 LLM 관여 없음). 출력 포맷은 기존과 동일(콤마구분/공백없음/정렬)하게 유지해
  프론트 pill 매칭·백엔드 매칭률 SQL을 전혀 건드리지 않음
- 사전에 없는 LLM 추출 후보는 버리되 `unmapped_ingredients` 컬럼에 기록 (추후 사전 보강용)
- 20건 샘플 검증 결과, 기존 룰베이스의 실제 버그들(예: "청양고추"를 "고추"+"청양고추"로 중복 집계, "만두"/"쌈무" 같은 요리명이 재료로 오인식, 블록 탐색 범위가 좁아 뒷부분 재료 누락)이 다수 수정됨을 확인
- 전체 44,121건에 대해 `--commit` 모드로 백그라운드 배치 실행 중 (모델: gemini-3.5-flash-lite, 분당 140건, 동시 7개)
- **버그 수정**: 최초 버전은 전체 결과를 다 모은 뒤에야 CSV/DB에 쓰는 구조라 중간에 죽으면 진행 상황이 전부 유실되는
  문제가 있었음 → 결과가 나오는 즉시 CSV에 flush, 30건마다 DB 커밋하도록 수정 후 재시작
- **실측 결과**: 이 API 키는 무료 티어라 `gemini-3.5-flash-lite`가 분당 15건으로 하드 제한됨. 분당 140건으로
  돌렸다가 에러율 27%를 확인하고 분당 13건(안전 마진)으로 재시작. 44,121건 전체 기준 약 2~2.5일 소요 예상

### git 추적 충돌 발견 및 정리
- 세션 도중 내가 만들지 않은 커밋 2개(`대거변동 260825`, `업데이트 20260825_12:37`)가 발견됨 — 다른 도구(Cursor 등
  자동 커밋으로 추정)가 같은 저장소에 동시에 작업 중인 것으로 보임. 이 커밋들이 `.gitignore`를 이전 버전으로
  되돌리고 pycache/chromedriver/crawler.log를 다시 추적시킴 → 재정리하고 배치 산출물 CSV도 gitignore에 추가
- **주의**: 다른 자동 커밋 도구가 계속 켜져 있으면 다시 충돌할 수 있음. Cursor 등 확인 필요

### 챗봇 UI 세부 디자인 개선
- FAB/헤더/말풍선의 스파클 아이콘 → 김이 나는 냄비 아이콘으로 교체 (SVG가 flex 컨테이너 안에서
  width 0으로 찌그러지던 렌더링 버그도 같이 수정)
- 지난 대화·새 대화 버튼을 아이콘 버튼(38px)으로 확대, 새 대화는 + 아이콘으로 통일
- 안내 문구를 기능 설명 → 사용자 편익 중심으로 변경, placeholder도 "맛"에서 "먹고 싶은 걸"로 범위 확장
- 채팅창 타이틀 강조(굵게/크게), 창 높이 70vh/560px → 84vh/700px로 확대
- GitHub main에 푸시 완료 (Vercel 자동 배포 트리거됨)

### 배포 환경변수 설정
- Railway 백엔드에 `GEMINI_API_KEY` 환경변수 추가 완료 (운영 환경 챗봇 활성화)

### 크롤러 자동화 안정화 (Task Scheduler 등록 준비)
- `run_all_crawlers.py`가 계속 실패하던 원인 파악: ChromeDriver 버전 자동 감지가
  PowerShell/wmic 서브프로세스에 의존하는데, 실패해도 감지된 버전을 실제로 쓰지
  않는 버그가 있어 최신 Chrome보다 오래된/새로운 ChromeDriver를 받아와 세션 생성이
  계속 실패했음. 레지스트리 기반 감지로 교체하고 감지된 버전을 실제로 전달하도록 수정
- 크롤러 파이프라인에 LLM 일일 증분 추출 단계 연결 (룰베이스 폴백 유지 + 무료 한도 내 LLM 보정)
- 의존성 누락(dnspython, tqdm), 미사용 import(mysql.connector) 정리
- 매일 오전 7시 자동 실행할 `run_crawlers_scheduled.bat` 준비 (Windows 작업 스케줄러 등록 예정)

### LLM 배치 처리 + 하루 500건 한도 대응
- `llm_ingredient_extraction.py`에 `--batch-size` 옵션 추가: 레시피 여러 건을 호출
  1번에 묶어 처리 (기본 8건) → 일일 호출 횟수 한도를 훨씬 적게 사용, 챗봇과 공존 가능
- `--pending-only` 옵션 추가: DB의 `llm_ingredients_done` 플래그로 "이미 처리됨"과
  "신규/미처리"를 구분해, 크롤러가 매일 신규 수집분만 무료 한도 내에서 처리하고
  오늘 못 채운 건 자동으로 다음날로 넘어가도록 함
- 44,121건 기존 백로그는 유료 티어로 별도 처리 예정 (실측 토큰 기반 예상 비용 약 9천~1만 8천원)

### UI 디자인 정리
- 실제 화면 4곳(내 냉장고/냉장고 요리/요즘 인기/마이페이지)을 스크린샷으로 검토해
  아쉬운 지점 정리 후 우선순위대로 수정:
  - AI FAB와 PWA 설치 유도 배너가 z-index 문제로 겹쳐 보이던 것 수정
  - 내 냉장고 재료 태그가 진회색+흰 글씨로 뭉쳐 보이던 것을 연회색+진회색으로 가볍게 변경
  - "부족 재료" pill이 연회색 배경에 흰 글씨라 실질적으로 안 보이던 대비 문제 수정
    (앱 전역 10곳의 범례도 함께 정리)
  - 냉장고요리 페이지 필터 버튼 4개가 전부 같은 톤이라 위계가 없던 것을 아이콘+진한
    "필터" 버튼으로 구분
  - 스플래시 화면을 베이지/일러스트 톤에서 실제 CookMatch 아이콘 기반 화이트/블랙/
    옐로우 톤으로 재구성 (본편과 첫인상 통일)

### 무료 티어 실측: 하루 500건이 진짜 병목
- 분당 13건(안전 마진)으로도 대량 에러 발생 → 원인은 분당 제한이 아니라
  `GenerateRequestsPerDayPerProjectPerModel-FreeTier` **하루 500건** 한도였음
- 44,121건 ÷ 500건/일 ≈ 88일. 게다가 운영 챗봇도 같은 키의 같은 일일 한도를 공유해서
  무료 티어로는 배치와 실사용자 챗봇을 동시에 운영할 수 없음 → 유료 티어 전환 필요, 사용자 결정 대기 중
- 오늘 처리분: 948건 시도, 성공 160건(DB 반영), 나머지는 일일 한도 초과로 실패

### 데이터 안전장치 추가 (중요)
- **버그 발견**: LLM이 빈 배열을 반환하면(파싱 실패/모델 이상 응답 등) 기존에 있던 재료 목록을
  조용히 빈 값으로 덮어써버리는 문제가 있었음. 160건 중 2건에서 실제 발생(id=1326 '초계라면'은
  진짜 레시피인데 빈 값으로 덮어써짐 → 기존 값으로 복구 완료. id=1295는 레시피가 아닌 홍보 영상이라
  원래도 룰베이스가 오탐한 것이었고 빈 값이 맞아서 그대로 둠)
- **수정**: 기존 `used_ingredients`가 있는데 LLM 결과가 비어 있으면 DB에 반영하지 않고 CSV에만
  `NEEDS_REVIEW`로 표시 (DB에는 절대 안 들어가므로 사용자에게 노출되는 값은 항상 기존 값 그대로 유지됨)

### 챗봇 FAB 로고/타이틀 변경
- FAB/헤더/말풍선 아이콘을 냄비 모티프에서 쿡매치 메인 아이콘(집 지붕 + 밥그릇) 모티프로 교체
- 타이틀 "AI 요리 챗" → "쿡매치 AI" (챗봇 느낌을 줄이고 브랜드를 앞세움)

### 레시피 아닌 본문 자동 삭제 로직 추가
- 룰베이스(old)와 LLM(new)이 둘 다 "재료 없음"에 동의하는 행만 레시피가 아닌 것으로 보고
  `recipes` 테이블에서 행 자체를 삭제 (예: 홍보 영상, 안내 영상 등)
- old에는 재료가 있었는데 new만 비어 있는 경우(불일치)는 삭제하지 않음 — LLM 쪽의 일시적
  오류일 수 있어 위 데이터 안전장치와 동일하게 NEEDS_REVIEW로만 표시하고 기존 값 보존
- 수동 확인한 사례 2건 처리: id=1326(진짜 레시피, 오류로 비워졌던 것)은 기존 값 복구,
  id=1295(레시피 아닌 통영 축제 홍보 영상)는 행 자체 삭제
- `ingredient_management/llm_extracted_158_review.csv`: 성공 처리된 158건의 링크 +
  LLM 추출 재료(원본/정규화 후) 검토용 파일 생성

## 2026-08-25

### 스티키 필터 영역 (냉장고 요리 페이지)
- `RecipeList.tsx`: 검색창 + 정렬/필터 바 + 재료 pill 범례("부족 재료 ... 총 000건")를
  스크롤해도 상단에 고정되도록 sticky 처리 (top: 56px, GNB 바로 아래)
- 범례가 화면 밖으로 사라져서 pill 색상의 의미를 잊게 되는 문제 해결

### 챗 패널 모바일 키보드 대응
- `RecipeChatWidget.tsx`: `visualViewport` API로 키보드 높이를 감지해 패널 위치/높이를 재계산.
  키보드가 올라와도 패널 상단이 잘리지 않음
- 메시지 목록/지난 대화 목록에 `minHeight: 0` 추가 — flex 자식이라 스크롤이 안 걸리던 문제 해결
- 패널 타이틀 글꼴/굵기를 앱 공통 톤(18px / 700)에 맞춤

### 내 정보 수정 진입 경로 + 닉네임 마스킹 문제
- `TopNavBar.tsx`: GNB의 닉네임을 버튼으로 바꿔 클릭 시 `/my-page?openEdit=true`로 이동
- `MyPage.tsx`: `openEdit=true` 쿼리 파라미터를 감지해 "내 정보 수정" 모달 자동 오픈
- 버튼 문구 "적용" → "변경 적용"
- **닉네임이 `9208****`로 보이던 원인**: 프론트/백엔드 어디에도 마스킹 코드는 없었고,
  네이버 로그인 API(`/v1/nid/me`)가 별명(nickname)을 마스킹된 형태로 내려주는데 그 값을
  그대로 저장한 것이었음 (DB의 users.nickname 실제 값 자체가 `9208****`)
- `app.py` 네이버 콜백 수정: 닉네임이 비어 있거나 `*`를 포함하면 이메일 로컬 파트를 대신 사용
  → 이후 신규 네이버 가입자는 마스킹된 닉네임이 저장되지 않음
- 기존에 저장된 값은 "내 정보 수정"에서 직접 변경 필요

### Sticky 영역 여백 + 다른 목록 페이지에도 동일 적용
- `RecipeList.tsx`: sticky 영역에 `paddingBottom: 10` 추가. 기존엔 범례의 `marginBottom`이
  margin collapsing으로 sticky 컨테이너 밖으로 빠져나가서, 스크롤 시 카드가 "부족 재료 / 대체 가능 /
  보유 재료" 글자에 바짝 붙어 지나갔음. 이제 범례 아래 흰 여백이 항상 유지됨
- 같은 형식의 범례를 쓰는 나머지 목록 페이지에도 동일하게 sticky 적용
  (정렬/필터 바 + 범례가 함께 상단 고정, top: 56px)
  - `IngredientDetail.tsx` — 요즘인기 페이지 하단 검색 → `/ingredient/:name`으로 이동하는 페이지
  - `RecordedRecipeListPage.tsx` — 내가 기록한 레시피
  - `CompletedRecipeListPage.tsx` — 내가 완료한 레시피
- `Popular.tsx`와 `MyPage.tsx`의 범례는 제외 — 이쪽은 섹션(유튜브 인기 / 네이버 인기 / 특별한 날 /
  즐겨찾기 / 기록 / 완료)마다 각각 붙어 있는 가로 스크롤 캐러셀 헤더라, sticky로 만들면 범례 여러 개가
  서로 밀어내며 겹침. 세로로 길게 스크롤되는 목록 페이지에만 적용하는 것이 맞음

### 챗봇 FAB를 노란 말풍선 버튼으로 리디자인
- 검정 원 + 쿡매치 마크(집 지붕+밥그릇) + 회전 그라디언트 링 조합이 무겁고 안 예쁘다는 피드백 반영
- 브랜드 옐로우(#FFD600) 원형 버튼 + 어두운 말풍선 아이콘(안쪽 점 3개는 노란색으로 뚫음)으로 교체
- 회전하는 conic-gradient 링 제거 (노란 버튼 위에서는 색이 뭉개짐). 은은한 노란 글로우 펄스는 유지
- "AI" 배지를 노란 배경/검정 글씨 → 검정 배경/노란 글씨로 반전 (노란 버튼 위 대비 확보)
- 누를 때 살짝 눌리는 `:active` 스케일 피드백 추가
- 아이콘 클래스를 FAB 전용(`.ai-fab-bubble`)으로 분리 — 패널 헤더/말풍선 아바타의 쿡매치 마크는
  기존 그대로(어두운 원 + 노란 마크) 유지되도록 함

### 챗 패널 아바타/헤더 정리 + FAB 애니메이션 복원
- 패널 헤더 아바타와 답변 말풍선 아바타를 FAB와 동일한 디자인(노란 원 + 어두운 말풍선)으로 통일.
  기존의 검정 원 + 쿡매치 마크(집 지붕+밥그릇) 조합은 FAB와 따로 놀아서 제거
- 사용하지 않게 된 `CookMatchMarkIcon` 컴포넌트와 관련 CSS 삭제
- 헤더의 부제 "오늘 뭐 먹을지, 냉장고 재료로 같이 찾아볼게요 🍳" 제거.
  좁은 화면(앱)에서 말줄임으로 잘리는 데다, 본문 첫 안내 문구
  "뭐 먹을지 고민될 때, 냉장고 재료로 딱 맞는 레시피를 찾아드릴게요."와 내용이 중복됨
- FAB 애니메이션 복원/강화: 회전 링을 뺀 대신
  - 노란 그라데이션이 버튼 위를 천천히 흐르는 `ai-fab-shimmer` (4.2s 무한 반복)
  - 주기적으로 스쳐 지나가는 하이라이트 `ai-fab-sheen`
  - 기존 노란 글로우 펄스 유지
  - 말풍선 아이콘은 하이라이트 위에 올려 흐려지지 않게 처리

### 챗봇 예시 질문 확대 + 입력창 UI 개선
- 첫 진입 시 보여주는 예시 질문을 3개 → 8개로 확대. 맛/냉장고 위주였던 걸
  대상·목적·상황까지 넓힘
  - 기존: 매운 거 / 있는 재료로 / 재료 상관없이 간단한 거
  - 추가: 아이가 잘 먹는 반찬 / 다이어트 중인데 뭐 먹지 / 캠핑 가서 해먹기 좋은 요리 /
    술안주 / 15분이면 되는 자취 요리
- 입력창이 흰 배경에 연회색(gray-100)이라 입력 가능한 영역인지 구분이 안 된다는 피드백 반영
  - 흰 배경 + 진한 테두리(#D8D8DE) + 라운드 필 형태로 변경해 필드 경계를 명확히 함
  - 포커스 시 브랜드 옐로우 테두리 + 글로우 링 표시
  - 입력 영역 전체 배경을 살짝 톤다운(#FAFAFB)해 대화 영역과 시각적으로 분리
  - "보내기" 버튼을 검정 → 브랜드 옐로우로 변경 (비활성 시 회색), 높이를 입력창과 40px로 맞춤

### 챗 패널 스크롤 문제 해결
- 측정해보니 스크롤 자체는 동작하고 있었음(scrollHeight 760 > clientHeight 512). 문제는 세 가지였음
- **뒤 페이지로 스크롤 전파**: 패널 안을 드래그하다 끝에 닿으면 뒤 페이지가 대신 밀려서
  "패널이 안 움직인다"고 느껴짐 → 스크롤 영역에 `overscroll-behavior: contain` 적용
- **뒤 페이지 스크롤 미잠금**: 패널이 열려 있는 동안 `document.body`를 `overflow: hidden`으로 잠그고
  닫힐 때 원복
- **답변 도착 시 맨 아래로 점프**: 긴 답변이 오면 곧장 최하단으로 스크롤해서 답변 앞부분이 안 보였고,
  사용자 입장에선 내용이 잘려 보임 → 이제 '마지막 질문'이 상단에 오도록 스크롤해서
  답변을 처음부터 읽을 수 있고, 아래에 더 있다는 것도 카드가 걸쳐 보여서 인지됨
- 스크롤바를 얇게(6px) 보이도록 스타일링해 스크롤 가능 영역임을 시각적으로 표시

## 2026-08-25 (UI 리팩터링)

### 1단계: 죽은 CSS 정리 + 촌스러움의 실제 원인 2건 제거
- **정정**: "폰트가 맑은고딕으로 떨어진다"는 최초 진단은 틀렸음. 브라우저 실측 결과
  body/h2/button/input 모두 Pretendard로 정상 렌더되고 있었음. 맑은고딕 규칙은
  `pill-label`/`tag-label`/`filter-pill`/`small-pill` 등 **사용처가 0인 클래스**만
  겨냥하고 있어 한 번도 적용된 적이 없었음
- **모든 글자의 흰색 윤곽선 제거** (진짜 원인 ①): `body`에 `text-shadow: 0 0 1px #fff`가
  4겹 걸려 있어 앱 전체 글자가 뿌옇게 번져 보였음. 이 규칙을 되돌리려고 코드 곳곳에
  `textShadow: 'none'` 인라인 우회가 22군데나 생겨 있었음 → 원인을 제거하고 우회 22곳도 함께 삭제
- **페이지 스크롤바를 보이게 변경** (진짜 원인 ②): `html/body/#root`의 스크롤바 thumb이
  `background: transparent`로 되어 있어 스크롤바가 아예 안 보였음. "스크롤이 자꾸 안 보인다"는
  체감의 직접 원인 → 은은하지만 보이는 thumb(검정 18%, hover 30%)으로 변경, Firefox용
  `scrollbar-width/color`도 추가
- **죽은 CSS 규칙 약 80줄 삭제**: `.text-[11px]`, `.bg-[#D1D1D1]`처럼 대괄호를
  이스케이프하지 않은 셀렉터가 목록에 섞이면 CSS 규칙 **전체가 무효**가 됨.
  pill 폰트 강제 블록(Noto Sans KR / font-weight 200 / 12~14px !important)이 전부 이 경우라
  적용된 적 없는 죽은 코드였음. 남겨두면 나중에 해당 클래스를 쓰는 순간 예상 못한 스타일이
  튀어나오는 지뢰라 제거

### 2단계: 디자인 토큰 도입 + 재료 pill/범례 단일화
- **수치 정정**: 앞서 "고유 색상 252종"이라고 했던 건 SVG 에셋 파일(`share_black.svg` 등)이
  섞여 부풀려진 값이었음. 실제 코드(tsx/ts/css) 기준 정확한 수치는 **92종**
- `src/index.css`의 `:root`에 디자인 토큰 정의 (ink 5단계 / line 2단계 / surface 2단계 /
  브랜드 5종 / 상태 3종 / z-index 6단계). `tailwind.config.js`에도 같은 팔레트를 반영해
  `text-ink-500`, `bg-brand-soft` 같은 클래스로도 쓸 수 있게 함
- 흩어진 색상 값 **506곳을 팔레트로 정규화** → 고유 색상 **92종 → 42종**.
  (`#222`/`#111`/`#1A1A1A` → `--ink-900`, `#E5E7EB`/`#E5E5E5` → `--line-200` 등.
  브랜드·플랫폼(네이버 초록/유튜브 빨강)·상태·캘린더 색은 의미가 있어 병합하지 않음)
- **쿠팡 광고 버튼 톤다운**: 채도 높은 주황 꽉 찬 버튼이라 카드에서 가장 눈에 띄었고,
  정작 주인공인 레시피보다 광고가 강조돼 있었음 → 흰 배경 + 옅은 주황 테두리 +
  주황 글씨의 외곽선 스타일로 변경
- **재료 pill 원색 노랑 제거**: 카드마다 보유 재료 pill이 4~6개씩 원색 노랑(#FFD600)으로
  꽉 차 있어 화면이 노란 덩어리가 되고, 브랜드 컬러가 "데이터 상태" 표시에 소모되고 있었음
  → 연한 노랑 배경(#FFF1B8) + 진한 노랑 글자(#6B5200)로 낮춤.
  부족 재료는 더 옅게(#F0F0F3) 물러나게 하고, 드물게 나오는 대체 가능만 진한 색 유지
- **pill 색과 범례를 단일 소스로 통합**: `src/styles/ingredientPill.ts` 신설.
  기존엔 범례 마크업이 6개 파일에 10곳 복붙돼 있어 pill 색을 바꿔도 범례는 안 따라오는
  구조였음 → `IngredientLegend` 컴포넌트로 추출해 10곳 모두 교체.
  이제 색을 바꾸려면 `ingredientPill.ts` 한 파일만 고치면 됨

### 3단계: z-index 체계화 + 모달 스택 맥락 탈출(Portal)
- z-index 값이 20종(1 ~ 10060)이 규칙 없이 흩어져 있었음. 새 요소를 넣을 때마다 숫자를
  키우는 식(9999 → 10001 → 10030 → 10060)으로 대응해 온 흔적 → **역할 기반 7단계**로 정리
  (`--z-sticky 100` / `--z-nav 200` / `--z-dropdown 300` / `--z-fab 400` /
  `--z-overlay 500` / `--z-modal 600` / `--z-toast 700`). 54곳 치환.
  카드 안 아이콘 같은 지역 스택(1~30)은 의미가 다르므로 그대로 둠
- **모달이 헤더/하단 네비 아래에 깔리던 버그 수정** (히트 테스트로 발견):
  필터 모달이 `position: sticky` 인 필터바 **안에서** 렌더되고 있었음. sticky는 새 스택 맥락을
  만들기 때문에, 모달의 z-index를 아무리 올려도 그 조상의 층위(100) 안에서만 높을 뿐
  바깥의 헤더·네비(200)보다 위로 갈 수 없었음
  → `Portal` 컴포넌트 신설(`createPortal`로 `document.body` 직속 렌더)하고
  FilterModal / IngredientDetailModal / RecipeSortBar의 매칭도·임박재료 모달에 적용.
  수정 후 히트 테스트로 헤더·네비·FAB 위치 모두 모달 딤이 덮는 것 확인

### 4단계: 남은 중복 제거
- **런타임 `<style>` 주입 제거**: 로딩 점 애니메이션 CSS가 5개 페이지 파일에 각각 복붙되어 있고,
  모듈 최상위에서 `document.createElement('style')` 로 `<head>` 에 주입하고 있었음
  (페이지를 옮겨 다니면 동일한 `<style>` 태그가 계속 쌓임) → `index.css` 로 옮기고 주입 코드 삭제.
  점 색상도 `var(--brand)` 로, z-index 도 `var(--z-overlay)` 로 토큰화
- **Toast / RecipeToast 통합**: 두 컴포넌트가 거의 같은 스타일 정의를 각자 들고 있었음
  → `RecipeToast` 가 `Toast` 에 위임하도록 변경(여러 줄 표시 옵션만 다름)
- `Toast` 를 `Portal` 로 감쌈 — sticky/transform 조상 안에서 호출돼도 층위에 갇히지 않도록
- 페이지에 인라인으로 복붙돼 있던 토스트 마크업 중 형태가 동일한 2곳을 `<Toast />` 로 교체

### 5단계: 화면 정리
- **`WelcomeModal` 에 딤 배경 추가**: 흰 카드만 화면 가운데 떠 있어서 모달인지 페이지 일부인지
  구분이 안 되는 상태였음(딤 배경이 처음부터 없었음) → 반투명 배경 추가 + 배경 클릭 시 닫힘 +
  `Portal` 적용
- `RegisterPromptModal` 도 `Portal` 적용 — 어디서 호출되든 층위 문제가 생기지 않도록

## 2026-08-25 (UX 재설계)

### A단계: 타이포 스케일 상향 + 터치 영역 확대
- **실측 진단**: 3개 주요 화면의 텍스트 642개를 측정한 결과
  - 전체의 **65%가 13px 미만**, 가장 많이 쓰인 크기가 11px(248곳), 7px도 20곳 존재
  - 터치 대상은 내냉장고 143개 중 **134개가 44px 미만**(Apple HIG / Material 권장 최소 미달)
  - "글씨가 작다 / 조작 영역이 좁다 / 옛날 앱 같다"는 체감의 직접 원인이었음
- 타이포 스케일 7단계 정의(`--text-caption 12` ~ `--text-2xl 26`, 본문 15px 기준)하고
  `tailwind.config.js` 의 `fontSize` 도 한 단계씩 상향(xs 12→13, sm 14→15, base 16)
- 인라인 `fontSize` 와 Tailwind `text-[Npx]` **214곳을 스케일에 맞춰 상향**
  - 결과: 13px 미만 텍스트 **65% → 3%**, 7px/10px 등 판독 곤란한 크기 소멸
  - 크기 종류도 11종 → 8종(12/13/15/16/18/22/26)으로 정리됨
- 재료 pill 을 11px/높이 22 → 13px/높이 26 으로, 정렬·필터 바 컨트롤을 높이 28 → 40 으로 확대
- 작은 아이콘 버튼용 `.tap-44` 유틸 추가 — 보이는 크기는 그대로 두고 투명한 히트 영역만
  44px로 넓혀 레이아웃 변화 없이 터치 정확도를 올리는 방식

### B단계: 공통 컴포넌트(프리미티브) 추출
- 버튼이 페이지마다 인라인으로 각각 작성돼 있었음(MyPage 18개, RecipeSortBar 10개,
  RecipeChatWidget 9개 …). 높이·색·radius가 제각각이고 28~30px 짜리가 많아 터치가 어려웠음
- `src/components/ui/` 신설 — C단계(화면 재설계)가 이 위에서 이뤄진다
  - `Button` : primary / secondary / outline / ghost / danger × sm / md / lg.
    **md·lg 는 항상 44px 이상**이라 터치 규격이 자동으로 지켜짐. 누를 때 축소 피드백 포함
  - `Chip` : 상태 없는 태그·필터 칩 (선택 상태 지원).
    재료 pill 은 보유/부족/대체 상태가 있어 `styles/ingredientPill.ts` 를 계속 사용
  - `Input` : 높이 44px, 테두리 + 브랜드 컬러 포커스 링.
    글자를 16px 로 고정 — iOS 는 16px 미만 입력창에 포커스하면 화면을 자동 확대해버림
  - `Sheet` : 바텀시트. 화면 가운데 모달은 한 손 조작 시 손이 닿기 어렵고 내용이 길면 잘림 →
    아래에서 올라와 엄지 근처에 액션이 오도록. Portal + 뒤 스크롤 잠금 + 손잡이 포함
- 기존 컴포넌트를 새 프리미티브에 위임해 실제로 쓰이게 연결
  - `PrimaryButton` → `ui/Button`
  - `TagPill` → `ui/Chip` (예전엔 폰트를 'Noto Sans KR' 로 따로 지정해 앱 전체 톤과 달랐음)
- `prefers-reduced-motion` 대응 추가 — 움직임 최소화 설정을 켠 사용자에겐 애니메이션을 줄임

### C단계(1차): 재료 pill을 화면의 주인공으로
- **방향 정정**: 처음엔 카드 정보가 많으니 보유 재료 pill을 빼자고 제안했으나,
  이 앱의 핵심 가치가 "보유 재료로 효율적으로 냉털이 + 부족하면 대체제 추천" 이라는 점을
  확인. pill 은 장식이 아니라 **답 그 자체**이므로 빼는 게 아니라 더 잘 읽히게 하는 방향으로 전환
- **pill 가로 스크롤 → 줄바꿈**: 기존엔 `flexWrap: nowrap` + `overflowX: auto` 라
  카드마다 재료가 잘려 있었고, 몇 개가 부족한지 보려면 카드마다 옆으로 밀어야 했음.
  핵심 정보가 스크롤 뒤에 숨어 있던 셈 → 줄바꿈으로 한눈에 보이게 하고,
  8개를 넘으면 점선 `+N` 칩으로 접음(탭하면 펼침).
  `pills` 배열이 이미 [부족 → 대체가능 → 보유] 순이라 접혀도 부족한 재료가 먼저 보임
- **매칭률 뱃지**: 한때 "1개만 더" 를 앞세우고 매칭률을 보조로 내렸다가 되돌림.
  재료 매칭률은 정렬 기준(재료매칭률순)이자 필터 기준(매칭도 30~100%)인 이 서비스의
  핵심 지표라 앞세우는 것이 맞음 → **"재료 매칭률 83% · 1개 부족"** 형태로,
  매칭률을 16px 굵게 강조하고 부족 개수를 보조로 덧붙임.
  부족 개수는 이미 계산돼 있던 `lackingIngredients`(대체 가능한 재료 제외) 를 사용
- 대체 가능 줄을 어두운 라벨 + 나열에서, 옅은 배경 칩 형태로 정리

### C단계(2차): 부족 재료를 눈에 띄게
- 부족 재료 pill이 가장 옅은 회색 채움이라 배경으로 물러나 있었음. 냉털이 판단에서
  실제로 행동을 부르는 건 부족한 쪽이므로 눈에 띄어야 한다는 판단
- 부족 재료를 **흰 배경 + 점선 테두리**로 변경 — "채워야 할 빈 칸"으로 읽히게.
  새 색을 추가하지 않고 테두리 형태만으로 구분해 팔레트가 늘어나지 않음
  - 보유 = 연노랑 채움(채워짐) / 부족 = 점선 빈 칸(채워야 함) / 대체 가능 = 진한 채움(드묾)
- 글자색도 #8A8A93 → #5A5A63 으로 올려 가독성 확보
- `PILL_COLORS` 에 `borderStyle` 을 추가하고 pill·범례가 모두 이를 따르게 함 →
  범례 견본도 자동으로 점선으로 바뀜 (단일 소스의 이점)

### C단계(3차): 필터 바텀시트 전환 + 카드 액션/광고 재배치
- **필터를 바텀시트로**: 화면 가운데 뜨는 모달이라 한 손 조작 시 상단 항목에 엄지가 닿지 않고
  내용이 길면 위아래가 잘렸음 → B단계에서 만든 `Sheet` 로 교체.
  적용 버튼이 엄지 근처로 내려오고, 손잡이·뒤 스크롤 잠금·슬라이드 업 애니메이션 포함
- **카드 액션 아이콘 재배치**: 즐겨찾기/완료/공유/기록 4개가 썸네일 위에 상시 노출돼
  음식 사진(레시피 선택의 주된 판단 근거)을 가리고 있었음. 4개 중 3개(완료·기록·공유)는
  요리한 *뒤* 에 쓰는 동작인데도 목록을 훑는 내내 떠 있던 것
  → 썸네일에는 **즐겨찾기 1개만** 남기고, 나머지는 좋아요/댓글 줄 오른쪽으로 이동.
  줄 높이를 그대로 써서 카드가 길어지지 않고 터치 영역은 36px 확보
- **쿠팡 구매 동선을 부족 재료 pill 로 이동**:
  - 기존엔 부족 재료가 여러 개여도 CTA 버튼은 **1개만 골라서** 보여줘서, 사용자가 사려는
    재료가 다르면 쓸모가 없었음. 게다가 모든 카드에 전체폭 버튼 + 2줄 고지문이 붙어
    20개 목록이면 광고 블록이 20개
  - 이제 점선(부족) pill 을 누르면 그 재료 구매로 연결(`+` 표시). 방금 부족 재료를
    "채워야 할 빈 칸"으로 만들었으므로 **"빈 칸을 누르면 채운다"** 는 동선이 자연스럽게 이어짐.
    사용자가 원하는 재료를 직접 고를 수 있고 클릭 의도도 높아짐
  - 링크 생성 로직을 `utils/coupangLink.ts` 로 분리 (기존엔 `CoupangProductAd` 안에만 있었음)
  - 별도 CTA 블록은 **부족 재료가 1~2개인 카드에만** 노출 — "하나만 사면 완성" 구간에서
    구매 전환이 실제로 일어나고, 5개 부족한 카드의 구매 버튼은 사실상 무의미하기 때문
- **미확인 사항**: 파트너스 고지문을 카드마다 반복하지 않고 목록 하단 1회로 줄이는 건
  쿠팡 파트너스 약관상 노출 요건 확인이 필요해 보류. 현재는 CTA 가 노출되는 카드에 함께 표시

### D단계: 모션 · 마감
- **로딩을 스켈레톤으로 교체** (`RecipeCardSkeleton` 신설, RecipeList / IngredientDetail 적용):
  예전엔 화면 한가운데 점 3개 스피너만 떠서 무엇이 로딩 중인지 알 수 없었고,
  로딩이 끝나는 순간 화면이 통째로 바뀌어 이동이 크게 느껴졌음
  → 실제 카드와 같은 모양(썸네일·제목·통계·pill)의 뼈대로 자리를 미리 잡아
  체감 대기시간을 줄이고 내용이 채워질 때 레이아웃이 흔들리지 않게 함.
  API 를 6초 지연시켜 로딩 상태를 잡은 뒤 스켈레톤 32개 렌더 확인
- **썸네일 페이드인 + `loading="lazy"`**: 이미지가 갑자기 튀어나오지 않고 부드럽게 나타나며,
  화면 밖 이미지는 늦게 불러와 초기 로딩 부담을 줄임
- **카드 누름 반응 추가**: 탭했을 때 아무 반응이 없으면 "안 눌렸나?" 하고 다시 누르게 됨
  → `:active` 에서 살짝 축소 + 투명도 변화. 기존의 hover translateY 는 모바일에서
  의미가 없어 제거하고 누름 반응으로 대체
- **접근성: 포커스 표시 복원**: 예전엔 `outline: none !important` 를 전역으로 걸어
  키보드 사용자가 현재 포커스 위치를 알 수 없었음 → `:focus-visible` 로 복원해
  마우스 클릭 시에는 안 보이고 키보드 이동 시에만 브랜드 컬러 아웃라인이 표시되도록 함

### 쿠팡 CTA 노출 기준 조정
- 부족 재료 1~2개 → **1~3개** 로 완화. 실제 목록에서 세어보니 1~2개 조건에서는
  해당하는 카드가 거의 없어 광고 노출이 과하게 줄어들었음
- (참고) 목록 하단으로 옮기자고 했던 것은 **광고가 아니라 고지 문구**임.
  고지문은 재료명이 들어가지 않는 일반 안내 문장이라 특정 재료와 매칭할 필요가 없음.
  구매 버튼과 pill 탭 구매 동선은 카드에 그대로 유지. 약관 확인 전까지는 현행 유지

### 쿠팡 클릭 측정 추가
- 광고 자리를 더 늘리기 전에 판단 근거부터 만들기로 함. 확인해보니 이 앱에는
  분석 도구가 **설치돼 있지 않았음** (`pwaAnalytics.ts` 에 gtag 호출 코드는 있으나
  `index.html` 에 GA 스크립트가 없어 어디서도 import되지 않는 죽은 파일),
  쿠팡 클릭도 전혀 기록되지 않고 있었음 → "카드 CTA vs pill 탭 중 뭐가 나은가"를
  누구도 답할 수 없는 상태였음
- `POST /api/track/coupang-click` 신설 + `coupang_clicks` 테이블 (없으면 자동 생성)
  - 기록 항목: 경로(`pill` / `card_cta`), 재료명, 그 카드의 부족 재료 개수, 레시피 id, 화면 경로
  - 측정 실패가 구매 동선을 막으면 안 되므로 어떤 예외에도 200 반환
- 프론트 `utils/trackCoupangClick.ts`: 클릭 직후 쿠팡으로 이동하므로 일반 fetch 는
  중간에 취소될 수 있어 `navigator.sendBeacon` 사용 (미지원 시 `keepalive` fetch 로 폴백)
- 2주 정도 쌓이면 "부족 1~3개 기준이 맞는지", "pill 탭이 실제로 쓰이는지",
  "하단 광고가 필요한지"를 추측이 아니라 숫자로 판단할 수 있음
- **하단 집계 광고는 만들지 않기로 함**: 50개 목록의 맨 아래는 도달률이 낮고,
  가장 자주 부족한 재료는 대개 설탕·간장 같은 기본 양념이라 구매 전환이 낮음.
  사용자가 "이게 없네" 하고 인지한 그 자리(부족 pill)에서 누르는 동선이 맥락상 우위

### 피드백 반영 1차: 애니메이션 회복 + 카드 세부
- **FAB 애니메이션이 사라진 것처럼 보이던 문제**: 측정해보니 shimmer/sheen 은 실제로
  돌고 있었으나(playState: running), **노란 버튼 위에 노란 그라데이션**이라 대비가 낮아
  움직임이 인지되지 않았음. 회전 링을 제거한 뒤 생긴 문제
  → sheen 을 더 또렷하게(피크 opacity 0.85 → 1), 글로우 세기 상향(0.5 → 0.75),
  버튼이 숨쉬듯 미세하게 커졌다 작아지는 `ai-fab-breathe` 추가.
  누를 때는 애니메이션을 일시정지시켜 축소 반응과 충돌하지 않게 함
- **로딩에 "Loading..." 표시 추가**: 스켈레톤만 있으면 "멈춘 화면"처럼 보인다는 피드백 →
  브랜드 컬러 링 스피너 + `Loading...` 문구를 목록 상단에 배치 (`role="status"`)
- **좋아요·댓글·조회수 줄바꿈 방지**: 한 줄로 고정하고 넘치면 말줄임 →
  카드 높이가 들쭉날쭉해지던 문제 해결
- **완료·기록·공유 버튼**: 아이콘이 작고 간격만 넓다는 피드백 →
  아이콘 18 → 22px 로 키우고 버튼 간 간격 제거(2 → 0), 오른쪽 여백 정리

### 피드백 반영 2차: 로딩 표시 통일 + 섹션 헤더 정리
- **로딩 표시가 화면마다 3종류로 달랐음** (탭을 옮길 때마다 다른 로딩이 떠 완성도가 떨어져 보임)
  - 내냉장고: 점 3개 + 한글 "로딩 중..."
  - 요즘인기 / 완료목록: 점 3개만 (문구 없음)
  - 냉장고요리 / 검색결과: 링 스피너 + "Loading..."
  → `LoadingIndicator` 컴포넌트 하나로 통일하고 **모든 화면에 적용**
- 링 스피너를 버리고 **점 3개가 순서대로 튀는 형태**로 되돌림. 점 크기도 12 → 13px 로 키우고
  간격·높이를 늘려 눈에 잘 띄게 함. 문구는 `Loading...` 으로 통일
- **섹션 헤더 통일** (`SectionHeader` 신설): 예전엔 섹션마다 마크업이 달랐음 —
  ⭐ 이모지를 제목 문자열에 섞어 넣은 것, 이미지 아이콘을 쓴 것이 섞여 있고
  정렬을 맞추려 `position: relative; top: 6px` 같은 임시 보정이 들어가 있었음
  → 이모지 제거(의미 있는 플랫폼 로고만 아이콘으로), 제목 16 → 18px, 설명문 지원
- **섹션 구분선 완화**: 2px 진한 회색 → 1px 옅은 선. 화면을 토막 내는 느낌 제거 (7곳)
- **요즘인기 카드 가로폭** 280 → 300px (좁아서 내용이 답답하다는 피드백)
- **재료 pill 접힘 기준** 8개 → 6개. 카드마다 3줄까지 늘어나 목록이 무거웠음

### 피드백 반영 3차: 팝업 규격 통일
- **팝업 6종의 수치가 전부 달랐음** — "다 제각각이라 촌스럽다"의 실제 원인
  | | 딤 | 모서리 | 폭 | 여백 |
  |---|---|---|---|---|
  | RegisterPrompt | 30% | rounded-xl | 320px | p-6 |
  | Welcome | (딤 없음) | rounded-lg | 가변 | px-5 py-4 |
  | IngredientDetail | 40% | rounded-2xl | 370px | p-8 |
  | IngredientDate | 40% | rounded-xl | 320px | p-6 |
  닫기 버튼도 `×` 텍스트인 곳, 아예 없는 곳이 섞여 있었음
- `ui/Dialog` 신설 — 확인/안내 팝업의 공통 규격
  (딤 40% / 모서리 20 / 폭 min(340, 100vw-40) / 여백 24 / 등장 애니메이션 /
  ESC 로 닫기 / 뒤 스크롤 잠금 / Portal / 하단 버튼은 `ui/Button` 사용)
- `RegisterPromptModal`, `WelcomeModal` 을 `Dialog` 로 완전 이관
- `IngredientDateModal`, `IngredientDetailModal` 은 내부 구조(달력·상세)가 복잡해
  전면 이관 대신 **겉 규격만 Dialog 와 동일**하게 맞춤 (모서리·폭·여백·그림자)
- 닫기 버튼 통일: `×` 텍스트 → 40×40 터치 영역의 SVG 아이콘 버튼 (`aria-label="닫기"`)
- 딤 농도를 40% 로 통일 (6개 파일)

### 피드백 반영 4차: 상단 GNB · 하단 네비 정리
- **상단 GNB**
  - 로고가 16px(`h-4`)로 지나치게 작아 브랜드가 눈에 안 들어왔음 → 20px 로 키우고
    44px 터치 영역의 버튼으로 감쌈
  - 닉네임·로그아웃 버튼에 `padding: 0` 이라 실제 터치 영역이 글자 높이뿐이었음 →
    높이 36px + 좌우 여백 확보, 공통 `NavTextButton` 으로 통일
  - **하단 구분선이 없어 스크롤 시 본문과 헤더가 섞여 보이던 문제** → `borderBottom` 추가
  - 닉네임이 길면 GNB 를 밀어내던 문제 → 최대 폭 + 말줄임
- **하단 네비게이션**
  - 실제로 쓰이지 않는 `STYLES` 상수 객체와 `CONSTANTS`(`NAV_HEIGHT: 16` 을 rem 으로 써
    실제 렌더와 무관했음)가 남아 있어 값을 어디서 고쳐야 할지 알 수 없었음 → 제거
  - 상단 구분선을 nav 에 붙이지 않고 **별도 `<div>` 를 절대배치**해 그리고 있었고
    그 div 만 `z-index: 30` 이라 다른 요소에 가려질 수 있었음 → nav 자체의 `borderTop` 으로 변경
  - **iPhone 홈 인디케이터 영역(safe-area) 대응 없어 하단이 가려지던 문제** →
    `env(safe-area-inset-bottom)` 반영
  - 탭 버튼을 `flex: 1` 로 균등 분배해 터치 영역을 화면 폭 전체로 확보
    (기존 `justify-around` 는 버튼 사이 빈 공간이 눌리지 않았음)
  - 활성 탭 표시를 `text-black/font-bold` → 토큰(`--ink-900` / `--ink-400`)으로 통일

### 피드백 반영 5차: 챗 패널을 팝업 공통 규격에 맞춤
- FAB 챗 패널만 다른 규격을 쓰고 있었음 (모서리 16 / 그림자 `0 8px 32px`) →
  `Dialog`·`Sheet` 와 동일하게 모서리 20 / `0 16px 48px` 로 통일
- 헤더 구분선을 `border-gray-100` → 토큰(`--line-200`)으로
- 닫기 버튼을 `×` 텍스트(38px) → 앱 공통 규격인 40×40 SVG 아이콘 버튼으로 교체.
  헤더의 원형 아이콘 버튼(지난 대화 / 새 대화)도 38 → 40px, 색을 토큰으로 통일
- 닫기 버튼 색이 옅어 잘 보이지 않아 `--ink-400` → `--ink-500` 로 한 단계 진하게
  (Dialog / 챗 패널 / 달력 3곳 공통)
- 챗 패널은 바텀시트로 바꾸지 않고 **떠 있는 패널 형태를 유지**함 —
  모바일 키보드 대응(`visualViewport` 로 높이·위치 재계산)이 이 구조에 맞춰져 있어
  시트로 바꾸면 그 처리를 다시 만들어야 하고, 얻는 것보다 잃는 게 큼

### 쿠팡 파트너스 고지문을 목록 하단 1회로
- 약관 확인 결과 페이지당 1회 노출로 진행하기로 함
- 기존엔 구매 버튼이 뜨는 **카드마다** 12px 2줄짜리 고지문이 반복돼,
  목록을 스크롤하는 내내 같은 문장이 계속 지나가며 화면을 무겁게 만들었음
  (재료명이 들어가지 않는 고정 안내 문장이라 카드마다 있을 이유가 없었음)
- `CoupangDisclaimer` 컴포넌트 신설 → `CoupangProductAd` 내부에서 제거하고
  광고가 노출되는 목록 화면 6곳(냉장고요리 / 검색결과 / 요즘인기 / 마이페이지 /
  기록한 레시피 / 완료한 레시피) 하단에 각 1회 배치
- 검증: 카드 50개가 렌더된 목록에서 고지문 노출 **1회** 확인

### 피드백 반영 6차: 애니메이션 정지 원인 해결 + 팝업 나가기 동선 통일
- **로딩·FAB 애니메이션이 동시에 멈춰 있던 원인 발견**: B단계에서 넣은
  `@media (prefers-reduced-motion: reduce)` 블록이 `*` 셀렉터로 **모든 애니메이션을
  0.01ms/1회로 죽이고 있었음**. 기기에서 "동작 줄이기"(iOS 손쉬운 사용 / Windows 애니메이션 표시)를
  켜면 로딩 점과 FAB 가 정지한 채로 보임 → 앱이 고장 난 것처럼 느껴짐
  - 로컬 측정에서 계속 "running" 으로 나왔던 건 테스트 브라우저가 그 설정을 물려받지 않았기 때문.
    `reducedMotion: 'reduce'` 로 재현해 확인함
  - → 장식적 움직임만 줄이고 **상태를 알리는 표시(로딩 점 / FAB / 스켈레톤)는 예외**로 둠.
    움직임 최소화 시 점은 튀는 대신 밝기만 변하도록 별도 keyframes 제공
- **팝업 나가기 동선 통일**: 우상단 X 가 있는 곳/없는 곳, 하단 취소 버튼이 있는 곳/없는 곳이
  섞여 있어 나갈 방법을 찾기 어려웠음
  - `ui/CloseButton` 신설 — 44px 터치 영역 + 연회색 원형 배경 + 굵은 SVG(22px, stroke 2.4).
    기존 `×` 문자는 폰트에 따라 크기·두께가 달라지고 얇아 잘 보이지 않았음
  - `Dialog` / `Sheet` / 챗 패널이 모두 이 버튼을 사용
  - `Dialog` 는 `actions` 가 비어 있으면 하단에 **취소 버튼을 자동으로** 넣음
  - `Sheet` 는 `footer` 가 없으면 하단에 **닫기 버튼을 자동으로** 넣음
  - 필터 시트 하단을 `취소` + `적용` 두 버튼으로 변경 (기존엔 적용만 있어 되돌릴 방법이 없었음)
- X 아이콘이 빈 원으로만 보이던 문제: flex 컨테이너 안에서 SVG 가 0px 로 찌그러지는 현상 →
  크기를 인라인으로 고정 (예전 FAB 아이콘과 동일한 원인)

### 피드백 반영 7차: 남아 있던 닫기 버튼 전부 교체
- 6차에서 `Dialog`/`Sheet`/챗 패널만 공통 버튼으로 바꿨고, **자체 마크업을 쓰는 팝업들은
  그대로 텍스트 `×` 였음** → 팝업마다 X 모양이 다른 상태가 계속됨
- 아래 8곳을 모두 `ui/CloseButton` 으로 교체
  - `RecipeSortBar` 매칭도 설정 / 임박 재료 설정 (텍스트 `×` 였음)
  - `CustomCalendar` 기간선택 (직접 만든 18px SVG — 원만 보이고 X 가 안 보였음)
  - `IngredientDetailModal`, `IngredientDateModal`
  - `MyPage` 내 정보 수정 (텍스트 `×`)
  - `RecordedRecipeListPage`, `CompletedRecipeListPage` 의 매칭률·임박재료 모달 (각 2곳)
  - `HomeInstallPrompt` 홈 화면 추가 안내
- 교체하지 않은 `×`: `FilterModal` 의 재료 칩 제거, `IngredientInput` 의 재료 삭제 —
  팝업 닫기가 아니라 항목 삭제 용도라 성격이 다름
- 검증: 매칭도 팝업에서 버튼 40×40 / SVG 22×22 / 배경 `#F5F5F7` 로 다른 팝업과 동일하게 렌더됨

### 피드백 반영 8차: 닫기 버튼 모양 · 팝업 헤더 높이 · 시트 드래그 닫기
- **닫기 버튼을 원형 → 둥근 모서리 사각형으로** (동그란 배경이 투박하다는 피드백)
  36×36, `borderRadius: 10`, 옅은 테두리 + 연회색 배경. 아이콘 20px
  - 실측 시 버튼 폭이 40px 로 나왔는데, 전역 `button { padding: 0.6em 1.2em }` 규칙이
    먹고 있었기 때문 → `padding: 0` + `boxSizing: border-box` 로 고정
- **팝업 제목 크기가 제각각이었음**: 달력 15px / 날짜입력 15px / 매칭도·임박재료 15px /
  기록·완료 모달 15px / 내 정보 수정 18px / 시트 18px → **전부 17px** 로 통일 (9곳)
- `ui/PopupHeader` 신설 — 높이 52px 고정 + 제목 17px + 우상단 닫기.
  기존엔 `minHeight: 56 + paddingTop 18` 같은 임의 값이 섞여 팝업마다 상단 높이가 달랐음.
  `Sheet` 가 이 헤더를 사용
- **바텀시트를 아래로 밀어서 닫기** 추가: 아래에서 올라온 시트는 아래로 미는 게 자연스러운
  닫기 동작인데 지원되지 않아 X 나 취소를 눌러야만 했음
  - 손가락을 그대로 따라 내려가고, 90px 이상 내리면 닫힘. 덜 내리면 스르르 제자리로 복귀
  - 내리는 동안 뒤 딤도 함께 옅어져 닫히는 느낌을 줌
  - 검증: 터치 이벤트로 160px 드래그 시 `translateY(160px)` 적용 → 손 뗀 뒤 닫힘 확인

### 피드백 반영 9차: 필터 시트 확장
- **시트가 중간 높이로만 열려 선택지가 안 보이던 문제**. 원인이 두 가지였음
  1. 시트 기본 높이가 낮았음 → 필터는 `82dvh` 로 넉넉히 열도록 지정
  2. **시트 안에 또 다른 고정 스크롤 영역**이 있었음 — 테마 선택란이 모바일에서
     `maxHeight: 100px` 로 묶여 있어 두 줄도 안 보였고, 시트 안에서 다시 스크롤해야 하는
     이중 스크롤 구조였음 → `46dvh` 로 확대
- **위로 끌어 펼치기 추가**: 시트를 위로 밀면 `95dvh` 로 확장. 손잡이를 탭해도 펼침/접힘 전환
  (드래그가 어려운 경우 대비). 펼친 상태에서 아래로 내리면 기본 높이로,
  기본 높이에서 내리면 닫힘 — 3단계 동작
- 최대로 펼쳐진 상태에서 위로 더 끌면 고무줄처럼 조금만 따라오게 해 한계를 알 수 있게 함
- 검증: 기본 82% → 펼침 95% 로 높이 변화 확인

### 피드백 반영 10차: 내냉장고 재료 설정 동선 개선
- **설정 버튼이 너무 작았음**: `⚙︎` 문자에 padding 4px 뿐이라 실제 터치 영역이 23px 남짓.
  유통기한을 자주 고치는 동선이라 32×32 아이콘 버튼(시계 모양)으로 교체
- **날짜 입력이 어색했던 부분 개선** (`IngredientDateModal`)
  - `yyyy-mm-dd` 를 손으로 다 입력하거나 달력을 열어야만 했음 →
    **빠른 선택 칩 추가** (유통기한: 3일 뒤 / 1주일 뒤 / 2주일 뒤 / 1개월 뒤,
    구매시점: 오늘 / 어제 / 3일 전 / 1주일 전)
  - 제목("일자를 선택하세요")과 바로 아래 질문("유통기한은 언제까지 인가요?")이 같은 말을
    두 번 하고 있었음 → 제목은 `유통기한` / `구매 시점` 으로 정리
  - 확인 버튼이 **파란색**이라 앱의 노랑·검정 체계와 따로 놀았음 → 공통 `Button` 사용
  - "잘 모르겠어요" 를 구매시점에만 주던 것을 유통기한에도 제공
- **재료 상세 팝업이 좌우로 잘리던 문제 수정 (제가 만든 회귀)**
  - 3차에서 팝업 규격을 통일하며 폭을 370 → 340px 로 줄였는데, 이 팝업은 내용이 넓어
    좌우가 잘려 "보관 공간"이 "산 공간"으로 보이고 닫기 버튼도 화면 밖으로 나갔음
  - 근본 원인은 **유통기한 버튼이 200px 고정 × 2개 + gap 32px = 432px** 로
    팝업 폭을 넘고 있었던 것 → 버튼을 `flex: 1` 가변폭으로, 아이콘 64 → 52px,
    팝업 폭 `min(360px, 100vw - 24px)` 로 조정
  - 유통기한 선택 버튼 색이 주황(#FF9800)/하늘색(#4FC3F7)이라 브랜드와 무관했음 →
    "있어요"는 브랜드 옐로우(주 선택), "없어요"는 중립 톤으로 위계 부여
  - 제목이 닫기 버튼과 겹쳐 잘려 보이던 것 → 좌우 여백 확보 + 공통 제목 규격(17px)

### 피드백 반영 11차: 입력창 포커스 스타일 통일
- **포커스 표시가 화면마다 제각각이었음**
  - 노란 테두리 + 노란 글로우: 챗 입력창(`.ai-chat-input`), 공통 `ui/Input`
  - 파란 링: `focus:ring-blue-*` (4곳)
  - 초록 링: `focus:ring-green-*`
  - 회색 테두리만: `focus:border-gray-400` (냉장고요리 검색창 등 3곳)
  - 아무 표시 없음: `focus:outline-none` 만 있는 곳
- 노란 링은 **브랜드 컬러를 "지금 입력 중"이라는 흔한 상태에 써버려** 과하게 튀었음
  → 테두리를 진하게(`--ink-700`) 하고 아주 옅은 회색 링(`rgba(26,26,30,0.07)`)만 두는
  절제된 방식으로 통일. 키보드로 이동해 들어온 경우(`:focus-visible`)엔 조금 더 또렷하게(0.14)
- `input` / `textarea` / `select` 전체에 적용되는 공통 규칙으로 만들고,
  컴포넌트에 흩어져 있던 색상 포커스 클래스를 6개 파일에서 제거
- 검증: 냉장고요리 검색창 / 내냉장고 재료 입력 / 챗 입력창 세 곳 모두
  `border: rgb(26,26,30)` + 동일한 링으로 렌더되는 것 확인

### LLM 재료 추출 매일 자동 실행 등록
- **어제 LLM 단계가 실행되지 않았음을 확인**: 로그에 `used_ingredients LLM 처리 시작` 이 0번
  찍혔고, 완료 문구도 코드상 문구가 아닌 옛 문구였음 → 어제 돌던 크롤러는 LLM 단계를 붙이기
  **전 버전의 스크립트를 메모리에 올린 상태**였음 (파이썬은 실행 중 파일 수정이 반영되지 않음)
- DB 실측: 전체 44,656건 중 LLM 처리 완료 **0건**.
  (수동 테스트로 처리했던 160건은 `llm_ingredients_done` 컬럼 추가 *전* 이라 표시가 남지 않음)
- **크롤러에서 분리**: 크롤러는 주 1회(월요일)만 도는데 LLM 처리는 매일 돌려야 하므로
  `run_llm_ingredients_daily.bat` 신설 + 작업 스케줄러 `CookMatch-DailyLLMIngredients` 등록
  (매일 새벽 3시, 절전 상태면 깨워서 실행)
- **묶음 8 → 12건**: 무료 티어 하루 500회 호출 중 450회만 사용(챗봇 몫 확보) →
  하루 450 × 12 = **5,400건**, 44,656건 기준 **약 9일** 소요
  (20건 이상으로 키우면 한 번에 보내는 본문이 길어져 LLM 이 뒷부분을 대충 처리하는 경향이 있어
   12건을 균형점으로 잡음)
- 소량(24건) 사전 검증 후 등록: 처리 24건 / 오류 0건 확인. 오늘분도 즉시 실행 시작

### ⚠️ 룰베이스 배치가 LLM 결과를 덮어쓰던 문제 수정
- **발견한 충돌**: 룰베이스 배치(`update_used_ingredients_batch.py`)가 조건 없이
  `SELECT ... FROM recipes ORDER BY id` 로 **전체 행**을 돌면서 `used_ingredients` 를
  덮어쓰고 있었음. LLM 추출도 **같은 열**을 쓰기 때문에,
  **주 1회 크롤러가 돌 때마다 그동안 LLM 이 며칠에 걸쳐 채운 결과가 룰베이스 값으로 되돌아감**
  - 게다가 `llm_ingredients_done` 은 1 로 남아 있어 LLM 이 다시 처리하지도 않음
    → 9일짜리 작업이 월요일마다 조용히 일부 무효화되는 구조였음
- 수정: 룰베이스 배치의 대상을 `WHERE COALESCE(llm_ingredients_done, 0) = 0` 으로 제한.
  룰베이스는 **아직 LLM 이 손대지 않은 신규 수집분의 임시 채움** 역할만 하도록 명확히 함
- 검증: 전체 44,518건 중 LLM 처리 완료 876건이 배치 대상에서 제외되어
  43,642건만 대상이 되는 것 확인

### 데이터 흐름 정리 (질문 답변 겸 기록)
- LLM 결과는 **새 열이 아니라 기존 `used_ingredients` 열을 덮어씀** →
  화면의 재료 pill 은 자동으로 LLM 결과로 바뀜 (UI 는 이 열 하나만 읽음).
  `llm_ingredients_done` 은 재료 값이 아니라 "이 행을 LLM 으로 처리했는지" 표시하는 깃발
- 이어서 처리 / 중복 없음: `--pending-only` 가 `done = 0` 인 행만 가져오고 성공 시 1 로 바꿈
  - 정상 추출 → 값 덮어쓰기 + `done=1`
  - 일시적 오류 → `done` 을 건드리지 않음 → 다음 실행에서 자동 재시도
  - LLM 결과만 비고 기존 값은 있음 → **값 보존** + `done=1` (무료 한도 낭비 방지)
  - 룰베이스도 비어 있고 LLM 도 비어 있음(둘 다 "재료 없음"에 동의) → **행 삭제**
    (레시피가 아닌 홍보성 본문으로 판단)

### 문서 전면 점검 및 갱신 (2026-08-26)
실제 코드·DB 와 대조해 문서를 최신 상태로 맞춤. 발견한 불일치는 아래와 같음.

**`DATABASE_SCHEMA.md` — 전면 교체 (실제와 거의 전부 달랐음)**
- `users`: 문서엔 `username`/`password_hash`/`phone`/`is_active` → 실제는
  `email`/`nickname`/`provider`/`provider_id`/`password`/`deleted_at` (소셜 로그인 구조)
- `user_ingredients`: 문서엔 `ingredient_name`/`category`/`quantity`/`unit`/`notes` →
  실제는 `name`/`storage_box`/`expiry_date`/`purchase_date`/`saved_at`
- 문서엔 `action_type` 을 가진 `user_recipes` 단일 테이블 → 실제는 즐겨찾기/기록/완료
  **3개 테이블로 분리**되어 있음
- 문서에 아예 없던 테이블: `recipes`(핵심!), `coupang_clicks`,
  `youtube_channel_cache`, `youtube_channel_meta`
- → 운영 DB 에서 직접 뽑아 다시 작성하고, `used_ingredients` 와 `llm_ingredients_done`
  의 관계·주의사항을 명시

**`PROJECT_OVERVIEW.md`**
- 재료 추출이 룰베이스/LLM **두 단계이고 주기가 다르다**는 점이 문서에 없었음 → 표로 추가
- 자동 실행 작업 2종(주간 크롤러 / 매일 LLM) 명시
- `llm_ingredients_done` 조건이 빠지면 룰베이스가 LLM 결과를 덮어쓴다는 경고 추가

**`DEVELOPMENT_GUIDE.md`**
- 개발 서버 포트가 **5173 으로 적혀 있었으나 실제는 5178** (`vite.config.ts`) → 수정
- 이번에 만든 **UI 체계 전체를 문서화**: 토큰 위치, `components/ui/` 7종,
  지켜야 할 규칙 6가지(팝업은 Dialog/Sheet, sticky 안에서 모달 렌더 금지,
  로딩은 하나만, reduced-motion 에서 `*` 금지, 포커스 개별 지정 금지, SVG 크기 고정)

**`README.md`**
- 폴더 구조도가 실제와 달랐음: 존재하지 않는 루트 `package.json`,
  `database/DATABASE_SCHEMA.md`, `data/*.csv` 등이 적혀 있고,
  실제로 있는 `ingredient_management/`, `scripts/`, `logs/`,
  `frontend/src/ui`·`styles/` 는 빠져 있었음 → 실제 구조로 교체
- CORS 예시에 5178 추가

**`MOBILE_APP_GUIDE.md`**
- "PWA 를 완성해야 한다"고 적혀 있었으나 실제로는 `manifest.json`·`sw.js` 가 이미 있음.
  다만 **서비스 워커 등록 코드가 어디에도 없어 실제로는 동작하지 않는 상태** →
  현재 상태를 표로 정리하고, 등록 시 캐시 무효화 주의사항 함께 기재

**`PERFORMANCE_OPTIMIZATION.md`**
- "FULLTEXT 인덱스 추가 (필수)" 로 남아 있었으나 실측 결과 `ft_title_content`
  (title, content) 가 **이미 적용돼 있음** → 적용 상태 표시 추가

### 피드백 반영 12차: 남은 UI 건 정리
- **챗 입력창 테두리가 굵고 노랗게 보이던 원인 발견 (제가 만든 버그)**
  - D단계에서 넣은 접근성 규칙 `input:focus-visible { outline: 2px solid var(--brand-strong);
    border-radius: 6px }` 이 **입력 요소까지 포함**하고 있었음
  - 챗 입력창은 패널이 열릴 때 자동 포커스되므로 **열자마자 노란 2px 외곽선**이 둘러졌고,
    `border-radius: 6px` 강제 때문에 알약 모양(9999px) 모서리까지 각지게 바뀌어 있었음
  - → 입력 요소를 규칙에서 제외(입력창은 `input:focus` 의 진한 테두리 + 옅은 링이 담당),
    버튼·링크용 외곽선 색도 노랑 → 중립 톤으로 변경. 모서리 강제도 제거
  - 검증: 모서리 `9999px` 복구, 노란 외곽선 사라진 것 확인
- **로딩이 화면마다 여전히 달랐던 이유**: 11차에서 컴포넌트는 통일했지만
  내냉장고·요즘인기는 `dotsOnly` 옵션을 써서 **점만 나오고 문구가 없었음** → 옵션 제거해 통일
- **로딩 중에 쿠팡 고지문이 함께 뜨던 문제**: `CoupangDisclaimer` 가 조건 없이 렌더되고 있었음
  → `{!loading && ...}` 으로 감싸 로딩이 끝난 뒤에만 표시 (4개 화면)
- **요즘인기 날짜 팝업의 연/월 스크롤바가 안 보이던 문제**: 드롭다운에 Firefox 전용
  `scrollbarWidth`/`scrollbarColor` 만 지정돼 있고 WebKit 규칙이 없었음.
  이미 있던 `.custom-scrollbar` 유틸을 두 드롭다운에 적용
- **재료 상세 팝업의 이모지 제거**: 얼음·눈송이·온도계 이모지풍 PNG 3장이 앱의 다른 요소와
  그림체가 달라 촌스러웠음 → **선으로 그린 SVG 아이콘**으로 교체(색은 토큰을 따름).
  선택 상태도 파란 ring(`ring-blue-400 bg-blue-50`) → 브랜드 톤(연노랑 배경 + 진한 테두리)으로

### 날짜 입력 팝업 여백이 없던 원인 수정 (제가 만든 버그)
- 증상: 유통기한·구매시점 팝업만 좌우·상하 여백이 전혀 없어 콘텐츠가 팝업을 뚫고 나오는 것처럼 보임
- 원인: 3차에서 팝업 규격을 통일할 때 `style` 속성을 **추가**했는데, 그 태그에는
  이미 `style={{ fontFamily: ... }}` 가 있었음.
  **JSX 는 같은 속성이 두 번 오면 뒤의 것이 앞의 것을 통째로 덮어쓴다.**
  결과적으로 `padding` / `width` / `borderRadius` / `boxShadow` 가 전부 무시되고
  `fontFamily` 만 적용된 상태였음 (측정값: `padding: 0px`, 카드 폭 286px)
- 수정: 두 style 을 하나로 합치고 `boxSizing: border-box`,
  `maxHeight: calc(100dvh - 64px)` + 세로 스크롤 추가 (긴 내용에서도 잘리지 않도록)
- 같은 실수가 다른 곳에도 있는지 **전체 tsx 를 검사** — 중복 `style` 태그 0건 확인
- 검증: 카드 폭 286 → 340px, `padding: 24px 20px 20px` 정상 적용 확인

### 내냉장고 재료 pill 동작 통합
- **문제**: pill 하나에 동작이 3개였음 —
  이름 탭(토스트로 날짜 표시) / 시계 버튼(설정 팝업) / X(삭제).
  시계 버튼은 pill 과 떨어져 있어 무엇인지 알기 어렵고, 이름 탭으로 뜨는 토스트는
  날짜를 "보여주기만" 하고 고칠 수는 없어 설정 팝업의 열화판이었음.
  (유통기한을 확인하는 이유는 대개 고치려는 것)
- **수정**: pill 을 누르면 **바로 설정 팝업**이 열리도록 합치고 시계 버튼 제거 →
  동작 3개 → 2개(탭 / X). 쓰이지 않게 된 `onInfoClick` prop 과 `handleTagInfo` 도 정리
  (`infoToast` 자체는 재료 저장 안내에도 쓰이므로 유지)
- **D-day 표시 추가**: 유통기한이 설정된 재료에만 `D-3` 형태로 표시.
  3일 이하·지난 재료는 붉은 톤으로 강조 → 임박 재료를 pill 을 눌러보지 않아도 한눈에 파악
- **날짜가 없는 재료에는 아무 표시도 넣지 않음**: 실측 결과 내 냉장고 172건 중
  유통기한 설정 **0건**, 구매일 **3건(2%)** 뿐이라, 빈 재료에 "+ 날짜" 같은 자리표시를 두면
  거의 모든 pill 에 붙어 방금 없앤 시계 아이콘과 같은 문제가 반복됨
- 부수 효과: 아이콘이 빠지면서 pill 이 짧아져 한 줄에 들어가는 재료 수가 늘어남

> 남은 과제: 날짜 입력률이 사실상 0% 인 근본 원인은 버튼 크기가 아니라
> **입력 시점에 날짜를 받지 않는 흐름**임. 영수증 촬영 → OCR 로 구매일·품목을 한 번에
> 얻는 방향이 유력하나 규모가 커 별건으로 논의 예정.

### 재료 상세 팝업 여백 + 뒤로가기 버튼 통일
- **재료 상세 팝업도 `style` 속성이 두 번이었음** (날짜 팝업과 동일한 원인).
  앞서 만든 중복 검사 스크립트가 이 건을 놓쳤는데, 정규식이 속성값 안의 화살표 함수(`=>`)를
  태그 종료(`>`)로 잘못 인식해 태그를 일찍 끊었기 때문
  → 중괄호 깊이를 세면서 **최상위 속성만** 검사하도록 스크립트를 고쳐 전체 재검사.
  실제 중복 **0건** 확인 (앞서 3건으로 나온 것은 prop 값 안의 중첩 JSX 속성을 함께 센 오탐)
- 두 style 을 합치고 `maxHeight` + 세로 스크롤 추가. 검증: 카드 폭 340px,
  `padding: 24px 20px 20px` 정상 적용

**뒤로가기 버튼 통일 (`ui/BackButton` 신설)**
- 닫기(X)는 둥근 사각형으로 통일해 놓았는데 뒤로가기만 제각각이라 같은 팝업 안에서
  두 버튼이 다른 물건처럼 보였음
  - `IngredientDateModal`: PNG 이미지(13px) + padding 4px
  - 기록/완료 목록, 재료 상세 페이지 헤더: PNG 이미지(13px)
  - 챗 패널 "지난 대화": `←` **문자** (18px)
- → `CloseButton` 과 완전히 같은 규격(36×36 둥근 사각형 + 20px SVG)으로 통일. 5곳 교체
- 쓰이지 않게 된 `뒤로가기.png` import(Popular), `backIcon`·`button` 스타일 상수 제거

### 내냉장고 재료 팝업: 키보드 가림 + 맥락 부족 수정
- **팝업이 키보드에 가리던 문제**: 재료명을 입력하고 "입력"을 누르면 팝업이 뜨는데,
  입력창에 포커스가 남아 있어 모바일 키보드가 그대로 떠 팝업을 덮고 있었음
  → 팝업을 열기 전에 `document.activeElement.blur()` 로 포커스를 풀어 키보드를 내림 (2곳)
- **팝업이 무엇을 하는 창인지 알기 어렵던 문제**: 제목이 "재료의 상세정보를 선택해 주세요"
  하나뿐이라 **어떤 재료**에 대한 것인지, **새로 넣는 것인지 이미 있는 걸 고치는 것인지**
  구분되지 않았음
  → 재료 이름을 제목으로 올리고(`돼지고기`), 그 아래에
  `보관 정보를 수정합니다` / `어디에 보관할지 알려주세요` 로 신규·수정 여부를 밝힘

### 내냉장고 저장 버튼 제거 + 자동 저장 신뢰성 개선
재료가 바뀔 때마다 이미 자동 저장이 돌고 있었고, 디스크 모양 저장 버튼은 그 위에 얹힌
수동 백업이었음. 자동 저장에 누락이 생기는 구조라 버튼이 필요했던 것 → 원인 3가지를 고치고 버튼 제거.

- **① 전체 삭제가 서버에 반영되지 않던 버그 (가장 심각)**
  ```js
  const hasData = frozen.length > 0 || fridge.length > 0 || room.length > 0;
  if (hasData) { ...저장... }   // ← 빈 배열이면 저장 자체를 건너뜀
  ```
  재료를 전부 지워도 저장을 하지 않아 서버엔 예전 목록이 남았고,
  **다음 로그인 때 지운 재료가 되살아났음** → 빈 상태도 정상 상태이므로 그대로 저장
- **② 저장 요청 순서 역전**: 연속으로 변경하면 요청이 겹치고, 늦게 출발한 요청이 먼저
  도착하면 옛 데이터가 최신을 덮어썼음 → 600ms 디바운스로 마지막 상태만 보내고,
  일련번호로 **오래된 응답은 무시**하도록 함
- **③ 실패해도 아무도 모름**: 자동 저장 실패 시 `console.error` 만 찍고 끝나서
  저장이 안 됐는데도 성공한 줄 알았음 (수동 저장 버튼이 생긴 원인)
  → 저장 상태를 화면에 표시(`저장 중… / 저장됨 / 저장하지 못했어요`)하고
  실패 시 **다시 시도** 버튼 제공
- 저장 버튼과 관련 온보딩 가이드 단계("저장버튼을 눌러 재료 정보를 저장할 수 있어요") 제거
- 검증: 저장 버튼 사라짐 확인, 냉동칸 재료를 전부 지운 뒤 저장값이 `frozen: 0` 으로
  반영되는 것 확인 (예전에는 저장 자체를 건너뛰던 경우)

### 피드백 반영 13차: 이모지 제거 · 드롭다운 떨림 · 포커스 톤 · 안내 문구
- **③ 섹션 표식 이모지 통일** (`ui/SectionIcon` 신설): 화면마다 표현이 달랐음 —
  내냉장고는 이모지(🧊 ❄️ 🌡️), 요즘인기는 ⭐ 를 제목 문자열에 섞어 넣고,
  마이페이지만 SVG 를 쓰고 있었음. 이모지는 기기·OS 마다 그림체와 크기가 달라
  같은 화면 안에서도 톤이 안 맞음 → 선으로 그린 SVG 7종으로 통일(색은 글자색을 따름)
- **④ 재료 입력 드롭다운 떨림 원인 발견·제거**: 스크롤바를 강제로 보이게 하려고
  드롭다운이 열릴 때 `scrollTop` 을 `3 → 0 → 2 → 0 → 1 → 0` 으로 튕기고,
  그것을 50ms·150ms·300ms 로 **세 번 반복 실행**하고 있었음 (총 15회 스크롤 조작)
  → 스크롤바는 CSS 로 표시하면 되므로 이 조작을 전부 제거하고 `.custom-scrollbar` 로 통일
- **② 입력창 포커스 톤 완화**: 진한 회색 테두리 + 링이 "굵어지고 칙칙하다"는 피드백 →
  테두리를 `--ink-400` 으로 한 단계만 진해지게 하고 링도 0.07 → 0.05 로 낮춤
  (키보드 탐색 시에는 여전히 더 또렷하게 유지)
- **⑤ '특별한 날 특별한 음식' 안내 문구 교체**: "값비싼 재료가 들어간 레시피를 모았어요" 는
  비싼 재료를 사게 하려는 의도가 그대로 드러나 보였음 →
  "기념일이나 손님상에 어울리는 레시피예요" 로 사용자가 얻는 것을 말하도록 변경

### 피드백 반영 14차: 전역 GNB 통일 (①)
- **탭마다 GNB 가 다르게 보이던 원인**: 일부 화면이 공통 GNB(`TopNavBar`) 위에
  **자기만의 고정 헤더를 하나 더** 그리고 있었음. `AppRouter` 가 이미 주요 경로에
  `TopNavBar` 를 렌더하는데, 아래 화면들이 `position: fixed; top: 0` 헤더를 또 만들어
  **정확히 같은 자리에 두 개가 겹쳐 있었음** (둘 다 높이 56px, 같은 z-index)
  - `MyPage`: 로고·닉네임·로그아웃까지 **공통 GNB 를 통째로 복제**.
    게다가 복제본은 `maxWidth: 400px` 이라 넓은 화면에서 정렬이 달라 보였음
  - `IngredientDetail`, `RecordedRecipeListPage`, `CompletedRecipeListPage`:
    뒤로가기(+필터) 전용 헤더가 GNB 와 겹침
- 수정
  - `MyPage` 의 복제 GNB 제거 → 공통 GNB 하나만 사용
  - 나머지 3개 화면은 고정 헤더를 없애고 **뒤로가기·필터를 본문 제목 줄로 이동**.
    (헤더를 top:56 으로 내리는 대신 제목 줄에 합쳐 세로 공간 56px 절약)
  - 헤더가 사라진 만큼 본문 상단 여백 88 → 72px 로 조정
- 검증: 5개 주요 화면 모두 고정 헤더 **1개**, 높이 56px, 동일한 하단 구분선으로 통일 확인
