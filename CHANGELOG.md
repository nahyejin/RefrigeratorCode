# CHANGELOG

쿡매치(CookMatch) 프로젝트 변경 이력. Cursor AI에서 Claude Code로 작업 환경을 이관한 2026-08-24부터 기록.

## 2026-08-24

### 프로젝트 구조 정리
- 옛 CRA 프론트엔드 잔재 삭제 (루트 `src/`, `package.json`, `vercel.json`, `main.py`) — 실제 배포는 `frontend/`에서 진행되므로 영향 없음
- `backend/App.tsx`, 루트 `check_db.py`, 중복 `export_recipes_all.ipynb` 등 죽은 코드/중복 파일 삭제
- 로컬 SQLite 흔적(`posts.db`, `recipes.db`, `refrigerator.db`) 삭제 — 현재는 MySQL(Railway) 사용
- `crawler.log`, `chromedriver-win64/`, `__pycache__/` 전체를 git 추적 해제 (로컬엔 유지, `.gitignore` 반영)
- 루트에 흩어진 디버그/점검용 스크립트 18개를 `scripts/`로 이동
- `utils/requirements.txt.txt`(오타성 파일명)를 `ingredient_management/requirements.txt`로 이동

### 요리 챗봇 기능 추가
- `backend/chat_service.py`: Gemini/Groq LLM으로 대화 의도를 파악해 DB에서 레시피를 검색하는 `/api/chat` 엔드포인트 신설. 일일 호출 한도(기본 250회) 적용
- 프론트에 전역 플로팅 챗 위젯(`RecipeChatWidget.tsx`) 추가 — 냉장고 재료(localStorage) + 대화 기록을 세션 단위로 유지하며 추천 레시피 카드 표시
- Gemini 기본 모델을 `gemini-2.0-flash`(단종됨) → `gemini-3.5-flash-lite`로 교체
- 레시피 썸네일에 기존 `getProxiedImageUrl` 유틸 적용 — 네이버 이미지 핫링크 차단(403) 우회
- 로컬 개발 CORS 이슈 해결 (프론트 dev 서버 포트를 화이트리스트에 있는 5178로 통일)

### 챗봇 FAB를 AI 기능처럼 리디자인
- 텍스트만 있던 "챗" 버튼을 스파클 아이콘 + 회전하는 그라디언트 링 + 펄스 글로우 + "AI" 배지로 교체
- 채팅창 헤더와 어시스턴트 말풍선에도 같은 스파클 아바타 적용해 AI 브랜딩 통일

### 챗봇 대화 히스토리 관리 + 재료 매칭 우선 검색으로 재설계
- `RecipeChatWidget.tsx`: 채팅창을 닫았다 다시 열면 항상 새 대화로 시작하도록 변경. 지난 대화는 localStorage에 스레드 단위로 저장(최근 30일 보관)되고, 헤더의 "지난 대화" 버튼으로 목록을 열어 이어보거나 삭제 가능
- `chat_service.py`: 기존엔 LLM이 뽑은 키워드가 title/content LIKE **강제 필터**라서, 냉장고 재료와 매칭률이 높은 레시피도 키워드가 안 맞으면 통째로 걸러졌음. 이제 냉장고 재료가 있으면 키워드는 `HAVING` 절의 소프트 조건(키워드 매칭 OR 매칭률 25% 이상)으로 바뀌어, 매칭률 높은 레시피가 우선 노출됨
- LLM 프롬프트에 `ignore_fridge` 필드 추가: 사용자가 "재료 상관없이" 등으로 명시적으로 요청할 때만 냉장고 매칭을 끄고 순수 키워드 검색으로 전환
- 레시피 카드의 "재료 매칭률 N%"를 일반 텍스트에서, 기존 `RecipeCard.tsx`와 동일한 톤(짙은 배지 + 노란색 굵은 글씨)의 배지로 변경

### 재료 추출 파이프라인을 LLM 기반으로 재설계 (진행 중)
- `ingredient_management/llm_ingredient_extraction.py` 신설: Gemini로 본문에서 실제 사용 재료를 "이해"해서 뽑아내고,
  정규화는 100% 기존 사전(`ingredient_profile_dict_with_substitutes.csv`)의 결정론적 매칭으로만 처리
  (LLM 역할을 추출까지만 최소화, 매칭은 LLM 관여 없음). 출력 포맷은 기존과 동일(콤마구분/공백없음/정렬)하게 유지해
  프론트 pill 매칭·백엔드 매칭률 SQL을 전혀 건드리지 않음
- 사전에 없는 LLM 추출 후보는 버리되 `unmapped_ingredients` 컬럼에 기록 (추후 사전 보강용)
- 20건 샘플 검증 결과, 기존 룰베이스의 실제 버그들(예: "청양고추"를 "고추"+"청양고추"로 중복 집계, "만두"/"쌈무" 같은 요리명이 재료로 오인식, 블록 탐색 범위가 좁아 뒷부분 재료 누락)이 다수 수정됨을 확인
- 전체 44,121건에 대해 `--commit` 모드로 백그라운드 배치 실행 중 (모델: gemini-3.5-flash-lite, 분당 140건, 동시 7개)
- **버그 수정**: 최초 버전은 전체 결과를 다 모은 뒤에야 CSV/DB에 쓰는 구조라 중간에 죽으면 진행 상황이 전부 유실되는
  문제가 있었음 → 결과가 나오는 즉시 CSV에 flush, 30건마다 DB 커밋하도록 수정 후 재시작
- **실측 결과**: 이 API 키는 무료 티어라 `gemini-3.5-flash-lite`가 분당 15건으로 하드 제한됨. 분당 140건으로
  돌렸다가 에러율 27%를 확인하고 분당 13건(안전 마진)으로 재시작. 44,121건 전체 기준 약 2~2.5일 소요 예상

### git 추적 충돌 발견 및 정리
- 세션 도중 내가 만들지 않은 커밋 2개(`대거변동 260825`, `업데이트 20260825_12:37`)가 발견됨 — 다른 도구(Cursor 등
  자동 커밋으로 추정)가 같은 저장소에 동시에 작업 중인 것으로 보임. 이 커밋들이 `.gitignore`를 이전 버전으로
  되돌리고 pycache/chromedriver/crawler.log를 다시 추적시킴 → 재정리하고 배치 산출물 CSV도 gitignore에 추가
- **주의**: 다른 자동 커밋 도구가 계속 켜져 있으면 다시 충돌할 수 있음. Cursor 등 확인 필요

### 챗봇 UI 세부 디자인 개선
- FAB/헤더/말풍선의 스파클 아이콘 → 김이 나는 냄비 아이콘으로 교체 (SVG가 flex 컨테이너 안에서
  width 0으로 찌그러지던 렌더링 버그도 같이 수정)
- 지난 대화·새 대화 버튼을 아이콘 버튼(38px)으로 확대, 새 대화는 + 아이콘으로 통일
- 안내 문구를 기능 설명 → 사용자 편익 중심으로 변경, placeholder도 "맛"에서 "먹고 싶은 걸"로 범위 확장
- 채팅창 타이틀 강조(굵게/크게), 창 높이 70vh/560px → 84vh/700px로 확대
- GitHub main에 푸시 완료 (Vercel 자동 배포 트리거됨)

### 배포 환경변수 설정
- Railway 백엔드에 `GEMINI_API_KEY` 환경변수 추가 완료 (운영 환경 챗봇 활성화)

### 크롤러 자동화 안정화 (Task Scheduler 등록 준비)
- `run_all_crawlers.py`가 계속 실패하던 원인 파악: ChromeDriver 버전 자동 감지가
  PowerShell/wmic 서브프로세스에 의존하는데, 실패해도 감지된 버전을 실제로 쓰지
  않는 버그가 있어 최신 Chrome보다 오래된/새로운 ChromeDriver를 받아와 세션 생성이
  계속 실패했음. 레지스트리 기반 감지로 교체하고 감지된 버전을 실제로 전달하도록 수정
- 크롤러 파이프라인에 LLM 일일 증분 추출 단계 연결 (룰베이스 폴백 유지 + 무료 한도 내 LLM 보정)
- 의존성 누락(dnspython, tqdm), 미사용 import(mysql.connector) 정리
- 매일 오전 7시 자동 실행할 `run_crawlers_scheduled.bat` 준비 (Windows 작업 스케줄러 등록 예정)

### LLM 배치 처리 + 하루 500건 한도 대응
- `llm_ingredient_extraction.py`에 `--batch-size` 옵션 추가: 레시피 여러 건을 호출
  1번에 묶어 처리 (기본 8건) → 일일 호출 횟수 한도를 훨씬 적게 사용, 챗봇과 공존 가능
- `--pending-only` 옵션 추가: DB의 `llm_ingredients_done` 플래그로 "이미 처리됨"과
  "신규/미처리"를 구분해, 크롤러가 매일 신규 수집분만 무료 한도 내에서 처리하고
  오늘 못 채운 건 자동으로 다음날로 넘어가도록 함
- 44,121건 기존 백로그는 유료 티어로 별도 처리 예정 (실측 토큰 기반 예상 비용 약 9천~1만 8천원)

### UI 디자인 정리
- 실제 화면 4곳(내 냉장고/냉장고 요리/요즘 인기/마이페이지)을 스크린샷으로 검토해
  아쉬운 지점 정리 후 우선순위대로 수정:
  - AI FAB와 PWA 설치 유도 배너가 z-index 문제로 겹쳐 보이던 것 수정
  - 내 냉장고 재료 태그가 진회색+흰 글씨로 뭉쳐 보이던 것을 연회색+진회색으로 가볍게 변경
  - "부족 재료" pill이 연회색 배경에 흰 글씨라 실질적으로 안 보이던 대비 문제 수정
    (앱 전역 10곳의 범례도 함께 정리)
  - 냉장고요리 페이지 필터 버튼 4개가 전부 같은 톤이라 위계가 없던 것을 아이콘+진한
    "필터" 버튼으로 구분
  - 스플래시 화면을 베이지/일러스트 톤에서 실제 CookMatch 아이콘 기반 화이트/블랙/
    옐로우 톤으로 재구성 (본편과 첫인상 통일)

### 무료 티어 실측: 하루 500건이 진짜 병목
- 분당 13건(안전 마진)으로도 대량 에러 발생 → 원인은 분당 제한이 아니라
  `GenerateRequestsPerDayPerProjectPerModel-FreeTier` **하루 500건** 한도였음
- 44,121건 ÷ 500건/일 ≈ 88일. 게다가 운영 챗봇도 같은 키의 같은 일일 한도를 공유해서
  무료 티어로는 배치와 실사용자 챗봇을 동시에 운영할 수 없음 → 유료 티어 전환 필요, 사용자 결정 대기 중
- 오늘 처리분: 948건 시도, 성공 160건(DB 반영), 나머지는 일일 한도 초과로 실패

### 데이터 안전장치 추가 (중요)
- **버그 발견**: LLM이 빈 배열을 반환하면(파싱 실패/모델 이상 응답 등) 기존에 있던 재료 목록을
  조용히 빈 값으로 덮어써버리는 문제가 있었음. 160건 중 2건에서 실제 발생(id=1326 '초계라면'은
  진짜 레시피인데 빈 값으로 덮어써짐 → 기존 값으로 복구 완료. id=1295는 레시피가 아닌 홍보 영상이라
  원래도 룰베이스가 오탐한 것이었고 빈 값이 맞아서 그대로 둠)
- **수정**: 기존 `used_ingredients`가 있는데 LLM 결과가 비어 있으면 DB에 반영하지 않고 CSV에만
  `NEEDS_REVIEW`로 표시 (DB에는 절대 안 들어가므로 사용자에게 노출되는 값은 항상 기존 값 그대로 유지됨)

### 챗봇 FAB 로고/타이틀 변경
- FAB/헤더/말풍선 아이콘을 냄비 모티프에서 쿡매치 메인 아이콘(집 지붕 + 밥그릇) 모티프로 교체
- 타이틀 "AI 요리 챗" → "쿡매치 AI" (챗봇 느낌을 줄이고 브랜드를 앞세움)

### 레시피 아닌 본문 자동 삭제 로직 추가
- 룰베이스(old)와 LLM(new)이 둘 다 "재료 없음"에 동의하는 행만 레시피가 아닌 것으로 보고
  `recipes` 테이블에서 행 자체를 삭제 (예: 홍보 영상, 안내 영상 등)
- old에는 재료가 있었는데 new만 비어 있는 경우(불일치)는 삭제하지 않음 — LLM 쪽의 일시적
  오류일 수 있어 위 데이터 안전장치와 동일하게 NEEDS_REVIEW로만 표시하고 기존 값 보존
- 수동 확인한 사례 2건 처리: id=1326(진짜 레시피, 오류로 비워졌던 것)은 기존 값 복구,
  id=1295(레시피 아닌 통영 축제 홍보 영상)는 행 자체 삭제
- `ingredient_management/llm_extracted_158_review.csv`: 성공 처리된 158건의 링크 +
  LLM 추출 재료(원본/정규화 후) 검토용 파일 생성

## 2026-08-25

### 스티키 필터 영역 (냉장고 요리 페이지)
- `RecipeList.tsx`: 검색창 + 정렬/필터 바 + 재료 pill 범례("부족 재료 ... 총 000건")를
  스크롤해도 상단에 고정되도록 sticky 처리 (top: 56px, GNB 바로 아래)
- 범례가 화면 밖으로 사라져서 pill 색상의 의미를 잊게 되는 문제 해결

### 챗 패널 모바일 키보드 대응
- `RecipeChatWidget.tsx`: `visualViewport` API로 키보드 높이를 감지해 패널 위치/높이를 재계산.
  키보드가 올라와도 패널 상단이 잘리지 않음
- 메시지 목록/지난 대화 목록에 `minHeight: 0` 추가 — flex 자식이라 스크롤이 안 걸리던 문제 해결
- 패널 타이틀 글꼴/굵기를 앱 공통 톤(18px / 700)에 맞춤

### 내 정보 수정 진입 경로 + 닉네임 마스킹 문제
- `TopNavBar.tsx`: GNB의 닉네임을 버튼으로 바꿔 클릭 시 `/my-page?openEdit=true`로 이동
- `MyPage.tsx`: `openEdit=true` 쿼리 파라미터를 감지해 "내 정보 수정" 모달 자동 오픈
- 버튼 문구 "적용" → "변경 적용"
- **닉네임이 `9208****`로 보이던 원인**: 프론트/백엔드 어디에도 마스킹 코드는 없었고,
  네이버 로그인 API(`/v1/nid/me`)가 별명(nickname)을 마스킹된 형태로 내려주는데 그 값을
  그대로 저장한 것이었음 (DB의 users.nickname 실제 값 자체가 `9208****`)
- `app.py` 네이버 콜백 수정: 닉네임이 비어 있거나 `*`를 포함하면 이메일 로컬 파트를 대신 사용
  → 이후 신규 네이버 가입자는 마스킹된 닉네임이 저장되지 않음
- 기존에 저장된 값은 "내 정보 수정"에서 직접 변경 필요

### Sticky 영역 여백 + 다른 목록 페이지에도 동일 적용
- `RecipeList.tsx`: sticky 영역에 `paddingBottom: 10` 추가. 기존엔 범례의 `marginBottom`이
  margin collapsing으로 sticky 컨테이너 밖으로 빠져나가서, 스크롤 시 카드가 "부족 재료 / 대체 가능 /
  보유 재료" 글자에 바짝 붙어 지나갔음. 이제 범례 아래 흰 여백이 항상 유지됨
- 같은 형식의 범례를 쓰는 나머지 목록 페이지에도 동일하게 sticky 적용
  (정렬/필터 바 + 범례가 함께 상단 고정, top: 56px)
  - `IngredientDetail.tsx` — 요즘인기 페이지 하단 검색 → `/ingredient/:name`으로 이동하는 페이지
  - `RecordedRecipeListPage.tsx` — 내가 기록한 레시피
  - `CompletedRecipeListPage.tsx` — 내가 완료한 레시피
- `Popular.tsx`와 `MyPage.tsx`의 범례는 제외 — 이쪽은 섹션(유튜브 인기 / 네이버 인기 / 특별한 날 /
  즐겨찾기 / 기록 / 완료)마다 각각 붙어 있는 가로 스크롤 캐러셀 헤더라, sticky로 만들면 범례 여러 개가
  서로 밀어내며 겹침. 세로로 길게 스크롤되는 목록 페이지에만 적용하는 것이 맞음

### 챗봇 FAB를 노란 말풍선 버튼으로 리디자인
- 검정 원 + 쿡매치 마크(집 지붕+밥그릇) + 회전 그라디언트 링 조합이 무겁고 안 예쁘다는 피드백 반영
- 브랜드 옐로우(#FFD600) 원형 버튼 + 어두운 말풍선 아이콘(안쪽 점 3개는 노란색으로 뚫음)으로 교체
- 회전하는 conic-gradient 링 제거 (노란 버튼 위에서는 색이 뭉개짐). 은은한 노란 글로우 펄스는 유지
- "AI" 배지를 노란 배경/검정 글씨 → 검정 배경/노란 글씨로 반전 (노란 버튼 위 대비 확보)
- 누를 때 살짝 눌리는 `:active` 스케일 피드백 추가
- 아이콘 클래스를 FAB 전용(`.ai-fab-bubble`)으로 분리 — 패널 헤더/말풍선 아바타의 쿡매치 마크는
  기존 그대로(어두운 원 + 노란 마크) 유지되도록 함

### 챗 패널 아바타/헤더 정리 + FAB 애니메이션 복원
- 패널 헤더 아바타와 답변 말풍선 아바타를 FAB와 동일한 디자인(노란 원 + 어두운 말풍선)으로 통일.
  기존의 검정 원 + 쿡매치 마크(집 지붕+밥그릇) 조합은 FAB와 따로 놀아서 제거
- 사용하지 않게 된 `CookMatchMarkIcon` 컴포넌트와 관련 CSS 삭제
- 헤더의 부제 "오늘 뭐 먹을지, 냉장고 재료로 같이 찾아볼게요 🍳" 제거.
  좁은 화면(앱)에서 말줄임으로 잘리는 데다, 본문 첫 안내 문구
  "뭐 먹을지 고민될 때, 냉장고 재료로 딱 맞는 레시피를 찾아드릴게요."와 내용이 중복됨
- FAB 애니메이션 복원/강화: 회전 링을 뺀 대신
  - 노란 그라데이션이 버튼 위를 천천히 흐르는 `ai-fab-shimmer` (4.2s 무한 반복)
  - 주기적으로 스쳐 지나가는 하이라이트 `ai-fab-sheen`
  - 기존 노란 글로우 펄스 유지
  - 말풍선 아이콘은 하이라이트 위에 올려 흐려지지 않게 처리

### 챗봇 예시 질문 확대 + 입력창 UI 개선
- 첫 진입 시 보여주는 예시 질문을 3개 → 8개로 확대. 맛/냉장고 위주였던 걸
  대상·목적·상황까지 넓힘
  - 기존: 매운 거 / 있는 재료로 / 재료 상관없이 간단한 거
  - 추가: 아이가 잘 먹는 반찬 / 다이어트 중인데 뭐 먹지 / 캠핑 가서 해먹기 좋은 요리 /
    술안주 / 15분이면 되는 자취 요리
- 입력창이 흰 배경에 연회색(gray-100)이라 입력 가능한 영역인지 구분이 안 된다는 피드백 반영
  - 흰 배경 + 진한 테두리(#D8D8DE) + 라운드 필 형태로 변경해 필드 경계를 명확히 함
  - 포커스 시 브랜드 옐로우 테두리 + 글로우 링 표시
  - 입력 영역 전체 배경을 살짝 톤다운(#FAFAFB)해 대화 영역과 시각적으로 분리
  - "보내기" 버튼을 검정 → 브랜드 옐로우로 변경 (비활성 시 회색), 높이를 입력창과 40px로 맞춤

### 챗 패널 스크롤 문제 해결
- 측정해보니 스크롤 자체는 동작하고 있었음(scrollHeight 760 > clientHeight 512). 문제는 세 가지였음
- **뒤 페이지로 스크롤 전파**: 패널 안을 드래그하다 끝에 닿으면 뒤 페이지가 대신 밀려서
  "패널이 안 움직인다"고 느껴짐 → 스크롤 영역에 `overscroll-behavior: contain` 적용
- **뒤 페이지 스크롤 미잠금**: 패널이 열려 있는 동안 `document.body`를 `overflow: hidden`으로 잠그고
  닫힐 때 원복
- **답변 도착 시 맨 아래로 점프**: 긴 답변이 오면 곧장 최하단으로 스크롤해서 답변 앞부분이 안 보였고,
  사용자 입장에선 내용이 잘려 보임 → 이제 '마지막 질문'이 상단에 오도록 스크롤해서
  답변을 처음부터 읽을 수 있고, 아래에 더 있다는 것도 카드가 걸쳐 보여서 인지됨
- 스크롤바를 얇게(6px) 보이도록 스타일링해 스크롤 가능 영역임을 시각적으로 표시

## 2026-08-25 (UI 리팩터링)

### 1단계: 죽은 CSS 정리 + 촌스러움의 실제 원인 2건 제거
- **정정**: "폰트가 맑은고딕으로 떨어진다"는 최초 진단은 틀렸음. 브라우저 실측 결과
  body/h2/button/input 모두 Pretendard로 정상 렌더되고 있었음. 맑은고딕 규칙은
  `pill-label`/`tag-label`/`filter-pill`/`small-pill` 등 **사용처가 0인 클래스**만
  겨냥하고 있어 한 번도 적용된 적이 없었음
- **모든 글자의 흰색 윤곽선 제거** (진짜 원인 ①): `body`에 `text-shadow: 0 0 1px #fff`가
  4겹 걸려 있어 앱 전체 글자가 뿌옇게 번져 보였음. 이 규칙을 되돌리려고 코드 곳곳에
  `textShadow: 'none'` 인라인 우회가 22군데나 생겨 있었음 → 원인을 제거하고 우회 22곳도 함께 삭제
- **페이지 스크롤바를 보이게 변경** (진짜 원인 ②): `html/body/#root`의 스크롤바 thumb이
  `background: transparent`로 되어 있어 스크롤바가 아예 안 보였음. "스크롤이 자꾸 안 보인다"는
  체감의 직접 원인 → 은은하지만 보이는 thumb(검정 18%, hover 30%)으로 변경, Firefox용
  `scrollbar-width/color`도 추가
- **죽은 CSS 규칙 약 80줄 삭제**: `.text-[11px]`, `.bg-[#D1D1D1]`처럼 대괄호를
  이스케이프하지 않은 셀렉터가 목록에 섞이면 CSS 규칙 **전체가 무효**가 됨.
  pill 폰트 강제 블록(Noto Sans KR / font-weight 200 / 12~14px !important)이 전부 이 경우라
  적용된 적 없는 죽은 코드였음. 남겨두면 나중에 해당 클래스를 쓰는 순간 예상 못한 스타일이
  튀어나오는 지뢰라 제거

### 2단계: 디자인 토큰 도입 + 재료 pill/범례 단일화
- **수치 정정**: 앞서 "고유 색상 252종"이라고 했던 건 SVG 에셋 파일(`share_black.svg` 등)이
  섞여 부풀려진 값이었음. 실제 코드(tsx/ts/css) 기준 정확한 수치는 **92종**
- `src/index.css`의 `:root`에 디자인 토큰 정의 (ink 5단계 / line 2단계 / surface 2단계 /
  브랜드 5종 / 상태 3종 / z-index 6단계). `tailwind.config.js`에도 같은 팔레트를 반영해
  `text-ink-500`, `bg-brand-soft` 같은 클래스로도 쓸 수 있게 함
- 흩어진 색상 값 **506곳을 팔레트로 정규화** → 고유 색상 **92종 → 42종**.
  (`#222`/`#111`/`#1A1A1A` → `--ink-900`, `#E5E7EB`/`#E5E5E5` → `--line-200` 등.
  브랜드·플랫폼(네이버 초록/유튜브 빨강)·상태·캘린더 색은 의미가 있어 병합하지 않음)
- **쿠팡 광고 버튼 톤다운**: 채도 높은 주황 꽉 찬 버튼이라 카드에서 가장 눈에 띄었고,
  정작 주인공인 레시피보다 광고가 강조돼 있었음 → 흰 배경 + 옅은 주황 테두리 +
  주황 글씨의 외곽선 스타일로 변경
- **재료 pill 원색 노랑 제거**: 카드마다 보유 재료 pill이 4~6개씩 원색 노랑(#FFD600)으로
  꽉 차 있어 화면이 노란 덩어리가 되고, 브랜드 컬러가 "데이터 상태" 표시에 소모되고 있었음
  → 연한 노랑 배경(#FFF1B8) + 진한 노랑 글자(#6B5200)로 낮춤.
  부족 재료는 더 옅게(#F0F0F3) 물러나게 하고, 드물게 나오는 대체 가능만 진한 색 유지
- **pill 색과 범례를 단일 소스로 통합**: `src/styles/ingredientPill.ts` 신설.
  기존엔 범례 마크업이 6개 파일에 10곳 복붙돼 있어 pill 색을 바꿔도 범례는 안 따라오는
  구조였음 → `IngredientLegend` 컴포넌트로 추출해 10곳 모두 교체.
  이제 색을 바꾸려면 `ingredientPill.ts` 한 파일만 고치면 됨

### 3단계: z-index 체계화 + 모달 스택 맥락 탈출(Portal)
- z-index 값이 20종(1 ~ 10060)이 규칙 없이 흩어져 있었음. 새 요소를 넣을 때마다 숫자를
  키우는 식(9999 → 10001 → 10030 → 10060)으로 대응해 온 흔적 → **역할 기반 7단계**로 정리
  (`--z-sticky 100` / `--z-nav 200` / `--z-dropdown 300` / `--z-fab 400` /
  `--z-overlay 500` / `--z-modal 600` / `--z-toast 700`). 54곳 치환.
  카드 안 아이콘 같은 지역 스택(1~30)은 의미가 다르므로 그대로 둠
- **모달이 헤더/하단 네비 아래에 깔리던 버그 수정** (히트 테스트로 발견):
  필터 모달이 `position: sticky` 인 필터바 **안에서** 렌더되고 있었음. sticky는 새 스택 맥락을
  만들기 때문에, 모달의 z-index를 아무리 올려도 그 조상의 층위(100) 안에서만 높을 뿐
  바깥의 헤더·네비(200)보다 위로 갈 수 없었음
  → `Portal` 컴포넌트 신설(`createPortal`로 `document.body` 직속 렌더)하고
  FilterModal / IngredientDetailModal / RecipeSortBar의 매칭도·임박재료 모달에 적용.
  수정 후 히트 테스트로 헤더·네비·FAB 위치 모두 모달 딤이 덮는 것 확인

### 4단계: 남은 중복 제거
- **런타임 `<style>` 주입 제거**: 로딩 점 애니메이션 CSS가 5개 페이지 파일에 각각 복붙되어 있고,
  모듈 최상위에서 `document.createElement('style')` 로 `<head>` 에 주입하고 있었음
  (페이지를 옮겨 다니면 동일한 `<style>` 태그가 계속 쌓임) → `index.css` 로 옮기고 주입 코드 삭제.
  점 색상도 `var(--brand)` 로, z-index 도 `var(--z-overlay)` 로 토큰화
- **Toast / RecipeToast 통합**: 두 컴포넌트가 거의 같은 스타일 정의를 각자 들고 있었음
  → `RecipeToast` 가 `Toast` 에 위임하도록 변경(여러 줄 표시 옵션만 다름)
- `Toast` 를 `Portal` 로 감쌈 — sticky/transform 조상 안에서 호출돼도 층위에 갇히지 않도록
- 페이지에 인라인으로 복붙돼 있던 토스트 마크업 중 형태가 동일한 2곳을 `<Toast />` 로 교체

### 5단계: 화면 정리
- **`WelcomeModal` 에 딤 배경 추가**: 흰 카드만 화면 가운데 떠 있어서 모달인지 페이지 일부인지
  구분이 안 되는 상태였음(딤 배경이 처음부터 없었음) → 반투명 배경 추가 + 배경 클릭 시 닫힘 +
  `Portal` 적용
- `RegisterPromptModal` 도 `Portal` 적용 — 어디서 호출되든 층위 문제가 생기지 않도록

## 2026-08-25 (UX 재설계)

### A단계: 타이포 스케일 상향 + 터치 영역 확대
- **실측 진단**: 3개 주요 화면의 텍스트 642개를 측정한 결과
  - 전체의 **65%가 13px 미만**, 가장 많이 쓰인 크기가 11px(248곳), 7px도 20곳 존재
  - 터치 대상은 내냉장고 143개 중 **134개가 44px 미만**(Apple HIG / Material 권장 최소 미달)
  - "글씨가 작다 / 조작 영역이 좁다 / 옛날 앱 같다"는 체감의 직접 원인이었음
- 타이포 스케일 7단계 정의(`--text-caption 12` ~ `--text-2xl 26`, 본문 15px 기준)하고
  `tailwind.config.js` 의 `fontSize` 도 한 단계씩 상향(xs 12→13, sm 14→15, base 16)
- 인라인 `fontSize` 와 Tailwind `text-[Npx]` **214곳을 스케일에 맞춰 상향**
  - 결과: 13px 미만 텍스트 **65% → 3%**, 7px/10px 등 판독 곤란한 크기 소멸
  - 크기 종류도 11종 → 8종(12/13/15/16/18/22/26)으로 정리됨
- 재료 pill 을 11px/높이 22 → 13px/높이 26 으로, 정렬·필터 바 컨트롤을 높이 28 → 40 으로 확대
- 작은 아이콘 버튼용 `.tap-44` 유틸 추가 — 보이는 크기는 그대로 두고 투명한 히트 영역만
  44px로 넓혀 레이아웃 변화 없이 터치 정확도를 올리는 방식

### B단계: 공통 컴포넌트(프리미티브) 추출
- 버튼이 페이지마다 인라인으로 각각 작성돼 있었음(MyPage 18개, RecipeSortBar 10개,
  RecipeChatWidget 9개 …). 높이·색·radius가 제각각이고 28~30px 짜리가 많아 터치가 어려웠음
- `src/components/ui/` 신설 — C단계(화면 재설계)가 이 위에서 이뤄진다
  - `Button` : primary / secondary / outline / ghost / danger × sm / md / lg.
    **md·lg 는 항상 44px 이상**이라 터치 규격이 자동으로 지켜짐. 누를 때 축소 피드백 포함
  - `Chip` : 상태 없는 태그·필터 칩 (선택 상태 지원).
    재료 pill 은 보유/부족/대체 상태가 있어 `styles/ingredientPill.ts` 를 계속 사용
  - `Input` : 높이 44px, 테두리 + 브랜드 컬러 포커스 링.
    글자를 16px 로 고정 — iOS 는 16px 미만 입력창에 포커스하면 화면을 자동 확대해버림
  - `Sheet` : 바텀시트. 화면 가운데 모달은 한 손 조작 시 손이 닿기 어렵고 내용이 길면 잘림 →
    아래에서 올라와 엄지 근처에 액션이 오도록. Portal + 뒤 스크롤 잠금 + 손잡이 포함
- 기존 컴포넌트를 새 프리미티브에 위임해 실제로 쓰이게 연결
  - `PrimaryButton` → `ui/Button`
  - `TagPill` → `ui/Chip` (예전엔 폰트를 'Noto Sans KR' 로 따로 지정해 앱 전체 톤과 달랐음)
- `prefers-reduced-motion` 대응 추가 — 움직임 최소화 설정을 켠 사용자에겐 애니메이션을 줄임

### C단계(1차): 재료 pill을 화면의 주인공으로
- **방향 정정**: 처음엔 카드 정보가 많으니 보유 재료 pill을 빼자고 제안했으나,
  이 앱의 핵심 가치가 "보유 재료로 효율적으로 냉털이 + 부족하면 대체제 추천" 이라는 점을
  확인. pill 은 장식이 아니라 **답 그 자체**이므로 빼는 게 아니라 더 잘 읽히게 하는 방향으로 전환
- **pill 가로 스크롤 → 줄바꿈**: 기존엔 `flexWrap: nowrap` + `overflowX: auto` 라
  카드마다 재료가 잘려 있었고, 몇 개가 부족한지 보려면 카드마다 옆으로 밀어야 했음.
  핵심 정보가 스크롤 뒤에 숨어 있던 셈 → 줄바꿈으로 한눈에 보이게 하고,
  8개를 넘으면 점선 `+N` 칩으로 접음(탭하면 펼침).
  `pills` 배열이 이미 [부족 → 대체가능 → 보유] 순이라 접혀도 부족한 재료가 먼저 보임
- **매칭률 뱃지를 행동 가능한 문구로**: "재료 매칭률 83%" → **"1개만 더 · 83%"**,
  부족한 재료가 없으면 **"바로 만들 수 있어요"**(브랜드 옐로우). 비율은 추상적이라
  "지금 만들 수 있나 / 몇 개를 사야 하나"를 판단하려면 한 번 더 계산이 필요했음.
  개수는 이미 계산돼 있던 `lackingIngredients`(대체 가능한 재료 제외) 를 사용
- 대체 가능 줄을 어두운 라벨 + 나열에서, 옅은 배경 칩 형태로 정리
