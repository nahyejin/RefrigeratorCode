# CHANGELOG

쿡매치(CookMatch) 프로젝트 변경 이력. Cursor AI에서 Claude Code로 작업 환경을 이관한 2026-08-24부터 기록.

## 2026-08-24

### 프로젝트 구조 정리
- 옛 CRA 프론트엔드 잔재 삭제 (루트 `src/`, `package.json`, `vercel.json`, `main.py`) — 실제 배포는 `frontend/`에서 진행되므로 영향 없음
- `backend/App.tsx`, 루트 `check_db.py`, 중복 `export_recipes_all.ipynb` 등 죽은 코드/중복 파일 삭제
- 로컬 SQLite 흔적(`posts.db`, `recipes.db`, `refrigerator.db`) 삭제 — 현재는 MySQL(Railway) 사용
- `crawler.log`, `chromedriver-win64/`, `__pycache__/` 전체를 git 추적 해제 (로컬엔 유지, `.gitignore` 반영)
- 루트에 흩어진 디버그/점검용 스크립트 18개를 `scripts/`로 이동
- `utils/requirements.txt.txt`(오타성 파일명)를 `ingredient_management/requirements.txt`로 이동

### 요리 챗봇 기능 추가
- `backend/chat_service.py`: Gemini/Groq LLM으로 대화 의도를 파악해 DB에서 레시피를 검색하는 `/api/chat` 엔드포인트 신설. 일일 호출 한도(기본 250회) 적용
- 프론트에 전역 플로팅 챗 위젯(`RecipeChatWidget.tsx`) 추가 — 냉장고 재료(localStorage) + 대화 기록을 세션 단위로 유지하며 추천 레시피 카드 표시
- Gemini 기본 모델을 `gemini-2.0-flash`(단종됨) → `gemini-3.5-flash-lite`로 교체
- 레시피 썸네일에 기존 `getProxiedImageUrl` 유틸 적용 — 네이버 이미지 핫링크 차단(403) 우회
- 로컬 개발 CORS 이슈 해결 (프론트 dev 서버 포트를 화이트리스트에 있는 5178로 통일)

### 챗봇 FAB를 AI 기능처럼 리디자인
- 텍스트만 있던 "챗" 버튼을 스파클 아이콘 + 회전하는 그라디언트 링 + 펄스 글로우 + "AI" 배지로 교체
- 채팅창 헤더와 어시스턴트 말풍선에도 같은 스파클 아바타 적용해 AI 브랜딩 통일

### 챗봇 대화 히스토리 관리 + 재료 매칭 우선 검색으로 재설계
- `RecipeChatWidget.tsx`: 채팅창을 닫았다 다시 열면 항상 새 대화로 시작하도록 변경. 지난 대화는 localStorage에 스레드 단위로 저장(최근 30일 보관)되고, 헤더의 "지난 대화" 버튼으로 목록을 열어 이어보거나 삭제 가능
- `chat_service.py`: 기존엔 LLM이 뽑은 키워드가 title/content LIKE **강제 필터**라서, 냉장고 재료와 매칭률이 높은 레시피도 키워드가 안 맞으면 통째로 걸러졌음. 이제 냉장고 재료가 있으면 키워드는 `HAVING` 절의 소프트 조건(키워드 매칭 OR 매칭률 25% 이상)으로 바뀌어, 매칭률 높은 레시피가 우선 노출됨
- LLM 프롬프트에 `ignore_fridge` 필드 추가: 사용자가 "재료 상관없이" 등으로 명시적으로 요청할 때만 냉장고 매칭을 끄고 순수 키워드 검색으로 전환
- 레시피 카드의 "재료 매칭률 N%"를 일반 텍스트에서, 기존 `RecipeCard.tsx`와 동일한 톤(짙은 배지 + 노란색 굵은 글씨)의 배지로 변경

### 재료 추출 파이프라인을 LLM 기반으로 재설계 (진행 중)
- `ingredient_management/llm_ingredient_extraction.py` 신설: Gemini로 본문에서 실제 사용 재료를 "이해"해서 뽑아내고,
  정규화는 100% 기존 사전(`ingredient_profile_dict_with_substitutes.csv`)의 결정론적 매칭으로만 처리
  (LLM 역할을 추출까지만 최소화, 매칭은 LLM 관여 없음). 출력 포맷은 기존과 동일(콤마구분/공백없음/정렬)하게 유지해
  프론트 pill 매칭·백엔드 매칭률 SQL을 전혀 건드리지 않음
- 사전에 없는 LLM 추출 후보는 버리되 `unmapped_ingredients` 컬럼에 기록 (추후 사전 보강용)
- 20건 샘플 검증 결과, 기존 룰베이스의 실제 버그들(예: "청양고추"를 "고추"+"청양고추"로 중복 집계, "만두"/"쌈무" 같은 요리명이 재료로 오인식, 블록 탐색 범위가 좁아 뒷부분 재료 누락)이 다수 수정됨을 확인
- 전체 44,121건에 대해 `--commit` 모드로 백그라운드 배치 실행 중 (모델: gemini-3.5-flash-lite, 분당 140건, 동시 7개)
- **버그 수정**: 최초 버전은 전체 결과를 다 모은 뒤에야 CSV/DB에 쓰는 구조라 중간에 죽으면 진행 상황이 전부 유실되는
  문제가 있었음 → 결과가 나오는 즉시 CSV에 flush, 30건마다 DB 커밋하도록 수정 후 재시작
- **실측 결과**: 이 API 키는 무료 티어라 `gemini-3.5-flash-lite`가 분당 15건으로 하드 제한됨. 분당 140건으로
  돌렸다가 에러율 27%를 확인하고 분당 13건(안전 마진)으로 재시작. 44,121건 전체 기준 약 2~2.5일 소요 예상

### git 추적 충돌 발견 및 정리
- 세션 도중 내가 만들지 않은 커밋 2개(`대거변동 260825`, `업데이트 20260825_12:37`)가 발견됨 — 다른 도구(Cursor 등
  자동 커밋으로 추정)가 같은 저장소에 동시에 작업 중인 것으로 보임. 이 커밋들이 `.gitignore`를 이전 버전으로
  되돌리고 pycache/chromedriver/crawler.log를 다시 추적시킴 → 재정리하고 배치 산출물 CSV도 gitignore에 추가
- **주의**: 다른 자동 커밋 도구가 계속 켜져 있으면 다시 충돌할 수 있음. Cursor 등 확인 필요

### 챗봇 UI 세부 디자인 개선
- FAB/헤더/말풍선의 스파클 아이콘 → 김이 나는 냄비 아이콘으로 교체 (SVG가 flex 컨테이너 안에서
  width 0으로 찌그러지던 렌더링 버그도 같이 수정)
- 지난 대화·새 대화 버튼을 아이콘 버튼(38px)으로 확대, 새 대화는 + 아이콘으로 통일
- 안내 문구를 기능 설명 → 사용자 편익 중심으로 변경, placeholder도 "맛"에서 "먹고 싶은 걸"로 범위 확장
- 채팅창 타이틀 강조(굵게/크게), 창 높이 70vh/560px → 84vh/700px로 확대
- GitHub main에 푸시 완료 (Vercel 자동 배포 트리거됨)

### 배포 환경변수 설정
- Railway 백엔드에 `GEMINI_API_KEY` 환경변수 추가 완료 (운영 환경 챗봇 활성화)

### 크롤러 자동화 안정화 (Task Scheduler 등록 준비)
- `run_all_crawlers.py`가 계속 실패하던 원인 파악: ChromeDriver 버전 자동 감지가
  PowerShell/wmic 서브프로세스에 의존하는데, 실패해도 감지된 버전을 실제로 쓰지
  않는 버그가 있어 최신 Chrome보다 오래된/새로운 ChromeDriver를 받아와 세션 생성이
  계속 실패했음. 레지스트리 기반 감지로 교체하고 감지된 버전을 실제로 전달하도록 수정
- 크롤러 파이프라인에 LLM 일일 증분 추출 단계 연결 (룰베이스 폴백 유지 + 무료 한도 내 LLM 보정)
- 의존성 누락(dnspython, tqdm), 미사용 import(mysql.connector) 정리
- 매일 오전 7시 자동 실행할 `run_crawlers_scheduled.bat` 준비 (Windows 작업 스케줄러 등록 예정)

### LLM 배치 처리 + 하루 500건 한도 대응
- `llm_ingredient_extraction.py`에 `--batch-size` 옵션 추가: 레시피 여러 건을 호출
  1번에 묶어 처리 (기본 8건) → 일일 호출 횟수 한도를 훨씬 적게 사용, 챗봇과 공존 가능
- `--pending-only` 옵션 추가: DB의 `llm_ingredients_done` 플래그로 "이미 처리됨"과
  "신규/미처리"를 구분해, 크롤러가 매일 신규 수집분만 무료 한도 내에서 처리하고
  오늘 못 채운 건 자동으로 다음날로 넘어가도록 함
- 44,121건 기존 백로그는 유료 티어로 별도 처리 예정 (실측 토큰 기반 예상 비용 약 9천~1만 8천원)

### UI 디자인 정리
- 실제 화면 4곳(내 냉장고/냉장고 요리/요즘 인기/마이페이지)을 스크린샷으로 검토해
  아쉬운 지점 정리 후 우선순위대로 수정:
  - AI FAB와 PWA 설치 유도 배너가 z-index 문제로 겹쳐 보이던 것 수정
  - 내 냉장고 재료 태그가 진회색+흰 글씨로 뭉쳐 보이던 것을 연회색+진회색으로 가볍게 변경
  - "부족 재료" pill이 연회색 배경에 흰 글씨라 실질적으로 안 보이던 대비 문제 수정
    (앱 전역 10곳의 범례도 함께 정리)
  - 냉장고요리 페이지 필터 버튼 4개가 전부 같은 톤이라 위계가 없던 것을 아이콘+진한
    "필터" 버튼으로 구분
  - 스플래시 화면을 베이지/일러스트 톤에서 실제 CookMatch 아이콘 기반 화이트/블랙/
    옐로우 톤으로 재구성 (본편과 첫인상 통일)

### 무료 티어 실측: 하루 500건이 진짜 병목
- 분당 13건(안전 마진)으로도 대량 에러 발생 → 원인은 분당 제한이 아니라
  `GenerateRequestsPerDayPerProjectPerModel-FreeTier` **하루 500건** 한도였음
- 44,121건 ÷ 500건/일 ≈ 88일. 게다가 운영 챗봇도 같은 키의 같은 일일 한도를 공유해서
  무료 티어로는 배치와 실사용자 챗봇을 동시에 운영할 수 없음 → 유료 티어 전환 필요, 사용자 결정 대기 중
- 오늘 처리분: 948건 시도, 성공 160건(DB 반영), 나머지는 일일 한도 초과로 실패

### 데이터 안전장치 추가 (중요)
- **버그 발견**: LLM이 빈 배열을 반환하면(파싱 실패/모델 이상 응답 등) 기존에 있던 재료 목록을
  조용히 빈 값으로 덮어써버리는 문제가 있었음. 160건 중 2건에서 실제 발생(id=1326 '초계라면'은
  진짜 레시피인데 빈 값으로 덮어써짐 → 기존 값으로 복구 완료. id=1295는 레시피가 아닌 홍보 영상이라
  원래도 룰베이스가 오탐한 것이었고 빈 값이 맞아서 그대로 둠)
- **수정**: 기존 `used_ingredients`가 있는데 LLM 결과가 비어 있으면 DB에 반영하지 않고 CSV에만
  `NEEDS_REVIEW`로 표시 (DB에는 절대 안 들어가므로 사용자에게 노출되는 값은 항상 기존 값 그대로 유지됨)

### 챗봇 FAB 로고/타이틀 변경
- FAB/헤더/말풍선 아이콘을 냄비 모티프에서 쿡매치 메인 아이콘(집 지붕 + 밥그릇) 모티프로 교체
- 타이틀 "AI 요리 챗" → "쿡매치 AI" (챗봇 느낌을 줄이고 브랜드를 앞세움)

### 레시피 아닌 본문 자동 삭제 로직 추가
- 룰베이스(old)와 LLM(new)이 둘 다 "재료 없음"에 동의하는 행만 레시피가 아닌 것으로 보고
  `recipes` 테이블에서 행 자체를 삭제 (예: 홍보 영상, 안내 영상 등)
- old에는 재료가 있었는데 new만 비어 있는 경우(불일치)는 삭제하지 않음 — LLM 쪽의 일시적
  오류일 수 있어 위 데이터 안전장치와 동일하게 NEEDS_REVIEW로만 표시하고 기존 값 보존
- 수동 확인한 사례 2건 처리: id=1326(진짜 레시피, 오류로 비워졌던 것)은 기존 값 복구,
  id=1295(레시피 아닌 통영 축제 홍보 영상)는 행 자체 삭제
- `ingredient_management/llm_extracted_158_review.csv`: 성공 처리된 158건의 링크 +
  LLM 추출 재료(원본/정규화 후) 검토용 파일 생성

## 2026-08-25

### 스티키 필터 영역 (냉장고 요리 페이지)
- `RecipeList.tsx`: 검색창 + 정렬/필터 바 + 재료 pill 범례("부족 재료 ... 총 000건")를
  스크롤해도 상단에 고정되도록 sticky 처리 (top: 56px, GNB 바로 아래)
- 범례가 화면 밖으로 사라져서 pill 색상의 의미를 잊게 되는 문제 해결

### 챗 패널 모바일 키보드 대응
- `RecipeChatWidget.tsx`: `visualViewport` API로 키보드 높이를 감지해 패널 위치/높이를 재계산.
  키보드가 올라와도 패널 상단이 잘리지 않음
- 메시지 목록/지난 대화 목록에 `minHeight: 0` 추가 — flex 자식이라 스크롤이 안 걸리던 문제 해결
- 패널 타이틀 글꼴/굵기를 앱 공통 톤(18px / 700)에 맞춤

### 내 정보 수정 진입 경로 + 닉네임 마스킹 문제
- `TopNavBar.tsx`: GNB의 닉네임을 버튼으로 바꿔 클릭 시 `/my-page?openEdit=true`로 이동
- `MyPage.tsx`: `openEdit=true` 쿼리 파라미터를 감지해 "내 정보 수정" 모달 자동 오픈
- 버튼 문구 "적용" → "변경 적용"
- **닉네임이 `9208****`로 보이던 원인**: 프론트/백엔드 어디에도 마스킹 코드는 없었고,
  네이버 로그인 API(`/v1/nid/me`)가 별명(nickname)을 마스킹된 형태로 내려주는데 그 값을
  그대로 저장한 것이었음 (DB의 users.nickname 실제 값 자체가 `9208****`)
- `app.py` 네이버 콜백 수정: 닉네임이 비어 있거나 `*`를 포함하면 이메일 로컬 파트를 대신 사용
  → 이후 신규 네이버 가입자는 마스킹된 닉네임이 저장되지 않음
- 기존에 저장된 값은 "내 정보 수정"에서 직접 변경 필요

### Sticky 영역 여백 + 다른 목록 페이지에도 동일 적용
- `RecipeList.tsx`: sticky 영역에 `paddingBottom: 10` 추가. 기존엔 범례의 `marginBottom`이
  margin collapsing으로 sticky 컨테이너 밖으로 빠져나가서, 스크롤 시 카드가 "부족 재료 / 대체 가능 /
  보유 재료" 글자에 바짝 붙어 지나갔음. 이제 범례 아래 흰 여백이 항상 유지됨
- 같은 형식의 범례를 쓰는 나머지 목록 페이지에도 동일하게 sticky 적용
  (정렬/필터 바 + 범례가 함께 상단 고정, top: 56px)
  - `IngredientDetail.tsx` — 요즘인기 페이지 하단 검색 → `/ingredient/:name`으로 이동하는 페이지
  - `RecordedRecipeListPage.tsx` — 내가 기록한 레시피
  - `CompletedRecipeListPage.tsx` — 내가 완료한 레시피
- `Popular.tsx`와 `MyPage.tsx`의 범례는 제외 — 이쪽은 섹션(유튜브 인기 / 네이버 인기 / 특별한 날 /
  즐겨찾기 / 기록 / 완료)마다 각각 붙어 있는 가로 스크롤 캐러셀 헤더라, sticky로 만들면 범례 여러 개가
  서로 밀어내며 겹침. 세로로 길게 스크롤되는 목록 페이지에만 적용하는 것이 맞음

### 챗봇 FAB를 노란 말풍선 버튼으로 리디자인
- 검정 원 + 쿡매치 마크(집 지붕+밥그릇) + 회전 그라디언트 링 조합이 무겁고 안 예쁘다는 피드백 반영
- 브랜드 옐로우(#FFD600) 원형 버튼 + 어두운 말풍선 아이콘(안쪽 점 3개는 노란색으로 뚫음)으로 교체
- 회전하는 conic-gradient 링 제거 (노란 버튼 위에서는 색이 뭉개짐). 은은한 노란 글로우 펄스는 유지
- "AI" 배지를 노란 배경/검정 글씨 → 검정 배경/노란 글씨로 반전 (노란 버튼 위 대비 확보)
- 누를 때 살짝 눌리는 `:active` 스케일 피드백 추가
- 아이콘 클래스를 FAB 전용(`.ai-fab-bubble`)으로 분리 — 패널 헤더/말풍선 아바타의 쿡매치 마크는
  기존 그대로(어두운 원 + 노란 마크) 유지되도록 함

### 챗 패널 아바타/헤더 정리 + FAB 애니메이션 복원
- 패널 헤더 아바타와 답변 말풍선 아바타를 FAB와 동일한 디자인(노란 원 + 어두운 말풍선)으로 통일.
  기존의 검정 원 + 쿡매치 마크(집 지붕+밥그릇) 조합은 FAB와 따로 놀아서 제거
- 사용하지 않게 된 `CookMatchMarkIcon` 컴포넌트와 관련 CSS 삭제
- 헤더의 부제 "오늘 뭐 먹을지, 냉장고 재료로 같이 찾아볼게요 🍳" 제거.
  좁은 화면(앱)에서 말줄임으로 잘리는 데다, 본문 첫 안내 문구
  "뭐 먹을지 고민될 때, 냉장고 재료로 딱 맞는 레시피를 찾아드릴게요."와 내용이 중복됨
- FAB 애니메이션 복원/강화: 회전 링을 뺀 대신
  - 노란 그라데이션이 버튼 위를 천천히 흐르는 `ai-fab-shimmer` (4.2s 무한 반복)
  - 주기적으로 스쳐 지나가는 하이라이트 `ai-fab-sheen`
  - 기존 노란 글로우 펄스 유지
  - 말풍선 아이콘은 하이라이트 위에 올려 흐려지지 않게 처리

### 챗봇 예시 질문 확대 + 입력창 UI 개선
- 첫 진입 시 보여주는 예시 질문을 3개 → 8개로 확대. 맛/냉장고 위주였던 걸
  대상·목적·상황까지 넓힘
  - 기존: 매운 거 / 있는 재료로 / 재료 상관없이 간단한 거
  - 추가: 아이가 잘 먹는 반찬 / 다이어트 중인데 뭐 먹지 / 캠핑 가서 해먹기 좋은 요리 /
    술안주 / 15분이면 되는 자취 요리
- 입력창이 흰 배경에 연회색(gray-100)이라 입력 가능한 영역인지 구분이 안 된다는 피드백 반영
  - 흰 배경 + 진한 테두리(#D8D8DE) + 라운드 필 형태로 변경해 필드 경계를 명확히 함
  - 포커스 시 브랜드 옐로우 테두리 + 글로우 링 표시
  - 입력 영역 전체 배경을 살짝 톤다운(#FAFAFB)해 대화 영역과 시각적으로 분리
  - "보내기" 버튼을 검정 → 브랜드 옐로우로 변경 (비활성 시 회색), 높이를 입력창과 40px로 맞춤

### 챗 패널 스크롤 문제 해결
- 측정해보니 스크롤 자체는 동작하고 있었음(scrollHeight 760 > clientHeight 512). 문제는 세 가지였음
- **뒤 페이지로 스크롤 전파**: 패널 안을 드래그하다 끝에 닿으면 뒤 페이지가 대신 밀려서
  "패널이 안 움직인다"고 느껴짐 → 스크롤 영역에 `overscroll-behavior: contain` 적용
- **뒤 페이지 스크롤 미잠금**: 패널이 열려 있는 동안 `document.body`를 `overflow: hidden`으로 잠그고
  닫힐 때 원복
- **답변 도착 시 맨 아래로 점프**: 긴 답변이 오면 곧장 최하단으로 스크롤해서 답변 앞부분이 안 보였고,
  사용자 입장에선 내용이 잘려 보임 → 이제 '마지막 질문'이 상단에 오도록 스크롤해서
  답변을 처음부터 읽을 수 있고, 아래에 더 있다는 것도 카드가 걸쳐 보여서 인지됨
- 스크롤바를 얇게(6px) 보이도록 스타일링해 스크롤 가능 영역임을 시각적으로 표시

## 2026-08-25 (UI 리팩터링)

### 1단계: 죽은 CSS 정리 + 촌스러움의 실제 원인 2건 제거
- **정정**: "폰트가 맑은고딕으로 떨어진다"는 최초 진단은 틀렸음. 브라우저 실측 결과
  body/h2/button/input 모두 Pretendard로 정상 렌더되고 있었음. 맑은고딕 규칙은
  `pill-label`/`tag-label`/`filter-pill`/`small-pill` 등 **사용처가 0인 클래스**만
  겨냥하고 있어 한 번도 적용된 적이 없었음
- **모든 글자의 흰색 윤곽선 제거** (진짜 원인 ①): `body`에 `text-shadow: 0 0 1px #fff`가
  4겹 걸려 있어 앱 전체 글자가 뿌옇게 번져 보였음. 이 규칙을 되돌리려고 코드 곳곳에
  `textShadow: 'none'` 인라인 우회가 22군데나 생겨 있었음 → 원인을 제거하고 우회 22곳도 함께 삭제
- **페이지 스크롤바를 보이게 변경** (진짜 원인 ②): `html/body/#root`의 스크롤바 thumb이
  `background: transparent`로 되어 있어 스크롤바가 아예 안 보였음. "스크롤이 자꾸 안 보인다"는
  체감의 직접 원인 → 은은하지만 보이는 thumb(검정 18%, hover 30%)으로 변경, Firefox용
  `scrollbar-width/color`도 추가
- **죽은 CSS 규칙 약 80줄 삭제**: `.text-[11px]`, `.bg-[#D1D1D1]`처럼 대괄호를
  이스케이프하지 않은 셀렉터가 목록에 섞이면 CSS 규칙 **전체가 무효**가 됨.
  pill 폰트 강제 블록(Noto Sans KR / font-weight 200 / 12~14px !important)이 전부 이 경우라
  적용된 적 없는 죽은 코드였음. 남겨두면 나중에 해당 클래스를 쓰는 순간 예상 못한 스타일이
  튀어나오는 지뢰라 제거

### 2단계: 디자인 토큰 도입 + 재료 pill/범례 단일화
- **수치 정정**: 앞서 "고유 색상 252종"이라고 했던 건 SVG 에셋 파일(`share_black.svg` 등)이
  섞여 부풀려진 값이었음. 실제 코드(tsx/ts/css) 기준 정확한 수치는 **92종**
- `src/index.css`의 `:root`에 디자인 토큰 정의 (ink 5단계 / line 2단계 / surface 2단계 /
  브랜드 5종 / 상태 3종 / z-index 6단계). `tailwind.config.js`에도 같은 팔레트를 반영해
  `text-ink-500`, `bg-brand-soft` 같은 클래스로도 쓸 수 있게 함
- 흩어진 색상 값 **506곳을 팔레트로 정규화** → 고유 색상 **92종 → 42종**.
  (`#222`/`#111`/`#1A1A1A` → `--ink-900`, `#E5E7EB`/`#E5E5E5` → `--line-200` 등.
  브랜드·플랫폼(네이버 초록/유튜브 빨강)·상태·캘린더 색은 의미가 있어 병합하지 않음)
- **쿠팡 광고 버튼 톤다운**: 채도 높은 주황 꽉 찬 버튼이라 카드에서 가장 눈에 띄었고,
  정작 주인공인 레시피보다 광고가 강조돼 있었음 → 흰 배경 + 옅은 주황 테두리 +
  주황 글씨의 외곽선 스타일로 변경
- **재료 pill 원색 노랑 제거**: 카드마다 보유 재료 pill이 4~6개씩 원색 노랑(#FFD600)으로
  꽉 차 있어 화면이 노란 덩어리가 되고, 브랜드 컬러가 "데이터 상태" 표시에 소모되고 있었음
  → 연한 노랑 배경(#FFF1B8) + 진한 노랑 글자(#6B5200)로 낮춤.
  부족 재료는 더 옅게(#F0F0F3) 물러나게 하고, 드물게 나오는 대체 가능만 진한 색 유지
- **pill 색과 범례를 단일 소스로 통합**: `src/styles/ingredientPill.ts` 신설.
  기존엔 범례 마크업이 6개 파일에 10곳 복붙돼 있어 pill 색을 바꿔도 범례는 안 따라오는
  구조였음 → `IngredientLegend` 컴포넌트로 추출해 10곳 모두 교체.
  이제 색을 바꾸려면 `ingredientPill.ts` 한 파일만 고치면 됨

### 3단계: z-index 체계화 + 모달 스택 맥락 탈출(Portal)
- z-index 값이 20종(1 ~ 10060)이 규칙 없이 흩어져 있었음. 새 요소를 넣을 때마다 숫자를
  키우는 식(9999 → 10001 → 10030 → 10060)으로 대응해 온 흔적 → **역할 기반 7단계**로 정리
  (`--z-sticky 100` / `--z-nav 200` / `--z-dropdown 300` / `--z-fab 400` /
  `--z-overlay 500` / `--z-modal 600` / `--z-toast 700`). 54곳 치환.
  카드 안 아이콘 같은 지역 스택(1~30)은 의미가 다르므로 그대로 둠
- **모달이 헤더/하단 네비 아래에 깔리던 버그 수정** (히트 테스트로 발견):
  필터 모달이 `position: sticky` 인 필터바 **안에서** 렌더되고 있었음. sticky는 새 스택 맥락을
  만들기 때문에, 모달의 z-index를 아무리 올려도 그 조상의 층위(100) 안에서만 높을 뿐
  바깥의 헤더·네비(200)보다 위로 갈 수 없었음
  → `Portal` 컴포넌트 신설(`createPortal`로 `document.body` 직속 렌더)하고
  FilterModal / IngredientDetailModal / RecipeSortBar의 매칭도·임박재료 모달에 적용.
  수정 후 히트 테스트로 헤더·네비·FAB 위치 모두 모달 딤이 덮는 것 확인

### 4단계: 남은 중복 제거
- **런타임 `<style>` 주입 제거**: 로딩 점 애니메이션 CSS가 5개 페이지 파일에 각각 복붙되어 있고,
  모듈 최상위에서 `document.createElement('style')` 로 `<head>` 에 주입하고 있었음
  (페이지를 옮겨 다니면 동일한 `<style>` 태그가 계속 쌓임) → `index.css` 로 옮기고 주입 코드 삭제.
  점 색상도 `var(--brand)` 로, z-index 도 `var(--z-overlay)` 로 토큰화
- **Toast / RecipeToast 통합**: 두 컴포넌트가 거의 같은 스타일 정의를 각자 들고 있었음
  → `RecipeToast` 가 `Toast` 에 위임하도록 변경(여러 줄 표시 옵션만 다름)
- `Toast` 를 `Portal` 로 감쌈 — sticky/transform 조상 안에서 호출돼도 층위에 갇히지 않도록
- 페이지에 인라인으로 복붙돼 있던 토스트 마크업 중 형태가 동일한 2곳을 `<Toast />` 로 교체

### 5단계: 화면 정리
- **`WelcomeModal` 에 딤 배경 추가**: 흰 카드만 화면 가운데 떠 있어서 모달인지 페이지 일부인지
  구분이 안 되는 상태였음(딤 배경이 처음부터 없었음) → 반투명 배경 추가 + 배경 클릭 시 닫힘 +
  `Portal` 적용
- `RegisterPromptModal` 도 `Portal` 적용 — 어디서 호출되든 층위 문제가 생기지 않도록

## 2026-08-25 (UX 재설계)

### A단계: 타이포 스케일 상향 + 터치 영역 확대
- **실측 진단**: 3개 주요 화면의 텍스트 642개를 측정한 결과
  - 전체의 **65%가 13px 미만**, 가장 많이 쓰인 크기가 11px(248곳), 7px도 20곳 존재
  - 터치 대상은 내냉장고 143개 중 **134개가 44px 미만**(Apple HIG / Material 권장 최소 미달)
  - "글씨가 작다 / 조작 영역이 좁다 / 옛날 앱 같다"는 체감의 직접 원인이었음
- 타이포 스케일 7단계 정의(`--text-caption 12` ~ `--text-2xl 26`, 본문 15px 기준)하고
  `tailwind.config.js` 의 `fontSize` 도 한 단계씩 상향(xs 12→13, sm 14→15, base 16)
- 인라인 `fontSize` 와 Tailwind `text-[Npx]` **214곳을 스케일에 맞춰 상향**
  - 결과: 13px 미만 텍스트 **65% → 3%**, 7px/10px 등 판독 곤란한 크기 소멸
  - 크기 종류도 11종 → 8종(12/13/15/16/18/22/26)으로 정리됨
- 재료 pill 을 11px/높이 22 → 13px/높이 26 으로, 정렬·필터 바 컨트롤을 높이 28 → 40 으로 확대
- 작은 아이콘 버튼용 `.tap-44` 유틸 추가 — 보이는 크기는 그대로 두고 투명한 히트 영역만
  44px로 넓혀 레이아웃 변화 없이 터치 정확도를 올리는 방식

### B단계: 공통 컴포넌트(프리미티브) 추출
- 버튼이 페이지마다 인라인으로 각각 작성돼 있었음(MyPage 18개, RecipeSortBar 10개,
  RecipeChatWidget 9개 …). 높이·색·radius가 제각각이고 28~30px 짜리가 많아 터치가 어려웠음
- `src/components/ui/` 신설 — C단계(화면 재설계)가 이 위에서 이뤄진다
  - `Button` : primary / secondary / outline / ghost / danger × sm / md / lg.
    **md·lg 는 항상 44px 이상**이라 터치 규격이 자동으로 지켜짐. 누를 때 축소 피드백 포함
  - `Chip` : 상태 없는 태그·필터 칩 (선택 상태 지원).
    재료 pill 은 보유/부족/대체 상태가 있어 `styles/ingredientPill.ts` 를 계속 사용
  - `Input` : 높이 44px, 테두리 + 브랜드 컬러 포커스 링.
    글자를 16px 로 고정 — iOS 는 16px 미만 입력창에 포커스하면 화면을 자동 확대해버림
  - `Sheet` : 바텀시트. 화면 가운데 모달은 한 손 조작 시 손이 닿기 어렵고 내용이 길면 잘림 →
    아래에서 올라와 엄지 근처에 액션이 오도록. Portal + 뒤 스크롤 잠금 + 손잡이 포함
- 기존 컴포넌트를 새 프리미티브에 위임해 실제로 쓰이게 연결
  - `PrimaryButton` → `ui/Button`
  - `TagPill` → `ui/Chip` (예전엔 폰트를 'Noto Sans KR' 로 따로 지정해 앱 전체 톤과 달랐음)
- `prefers-reduced-motion` 대응 추가 — 움직임 최소화 설정을 켠 사용자에겐 애니메이션을 줄임

### C단계(1차): 재료 pill을 화면의 주인공으로
- **방향 정정**: 처음엔 카드 정보가 많으니 보유 재료 pill을 빼자고 제안했으나,
  이 앱의 핵심 가치가 "보유 재료로 효율적으로 냉털이 + 부족하면 대체제 추천" 이라는 점을
  확인. pill 은 장식이 아니라 **답 그 자체**이므로 빼는 게 아니라 더 잘 읽히게 하는 방향으로 전환
- **pill 가로 스크롤 → 줄바꿈**: 기존엔 `flexWrap: nowrap` + `overflowX: auto` 라
  카드마다 재료가 잘려 있었고, 몇 개가 부족한지 보려면 카드마다 옆으로 밀어야 했음.
  핵심 정보가 스크롤 뒤에 숨어 있던 셈 → 줄바꿈으로 한눈에 보이게 하고,
  8개를 넘으면 점선 `+N` 칩으로 접음(탭하면 펼침).
  `pills` 배열이 이미 [부족 → 대체가능 → 보유] 순이라 접혀도 부족한 재료가 먼저 보임
- **매칭률 뱃지**: 한때 "1개만 더" 를 앞세우고 매칭률을 보조로 내렸다가 되돌림.
  재료 매칭률은 정렬 기준(재료매칭률순)이자 필터 기준(매칭도 30~100%)인 이 서비스의
  핵심 지표라 앞세우는 것이 맞음 → **"재료 매칭률 83% · 1개 부족"** 형태로,
  매칭률을 16px 굵게 강조하고 부족 개수를 보조로 덧붙임.
  부족 개수는 이미 계산돼 있던 `lackingIngredients`(대체 가능한 재료 제외) 를 사용
- 대체 가능 줄을 어두운 라벨 + 나열에서, 옅은 배경 칩 형태로 정리

### C단계(2차): 부족 재료를 눈에 띄게
- 부족 재료 pill이 가장 옅은 회색 채움이라 배경으로 물러나 있었음. 냉털이 판단에서
  실제로 행동을 부르는 건 부족한 쪽이므로 눈에 띄어야 한다는 판단
- 부족 재료를 **흰 배경 + 점선 테두리**로 변경 — "채워야 할 빈 칸"으로 읽히게.
  새 색을 추가하지 않고 테두리 형태만으로 구분해 팔레트가 늘어나지 않음
  - 보유 = 연노랑 채움(채워짐) / 부족 = 점선 빈 칸(채워야 함) / 대체 가능 = 진한 채움(드묾)
- 글자색도 #8A8A93 → #5A5A63 으로 올려 가독성 확보
- `PILL_COLORS` 에 `borderStyle` 을 추가하고 pill·범례가 모두 이를 따르게 함 →
  범례 견본도 자동으로 점선으로 바뀜 (단일 소스의 이점)

### C단계(3차): 필터 바텀시트 전환 + 카드 액션/광고 재배치
- **필터를 바텀시트로**: 화면 가운데 뜨는 모달이라 한 손 조작 시 상단 항목에 엄지가 닿지 않고
  내용이 길면 위아래가 잘렸음 → B단계에서 만든 `Sheet` 로 교체.
  적용 버튼이 엄지 근처로 내려오고, 손잡이·뒤 스크롤 잠금·슬라이드 업 애니메이션 포함
- **카드 액션 아이콘 재배치**: 즐겨찾기/완료/공유/기록 4개가 썸네일 위에 상시 노출돼
  음식 사진(레시피 선택의 주된 판단 근거)을 가리고 있었음. 4개 중 3개(완료·기록·공유)는
  요리한 *뒤* 에 쓰는 동작인데도 목록을 훑는 내내 떠 있던 것
  → 썸네일에는 **즐겨찾기 1개만** 남기고, 나머지는 좋아요/댓글 줄 오른쪽으로 이동.
  줄 높이를 그대로 써서 카드가 길어지지 않고 터치 영역은 36px 확보
- **쿠팡 구매 동선을 부족 재료 pill 로 이동**:
  - 기존엔 부족 재료가 여러 개여도 CTA 버튼은 **1개만 골라서** 보여줘서, 사용자가 사려는
    재료가 다르면 쓸모가 없었음. 게다가 모든 카드에 전체폭 버튼 + 2줄 고지문이 붙어
    20개 목록이면 광고 블록이 20개
  - 이제 점선(부족) pill 을 누르면 그 재료 구매로 연결(`+` 표시). 방금 부족 재료를
    "채워야 할 빈 칸"으로 만들었으므로 **"빈 칸을 누르면 채운다"** 는 동선이 자연스럽게 이어짐.
    사용자가 원하는 재료를 직접 고를 수 있고 클릭 의도도 높아짐
  - 링크 생성 로직을 `utils/coupangLink.ts` 로 분리 (기존엔 `CoupangProductAd` 안에만 있었음)
  - 별도 CTA 블록은 **부족 재료가 1~2개인 카드에만** 노출 — "하나만 사면 완성" 구간에서
    구매 전환이 실제로 일어나고, 5개 부족한 카드의 구매 버튼은 사실상 무의미하기 때문
- **미확인 사항**: 파트너스 고지문을 카드마다 반복하지 않고 목록 하단 1회로 줄이는 건
  쿠팡 파트너스 약관상 노출 요건 확인이 필요해 보류. 현재는 CTA 가 노출되는 카드에 함께 표시

### D단계: 모션 · 마감
- **로딩을 스켈레톤으로 교체** (`RecipeCardSkeleton` 신설, RecipeList / IngredientDetail 적용):
  예전엔 화면 한가운데 점 3개 스피너만 떠서 무엇이 로딩 중인지 알 수 없었고,
  로딩이 끝나는 순간 화면이 통째로 바뀌어 이동이 크게 느껴졌음
  → 실제 카드와 같은 모양(썸네일·제목·통계·pill)의 뼈대로 자리를 미리 잡아
  체감 대기시간을 줄이고 내용이 채워질 때 레이아웃이 흔들리지 않게 함.
  API 를 6초 지연시켜 로딩 상태를 잡은 뒤 스켈레톤 32개 렌더 확인
- **썸네일 페이드인 + `loading="lazy"`**: 이미지가 갑자기 튀어나오지 않고 부드럽게 나타나며,
  화면 밖 이미지는 늦게 불러와 초기 로딩 부담을 줄임
- **카드 누름 반응 추가**: 탭했을 때 아무 반응이 없으면 "안 눌렸나?" 하고 다시 누르게 됨
  → `:active` 에서 살짝 축소 + 투명도 변화. 기존의 hover translateY 는 모바일에서
  의미가 없어 제거하고 누름 반응으로 대체
- **접근성: 포커스 표시 복원**: 예전엔 `outline: none !important` 를 전역으로 걸어
  키보드 사용자가 현재 포커스 위치를 알 수 없었음 → `:focus-visible` 로 복원해
  마우스 클릭 시에는 안 보이고 키보드 이동 시에만 브랜드 컬러 아웃라인이 표시되도록 함

### 쿠팡 CTA 노출 기준 조정
- 부족 재료 1~2개 → **1~3개** 로 완화. 실제 목록에서 세어보니 1~2개 조건에서는
  해당하는 카드가 거의 없어 광고 노출이 과하게 줄어들었음
- (참고) 목록 하단으로 옮기자고 했던 것은 **광고가 아니라 고지 문구**임.
  고지문은 재료명이 들어가지 않는 일반 안내 문장이라 특정 재료와 매칭할 필요가 없음.
  구매 버튼과 pill 탭 구매 동선은 카드에 그대로 유지. 약관 확인 전까지는 현행 유지

### 쿠팡 클릭 측정 추가
- 광고 자리를 더 늘리기 전에 판단 근거부터 만들기로 함. 확인해보니 이 앱에는
  분석 도구가 **설치돼 있지 않았음** (`pwaAnalytics.ts` 에 gtag 호출 코드는 있으나
  `index.html` 에 GA 스크립트가 없어 어디서도 import되지 않는 죽은 파일),
  쿠팡 클릭도 전혀 기록되지 않고 있었음 → "카드 CTA vs pill 탭 중 뭐가 나은가"를
  누구도 답할 수 없는 상태였음
- `POST /api/track/coupang-click` 신설 + `coupang_clicks` 테이블 (없으면 자동 생성)
  - 기록 항목: 경로(`pill` / `card_cta`), 재료명, 그 카드의 부족 재료 개수, 레시피 id, 화면 경로
  - 측정 실패가 구매 동선을 막으면 안 되므로 어떤 예외에도 200 반환
- 프론트 `utils/trackCoupangClick.ts`: 클릭 직후 쿠팡으로 이동하므로 일반 fetch 는
  중간에 취소될 수 있어 `navigator.sendBeacon` 사용 (미지원 시 `keepalive` fetch 로 폴백)
- 2주 정도 쌓이면 "부족 1~3개 기준이 맞는지", "pill 탭이 실제로 쓰이는지",
  "하단 광고가 필요한지"를 추측이 아니라 숫자로 판단할 수 있음
- **하단 집계 광고는 만들지 않기로 함**: 50개 목록의 맨 아래는 도달률이 낮고,
  가장 자주 부족한 재료는 대개 설탕·간장 같은 기본 양념이라 구매 전환이 낮음.
  사용자가 "이게 없네" 하고 인지한 그 자리(부족 pill)에서 누르는 동선이 맥락상 우위

### 피드백 반영 1차: 애니메이션 회복 + 카드 세부
- **FAB 애니메이션이 사라진 것처럼 보이던 문제**: 측정해보니 shimmer/sheen 은 실제로
  돌고 있었으나(playState: running), **노란 버튼 위에 노란 그라데이션**이라 대비가 낮아
  움직임이 인지되지 않았음. 회전 링을 제거한 뒤 생긴 문제
  → sheen 을 더 또렷하게(피크 opacity 0.85 → 1), 글로우 세기 상향(0.5 → 0.75),
  버튼이 숨쉬듯 미세하게 커졌다 작아지는 `ai-fab-breathe` 추가.
  누를 때는 애니메이션을 일시정지시켜 축소 반응과 충돌하지 않게 함
- **로딩에 "Loading..." 표시 추가**: 스켈레톤만 있으면 "멈춘 화면"처럼 보인다는 피드백 →
  브랜드 컬러 링 스피너 + `Loading...` 문구를 목록 상단에 배치 (`role="status"`)
- **좋아요·댓글·조회수 줄바꿈 방지**: 한 줄로 고정하고 넘치면 말줄임 →
  카드 높이가 들쭉날쭉해지던 문제 해결
- **완료·기록·공유 버튼**: 아이콘이 작고 간격만 넓다는 피드백 →
  아이콘 18 → 22px 로 키우고 버튼 간 간격 제거(2 → 0), 오른쪽 여백 정리

### 피드백 반영 2차: 로딩 표시 통일 + 섹션 헤더 정리
- **로딩 표시가 화면마다 3종류로 달랐음** (탭을 옮길 때마다 다른 로딩이 떠 완성도가 떨어져 보임)
  - 내냉장고: 점 3개 + 한글 "로딩 중..."
  - 요즘인기 / 완료목록: 점 3개만 (문구 없음)
  - 냉장고요리 / 검색결과: 링 스피너 + "Loading..."
  → `LoadingIndicator` 컴포넌트 하나로 통일하고 **모든 화면에 적용**
- 링 스피너를 버리고 **점 3개가 순서대로 튀는 형태**로 되돌림. 점 크기도 12 → 13px 로 키우고
  간격·높이를 늘려 눈에 잘 띄게 함. 문구는 `Loading...` 으로 통일
- **섹션 헤더 통일** (`SectionHeader` 신설): 예전엔 섹션마다 마크업이 달랐음 —
  ⭐ 이모지를 제목 문자열에 섞어 넣은 것, 이미지 아이콘을 쓴 것이 섞여 있고
  정렬을 맞추려 `position: relative; top: 6px` 같은 임시 보정이 들어가 있었음
  → 이모지 제거(의미 있는 플랫폼 로고만 아이콘으로), 제목 16 → 18px, 설명문 지원
- **섹션 구분선 완화**: 2px 진한 회색 → 1px 옅은 선. 화면을 토막 내는 느낌 제거 (7곳)
- **요즘인기 카드 가로폭** 280 → 300px (좁아서 내용이 답답하다는 피드백)
- **재료 pill 접힘 기준** 8개 → 6개. 카드마다 3줄까지 늘어나 목록이 무거웠음

### 피드백 반영 3차: 팝업 규격 통일
- **팝업 6종의 수치가 전부 달랐음** — "다 제각각이라 촌스럽다"의 실제 원인
  | | 딤 | 모서리 | 폭 | 여백 |
  |---|---|---|---|---|
  | RegisterPrompt | 30% | rounded-xl | 320px | p-6 |
  | Welcome | (딤 없음) | rounded-lg | 가변 | px-5 py-4 |
  | IngredientDetail | 40% | rounded-2xl | 370px | p-8 |
  | IngredientDate | 40% | rounded-xl | 320px | p-6 |
  닫기 버튼도 `×` 텍스트인 곳, 아예 없는 곳이 섞여 있었음
- `ui/Dialog` 신설 — 확인/안내 팝업의 공통 규격
  (딤 40% / 모서리 20 / 폭 min(340, 100vw-40) / 여백 24 / 등장 애니메이션 /
  ESC 로 닫기 / 뒤 스크롤 잠금 / Portal / 하단 버튼은 `ui/Button` 사용)
- `RegisterPromptModal`, `WelcomeModal` 을 `Dialog` 로 완전 이관
- `IngredientDateModal`, `IngredientDetailModal` 은 내부 구조(달력·상세)가 복잡해
  전면 이관 대신 **겉 규격만 Dialog 와 동일**하게 맞춤 (모서리·폭·여백·그림자)
- 닫기 버튼 통일: `×` 텍스트 → 40×40 터치 영역의 SVG 아이콘 버튼 (`aria-label="닫기"`)
- 딤 농도를 40% 로 통일 (6개 파일)

### 피드백 반영 4차: 상단 GNB · 하단 네비 정리
- **상단 GNB**
  - 로고가 16px(`h-4`)로 지나치게 작아 브랜드가 눈에 안 들어왔음 → 20px 로 키우고
    44px 터치 영역의 버튼으로 감쌈
  - 닉네임·로그아웃 버튼에 `padding: 0` 이라 실제 터치 영역이 글자 높이뿐이었음 →
    높이 36px + 좌우 여백 확보, 공통 `NavTextButton` 으로 통일
  - **하단 구분선이 없어 스크롤 시 본문과 헤더가 섞여 보이던 문제** → `borderBottom` 추가
  - 닉네임이 길면 GNB 를 밀어내던 문제 → 최대 폭 + 말줄임
- **하단 네비게이션**
  - 실제로 쓰이지 않는 `STYLES` 상수 객체와 `CONSTANTS`(`NAV_HEIGHT: 16` 을 rem 으로 써
    실제 렌더와 무관했음)가 남아 있어 값을 어디서 고쳐야 할지 알 수 없었음 → 제거
  - 상단 구분선을 nav 에 붙이지 않고 **별도 `<div>` 를 절대배치**해 그리고 있었고
    그 div 만 `z-index: 30` 이라 다른 요소에 가려질 수 있었음 → nav 자체의 `borderTop` 으로 변경
  - **iPhone 홈 인디케이터 영역(safe-area) 대응 없어 하단이 가려지던 문제** →
    `env(safe-area-inset-bottom)` 반영
  - 탭 버튼을 `flex: 1` 로 균등 분배해 터치 영역을 화면 폭 전체로 확보
    (기존 `justify-around` 는 버튼 사이 빈 공간이 눌리지 않았음)
  - 활성 탭 표시를 `text-black/font-bold` → 토큰(`--ink-900` / `--ink-400`)으로 통일

### 피드백 반영 5차: 챗 패널을 팝업 공통 규격에 맞춤
- FAB 챗 패널만 다른 규격을 쓰고 있었음 (모서리 16 / 그림자 `0 8px 32px`) →
  `Dialog`·`Sheet` 와 동일하게 모서리 20 / `0 16px 48px` 로 통일
- 헤더 구분선을 `border-gray-100` → 토큰(`--line-200`)으로
- 닫기 버튼을 `×` 텍스트(38px) → 앱 공통 규격인 40×40 SVG 아이콘 버튼으로 교체.
  헤더의 원형 아이콘 버튼(지난 대화 / 새 대화)도 38 → 40px, 색을 토큰으로 통일
- 닫기 버튼 색이 옅어 잘 보이지 않아 `--ink-400` → `--ink-500` 로 한 단계 진하게
  (Dialog / 챗 패널 / 달력 3곳 공통)
- 챗 패널은 바텀시트로 바꾸지 않고 **떠 있는 패널 형태를 유지**함 —
  모바일 키보드 대응(`visualViewport` 로 높이·위치 재계산)이 이 구조에 맞춰져 있어
  시트로 바꾸면 그 처리를 다시 만들어야 하고, 얻는 것보다 잃는 게 큼

### 쿠팡 파트너스 고지문을 목록 하단 1회로
- 약관 확인 결과 페이지당 1회 노출로 진행하기로 함
- 기존엔 구매 버튼이 뜨는 **카드마다** 12px 2줄짜리 고지문이 반복돼,
  목록을 스크롤하는 내내 같은 문장이 계속 지나가며 화면을 무겁게 만들었음
  (재료명이 들어가지 않는 고정 안내 문장이라 카드마다 있을 이유가 없었음)
- `CoupangDisclaimer` 컴포넌트 신설 → `CoupangProductAd` 내부에서 제거하고
  광고가 노출되는 목록 화면 6곳(냉장고요리 / 검색결과 / 요즘인기 / 마이페이지 /
  기록한 레시피 / 완료한 레시피) 하단에 각 1회 배치
- 검증: 카드 50개가 렌더된 목록에서 고지문 노출 **1회** 확인

### 피드백 반영 6차: 애니메이션 정지 원인 해결 + 팝업 나가기 동선 통일
- **로딩·FAB 애니메이션이 동시에 멈춰 있던 원인 발견**: B단계에서 넣은
  `@media (prefers-reduced-motion: reduce)` 블록이 `*` 셀렉터로 **모든 애니메이션을
  0.01ms/1회로 죽이고 있었음**. 기기에서 "동작 줄이기"(iOS 손쉬운 사용 / Windows 애니메이션 표시)를
  켜면 로딩 점과 FAB 가 정지한 채로 보임 → 앱이 고장 난 것처럼 느껴짐
  - 로컬 측정에서 계속 "running" 으로 나왔던 건 테스트 브라우저가 그 설정을 물려받지 않았기 때문.
    `reducedMotion: 'reduce'` 로 재현해 확인함
  - → 장식적 움직임만 줄이고 **상태를 알리는 표시(로딩 점 / FAB / 스켈레톤)는 예외**로 둠.
    움직임 최소화 시 점은 튀는 대신 밝기만 변하도록 별도 keyframes 제공
- **팝업 나가기 동선 통일**: 우상단 X 가 있는 곳/없는 곳, 하단 취소 버튼이 있는 곳/없는 곳이
  섞여 있어 나갈 방법을 찾기 어려웠음
  - `ui/CloseButton` 신설 — 44px 터치 영역 + 연회색 원형 배경 + 굵은 SVG(22px, stroke 2.4).
    기존 `×` 문자는 폰트에 따라 크기·두께가 달라지고 얇아 잘 보이지 않았음
  - `Dialog` / `Sheet` / 챗 패널이 모두 이 버튼을 사용
  - `Dialog` 는 `actions` 가 비어 있으면 하단에 **취소 버튼을 자동으로** 넣음
  - `Sheet` 는 `footer` 가 없으면 하단에 **닫기 버튼을 자동으로** 넣음
  - 필터 시트 하단을 `취소` + `적용` 두 버튼으로 변경 (기존엔 적용만 있어 되돌릴 방법이 없었음)
- X 아이콘이 빈 원으로만 보이던 문제: flex 컨테이너 안에서 SVG 가 0px 로 찌그러지는 현상 →
  크기를 인라인으로 고정 (예전 FAB 아이콘과 동일한 원인)

### 피드백 반영 7차: 남아 있던 닫기 버튼 전부 교체
- 6차에서 `Dialog`/`Sheet`/챗 패널만 공통 버튼으로 바꿨고, **자체 마크업을 쓰는 팝업들은
  그대로 텍스트 `×` 였음** → 팝업마다 X 모양이 다른 상태가 계속됨
- 아래 8곳을 모두 `ui/CloseButton` 으로 교체
  - `RecipeSortBar` 매칭도 설정 / 임박 재료 설정 (텍스트 `×` 였음)
  - `CustomCalendar` 기간선택 (직접 만든 18px SVG — 원만 보이고 X 가 안 보였음)
  - `IngredientDetailModal`, `IngredientDateModal`
  - `MyPage` 내 정보 수정 (텍스트 `×`)
  - `RecordedRecipeListPage`, `CompletedRecipeListPage` 의 매칭률·임박재료 모달 (각 2곳)
  - `HomeInstallPrompt` 홈 화면 추가 안내
- 교체하지 않은 `×`: `FilterModal` 의 재료 칩 제거, `IngredientInput` 의 재료 삭제 —
  팝업 닫기가 아니라 항목 삭제 용도라 성격이 다름
- 검증: 매칭도 팝업에서 버튼 40×40 / SVG 22×22 / 배경 `#F5F5F7` 로 다른 팝업과 동일하게 렌더됨

### 피드백 반영 8차: 닫기 버튼 모양 · 팝업 헤더 높이 · 시트 드래그 닫기
- **닫기 버튼을 원형 → 둥근 모서리 사각형으로** (동그란 배경이 투박하다는 피드백)
  36×36, `borderRadius: 10`, 옅은 테두리 + 연회색 배경. 아이콘 20px
  - 실측 시 버튼 폭이 40px 로 나왔는데, 전역 `button { padding: 0.6em 1.2em }` 규칙이
    먹고 있었기 때문 → `padding: 0` + `boxSizing: border-box` 로 고정
- **팝업 제목 크기가 제각각이었음**: 달력 15px / 날짜입력 15px / 매칭도·임박재료 15px /
  기록·완료 모달 15px / 내 정보 수정 18px / 시트 18px → **전부 17px** 로 통일 (9곳)
- `ui/PopupHeader` 신설 — 높이 52px 고정 + 제목 17px + 우상단 닫기.
  기존엔 `minHeight: 56 + paddingTop 18` 같은 임의 값이 섞여 팝업마다 상단 높이가 달랐음.
  `Sheet` 가 이 헤더를 사용
- **바텀시트를 아래로 밀어서 닫기** 추가: 아래에서 올라온 시트는 아래로 미는 게 자연스러운
  닫기 동작인데 지원되지 않아 X 나 취소를 눌러야만 했음
  - 손가락을 그대로 따라 내려가고, 90px 이상 내리면 닫힘. 덜 내리면 스르르 제자리로 복귀
  - 내리는 동안 뒤 딤도 함께 옅어져 닫히는 느낌을 줌
  - 검증: 터치 이벤트로 160px 드래그 시 `translateY(160px)` 적용 → 손 뗀 뒤 닫힘 확인

### 피드백 반영 9차: 필터 시트 확장
- **시트가 중간 높이로만 열려 선택지가 안 보이던 문제**. 원인이 두 가지였음
  1. 시트 기본 높이가 낮았음 → 필터는 `82dvh` 로 넉넉히 열도록 지정
  2. **시트 안에 또 다른 고정 스크롤 영역**이 있었음 — 테마 선택란이 모바일에서
     `maxHeight: 100px` 로 묶여 있어 두 줄도 안 보였고, 시트 안에서 다시 스크롤해야 하는
     이중 스크롤 구조였음 → `46dvh` 로 확대
- **위로 끌어 펼치기 추가**: 시트를 위로 밀면 `95dvh` 로 확장. 손잡이를 탭해도 펼침/접힘 전환
  (드래그가 어려운 경우 대비). 펼친 상태에서 아래로 내리면 기본 높이로,
  기본 높이에서 내리면 닫힘 — 3단계 동작
- 최대로 펼쳐진 상태에서 위로 더 끌면 고무줄처럼 조금만 따라오게 해 한계를 알 수 있게 함
- 검증: 기본 82% → 펼침 95% 로 높이 변화 확인

### 피드백 반영 10차: 내냉장고 재료 설정 동선 개선
- **설정 버튼이 너무 작았음**: `⚙︎` 문자에 padding 4px 뿐이라 실제 터치 영역이 23px 남짓.
  유통기한을 자주 고치는 동선이라 32×32 아이콘 버튼(시계 모양)으로 교체
- **날짜 입력이 어색했던 부분 개선** (`IngredientDateModal`)
  - `yyyy-mm-dd` 를 손으로 다 입력하거나 달력을 열어야만 했음 →
    **빠른 선택 칩 추가** (유통기한: 3일 뒤 / 1주일 뒤 / 2주일 뒤 / 1개월 뒤,
    구매시점: 오늘 / 어제 / 3일 전 / 1주일 전)
  - 제목("일자를 선택하세요")과 바로 아래 질문("유통기한은 언제까지 인가요?")이 같은 말을
    두 번 하고 있었음 → 제목은 `유통기한` / `구매 시점` 으로 정리
  - 확인 버튼이 **파란색**이라 앱의 노랑·검정 체계와 따로 놀았음 → 공통 `Button` 사용
  - "잘 모르겠어요" 를 구매시점에만 주던 것을 유통기한에도 제공
- **재료 상세 팝업이 좌우로 잘리던 문제 수정 (제가 만든 회귀)**
  - 3차에서 팝업 규격을 통일하며 폭을 370 → 340px 로 줄였는데, 이 팝업은 내용이 넓어
    좌우가 잘려 "보관 공간"이 "산 공간"으로 보이고 닫기 버튼도 화면 밖으로 나갔음
  - 근본 원인은 **유통기한 버튼이 200px 고정 × 2개 + gap 32px = 432px** 로
    팝업 폭을 넘고 있었던 것 → 버튼을 `flex: 1` 가변폭으로, 아이콘 64 → 52px,
    팝업 폭 `min(360px, 100vw - 24px)` 로 조정
  - 유통기한 선택 버튼 색이 주황(#FF9800)/하늘색(#4FC3F7)이라 브랜드와 무관했음 →
    "있어요"는 브랜드 옐로우(주 선택), "없어요"는 중립 톤으로 위계 부여
  - 제목이 닫기 버튼과 겹쳐 잘려 보이던 것 → 좌우 여백 확보 + 공통 제목 규격(17px)

### 피드백 반영 11차: 입력창 포커스 스타일 통일
- **포커스 표시가 화면마다 제각각이었음**
  - 노란 테두리 + 노란 글로우: 챗 입력창(`.ai-chat-input`), 공통 `ui/Input`
  - 파란 링: `focus:ring-blue-*` (4곳)
  - 초록 링: `focus:ring-green-*`
  - 회색 테두리만: `focus:border-gray-400` (냉장고요리 검색창 등 3곳)
  - 아무 표시 없음: `focus:outline-none` 만 있는 곳
- 노란 링은 **브랜드 컬러를 "지금 입력 중"이라는 흔한 상태에 써버려** 과하게 튀었음
  → 테두리를 진하게(`--ink-700`) 하고 아주 옅은 회색 링(`rgba(26,26,30,0.07)`)만 두는
  절제된 방식으로 통일. 키보드로 이동해 들어온 경우(`:focus-visible`)엔 조금 더 또렷하게(0.14)
- `input` / `textarea` / `select` 전체에 적용되는 공통 규칙으로 만들고,
  컴포넌트에 흩어져 있던 색상 포커스 클래스를 6개 파일에서 제거
- 검증: 냉장고요리 검색창 / 내냉장고 재료 입력 / 챗 입력창 세 곳 모두
  `border: rgb(26,26,30)` + 동일한 링으로 렌더되는 것 확인

### LLM 재료 추출 매일 자동 실행 등록
- **어제 LLM 단계가 실행되지 않았음을 확인**: 로그에 `used_ingredients LLM 처리 시작` 이 0번
  찍혔고, 완료 문구도 코드상 문구가 아닌 옛 문구였음 → 어제 돌던 크롤러는 LLM 단계를 붙이기
  **전 버전의 스크립트를 메모리에 올린 상태**였음 (파이썬은 실행 중 파일 수정이 반영되지 않음)
- DB 실측: 전체 44,656건 중 LLM 처리 완료 **0건**.
  (수동 테스트로 처리했던 160건은 `llm_ingredients_done` 컬럼 추가 *전* 이라 표시가 남지 않음)
- **크롤러에서 분리**: 크롤러는 주 1회(월요일)만 도는데 LLM 처리는 매일 돌려야 하므로
  `run_llm_ingredients_daily.bat` 신설 + 작업 스케줄러 `CookMatch-DailyLLMIngredients` 등록
  (매일 새벽 3시, 절전 상태면 깨워서 실행)
- **묶음 8 → 12건**: 무료 티어 하루 500회 호출 중 450회만 사용(챗봇 몫 확보) →
  하루 450 × 12 = **5,400건**, 44,656건 기준 **약 9일** 소요
  (20건 이상으로 키우면 한 번에 보내는 본문이 길어져 LLM 이 뒷부분을 대충 처리하는 경향이 있어
   12건을 균형점으로 잡음)
- 소량(24건) 사전 검증 후 등록: 처리 24건 / 오류 0건 확인. 오늘분도 즉시 실행 시작

### ⚠️ 룰베이스 배치가 LLM 결과를 덮어쓰던 문제 수정
- **발견한 충돌**: 룰베이스 배치(`update_used_ingredients_batch.py`)가 조건 없이
  `SELECT ... FROM recipes ORDER BY id` 로 **전체 행**을 돌면서 `used_ingredients` 를
  덮어쓰고 있었음. LLM 추출도 **같은 열**을 쓰기 때문에,
  **주 1회 크롤러가 돌 때마다 그동안 LLM 이 며칠에 걸쳐 채운 결과가 룰베이스 값으로 되돌아감**
  - 게다가 `llm_ingredients_done` 은 1 로 남아 있어 LLM 이 다시 처리하지도 않음
    → 9일짜리 작업이 월요일마다 조용히 일부 무효화되는 구조였음
- 수정: 룰베이스 배치의 대상을 `WHERE COALESCE(llm_ingredients_done, 0) = 0` 으로 제한.
  룰베이스는 **아직 LLM 이 손대지 않은 신규 수집분의 임시 채움** 역할만 하도록 명확히 함
- 검증: 전체 44,518건 중 LLM 처리 완료 876건이 배치 대상에서 제외되어
  43,642건만 대상이 되는 것 확인

### 데이터 흐름 정리 (질문 답변 겸 기록)
- LLM 결과는 **새 열이 아니라 기존 `used_ingredients` 열을 덮어씀** →
  화면의 재료 pill 은 자동으로 LLM 결과로 바뀜 (UI 는 이 열 하나만 읽음).
  `llm_ingredients_done` 은 재료 값이 아니라 "이 행을 LLM 으로 처리했는지" 표시하는 깃발
- 이어서 처리 / 중복 없음: `--pending-only` 가 `done = 0` 인 행만 가져오고 성공 시 1 로 바꿈
  - 정상 추출 → 값 덮어쓰기 + `done=1`
  - 일시적 오류 → `done` 을 건드리지 않음 → 다음 실행에서 자동 재시도
  - LLM 결과만 비고 기존 값은 있음 → **값 보존** + `done=1` (무료 한도 낭비 방지)
  - 룰베이스도 비어 있고 LLM 도 비어 있음(둘 다 "재료 없음"에 동의) → **행 삭제**
    (레시피가 아닌 홍보성 본문으로 판단)

### 문서 전면 점검 및 갱신 (2026-08-26)
실제 코드·DB 와 대조해 문서를 최신 상태로 맞춤. 발견한 불일치는 아래와 같음.

**`DATABASE_SCHEMA.md` — 전면 교체 (실제와 거의 전부 달랐음)**
- `users`: 문서엔 `username`/`password_hash`/`phone`/`is_active` → 실제는
  `email`/`nickname`/`provider`/`provider_id`/`password`/`deleted_at` (소셜 로그인 구조)
- `user_ingredients`: 문서엔 `ingredient_name`/`category`/`quantity`/`unit`/`notes` →
  실제는 `name`/`storage_box`/`expiry_date`/`purchase_date`/`saved_at`
- 문서엔 `action_type` 을 가진 `user_recipes` 단일 테이블 → 실제는 즐겨찾기/기록/완료
  **3개 테이블로 분리**되어 있음
- 문서에 아예 없던 테이블: `recipes`(핵심!), `coupang_clicks`,
  `youtube_channel_cache`, `youtube_channel_meta`
- → 운영 DB 에서 직접 뽑아 다시 작성하고, `used_ingredients` 와 `llm_ingredients_done`
  의 관계·주의사항을 명시

**`PROJECT_OVERVIEW.md`**
- 재료 추출이 룰베이스/LLM **두 단계이고 주기가 다르다**는 점이 문서에 없었음 → 표로 추가
- 자동 실행 작업 2종(주간 크롤러 / 매일 LLM) 명시
- `llm_ingredients_done` 조건이 빠지면 룰베이스가 LLM 결과를 덮어쓴다는 경고 추가

**`DEVELOPMENT_GUIDE.md`**
- 개발 서버 포트가 **5173 으로 적혀 있었으나 실제는 5178** (`vite.config.ts`) → 수정
- 이번에 만든 **UI 체계 전체를 문서화**: 토큰 위치, `components/ui/` 7종,
  지켜야 할 규칙 6가지(팝업은 Dialog/Sheet, sticky 안에서 모달 렌더 금지,
  로딩은 하나만, reduced-motion 에서 `*` 금지, 포커스 개별 지정 금지, SVG 크기 고정)

**`README.md`**
- 폴더 구조도가 실제와 달랐음: 존재하지 않는 루트 `package.json`,
  `database/DATABASE_SCHEMA.md`, `data/*.csv` 등이 적혀 있고,
  실제로 있는 `ingredient_management/`, `scripts/`, `logs/`,
  `frontend/src/ui`·`styles/` 는 빠져 있었음 → 실제 구조로 교체
- CORS 예시에 5178 추가

**`MOBILE_APP_GUIDE.md`**
- "PWA 를 완성해야 한다"고 적혀 있었으나 실제로는 `manifest.json`·`sw.js` 가 이미 있음.
  다만 **서비스 워커 등록 코드가 어디에도 없어 실제로는 동작하지 않는 상태** →
  현재 상태를 표로 정리하고, 등록 시 캐시 무효화 주의사항 함께 기재

**`PERFORMANCE_OPTIMIZATION.md`**
- "FULLTEXT 인덱스 추가 (필수)" 로 남아 있었으나 실측 결과 `ft_title_content`
  (title, content) 가 **이미 적용돼 있음** → 적용 상태 표시 추가

### 피드백 반영 12차: 남은 UI 건 정리
- **챗 입력창 테두리가 굵고 노랗게 보이던 원인 발견 (제가 만든 버그)**
  - D단계에서 넣은 접근성 규칙 `input:focus-visible { outline: 2px solid var(--brand-strong);
    border-radius: 6px }` 이 **입력 요소까지 포함**하고 있었음
  - 챗 입력창은 패널이 열릴 때 자동 포커스되므로 **열자마자 노란 2px 외곽선**이 둘러졌고,
    `border-radius: 6px` 강제 때문에 알약 모양(9999px) 모서리까지 각지게 바뀌어 있었음
  - → 입력 요소를 규칙에서 제외(입력창은 `input:focus` 의 진한 테두리 + 옅은 링이 담당),
    버튼·링크용 외곽선 색도 노랑 → 중립 톤으로 변경. 모서리 강제도 제거
  - 검증: 모서리 `9999px` 복구, 노란 외곽선 사라진 것 확인
- **로딩이 화면마다 여전히 달랐던 이유**: 11차에서 컴포넌트는 통일했지만
  내냉장고·요즘인기는 `dotsOnly` 옵션을 써서 **점만 나오고 문구가 없었음** → 옵션 제거해 통일
- **로딩 중에 쿠팡 고지문이 함께 뜨던 문제**: `CoupangDisclaimer` 가 조건 없이 렌더되고 있었음
  → `{!loading && ...}` 으로 감싸 로딩이 끝난 뒤에만 표시 (4개 화면)
- **요즘인기 날짜 팝업의 연/월 스크롤바가 안 보이던 문제**: 드롭다운에 Firefox 전용
  `scrollbarWidth`/`scrollbarColor` 만 지정돼 있고 WebKit 규칙이 없었음.
  이미 있던 `.custom-scrollbar` 유틸을 두 드롭다운에 적용
- **재료 상세 팝업의 이모지 제거**: 얼음·눈송이·온도계 이모지풍 PNG 3장이 앱의 다른 요소와
  그림체가 달라 촌스러웠음 → **선으로 그린 SVG 아이콘**으로 교체(색은 토큰을 따름).
  선택 상태도 파란 ring(`ring-blue-400 bg-blue-50`) → 브랜드 톤(연노랑 배경 + 진한 테두리)으로

### 날짜 입력 팝업 여백이 없던 원인 수정 (제가 만든 버그)
- 증상: 유통기한·구매시점 팝업만 좌우·상하 여백이 전혀 없어 콘텐츠가 팝업을 뚫고 나오는 것처럼 보임
- 원인: 3차에서 팝업 규격을 통일할 때 `style` 속성을 **추가**했는데, 그 태그에는
  이미 `style={{ fontFamily: ... }}` 가 있었음.
  **JSX 는 같은 속성이 두 번 오면 뒤의 것이 앞의 것을 통째로 덮어쓴다.**
  결과적으로 `padding` / `width` / `borderRadius` / `boxShadow` 가 전부 무시되고
  `fontFamily` 만 적용된 상태였음 (측정값: `padding: 0px`, 카드 폭 286px)
- 수정: 두 style 을 하나로 합치고 `boxSizing: border-box`,
  `maxHeight: calc(100dvh - 64px)` + 세로 스크롤 추가 (긴 내용에서도 잘리지 않도록)
- 같은 실수가 다른 곳에도 있는지 **전체 tsx 를 검사** — 중복 `style` 태그 0건 확인
- 검증: 카드 폭 286 → 340px, `padding: 24px 20px 20px` 정상 적용 확인

### 내냉장고 재료 pill 동작 통합
- **문제**: pill 하나에 동작이 3개였음 —
  이름 탭(토스트로 날짜 표시) / 시계 버튼(설정 팝업) / X(삭제).
  시계 버튼은 pill 과 떨어져 있어 무엇인지 알기 어렵고, 이름 탭으로 뜨는 토스트는
  날짜를 "보여주기만" 하고 고칠 수는 없어 설정 팝업의 열화판이었음.
  (유통기한을 확인하는 이유는 대개 고치려는 것)
- **수정**: pill 을 누르면 **바로 설정 팝업**이 열리도록 합치고 시계 버튼 제거 →
  동작 3개 → 2개(탭 / X). 쓰이지 않게 된 `onInfoClick` prop 과 `handleTagInfo` 도 정리
  (`infoToast` 자체는 재료 저장 안내에도 쓰이므로 유지)
- **D-day 표시 추가**: 유통기한이 설정된 재료에만 `D-3` 형태로 표시.
  3일 이하·지난 재료는 붉은 톤으로 강조 → 임박 재료를 pill 을 눌러보지 않아도 한눈에 파악
- **날짜가 없는 재료에는 아무 표시도 넣지 않음**: 실측 결과 내 냉장고 172건 중
  유통기한 설정 **0건**, 구매일 **3건(2%)** 뿐이라, 빈 재료에 "+ 날짜" 같은 자리표시를 두면
  거의 모든 pill 에 붙어 방금 없앤 시계 아이콘과 같은 문제가 반복됨
- 부수 효과: 아이콘이 빠지면서 pill 이 짧아져 한 줄에 들어가는 재료 수가 늘어남

> 남은 과제: 날짜 입력률이 사실상 0% 인 근본 원인은 버튼 크기가 아니라
> **입력 시점에 날짜를 받지 않는 흐름**임. 영수증 촬영 → OCR 로 구매일·품목을 한 번에
> 얻는 방향이 유력하나 규모가 커 별건으로 논의 예정.

### 재료 상세 팝업 여백 + 뒤로가기 버튼 통일
- **재료 상세 팝업도 `style` 속성이 두 번이었음** (날짜 팝업과 동일한 원인).
  앞서 만든 중복 검사 스크립트가 이 건을 놓쳤는데, 정규식이 속성값 안의 화살표 함수(`=>`)를
  태그 종료(`>`)로 잘못 인식해 태그를 일찍 끊었기 때문
  → 중괄호 깊이를 세면서 **최상위 속성만** 검사하도록 스크립트를 고쳐 전체 재검사.
  실제 중복 **0건** 확인 (앞서 3건으로 나온 것은 prop 값 안의 중첩 JSX 속성을 함께 센 오탐)
- 두 style 을 합치고 `maxHeight` + 세로 스크롤 추가. 검증: 카드 폭 340px,
  `padding: 24px 20px 20px` 정상 적용

**뒤로가기 버튼 통일 (`ui/BackButton` 신설)**
- 닫기(X)는 둥근 사각형으로 통일해 놓았는데 뒤로가기만 제각각이라 같은 팝업 안에서
  두 버튼이 다른 물건처럼 보였음
  - `IngredientDateModal`: PNG 이미지(13px) + padding 4px
  - 기록/완료 목록, 재료 상세 페이지 헤더: PNG 이미지(13px)
  - 챗 패널 "지난 대화": `←` **문자** (18px)
- → `CloseButton` 과 완전히 같은 규격(36×36 둥근 사각형 + 20px SVG)으로 통일. 5곳 교체
- 쓰이지 않게 된 `뒤로가기.png` import(Popular), `backIcon`·`button` 스타일 상수 제거

### 내냉장고 재료 팝업: 키보드 가림 + 맥락 부족 수정
- **팝업이 키보드에 가리던 문제**: 재료명을 입력하고 "입력"을 누르면 팝업이 뜨는데,
  입력창에 포커스가 남아 있어 모바일 키보드가 그대로 떠 팝업을 덮고 있었음
  → 팝업을 열기 전에 `document.activeElement.blur()` 로 포커스를 풀어 키보드를 내림 (2곳)
- **팝업이 무엇을 하는 창인지 알기 어렵던 문제**: 제목이 "재료의 상세정보를 선택해 주세요"
  하나뿐이라 **어떤 재료**에 대한 것인지, **새로 넣는 것인지 이미 있는 걸 고치는 것인지**
  구분되지 않았음
  → 재료 이름을 제목으로 올리고(`돼지고기`), 그 아래에
  `보관 정보를 수정합니다` / `어디에 보관할지 알려주세요` 로 신규·수정 여부를 밝힘

### 내냉장고 저장 버튼 제거 + 자동 저장 신뢰성 개선
재료가 바뀔 때마다 이미 자동 저장이 돌고 있었고, 디스크 모양 저장 버튼은 그 위에 얹힌
수동 백업이었음. 자동 저장에 누락이 생기는 구조라 버튼이 필요했던 것 → 원인 3가지를 고치고 버튼 제거.

- **① 전체 삭제가 서버에 반영되지 않던 버그 (가장 심각)**
  ```js
  const hasData = frozen.length > 0 || fridge.length > 0 || room.length > 0;
  if (hasData) { ...저장... }   // ← 빈 배열이면 저장 자체를 건너뜀
  ```
  재료를 전부 지워도 저장을 하지 않아 서버엔 예전 목록이 남았고,
  **다음 로그인 때 지운 재료가 되살아났음** → 빈 상태도 정상 상태이므로 그대로 저장
- **② 저장 요청 순서 역전**: 연속으로 변경하면 요청이 겹치고, 늦게 출발한 요청이 먼저
  도착하면 옛 데이터가 최신을 덮어썼음 → 600ms 디바운스로 마지막 상태만 보내고,
  일련번호로 **오래된 응답은 무시**하도록 함
- **③ 실패해도 아무도 모름**: 자동 저장 실패 시 `console.error` 만 찍고 끝나서
  저장이 안 됐는데도 성공한 줄 알았음 (수동 저장 버튼이 생긴 원인)
  → 저장 상태를 화면에 표시(`저장 중… / 저장됨 / 저장하지 못했어요`)하고
  실패 시 **다시 시도** 버튼 제공
- 저장 버튼과 관련 온보딩 가이드 단계("저장버튼을 눌러 재료 정보를 저장할 수 있어요") 제거
- 검증: 저장 버튼 사라짐 확인, 냉동칸 재료를 전부 지운 뒤 저장값이 `frozen: 0` 으로
  반영되는 것 확인 (예전에는 저장 자체를 건너뛰던 경우)

### 피드백 반영 13차: 이모지 제거 · 드롭다운 떨림 · 포커스 톤 · 안내 문구
- **③ 섹션 표식 이모지 통일** (`ui/SectionIcon` 신설): 화면마다 표현이 달랐음 —
  내냉장고는 이모지(🧊 ❄️ 🌡️), 요즘인기는 ⭐ 를 제목 문자열에 섞어 넣고,
  마이페이지만 SVG 를 쓰고 있었음. 이모지는 기기·OS 마다 그림체와 크기가 달라
  같은 화면 안에서도 톤이 안 맞음 → 선으로 그린 SVG 7종으로 통일(색은 글자색을 따름)
- **④ 재료 입력 드롭다운 떨림 원인 발견·제거**: 스크롤바를 강제로 보이게 하려고
  드롭다운이 열릴 때 `scrollTop` 을 `3 → 0 → 2 → 0 → 1 → 0` 으로 튕기고,
  그것을 50ms·150ms·300ms 로 **세 번 반복 실행**하고 있었음 (총 15회 스크롤 조작)
  → 스크롤바는 CSS 로 표시하면 되므로 이 조작을 전부 제거하고 `.custom-scrollbar` 로 통일
- **② 입력창 포커스 톤 완화**: 진한 회색 테두리 + 링이 "굵어지고 칙칙하다"는 피드백 →
  테두리를 `--ink-400` 으로 한 단계만 진해지게 하고 링도 0.07 → 0.05 로 낮춤
  (키보드 탐색 시에는 여전히 더 또렷하게 유지)
- **⑤ '특별한 날 특별한 음식' 안내 문구 교체**: "값비싼 재료가 들어간 레시피를 모았어요" 는
  비싼 재료를 사게 하려는 의도가 그대로 드러나 보였음 →
  "기념일이나 손님상에 어울리는 레시피예요" 로 사용자가 얻는 것을 말하도록 변경

### 피드백 반영 14차: 전역 GNB 통일 (①)
- **탭마다 GNB 가 다르게 보이던 원인**: 일부 화면이 공통 GNB(`TopNavBar`) 위에
  **자기만의 고정 헤더를 하나 더** 그리고 있었음. `AppRouter` 가 이미 주요 경로에
  `TopNavBar` 를 렌더하는데, 아래 화면들이 `position: fixed; top: 0` 헤더를 또 만들어
  **정확히 같은 자리에 두 개가 겹쳐 있었음** (둘 다 높이 56px, 같은 z-index)
  - `MyPage`: 로고·닉네임·로그아웃까지 **공통 GNB 를 통째로 복제**.
    게다가 복제본은 `maxWidth: 400px` 이라 넓은 화면에서 정렬이 달라 보였음
  - `IngredientDetail`, `RecordedRecipeListPage`, `CompletedRecipeListPage`:
    뒤로가기(+필터) 전용 헤더가 GNB 와 겹침
- 수정
  - `MyPage` 의 복제 GNB 제거 → 공통 GNB 하나만 사용
  - 나머지 3개 화면은 고정 헤더를 없애고 **뒤로가기·필터를 본문 제목 줄로 이동**.
    (헤더를 top:56 으로 내리는 대신 제목 줄에 합쳐 세로 공간 56px 절약)
  - 헤더가 사라진 만큼 본문 상단 여백 88 → 72px 로 조정
- 검증: 5개 주요 화면 모두 고정 헤더 **1개**, 높이 56px, 동일한 하단 구분선으로 통일 확인

### 피드백 반영 15차: 빈 영역 · 세로 간격 · 스크롤 어려움 (⑥)
- **화면이 벙벙해 보이던 원인**: 가로형 카드에 쿠팡 광고 자리를 **항상 62px 예약**해 두고
  있었음(`HORIZONTAL_COUPANG_SLOT_MIN_HEIGHT_PX`). 그런데 `CoupangProductAd` 는
  링크가 없으면 아무것도 렌더하지 않으므로, 광고가 거의 안 뜨는 상태에서는
  **카드마다 빈 공간만 남아** 있었음 → 슬롯 높이 예약 제거, 내용이 있을 때만 자리 차지
- **세로 간격이 들쭉날쭉하던 원인**: 요즘인기 섹션 간격이 실측 `6 / 4 / 4 / 48px` 로
  제각각이었음(`marginTop: 4, marginBottom: 0` 등이 섞임) → **32px 로 통일**
- **스크롤이 어렵던 원인 (가장 큰 문제)**: 가로 캐러셀의 카드 래퍼에
  `touchAction: 'pan-x'` 만 지정돼 있어, **카드 위에서 시작한 세로 스와이프가 아무 데도
  전달되지 않아 페이지 스크롤이 막혔음.** 요즘인기 화면은 높이 278~286px 짜리 가로
  스크롤 영역이 3개나 되어 화면 대부분이 "스크롤 안 되는 구역" 이었음
  → `touchAction: 'pan-x pan-y'` 로 변경해 세로 스와이프는 페이지로 넘어가게 함
- 검증: 섹션 간격 `32/32/32/32`, 카드 래퍼 `touchAction: pan-x pan-y` 확인

### 피드백 반영 16차: 필터 시트 재설계 (⑦)
- **문제**: 조건이 `■ 채널선택` / `■ 꼭 포함할 재료` / `■ 꼭 제외할 재료` / `■ 테마 선택`
  네 덩어리로 나열돼 있었지만, 사이 구분이 얇은 회색 선 하나뿐이라
  **각 조건이 AND 로 함께 걸린다는 사실이 전혀 드러나지 않았음.**
  "채널을 고르면 테마가 무시되는 건가?" 같은 오해가 생길 수 있는 구조
- `components/ui/FilterGroup.tsx` 신설
  - `FilterGroup`: 번호 배지(①②③) + 제목 + 보조 설명. 묶음의 시작·끝이 눈에 보이게 함
  - `AndDivider`: 선 — **"그리고"** — 선. AND 결합을 **글자로 명시**. 구분선만으로는
    "또는" 인지 "그리고" 인지 알 수 없어서 텍스트로 못박음
- **적용 중인 검색어를 시트 맨 위에 읽기 전용 칩으로 표시**
  - 시트 안에 검색 입력창을 새로 넣지 않은 이유: 화면 상단 검색창과 필터가 **같은 값**
    (`includeKeyword`)을 조작하므로, 입력창을 하나 더 두면 한 값을 고치는 곳이 두 군데가 됨
  - 맨 위에 둔 이유: 검색어는 시트를 열기 **전에 이미 정한 조건**이라,
    "이 검색어에 더해서 좁힌다" 는 흐름이 먼저 읽혀야 함. 여기서는 확인과 해제(×)만 가능
- **채널 선택을 체크박스 → 칩 토글로 변경**: 16px 체크박스는 손가락으로 정확히 누르기
  어려웠음. 38px 높이 칩으로 바꾸고 선택 시 배경 반전 + `aria-pressed` 부여
- **재료 포함/제외를 한 그룹으로 통합**: 예전엔 별도 섹션이라 서로 무관해 보였으나
  실제로는 "재료 조건" 하나임. 그룹 안에서 소제목으로만 구분
- **입력창 규격 통일**: 12px 글자 / 30px 높이 → **16px 글자 / 44px 높이**.
  16px 미만은 iOS 에서 포커스 시 화면이 확대되는 문제도 있었음
- **테마 키워드 버튼**: 20px 남짓(`py-0.5`)이라 오탭이 잦았음 → 34px / 13px 로 확대,
  선택 상태를 노란 계열 대신 잉크 계열 반전으로 통일
- **`■` 문자 표식 제거**: 테마 대분류/중분류도 굵기·색으로만 층위를 구분하도록 변경
- 검증(390×800 실측): 시트 안 `■` 0개, "그리고" 구분선 3개, 검색어 칩 표시,
  입력창 44px/16px, 테마 버튼 34px/13px, 채널 칩 `aria-pressed=true` + 배경 반전,
  적용 시 시트 정상 종료

### 자동 배치가 한 번도 실행되지 않던 문제 수정
- 예약 작업 상태를 확인해보니 `CookMatch-DailyLLMIngredients` / `CookMatch-WeeklyCrawler`
  둘 다 **등록 이후 한 번도 성공하지 못한 상태**였음 (LastTaskResult=1, 즉시 종료).
  DB의 LLM 처리량도 5,260건에서 멈춰 있었음
- 원인: 두 `.bat` 파일이 **LF 개행으로 저장**돼 있었음. cmd.exe 는 배치 파일이 CRLF 인
  것을 전제로 파싱하기 때문에 캐럿(`^`) 줄바꿈이 깨져
  `'ng-only'은(는) 내부 또는 외부 명령이 아닙니다` 오류로 즉시 실패했음
  (`--pending-only` 인자가 중간에서 잘림). 예약 등록 자체는 정상이었기 때문에
  작업 상태만 봐서는 "Ready" 로 보여 놓치기 쉬운 문제였음
- 조치
  - 두 배치 파일을 CRLF 로 재저장
  - LLM 배치는 캐럿 줄바꿈에 의존하지 않도록 **인자를 한 줄로** 합침
  - `.gitattributes` 신설: `*.bat`, `*.cmd`, `*.ps1` 은 항상 CRLF 로 체크아웃되게 고정
    (git 이 개행을 바꾸면서 같은 문제가 재발하는 것을 막음)
- 검증: 예약 작업을 수동 실행해 45초 만에 100건 처리, 오류 0건 확인.
  다음 자동 실행은 매일 03:00

### 유통기한 추정 (⑧)
- **왜 필요했나**: 구매일자를 받아 저장은 하고 있었지만 그 값으로 아무것도 하지 않았다.
  사용자 입장에서는 "적어서 뭐 하려고?" 인 입력이었고, 실제로 유통기한을 직접 넣는
  사용자가 드물어 임박 재료 기능도 사실상 비어 있었음
- `utils/shelfLife.ts` 신설 — 재료 사전의 **세분류 → 소분류 → 중분류** 순으로 찾는
  보관 일수 표. 재료가 2,900개가 넘어 하나씩 채우는 것은 불가능하고, 채워도
  "삼겹살 3일 / 목살 3일" 처럼 같은 값이 반복되므로 카테고리 층위에 값을 붙였다
  - 세분류(가장 구체적): 잎채소 5일 / 뿌리채소 21일 / 육류 3일 / 생선 2일 / 달걀 30일 …
  - 보관 방법(냉동·냉장·실온)별로 값을 따로 둠. 육류·수산의 실온처럼 **권하지 않는
    조합은 값을 비워** 추정하지 않는다
  - **카테고리를 못 찾으면 추정하지 않는다.** 임의의 기본값을 씌우면 실제로는 한 달 가는
    재료에 D-7 이 붙어 멀쩡한 재료를 버리게 되므로, 틀릴 바엔 표시하지 않는 쪽을 택함
- 구매일자 팝업에 안내 문구 추가 — "구매일자를 입력하면 쿡매치가 재료 종류와 보관
  방법에 맞춰 대략적인 유통기한을 짐작해 계산해 드려요. 실제 표기된 기한과는 다를 수
  있어요." 무엇을 위해 묻는지 밝히지 않으면 그냥 귀찮은 입력이 된다
- 표시: 직접 넣은 유통기한은 `D-3`, 짐작한 값은 **`약 D-3`**. 추정값은 급해도 붉은색
  대신 옅은 주황으로 한 단계 낮춤 — 추정을 확정처럼 보여주면 안 되기 때문
- 저장 구조: `estimatedExpiry` 를 별도 필드로 둬서 사용자가 직접 넣은 `expiry` 와 섞지
  않음. 카테고리 표가 로드되면 **기존에 구매일만 있던 재료도 한 번 훑어 채운다**
- 임박 재료 정렬·목록(`RecipeSortBar`, `RecipeList`)에도 추정값을 포함.
  직접 넣은 것만 세면 임박 재료가 거의 항상 비어 기능이 동작하지 않았음
- 검증: 삼겹살 → 냉장보관 → 구매일 오늘 → `약 D-3` 표시 확인 (육류·냉장 3일)

### 피드백 반영 17차: 남은 이모지 · 영역 구분 · 마이페이지 구조
- **요즘인기에 남아 있던 이모지 제거**: 앞서 섹션 제목을 정리했지만 `📈 인기 급상승 요리
  TOP 10`, `📈 인기 급상승 테마 TOP 10`, `🔍️ 특정 재료·테마 등 키워드로 찾아보기` 세 곳은
  `SectionHeader` 를 쓰지 않고 생 `<h2>` 로 되어 있어 빠져 있었음 → 전부 `SectionHeader`
  + `SectionIcon` 으로 교체. `SectionIcon` 에 `trending`(오름세), `search`(돋보기) 추가
- **레시피 카드 순위의 🥇🥈🥉 제거**: 기기마다 그림체·크기가 달라 글자와 높이가 안 맞았음.
  순위 숫자 자체가 이미 정보라 장식을 덧붙이지 않음
- **제목 크기 통일**: 생 `<h2>` 들이 16px, `SectionHeader` 는 18px 이라 같은 층위의 제목이
  두 가지 크기로 섞여 있었음 → 전부 18px
- **"영역이 어디서 시작되는지 모르겠다" 의 실제 원인 세 가지**
  1. 구분선을 **제목 바로 아래**에 그어 놔서, 선이 "섹션의 시작" 이 아니라
     "제목과 내용의 분리" 를 뜻하고 있었음 → 제거
  2. 화면이 전부 흰색이라 섹션 사이 32px 여백이 그냥 빈 공간으로 보임
  3. `인기 급상승` 은 한 섹션 안에 두 덩어리가 **gap 48** 로 들어 있어,
     섹션 사이(32)보다 섹션 **안쪽**이 더 벌어진 역전 상태였음
  → `components/ui/SectionBand.tsx` 신설. 화면 좌우 끝까지 닿는 8px 옅은 회색 띠를
    섹션 맨 앞에 둔다(사이가 아니라 맨 앞에 두는 이유: 조건부로 안 보이는 섹션이 있어도
    구분이 어긋나지 않음). `인기 급상승` 은 두 개의 독립 섹션으로 분리
- **상단 타이틀 여백**: `header` 와 그 안 `h2` 가 각각 `marginBottom: 32` 를 걸어 64px 이
  비어 있었음 → 20px
- **빈 목록이 카드 한 장 높이(280px)를 그대로 비워 두던 문제**: 마이페이지처럼 세 목록이
  모두 비어 있으면 840px, 화면 한 장을 넘는 빈 공간이 생겼음 → 안내 문구 크기만큼만
  차지하게 하고 옅은 배경을 깔아 "비어 있는 목록" 임이 보이게 함 (마이페이지 전체 높이
  1,551 → 999px)

### 마이페이지 구조 재설계
- **문제**: 이 화면의 목적은 "내가 즐겨찾고·기록하고·완료한 레시피 보기" 인데
  - 위 130px + 아래 70px 을 비우고 가운데에 닉네임·이메일·버튼만 세로로 쌓아 둬서
    화면을 열면 **200px 짜리 빈 구역**이 먼저 보였고
  - 브랜드 강조색(노랑)이 하필 **'내 정보 수정'** 에만 칠해져 있어,
    가장 눈에 띄는 것이 이 화면에서 가장 덜 중요한 기능이었음 (위계 역전)
- 프로필을 **한 줄 카드**로 접음. 닉네임·이메일은 왼쪽, `정보 수정` 은 오른쪽 아웃라인
  버튼으로. 노란색은 비로그인 상태의 `로그인` 처럼 실제로 유도할 행동에만 남김
- **요약 줄 추가**: 즐겨찾기/기록/완료 개수를 세 칸으로 보여 주고 각각 전체 목록으로
  이동. 예전엔 스크롤을 끝까지 내려야 각 목록에 뭐가 몇 개 있는지 알 수 있었음
- **전체보기 `☰` → `전체보기 ›`**: 햄버거 기호는 '메뉴' 를 뜻해 '이 목록 전부 보기' 와
  뜻이 맞지 않고, 22px 글자 하나라 무슨 버튼인지 알 수 없었음
- 섹션 제목 3개를 `SectionHeader` + `SectionIcon`(favorite/recorded/completed)으로 통일

### 특별한 날 섹션 설명 문구
- `기념일이나 손님상에 어울리는 레시피예요` → `평소엔 잘 안 쓰는 재료로 만드는 요리예요`
  → 다시 `특별한 재료로 만드는 요리예요` (자주 쓰는 재료가 섞인 경우도 있어
  "평소엔 잘 안 쓰는" 이 사실과 어긋났음. 단정할 수 있는 만큼만 말한다)
- 기념일·손님상으로 못박으니 "그런 날" 이 아닌 사람에게는 남의 이야기가 됐음.
  실제로는 아이에게 잘 먹이고 싶은 날처럼 아무 날에도 보게 되는 목록이라,
  상황을 한정하지 말고 재료의 성격만 말하도록 바꿈

### 피드백 반영 18차: 요즘인기 기간 컨트롤 통일
- **문제**: 기간 컨트롤(최근 7일/30일/기간선택)은 아래 **모든 섹션에 함께 적용되는
  화면 전체 조건**인데, 화면에서는 아래 내용과 무관한 별도 영역처럼 보였음
- 실측으로 확인한 원인
  - **규격이 다른 탭과 어긋남**: 냉장고요리 탭의 같은 성격 컨트롤은 높이 **40px**,
    선택 시 **검정(#1A1A1E)** 인데, 요즘인기의 기간 칩은 높이 **28px**,
    선택 시 **회색(#6A6A73)** 이었음 → 이 화면만 다른 앱처럼 보이던 실제 정체
  - **라벨이 없고 오른쪽 끝에 정렬**돼 있어 무엇을 제어하는지 알 수 없었음
  - 17차에서 넣은 구분 밴드가 **첫 섹션 앞에도** 붙어, 컨트롤과 콘텐츠 사이를 끊었음
- 조치
  - 칩 규격을 냉장고요리와 동일하게(높이 40 / radius 6 / 선택 시 잉크색)
  - `기간` 라벨을 붙이고 왼쪽 정렬
  - 구분 밴드는 **첫 번째로 그려지는 섹션에는 붙이지 않도록** 함.
    밴드는 "앞 내용과 나뉜다" 는 뜻이라 맨 위에 오면 컨트롤을 갈라놓음
- 부수 정리: `특별한 날` 목록 계산이 JSX 안 즉시실행 함수에 들어 있어 (a) 렌더마다
  다시 돌고 (b) 바깥에서 이 섹션이 그려지는지 알 수 없었음 → `useMemo` 로 끌어올림
  (첫 섹션 판정에 필요했음)

### 로그인 세션 · 모달 층위 버그 수정

#### 소셜 로그인 시 닉네임 변경이 "유효하지 않은 토큰입니다" 로 실패하던 문제
- **먼저 확인한 것**: 백엔드는 소셜 로그인 사용자의 닉네임 변경을 **막지 않는다.**
  `provider='kakao'` 로 서명한 토큰으로 `/api/auth/update-profile` 을 직접 호출해
  200 응답을 확인함. (비밀번호만 `provider == 'local'` 일 때 반영되고, 프론트도
  소셜 로그인이면 비밀번호 입력을 이미 숨기고 있음)
- **실제 원인**: 토큰 자체가 서버에서 거부되고 있었음
  - `JWT_SECRET_KEY` 가 없으면 `app.secret_key = secrets.token_hex(32)` 로 떨어지는데,
    이 값은 **서버가 기동할 때마다 새로 만들어진다.** 즉 백엔드를 재시작하면
    이미 발급한 로그인 토큰이 전부 무효가 됨
  - 그런데 프론트는 토큰을 서버에 확인하지 않고 payload 만 base64 로 풀어 화면을 그린다.
    → **죽은 토큰을 들고도 로그인 상태로 보이고**, 인증이 필요한 동작에서만 실패
  - 사용자 입장에서는 "소셜 로그인이라 닉네임을 못 바꾸나?" 로 보였음
- 조치
  - `GET /api/auth/me` 신설 — 토큰 유효성 확인 + 최신 사용자 정보 반환.
    계정이 삭제된 경우(탈퇴)도 401 로 처리
  - 앱을 열 때 이 엔드포인트로 세션을 확인하고, 401 이면 조용히 로그아웃 처리.
    네트워크 오류는 로그아웃 사유로 삼지 않음(끊겼다고 로그아웃시키면 더 불편)
  - 프로필 저장에서 401 을 받으면 백엔드 문구 대신
    **"로그인이 만료되었어요. 다시 로그인해 주세요."** 를 띄우고 로그인 화면으로 보냄
  - `JWT_SECRET_KEY` 가 비어 있으면 서버 기동 시 경고 출력 (원인을 찾기 매우 어려운 형태라서)

#### 회원탈퇴 확인 팝업이 '내 정보 수정' 뒤에 숨던 문제
- 원인: `z-[calc(var(--z-modal) + 1)]` — **Tailwind 의 임의값(`[]`)은 공백을 포함할 수 없다.**
  공백 때문에 클래스가 아예 생성되지 않아 z-index 가 `auto` 였고,
  부모인 '내 정보 수정' 모달(z-index 600) 뒤로 밀렸음
  - 실측: 확인 팝업을 띄운 뒤 화면 한가운데를 hit-test 하면
    확인 팝업이 아니라 **정보수정 모달의 입력칸**이 잡혔음 (= 눌리지도 않는 상태)
- 조치
  - z 토큰에 `--z-modal-dim-nested: 640`, `--z-modal-nested: 650` 추가
    (토스트 700 아래라 토스트는 여전히 최상단)
  - 공용 `Dialog` 에 `nested` 옵션 추가 — 같은 z-index 끼리 DOM 순서로 이기는 방식은
    부모 모달이 어디에 렌더되느냐에 따라 깨지므로, 한 층 위로 올려 순서와 무관하게 만듦
  - 회원탈퇴 확인 팝업을 `<Dialog nested>` 로 교체 (규격도 함께 통일)
- 전수 점검: 나머지 고정 오버레이 12곳은 모두 z 토큰을 정상적으로 쓰고 있었고,
  공백이 들어간 Tailwind 임의값도 이 한 곳뿐이었음

#### 운영 API 주소가 프론트 자신을 가리키던 문제
- `frontend/.env.production` 의 `VITE_API_BASE_URL` 이 **프론트 자신의 주소**
  (`https://refrigerator-code.vercel.app`)로 되어 있었음. `vercel.json` 이 모든 경로를
  `index.html` 로 돌려보내므로 이 값으로 빌드되면 모든 API 호출이 HTML 을 받는다
  (실측: 해당 주소로 `POST /api/auth/update-profile` → **405**, JSON 아님.
  올바른 Railway 주소 → 401 JSON)
- Railway 주소로 수정. 점 없는 `env.production` 은 Vite 가 읽지 않는 문서용 사본이라
  이 사실을 파일 주석에 남김

### 쿠팡 광고 구조 재설계 (파트너스 정책 반영)
파트너스 이용 가이드(2024.12)를 확인한 뒤, 처음 잡았던 계획 중 **두 가지를 바꿨다.**

#### 바꾼 것 ① 고지 문구를 목록 **위**로 (원래 계획: 페이지 최하단 유지)
- 가이드 STEP 7 "경제적 이해관계 표시": 문구는 **"게시물의 제목 또는 첫 부분에 기재"**,
  "소비자가 쉽게 인식할 수 있도록", "모든 게시물에" 게재
- 기존에는 화면 맨 아래 1회였는데, 스크롤을 끝까지 내리지 않으면 보이지 않아
  "쉽게 인식" 요건을 만족한다고 보기 어려웠음 → 광고가 나오기 **전**, 목록 위쪽으로 이동
  (요즘인기 / 냉장고요리 / 즐겨찾기·기록·완료 목록 / 재료 상세 전부)
- 광고 카드와 구매 시트 안에도 짧은 문구를 함께 넣어, 그 화면만 보고 지나가도 보이게 함

#### 바꾼 것 ② 부족 재료 pill 을 눌러도 **바로 쿠팡으로 보내지 않는다**
- 가이드 STEP 7 "자동실행 금지": 광고 클릭은 **"사용자가 그 클릭을 의도했을 경우에만
  발생할 것"** 을 전제로 하고, 의도에 반하는 클릭 유도를 금지
- pill 은 화면에서 "이 레시피에 없는 재료" 를 알려주는 **정보 표시**로 보인다.
  누르는 사람은 재료 설명이나 대체재를 기대하지 광고 클릭을 의도했다고 보기 어려움
  → `CoupangAdSheet` 신설. pill 을 누르면 **광고를 보여주기만** 하고,
    쿠팡으로 나가는 클릭은 사용자가 시트 안에서 직접 하게 함
- (직전 대화에서 "pill 은 이미 광고를 띄우고 있다" 고 적었는데 사실이 아니었음.
  실제로는 `window.open` 으로 곧바로 이동하고 있었다)

#### 계획대로 진행한 것
- **레시피 카드 하단 CTA 제거**: 부족 재료 pill 과 같은 재료·같은 목적지라 중복이었고,
  CTA 유무에 따라 카드 높이가 달라져 가로 캐러셀의 고정 높이를 쓸 수 없었음
- **`CoupangAdCard` 신설** — 레시피 카드와 **같은 규격(300×280)** 의 광고 카드를
  목록 사이에 끼운다
  - 바로 앞 레시피의 부족 재료가 **1~3개**일 때만 (0개는 광고할 재료가 없고,
    4개 이상은 "몇 개만 사면 완성" 이 아니라 구매로 이어지지 않음)
  - 재료 선택은 레시피 id 기반이라 다시 그려도 결과가 바뀌지 않음
    (매번 달라지면 스크롤할 때마다 광고가 바뀌어 어수선함)
  - 첫 2장 뒤부터, 광고 사이 최소 4장 간격
  - 연결할 상품이 없으면 카드를 아예 렌더하지 않음 → 빈 카드가 남지 않음
  - 광고임을 카드 첫 줄에 명시(`광고` 배지 + `쿠팡 파트너스`)
- **마이페이지는 광고 카드를 끄기**(`showAds={false}`) — 즐겨찾기·기록·완료는 사용자가
  직접 모아 둔 목록이라 광고가 섞이면 목록의 성격이 흐려짐
- 클릭 측정에 `feed_card` 경로 추가 (기존 `pill` / `card_cta` 와 비교 가능)

#### 정책상 함께 확인한 것
- **커버형 배너 금지**: 광고 위에 "밀어서 확인" 류 문구나 조작 연상 요소를 덧씌우지 않음.
  캐러셀 좌우 화살표는 광고 이전부터 있던 목록 이동 장치이고 모든 카드에 동일 적용되며,
  세로 위치가 달라 광고 카드의 구매 버튼을 덮지 않음
- **플로팅 배너 금지**: 쿠팡 광고 중 화면에 고정(`position: fixed`)되는 것은 없음
- **지식재산권**: 쿠팡 로고/BI 이미지는 쓰지 않고 상호명만 글자로 표기

#### 곁들여 고친 카드 높이 문제
- 가로 카드의 `대체 가능` 줄이 길어지면 카드가 세로로 늘어나, 실측 높이가
  **228~324px** 로 벌어져 있었음. 목록이 잡아 둔 슬롯은 280px 이라
  **324px 짜리 카드는 아래가 잘려 나가고 있었음**
- 가로 목록은 훑어보는 자리이므로 대체재는 대표 1개만 보이고 나머지는 `외 N개` 로 표시
  (세로 목록·상세는 그대로 전부 표시)
- 결과: 가로 카드 높이 **228~280px**, 슬롯을 넘는 카드 없음 → 잘림 해소

### 피드백 반영 19차: 고지문 위치 재조정 · 기간 컨트롤 · 매칭률 설명

#### 고지문을 페이지 상단 → **광고 소재 자체**로
- 18차에서 정책(가이드: "제목 또는 첫 부분")에 맞춰 페이지 맨 위로 올렸는데,
  **광고가 하나도 없는 화면에서도 문구가 먼저 보여 서비스 전체가 광고처럼 읽혔다.**
  광고 없는 목록 위에 "수수료를 제공받습니다" 가 떠 있는 것은 정확하지도 않다
- → 광고가 실제로 나오는 자리마다 붙인다: 광고 카드 / 구매 안내 시트 / 하단 배너.
  각각의 **첫 부분**(구매 버튼보다 위)에 두므로 "누르기 전에 알 수 있어야 한다" 는
  취지는 그대로 지켜진다
- 문장은 `CoupangDisclaimer` 한 곳에서만 관리 (세 자리가 따로 문구를 갖고 있지 않게)
- `BottomCoupangAd`(다이나믹 배너)에도 문구를 붙임 — 예전엔 페이지 단위 고지문에
  기대고 있어서, 상단 고지문을 걷어내면 이 배너만 고지 없이 남을 뻔했음

#### 기간 컨트롤이 어중간해 보이던 문제
- 좌측 정렬만으로는 **가운데 정렬된 제목과 축이 어긋나** 어중간했고,
  바로 아래 섹션과 붙어 어디까지가 컨트롤인지 불분명했음
- → 옅은 판(`--surface-sub` + 1px 테두리) 위에 얹어 한 덩어리로 묶고,
  아래 섹션과의 간격을 20 → **36px** 로 넓힘. 칩 높이는 40 → 38

#### 매칭률이 어떻게 계산되는지 밝히기
- 매칭률은 이 앱의 핵심 지표인데 **어떻게 나온 숫자인지 어디에서도 말하지 않고 있었음**
- 넣을 자리로 스플래시 화면을 검토했으나, 첫 실행에 한 번 스쳐 지나가는 화면이라
  정작 궁금해지는 시점에는 이미 사라진 뒤다 → **`재료 매칭도 설정` 팝업 안**에 배치.
  사용자가 이 숫자를 만지러 오는 바로 그 자리라 맥락이 맞다
- 문구: "매칭률은 레시피 본문에서 뽑아낸 재료와 내 냉장고 재료를 비교해 계산해요.
  재료는 AI가 본문을 읽고 정리하기 때문에, 글쓴이가 적지 않은 재료는 빠질 수 있어요."
  (한계까지 함께 밝혀야 매칭률이 어긋날 때 사용자가 납득할 수 있다)

### 피드백 반영 20차: 카드 높이 통일 · 내부 표현 제거 · 매칭도 팝업 정리

#### 가로 카드 높이를 전부 같게
- 광고 카드에 고지 문구가 들어가면서 광고 카드(280px)와 레시피 카드(228~280px)의
  높이가 서로 달라 보였음. 그런데 원인은 광고 카드가 아니라 **레시피 카드 쪽**이었다 —
  재료 pill 이 몇 줄로 감기느냐에 따라 카드마다 높이가 제각각이었고,
  목록이 잡아 둔 슬롯(280px)과도 어긋나 있었음
- `RecipeCard` 에 `fixedHeight` 를 추가해 **가로 목록에서는 슬롯 높이와 카드 높이를 일치**시킴.
  내용이 적은 카드는 아래가 남지만, 높이가 들쭉날쭉한 것보다 훨씬 정돈돼 보인다
- 검증: 실측 123장 전부 **280px 단일값** (이전 228~280), 광고 카드도 280px

#### 광고 카드에서 "앞 레시피에 부족한 재료" 문구 제거
- 이 문장은 우리가 그 재료를 고른 **내부 규칙**을 드러낸 것이지 사용자에게 필요한 정보가 아님
- 사용자에게 필요한 건 "무엇에 대한 광고인가" 하나뿐 → 재료명만 남기고,
  위아래 여백을 똑같이 나눠 카드 가운데에 배치 (아래로만 밀면 사이가 텅 비어 보였음)

#### 재료 매칭도 팝업이 답답하던 문제
- 실측한 원인
  - **라벨이 하나도 없었음**: `30 % ~ 100 %` 두 칸이 무엇을 정하는지,
    아래 선택지가 무엇을 고르는 건지 화면에 적혀 있지 않았음
  - **두 설정이 붙어 있었음**: 슬라이더와 선택지 사이 **0px**,
    선택지와 적용 버튼 사이 **8px** → 서로 다른 설정이 한 덩어리로 보였음
  - **기본 라디오 버튼**: 지름 13px 남짓이라 누르기 어렵고, 브라우저 기본 파란색이라
    앱의 다른 컨트롤과 색이 따로 놀았음
- 조치
  - `매칭률 범위` / `부족해도 되는 재료` 소제목 추가
  - 두 설정 사이에 구분선 + 상하 22px 여백
  - 라디오 → 38px 칩 토글(앱의 다른 칩과 동일 규격), 선택 시 잉크색 반전
  - 적용 버튼 위 간격 8 → 24px, 높이 48px 로 통일
- 검증: 요소 간 간격 `15/16/10/14/22/22/9/24` (이전 `16/16/0/8/8`)

### 피드백 반영 21차: 임박 재료 설정 팝업

#### 추정 유통기한이 반영된다는 사실을 밝힘
- 질문: "짐작한 유통기한 데이터도 '유통기한 임박순' 계산에 들어가나?" → **들어간다.**
  18차에서 `RecipeSortBar` / `RecipeList` 의 임박 재료 목록에 `estimatedExpiry` 를 포함시켰음
  (직접 넣은 것만 세면 임박 재료가 거의 항상 비어 기능이 동작하지 않았기 때문)
- 그런데 화면에는 그 사실이 적혀 있지 않아, 넣은 적 없는 재료가 목록에 떠 있으면
  "내가 언제 이걸 입력했지?" 가 됨 → 팝업 상단에 도움말 추가
  "유통기한을 직접 넣은 재료뿐 아니라 **구매일만 넣어 둔 재료**도 재료 종류와 보관 방법으로
   기한을 짐작해 함께 계산해요. 짐작한 값은 **약 D-00** 으로 표시돼요."
- 도움말 상자는 매칭도 팝업과 같은 규격(`STYLES.helpBox`)으로 통일

#### 날짜 표기를 D-표기로
- `구매일 오래된순` 탭이 `2026.08.12` 같은 **절대 날짜**를 그대로 보여주고 있었음.
  며칠 지났는지 머릿속으로 빼야 해서 한눈에 안 들어옴 → 두 탭 모두 D-표기로 통일
  (구매일은 지난 날짜라 `D+10` 처럼 나옴)
- **기한이 지난 재료는 `지남` 으로 못박음**: 같은 `D+1` 이라도 유통기한 탭에서는
  "1일 지남", 구매일 탭에서는 "산 지 1일" 이라 뜻이 정반대여서 혼동됨
- `getDDay` 가 `yyyy.mm.dd`(점 표기)를 확실히 읽도록 대시로 정규화.
  브라우저에 따라 Invalid Date 가 되어 날짜가 그대로 노출될 수 있었음
- 검증(실측): 임박순 `지남 / D-2 / 약 D-2 / 약 D-6 / 약 D-20 / 약 D-115`,
  구매일순 `D+10 / D+8 / D+5 / D+3` — 정렬과 표기 모두 정상

#### AND/OR 선택을 칩 토글로
- 매칭도 팝업과 같은 문제(13px 기본 라디오 + 브라우저 기본 파란색)가 남아 있었음
  → 38px 칩 토글로 교체하고 `선택한 재료를` 소제목 추가.
  문구도 `모두 포함(AND)` → `모두 포함` 으로 줄임 (AND/OR 표기는 개발 용어)

### 피드백 반영 22차: 내냉장고 키보드 · 고지문 정리

#### 재료 선택 후에도 키보드가 안 내려가던 문제
- 이전에 "팝업을 열기 전에 포커스를 풀어 키보드를 내린다" 는 처리를 넣었는데도
  증상이 그대로였음. 코드를 보니 `blur()` 바로 **다음 줄에서 `inputRef.current?.focus()`**
  를 불러 **방금 내린 키보드를 도로 올리고 있었다.**
  (다음 재료를 이어서 입력하라고 넣은 코드인데, 팝업이 뜨는 흐름에서는 키보드가 팝업을 가림)
- 조치
  - `handleSelect` 끝의 `focus()` 제거 — 다음 입력을 위한 포커스는 팝업을 닫은 뒤에 준다
  - 드롭다운 항목의 `onClick` → `onMouseDown` 으로 변경.
    `onClick` 은 입력창의 `onBlur` 뒤에 실행돼 순서 경합이 생긴다
- 검증(실측): 선택 전 `document.activeElement = INPUT` → 선택 후 `BODY`,
  팝업 정상 표시 (포커스가 풀렸으므로 모바일에서 키보드가 내려감)

#### 광고가 없는데 고지문만 남던 문제
- 내냉장고 화면 아래에 "이 광고는 쿠팡 파트너스 활동의 일환으로…" 만 덩그러니 있었음
- 원인: 이 자리는 **다이나믹 배너**(`BottomCoupangAd`) 자리인데,
  `VITE_COUPANG_AD_UNIT_ID` 가 설정돼 있지 않아 배너 자체는 아무것도 그리지 않는다.
  그런데 고지 문구는 감싸는 쪽이 그리고 있어서 문구만 남았음
- → 광고 단위 ID 가 없으면 고지문을 포함해 통째로 렌더하지 않도록 수정.
  (광고가 없으면 고지할 것도 없다)

#### 고지 문구 줄바꿈
- 폭에 따라 아무 데서나 감기던 것을 쉼표 뒤에서 끊어 두 줄로 고정
  `이 광고는 쿠팡 파트너스 활동의 일환으로,` / `이에 따른 일정액의 수수료를 제공받습니다.`
- 문구가 `CoupangDisclaimer` 한 곳에만 있으므로 광고 카드·구매 시트·배너에 모두 반영됨

### 필터 시트: '꼭 제외할 재료' pill 간격
- 선택한 재료 pill 이 입력창에 바짝 붙어 나왔음. `꼭 포함할 재료` 입력창에는 `mb-2` 가
  있는데 `꼭 제외할 재료` 쪽만 빠져 있었음 → 동일하게 맞춤
- 검증(실측): 입력창~pill 간격 포함 8px / 제외 8px (이전 8 / 0)

### 피드백 반영 23차: 바텀시트 드래그 민감도 · 상단 여백

#### 시트가 너무 쉽게 닫히거나 붕 뜨던 문제
- 원인: 터치 핸들러가 **시트 전체**(`div[role="dialog"]`)에 걸려 있었음.
  그래서 본문 목록을 스크롤하려는 손짓까지 "시트를 끄는 동작" 으로 읽혀,
  필터처럼 내용이 긴 시트에서 목록을 내리려다 시트가 통째로 닫히거나 위로 떴음
- 조치: 드래그를 **두 갈래로 분리**
  - 손잡이·헤더에서 시작 → 언제나 끌기 (펼치기/닫기 모두)
  - 본문에서 시작 → **맨 위까지 스크롤된 상태에서 아래로 당길 때만** 끌기.
    위로 올리는 손짓은 "내용을 더 보겠다" 는 뜻이므로 스크롤에 양보
  - 14px 데드존 추가 — 손 떨림이나 스크롤 시작 동작에 시트가 따라 움직이지 않게
- 함께 고친 잠재 버그: 닫기 판정이 `dragY`(state)를 보고 있었는데,
  터치 이벤트가 한 프레임에 몰리면 리렌더 전이라 옛 값(0)을 읽어
  **빠르게 쓸어내려도 안 닫히는** 경우가 있었음 → 판정은 ref, 화면 표시는 state 로 분리
- 검증(iPhone 13 에뮬레이션, 실측)
  - 본문을 스크롤한 상태에서 아래로 쓸기 → 시트 유지 ✅
  - 손잡이에서 아래로 끌기 → 닫힘 ✅
  - 손잡이에서 위로 끌기 → 높이 544 → 631px 로 펼쳐짐 ✅

#### 시트 첫 항목이 헤더 구분선에 붙던 문제
- 본문 영역의 `padding` 이 `0 20px 16px` 로 **위쪽이 0** 이라,
  `채널 · 고르지 않으면 전체` 같은 첫 항목이 헤더 아래 선에 바짝 붙어 있었음
- → `18px 20px 16px` 로 위 여백 확보 (모든 바텀시트에 함께 적용)

### 필터: 같은 재료를 포함·제외 양쪽에 넣을 수 있던 문제
- `가지` 를 `꼭 포함할 재료` 에 넣고 `꼭 제외할 재료` 에도 넣을 수 있었음.
  그러면 조회 조건이 `가지가 있고 AND 가지가 없고` 가 되어 **결과가 항상 0건**이 되는데,
  사용자는 왜 아무것도 안 나오는지 알 길이 없었음
- 조치: 애초에 고를 수 없게 막는다
  - 자동완성 후보에서 **반대쪽에 이미 고른 재료도 함께 제외**
  - 자동완성 후보가 없을 때 입력값이 그대로 들어가던 Enter 경로도 차단하고,
    `'가지' 은(는) 제외 재료로 골라 뒀어요. 한 재료를 포함과 제외에 함께 넣으면
    결과가 하나도 나오지 않아요.` 안내를 3.5초간 표시
- 검증(실측): 포함에 `가지` 선택 → 제외 후보 목록에서 `가지` 사라짐(후보 0개),
  Enter 로 강제 추가 시도 → 안내 표시 + pill 추가 안 됨

### 쿠팡 광고 대상 재료 순위 추출
- `coupang_ads.csv` 를 **레시피 사용 빈도 기준**으로 다시 만듦 (44,471건 집계)
- 대상에서 뺀 것
  - 앱이 새 사용자에게 기본으로 채워 주는 재료 31종(소금·설탕·간장·마늘·양파 등)
    — 대부분 이미 갖고 있어 "부족 재료" 가 될 일이 드물다
  - 물·얼음처럼 살 대상이 아닌 것
- 상위 120종 + 기존 항목 = 194행. **이미 채워 둔 URL 은 덮어쓰지 않음**
- 참고용 컬럼 `recipe_count`, `rank` 추가 — 어느 재료부터 채우면 되는지 보이게.
  파서는 컬럼을 헤더 이름으로 찾으므로 컬럼이 늘어도 동작에 영향 없음
- `coupang_url` 이 빈 행은 파서가 건너뛰므로, 링크를 안 채운 재료는 광고가 뜨지 않음
- 광고 CSV 캐시 버전 1.0 → 1.1 (브라우저에 남은 옛 목록 무효화)
- 상위 10: 다진마늘 11,678 / 파 11,540 / 고추 10,332 / 참깨 6,913 / 액젓 6,223 /
  청양고추 5,929 / 가지 4,758 / 황다랑어 4,633 / 육수 4,585 / 오이 4,306

### AI 챗봇 검색 품질 개선 (A + B)

#### A. 키워드를 정렬에 반영 (가장 큰 원인)
- 예전: 냉장고에 재료가 있으면 정렬이 `match_rate DESC` 뿐이었고, 키워드는
  `HAVING (match_rate >= 25 OR 키워드일치)` 의 **OR 조건**이었음.
  매칭률 25% 이상이 13,869건이라 사실상 전부 통과했고, 정렬에 키워드가 안 들어가
  **"매운 거" 라고 말하든 말든 결과가 거의 같았음**
- 바꾼 것
  1. 키워드가 있으면 **관련도 0인 글은 제외** (OR → 필수 조건)
  2. 어디에서 걸렸는지에 따라 점수 차등: **제목 3 / 재료 2 / 본문 1**
     (제목에 있으면 그 주제를 다룬 글, 본문에 스친 것은 "안 맵게 하려면" 일 수도 있음)
  3. 정렬을 `관련도 × 10 + 매칭률` 로 — 주제가 맞으면서 만들 수 있는 것이 위로

#### B. 낱말 1개 → 여러 개 + 동의어 확장
- 예전에는 LLM 이 낱말 하나만 뽑아서, `매운` 으로 찾으면
  `얼큰`(1,606건) `칼칼`(3,317건) `청양`(9,710건) 이 쓰인 글을 통째로 놓쳤음
- 프롬프트를 바꿔 **1~5개**를 뽑게 하고, 같은 뜻으로 글에 쓰일 만한 말을 함께
  달라고 지시 (`매운 거` → 매운/매콤/얼큰/칼칼/청양고추).
  **LLM 호출 수는 그대로** — 한 번의 응답에서 배열로 받는다
- 옛 형식(`keyword` 단수)으로 답하는 경우도 함께 받아 준다

#### 속도 대책
- 본문(content)은 평균 2,000자라 LIKE 비용이 큼. 실측:
  낱말 5개를 전부 본문까지 보면 **4.93초**, 제목·재료만 보면 **0.32초**
- → **첫 번째 낱말만 본문까지** 보고 나머지는 제목·재료에서만 찾는다.
  첫 낱말이 가장 중요한 말이라 손해가 적음. 실측 **1.75~2.42초**

#### 결과 (실측, 냉장고 재료 12종 기준)
| | 상위 8건 중 관련 |
|---|---|
| 예전 방식 | **1건** |
| 새 방식 (낱말 1개) | 7건 |
| 새 방식 (동의어 5개) | **8건** |
| "국물 요리" | **8건** |

#### ⚠️ 확인된 운영 문제: Gemini 무료 한도를 배치와 챗봇이 나눠 씀
- 종단 테스트 중 `/api/chat` 이 LLM 실패 경로로 빠지는 것을 발견.
  Gemini 를 직접 호출해 보니 **429 — 무료 티어 일일 500회 초과**
- 원인: 재료 추출 배치가 하루 450회를 쓰도록 잡혀 있어 챗봇 몫이 50회뿐이고,
  그마저 소진된 상태. **둘이 같은 API 키를 쓴다**
- 검색 로직 자체는 `_search_recipes` 직접 호출로 검증했으나,
  LLM 이 낱말을 뽑아 주는 구간은 한도가 회복되는 내일 재검증 필요

### 내냉장고: 사진 인식 준비 중 안내 추가
- 검색창 옆에 카메라 버튼 추가 (영수증 버튼과 같은 40×40 규격).
  누르면 `재료 사진을 찍으면 알아서 인식해 담아 주는 기능을 준비하고 있어요.` 안내
- 영수증 버튼도 `alert` → 같은 토스트로 통일.
  `alert` 는 OS 창이라 앱 밖의 일처럼 보이고 확인을 눌러야만 사라짐
- 안내에 `showToast` 를 쓰지 않은 이유: 그 토스트는 '삭제 취소' 버튼이 함께 붙는
  형태라 단순 안내에 맞지 않음 → `showPrepNotice` 신설(3초 후 자동 사라짐)

### 문서 갱신: 요리 챗봇 · LLM 키 분리
- `PROJECT_OVERVIEW.md` 에 **요리 챗봇** 절 신설
  - 동작 순서(① 대화+재료 → ② LLM 이 낱말만 고름 → ③ 서버가 MySQL 검색 → ④ 응답)
  - **LLM 은 레시피 본문을 읽지 않는다**는 점을 명시. 답변은 검색 전에 만들어지므로
    "이 레시피는 이래서 추천해요" 같은 말은 구조상 불가능
  - 검색 규칙(제목 3 / 재료 2 / 본문 1, 관련도 필수 조건, `관련도×10 + 매칭률` 정렬)
  - 낱말 확장(1~5개, LLM 호출 수는 그대로)
  - 속도 실측표와, FULLTEXT 인덱스가 ngram 파서 없이 만들어져 한국어에 쓸 수 없다는 사실
- `ENVIRONMENT_SETUP.md` 에 **LLM 키 분리** 절 신설
  - 무료 한도 500회는 **API 키 단위**라 배치와 챗봇이 공유하면 챗봇 몫이 50회뿐
  - `GEMINI_API_KEY_CHAT` 을 넣으면 챗봇만 그 키를 씀 (없으면 기존처럼 공용)
  - 키 발급 절차 — **반드시 `Create API key in new project`** 로 만들어야 별도 한도를 받음
- `chat_service.py` 가 `GEMINI_API_KEY_CHAT` / `GROQ_API_KEY_CHAT` 을 먼저 보도록 수정.
  설정하지 않으면 동작이 전과 같으므로 지금 당장 넣지 않아도 문제 없음

### 챗봇 전용 LLM 키 적용 확인 · 내냉장고 입력 영역 정리

#### 챗봇 전용 키가 실제로 동작함을 확인
- `GEMINI_API_KEY_CHAT` 신규 키(새 프로젝트로 발급) → **정상 응답**
  (기존 `GEMINI_API_KEY` 는 배치가 다 써서 여전히 429 — 의도한 분리가 그대로 작동)
- **종단 검증**(격리 프로세스에서 `handle_chat` 직접 호출)
  - "매운 거 먹고 싶어" → 낱말 `['매운','매콤','얼큰','칼칼','고추장']`,
    결과 4건 전부 매운 요리 (매운 감자조림 / 매운 고추장 감자조림 …)
  - "따뜻한 국물 요리 추천해줘" → 낱말 `['국물','찌개','탕','전골','국']`,
    결과 4건 전부 국물 요리
  - 어제 한도 소진으로 확인하지 못했던 **LLM 의 동의어 확장 구간까지 검증 완료**

#### 영수증·카메라 버튼의 톤이 어긋나던 문제
- 영수증만 PNG 이미지(`영수증.png`)였고 카메라는 선으로 그린 SVG 라,
  나란히 두니 선 굵기와 농도가 달라 보였음
- → 영수증도 같은 규격(`stroke-width: 1.7`)의 선 아이콘으로 교체. 이제 둘 다 SVG
- 쓰이지 않게 된 `영수증.png` import 제거

#### 안내 토스트 글자가 잘리던 문제
- `white-space: nowrap` + 말줄임이라 안내문이 통째로 잘려 무슨 말인지 알 수 없었음
- → 줄바꿈 허용(`wordBreak: keep-all`), 폭을 `min(320px, 100vw - 40px)` 로.
  실측: 높이 120px, 잘림 없음

#### 입력창이 눌려 플레이스홀더조차 안 보이던 문제
- 한 줄 폭 360px 에 입력창 250 + 입력버튼 70 + 아이콘 40×2 + 여백 = **424px** 이라
  입력창이 밀려 있었음 (카메라 버튼이 추가되면서 더 심해짐)
- → 입력창을 `flex: 1 1 auto` 로 바꿔 남는 폭을 다 갖게 하고,
  입력 버튼 여백 18→14px, 아이콘 버튼은 `flexShrink: 0` 으로 고정
- 문구도 `추가할 재료명을 입력하세요` → `재료명을 입력하세요` 로 축약
- 검증(실측): 입력창 내부 폭 148px / 문구 필요 폭 120px → **다 보임**

#### 자동완성 목록에 더 있다는 것이 안 보이던 문제
- 모바일은 스크롤바가 **만졌을 때만** 잠깐 나타나는 방식이라, 손대기 전에는
  아래에 더 있다는 사실을 알 수 없었음 (CSS 로 상시 표시를 시도해도 OS 방식이 이김)
- → 목록 오른쪽 아래에 `72개 더 ⌄` 알약 표시. 끝까지 내리면 사라짐
- 만드는 과정에서 걸린 것 두 가지
  - 목록에 `position: absolute` 가 남아 있어 감싼 요소의 높이가 0이 되고
    표시가 목록 **위쪽**에 붙었음 → `relative` 로 변경
  - 목록에 `z-[10]` 이 걸려 있어 표시가 목록 **뒤로 깔려** 보이지 않았음 → `zIndex: 11`
  - 처음엔 흐려지는 띠(gradient)도 깔았는데 마지막 항목 글자가 씻겨 나가 읽기 어려워짐
    → 띠는 걷어내고 알약 표시만 남김. 위치도 가운데 → 오른쪽으로 옮겨 글자를 가리지 않게

### 챗봇 결과 다양성 · 기본 재료에 D-표기 예시

#### 같은 요리만 늘어서던 문제
- "매운 거 먹고 싶어" 결과 8건이 **전부 감자조림**이었음.
  레시피 글은 같은 요리를 여러 사람이 쓴 경우가 많아, 관련도와 매칭률이 비슷한 것들이
  한 요리로 몰린다. 같은 요리만 나오면 "추천" 으로서 값이 없다
- 조치: **한 요리는 최대 2건까지**(`MAX_PER_DISH = 2`)
  - 제목에서 요리 이름을 알아내는 데 재료 사전의 `대분류 = 요리이름` 994개를 씀.
    제목이 `매운 감자조림 만드는법 고기없이도 짱맛! 고추장 양념 …` 처럼 검색어 범벅이라
    사전 없이 글자만 비교해서는 같은 요리를 골라내기 어려움
  - **긴 이름부터** 대조해 `감자조림` 을 `조림` 으로 잘못 잡지 않게 함
  - 넘치는 것은 버리지 않고 뒤로 미뤄, 개수가 모자라면 그것으로 채움
    (결과가 줄어드는 것보다 겹치더라도 채우는 편이 낫다)
  - SQL 은 `SEARCH_LIMIT × 5` 로 넉넉히 가져와 걸러냄
    (WHERE/HAVING 스캔이 비용의 대부분이라 LIMIT 을 늘려도 속도 차이 없음 — 실측 2.2~2.5초로 동일)
  - 사전 파일을 못 찾아도 검색이 멈추지 않도록, 그때는 다양성 필터만 건너뜀
- 검증(실측)
  - `매운` → 감자조림 2 / 알감자조림 1 / 찜닭 1 / 고추장찌개 1 / 달걀조림 1 / 닭볶음탕 1 / 기타 1
  - `국물` → 닭볶음탕 2 / 김치찌개 2 / 닭도리탕 1 / 참치김치찌개 1 / 계란탕 1 / 기타 1
  - 실제 챗봇 화면에서도 감자조림 2건 뒤로 알감자조림·고추장찜닭·감자 고추장찌개가 이어짐

#### 기본 재료 두 가지에 구매일 예시
- 처음 앱을 열면 기본 재료 31종이 담기는데 날짜가 하나도 없어,
  **D-표기가 어떤 기능인지 알 수 없었음**
- `달걀`, `감자` 두 가지에만 구매일(= 앱을 처음 연 날)을 넣음 → `약 D-30`, `약 D-21`
- 전부 넣지 않은 이유: 화면이 D-표기로 뒤덮이고, 앱을 오래 안 열면 전부 `지남` 으로
  바뀌어 고장난 것처럼 보임
- 유통기한이 아니라 **구매일**을 넣은 이유: 사용자가 직접 넣지도 않은 유통기한을 채우면
  거짓 정보가 됨. 구매일은 사실이고, 거기서 짐작한 값은 `약 D-30` 처럼 추정임을 밝혀 표시됨
- `달걀`(냉장 30일)·`감자`(21일)를 고른 이유: 한동안 의미 있는 숫자가 보임.
  `우유`(7일) 같은 것은 일주일 만에 `지남` 이 되어 예시로 부적절
- 검증(첫 방문 상태 실측): `달걀 약 D-30`, `감자 약 D-21` 두 개만 표기됨

## 2026-08-27

### AI 챗봇: 광범위 질문 처리 + 조미료 매칭 제외

#### 문제 제기
"있는 재료로 뭐 해먹을 수 있어?" 처럼 넓게 물으면 챗봇이 냉장고 재료를 제대로
반영하지 못하고 "멍청하게" 대답한다는 지적. 원인을 짚어 보니:
- LLM 은 레시피를 직접 고르지 않고 검색용 `keywords` 만 뽑는다(`_build_prompt`).
  그런데 `keywords` 가 하나라도 있으면 `_search_recipes` 의 `HAVING relevance > 0`
  이 강제로 걸려서, 그 낱말과 무관한 관련도 필터링이 우선시되고 정작 냉장고
  매칭(`match_rate`)은 뒷전으로 밀린다.
- 프롬프트엔 "매운 거"/"국물" 같은 **구체적 취향 요청**의 낱말 확장 예시만 있고,
  이런 **광범위 요청**(keywords 를 반드시 비워야 하는 경우)에 대한 규칙·예시가
  없어서, 모델이 "재료"/"추천" 같은 낱말을 지어내 위 필터를 걸어 버릴 위험이 있었음.
- `reply` 는 검색 **전에** 쓰여진다(LLM 은 레시피 본문을 안 읽는 구조). 그래서
  "이 레시피는 이래서 추천해요" 식 답변은 애초에 구조적으로 불가능함.
- 매칭률이 재료 **개수 비율**이라, 소금·후추 같은 조미료도 새우·소고기 같은
  메인 재료와 똑같은 가중치를 가진다 — "재료 우선순위를 모르는 것 같다"는
  두 번째 지적의 실체.

프롬프트만으로는 세 번째(구조적 reply 한계)와 네 번째(매칭률 가중치)는 해결이
안 돼서, 아이디어를 여러 개 늘어놓고 사용자와 상의해 아래 3가지 조합으로 진행:
**A2(패턴으로 광범위 질문 감지) + B4(검색 후 템플릿 답변) + C6(조미료 매칭 제외, 가벼운 버전)**.

#### 조치
1. **`_is_broad_fridge_request()` 신설** — "뭐 해먹", "뭐 만들", "추천해줘" 류의,
   취향·재료·요리명 없는 순수 광범위 질문을 정규식으로 감지한다. 오탐(구체적
   요청을 광범위로 오판)이 더 위험하므로 패턴을 일부러 좁게 잡았다 — 여기 안
   걸리는 표현은 그냥 기존처럼 LLM 이 처리한다(기능 후퇴 없음).
2. 광범위 질문으로 판정되면 **LLM 호출 자체를 건너뛰고** `keywords=[]` 로 확정해
   순수 냉장고 매칭(`match_rate DESC`) 검색으로 보낸다. 호출을 안 하니 하루
   LLM 한도(`_consume_quota`)도 쓰지 않는다.
3. 검색이 끝나 **결과를 알고 난 뒤**, 서버가 템플릿으로 답변 문구를 짓는다
   ("냉장고에 있는 재료 기준으로 만들 수 있는 레시피 N개를 찾았어요..."). LLM 경로의
   "검색 전에 답변부터 써야 하는" 구조적 한계를, 이 경로에 한해 결과를 본 뒤
   문구를 만드는 방식으로 우회했다.
4. **`COMMON_SEASONINGS`**(소금·후추·설탕·식용유·참기름·들기름·맛술·미림·식초·물)
   10종을 매칭률 계산의 분모·분자 양쪽에서 제외했다(`_search_recipes`, SQL에서
   `REPLACE` 체인으로 해당 재료 토큰만 지운 문자열로 계산). 고추장·된장·고춧가루처럼
   요리 정체성을 결정하는 양념은 일부러 빼지 않음 — "매운" 검색이 고추장 매칭에
   기대는 부분이 있어서, 여기까지 빼면 기존 다양성 검증(8/26)이 흔들릴 수 있음.

#### 검증 (실측, 운영 DB 대상 — 격리 프로세스에서 `handle_chat` 직접 호출)
| 입력 | 보유 재료 | is_broad | LLM 한도 소모 | 결과 |
|---|---|---|---|---|
| "있는 재료로 뭐 해먹을 수 있어?" | 돼지고기,양파,대파,소금,후추,식용유,고추장 | True | 0 (250→250) | 8건, 템플릿 답변 |
| "추천해줘" | 배추,두부,멸치,소금 | True | 0 (250→250) | 8건, 매칭률 100/67/60/50/50% 로 분산 |
| "매운 거 먹고 싶어" | 감자,고추장,대파 | False | 1 (250→249) | 기존처럼 LLM 경유, keywords: 매운/매콤/얼큰/칼칼 |
| "돼지고기로 뭐 해먹을 수 있어?" | 돼지고기,김치 | False | 1 (249→248) | LLM 경유. "감자,소금,후추"만 필요한 레시피가 100% 매칭으로 올라와 조미료 제외 효과 확인 |

#### 범위
챗봇(`backend/chat_service.py`)에만 적용. 냉장고요리·요즘인기 등 다른 화면의
매칭률(`frontend/src/utils/recipeUtils.ts`)은 그대로다. 앱 전체로 확장(사전의
`중분류`를 이용한 정식 가중치 등)은 이번 범위 밖 — 필요하면 별도로 진행.

### AI 챗봇: 답변에 "어떤 재료를 봤는지" 밝히기 + 대화 이어받기

#### 문제 제기
위 개선 이후에도 "다이어트 중인데 뭐먹지?" 같은 질문에 "냉장고에 있는 재료들로
가볍게 만들기 좋은 요리들을 골라보겠습니다" 처럼 뭉뚱그린 답만 온다는 지적.
어떤 재료를 근거로 골랐는지, 어떤 기준으로 정렬했는지가 답변에 없어서였다.
(이 질문 자체는 `_is_broad_fridge_request` 에 안 걸려 정상적으로 LLM 경유 —
"다이어트" 라는 주제가 있는 요청이라 keywords 를 비우면 오히려 손해라 의도한 동작.
문제는 LLM 경로의 reply 라는 게 드러남)
추가로, 이어지는 대화에서 "그중 A는 빼고 B는 넣어서 다시 줘" 처럼 이전 요청을
수정하는 후속 질문에 자연스럽게 이어 답했으면 한다는 요청. (사용자별 하루
질문 한도는 필요하다고 언급했으나, 테스트 단계라 이번엔 반영하지 않음 — 명시적 보류)

#### 조치
1. **`_top_matched_ingredients()` 신설** — 검색 결과 상위권에 실제로 겹친 보유
   재료를 빈도순으로 추린다. `is_broad`/LLM 두 경로 모두, 검색이 끝나 결과를
   안 뒤에 이 재료 이름들을 답변에 붙인다 — "가지고 계신 재료 중 OO, OO 위주로
   골랐고, 매칭률 높은 순으로 정렬했어요." LLM 경로는 LLM이 쓴 문장 뒤에
   덧붙이는 방식이라(LLM은 검색 전에 답변을 써서 이 사실을 알 수 없음), 기존
   장점(자연스러운 톤)은 유지하면서 구체성만 서버가 보강한다.
   - 방금 사용자가 "빼달라"고 한 재료(`exclude_ingredients`)는 이 집계에서도
     제외한다. 안 그러면 "두부 말고 계란으로 줘" 라고 했는데 "두부 위주로
     골랐다"고 답하는 모순이 생김(실제로 테스트 중 발견해서 수정).
2. **대화 이어받기 프롬프트 규칙 추가** — `_build_prompt` 에 "직전에 네(도우미)
   답변이 있으면 새 메시지를 이전 요청에 대한 수정으로 보고, 사용자가 취소하지
   않은 조건은 이어서 반영해라" 규칙 추가. 프론트(`RecipeChatWidget.tsx`)가
   이미 전체 대화(`nextMessages`)를 매 요청마다 보내고 있어서(세션 내 기억
   자체는 기존 구조로 이미 가능), 이번 조치는 그 대화를 "매 턴 새 질문처럼"
   보지 말고 이어서 보라는 프롬프트 지시만 추가한 것.
   - **실측 중 발견한 버그**: 규칙 설명에 든 예시 문장에 구체적 재료명("두부",
     "계란")을 그대로 썼더니, 사용자가 진짜로 "두부 요리 추천해줘" 라고 **첫
     메시지**를 보냈는데도 모델이 예시 속 가상의 "이전 요청"을 실제 대화로
     착각해 엉뚱하게 계란 레시피를 찾아버림(이전 대화가 전혀 없었는데도).
     예시의 재료명을 A/B 같은 자리표시자로 바꾸고, "지금이 첫 메시지면 이
     규칙은 해당 없다"는 문장을 명시적으로 추가해 해결. → **프롬프트에 넣는
     예시가 실제 사용자 입력과 겹치는 낱말을 쓰면, 특히 싼 모델일수록 예시를
     실제 맥락으로 착각할 수 있다**는 걸 확인함.

#### 검증 (실측, 운영 DB 대상 직접 호출)
| 시나리오 | 확인 내용 |
|---|---|
| "다이어트 중인데 뭐먹지?" (닭가슴살,브로콜리,계란,양파 보유) | keywords: 다이어트/저칼로리/식단/건강식. 답변 끝에 "가지고 계신 재료 중 닭가슴살, 양파, 브로콜리 위주로 골랐고, 매칭률 높은 순으로 정렬했어요." 추가됨 |
| "두부 요리 추천해줘" (첫 메시지, 두부/계란/양파/김치 보유) | 버그 수정 전: 계란 레시피로 엉뚱하게 답함. 수정 후: keywords 전부 두부 관련, 그라운딩 문구도 "두부, 김치, 양파" |
| → 이어서 "두부 말고 계란으로 다시 줘" | keywords가 계란/달걀 관련으로 정확히 전환. 그라운딩 문구에서 "두부"가 (요청대로) 빠지고 "양파, 김치"만 남음 |
| 기존 4개 시나리오(광범위 2건 + 구체적 2건) 재검증 | 전부 그대로 통과, 답변에 그라운딩 문구만 자연스럽게 추가됨 — 회귀 없음 |

#### 반영하지 않은 것
사용자별 하루 질문 한도(예: 10회) — 테스트 단계라 이번엔 보류. 실제 운영 전환
시점에 별도로 반영 필요.

### 잡다한 개선: 유통기한 퀵버튼 · 토스트 줄바꿈 · 임박재료 탭 통합 · 챗봇 유통기한 필터

- `IngredientDateModal`: 유통기한 퀵버튼에 3개월/6개월/1년 뒤 추가(기존 3일/1주일/2주일/1개월)
- `RecipeToast`: `multiline` + 좁은 `maxWidth`(260px) 조합 때문에 "레시피를 즐겨찾기에
  추가했습니다!" 같은 한 줄짜리 문구도 줄바꿈돼 두 줄로 보이던 문제 수정
- 냉장고요리 "임박 재료" 모달의 "유통기한 임박순"/"구매일 오래된순" 두 탭을 하나로 통합.
  구매일을 넣으면 그 자리에서 `estimatedExpiry`가 함께 계산·저장되므로(`MyFridge.tsx`),
  구매일이 있는 재료는 이미 유통기한 임박순 목록에도 다 들어와 있었음 — 사실상 같은 걸
  두 기준으로 보여주기만 했던 탭이었다. `expirySortType` 관련 상태·prop을 `RecipeSortBar`와
  이를 쓰는 5개 페이지에서 전부 정리(`RecipeList`/`Popular`/`IngredientDetail`/
  `RecordedRecipeListPage`/`CompletedRecipeListPage`)
- `RecipeChatWidget`: 챗봇에 보내는 냉장고 재료 목록에서 유통기한(직접 입력 또는
  구매일로 짐작한 값)이 지난 재료를 제외 — 못 쓸 가능성이 높은 재료를 "보유 재료"로
  추천에 반영하지 않도록. 매칭률 계산(`recipeUtils.ts`)에는 적용하지 않음 — 요청 범위가
  "LLM 돌릴 때"로 한정돼 있어 앱 전체 매칭 로직까지는 건드리지 않았다

### 재료 매칭률 가중치 (조미료 낮게 / 나머지 재료 높게) — 앱 전체 적용, 성능 제약 발견

#### 요청과 시도
지난번 챗봇 개선 때 "조미료는 매칭 계산에서 아예 뺀다"(0 또는 1의 하드 제외, 10종
하드코딩)로 임시 처리했던 것을, 이번엔 "정식 버전"으로 — 재료 사전의 기존 분류
(`대분류=재료`, `중분류=양념/조미료`, 287개)를 그대로 써서 **완전 제외가 아니라
가중치(SEASONING_WEIGHT=0.3 vs CORE_WEIGHT=1.0)** 로 반영하고, 챗봇뿐 아니라
냉장고요리·요즘인기 등 **앱 전체 매칭률 표시**에도 적용해 달라는 요청.

#### 막힌 지점 (실측)
287개 전체를 챗봇 검색 SQL에 그대로 쓰니 **응답이 25~27초**까지 걸렸다(정상은
2초 안팎). 원인을 좁히려고 두 가지 SQL 방식을 다 실측했다:
- 조미료만 지운 문자열을 만드는 REPLACE 체인(287번 중첩) → 27초
- 조미료 287개 각각 FIND_IN_SET 으로 세어 합산 → 25초

**두 방식이 거의 같은 시간이 나왔다** — 즉 REPLACE 냐 FIND_IN_SET 이냐가 문제가
아니라, **행마다 287개 낱말을 대조하는 연산 자체가 44,504건짜리 테이블(냉장고
우선 검색은 키워드로 좁혀지지 않아 대부분을 훑음)에서 감당이 안 되는 비용**이었다.
목록 크기를 줄여 가며 재보니: 50개 5.4초, 20개 3.1초, 15개 2.9초, 10개 2.1초로
**목록 크기에 거의 선형으로 비례**했다.

#### 조치
287개 사전 전체 대신, 실측으로 성능이 확인된 **15종을 직접 골라 하드코딩**
(`backend/chat_service.py`의 `_load_seasoning_set()`): 소금·후추·설탕·식용유·
참기름·들기름·맛술·미림·식초·물·간장·올리고당·굴소스·다시다·미원. 가중치는
SEASONING_WEIGHT=0.3 / CORE_WEIGHT=1.0 로 적용(완전 제외가 아니라 낮게만).
- `frontend/src/utils/recipeUtils.ts`의 `calculateMatchRate()`(냉장고요리·
  요즘인기·마이페이지 등 카드에 표시되는 매칭률)에도 **같은 15종·같은 가중치**를
  똑같이 반영 — 챗봇과 앱 다른 화면의 매칭률이 서로 다른 기준으로 보이면 안 되므로,
  두 파일의 목록은 반드시 같이 맞춰야 한다(주석으로 상호 참조 남김)
- 프론트는 재료 하나당 계산이라 287개를 다 써도 성능에 문제가 없다. 그런데도 15개로
  맞춘 이유는 순전히 챗봇 SQL의 한계 때문 — 두 곳의 숫자가 갈리는 게 더 나쁘다고 판단

#### 검증 (실측, 운영 DB 대상)
| 시나리오 | 결과 |
|---|---|
| "매운 거 먹고 싶어" (감자,고추장,대파 보유) | "매운 고추장 감자조림"(감자,고추,고추장) → 67%(2/3, 고추만 없음). "매콤한 감자조림"(재료 12개, 조미료 다수) → 26%로 낮게 — 조미료 비중이 큰 레시피가 과대평가되지 않음 |
| "돼지고기로 뭐 해먹을 수 있어?" (돼지고기,김치 보유) | "목살 김치찜"(김치,돼지고기만 필요) → 100%, "삼겹살 제육볶음"(재료 5개, 조미료 섞임) → 40% — 실제 부담과 매칭률이 더 잘 맞음 |
| 응답 시간 | 5개 시나리오 1.9~3.6초 — 기존 수준(2초 안팎) 유지 확인 |
| 프론트 타입체크(`tsc --noEmit`) | 에러 0건 |

#### 남겨둔 것 (범위 밖)
- **`backend/app.py`의 `/api/recipes/filter`** — 냉장고요리·요즘인기의 서버 쪽
  정렬(`sort_by=match_rate`)에 쓰이는 **세 번째** 매칭률 구현. 이미 REGEXP 배치 등으로
  한 차례 성능 최적화가 되어 있는 코드라, 같은 가중치를 넣으려면 여기서도 동일한
  성능 검증이 필요하다 — 이번 범위에서는 손대지 않았다. 그 결과 **카드에 표시되는
  매칭률(가중치 적용됨)과 서버가 정렬한 순서(가중치 미적용)가 서로 다른 기준일 수
  있다** — 목록이 카드 자신의 매칭률 내림차순과 정확히 일치하지 않게 보일 가능성.
  다음에 이어서 처리 필요
- 287개 전체를 챗봇 SQL에서도 쓰려면, 지금처럼 매 요청마다 실시간 계산하는 대신
  `recipes` 테이블에 가중치 적용 값을 **미리 계산해 컬럼으로 저장**(배치로 한 번
  돌리고 이후 크롤러/LLM 배치 때 갱신)하는 구조 변경이 필요 — 더 큰 작업이라 이번엔
  하지 않음

### 요즘인기: "특별한 날" 레시피 액션 실패 + 즐겨찾기/완료/기록 취소 확인 누락

#### 문제 제기
"냉장고요리에서는 즐겨찾기가 잘 되는데 요즘인기에서는 마이페이지에 반영이 안
된다", "완료·기록도 요즘인기에서는 작동을 안 한다", "즐겨찾기 취소할 때 완료
버튼처럼 확인 팝업이 떠야 하는데 없다"는 세 가지 지적.

#### 원인 (실측으로 확인)
`Popular.tsx`의 `handleRecipeAction`이 클릭된 레시피를 찾을 때
`youtubeRecipes.find(...) || naverRecipes.find(...)` 두 배열만 봤는데, 화면엔
**세 번째 배열 `premiumRecipes`**("특별한 날 특별한 음식" 섹션)로 렌더링되는
카드도 있었다. 그 섹션 카드에서 버튼을 누르면 `recipe`가 `undefined`가 되어
버튼 하나로 통합된 로직의 조건문을 통과하지 못하고 **에러도 토스트도 없이
조용히 아무 일도 안 일어남**.
- 실측: 완료 버튼 26개(요즘인기 전체)를 인덱스별로 눌러 재현 — 특별한 날
  섹션에 해당하는 6개(20~25번)만 전부 실패, 나머지 유튜브/네이버 섹션은 정상
- "요즘인기에서만" 이라는 제보와 정확히 일치 — 냉장고요리(`RecipeList.tsx`)는
  레시피 배열이 하나뿐이라 이 문제 자체가 생길 수 없는 구조였음

즐겨찾기 취소 확인 팝업은 실제로 마이페이지(`MyPage.tsx`, `IngredientDetail.tsx`)
에는 이미 있었다(즐겨찾기 포함). 냉장고요리·요즘인기 두 페이지만 애초에 이
패턴이 없어서, 완료·기록·즐겨찾기 전부 눌러서 켜진 걸 다시 누르면 확인 없이
바로 꺼졌다.

#### 조치
- `Popular.tsx`: 레시피 조회 4곳(favorite/done/write/share) 전부
  `|| premiumRecipes.find(r => r.id === id)` 추가
- `RecipeList.tsx`, `Popular.tsx` 둘 다에 마이페이지와 같은 취소 확인 팝업
  ("레시피 OO를 취소하시겠어요? [아니요][네]") 추가. `Popular.tsx`는 즐겨찾기/
  완료/기록 세 곳에 거의 똑같이 반복되던 취소 로직(로컬스토리지 삭제 + 로그인
  시 DB 동기화 + 상태 갱신 + 토스트)을 `performRemoval()` 하나로 묶어서 확인
  팝업의 "네"에서 호출하도록 정리

#### 검증 (실측)
- 요즘인기 "특별한 날" 섹션 카드에서 완료(id 169608 저장 확인)·즐겨찾기 둘 다
  정상 동작 확인
- 즐겨찾기 추가 → 재클릭 시 즉시 삭제되지 않고 확인 팝업이 뜨며, 이 시점엔
  로컬스토리지에 그대로 남아 있음을 확인 → "네" 클릭 시 정상 삭제됨. 냉장고요리
  쪽도 완료 기준으로 동일하게 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### app.py `/api/recipes/filter` 에도 재료 가중치 적용 (남겨뒀던 세 번째 구현체)

앞서 "범위 밖으로 남겨둔 것"으로 적어둔 `/api/recipes/filter`(냉장고요리·요즘인기
서버 정렬용) 에도 챗봇·`recipeUtils.ts`와 같은 가중치를 적용했다. 카드에 보이는
매칭률과 서버가 정렬한 순서가 다른 기준이면 안 된다는 판단.

이 엔드포인트는 사용자의 냉장고 재료(최대 40개, 작음)만 반복하므로 챗봇이
겪은 "287개 조미료를 테이블 전체에 대조" 같은 병목은 없다. 다만 실측해보니:
- 가중치 적용 전 기준: 2.0초
- 그대로 적용: 5.1초 — `match_rate_expr`의 바깥쪽 `CASE WHEN 분모=0` 검사가
  무거운 조미료 제외 문자열을 두 번 평가하게 만들었음
- 그 중복 검사를 `used_ingredients IS NULL OR ''` (원래 있던 가벼운 체크)로
  바꿔 한 번만 평가하도록 최적화 → **3.5초**

2.0초 대비 3.5초는 냉장고에 재료가 있는 모든 사용자가 냉장고요리·요즘인기를
열 때마다 매번 겪는 지연이라 사용자에게 트레이드오프를 그대로 알리고 선택을
물었다 — **일관성 우선(+1.5초 감수)** 으로 결정, 적용함.

더 줄이려면(범위 밖으로 남김): `recipes` 테이블에 가중치 적용 값을 미리 계산해
컬럼으로 저장하는 구조 변경이 필요 — 위 챗봇 항목과 동일한 근본 해법.

### 서비스워커가 배포된 새 코드를 계속 못 받게 막고 있던 문제 (중요)

#### 문제 제기
요즘인기의 즐겨찾기/완료/기록 버그를 고쳐서 배포했다고 안내했는데, 사용자는
"여전히 마이페이지에 반영이 안 된다"고 재현. 그런데 로컬에서 직접 재현해보면
정상 동작함 — 코드 문제가 아니라 **사용자 브라우저가 새로 배포된 코드를 아예
받지 못하고 있을 가능성**을 의심하고 `public/sw.js`를 확인했다.

#### 원인
`sw.js`의 캐시 전략이 **정적 리소스(HTML 포함)를 "캐시 우선"** 으로 서빙하고
있었다:
- `CACHE_NAME = 'cookmatch-v1.0.0'` 가 프로젝트 시작 이후 한 번도 안 바뀜 →
  `activate` 핸들러의 "이전 캐시 정리" 로직이 조건부(`cacheName !== CACHE_NAME`)라,
  이름이 안 바뀌면 정리도 안 걸림
- `/`(진입 HTML)를 포함한 모든 정적 요청을 캐시에 있으면 **네트워크를 아예 안
  보고** 캐시된 것을 그대로 반환. 한 번 방문(또는 PWA로 설치)한 브라우저는
  Vercel에 새 빌드가 올라가도 계속 예전 `index.html`(예전 빌드의 JS/CSS 해시
  경로를 가리킴)을 받아서, **이번 세션에서 커밋·배포한 수정 사항이 그 브라우저엔
  하나도 반영되지 않고 있었을 가능성**이 있다
- 참고로 `frontend/public/workbox-config.js`(`StaleWhileRevalidate` 등 더 나은
  전략 포함)와 `npm run generate-sw` 스크립트가 따로 있었지만, 실제 배포에 쓰는
  `npm run build`(`vite build`)는 이 스크립트를 안 부른다 — 즉 이 설정은 애초에
  한 번도 실제 배포에 반영된 적이 없는 죽은 설정이었고, 배포되는 건 항상 순수
  `public/sw.js` 원본이었다

#### 조치
- `CACHE_NAME`을 `v1.0.1`로 올려 기존 캐시를 정리하도록 함(당장의 staleness 해소)
- HTML 문서 요청(`mode: 'navigate'` 또는 `destination: 'document'`)만 **네트워크
  우선**으로 전략 변경 — 온라인이면 항상 최신 문서를 받고, 오프라인일 때만 캐시로
  폴백. 해시된 JS/CSS/이미지는 그대로 캐시 우선 유지(Vite가 내용이 바뀌면 파일명도
  바꾸므로 안전)

#### 남겨둔 것
`CACHE_NAME`을 배포마다 손으로 올려야 하는 구조는 여전히 남아 있다(까먹기 쉬움).
근본적으로는 `workbox-config.js`를 실제 빌드 파이프라인에 연결하거나, Vite PWA
플러그인(`vite-plugin-pwa`)으로 교체해 빌드마다 자동으로 캐시가 무효화되게
하는 게 맞다 — 이번엔 당장의 문제만 고치고 범위 밖으로 남김.

#### 사용자에게 필요한 조치
이미 접속해 있던 브라우저/PWA는 새 서비스워커를 받을 때까지 여전히 예전 코드를
쓸 수 있다. 확인하려면: 브라우저 탭을 완전히 닫았다가 새로 열기(모바일 PWA는
완전히 종료 후 재실행), 그래도 안 되면 사이트 데이터/캐시를 지우고 재접속.

### 로그인 화면 "최근 로그인" 배지 + 사용법 가이드 정합성 + 챗봇 대화 기록 30개 제한

서비스워커 수정 이후 이어서 처리한 자잘한 UX 정리 세 가지. 각각 별도 커밋으로
반영했으나 이 CHANGELOG에는 한 번에 기록한다.

#### "최근 로그인" 배지
카카오/구글/네이버가 나란히 있으면 다음에 로그인할 때 지난번에 뭘 눌렀는지
헷갈리기 쉽다.
- `AuthContext.tsx`의 `loginWithToken()`에서 로그인 성공 시 이 기기의
  localStorage에 방금 쓴 방법(`google`/`kakao`/`naver`/`email`)을 저장
- `Login.tsx`: 저장된 값과 일치하는 SSO 버튼 우상단에 "최근 로그인" 배지 표시
- 계정 정보가 아니라 **기기별** 저장이라 로그아웃해도 지워지지 않음(다음에 이
  기기에서 로그인할 때 다시 참고할 수 있어야 하므로)

#### 사용법 가이드 정합성
- MyFridge 가이드 문구 "설정 아이콘(⚙️)을 누르면"이 예전 UI를 가리키고 있었음
  — 지금은 재료 배지를 직접 누르면 설정이 열리도록 이미 바뀌어 있었다(별도
  아이콘 없음). 문구를 "이 재료 배지를 누르면"으로 수정
- 냉장고요리 가이드(즐겨찾기/완료/기록/공유): 완료·기록·공유 버튼이 썸네일
  위에서 통계 줄 아래로 옮겨진 이전 UI 개편 때 `RecipeCard`의 해당 버튼들에서
  `data-guide-target`이 함께 옮겨지지 않고 빠져 있었음. 가이드가 대상 요소를
  못 찾으면 `GuideOverlay`가 **직전 단계의 하이라이트 위치를 그대로 남겨둬서**,
  노란 박스가 엉뚱한 자리에 떠 있는 것처럼 보였다(사용자가 "하이라이팅 표시가
  영역과 안 맞는다"고 직접 보고, 본인이 "UI가 변경돼서 그런 것 같다"고 원인까지
  맞혔다) — 실제 버튼에 `data-guide-target` 추가 + 대상을 못 찾았을 때
  `targetRect`를 확실히 `null`로 비우도록 `GuideOverlay.tsx` 수정
- 같은 가이드에서 아이콘 안내를 "즐겨찾기(☆)", "기록(⟎)"처럼 글자로 대신하고
  있었음(⟎는 폰트에 따라 깨져 보임) — 카드에 실제로 쓰는 아이콘을 그대로
  렌더링하도록 교체, 안 쓰이던 아이콘 import 6개 정리

#### 챗봇 대화 기록 30개 제한
지난 대화 보관 개수를 100개 → 30개로 축소하고, 목록 하단에 "최근 대화 30개까지만
보관돼요. 그 이전 대화는 자동으로 지워져요." 안내 문구 추가.

### 임박재료 버튼 시계 이모지 → 검정 선 아이콘

`⏱`(컬러 이모지)가 앱 전반의 검정 테두리 선 아이콘들 사이에서 혼자 튀어 보인다는
지적. 매칭도 버튼의 `%`처럼 같은 톤(stroke `#1A1A1E`, strokeWidth 1.8)으로 맞추기
위해 `RecipeSortBar.tsx`의 이모지 span을 시계 모양 SVG로 교체.

### 냉장고 재료 앱 재진입 시 새로고침 + 로그인 화면 가족 공유 안내

가족끼리 같은 계정으로 냉장고를 공유하는 상황(여러 기기, 동일 로그인)에 대한
후속 조치 두 가지.

#### 앱 재진입 시 DB 새로고침
`MyFridge.tsx`는 로그인/마운트 시점에만 서버에서 재료를 불러오고 그 뒤로는
실시간 동기화가 없어서, 다른 기기(가족 등)가 그새 바꾼 내용이 화면에 반영되지
않을 수 있었다. `visibilitychange`/`focus`로 앱이 다시 화면에 보이는 시점에
`loadIngredientsFromDB()`를 다시 호출하도록 추가. 저장 중이거나(`isSaving`)
이 기기에서 아직 저장되지 않은 변경사항이 있으면(`hasChanges`) 덮어써서 잃을
수 있으므로 그 경우엔 새로고침을 건너뛴다.

#### 남겨둔 것
이 새로고침은 "다시 열었을 때" 시점 동기화일 뿐, 두 기기가 **동시에** 다른
내용을 수정하면 여전히 나중에 저장하는 쪽이 이긴다(`save_user_ingredients`가
DELETE 후 재삽입하는 전체 덮어쓰기 방식이라 병합/충돌 감지가 없음). 사용자에게
이 위험을 그대로 설명했고, 좁은 범위로 고치는 대신 **가구(household)/초대 코드
기반 그룹 기능**을 별도로 구축하기로 결정 — 다음 항목 참고.

#### 로그인 화면 가족 공유 안내
냉장고 재료·즐겨찾기가 계정 기준으로 저장된다는 점을 이용해, 함께 냉장고를 쓰는
가족과 이용하려면 한 계정으로 같이 로그인하면 된다는 안내 문구를 로그인 화면에
추가(SSO 버튼 아래, 작은 회색 텍스트).

### 내냉장고: "영수증 인식"/"사진으로 재료 인식" 버튼을 카메라 버튼 하나로 통합

#### 문제 제기
내냉장고 페이지에 영수증 인식, 사진으로 재료 인식 버튼이 따로 있었는데 — 이미
찍어보기 전에는 어느 쪽인지 사용자가 먼저 골라야 하는 구조라 버튼이 두 개일
필요가 없다는 지적. 뭘 찍을지 먼저 고르게 하고, 그 대신 "카메라" 입구는 하나로
모으는 편이 낫다는 방향으로 재설계 요청. 더불어 "지금 갖고 있는 사진"(앨범)과
"파일"에서 추가하는 경로도 필요하다는 요청, 그리고 이 촬영 화면에 아직 실제
이미지 인식(OCR) 기능이 없다는 걸 사용자가 알 수 있도록 "준비 중" 배지를 눈에
띄게 넣어 달라는 요청.

참고: 두 버튼 모두 이전부터 이미 `showPrepNotice()`(3초 안내 토스트)만 띄우는
순수 placeholder였다 — 이번 작업은 "없던 placeholder를 새로 만드는" 게 아니라,
이미 준비 중이던 자리를 더 쓸모 있는 흐름(선택 시트 + 실제 파일 입력 + 안내)으로
확장하는 것이었다.

#### 조치
- `frontend/src/components/CameraCaptureSheet.tsx` 신규: 카메라 버튼을 누르면
  뜨는 바텀시트. [영수증] / [음식(재료 1개)] / [음식(재료 여러 개)] 세 가지
  주 선택지 + [사진 앨범에서 선택] / [파일에서 추가] 보조 버튼으로 구성
  - 촬영/선택은 표준 `<input type="file" accept="image/*">` 세 개로 처리.
    카메라 3개 옵션은 `capture="environment"`를 붙여 모바일에서 후면 카메라가
    바로 열리게 하고, 앨범/파일 옵션은 `capture` 없이 일반 선택 다이얼로그를 연다
  - 상단에 살짝 기울어진(`rotate(-4deg)`) 노란 배지로
    "✨ AI 이미지 자동인식 기능을 준비하고 있어요" 표시 — 개발 중인 영역임을
    한눈에 알 수 있게
  - `isStandaloneAppMode()`가 아닐 때(=PWA로 설치되지 않은 상태)만, 하단에
    "홈 화면에 추가해두면 더 빠르게 올 수 있어요" 안내 + (안드로이드/크롬은
    `showInstallPrompt()` 바로 호출 버튼, iOS는 "공유 → 홈 화면에 추가" 안내
    텍스트)를 보여준다
- `MyFridge.tsx`: 기존 두 아이콘 버튼을 제거하고 카메라 아이콘 버튼 하나로 교체,
  클릭 시 `CameraCaptureSheet`를 열고 파일이 선택되면 모드별 안내 문구로
  `showPrepNotice()`를 호출(기존 placeholder 토스트 패턴 그대로 재사용)

#### 명확히 하지 않은 것 — 홈 화면 "위젯"
사용자는 이 카메라가 "핸드폰 위젯으로 떠 있거나 바탕화면에 늘 떠 있는" 수준의
접근성을 원했다. 이건 지금의 웹/PWA 구조로는 만들 수 없다:
- 다른 앱 위에 떠 있는 플로팅 버튼은 안드로이드에만 있는 특수 권한 기능이고
  최근 OS 버전일수록 더 제한적이며, iOS엔 아예 없다
- 진짜 홈 화면 위젯(iOS WidgetKit, Android App Widget)은 네이티브 앱 코드가
  있어야 하고, 이 프로젝트는 `frontend/capacitor-setup.md`에 정리된 대로
  Capacitor로 감싸 앱스토어에 낼 계획은 있지만 아직 그 단계는 아니다

지금 만든 "홈 화면에 추가" 안내는 이미 있는 PWA 설치 유도(`HomeInstallPrompt.tsx`,
`pwa.ts`의 `showInstallPrompt()`)를 재사용한, 현재 구조에서 정직하게 줄 수 있는
가장 가까운 대안이다. 실제 OCR(영수증/음식 이미지 인식) 자체는 LLM 재료 추출
배치 작업이 끝난 뒤로 여전히 범위 밖.

#### 검증
- 프론트 타입체크(`tsc --noEmit`) 에러 0건
- 로컬 브라우저에서 확인: 카메라 버튼 클릭 → 시트가 뜨고 배지·3개 선택지·
  앨범/파일 버튼·설치 안내가 모두 정상 렌더링, 닫기 버튼으로 정상 닫힘

### 가족 그룹(초대 코드) 기능 신규 구축

#### 배경
로그인 화면에 "한 계정으로 같이 로그인하면 가족과 냉장고를 공유할 수 있다"는
안내를 넣어뒀지만(바로 앞 항목), 이 방식은 **같은 로그인 정보를 여러 사람이
공유**해야 하고, `save_user_ingredients`가 DELETE 후 재삽입하는 전체 덮어쓰기
방식이라 두 기기가 동시에 재료를 다르게 고치면 나중에 저장하는 쪽이 이기며
먼저 저장된 내용을 통째로 잃는 위험이 있었다. 앱 출시까지 고려하면 결국
계정은 각자 쓰고 냉장고만 공유하는 구조가 필요하다는 판단으로, 별도
가족 그룹/초대 코드 기능을 새로 만들었다.

구현 전 정한 것(사용자 결정):
- 한 계정은 최대 1개 그룹에만 속한다(다중 그룹 소속 없음)
- 그룹 참여 시 참여자가 갖고 있던 개인 재료는 그룹 재료로 합친다(중복은 하나로)
- 그룹원은 모두 동등한 권한(방장 개념 없음) — 누구나 코드 재발급·나가기 가능
- 즐겨찾기/완료/기록은 계정별 개인 기록으로 그대로 둔다 — 그룹과 무관.
  "OO님이 즐겨찾기함" 배지 같은 표시는 이번 범위에 넣지 않음(아래 참고)

#### 스키마/구현 방식
테이블 구조를 크게 바꾸는 대신 **저장 위치 리다이렉션** 방식을 택했다:
- `households` 테이블 신규(`id`, `invite_code` UNIQUE, `storage_user_id`,
  `created_by`, `created_at`). `storage_user_id`는 "이 그룹의 재료가 실제로
  저장되는 계정"을 가리킬 뿐, 권한상 의미는 없다(모두 동등이므로)
- `users` 테이블에 `household_id` 컬럼 추가(기존 `ensure_users_table()`류
  마이그레이션 패턴과 동일하게, `INFORMATION_SCHEMA.COLUMNS` 확인 후
  `ALTER TABLE`로 추가)
- `get_user_ingredients`/`save_user_ingredients`가 요청한 `user_id`로 바로
  조회/저장하는 대신, `resolve_ingredient_storage_user_id()`로 "이 사람이
  속한 그룹의 storage_user_id(없으면 자기 자신)"를 구해 그 id로 조회/저장
  하도록 수정. **덕분에 `user_ingredients` 테이블 구조도, 프론트에서 이
  두 엔드포인트를 부르는 코드(`MyFridge.tsx`)도 전혀 바꿀 필요가 없었다** —
  그룹에 속한 여러 계정이 같은 행을 자연스럽게 공유하게 됨
- 신규 엔드포인트 5개: `GET /api/households/me`(내 그룹 정보),
  `POST /api/households`(그룹 생성), `POST /api/households/join`(초대 코드로
  참여 — 참여자 개인 재료와 그룹 재료를 이름+보관위치 기준으로 병합, 유통기한은
  있는 쪽을 우선하고 둘 다 있으면 더 임박한 날짜를 남김), `POST
  /api/households/leave`(그룹 나가기 — 그룹의 공유 재료는 그대로 두고
  `household_id`만 NULL로), `POST /api/households/regenerate-code`(초대
  코드 재발급, 그룹원 누구나 가능)
- 초대 코드는 0/O, 1/I/L처럼 헷갈리는 문자를 뺀 8자리(`secrets.choice` 기반)
- 프론트: `frontend/src/components/HouseholdSection.tsx` 신규 — 그룹
  생성/참여/초대 코드 표시·복사/멤버 목록/코드 재발급/나가기 UI. 참여·나가기는
  `Dialog` 공통 컴포넌트로 입력창/확인 팝업 처리. 마이페이지 프로필 카드
  바로 아래(요약 탭 위)에 배치(`MyPage.tsx`)

#### 검증 (실측, 프로덕션 DB에 임시 테스트 계정 2개로 직접 확인 후 정리)
- 계정 A(감자 유통기한 09-10, 계란)와 계정 B(감자 유통기한 09-20, 양파)가
  각자 재료를 저장한 상태에서 A가 그룹 생성 → B가 초대 코드로 참여
- 참여 직후 두 계정 모두 `GET .../ingredients` 응답이 동일: 감자(더 임박한
  09-10 유지), 계란, 양파 — 중복 병합과 리다이렉션 동시 확인
- B가 초대 코드 재발급 → 예전 코드로 참여 시도 시 "유효하지 않은 초대
  코드입니다" 정상 거부
- B가 그룹 나가기 → B는 그룹 참여 전 개인 재료(감자 09-20, 양파)로 복귀,
  A는 그룹의 병합된 재료 그대로 유지 확인
- 프론트 UI 실브라우저 테스트: 그룹 만들기 → 초대 코드/멤버 배지 표시 →
  나가기 확인 팝업 → 나가기 → "그룹 만들기/참여하기" 초기 화면으로 정상 복귀
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

#### 남겨둔 것
- **그룹 나가기 시 개인 재료 복원의 정합성**: 나가면 `household_id`만
  지우고 참여 전 개인 `user_ingredients` 행은 손대지 않으므로, 그룹에서
  활동한 기간 동안 바뀐 내용은 반영되지 않은 "참여 시점 그대로"가 복원된다.
  가끔 있는 일(그룹 나가기 자체가 드묾)이라 지금은 그대로 두고, 필요해지면
  나가는 시점에 그룹 재료 스냅샷을 개인 쪽에 복사하는 방식을 추가하면 된다
- **그룹을 만든 사람(`storage_user_id`)이 나가는 경우**: 그 사람의
  `household_id`만 지워질 뿐 그룹은 그대로 남고, 남은 멤버는 계속 그 사람의
  옛 계정 아래 저장된 행을 공유한다 — 데이터가 사라지진 않지만 "누구 이름
  아래 저장되어 있는지"가 사용자에게는 안 보이는 내부 구현이라 문제 없음
- **즐겨찾기/완료/기록의 그룹 공유 표시**("OO님이 즐겨찾기함" 배지): 이번엔
  구현하지 않음. `user_favorite_recipes` 등은 계정별로 그대로 두는 게
  맞다는 결정은 확정했지만, 그룹 화면에서 조회·배지로 보여주는 프론트 작업은
  `MyPage.tsx`/`RecipeList.tsx`/`Popular.tsx` 세 곳의 카드 렌더링을 모두
  건드려야 해서 범위 밖으로 남김 — 필요해지면 `GET
  /api/households/me/<action>-recipes` 같은 엔드포인트로 멤버별 목록을
  모아 오는 것부터 시작하면 된다

### 가족 그룹 후속: 그룹 나가기 재료 처리 수정 + 배지 + 초대 링크 + 참여 옵션

바로 위 항목을 쓴 뒤 사용자가 "그룹 나가면 재료는 어떻게 되는 거냐"고
구체적으로 캐물으면서, 그때 남겨뒀던 문제 두 가지(나가기 시 재료 복원,
즐겨찾기 배지)를 실제로 해결했다. 여기에 카메라 버튼/시트 재설계, 초대
링크 공유, 참여 시 옵션 선택까지 같이 처리했다.

#### 그룹 나가기 시 재료 처리 — 실제로 고침
기존엔 나가면 `household_id`만 지우고 끝이라, 그룹에서 활동하며 바뀐
내용은 전혀 반영 안 된 **가입 전 옛 개인 재료**가 그대로 복원됐다. 사용자
지적("지금 그룹 재료를 내 개인 공간으로 복사해와야 하는 거 아냐?")이
맞았다 — `leave_household()`를 다시 짜서, 나가는 시점 **그룹의 현재
재료 스냅샷**을 그대로 내 개인 저장공간에 복사하도록 바꿨다
(`_copy_ingredients()`).
- 내가 그룹의 저장 계정(`storage_user_id`)이 아니었던 경우: 그룹 재료를
  내 `user_id` 쪽으로 복사
- 내가 저장 계정이었던 경우(그룹 만든 사람): 남은 멤버가 있으면 그 중
  한 명에게 저장 위치를 옮겨준다(데이터를 그 사람 앞으로 복사 +
  `households.storage_user_id` 갱신) — 그래야 내가 나간 뒤에도 남은
  멤버들이 계속 같은 데이터를 본다. 남은 멤버가 없으면(사실상 혼자
  남은 그룹) 옮길 곳이 없으므로 그대로 둔다
- 즐겨찾기·완료·기록은 애초에 그룹과 무관한 개인 기록이라 나가도 전혀
  바뀌지 않는다 — 이 부분은 처음부터 맞게 설계돼 있었다

#### "OO님도 즐겨찾기함" 배지 — 실제로 구현
- `users` 테이블에 `share_recipe_actions`(기본 1) 컬럼 추가. 그룹
  참여자가 "내 즐겨찾기·완료·기록을 그룹원에게 보여줄지"를 직접 고를 수
  있게 하기 위함(뒤에서 설명)
- `GET /api/households/me/recipe-actions` 신규: 그룹원 중
  `share_recipe_actions=1`인 사람들의 즐겨찾기/완료/기록 `recipe_id`와
  누가 했는지(닉네임)를 모아서 준다. 실제 레코드를 합치는 게 아니라
  조회 시점에만 모으는 것이라, 계정별 개인 기록이라는 원칙은 그대로
  유지된다
- `RecipeCard.tsx`에 `attributionLabel?: string` prop 추가 — 있으면
  제목 아래 작은 배지로 표시. `VirtualizedHorizontalRecipeList.tsx`에
  `getAttributionLabel` prop을 추가해 각 카드에 전달
- `MyPage.tsx`: 그룹 활동 데이터를 불러와(`loadHouseholdActions`), **내가
  이미 갖고 있는 즐겨찾기/기록/완료 카드 중** 다른 그룹원도 같은 행동을
  한 경우에만 "OO님도 즐겨찾기함" 배지를 붙인다. 내가 하지 않은 것까지
  목록에 새로 끌어오지는 않음 — "내가 즐겨찾는 레시피" 라는 제목의 뜻이
  바뀌지 않게 하기 위함(남겨둔 것 참고)

#### 참여 시 옵션 선택 + 초대 링크
"그룹만들기 눌러도 뭐가 어떻게 되는지 잘 안 와닿는다"는 지적에 두 가지로
대응:
- **그룹 만들기 버튼에 확인 팝업 추가**: 누르기 전에 "초대 코드가
  만들어지고, 그 코드로 가족이 참여할 수 있다"는 걸 먼저 설명하고 확인을
  받는다(`HouseholdSection.tsx`)
- **참여 시 옵션 선택**: 초대 코드 입력 팝업(그리고 아래 초대 링크로
  들어왔을 때도 동일하게)에 체크박스 두 개 추가 — "지금 내 재료를 그룹에
  합치기"(끄면 내 재료는 그대로 두고 그룹의 기존 재료만 보게 됨), "내
  즐겨찾기·완료·기록을 그룹원에게 보여주기"(끄면 위 배지에서 내 이름이
  빠짐). `POST /api/households/join`이 `merge_ingredients`,
  `share_recipe_actions` 두 값을 받도록 확장
- **초대 링크**: 초대 코드 옆에 "초대 보내기" 버튼 추가 —
  `navigator.share()`(모바일에서 카카오톡 등 공유 시트가 그대로 뜸)를
  우선 쓰고, 지원 안 하면 링크를 클립보드에 복사. 링크
  형식은 `/join-household?code=XXXX`
- **`frontend/src/pages/JoinHousehold.tsx` 신규**: 이 링크로 들어왔을 때
  보여주는 화면. 로그인 안 돼 있으면 코드를
  `localStorage`(`utils/householdInvite.ts`)에 잠깐 저장해 두고 로그인/
  회원가입으로 보낸 뒤, 로그인이 끝나면(`AuthSuccess.tsx`, `Login.tsx`,
  `Signup.tsx` 세 곳의 로그인 성공 처리에 `getPostLoginRedirectPath()`
  추가) 자동으로 이 페이지로 돌아와 이어서 참여할 수 있다. 로그인 상태면
  위와 같은 옵션 체크박스와 함께 바로 참여할 수 있다

#### 그룹 멤버 목록의 닉네임이 안 바뀌던 문제
멤버 목록(`info.members`)은 `/api/households/me`를 부를 때마다 DB에서
새로 읽어오므로 서버 쪽은 항상 최신인데, `HouseholdSection`의 조회
`useEffect`가 마운트 시 한 번만 실행돼서 **내가 마이페이지를 떠나지
않고 내 닉네임을 바꾸면** 배지가 그대로 남아 있었다. `useAuth()`의
`user?.nickname`을 이 effect의 의존성에 추가해서, 내 닉네임이 바뀌는
순간 다시 불러오게 했다. (다른 멤버의 닉네임 변경은 여전히 페이지를
새로 열어야 반영됨 — 실시간 동기화는 범위 밖)

#### 카메라 버튼/시트 재설계
- 영수증/음식 1개/음식 여러 개 선택지가 설명문처럼 가로로 긴 줄이라
  "눌러서 찍는다"는 게 잘 안 느껴진다는 지적 → 큰 정사각 아이콘 타일
  3개를 나란히 두는 방식으로 교체(`CameraCaptureSheet.tsx`)
- "사진 앨범에서 선택"과 "파일에서 추가"가 나뉘어 있을 필요가 없다는
  지적 — 애초에 `capture` 속성 없는 파일 입력 하나면 모바일에서도
  앨범/파일 앱을 그대로 보여주므로, 버튼 하나("사진 앨범/파일에서
  선택")로 합침
- 내냉장고의 카메라 버튼 자체에도 AI 챗봇 FAB과 같은 시각 언어(노란
  셰이머 애니메이션 + "AI" 배지)를 입혀서, "AI가 나중에 관여할 자리"라는
  느낌을 챗봇 버튼과 통일했다(`MyFridge.tsx`, 기존 `.ai-fab-*` CSS
  클래스 재사용)

#### 그 밖의 자잘한 수정
- `RecipeSortBar.tsx`의 "임박 재료" 버튼이 좁은 화면에서 아이콘+글자가
  두 줄로 보이는 문제 — 버튼에 `display:inline-flex`를 명시해 한 줄로
  고정(`white-space:nowrap`만으로는 인라인 요소 사이 줄바꿈을 완전히
  막지 못했음)

#### 검증 (실측, 프로덕션 DB에 임시 테스트 계정으로 직접 확인 후 정리)
- 재료 없이 그룹 참여(`merge_ingredients:false`) → 개인 재료 안 건드리고
  그룹 재료만 보임 확인
- B가 즐겨찾기 후 `share_recipe_actions:false`로 참여 → 그룹 배지 조회
  결과에서 B가 빠짐, A가 같은 레시피 즐겨찾기하면 A만 표시됨 확인
- 초대 링크로 로그인된 새 계정 진입 → 옵션 선택 후 참여 → 마이페이지에
  멤버 배지 2명 정상 표시 확인
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### 카메라 시트 드래그로 닫기 + "합치기 안 하면 재료 사라지는지" 안전장치

#### 카메라 시트도 필터처럼 끌어서 닫기
"필터" 팝업과 같은 바텀시트 모양인데 끌어서 닫는 동작이 없다는 지적. 직접
드래그 로직을 새로 짜는 대신, 필터가 이미 쓰고 있는 공용 `components/ui/Sheet.tsx`
(손잡이/헤더에서 끌면 언제나 반응, 본문에서는 맨 위로 스크롤된 상태에서
아래로 당길 때만 반응 — 목록 스크롤과 안 헷갈리게 이미 구분해 둔 로직)로
`CameraCaptureSheet.tsx`를 새로 짰다. 직접 그리던 배경 딤·둥근 모서리·닫기
버튼을 다 걷어내고 `Sheet`에 맡겨서, 다른 시트들과 여닫는 손맛이 완전히
같아졌다.

#### 그룹 참여 시 "재료 합치기"를 껐을 때의 실제 보장
"합치기를 안 하면 내 재료가 지워지고 백업도 안 되는 거 아니냐"는 질문에
실제 구현을 다시 보니 — 참여 시점에는 안전했지만(개인 재료 행은 건드리지
않음), **나중에 그룹을 나갈 때** 무조건 "그 시점 그룹 재료"로 덮어써 버리는
코드였다. 즉 합친 적 없는 사람도 나갈 때는 자기 옛 재료를 잃고 있었다 —
지적이 맞았다.

`users.ingredients_merged` 컬럼을 추가해 "참여할 때 실제로 합쳤는지"를
기록하고, `leave_household()`가 이 값을 보고 갈린다:
- 합쳤던 사람(`ingredients_merged=1`): 기존과 동일하게 나가는 시점의 그룹
  재료 스냅샷을 그대로 들고 나간다
- 합친 적 없는 사람(합치기를 꺼두고 참여): 참여 후 자기 `user_ingredients`
  행을 한 번도 안 건드렸으므로, **참여 전 재료가 그대로 보존돼 있다가
  그대로 돌아온다** — 그룹의 최신 상태로 덮어쓰지 않는다

참여 다이얼로그(`HouseholdSection.tsx`, `JoinHousehold.tsx`)의 체크박스
설명도 "꺼두면 삭제되지 않고 보존되며, 나갈 때 그대로 돌려받는다"는 실제
동작 그대로 명시하도록 다시 썼다.

#### 검증 (실측)
- B가 `merge_ingredients:false`로 참여 → 그룹에 있는 동안 A가 그룹 재료를
  바꿔도 → B가 나가면 **참여 전 B의 원래 재료**를 그대로 돌려받음 확인
  (그룹의 바뀐 최신 상태가 아님)
- C가 `merge_ingredients:true`로 참여 → 나가면 그 시점 그룹 재료(합쳐진
  전체 목록)를 그대로 가져감 확인 — 기존 동작 그대로 유지됨
- 프론트 타입체크, 백엔드 `python -m py_compile` 에러 0건

### 즐겨찾기/완료/기록 — "겹치는 것만 배지" → "그룹원 전체 피드"로 확장

#### 문제 제기
바로 앞에서 만든 배지 기능은 "내가 이미 갖고 있는 항목 중 다른 멤버도 같은
걸 했으면 배지만 붙는" 정도였다 — B가 즐겨찾기했지만 나는 안 한 레시피는
내 화면에 아예 안 보였다. 사용자가 원한 건 "B가 한 건 나도 볼 수 있어야
한다"는, 그룹원 전체를 하나의 피드로 보여주는 것이었고, 같은 레시피를
둘 이상이 했으면 카드 하나에 배지 여러 개(이름 여러 개)가 붙어야 한다는
것도 확인했다.

#### 조치
`user_favorite_recipes`/`user_completed_recipes`/`user_recorded_recipes`
자체를 합치는 게 아니라(계정별 개인 기록 원칙은 그대로), **조회할 때**
그룹원 전체(+나는 내 `share_recipe_actions` 설정과 무관하게 항상 포함)의
레코드를 레시피 단위로 모아서 하나의 피드를 만든다.

- 기존에 있던 "겹치는 것만 배지" 용 엔드포인트(`GET
  /api/households/me/recipe-actions`, recipe_id→닉네임 목록만 주던 것)를
  걷어내고, 실제 레시피 전체 데이터를 주는 3개로 교체: `GET
  /api/households/me/{favorite,completed,recorded}-recipes`. 쿼리는
  `recipes` 테이블과 각 액션 테이블을 레시피 id로 GROUP BY 해서, 같은
  레시피에 여러 명이 액션했으면 `GROUP_CONCAT`으로 닉네임을 모아
  `acted_by` 배열로 얹어 한 장만 반환한다
- `MyPage.tsx`: 그룹에 속해 있으면 이 3개 엔드포인트로 받은 목록을
  "내가 즐겨찾는 레시피" 등 미리보기 영역에 그대로 쓰고, 섹션 제목도
  "우리 가족이 즐겨찾는 레시피"로 바뀐다. 카드의 `attributionLabel`은
  이제 서버가 준 `acted_by`에서 내 닉네임을 뺀 나머지로 만든다(여러 명이면
  "엄마·아빠님도 즐겨찾기함"처럼 이어 붙임)
- **중요**: 액션 상태(카드 위 하트/체크 버튼이 채워져 보이는지)는 여전히
  `localStorage`(`my_favorite_recipes` 등, 내 계정 것만)로만 판단한다.
  화면에 표시하는 목록(`recipes` prop)과 "내가 눌렀는지"를 판단하는 값을
  분리해 둔 덕분에, 그룹 피드에 다른 사람이 한 항목이 섞여 들어와도 그게
  내 것으로 잘못 표시되는 일은 없다 — `favoriteRecipes` 등 개인 목록
  상태는 그대로 두고, 화면에 뭘 뿌릴지만 `isInHousehold` 에 따라
  `displayFavoriteRecipes` 등으로 분기했다
- `HouseholdSection`에 `onChange` 콜백을 추가해서, 그룹 생성·참여·나가기가
  성공하면 마이페이지가 이 세 피드를 즉시 다시 불러오게 했다(전에는
  페이지를 새로 열어야 반영됐다)

#### 남겨둔 것
"전체보기"(`/mypage/favorite` 등, `IngredientDetail.tsx` 재사용) 페이지는
이번에 안 건드려서 여전히 내 개인 목록만 보여준다 — 미리보기 영역과
개수/내용이 안 맞을 수 있다. 필요해지면 그 페이지도 같은 방식으로
확장하면 된다.

#### 검증 (실측)
- A가 레시피 X 즐겨찾기, B가 레시피 X·Y 즐겨찾기(그룹) → A 화면에 X
  (배지: "B님도 즐겨찾기함")와 **A가 한 적 없는 Y**가 모두 나타나고, Y에도
  "B님도 즐겨찾기함" 배지 정상 표시 확인
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### 카메라 시트 마무리 + 그룹 UX 다듬기(토글, 공유 상태 표시, 문구 단축)

#### 카메라 시트
- 상단에 이미 × 닫기 버튼이 있는데 하단에 또 "닫기" 버튼이 있어서 중복이라는
  지적 — `Sheet`에 `hideFooter` 옵션을 추가하고 카메라 시트에 적용해 하단
  버튼을 없앴다(다른 시트는 그대로 기본 동작 유지)
- "위젯 관련 설명이 왜 없냐"는 재질문에 실제로는 PWA 설치 안내만 있고 정작
  "위젯"이라는 단어가 없어 오해를 줬다는 걸 확인 — `isStandaloneAppMode()`
  여부와 무관하게 항상 보이는 문구를 추가: "📱 바탕화면에 바로 찍는 위젯도
  준비 중이에요. 앱 출시 후 지원될 예정이에요." (PWA 설치 안내와는 별개로,
  이미 설치한 사람에게도 위젯 계획 자체는 알려야 하므로)

#### 그룹원의 공유 상태를 눈에 보이게
"재료는 처음 합류할 때만 영향 주지만(그 뒤엔 안 보임), 즐겨찾기 공유 여부는
계속 남의 화면에 영향을 주니 상태가 보여야 한다"는 지적. 멤버 목록의 각
닉네임 배지 앞에 작은 점을 추가(🟢 공유 중 / ⚪ 비공개) — `GET
/api/households/me`가 멤버별 `share_recipe_actions` 값을 함께 내려주도록
확장하고, 배지 아래 범례 한 줄을 추가했다.

#### 체크박스 → 토글 스위치, 다이얼로그 문구 단축
- `components/ui/Toggle.tsx` 신규(On/Off 스위치). 그룹 참여(`HouseholdSection`,
  `JoinHousehold`)의 두 체크박스를 이걸로 교체 — 지금 상태가 켜져 있는지
  꺼져 있는지 체크박스보다 한눈에 들어온다
- **그룹 만들기에도 같은 두 토글 추가**(이전엔 참여할 때만 물어보고 만들 때는
  선택지가 없었다): "지금 내 재료를 그룹 재료로 쓰기"(끄면 빈 상태로
  시작 — 재료를 지운다는 뜻이라 "파기"로 명확히 이해하고 고르게 함),
  "즐겨찾기·완료·기록 그룹에 공유". 둘 다 기본 켜짐. `POST
  /api/households`가 `keep_ingredients`/`share_recipe_actions`를 받도록
  확장 — false면 생성 시점에 내 `user_ingredients`를 비운다(그룹의 저장
  계정이 곧 내 계정이므로, "가져가지 않기"는 곧 지금 재료를 없애는 것과
  같다 — 참여자의 "보존" 옵션과는 다른 의미라 문구도 다르게 씀)
- 그룹 만들기/나가기 확인 팝업 문구를 절반 이하로 줄임(4줄 → 1~2줄)

#### 검증 (실측)
- `POST /api/households` `keep_ingredients:false`로 생성 → 생성 직후 내
  재료가 실제로 비워짐 확인, `share_recipe_actions:false`로 생성 →
  `/api/households/me` 멤버 목록에 반영됨 확인
- 프론트 UI에서 토글 클릭 → 상태 전환 → 그대로 그룹 생성 API 호출에 반영되는
  것까지 실제 클릭으로 확인
- 프론트 타입체크, 백엔드 `python -m py_compile` 에러 0건

### 요리 캘린더 신규 (완료 레시피를 날짜별로 보기)

#### 배경
"완료한 레시피를 날짜별로 볼 수 있는 달력이 있으면 좋겠다, 그룹이면 누가
언제 뭘 완료했는지도 보이면 좋겠다"는 아이디어. 일/주/월 전환, 그룹
요약까지 포함된 꽤 큰 기능이라 다른 작업과 묶지 않고 별도로 진행했다.

**어디에 넣을지**: 하단 탭을 새로 만드는 대신 마이페이지 안에 넣기로
했다 — 하단 탭 4개(내냉장고/냉장고요리/요즘인기/마이페이지)는 이미 자주
쓰는 핵심 동선이고, 캘린더는 "완료한 레시피"를 다르게 보여주는 것뿐이라
그 섹션에서 들어가는 하위 화면이 자연스럽다. 이름은 "요리 캘린더",
아이콘은 이모지 대신 앱 전체가 쓰는 검정 선 아이콘 스타일로 새로 그렸다.

#### 조치
- 백엔드 `GET /api/households/me/completed-calendar` 신규(쿼리:
  `start`, `end`, YYYY-MM-DD). `completed-recipes`(레시피 단위로 묶어서
  한 장씩 주는 것)와 달리, 여기서는 **(사용자, 레시피, 날짜) 조합별로
  한 행씩** 그대로 준다 — 달력은 날짜가 핵심이라 뭉개면 안 된다. 그룹
  멤버 중 `share_recipe_actions=1`인 사람(+나는 항상 포함)만 대상
- `frontend/src/pages/CookingCalendar.tsx` 신규(`/mypage/cooking-calendar`):
  - 상단 퀵버튼으로 [일] [주] [월] 전환, ‹ › 로 이전/다음 이동, "오늘" 버튼
  - 월 보기: 표준 달력 그리드, 완료 기록이 있는 날짜에 개수 배지. 날짜를
    누르면 그 날의 일 보기로 바로 이동
  - 주 보기: 7일을 세로로 나열, 그 날 완료한 레시피 제목들을 미리 보여줌
  - 일 보기: 그 날 완료한 레시피 카드 목록. 그룹이면 카드마다 "OO님이
    완료" 표시
  - 상단 요약 박스: 지금 보고 있는 범위(달/주/일에 맞춰) 기준으로 총
    횟수 + 그룹이면 인원별 횟수("총 5회 · 엄마 3회 · 나 2회")
  - 항상 anchorDate가 속한 달 전체를 한 번에 불러와 두고, 일/주 보기
    전환 시에는 같은 달 안에서는 다시 불러오지 않는다(클라이언트에서
    필터링만)
- `MyPage.tsx`의 "완료한 레시피" 섹션 제목 옆에 캘린더 아이콘 버튼 추가 —
  이게 유일한 진입점

#### 검증 (실측)
- 그룹 상태에서 A와 B가 같은 날 서로 다른 레시피를 완료 → 월 보기에 그
  날짜 배지 "2" 표시, 그 날짜 클릭 시 일 보기에 두 카드 모두 "OO님이
  완료" 정상 표시, 주 보기에서도 같은 날짜 칸에 두 레시피 제목과 배지
  "2" 정상 표시, 요약 박스에 "총 2회 · CalA 1회 · CalB 1회" 정상 계산 확인
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

#### 남겨둔 것
"완료 레시피 축약판을 홈 화면 위젯으로도"는 아이디어로만 남겨 둔다 —
`capacitor-setup.md`의 위젯 계획과 마찬가지로 네이티브 앱 출시 이후에나
가능한 별도 작업이다.

### 카메라 시트 문구 정리 + 그룹 재료 합치기 정책 + 마이너 UX 수정 6건

#### 카메라 시트
- "위젯 준비 중" 문구가 한 줄로 길게 이어져 좁은 화면에서 마지막 글자만
  다음 줄로 밀려 어색하게 보였다 — 문장을 둘로 나누고 `<br/>`로 줄바꿈
  위치를 직접 지정
- "홈 화면에 추가하면 더 빠르다"는 PWA 설치 안내는 이 시트가 아니어도 다른
  화면(`HomeInstallPrompt`)에서 이미 하고 있어 중복이라는 지적 — 이 시트
  에서는 위젯 준비 중이라는 안내만 남기고 설치 유도 블록은 제거

#### 그룹 멤버 배지 재설계
"닉네임 옆 초록/흰 점이 뭔지 헷갈린다"는 지적(로그인 중인 사람 표시인 줄
알았다고 함) — 점 + 별도 범례 조합을 걷어내고, 멤버를 한 줄씩(닉네임 —
"즐겨찾기·완료·기록 공유 중"/"비공개") 텍스트로 바로 풀어 썼다. 해석이
필요 없게 만드는 게 목적이라 범례도 같이 없앴다.

#### 그룹 재료 합치기 정책(기존 그룹원이 막을 수 있게)
"새로 들어오는 사람이 원치 않는데 재료가 섞이면 어떡하냐"는 지적이 정확한
설계 공백이었다 — 지금까지는 참여자 개인의 선택(토글)만으로 합쳐졌고,
기존 그룹원은 이를 막을 방법이 없었다.
- `households.allow_ingredient_merge`(기본 1) 신규. 그룹원 누구나 켜고 끌
  수 있다("모두 동등" 원칙 유지) — `HouseholdSection`에 토글 추가
- `POST /api/households/settings`로 이 값을 바꾼다
- `join_household()`: 그룹이 이 값을 꺼 뒀으면, 참여자가 무엇을 선택했든
  서버가 강제로 "합치지 않음"으로 처리한다(`merge_denied_by_policy` 응답
  플래그) — 참여자의 선택보다 그룹의 정책이 우선한다
- `HouseholdSection.tsx`/`JoinHousehold.tsx`: 이 플래그가 오면 "이 그룹은
  재료 합치기를 막아 둬서 내 재료가 보존됐다"고 정확히 안내

#### 나가기 확인창 문구가 실제 동작과 다르던 문제
참여 시 토글 설명("꺼두면 나갈 때 내 재료를 돌려받아요")과 나가기 확인창
문구("그룹 재료를 복사해서 가져가요")가 서로 다른 얘기를 하고 있었다 —
실제로는 `ingredients_merged` 값에 따라 둘 중 하나가 맞는 상황이라, 확인창
문구를 그 값에 따라 갈리도록 고쳤다. `GET /api/households/me`가
`my_ingredients_merged`(내 계정 기준)를 함께 내려주도록 확장

#### 그 밖의 수정
- "우리 가족이 즐겨찾는/기록한/완료한 레시피" → "우리 식구가 ~"로 문구 변경
  (식구=같이 밥 먹는 사이라는 의미가 더 맞다는 의견)
- 마이페이지 "우리 식구가 기록한 레시피" 제목 옆 아이콘이 문서/메모
  모양이었는데, 실제 카드의 '기록' 버튼(`기록하기버튼.png`)은 책갈피(리본)
  모양이라 서로 달라 인지 오류가 있었다 — `SectionIcon.tsx`의 `recorded`
  케이스를 책갈피 모양으로 교체해 실제 버튼과 통일

#### 검증 (실측)
- A가 그룹의 `allow_ingredient_merge`를 끔 → B가 `merge_ingredients:true`로
  참여해도 서버가 강제로 막고 `merge_denied_by_policy:true` 반환, B의 개인
  재료가 DB에 그대로 보존돼 있음을 직접 조회로 확인
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

#### 남겨둔 것 — 진행 여부 확인 필요
"마이페이지에서 그룹원 아이디를 누르면 그 사람의 기록/완료/즐겨찾기 개수를
요약해 보여주는 프로필 팝업 + '공유 요청하기' 버튼으로 상대에게 앱이
꺼져 있어도 알림이 가서 수락/거절하게 하자"는 아이디어는 이번에 반영하지
않았다. 앱이 꺼져 있을 때도 알림을 보내려면 웹 푸시 알림(서비스워커 +
VAPID 키 + 구독 저장 + 발송 백엔드)이라는 별도의 큰 인프라가 필요해서,
범위와 우선순위를 먼저 사용자와 확인한 뒤 진행하기로 함.

### 요리 캘린더를 하단 탭으로 승격 + 인원별 색 구분 + 월 목표

#### 배경
"캘린더 들어가는 입구가 마이페이지 안에 너무 숨어 있다"는 지적과, 신혼부부
집안일 분담을 통계로 보여주고 싶다는 원래 취지, 여기에 "돈 아끼려고 집밥
해먹는 사람들을 위한 월간 목표 달성률"까지 아이디어가 붙으면서 — 이 기능은
마이페이지의 부속 화면이 아니라 그 자체로 하나의 탭이 맞다고 판단해서
승격시켰다.

#### 조치
- **하단 탭으로 이동**: `내냉장고 / 냉장고요리 / 요즘인기 / 요리 캘린더 /
  마이페이지` 순서로 마이페이지 바로 앞에 추가(`BottomNavBar.tsx`). PNG
  아이콘 세트가 없어 인라인 SVG로 새로 그렸는데, 처음에 비활성 상태를
  흰색 선으로 그렸다가 — 다른 탭의 `_white.png` 파일들이 실제로는 흰
  배경 위 **회색/검정 선** 아이콘이라는 걸(직접 이미지를 열어 확인) 뒤늦게
  깨닫고 회색(`#9A9AA2`)으로 고쳤다(안 고쳤으면 탭 안 눌렀을 때 아이콘이
  안 보이는 상태로 배포될 뻔함). 경로도 `/mypage/cooking-calendar` →
  `/cooking-calendar`로 옮기고, 마이페이지 쪽의 숨어 있던 아이콘 입구는
  제거
- **헤더를 다른 탭과 같은 규격으로**: 직접 그리던 뒤로가기 버튼 + "요리
  캘린더" 제목 줄을 없애고, 다른 탭처럼 공용 GNB(`TopNavBar`, 로고+
  닉네임) 아래에 본문이 오는 여백(`paddingTop: 72`)만 맞췄다 — 탭 이름은
  하단 탭 라벨로 이미 보이므로 페이지 안에 또 적을 필요가 없었음
- **"오늘" 버튼 제거**: 어디에 쓰는 건지 불명확하다는 지적 — 뺐다. 대신
  일 보기에서 각 레시피 옆에 **완료한 시각**(예: "14:32")을 보여주도록
  해서, 그냥 날짜만 나열하던 것보다 그 날 안에서 언제 요리했는지도
  드러나게 했다
- **인원별 색 구분**: 월/주 보기의 숫자 배지가 "총 몇 번"만 말해줄 뿐 누가
  했는지 안 보였다는 지적 — 그룹원마다 고정 색(파랑/주황/초록 등 6색
  팔레트, id 순서로 배정)을 주고, 날짜 칸에는 그 날 완료한 사람 수만큼
  작은 색 점(2번 이상이면 점 안에 숫자)을 찍는다. 요약 박스에는
  "🔵 TabA 1회  🟠 TabB 2회"처럼 이름+색 범례를 같이 보여준다. 일 보기의
  레시피 카드에도 같은 색 점 + 닉네임을 붙여 색이 전체 화면에서 일관되게
  같은 사람을 가리키게 했다
- **이번 달 목표 + 달성률**: `users.monthly_cooking_goal`(기본값 12 —
  "설정 안 해도 되게 기본값이 있으면 좋겠다"는 요청 반영, 대략 주 3회) 신규.
  화면 상단에 진행바 + "N회 / 목표회 달성 (%)"로 보여주고, 목표 숫자는
  눌러서 바로 수정 가능(`POST /api/users/<id>/monthly-goal`). 지금은
  본인 목표만 편집하고, 그룹 요약 박스의 인원별 횟수와 나란히 보여 자연스럽게
  비교가 되게 했다

#### 검증 (실측)
- 그룹 상태에서 A 1회 + B 2회(같은 날) 완료 → 월 보기 그 날짜 칸에 파랑
  점(무표시=1회) + 주황 점("2") 정상 표시, 요약 박스에 "총 3회 · TabA 1회 ·
  TabB 2회" 정상 계산, 일 보기 진입 시 각 카드에 "05:51 · TabA" 형식으로
  시각+이름 정상 표시 확인
- 목표 수정(12→20) → 진행바와 퍼센트가 그 자리에서 재계산되는 것 확인
- 하단 탭 순서·라벨 정상 표시, 탭 클릭 시 라우팅 정상 확인
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

#### 남겨둔 것 — 다음 단계로 미룸
아이디어로 나온 것 중 이번엔 반영하지 않은 것들. 전부 "데이터를 어떻게
모델링할지"부터 결정해야 해서 별도로 논의 후 진행하는 게 맞다고 판단:
- **연도별 통계**: 지금은 달 단위 조회만 있음. 연간 뷰 자체는 쿼리
  범위만 넓히면 되지만, 어떤 지표를 보여줄지(월별 추이 그래프? 연간
  총합만?) 화면 설계가 더 필요함
- **절약 금액 추정**("총 평균 얼마 아꼈는지")**: 지금 DB엔 재료 가격
  데이터가 전혀 없다 — 레시피별 재료 목록은 있지만 가격표는 없음. "완료
  1회당 평균 X원"처럼 대략치를 낼지, 실제 쿠팡 연동 가격을 붙일지부터
  정해야 계산 로직을 짤 수 있음
- **모두가 요청 없이 앱을 켜면 뜨는 알림 팝업**(공유 요청 등): 바로 위
  항목과 이어지는 것으로, 요청/수락 데이터를 어디에 쌓고 언제 팝업을
  띄울지(마이페이지 진입 시? 앱 전체 진입 시?) 설계가 필요함

### 요리 캘린더 탭 아이콘이 실제로는 안 보이던 버그 + 목표 위계 재정리

#### 하단 탭 아이콘이 배포판에서 안 보이던 문제(실제 버그)
"아이콘이 없다"는 보고를 받고 확인해보니 — `BottomNavBar.tsx`의 렌더
부분이 여전히 모든 탭에 대해 무조건 `<img src={isActive ? nav.icon :
nav.iconInactive}>` 를 그리고 있었다. 요리 캘린더 항목에는 PNG 대신
`renderIcon` 함수만 넣어 뒀는데, 그 값을 실제로 쓰는 분기를 빼먹어서
`nav.icon`/`nav.iconInactive`가 `undefined`인 채로 `<img>`가 렌더링되고
있었다 — 캐시나 배포 지연 문제가 아니라 순수한 로직 버그였다.
`'renderIcon' in nav` 로 분기해 실제로 SVG를 그리도록 수정. 로컬에서
`hasSvg` 를 직접 확인해 재발 방지 확인.

#### 목표 위계 재정리
"주별로 볼 때랑 월별로 볼 때 목표가 똑같이 나오는 게 이상하다, 월 단위
목표면 일/주/월 선택 버튼보다 위에 있어야 하지 않냐"는 지적이 맞았다 —
목표 카드를 일/주/월 전환 버튼 **위**, 화면에서 가장 먼저 오는 자리로
옮기고, "이번 달 목표" 대신 **"{n}월 목표"**로 실제 몇 월인지 못 박아
표기했다. 이렇게 하면 일/주 보기로 바꿔도 같은 카드가 계속 보이는 게
당연하게 읽힌다 — 애초에 월 단위 개념이라고 라벨이 말해주기 때문.

#### 그룹 설정 문구 개선
"재료 합치기 허용" 토글의 설명이 뭘 의미하는지 애매하다는 지적 — 사용자가
직접 제안한 문구("꺼두면 누가 참여하든 그 사람이 갖고 있던 재료는 합쳐지지
않고 그대로 보존돼요")로 교체해 끔의 실제 결과를 명확히 했다.

#### 검증 (실측)
- 로컬 브라우저에서 하단 탭의 요리 캘린더 아이콘이 `<svg>`로 렌더링되고
  비활성 색(`#9A9AA2`, 회색)이 실제로 보이는지 확인
- 월 목표 카드가 일/주/월 전환 버튼 위에 위치, "주" 보기로 바꿔도 "8월
  목표" 카드가 그대로(값도 동일하게) 유지되는 것 확인

### 공유 요청 기능 신규 + 캘린더/그룹 UX 8건 정리

#### 요리 캘린더
- **하단 탭 아이콘 활성/비활성 모양이 서로 다르던 문제**: 활성 상태에만
  격자 눈금을 그려 넣어서, 다른 탭들(같은 모양에 채움/선만 다름)과 달리
  모양 자체가 바뀌어 보였다 — 눈금을 빼고 둥근 사각 + 위쪽 손잡이 2개라는
  같은 모양을 채움/선으로만 구분하도록 정리
- **목표 카드**: "{n}월 목표" → "{연도}년 {n}월 목표"로 연도까지 표기.
  기본값 12회 → **20회**로 상향(이미 만들어진 계정들도 아직 아무도 손대지
  않은 기본값이라 일괄 20으로 올림 — DB에 직접 확인 후 5개 계정 업데이트).
  "매월 1일에 진행률이 다시 0%부터 시작돼요. 목표는 계정별 개인 설정이라
  그룹원마다 따로 정할 수 있어요(공동 목표 아님)" 안내 문구 추가 — "이거
  가족 공동 목표냐"는 질문에 대한 답이자, 매달 초기화되는 게 목표 숫자가
  아니라 달성 퍼센트라는 것도 명확히 함
- **목표 수정 UX**: "목표 12회 수정"처럼 값과 버튼이 붙어 있던 걸 "목표
  12회" + 별도 [목표수정] 버튼으로 분리. 입력창에 포커스를 잃으면(Enter
  포함) 자동 저장되도록 `onBlur` 추가 — 전에는 저장 버튼을 안 누르고 다른
  곳을 누르면 입력값이 반영도 취소도 안 된 채 남아 있었음

#### 그룹 설정 문구 재정리
"재료 합치기 허용을 꺼도 그 사람 재료가 그냥 없어지는 거 아니냐, 우리
재료는 안 바뀐다는 걸 분명히 해야 한다"는 지적 — 관련된 문구 3곳
(그룹 설정 토글, 참여 다이얼로그, 초대 링크 참여 페이지) 모두 "참여자의
기존 재료는 지워지지 않고 보존되고, 대신 우리 그룹의 기존 재료를 보게
된다 — 우리 쪽 재료는 전혀 바뀌지 않는다"는 세 가지 사실을 한 문장에
다 담도록 다시 썼다.

#### 마이페이지: 배지 간소화 + 모두 보기/나의 것만 보기 토글
- 카드 배지를 "OO님도 즐겨찾기함"(문장) → "OO"(닉네임만)로 줄였다. 세
  영역이 한 화면에 나열되면 문장형 배지가 계속 반복돼 오히려 안 읽힌다는
  지적 반영 — 어느 영역인지는 섹션 제목이 이미 말해 주므로 배지는 누가
  했는지만 짧게 보여주면 된다
- 즐겨찾기·기록·완료 세 영역 전체를 아우르는 **[우리 식구 모두 보기] /
  [나의 것만 보기]** 토글을 그 위에 추가(기본은 모두 보기). "이 화면이
  그룹원 전체 걸 보여주는 공간이 맞다"는 걸 사용자가 직접 확인하고 원하면
  개인 목록으로 되돌릴 수 있게 했다 — 배지가 안 보인다는 문의가 실제로는
  "이 화면 자체가 뭘 보여주는 건지" 에 대한 혼란과 겹쳐 있을 수 있어서,
  토글로 그 경계를 명시적으로 만들었다

#### 공유 요청 기능 신규 (앱을 켰을 때 뜨는 팝업 방식)
전에 "웹 푸시까지는 큰 인프라라 필요하다"고 남겨 뒀던 것을, "앱이 완전히
꺼져 있을 때 말고, 앱을 켰을 때 팝업으로 물어보는 정도"로 범위를 좁혀서
이번에 구현했다.
- `household_share_requests` 테이블 신규(requester_id, target_id, status).
- `GET /api/households/members/<id>/stats`: 같은 그룹원의 즐겨찾기/기록/
  완료 **개수만** 준다(목록 아님) — 요청 팝업에 "이 정도 활동이 있다"는
  요약용
- `POST /api/households/share-requests`: 비공개 멤버에게 공유 요청 보내기.
  이미 공유 중이거나 이미 보낸 pending 요청이 있으면 막음
- `GET /api/households/share-requests/pending`: 내가 응답해야 할 요청 목록
- `POST /api/households/share-requests/<id>/respond`: 수락하면 내
  `share_recipe_actions`를 켠다
- `HouseholdSection.tsx`: 비공개인 다른 멤버 행을 누르면 그 사람의 활동
  개수 요약 + [공유 요청하기] 팝업이 뜬다
- `MyPage.tsx`: 마이페이지 진입 시 대기 중인 요청이 있으면 "OO님이
  공유해 달라고 요청했어요 [거절]/[수락]" 팝업이 뜬다(배경 클릭으로
  안 닫힘 — 응답해야 넘어가게)

#### 검증 (실측)
- A가 B(비공개)의 통계 조회 → 정확한 개수 확인 → A가 요청 전송 → B가
  대기 목록에서 확인 → B가 수락 → B의 `share_recipe_actions`가 실제로
  켜지고 대기 목록에서 사라짐, 이미 공유 중인 상태에서 재요청 시 정상
  거부되는 것까지 curl로 전 과정 확인
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

#### 답변만 하고 코드는 그대로 둔 것
- **캘린더/마이페이지에서 그룹원 데이터가 안 보인다는 문제**: 코드 자체는
  이미 여러 번 실제 계정 2개로 직접 재현해 정상 동작을 확인한 상태다.
  실제 원인일 가능성이 높은 건 (1) 상대방의 `share_recipe_actions`가
  꺼져 있는 경우(마이페이지 "우리 식구 모두 보기" 토글 옆, 그룹 멤버 목록의
  🟢/⚪ 표시로 확인 가능해짐 — 이번 업데이트로 그 확인이 더 쉬워졌다) 또는
  (2) 상대방이 아직 아무 즐겨찾기/완료/기록도 하지 않은 경우. 위 배지
  상태 표시로 먼저 확인해 보고, 그래도 재현되면 구체적으로 알려주면 좋겠다
- **초대 코드가 링크랑 따로 있는 이유**: 둘 다 이미 구현돼 있다 — "초대
  코드로 참여하기" 버튼으로 8자리 코드를 직접 입력하는 것도 완전히 별도로
  동작하는 방법이고, "초대 보내기"가 만드는 링크는 그 코드를 URL에 담아
  자동으로 입력해 주는 편의 기능일 뿐이다. 코드만 따로 있는 이유는 전화로
  불러주거나 문자로 텍스트만 보낼 때처럼, 클릭 가능한 링크 렌더링이 안 되는
  상황에서도 쓸 수 있게 하기 위함
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### 절약액 추정 + 모바일 월 캘린더 가로 넘침 수정 + 목표 게이지 인원별 색 분할

#### 절약액 추정
"이 정도 요리하면 외식/배달 대비 얼마 아꼈는지 보고 싶다"는 요청. 재료
가격 데이터가 DB에 없어 정확한 계산은 불가능하지만, 끼니당 대략적인 평균
절약액(외식/배달 대비 한 끼 8,000원으로 추정 — `ESTIMATED_SAVINGS_PER_MEAL`
상수)에 이번 달 완료 횟수를 곱하는 방식으로 대략치는 낼 수 있었다. 목표
카드에 "💰 이번 달 예상 절약액 약 64,000원 (한 끼 8,000원 추정 × 8회)"
형태로 표시 — 실제 계산이 아니라 추정치라는 걸 문구에 명시해 오해를 막는다.

#### 크롬 개발자도구 모바일 에뮬레이션에서 월 캘린더가 화면을 넘어가던 문제
CSS Grid의 잘 알려진 함정 — `gridTemplateColumns: 'repeat(7, 1fr)'`의
`1fr`은 기본적으로 `minmax(auto, 1fr)`로 취급돼서, 칸 안의 내용(요일 배지
여러 개)이 그 칸의 몫보다 넓어지면 **grid 자체가 줄어들지 않고 넘쳐버린다**.
`repeat(7, minmax(0, 1fr))`로 바꿔서 칸이 필요하면 실제로 좁아지도록 수정.
크롬 F12 모바일 에뮬레이션(375px)에서 `document.documentElement.scrollWidth`가
`window.innerWidth`와 같아지는 것으로 확인.

#### 목표 게이지를 인원별 색으로 분할
"A가 7회, B가 3회 했으면 게이지도 그 비율만큼 각자 색으로 나뉘어 채워지고,
아래 범례에 색-이름-횟수가 있어야 한다"는 요청. 완료 횟수가 많은 사람
순서대로 막대 앞부분부터 채우도록 다시 짰다:
- `monthlyByUser`(이 달 전체, 지금 보고 있는 일/주/월 뷰와 무관 — 목표는
  달 단위 개념이므로) 를 내림차순 정렬해 각자 몫(`count/목표 × 100%`)만큼
  이어 붙이고, 합이 100%를 넘으면 시각적으로만 잘라낸다(`goalSegments`)
- 각 구간은 같은 페이지 다른 곳(달력 점, 요약 범례)과 같은
  `colorForUser()` 색을 그대로 써서, 화면 전체에서 "이 색 = 이 사람"이
  일관되게 유지된다
- 게이지 아래에 "🔵 GaugeA 5회 · 🟠 GaugeB 3회" 범례 추가. 혼자(그룹 아님)면
  전과 같은 단색 막대 그대로 유지
- "N회/목표회 달성" 의 N도 지금 보고 있는 뷰가 아니라 이 달 전체 합계로
  고정(이전에는 "내 것만"이었는데, 게이지가 그룹 합계를 보여주는 이상 이
  숫자도 같은 기준이어야 앞뒤가 맞음)

#### 검증 (실측)
- A 5회 + B 3회(그룹, 목표 20회) → 게이지 파랑 25%(5/20) + 주황
  15%(3/20) = 40%, "8회 / 20회 달성 (40%)" 정상 계산, 범례 "GaugeA 5회 ·
  GaugeB 3회" 순서(내림차순) 정상 확인
- 절약액 "8,000원 × 8회 = 64,000원" 계산 정확성 확인
- 크롬 모바일 에뮬레이션(375px)에서 캘린더 페이지 가로 스크롤 없음(scrollWidth
  == innerWidth) 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

## 2026-08-28

### 이번 달 목표를 "개인 설정"에서 "그룹 공동 목표"로 정정
전날 목표를 계정별 개인 설정(`users.monthly_cooking_goal`)으로 구현했는데,
"그룹 전체에 대한 목표인거야!"라는 정정을 받고 그룹이 있을 땐 그룹 전체가
공유하는 하나의 값으로 바꿨다.

- `households` 테이블에 `monthly_cooking_goal INT DEFAULT 20` 컬럼 추가
  (`ensure_households_table()`), 그룹이 있으면 이 값을 쓰고 없으면(혼자)
  기존 `users.monthly_cooking_goal`을 그대로 폴백으로 씀
- `POST /api/households/goal` 신설 — 그룹원 누구나 그룹 공동 목표를 바꿀 수
  있음("모두 동등" 원칙, 재료 합치기 허용 토글과 같은 방식)
- `GET /api/households/me/completed-calendar` 응답에 `group_goal` /
  `my_personal_goal` / `household_size` 필드 추가. 프론트는
  `isInHousehold ? groupGoal : personalGoal` 로 어느 쪽을 쓸지 결정
- `CookingCalendar.tsx`의 목표 카드 안내 문구도 "그룹원마다 따로 정할 수
  있어요(공동 목표 아님)" → "이 목표는 그룹 전체가 공유하는 값이라 누가
  바꿔도 모두에게 적용돼요"로 정정
- 검증: 계정 A/B로 그룹을 만들고 A가 목표를 15회로 바꾸면 B의 캘린더
  조회에도 즉시 15회로 반영되는 것을 API 레벨에서 확인

### 절약액 계산에 쓰는 "식구 수" 직접 설정 기능 추가
"그룹에 연동된 계정 수를 식구 수로 가정해 곱해주지만, 아이가 있는 집은
계정 없이도 같이 먹으니 그 수를 늘릴 수 있어야 한다"는 요청. 연동 계정
수와는 별개로 그룹(또는 혼자면 개인)이 직접 조정하는 `family_size` 값을
추가했다.

- `households.family_size INT NULL`, `users.family_size INT NULL` 컬럼
  추가. NULL이면(설정 안 함) 그룹은 연동 계정 수, 혼자면 1을 기본값으로
  대신 씀 — `family_size_is_custom` 필드로 프론트에 직접 설정 여부를 알림
- `POST /api/households/family-size`(그룹, 누구나 변경 가능), `POST
  /api/users/<id>/family-size`(혼자) 신설. 1~20 범위 검증
- 절약액 계산을 `이번 달 완료 횟수 × 8,000원 × family_size`로 변경하고,
  캡션 옆에 작은 인라인 수정 버튼("N명" 클릭 → 숫자 입력, 포커스 아웃 시
  저장)을 둬서 목표 수정과 같은 방식으로 조정 가능하게 함
- 검증: A/B 그룹에서 A가 family_size를 4로 바꾸면 B의 캘린더 조회에도
  4로 반영, 값을 null로 리셋하면 다시 연동 계정 수(2)로 자동 폴백되는 것
  확인. 혼자 계정에서도 기본값 1 → 직접 3으로 변경 → 재조회 시 3 유지 확인.
  `family_size=25`(범위 밖) 요청 시 400 확인

### 요리 캘린더: 목표 카드와 달력 카드 사이 시각적 구분 + 일/주/월 카드 분리
"목표 영역과 달력 영역이 너무 붙어 있어서 모바일에서 한눈에 구분이 안
된다"는 지적. 일/주/월 전환 버튼부터 달력 그리드까지를 흰 배경 +
테두리가 있는 별도 카드로 묶어, 회색 배경의 목표 카드와 명확히 분리되도록
했다(두 카드 사이 14px 여백 추가).

### 요리 캘린더 하단 탭 아이콘 색상 수정
다른 탭(마이페이지 등)의 실제 PNG 아이콘을 직접 열어서 확인해 보니
"_white"라는 파일명과 달리 실제로는 흰 배경 위 **검정** 선 아이콘이었고,
활성/비활성 구분은 색이 아니라 "채움(활성) vs 얇은 선(비활성)"으로만
이뤄지고 있었다. 캘린더 아이콘만 회색(#9A9AA2)으로 그려져 있던 걸 검정
(#1A1A1E)으로 맞추고, 채운 상태가 "그냥 검정 사각형"처럼 안 보이도록
안쪽에 흰색 헤더 띠 + 손잡이를 파낸 것처럼 그려 넣었다.

### 마이페이지: "가족" → "식구"로 용어 통일
캘린더 등 최근에 쓴 문구는 "식구"(食口 — 함께 밥 먹는 사이, 혈연이 아니어도
쓸 수 있는 말)로 통일했는데, 마이페이지의 그룹 관리 카드는 예전 표현인
"가족 그룹"이 그대로 남아 있어 같은 기능을 가리키는 두 용어가 섞여 있었다.
`HouseholdSection.tsx`, `JoinHousehold.tsx`의 사용자 노출 문구를 모두
"식구 그룹"으로 통일(초대 문구, 다이얼로그 제목, 안내 문구 등). 검색·추천
키워드처럼 이 기능과 무관하게 "가족"을 쓰는 곳(`RecipeList.tsx` 등)은
그대로 둠.

### 마이페이지: "우리 식구 모두 보기" 토글 위치 & 구분선 정정
- **위계 순서 오류**: 즐겨찾기/기록/완료 개수를 보여주는 숫자 카드가 그
  개수를 어느 기준(모두 vs 나만)으로 셀지 정하는 토글보다 **위에** 있어서
  원인(토글)과 결과(숫자)의 순서가 거꾸로였다. 토글을 숫자 카드 위로
  옮겼다.
- **구분선 오해**: 숫자 카드 바로 아래 `SectionBand`(면 구분선)가 있어서,
  사실은 이어지는 이야기(요약 숫자 → 그 아래 상세 목록)인데 마치 완전히
  다른 얘기가 시작되는 것처럼 보였다. 이 구분선을 없애고 여백(20px)만
  남겼다 — 즐겨찾기→기록→완료 목록 사이의 `SectionBand`는 실제로 서로
  다른 섹션이라 그대로 뒀다.

### 마이페이지: "식구 그룹" 카드 접기/펼치기
그룹에 속하면 초대 코드·멤버 목록·재료 합치기 토글·코드 재발급/나가기
버튼까지 카드가 계속 길어져 마이페이지 첫 화면을 다 차지했다. 제목과 한
줄 설명("같은 초대 코드로 들어온 식구와 냉장고 재료를...")만 항상 보이게
하고, 나머지는 제목 오른쪽 화살표 버튼으로 펼쳤다 접었다 하게 했다.
기본은 접힌 상태. 그룹이 없을 때는 원래도 설명 한 줄 + 버튼 2개뿐이라
접을 이유가 없어 그대로 뒀다.

- 구현 중 `{expanded && (<div/> {...} <div/>)}` 처럼 여러 형제 JSX
  엘리먼트를 감싸는 걸 빠뜨려 `<>...</>` 프래그먼트 없이 나열해 버렸고,
  이게 `tsc --noEmit`은 통과하지만 Vite(esbuild/babel)에서는 실제
  구문 오류로 걸렸다 — 브라우저에서 직접 열어 콘솔 에러로 잡아내 수정.
  타입체크만으로는 못 잡는 오류가 있다는 걸 다시 확인한 사례라 기록해 둠

#### 검증 (실측)
- 계정 A/B로 그룹 생성 후 A가 그룹 목표(15회)·식구 수(4명)를 바꾸면 B의
  캘린더 조회에 즉시 반영되는 것을 API로 확인, 혼자 계정에서 개인 목표/
  식구 수가 그룹과 완전히 분리되어 동작하는 것도 확인
- 프론트에서 목표 카드가 "목표 15회", 절약액 "32,000원(= 8,000×2회×2명)"
  → 식구 수를 "4명"으로 수정하니 "64,000원(= 8,000×2회×4명)"으로 즉시
  재계산되는 것 확인
- 마이페이지에서 토글이 숫자 카드 위로 이동하고 그 사이에 구분선이 없어진
  것, "식구 그룹" 문구로 통일된 것을 실제 화면에서 확인
- "식구 그룹" 카드가 기본 접힘 상태로 제목+설명만 보이다가, 화살표를
  누르면 초대 코드/멤버/토글/버튼이 펼쳐지는 것을 실제 브라우저에서 확인
- 테스트로 만든 `@test.local` 계정 전부(가입한 유저, 완료/즐겨찾기 기록,
  빈 households row 포함) 확인 후 프로덕션 DB에서 직접 삭제
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### "B 계정으로 완료해도 캘린더에 안 보인다" 원인 발견 — 프로덕션 DB 직접 조회로 확인
반복해서 들어온 문제 제기라, 이번엔 추측 대신 프로덕션 DB를 직접 조회해
원인을 찾았다. household 11(구글/네이버/카카오 로그인 계정 3개로 구성된
실사용자 본인의 테스트 그룹)에서 계정 하나(`920803hj@gmail.com`, 구글
로그인)만 `share_recipe_actions = 0`(비공개)이었다 — 의도한 프라이버시
설정이 정확히 동작한 것이었지, 버그가 아니었다.

다만 이 과정에서 실제 기능 공백을 하나 찾았다: **본인이 스스로 자기
공유를 다시 켤 방법이 없었다.** 기존에는 "다른 그룹원이 공유 요청을
보내고 내가 수락"하는 경로만 있어서, 요청을 받기 전까지는 본인 의지로
켤 수 없었다. 이게 실제로 이 문제가 반복해서 제기된 이유로 보인다 —
꺼진 걸 본인이 알아도 스스로 고칠 방법이 없었으니까.

- `POST /api/households/my-sharing` 신설 — 내 `share_recipe_actions`를
  내가 직접 켜고 끔 (그룹 여부 무관하게 허용, 나중에 그룹을 만들 수도
  있으니)
- `HouseholdSection.tsx`의 멤버 목록에서, 본인 행은 눌러서 바로 토글되게
  변경(라벨도 "즐겨찾기·완료·기록 공유 중 · 클릭해서 끄기"처럼 명확하게).
  전에는 본인 행이 아예 클릭 불가능한 `<div>`였다
- 검증: A/B 그룹에서 B가 공유 꺼진 채 완료 → A의 캘린더에 정말 안 보이는
  것 재현 → B가 새 self-toggle로 켜기 → A의 캘린더에 즉시 반영되는 것을
  API와 실제 브라우저(멤버 목록 클릭) 양쪽에서 확인
- 실사용자의 진짜 계정(`920803hj@gmail.com`)은 자동 UPDATE로 건드리지
  않음 — 직접 DB를 고치는 건 자동화 분류기가 막았고, 본인 데이터라도
  동의 없이 조용히 바꾸지 않는 게 맞다고 판단해 앱에서 새 스위치로 직접
  켜도록 안내함

### 그룹 설정 카드: 제목 변경 + 펼치기 버튼을 눈에 띄게
"식구 그룹" 카드의 펼치기/접기가 작은 화살표 아이콘 하나뿐이라 눈에 잘
안 띈다는 지적. 텍스트(펼치기/접기) + 화살표가 있는 알약 버튼으로 바꾸고,
카드 제목도 "식구 그룹" → "그룹 설정"으로 바꿨다(초대 문구·다이얼로그
제목처럼 그룹 자체를 가리키는 곳은 "식구" 표현을 그대로 둠 — 이건 설정
화면이라는 성격을 더 잘 나타내는 이름으로).

### 마이페이지: 그룹 설정과 레시피 활용 내역 사이 구분선 추가
"내 정보/그룹 설정 영역과, 그 아래 즐겨찾기·기록·완료 내역 영역 사이에도
구분선이 있어야 한다"는 요청. `HouseholdSection` 카드와 "우리 식구 모두
보기" 토글 사이에 `SectionBand`를 추가했다. 화면 최상위(좌우 패딩 0)에
들어가는 자리라 다른 곳(paddingLeft/Right 14px 안에서 bleed=14로 상쇄)과
달리 `bleed=0`으로 줘야 밴드가 화면 밖으로 새지 않는다는 걸 확인하고
반영. 반대로 토글 바로 아래(즐겨찾기 목록 시작 지점)는 전 업데이트에서
이미 구분선을 없앴으므로 그대로 뒀다 — 토글이 세 목록 전체를 포괄한다는
게 위쪽 구분선 하나로 명확해졌다.

### 요리 캘린더: 목표 카드 접기/펼치기 + "식구수 수정" 버튼 명확화
"목표 카드가 절약액 줄까지는 보이고, 그 아래는 접고 펼 수 있으면 좋겠다
(캘린더가 한 화면에 안 보인다)"는 요청. 목표·달성률·절약액까지는 항상
보이게 두고, 인원별 색 범례와 하단 안내 문구만 "자세히 보기/접기" 알약
버튼으로 접었다 폈다 하게 했다(기본 접힘). 또 식구 수를 숫자 자체를
눌러야만 수정할 수 있어 발견하기 어렵다는 지적에 맞춰, "목표수정"과
같은 패턴으로 눈에 띄는 "식구수 수정" 버튼을 따로 뒀다.

### 요리 캘린더 탭 아이콘: 이모지처럼 알록달록해 보이는 문제 재수정
전에 채운(활성) 상태에 흰색 헤더 띠 + 손잡이를 "파내" 넣은 게 오히려
"이모지 같다"는 지적을 받았다. 다른 탭 PNG(`navigator_popularity_black/
white.png` 등)를 다시 열어 비교해 보니, 두 상태의 차이는 순수하게
"같은 도형을 얇은 선으로 그리느냐 vs 그 도형을 통째로 단색으로
채우느냐" 뿐이고 색이 여러 개 섞이거나 도형 안을 파내는 디테일은 전혀
없었다. 같은 도형(몸통 사각형 + 위쪽 손잡이 2개)을 비활성은 얇은 선
(strokeWidth 1.3 — 참고 PNG들의 선 굵기 비율에 맞춤, 전엔 1.7로 다른
탭보다 두꺼웠다)으로, 활성은 그 도형 그대로 단색 채움으로만 다시 그렸다.

#### 검증 (실측)
- 프로덕션 DB 직접 조회로 실사용자의 household 11에서 계정 하나
  (`920803hj@gmail.com`)만 `share_recipe_actions=0`인 것 확인 — 반복
  제기된 "B 계정 기록이 캘린더에 안 보임" 문제의 실제 원인
- A/B 테스트 계정으로 위 시나리오 재현(B 비공개로 완료 → A 캘린더에
  안 보임) → 새 self-toggle 엔드포인트/UI로 B가 직접 켬 → A 캘린더에
  "총 1회, VtB 1회"로 즉시 반영되는 것 확인
- 마이페이지에서 "그룹 설정" 제목, 눈에 띄는 "펼치기/접기" 버튼, 본인 행
  클릭 시 "공유 중 ↔ 비공개" 즉시 토글되는 것을 실제 브라우저에서 확인
- 요리 캘린더에서 목표 카드가 절약액 줄까지 기본 노출되고, "자세히 보기"
  클릭 시 범례·안내 문구가 펼쳐지는 것, "식구수 수정" 버튼으로 2명 →
  5명 변경 시 절약액이 16,000원 → 40,000원으로 재계산되는 것 확인
- 캘린더 탭 아이콘이 비활성일 때 strokeWidth 1.3의 얇은 선, 활성일 때
  다른 색 섞임 없는 단색(#1A1A1E) 채움인 것을 렌더된 SVG로 직접 확인
- 테스트로 만든 `@test.local` 계정 전부 확인 후 프로덕션 DB에서 직접 삭제
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### 위 업데이트에 대한 후속 피드백 6건 정리

#### 캘린더 탭 아이콘 — 비활성 선 더 얇게
strokeWidth 1.3도 다른 탭보다 두껍다는 지적. 1로 낮춤.

#### 목표/식구 수 수정에 명시적 [적용] 버튼 추가
"수정 버튼을 누르면 입력창으로 바뀌는데, 그 다음 어떻게 적용되는지 모르겠다"
(포커스를 잃으면 자동 저장되는 방식이라 눈에 보이는 커밋 동작이 없었음).
목표수정·식구수 수정 둘 다 같은 패턴으로 바꿨다: 입력창 옆에 **[적용]**
버튼을 두고, 그 버튼을 누르거나 입력창에서 Enter를 눌러야만 저장되게
했다. 기존 `onBlur`로 조용히 저장하던 동작은 제거 — 포커스를 잃는 것만으로
값이 바뀌면 실수로 딴 곳을 눌렀을 때도 저장돼 버린다.

#### 그룹 설정: "클릭해서 끄기"를 실제 버튼으로 분리
본인 행에서 상태 텍스트("공유 중")와 액션("클릭해서 끄기")이 같은 줄에
글자로만 붙어 있어 뭐가 상태고 뭐가 버튼인지 헷갈린다는 지적. 상태 텍스트는
그대로 두고, 오른쪽에 별도의 **[켜기]/[끄기]** 버튼 엘리먼트를 뒀다.

#### "그룹 나가기" 확인 팝업 줄바꿈 정리
`Dialog`의 본문에 `word-break: keep-all`은 이미 걸려 있었지만, 원인 문장이
길게 이어 붙어 있어(예: "...가져가요(그룹에는 그대로 남아요). 즐겨찾기...")
좁은 팝업 폭에서 괄호 앞뒤나 문장 중간의 애매한 지점에서 줄이 꺾였다.
문장을 의미 단위로 쪼개 `<br/>`로 명시적으로 줄바꿈하도록 바꿨다(예:
"지금 그룹 재료를 복사해서 가져가요." / "그룹에는 그대로 남아요." / (빈 줄) /
"즐겨찾기·완료·기록은 원래 그대로예요.").

#### 마이페이지 상단 구분선 — "반영 안 됐다"는 재확인 결과
지난 업데이트에서 그룹 설정과 레시피 활동 내역 사이에 넣은 구분선은
코드상 정상 렌더되고 있는 것을 다시 확인했다(`bleed=0`이 필요한 이유까지
포함). 실제 문제 제기는 이 구분선이 아니라 **그 아래, 즐겨찾기·기록·완료
세 목록 사이의 구분선**이었다 — 이어지는 후속 항목 참고.

#### 즐겨찾기/기록/완료 사이 구분선이 너무 굵어서 별개 영역처럼 보임
세 목록 모두 "우리 식구 모두 보기" 토글이 지배하는 하나의 이야기인데, 그
사이 구분선이 화면 최상단(설정↔활동 내역 사이)과 똑같이 굵은 회색 면이라
"완전히 무관한 영역"처럼 읽힌다는 지적. `SectionBand`에 `subtle` prop을
추가해 얇은 1px 선 모드를 만들고, 이 세 목록 사이에만 적용했다. 정말 다른
주제로 넘어가는 지점(설정 → 활동 내역)은 기존 굵은 면 그대로 둬서, 이제
"완전히 다른 얘기"와 "같은 얘기의 하위 구분"이 시각적으로 구분된다.

#### 기능 문서화 — `HOUSEHOLD_FEATURE.md` 신설
"나중에 기능 이해하는 데 어려움 없도록 변경 내용을 md 파일에 정리해 달라"는
요청. `CHANGELOG.md`는 시간순 변경 히스토리라 "지금 이 기능이 어떻게
동작하는지"를 알려면 여러 날짜의 항목을 다시 읽어야 했다. 그 대신 현재
동작 방식만 정리한 `HOUSEHOLD_FEATURE.md`를 새로 만들었다 — 그룹 생성/참여/
탈퇴, 재료 공유(리다이렉션 방식), 즐겨찾기·완료·기록 공유 판정과 자주
헷갈리는 포인트("B 계정이 캘린더에 안 보이는" 문제의 원인), 그룹 공동
목표 vs 개인 목표, 식구 수와 절약액 계산, 화면 레이아웃 위계, 엔드포인트
전체 목록, 테스트 시 주의사항까지 포함. `DATABASE_SCHEMA.md`에도
`households`/`household_share_requests` 테이블과 `users`의 새 컬럼들을
반영하고 두 문서를 서로 링크했다. `README.md`/`PROJECT_OVERVIEW.md`의
문서 목록에도 추가.

#### 검증 (실측)
- 캘린더 탭 비활성 아이콘의 렌더된 SVG `stroke-width`가 1인 것을 직접 확인
- 목표수정·식구수 수정 모두: 버튼 클릭 → 입력창+[적용] 노출 → 값 변경 후
  [적용] 클릭 → 실제 반영(목표 20→25회, 식구 수 2→4명, 절약액 재계산)
  확인. 입력창 포커스만 잃었을 때는 저장되지 않는 것도 확인
- 그룹 설정 멤버 목록에서 본인 행의 [끄기]/[켜기] 버튼이 상태 텍스트와
  분리되어 있고, 클릭 시 토스트("비공개로 바꿨어요" 등)와 함께 즉시
  반영되는 것 확인
- "그룹 나가기" 팝업의 실제 DOM 텍스트를 읽어 줄바꿈이 문장 경계에서
  일어나는 것 확인
- 마이페이지의 구분선 3개를 실제 DOM에서 조회 — 설정↔활동 내역 사이는
  8px 굵은 면, 즐겨찾기↔기록↔완료 사이 2개는 1px 얇은 선인 것 확인
- 테스트로 만든 `@test.local` 계정 전부 확인 후 프로덕션 DB에서 직접 삭제
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### 한 끼 절약 추정액도 식구 수처럼 직접 설정 가능하게
"식구 수는 수정 가능한데 한 끼 추정액(8,000원)은 못 고친다"는 지적. 완전히
같은 패턴으로 만들었다 — `family_size`를 그대로 본떠 `savings_per_meal`을
추가했다.

- `households.savings_per_meal`, `users.savings_per_meal`(둘 다 INT NULL)
  컬럼 추가. NULL이면 `ESTIMATED_SAVINGS_PER_MEAL_DEFAULT`(8,000원)를 대신
  씀
- `POST /api/households/savings-per-meal`(그룹원 누구나), `POST
  /api/users/<id>/savings-per-meal`(혼자) 신설. 0~100,000원 범위 검증
- **그룹에 속해 있으면 이 값도 그룹 전체가 공유한다** — 목표·식구 수와
  같은 원칙("모두 동등"). 한 사람이 바꾸면 그룹원 전원의 캘린더에 즉시
  반영된다
- 캘린더 응답에 `savings_per_meal` / `savings_per_meal_is_custom` 필드
  추가
- 목표 카드의 절약액 계산 줄에 식구 수와 나란히 "추정액 수정" 버튼을 추가
  — 같은 [입력창 + 적용] 패턴
- `HOUSEHOLD_FEATURE.md`, `DATABASE_SCHEMA.md`에 새 컬럼/엔드포인트 반영

#### 검증 (실측)
- A/B 그룹에서 A가 한 끼 추정액을 12,000원으로 바꾸면 B의 캘린더 조회에도
  즉시 12,000원으로 반영되는 것을 API로 확인(직접 설정 전엔 둘 다 기본값
  8,000원인 것도 확인)
- 프론트에서 "추정액 수정" 버튼 → 9,000원 입력 → [적용] → 절약액이
  "9,000원 × 1회 × 2명 = 18,000원"으로 즉시 재계산되는 것 확인
- 테스트로 만든 `@test.local` 계정 전부 확인 후 프로덕션 DB에서 직접 삭제
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### 절약액 표현 2건 추가 정리
- **"1인당" 명시**: "외식·배달 대비 한 끼 8,000원 추정 × 17회 × 식구
  3명"은 8,000원이 1인 기준인지 전체 기준인지 애매했다(식구 수를 곱하는
  이상 1인 기준이어야 계산이 맞음). "외식·배달 대비 **1인** 한 끼"로
  최소한만 고쳐 문장이 길어지지 않게 하면서 애매함을 없앴다.
- **"지금까지" 명시**: "이번 달 예상 절약액"이 "목표를 달성하면 받을 예상
  절약액"인지 "지금까지 완료한 횟수 기준 절약액"인지 헷갈린다는 지적.
  실제 계산은 항상 후자(완료 횟수 `monthlyTotal` 기준)라 "이번 달" →
  "**지금까지**"로 헤드라인 자체를 바꿔 오해 소지를 없앴다.

#### 검증 (실측)
- 완료 1회·식구 3명·기본 8,000원 계정에서 "💰 지금까지 예상 절약액 약
  24,000원" / "외식·배달 대비 1인 한 끼 8,000원 추정 × 1회 × 식구 3명"
  문구를 실제 화면에서 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### 마이페이지 "전체보기"가 "우리 식구 모두 보기" 상태를 무시하던 버그 수정
"'우리 식구 모두 보기'로 본 다음 '전체보기 >'를 누르면 페이지가 넘어가면서
'나의 것만 보기'로 필터가 바뀐다"는 제보. 원인을 찾아보니 `전체보기` 목적지
화면(`IngredientDetail.tsx`, `/mypage/{favorite,recorded,completed}`)이
애초에 마이페이지의 화면 상태를 전혀 모르고 **항상 내 localStorage만
직접 읽게** 짜여 있었다 — "우리 식구 모두 보기"로 보고 있던 그룹 combined
목록은 마이페이지 컴포넌트 안의 state일 뿐이라, 다른 라우트로 넘어가는
순간 사라지고 없었다.

- `navigate(to, { state: { recipes: displayXxxRecipes, isHouseholdView:
  showAllHousehold } })`로, 마이페이지가 지금 보여주고 있던 배열(그룹
  combined든 개인 것만이든)을 라우터 state로 그대로 넘긴다
- `IngredientDetail.tsx`는 `location.state?.recipes`가 있으면 그걸 쓰고,
  없을 때만(직접 URL 진입/새로고침) 예전처럼 localStorage를 읽는다
- 그룹 combined로 넘어온 경우 각 카드에 `acted_by` 기반 배지("OO·OO")도
  붙도록 `VirtualizedRecipeList`에 `getAttributionLabel` prop을 추가(그동안
  `VirtualizedHorizontalRecipeList`에만 있었음), 화면 제목도 "우리 식구가
  ..."로 맞춤
- "전체삭제" 버튼은 내 localStorage만 지우는 동작이라, 여러 사람 항목이
  섞인 "모두 보기" 목록에서는 화면과 실제 결과가 어긋나 이 모드에서는
  숨김

### 그룹 설정 멤버 목록: 상태 텍스트와 액션 버튼 분리 (2건째)
지난 업데이트에서 본인 행의 "클릭해서 끄기"는 이미 별도 버튼으로 뺐는데,
**다른 사람 행**의 "비공개 · 요청하기 >"는 그대로 남아 있었다. "이게
한 줄로 붙어 있으니 '비공개 요청하기'라는 하나의 버튼처럼 보인다(실제
의미와 정반대라 헷갈림)"는 지적. 본인 행과 같은 패턴으로 상태 텍스트만
남기고 오른쪽에 별도 **[공개 요청]** 버튼을 뺐다.

### 요리 캘린더 목표 카드: 인원별 범례를 다시 항상 보이게
지난 업데이트에서 "자세히 보기" 접힘 영역 안에 인원별 색 범례(누가 몇 회
했는지)까지 같이 넣었는데, "이건 게이지 옆에 항상 붙어 있어야지 숨기면
안 된다 — 계산 공식이면 몰라도"라는 지적. 인원별 범례를 게이지 바로
아래(항상 보이는 자리)로 다시 옮기고, "자세히 보기"에는 안내 문구(매월
1일 초기화 등)만 남겼다.

### 공유 요청 팝업을 마이페이지 전용에서 앱 전역으로
"다른 그룹원의 공유 요청 팝업이 마이페이지에 들어가야만 뜬다. 앱에 다시
들어오는 순간 어느 탭에 있든 떠야 하고, 팝업에서 수락을 누르면 바로
반영돼야 하는데 안 된다(마이페이지 가서 직접 [켜기]를 또 눌러야 함)"는
제보. 두 가지가 섞인 문제였다.

- **탭 제한**: 요청 확인 로직이 `MyPage.tsx` 안에서만 돌고 있었다 —
  전용 컴포넌트 `ShareRequestPopup.tsx`를 새로 만들어 `AppRouter.tsx`에
  `<HomeInstallPrompt/>`/`<RecipeChatWidget/>`와 같은 자리(전역, 모든
  라우트 위)에 마운트했다. 확인 시점은 로그인 상태가 잡히는 순간과 앱이
  다시 보이게 될 때(`visibilitychange`/`focus`) — "다시 들어오는 순간"
  요구를 이 두 시점으로 커버한다.
- **수락해도 반영 안 됨**: 실제로는 서버 값(`share_recipe_actions`)은
  수락 즉시 바뀌고 있었다 — 화면(그룹 설정 카드)이 이미 마운트돼 있으면
  그 결과를 알 방법이 없어서 계속 "비공개"로 보였을 뿐이었다. 수락 시
  `window.dispatchEvent(new CustomEvent('household-share-updated'))`로
  전역에 알리고, `HouseholdSection`(멤버 목록)과 `MyPage`(그룹 활동 피드)
  양쪽이 이 이벤트를 들어서 즉시 다시 불러오게 했다.
- **이 과정에서 만든 버그를 바로 잡음**: `MyPage.tsx`의 이벤트 리스너
  `useEffect`를 처음에 의존성 배열 `[]`로 짰더니, `loadHouseholdRecipeFeeds`
  가 `useCallback`으로 감싸지 않은 일반 함수라 "마운트 시점의"(아직 인증이
  안 잡혔을 수 있는) `isLoggedIn`을 가둔 첫 렌더의 함수가 계속 쓰이는
  stale closure가 생겼다 — 이벤트가 올 때마다 "로그인 안 됨" 분기로 빠져
  `isInHousehold`를 엉뚱하게 `false`로 초기화해 버려서, 수락 직후
  "우리 식구 모두 보기" 토글과 그룹 활동 피드가 통째로 사라지고 개인
  localStorage 목록으로 잘못 되돌아갔다. 실제 브라우저 테스트로 이 회귀를
  직접 잡아냈고, 의존성 배열에 `[isLoggedIn, authUser?.id]`를 넣어 고쳤다.

#### 검증 (실측)
- A/B 그룹에서 A가 즐겨찾기한 레시피와 B가 즐겨찾기한(서로 다른) 레시피가
  A의 마이페이지 "우리 식구 모두 보기"에 둘 다 뜨는 것, "전체보기"를 눌러도
  제목이 "우리 식구가 즐겨찾는 레시피"로 유지되고 총 2건(B 배지 포함)이
  그대로 보이는 것을 실제 브라우저에서 확인(수정 전에는 "나의 것만"으로
  되돌아가며 1건만 보였음)
  - B가 공유를 끈 상태에서 A가 공유 요청 → B가 `/my-fridge`(마이페이지가
  아닌 탭)에 있어도 팝업이 뜨는 것 확인
- B가 마이페이지에 그룹 설정을 펼쳐 둔 채로("비공개" 표시) 같은 화면 위
  팝업에서 [수락]을 누르면, 페이지 이동/새로고침 없이 몇 초 안에 "공유
  중 · 끄기"로 바뀌는 것을 확인 — 처음 구현에서는 이 자리에서 "우리 식구
  모두 보기" 토글 자체가 사라지는 회귀가 발생했었고, 의존성 배열 수정 후
  재현되지 않는 것까지 확인
- 테스트로 만든 `@test.local` 계정 전부(가입한 유저, 완료/즐겨찾기 기록,
  빈 households row 포함) 확인 후 프로덕션 DB에서 직접 삭제
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

### 카카오톡으로 초대 보내면 링크가 두 번 가던 버그
"초대 보내기 → 카카오톡 전송" 하면 링크가 두 번 들어간다는 제보.
`handleShareInvite`가 `navigator.share({ title, text, url })`를 호출하고
있었는데, `text` 문자열 안에 이미 초대 링크를 넣어 뒀으면서(`text =
"...\n${url}"`) **별도의 `url` 필드에도 같은 링크를 또** 넘기고 있었다.
Web Share API는 `text`와 `url`을 각 공유 대상 앱이 알아서 조합하게
맡기는데, 카카오톡 공유 시트는 이 둘을 이어 붙여서 메시지를 만들기 때문에
결과적으로 같은 링크가 두 번(문장 안에 한 번 + 시트가 붙인 것 한 번)
들어갔다. `url` 필드를 빼고 `text` 하나로만 공유하도록 고쳤다 — 이미
`text` 안에 링크가 온전히 들어 있으므로 정보 손실은 없다.

#### 검증 (실측)
- `navigator.share` 호출부에서 `url` 필드를 제거한 코드로 수정, 프론트
  타입체크(`tsc --noEmit`) 에러 0건 확인. Web Share API는 실제 모바일
  브라우저의 사용자 제스처와 카카오톡 앱 연동이 필요해 자동화 브라우저로
  직접 재현·검증하기 어려운 영역이라, 원인(공유 대상이 `text`+`url`을
  이어 붙이는 동작)을 코드로 확인하고 중복 원인을 제거하는 방식으로 수정함

#### 후속 수정 — 진짜 원인은 `url`이 아니라 `title`이었다
`url` 필드를 뺐는데도 "여전히 두 번 온다"는 재확인. 이번엔 사용자가 결정적
단서를 줬다 — 초대 보내기를 누르면 뜨는 카카오톡 쪽 전송 팝업 자체가
**"메시지 보내기 2/2"** 라고 표시되고 있었다. 이건 링크가 텍스트 안에서
중복 표시되는 게 아니라, 애초에 **전송 항목이 2개로 잡혀서 실제로 메시지가
두 번 전송되는 것**이라는 뜻이다.

Android의 공유 인텐트는 `navigator.share()`의 `title`을 `EXTRA_SUBJECT`,
`text`를 `EXTRA_TEXT`라는 서로 다른 값으로 각각 담아 전달한다. 카카오톡
공유 대상이 이 둘을 하나의 메시지로 합치지 않고 별도의 전송 항목 두 개로
처리해("2/2") 메시지를 두 번 보낸 것으로 보인다. `title: '쿡매치 식구
그룹 초대'`를 완전히 빼고 `text` 하나만 넘기도록 고쳤다 — 초대 문구와
링크는 이미 `text` 안에 다 들어 있어 정보 손실은 없다.

#### 검증 (실측)
- `navigator.share` 호출에서 `title` 필드를 제거, `text` 하나만 남김.
  프론트 타입체크(`tsc --noEmit`) 에러 0건 확인. 역시 실제 검증은 카카오톡
  연동이 필요해 자동화 브라우저로는 어렵고, 사용자가 알려준 "2/2" 표시라는
  구체적 증상으로 원인(제목/본문이 별도 전송 항목으로 분리됨)을 특정해
  수정함 — 실제 기기에서 한 번 더 확인 필요

### 재료 합치기 토글 안내 문구 3건 단순화
"'꺼두면 참여자의 기존 재료는 지워지지 않고 그대로 보존되고, 대신 우리
그룹의 기존 재료를 함께 보게 돼요. 우리 쪽 재료는 전혀 바뀌지 않아요.'
이거 말이 너무 어렵다 — '이걸 켜면 초대되는 사용자의 냉장고 재료도 함께
합쳐져요' 정도 뉘앙스로 한 줄이면 되지 않나"는 지적. 3군데 있던 같은 계열
안내 문구를 전부 한 줄로 줄였다:
- 그룹 정책 토글(`HouseholdSection.tsx`, 새로 참여하는 사람의 재료 합치기
  허용): "이걸 켜두면 새로 참여하는 사람의 냉장고 재료도 함께 합쳐져요."
- 참여자 본인 토글(그룹 생성/`HouseholdSection.tsx`·참여/`JoinHousehold.tsx`
  양쪽, 내 재료를 그룹 재료에 합치기): "이걸 켜면 내 냉장고 재료가 그룹
  재료와 합쳐져요."

꺼졌을 때의 되돌리기 보장("돌려받아요" 등)까지 문구에 다 담으려다 오히려
길고 어려워졌던 것 — 토글은 On 상태의 의미만 짧게 말해도 충분하다는 판단.

#### 검증 (실측)
- 프론트 타입체크(`tsc --noEmit`) 에러 0건 확인. 순수 문구 변경이라 로직
  영향 없음

### "홈 화면에 추가" 팝업 대비 문제 2건
"7일간 보지 않기 버튼이 너무 안 보이고, 반대로 우상단 닫기(×) 버튼은 너무
흰색이라 튄다"는 지적. 이 팝업(`HomeInstallPrompt.tsx`)만 짙은 배경
(`rgba(20,20,20,0.94)`)을 쓰는 유일한 팝업인데, 두 버튼 다 이 어두운
배경을 고려하지 않은 색이었다.

- **닫기(×) 버튼**: 앱 공통 `CloseButton`은 밝은 배경(연한 회색) 전제로
  만들어져 있어, 이 팝업 위에서는 하얀 사각형이 튀어 보였다. `CloseButton`에
  `dark` prop을 추가해(다른 어두운 팝업이 생기면 재사용 가능) 반투명 흰색
  배경(`rgba(255,255,255,0.12)`) + 옅은 흰색 아이콘으로, 카드와 자연스럽게
  어울리면서도 여전히 클릭 가능한 버튼으로 보이게 했다.
- **"7일간 보지 않기" 버튼**: 배경 `#6B5200`(어두운 갈색) 위에 글자
  `#1A1A1E`(거의 검정)를 올려 둬서, 버튼 자체가 카드 배경과 거의 구분이
  안 되고 글자도 안 읽혔다. 옆의 "이번엔 닫기"(`#3A3A42` 배경 + 밝은
  회색 글자, 또렷하게 잘 보임)와 대비되던 부분. 브랜드색(`--brand`,
  `#FFD600`)을 옅게 깐 배경(`rgba(255,214,0,0.16)`) + 밝은 브랜드색
  글자로 바꿔 확실한 대비를 냈다.

#### 검증 (실측)
- 온보딩 가이드를 건너뛰게 세션 플래그를 직접 세팅해 팝업을 띄운 뒤,
  실제 렌더된 두 버튼의 `getComputedStyle`을 읽어 확인: 닫기 버튼은
  `rgba(255,255,255,0.12)` 배경 + `rgba(255,255,255,0.85)` 아이콘,
  "7일간 보지 않기" 버튼은 `rgba(255,214,0,0.16)` 배경 + `rgb(255,214,0)`
  글자로 렌더되는 것을 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### 절약액 계산식 문구에 "절약" 빠진 오타
"외식·배달 대비 1인 한 끼 8,000원 추정 × 4회 × 식구 3명" — "8,000원
추정"이라 8,000원이라는 금액 자체가 추정치라는 뜻처럼 읽혔는데, 실제
의도는 "한 끼에 8,000원을 절약한다고 추정"이다. "절약" 한 글자가 빠진
오타였다 — "8,000원 절약 추정"으로 수정. (참고: 같은 대화에서 나온
"0원 절약 추정인 거지" 언급은 실제 0원 표시 버그가 아니라 이 오타를
가리킨 것으로 확인 — 직접 재현 테스트로 96,000원(8,000×4×3)이 정상
계산되는 것 확인함.)

#### 검증 (실측)
- 프론트 타입체크(`tsc --noEmit`) 에러 0건. 순수 문구 변경 1건

### 목표/식구수/추정액 [적용] 버튼이 찌그러진 원으로 보이던 버그
"인풋박스 활성화되면 옆에 적용 버튼이 있는데 찌그러진 원모양이다"는 지적.
`width: 20, height: 20, borderRadius: '50%'`로 정사각형 원을 그리려 했는데,
`padding: 0`을 안 줘서 앱 전역 `button { padding: 0.6em 1.2em }` 규칙이
그대로 적용돼 버튼이 가로로 늘어나 있었다(가로 패딩 1.2em이 세로 0.6em보다
커서 정사각형이 아니게 됨 — `Button.tsx`에 같은 문제를 겪은 이력이 있는
주석이 있었는데 이 버튼엔 반영이 안 돼 있었다). 원 모양을 억지로 고치는
대신 모서리 둥근 사각형(`padding: '0 8px', borderRadius: 6`)으로 바꿨다 —
사용자가 요청한 대로.

#### 검증 (실측)
- 실제 브라우저에서 버튼을 렌더해 `getBoundingClientRect()`/
  `getComputedStyle()`로 확인: 26.2×22px, `border-radius: 6px` — 찌그러진
  원이 아니라 의도한 모서리 둥근 사각형으로 렌더되는 것 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### 요리 캘린더 + 마이페이지에 당겨서 새로고침 추가
"인스타그램처럼 당겨서 새로고침이 왜 안 되냐"는 질문. 확인해 보니
`index.css`에 `overscroll-behavior: none`이 앱 전체에 걸려 있어 브라우저
기본 당겨서 새로고침 자체를 꺼 둔 상태였다 — 그 기본 동작은 페이지를
통째로 하드 리로드시켜 SPA 상태(로그인, 스크롤 위치, 입력 중이던 값)를
날려버리기 때문에 의도적으로 막아 둔 것으로 보인다. 대신 커스텀 당겨서
새로고침 컴포넌트(`PullToRefresh.tsx`)를 새로 만들어, 터치 제스처만
감지해서 지정된 데이터 재조회 콜백만 다시 실행하게 했다(페이지 자체는
그대로).

- 다른 그룹원의 행동으로 화면 내용이 바뀔 수 있는 두 화면에만 적용:
  **요리 캘린더**(`onRefresh={loadCalendar}`), **마이페이지**
  (`onRefresh={refreshMyPage}` — 내 로컬 목록 + 그룹 활동 피드 둘 다
  재조회). 내 냉장고/냉장고요리는 내가 직접 바꾼 것만 반영되면 되고
  이미 즉시 반영되고 있어 제외
- 구현: 페이지가 맨 위로 스크롤돼 있을 때만 당기기 제스처로 인식(그 외엔
  평범한 스크롤), 고무줄 저항(0.5배)을 줘서 자연스럽게, 64px 이상
  당기고 손을 떼면 상단에 스피너를 보여주며 새로고침, 끝나면 원위치로
  애니메이션
- **실제 기기 테스트로 잡은 버그**: 처음엔 손을 뗄 때(`touchend`)
  `setPullDistance(current => { ...; setRefreshing(true); ... })`처럼
  한 state의 함수형 업데이터 안에서 다른 state의 setter를 바로 호출했는데,
  이게 React의 "Cannot update a component while rendering a different
  component" 경고를 유발했다. 실제 터치 시퀀스를 시뮬레이션해서 콘솔에서
  직접 재현·확인한 뒤, 최신 pull 거리를 ref에도 미러링해 두고 `touchend`
  에서는 그 ref만 읽어 판단하도록 고쳐서 두 state가 서로의 업데이터 안에서
  체이닝되지 않게 정리했다

#### 검증 (실측)
- 새 탭에서 두 화면 모두 합성 터치 이벤트(touchstart/move/end)로 실제
  당기기 제스처를 재현: 당기는 동안 인디케이터/콘텐츠가 정확히
  `거리 × 0.5`만큼 내려가고, 64px 이상에서 손을 떼면 요리 캘린더는
  `/api/households/me/completed-calendar`를 다시 호출하는 것을 네트워크
  로그로 확인, 끝나면 두 화면 다 `translateY(0px)`로 복귀하는 것 확인
- 콘솔 경고 재현 전/후 비교: 수정 전 코드로는 매 새로고침마다 "Cannot
  update a component..." 경고가 재현됐고, ref 기반으로 고친 뒤 완전히
  새 탭에서 같은 시퀀스를 반복해도 콘솔에 에러/경고가 전혀 없는 것을 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### 운영 스크립트 확인 + 임시 스케줄 변경
- **LLM 재료 추출기 정상 실행 확인**: `llm_ingredients.log`에서 오늘
  (2026-08-29) 03:00:01에 시작해 03:37:59에 정상 종료(exit=0)한 것을
  확인 — 5,400건 처리, 5,190건 재료 집합 변경, LLM 오류 12건(정상 범위),
  DB 반영 완료
- **크롤러 스케줄 임시 변경**: 원래 주 1회(월요일 07:00)이던 Windows
  작업 스케줄러 작업("CookMatch-WeeklyCrawler")의 트리거를 당분간 매일
  07:00로 변경. 스크립트(`run_crawlers_scheduled.bat`)와 다른 설정(실행
  권한, 배터리 조건 등)은 그대로 두고 트리거만 `MSFT_TaskWeeklyTrigger`
  (월요일만)에서 `MSFT_TaskDailyTrigger`(매일)로 교체. `README.md`에
  임시 변경임을 표시해 둠 — 원래대로 되돌리려면 다시 주간 트리거로
  바꿔야 함

#### 검증 (실측)
- `Get-ScheduledTask`로 변경 후 트리거를 다시 조회해
  `MSFT_TaskDailyTrigger`, `DaysInterval=1`, 시작 시각 07:00인 것을 확인,
  액션(실행 파일 경로)이 그대로 보존된 것도 확인

### 냉장고요리 페이지: 20개(1페이지)만 뜨고 그 이상은 멈추던 성능 버그
"1페이지(20건)만 뜨고 그 이상은 로드가 안 되고 멈춰 있다, 40개(2페이지)
뜨는 데도 한참 걸린다"는 제보. 서브에이전트로 원인을 찾아본 결과, 프론트·
백엔드 양쪽에 겹쳐 있던 문제였다 — 페이지가 쌓일수록 매 페이지가 이전
페이지보다 점점 더 느려지는(총 계산량이 페이지 수의 제곱에 비례하는)
구조라, "처음 몇 페이지는 그럭저럭 되다가 사실상 멈춘 것처럼 보인다".

- **프론트(`RecipeList.tsx`)**: 백그라운드에서 페이지가 하나씩 도착할
  때마다(20개씩), 그 시점까지 쌓인 레시피 **전체**에 대해
  `calculateMatchRate`(동의어 사전 대조 포함)를 처음부터 다시 계산하고,
  `maxLack` 필터에서도 같은 계산을 한 번 더 중복 실행하고 있었다
  (`useEffect` 의존성이 `cachedFilteredRecipes` 배열 전체였음). 레시피
  id별로 계산 결과를 캐시해 두고(내 재료가 바뀔 때만 캐시를 비움), 새로
  도착한 레시피만 계산하도록 고쳤다 — `maxLack` 필터도 이 캐시된 결과를
  그대로 재사용하게 해서 중복 계산도 없앴다.
- **백엔드(`/api/recipes/filter`)**: 기본 정렬(매칭률순)일 때 COUNT
  쿼리가 `match_rate` 계산식을 WHERE에 맞는 행 전체에 대해 실행하는
  무거운 서브쿼리였는데, 이 무거운 COUNT를 **페이지마다 매번** 다시
  실행하고 있었다. 정작 프론트는 1페이지 응답의 `total`만 실제로 쓰고
  2페이지 이후 응답의 `total`은 아예 무시하고 있어서(코드 확인함),
  `page > 1`이면 이 COUNT 쿼리 자체를 건너뛰도록 고쳤다.

#### 검증 (실측)
- 백엔드 API를 페이지 1/2/3/5/10에 대해 직접 호출해 응답 시간 측정:
  수정 전 구조에서 우려됐던 "페이지가 늘어날수록 점점 느려짐" 패턴 없이
  각각 약 2.0~2.6초로 고르게 나오는 것 확인(수정 전엔 매 페이지가 COUNT
  서브쿼리를 다시 돌려 페이지가 늘어날수록 서버 부하가 누적됐을 것으로
  추정)
- 실제 브라우저(재료 29개 등록, 매칭 구간 30~100%, 총 1,194페이지 대상)
  에서 페이지가 끊기지 않고 계속 진행되는 것을 확인 — 약 20초에 7페이지,
  약 40초에 14페이지까지 꾸준히(페이지당 평균 약 2.5~3초 유지, 점점
  느려지는 패턴 없음) 진행됐고 콘솔에 에러 없음
- 프론트 타입체크(`tsc --noEmit`), 백엔드 `python -m py_compile` 에러 0건

#### 남겨둔 것
- 백엔드 SELECT 쿼리 자체는 `match_rate` 계산식으로 정렬하기 위해
  WHERE에 맞는 행 전체를 계산·정렬한 뒤 LIMIT/OFFSET을 적용하는 구조라,
  페이지당 약 2초 정도는 이 설계상 남아 있다(더 줄이려면 필터+재료
  조합별로 정렬된 결과를 서버에 잠시 캐싱해 두는 등 더 큰 구조 변경이
  필요함 — 이번엔 "페이지가 늘어날수록 점점 느려지다 멈추는" 것만
  고쳤고, "각 페이지 자체의 절대 속도"는 다음 기회에)
- "다른 탭에 가 있어도 냉장고요리 페이지가 계속 백그라운드로 로드되게"
  해 달라는 요청은 별개 논의로 남겨 둠 — 지금은 이 페이지가 마운트돼
  있는 동안만 백그라운드 로딩이 진행되고, 다른 탭으로 이동하면(컴포넌트가
  언마운트되면) 같이 멈춘다. 이걸 마운트 여부와 무관하게 계속 돌게
  하려면 이 로딩 로직을 페이지 컴포넌트 밖(앱 전역 상태)으로 옮기는
  구조 변경이 필요해서, 이번 성능 버그 수정과는 분리해 별도로 논의하기로 함

### 요리 캘린더 비로그인 안내 문구 구체화
비로그인 상태에서 뜨는 문구가 "로그인 후 볼 수 있어요."뿐이라 로그인하면
뭘 얻는지 와닿지 않는다는 지적. 이 화면은 냉장고/레시피 목록과 달리 대신
보여줄 로컬(localStorage) 데이터가 아예 없다 — 완료 기록·목표·절약액이
전부 서버 계정에 묶인 데이터라서다(바로 위 "요리 캘린더 로그인 정책"
질문에 대한 답과 같은 이유). 그래서 "왜 로그인해야 하는지" 자체를 문구로
구체적으로 설명하는 쪽으로 보완했다: "로그인하면 요리 캘린더를 쓸 수
있어요" + "완료한 요리 이력을 날짜별로 관리하고, 절약액을 확인하고,
이번 달 목표도 설정할 수 있어요."

#### 검증 (실측)
- 실제 브라우저에서 로그아웃 상태로 `/cooking-calendar` 진입 시 새
  문구가 그대로 렌더되는 것을 확인

## 2026-08-29

### 이모지 → 검은 테두리 SVG 아이콘으로 교체
`SectionIcon.tsx`가 이미 명시하고 있던 "이모지는 기기·OS마다 다르게
보여서 앱 전체를 선으로 그린 SVG로 통일한다"는 기존 원칙을, 요리
캘린더 로그인 안내 문구에 📅를 새로 추가하면서 어긴 것을 계기로 재확인.
- `CookingCalendar.tsx`: 📅 → `BottomNavBar.tsx`에서 새로 export한
  `CALENDAR_ICON_PATHS` 재사용, 💰 → 동전 더미 SVG, ✓/✎ 유니코드 기호 →
  `CheckIcon`/`PencilIcon` 컴포넌트로 교체
- `MyPage.tsx`: 비밀번호 표시/숨김 토글의 👁️/👁️‍🗨️ → `EyeIcon`/`EyeOffIcon`
  컴포넌트로 교체 (표시 중일 때 뜬 눈이 되도록 방향 재확인)
- 나머지 17개 파일에 남아있는 기존 이모지는 이번엔 범위 밖으로 남겨둠
  (한 번에 전부 바꾸기엔 범위가 커서 사용자 확인 후 진행 예정)

### DB 서버 시간대(UTC) 문제 근본 수정
`SELECT * FROM users`로 직접 조회했을 때 `created_at`이 실제 한국
시간보다 정확하지 않다는 지적으로 원인 확인. Railway MySQL 서버 자체
시계가 UTC(`SELECT NOW()`와 로컬 KST를 직접 대조해 확인)로 돌아가는데,
`NOW()`/`CURRENT_TIMESTAMP`로 채워지는 모든 타임스탬프 컬럼에 시간대
보정이 전혀 없었던 게 원인. 이 때문에 요리 캘린더의 날짜별 완료 기록
묶기(`DATE(created_at)`)도 자정~오전9시(KST) 완료 건이 하루 전 날짜로
잘못 묶이는 실제 버그가 있었다.

임시로 그 캘린더 쿼리 하나에만 `DATE_ADD(..., INTERVAL 9 HOUR)`를 붙였다가,
다른 곳에도 같은 문제가 있을 수 있어 근본적으로 고치는 쪽으로 변경:
- `backend/app.py`의 `get_db()`에 `init_command="SET time_zone = '+09:00'"`
  추가 — DB 커넥션 자체를 KST로 고정해서, 이 앱이 쓰는 모든 커넥션에서
  `NOW()`/`CURRENT_TIMESTAMP`가 항상 정확한 한국 시간을 반환하도록 함.
  캘린더 쿼리의 임시 보정 코드는 필요 없어져서 원복
- 이미 UTC로 쌓여있던 과거 데이터 11개 테이블·컬럼(`users.created_at`/
  `updated_at`, `households.created_at`, `household_share_requests.
  created_at`/`responded_at`, `user_completed_recipes.created_at`,
  `user_favorite_recipes.created_at`, `user_ingredients.created_at`/
  `updated_at`, `user_recorded_recipes.created_at`, `coupang_clicks.
  created_at`, `youtube_channel_cache.created_at`, `youtube_channel_meta.
  created_at`)을 일회성으로 `DATE_ADD(컬럼, INTERVAL 9 HOUR)`로 보정해
  새로 쌓이는 데이터와 같은 기준(KST)으로 맞춤 (사용자 승인 후 실행)
- 참고: `recipes.collected_at`은 크롤러가 한국에 있는 로컬 PC에서
  Python으로 직접 시각을 찍어 넣는 값이라 이 문제와 무관 — 그대로 둠

#### 검증 (실측)
- `SELECT NOW()`를 새 커넥션 설정으로 재조회 → 세션 타임존 `+09:00`,
  실제 한국 시각과 일치하는 값 반환 확인
- 마이그레이션 전/후 각 테이블의 MIN/MAX/COUNT를 기록해 행 손실 없이
  정확히 9시간씩만 이동했는지 대조 확인 (예: `users.created_at` 최솟값
  `2025-12-27 03:57:48` → `2025-12-27 12:57:48`)
- `python -m py_compile app.py` 에러 0건

### AI 챗봇: 후속 질문("감자는 빼줘") 무시 버그 수정
사용자가 "너가 준 재료 중에 감자는 빼줘" 처럼 직전 답변을 전제로 한
후속 요청을 해도, 답변 문구는 "감자를 빼고 골랐다"고 하면서 실제로는
직전과 똑같은(감자 포함) 레시피를 그대로 반환하는 문제.

원인(직접 백엔드를 띄워 실제 대화로 재현해서 확인): LLM은 제외 요청
자체는 정확히 `exclude_ingredients: ["감자"]`로 정확히 파싱했지만,
같은 프롬프트 지시 때문에 나머지 냉장고 재료를 전부 `include_ingredients`에
채워 넣어 "네 재료가 전부 동시에 들어간 레시피"라는 지나치게 좁은 조건이
되어 검색 결과가 0건이 됐고, 결과가 0건일 때 조건을 완화하는 로직이
`exclude_ingredients`까지 포함해 모든 조건을 날려버리면서 원래(제외 반영
안 된) 냉장고 검색으로 조용히 되돌아가고 있었다.
- `chat_service.py`의 완화(fallback) 체인을 `keywords` → `include_ingredients`
  → 냉장고 우선순위 순으로 한 단계씩만 완화하도록 고치고, `exclude_ingredients`는
  정말 마지막 단계가 아니면 항상 유지하도록 수정
- 프롬프트에 "사용자가 명시하지 않은 재료를 include_ingredients에 채워
  넣지 마라"는 지시 추가
- 세션별로 직전 턴에 실제로 검색한 조건과 실제로 보여준 레시피(제목+재료)를
  서버가 기억해뒀다가 다음 프롬프트에 사실 그대로 다시 넣어줌 — 답변
  문장에 뭐가 있었는지가 아니라, 실제 검색 결과를 근거로 후속 요청을
  해석하게 함

#### 검증 (실측)
- 실제 백엔드(포트 5000, 실제 Gemini 키·DB)로 2턴 대화 재현: "감자는
  빼줘" 후속 요청 → 8개 결과 전부 감자 없음, 답변 문구도 일치하는 것 확인
- 같은 세션에서 "라면 끓이는 법" 같은 무관한 새 주제로 전환 → 이전
  조건에 얽매이지 않고 새로 검색되는 것 확인(과도한 완화로 이어지지 않음)

### AI 챗봇: 답변에서 재료 언급 시 조미료보다 일반 재료, 유통기한 임박 재료 우선
챗봇이 답변 문구에서 구체적인 재료 이름을 몇 개 골라 언급할 때, 소금·후추
같은 조미료가 섞여 나오거나 유통기한과 무관하게 아무 재료나 골라 언급하는
문제. 기존 매칭률 계산에 이미 있던 조미료/일반재료 가중치 분류
(`_load_seasoning_set()`)를 그대로 재사용해서, 답변 문구가 재료를
언급할 때만 다음 우선순위를 따르도록 프롬프트에 추가:
1. 조미료보다 일반 식재료를 우선 언급
2. 그중에서도 유통기한이 임박한 재료를 우선 언급
3. 단, 사용자가 "다이어트식으로 추천해줘" 처럼 구체적인 목표를 명시하면
   그 목표가 유통기한보다 우선
4. 유통기한 임박이 실제로 답변에 영향을 준 경우에만 그 사실을 답변에
   명시("유통기한이 임박한 OO를 먼저 고려해서 답변드렸어요") — 그래야
   사용자가 "유통기한 신경쓰지 말고 답해줘"처럼 다음 질문으로 되돌릴 수 있음

프론트에서 유통기한 데이터 자체가 애초에 백엔드로 전달되지 않고 있어서
(`RecipeChatWidget.tsx`가 재료 이름만 뽑아 보내고 있었음), `ingredient_expiry`
필드를 새로 추가해 함께 전송하도록 수정. `keywords`/`include_ingredients`/
`exclude_ingredients` 같은 실제 검색 조건에는 영향 없고, 오직 답변 문구의
재료 언급 방식에만 적용됨.

#### 검증 (실측)
- 조미료+유통기한이 제각각인 냉장고(삼겹살 D-1, 닭가슴살 D-6, 두부 D-10,
  감자 D-15, 양파 D-12, 대파 D-8, 소금/후추)로 실제 대화 재현
- 목표 없는 질문 → 삼겹살(가장 임박)/닭가슴살/두부 언급 + "유통기한이
  임박한 삼겹살을 먼저 고려해서 답변드렸어요" 문구 확인
- "다이어트식으로 추천해줘" → 임박도는 낮지만 다이어트에 맞는 닭가슴살을
  언급, 유통기한 문구는 붙지 않는 것 확인(유통기한이 결정 요인이 아니었으므로)
- 위 "후속 질문 무시 버그" 수정 재검증 — 회귀 없음 확인
- `python -m py_compile`, `npx tsc --noEmit` 에러 0건

### AI 챗봇: 지식성 질문에도 무관한 레시피 카드가 붙던 문제 수정
다이어트 대화 중 "너가 추천하는 다이어트용 양념은 뭐야?" 같은 질문을 하면,
`reply`는 "식초, 알룰로스"처럼 구체적으로 답하는데 그 아래 뜨는 레시피 카드는
그 답변과 전혀 무관한 것들이 뜨는 문제. 원인: 이런 지식/조언성 질문도 매번
`keywords`/`include_ingredients`를 뽑아 레시피 검색을 강행했는데, LLM이 `reply`
문장과 검색어를 서로 독립적으로 지어내서 둘이 어긋났다.

LLM 출력에 `recipe_search`(boolean, 기본 true) 필드를 추가: 사용자 메시지가
레시피를 찾아달라는 게 아니라 재료·양념·손질법·대체재에 대한 지식성 질문이면
false로 내려주고, 이때는 `_search_recipes` 자체를 건너뛰어 레시피 카드를 아예
안 붙인다. `recipe_search`가 true인 턴은 `reply`가 언급한 구체적 이름이
`keywords`/`include_ingredients` 중 하나와 실제로 겹쳐야 한다는 규칙을 프롬프트에
추가해 이중으로 막았다. `recipe_search=false` 턴은 세션의 "직전 검색 결과"를
갱신하지 않아, 지식성 질문이 중간에 끼어도 그 다음 "감자는 빼줘" 같은 팔로우업이
여전히 그 이전 진짜 검색 결과를 가리킬 수 있게 했다.

#### 검증 (실측)
- 다이어트 양념 질문 재현 → 수정 후 `recipe_search: false`, `keywords: []`,
  카드 없이 직접 답변만 오는 것 확인
- 일반화 확인: "계란 대신 뭐 쓸 수 있어?"(대체재), "대파 손질법"(손질법) 모두
  `recipe_search: false`로 직접 답변, "매콤한 두부 요리 추천해줘"는 그대로
  `recipe_search: true`에 답변과 일치하는 키워드로 검색되는 것 확인
- "감자는 빼줘" 후속 요청 재검증(8개 전부 감자 없음), 유통기한 우선순위
  답변 2가지 케이스 재검증 — 전부 회귀 없음
- `python -m py_compile` 에러 0건
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### AI 챗봇: 답변을 한 글자씩 타이핑되듯 표시
`RecipeChatWidget.tsx`: LLM 응답이 한 번에 말풍선으로 뜨던 것을, 실제로는
비스트리밍 API 응답을 받아둔 채 클라이언트에서 일정 간격(24ms마다 2자)으로
글자 수를 늘려가며 보여주는 방식으로 흉내냈다. 타이핑이 끝나기 전에는 레시피
카드도 숨겨서, 문장이 다 나오고 나서 카드가 뜨도록 했다.

### 요리 캘린더: 로그인 안 했을 때 하단 탭이 사라지던 버그 수정
`CookingCalendar.tsx`의 로그인 유도 화면이 하단 전역 탭바(`BottomNavBar`)
없이 렌더링되어, 로그인 안 한 사용자가 요리 캘린더 탭에 들어오면 다른 탭으로
이동할 방법이 없었다. 같은 화면에서 달력 아이콘 이미지도 제거.

### 로그인/회원가입 화면에 뒤로가기 버튼 추가
두 화면 모두 뒤로가기 수단이 없었다. 이 앱의 기존 관례(`paddingTop:72` +
상대 위치 배치)는 세로 중앙 정렬 레이아웃에서는 화면 높이에 따라 버튼이
헤더에 가려지는 문제가 있어서(짧은 페이지일수록 더 심함), 대신 전역 헤더
바로 아래 고정 위치(`position:fixed, top:64, zIndex:201`)로 배치했다.

### 로그인 화면의 잘못된 가족 공유 안내 문구 삭제
"한 계정으로 같이 로그인하시면 돼요"라는 문구가 실제 기능(각자 계정으로
가입 후 식구 그룹 초대로 연결)과 달라 삭제.

### 쿠팡 광고 카드: 세로 목록(냉장고요리)에는 아예 안 붙어있던 버그 수정
부족 재료 1~3개인 레시피 카드 뒤에 쿠팡 광고 카드를 끼우는 로직
(`getLackingIngredients`/`pickAdIngredient`/`CoupangAdCard`)이 가로 캐러셀
컴포넌트(`VirtualizedHorizontalRecipeList`, 요즘인기 페이지)에만 구현돼 있었고,
냉장고요리 페이지가 쓰는 세로 목록 컴포넌트(`VirtualizedRecipeList`)에는
연결된 적이 없었다. 같은 규칙(3번째 카드부터, 광고 사이 최소 4개 간격)으로
`VirtualizedRecipeList.tsx`에도 동일하게 구현.

마이페이지(즐겨찾기/기록/완료)는 "사용자가 직접 모아둔 목록 사이에 광고를
끼우면 성격이 흐려진다"는 판단으로 예전에 `showAds={false}`로 꺼뒀었는데,
실제 트래픽이 몰리는 페이지라 판단해 다시 켜기로 함 — 세 섹션 모두
`showAds={false}` 제거(기본값 true).

#### 검증 (실측)
- 로컬(`localhost:5178`)에서 요즘인기 → 유튜브 인기 레시피 캐러셀을
  4번째 카드까지 스크롤 → "오징어" 쿠팡 광고 카드 정상 노출 확인
- 냉장고요리 페이지에서 "부족해도 되는 재료: 3개까지" 필터 + 최신순 정렬로
  1~3개 부족 카드들을 확인 → 3번째 카드(2개 부족) 뒤에 "스팸" 광고,
  이후 4개 간격으로 "버터"/"면"/"가지"/"액젓"/"원두" 광고가 규칙대로
  삽입되는 것을 페이지 텍스트로 확인
- 프론트 타입체크(`tsc --noEmit`) 에러 0건

### 스플래시 화면: 카운팅 시작 전 "0,000"으로 찍히던 것 수정
누적 레시피 수가 아직 안 불러와졌을 때 초기값이 4자리(`['0','0','0','0']`)라
"0,000"으로 보였다. 초기값을 1자리(`['0']`)로 바꿔 그냥 "0"으로 시작하게 함
(`SplashScreen.tsx`).

### 요리 캘린더: 완료일자를 직접 고칠 수 있는 기능 추가
완료 버튼을 실제로 요리한 날 바로 안 누르면(예: 하루 지나서 누르면) 캘린더에
엉뚱한 날짜로 찍히는 문제. 일 보기에서 내가 완료한 기록 옆에 [완료일자 수정]
버튼을 추가 — 날짜 선택 후 적용하면 해당 기록의 `created_at` 날짜만 바뀌고
시각은 그대로 유지된다(다른 식구의 기록은 본인만 고칠 수 있음, 미래 날짜 불가).
백엔드에 `PATCH /api/users/<user_id>/completed-recipes/<recipe_id>/date` 신설.

**버그 발견**: 구현하다 보니 CORS 설정(`methods=[...]`)에 `PATCH`가 아예
빠져있어서, 넣지 않았다면 브라우저 프리플라이트에서 막혀 요청 자체가 안
나갔을 것 — CORS 허용 메서드에 `PATCH` 추가.

#### 검증 (실측)
- `python -m py_compile`, `npx tsc --noEmit` 에러 0건
- 로그인 세션이 없어 실제 클릭 흐름은 라이브로 확인 못 함 — 다음에 로그인
  상태에서 한 번 실제로 날짜를 바꿔보고 캘린더에 반영되는지 확인 필요

### 그룹 참여("참여하기") 시 항상 500 에러 나던 버그 수정
새로 만든 계정(구글 로그인 포함)으로 초대 링크를 받아 "참여하기"를 누르면
매번 서버 오류가 나던 문제. 원인: `join_household()`에서 내 개인 재료와
그룹 재료를 합칠 때 `group_ings + personal`로 두 조회 결과를 이어 붙이는데,
**pymysql은 조회 결과가 0건이면 `fetchall()`이 list가 아니라 빈 tuple을
돌려준다.** 새로 가입한 계정은 냉장고 재료가 항상 0개라 `personal`이 거의
매번 tuple이 되고, `list + tuple`은 파이썬에서 TypeError라 곧장 500으로
떨어졌다 — 즉 이 버그는 우연이 아니라 **신규 계정으로 그룹에 참여하는
거의 모든 시도에서 재현되는** 구조적 버그였다.

같은 패턴(빈 결과일 때 tuple이 돌아올 수 있는데 뒤에서 list처럼
다루는 코드)이 요리 캘린더 API(`get_household_completed_calendar`)의
`member_rows.append(me)`에도 있어 같이 고쳤다(발생 조건은 훨씬 드묾 —
그룹에 있지만 기록 공유를 꺼둔 상태로 필터에 아무도 안 걸릴 때만).

**발견 경위**: 실제로 문제가 재현된 계정(id=58, 방금 새로 만든 구글
계정)으로 프로덕션 DB에 대고 `join_household()`의 SQL을 그대로 트랜잭션
안에서 실행(커밋은 안 함)해 원인을 특정 → 수정 후 같은 시나리오로
재실행해 예외 없이 끝까지 도는 것을 확인.

**별도로 발견한 이슈(수정 안 함, 확인 필요)**: 이번에 테스트하며 초대
코드 `SPMT3ZY9`에 연결된 그룹(household id=11)의 소유 계정(storage_user_id,
id=8)이 그룹에서 안 나가고 바로 회원탈퇴(soft delete)돼 있었다.
`leave_household()`는 그룹장이 나갈 때 남은 멤버에게 저장 위치를
넘기는데, 회원탈퇴(`delete_account()`)는 이 처리를 전혀 안 해서 그룹이
탈퇴한 계정 소유로 그대로 남는다 — 이번엔 우연히 기술적으로는 계속
작동하지만(soft delete라 행 자체는 남아있음), 그룹장이 탈퇴하면 그
그룹의 재료가 있는 계정을 아무도 못 보는 상태가 될 수 있다. 이 계정
정리/재할당 로직은 이번 수정 범위에서 뺐다 — 필요하면 별도로 다뤄야 함.

#### 검증 (실측)
- 프로덕션 DB에서 실제 재현 계정으로 수정 전(TypeError 재현)/수정 후
  (예외 없이 완주) 대조 확인 — 실제 커밋 없이 트랜잭션 롤백으로 진행
- `python -m py_compile` 에러 0건

### 회원탈퇴 시 내가 그룹장이면 그룹 재료가 고아가 되던 문제 수정
그룹에서 안 나가고 바로 회원탈퇴하면, 내가 그 그룹의 재료 저장 계정
(storage_user_id)이었을 경우 그룹 재료가 탈퇴한(soft delete된) 계정에
그대로 남아 아무도 못 보게 되는 문제. `leave_household()`는 그룹장이
나갈 때 남은 멤버에게 저장 위치를 넘기는데, `delete_account()`는 이
처리를 전혀 안 하고 있었다.

`delete_account()`에 동일한 handoff 로직 추가: 탈퇴하는 사람이 그룹의
storage_user_id이면 (1) 남은 멤버가 있으면 그 사람에게 재료를 복사하고
storage_user_id를 옮김, (2) 남은 멤버가 없으면(그룹에 혼자였으면) 그룹
행 자체를 삭제. 탈퇴하는 사람의 `household_id`/`ingredients_merged`도
같이 초기화.

**구현 중 발견한 2차 버그**: 이 로직을 넣으며 앞쪽에서 `datetime.now()`를
먼저 쓰고, 뒤에서 기존 코드가 `from datetime import datetime, timezone,
timedelta`로 다시 import하고 있었다 — 파이썬은 함수 안에서 어디서든
재할당(import 포함)되는 이름은 그 함수 전체에서 지역 변수로 취급하므로,
앞의 `datetime.now()` 호출이 `UnboundLocalError`로 죽는다. 뒤쪽 import를
이미 파일 상단에 있는 `datetime`/`timedelta`는 다시 안 가져오고 없던
`timezone`만 추가로 가져오도록 수정.

**실제로 이미 이 상태로 깨져 있던 그룹 발견**: household id=11(초대코드
SPMT3ZY9)이 정확히 이 시나리오였다 — storage_user_id=8 계정이 그룹에
멤버 2명(id=1, id=4)을 남겨둔 채 그룹을 안 나가고 바로 탈퇴해서, 그
그룹의 재료(44개)가 탈퇴한 계정 밑에 그대로 있었다. 코드 수정과는
별도로, 이 기존 데이터도 실제로 복구할지는 사용자 확인 후 진행 예정.

#### 검증 (실측)
- 프로덕션 DB에 대고 실제 handoff 로직(재료 복사 + storage_user_id 이전)을
  트랜잭션으로 실행해 예외 없이 끝까지 도는 것을 확인 — 커밋은 안 하고 롤백
- `python -m py_compile` 에러 0건

---

## 2026-08-31

### LLM 재료 추출 배치: 일일 한도 소진 시 즉시 중단 + 호출 예비분 확대

새벽 3시 배치(`run_llm_ingredients_daily.bat`)의 오류 108건을 조사한 결과,
성격이 전혀 다른 두 가지가 섞여 있었다.

| 오류 | 건수 | CSV 내 위치 | 원인 |
|---|---|---|---|
| `HTTP 429` | 84건 | 5304~5387 (연속) | 일일 호출 한도 소진 |
| `batch response missing this index` | 24건 | 336, 1679 부근 (산발) | 모델이 응답 JSON에서 해당 번호 누락 |

**429가 일일 한도 소진인 근거**: 실패 위치가 5304~5387로 완벽히 연속이다.
분당 rate limit이면 5,400건 전체에 흩어져 나온다. 끝에만 뭉친 건 누적 한도가
바닥났다는 뜻. 호출 수로 환산하면 `5400/12 = 450회` 중 **442회까지 성공,
443회부터 전부 429** — 챗봇이 같은 키로 예비분 50회를 넘겨 58회쯤 쓴 결과다.

#### 1) 한도 소진 시 즉시 중단 (`llm_ingredient_extraction.py`)
기존엔 429마다 5회 지수 백오프(총 31초)를 돌았는데, **일일 한도는 자정(PT)까지
안 풀리므로 이 31초가 전부 헛돈다.** 남은 배치마다 반복되어 시간만 낭비했다.

- `GeminiExtractor`에 스레드 공유 플래그 `_quota_exhausted`(Event) 추가.
  한 번 서면 이후 호출은 API를 때리지 않고 즉시 `QUOTA_EXHAUSTED_ERR`로 반환
- 429 응답의 `quotaId`로 제한 종류를 판별하는 `_quota_scope()` 추가
  (`day` / `minute` / `None`)
- 판정 규칙:
  - `PerDay` 명시 → **첫 응답에서 즉시** 중단 (백오프 0초)
  - quotaId 불명확 → 재시도 5회 소진 후에도 429면 중단
  - `PerMinute` 명시 → **중단하지 않음.** 분당 제한은 곧 풀리는데 이걸 일일
    한도로 오판하면 그날 작업 전체를 통째로 조기 종료시키게 되어 손해가 크다
- 최종 요약에 `(그 중 일일 한도 소진으로 건너뜀 N건)` 표기 추가

한도로 건너뛴 행은 기존 오류 처리와 동일하게 `llm_ingredients_done`을 건드리지
않으므로 `--pending-only` 다음 실행에서 자동 재시도된다 (데이터 손실 없음).

#### 2) 호출 예비분 50 → 60회 (`run_llm_ingredients_daily.bat`)
`--limit 5400` → `5280` (450회 → 440회). 챗봇이 쓸 몫을 60회로 늘려 이번 같은
초과를 흡수한다. 남은 물량 기준 총 소요일은 그대로다.

#### 검증 (실측)
- 모의 429 응답 4종 전부 의도대로 동작 확인:
  일일한도(호출 1회 후 즉시 중단) / 분당제한(5회 재시도, 중단 안 함) /
  불명확(5회 재시도 후 중단) / 정상응답(중단 안 함)
- 한도 소진 후 후속 배치는 **추가 API 호출 0회**로 즉시 스킵되는 것 확인
- 실제 API로 24건 스모크 테스트(`--commit` 없이): 오류 0건, DB 미반영 확인
- `.bat`은 CRLF 유지 확인 (LF로 저장되면 cmd.exe가 즉시 깨짐)
- `python -m py_compile` 에러 0건

### 진행률 실측 (2026-08-31 기준)
- 전체 44,845건 중 **완료 33,144건 / 남음 11,701건 (73.9%)**
- `llm_ingredients_done=1`인데 재료가 빈 행: 0건
- 하루 실효 처리량 약 5,290건 → **9/3 새벽 배치에서 완주 예상**

### 재료 누락의 진짜 원인은 OCR이 아니라 사전 미등록 (조사 결과)
재료가 1~2개뿐인 행을 OCR 대상 후보로 보고 본문을 열어봤더니, **재료가 본문에
텍스트로 멀쩡히 적혀 있는데도 누락**되고 있었다.

- id=17212: 본문에 "춘권피 / 비엔나소시지 / 올리브유"가 명시돼 있으나
  결과는 `엑스트라버진,춘권피` — `비엔나소시지`가 사전에 없어 탈락
- id=16905: 본문에 "간장/참깨가루/생들기름/마늘"과 주재료 송화버섯이 있으나
  결과는 `간장,마늘`

최근 CSV 3일치(정상 처리 16,080행) 집계:
- **LLM이 뽑은 재료의 18.7%(35,914건)가 사전 정규화에서 버려지고 있다**
- 버려진 것 중 고유 7,541종. 상위: 물(5,692) 밥(1,048) 물엿(843) 깨(552)
  봄동(413) 쌀뜨물(358) 신김치(315) 다진생강(548, 띄어쓰기 변형 합산)
- 이 중 **수식어(다진/삶은/냉동/건/신/고운 등)만 떼면 사전에 잡히는 것이
  8.5%(3,057건)** — 예: 신김치→김치, 다진생강→생강, 냉동새우→새우
- `물엿`,`케첩`,`목살`,`크래미`,`또띠아`,`땅콩버터`,`비엔나소시지` 등 흔한
  재료가 사전(실재료 1,337종 / 별칭 1,767개)에 아예 없음
- `올리브유`의 대표 keyword가 `엑스트라버진`으로 잡혀 있음 — 화면 pill에
  그대로 노출되는 값이라 사전 자체의 품질 문제

→ OCR을 붙이기 전에 사전 보강이 선행되어야 ROI가 맞다. 이 조사 내용만 기록하고
사전 수정은 이번 범위에서 제외 (별도 작업).

### 재료 사전 감사 및 보강

LLM 재료 추출의 재료 누락 원인을 파고들다가, 문제가 LLM이 아니라 **사전 정규화
단계**에 있다는 걸 확인하고 사전 전체를 감사했다.

발단은 재료가 1~2개뿐인 행을 열어본 것이었다. 재료가 본문에 텍스트로 멀쩡히
적혀 있는데도 누락되고 있었다.

- id=17212: 본문에 "춘권피 / 비엔나소시지 / 올리브유"가 명시돼 있으나
  결과는 `엑스트라버진,춘권피` — `비엔나소시지`가 사전에 없어 탈락

#### 감사에서 드러난 구조적 결함

| 항목 | 감사 전 | 감사 후 |
|---|---|---|
| 동의어끼리 충돌 (CSV 행 순서로 승자 결정) | 4건 | **0건** |
| 자기참조 동의어 | 8건 | **0건** |
| 매핑 연쇄 미해소 (A→B인데 B→C) | 7종 | **0종** |
| 통합 방향 역전 (일반어→특정어) | 6건 | **1건** |
| 미매핑 재료 복구율 | — | **28.3%** (20,040건) |

**연쇄 미해소**: `볶은참깨 → 통깨` 인데 `통깨 → 참깨` 라, 같은 재료가 본문 표기에
따라 서로 다른 pill 로 갈라져 냉장고 매칭이 어긋나고 있었다. `load_alias_to_canonical()`
에서 끝까지 따라가 최종 대표어로 접도록 수정.

**행 순서 의존**: `돼지기름`을 `라드`와 `돼지비계`가 동시에 동의어로 주장해서 CSV
아래쪽 행이 조용히 이기고 있었다. 라드로 확정 (멸치육수도 같은 방식으로 육수 확정).

#### 통합 방향 뒤집기 (통용어를 대표어로)

`pill 노출 횟수` 대비 `원본 등장 횟수`를 비교해 "화면엔 자주 뜨는데 정작 아무도
그 단어를 안 쓰는" 대표어를 찾아냈다.

| 수정 | pill노출 | 원본등장 |
|---|---|---|
| 참치 ← 다랑어·황다랑어 | 605 | 1 |
| 페페론치노 ← 페퍼론치노 (페퍼에서 분리) | 484 | 0 |
| 죽순 ← 대나무순 | — | 0 |
| 디포리 ← 밴댕이 | — | 6 |
| 올리브유 (엑스트라버진에서 분리) | 1,675 | — |

기존 이름은 전부 동의어로 남겨 검색·매칭은 그대로 유지된다.

#### 과한 통합 분리

- **대파를 파에서 분리**: `대파`(11,229회)가 `파`(539회)에 묻혀 있었다. 사전 최대
  통합이었고 pill 12,088건이 전부 "파"로 떴다. 쪽파·실파는 원래대로 각자 유지.
  단 단독 표기 `파`는 대파로 흡수 — 한국 요리에서 그냥 "파"는 보통 대파를 뜻하고,
  분리만 하면 냉장고에 `파`를 넣어둔 사용자가 대파 레시피와 매칭되지 않는다.
- **오징어채 → 진미채**: 가공 건어물인데 생오징어로 합쳐져 있었다.
- **콜라·사이다 분리**: 탄산음료로 묶여 있었으나 요리에서 서로 대체가 안 된다.

#### 낯선 표시명 교체

`빨간고추 → 홍고추`(3,312건) / `머스타드 → 머스터드` / `월계수 → 월계수잎` /
`페스토 → 바질페스토`. 기존 이름은 동의어로 남김.

#### 재료 보강

- 신규 12종: 물엿, 케첩, 봄동, 또띠아, 목살, 땅콩버터, 스리라차, 참치, 대파,
  밥, 오이지, 딜
- 동의어 40여 종 흡수: `엑스트라버진 올리브오일→올리브유`, `집간장→간장`,
  `노른자→달걀`, `흑임자→검정깨`, `크러쉬드 레드페퍼→고춧가루`, `크래미→게맛살`,
  `비엔나소시지→소시지`, `어간장→간장`, `고추가루→고춧가루` 등
- **손질 수식어 자동 제거**(`llm_ingredient_extraction.py`): 사전 조회 실패 시
  `다진/삶은/냉동/건/신/고운` 등 접두어를 떼고 재시도. 사전에 실제로 있는 결과만
  채택하므로 오탐이 없다. 신김치→김치, 다진생강→생강, 냉동새우→새우 등
- **`깍두기`는 손대지 않음**: 사전에 `대분류=요리이름`으로 등록돼 있어 재료로는
  안 잡히는데, 분류 정책을 바꾸면 다른 요리이름 994개와 기준이 어긋난다

#### 소급 적용 (`renormalize_used_ingredients.py` 신설, LLM 호출 0회)

사전 정규화는 LLM 원본 출력을 입력으로 하는 결정론적 단계다. 미리보기 CSV에
남아 있는 `llm_raw_ingredients`(완료분의 98.2%, 32,559건)를 다시 정규화하면
LLM 을 다시 부르지 않고 결과를 갱신할 수 있다.

**안전장치**: 사전 변경으로 설명되지 않는 재료 소실이 있는 행은 건너뛴다
(`--old-dict` 로 보강 전 사전을 주면 '사라져도 되는 대표어' 집합을 자동 계산).
LLM 결과가 옛 사전에서 전부 미매핑이라 룰베이스 값이 남아 있던 행이 여기 해당하는데,
새 사전으로는 일부만 매핑돼 오히려 재료를 잃을 수 있다. 실제로 19건이 걸러졌다.

**중간에 겪은 사고**: 1차 실행이 `Lost connection to MySQL server` 로 끊겼다.
읽기 커넥션을 fetchall 이후에도 열어둔 채 12,000건을 쓰는 동안 원격(Railway)이
먼저 끊은 것. 읽기 커넥션을 즉시 닫고, 쓰기 중 끊기면 재연결 후 재시도하도록 수정.
스크립트가 멱등이라 데이터 손상은 없었다.

#### 검증 (실측)
- 제거되는 재료가 감사에서 확정한 12종과 정확히 일치하는 것을 미리보기 CSV로 확인
- 수식어 제거 회귀 확인: 생강→생강, 건포도→건포도, 신라면→신라면, 건새우→건새우
  (사전에 있는 말은 수식어처럼 보여도 그대로 유지됨)
- 상위어 통합 유지 확인: 방울토마토→토마토, 샤인머스캣→포도, 오뎅→어묵, 진간장→간장
- 실사용자 재료 48종 중 이번 변경에 영향받는 것은 파(4건)·대파(2건)뿐임을 확인
- `python -m py_compile` 에러 0건

### 냉장고 재료와 레시피 재료의 매칭 기준 일치시키기

"LLM이 뽑은 레시피 재료"와 "사용자가 냉장고에 넣은 재료"가 정말 같은 기준으로
매칭되는지 확인한 결과, 기준 자체는 같았지만 구멍이 세 개 있었다.

기준은 원래 같다 — 양쪽 다 같은 사전 파일을 쓴다:
- 레시피: LLM 원본 → `load_alias_to_canonical()` → `used_ingredients` 에 대표어 저장
- 냉장고: 사용자 입력 → `ingredientDict[입력]`(MyFridge.tsx) → 대표어로 저장

#### 구멍 ① 백엔드는 사전을 모른다
`app.py` 에는 재료 사전을 로드하는 코드가 아예 없다. `my_ingredients` 로 받은
문자열을 그대로 `FIND_IN_SET`/`REGEXP` 에 넣는다. 프론트가 대표어로 변환해
보내주는 동안은 맞지만, 저장된 값이 옛 대표어면 서버는 알아채지 못한다.

#### 구멍 ② 사전이 바뀌어도 저장된 냉장고 재료는 안 따라온다
실제로 8건이 어긋나 있었다. `미림`(4건, 사전에선 맛술이 대표어 — 이번 작업
이전부터 있던 문제)과 `파`(4건, 이번 대파 분리로 발생).

#### 구멍 ③ 화면 매칭률과 서버 매칭률의 기준이 달랐다
프론트 `calculateMatchRate` 는 양쪽을 다시 사전으로 변환해 비교하므로 미림·파도
맞힌다. 반면 서버는 원문 비교라 못 맞힌다. 결과적으로 **카드에 보이는 %는 맞는데
매칭률 정렬·부족재료 필터는 어긋나는** 상태였다 (정렬·필터는 SQL 에서 돈다).

#### 조치

**A. `migrate_user_ingredients.py` 신설** — 냉장고 재료를 현재 사전 대표어로 맞춘다.
사전을 고친 뒤 한 번씩 돌리면 된다. 같은 사용자가 이미 대표어 이름으로 그 재료를
갖고 있으면 이름만 바꿀 경우 같은 재료가 두 줄이 되므로, 옛 이름 행을 지운다.
→ 이름 변경 3건, 중복 삭제 5건 (190건 → 185건). 재실행 시 어긋남 0건 확인.

**B. 서버로 보내는 `my_ingredients` 를 대표어로 변환** — `recipeUtils.ts` 에
`getMyIngredientsAsKeywords()` 추가하고 `RecipeList.tsx` 가 이걸 쓰도록 변경.
변환 후 중복은 제거한다(파·대파가 둘 다 있으면 대파 하나로). 사전 캐시가 아직
안 올라왔으면 원래 이름을 그대로 쓰므로 기존 동작 대비 퇴행이 없다.
→ 앞으로 사전이 바뀌어도 서버·프론트 기준이 갈라지지 않는다.

**보너스: 룰베이스 배치와의 기준 불일치도 해소.** `update_used_ingredients_batch.py`
는 자체적으로 사전을 만드는데 연쇄를 풀지 않는다(`normal_dict[name] = base_name`).
그래서 코드에서만 연쇄를 풀면 신규 수집분(룰베이스가 임시로 채우는 값)은 여전히
다른 답을 낸다. 원본 데이터에 남아 있던 연쇄 6건을 **데이터 차원에서** 정리해
두 경로가 같은 답을 내도록 했다.

| 별칭 | 고치기 전 | 고친 후 |
|---|---|---|
| 찰토마토 | 완숙토마토 (→토마토) | 토마토 |
| 레드치커리 | 라디초 (→치커리) | 치커리 |
| 목심 | 돼지목살 (→목살) | 목살 |
| 볶은참깨 | 통깨 (→참깨) | 참깨 |
| 물오징어 | 살오징어 (→오징어) | 오징어 |
| 말백합 | 백합 (→조개) | 조개 |

이 6건은 LLM 경로에선 이미 코드가 연쇄를 풀고 있었으므로 DB 값은 그대로다
(재정규화 불필요 — 실측으로 확인).

#### 검증 (실측)
- 마이그레이션 재실행 결과 "바꿀 재료 없음" 확인
- 데이터 연쇄 정리 후 룰베이스 사전이 LLM 경로와 같은 대표어를 내는 것 확인
- `npx tsc -b` 오류 17건은 전부 기존 것이며 이번에 건드린
  `recipeUtils.ts`/`RecipeList.tsx` 에는 없음. 배포 빌드는 `vite build` 라 tsc 를
  거치지 않으므로 배포에 영향 없음
- `npx vite build` 통과

### 재료명 매칭에 띄어쓰기 조각 보완 추가

"다시다 감칠맛"이 사전에 `다시다`가 있는데도 매칭되지 않는 걸 확인하고,
정규화에 3단계 보완을 넣었다.

1. 정확히 일치
2. 손질 수식어(다진/삶은/냉동/건…)를 떼고 재시도
3. **띄어쓰기 조각으로 재시도** (신규)

한국어는 보통 뒤쪽 말이 핵심이므로("돼지고기 **목살**") 뒤에서 시작하는 조각을
먼저 채택하되, 뒤쪽이 사전에 없으면 앞쪽이 채택된다("**다시다** 감칠맛").
어느 단계든 사전에 실제로 있는 조각만 받아들이므로 없는 말을 지어내지 않는다.

효과: 미매핑 재료 복구율 **28.3% → 41.4%** (29,279건). 새로 잡히는 것 9,239건은
`모차렐라 치즈→치즈`, `알배기 배추→배추`, `재래식 된장→된장`, `돼지고기 삼겹살→삼겹살`,
`빨강/노랑 파프리카→파프리카` 같은 것들이다.

소급 재정규화 결과 **제거 0건, 순수 추가만 6,317행** — 기존 매핑을 건드리지 않고
못 잡던 것만 잡는다는 게 실측으로 확인됐다.

알려진 한계: `배 음료`(100건), `갈아만든 배`(34건)가 `배`로 잡힌다. 배 음료는
양념용 음료라 생배와는 다르지만, 전체 9,239건 중 1.4%라 그대로 뒀다.

### 내 냉장고: 영수증 사진으로 재료 담기 (1차 — 영수증만)

`MyFridge` 의 카메라 버튼과 `CameraCaptureSheet`(영수증/음식1개/음식여러개 타일)는
이미 있었지만 찍은 파일을 버리고 "준비 중" 안내만 띄우고 있었다. 실제 인식을 붙였다.

#### `backend/ingredient_vision.py` 신설
- Gemini 멀티모달로 이미지에서 재료명을 읽고, **레시피와 같은 사전**으로 정규화한다.
  여기서 다른 기준을 쓰면 인식해서 담아준 재료가 정작 레시피와 매칭되지 않는다.
- LLM 은 "이미지에서 이름을 읽는" 것까지만 하고, 사전 매칭은 결정론적으로 처리해
  없는 재료를 지어내는 걸 막는다.
- 사전은 프로세스 수명 동안 1회만 읽는다 (2,900행 CSV 파싱이 매 요청마다 돌면 느리다).
- 전용 키(`GEMINI_API_KEY_VISION`)를 둘 수 있고, 없으면 챗봇/공용 키로 내려간다.

#### `POST /api/ingredients/recognize`
multipart 로 이미지를 받아 `{ingredients: [{name, raw}], unmatched: [...]}` 를 돌려준다.
챗봇과 같은 하루 한도(`chat_service._consume_quota`)를 공유한다 — 같은 무료 키를 쓰므로.
8MB 초과 413, 지원하지 않는 형식 415, 한도 소진 429.

#### 프론트
- `IngredientRecognitionSheet` 신설: 인식 결과를 칩으로 보여주고 눌러서 뺄 수 있게 한다.
  **바로 담지 않는 이유**: OCR 은 반드시 틀리고, 사용자가 모르는 사이에 없는 재료가
  들어가면 추천 품질이 통째로 어긋난다. 기본값은 전부 선택이라 확인은 탭 한 번이면 끝난다.
- 보관함(냉장/냉동/실온)을 고르게 하고, 기존 `estimateExpiry` 로 유통기한을 잡아 담는다.
  이미 있는 재료는 건너뛴다.
- `shrinkImageForUpload()` 추가: 폰 원본(3~5MB)을 긴 변 1600px JPEG 으로 줄여 보낸다.
  실패하면 원본을 그대로 쓰므로 호출한 쪽은 신경 쓸 게 없다.
- **지금은 영수증만 동작한다.** 서버는 음식 사진 프롬프트도 갖고 있지만 인식 품질을
  아직 확인하지 않아 열지 않았다 (`mode !== 'receipt'` 조건만 풀면 열린다).

#### 검증 (실측)
테스트 영수증(9개 품목)으로 엔드포인트까지 실제 호출:

| 영수증 원문 | 인식 | 사전 정규화 |
|---|---|---|
| CJ 백설 물엿 700g | 물엿 | 물엿 |
| 하선정 까나리액젓 500ml | 까나리액젓 | 액젓 |
| 무항생제 대파 1단 | 대파 | 대파 |
| 한성 크래미 180g | 크래미 | 게맛살 |
| 오뚜기 케찹 500g | 케찹 | 케첩 |
| 돼지고기 목살 500g | 돼지고기 목살 | 목살 |
| 청양고추 100g | 청양고추 | 청양고추 |
| CJ 다시다 감칠맛 100g | 다시다 감칠맛 | 다시다 |
| 비닐봉투 | (모델이 제외) | — |

**8/8 매핑, 비닐봉투는 알아서 걸렀다.** 이 중 6개(물엿·게맛살·케첩·목살·대파·다시다)는
오늘 사전 감사·보강 덕분에 잡힌 것이다 — 어제 사전이었다면 8개 중 2개만 잡혔다.

- 오류 경로 확인: 이미지 없음 400, 텍스트 파일 415
- `npx tsc -b --force` 오류 18건은 전부 기존 것이며 이번에 건드린
  MyFridge/IngredientRecognitionSheet/imageUtils/CameraCaptureSheet 에는 없음
- `npx vite build` 통과

### 앨범에서 여러 장 한 번에 + 무엇을 찍었는지 모델이 판별

영수증 인식을 붙이고 나서 앨범/파일 경로에 세 가지 문제가 있는 걸 확인했다.

**① 고를 수 있는 형식과 서버가 받는 형식이 어긋나 있었다.**
파일 선택은 `accept="image/*"` 라 GIF·BMP·AVIF 까지 고를 수 있는데 서버는
jpeg/png/webp/heic/heif 5종만 받아, 그 밖의 형식을 고르면 415 로 튕겼다.
→ `shrinkImageForUpload()` 가 **항상 JPEG 으로 다시 뽑도록** 바꿨다. 브라우저가
그릴 수 있는 형식이면 무엇이든 통과한다. 이미 JPEG 이고 작으면 그대로 둔다
(다시 뽑아봐야 화질만 깎인다). 아이폰 HEIC 은 크롬이 디코딩하지 못하는데,
그 경우 원본이 그대로 가고 서버·Gemini 둘 다 HEIC 을 받으므로 문제없다.

**② 앨범에서 고른 사진이 영수증인지 음식인지 알 수 없었다.**
사용자에게 한 번 더 물어보는 UI 를 붙일 수도 있었지만, 탭만 늘어난다.
→ `_AUTO_PROMPT` 를 두고 **모델이 판단하게** 했다 (`mode=file`). 카메라의 3개
타일은 그대로 두는데, 모드 선택보다 "뭘 찍어야 잘 인식되는지" 알려주는 역할이
더 크기 때문이다.

**③ 한 장씩만 올릴 수 있었다.**
영수증이 여러 장이면 건건이 올려야 했다.
→ 앨범 선택에 `multiple` 을 붙이고 최대 5장까지 받는다. **여러 장이어도 LLM
호출은 1회다** — 한 요청의 parts 에 이미지를 여러 개 넣을 수 있어서, 장수만큼
하루 한도를 쓰지 않는다. 이게 되는지 먼저 실측으로 확인하고 설계했다.

서버 상한: 장당 8MB, 합계 16MB, 5장. 초과 시 413.

#### 검증 (실측)
- 영수증 2장 + `mode=file`(자동판별) → **재료 12개 인식, 사전 통과 12/12, 호출 1회**
  (`물엿, 액젓, 대파, 게맛살, 케첩, 목살, 청양고추, 다시다, 양파, 마늘, 애호박, 두부`).
  두 영수증의 `비닐봉투`와 `적립카드 할인`은 모델이 알아서 제외했다.
- 6장 → 413 / GIF → 415 / 이미지 없음 → 400
- `npx tsc -b --force` 오류 18건은 전부 기존 것, 이번 변경 파일에는 없음
- `npx vite build` 통과

### 사진에서 날짜까지 읽고, 담기 전에 사용자가 검토·수정하게

#### 구매일자·유통기한 인식
재료마다 구매일자를 적게 돼 있는데 사진에서 읽을 수 있는 건 읽는 게 낫다.
프롬프트 3종의 출력을 배열에서 객체로 바꿔 날짜를 함께 받는다.

```
{"purchase_date": "2026-08-30",
 "ingredients": [{"name": "우유", "expiry": "2026-09-05"}]}
```

우선순위는 이렇게 정했다:
1. **영수증** → 거래일시를 구매일자로
2. **포장지 사진** → 찍혀 있는 유통기한/소비기한을 그 재료의 expiry 로
3. 둘 다 없으면 → **업로드 시점(오늘)** 을 구매일자로

읽은 날짜는 그대로 믿지 않고 검증한다. 구매일자는 미래일 수 없고 1년보다
오래되면 오독으로 본다. 유통기한은 과거일 수 없고 10년 뒤도 오독으로 본다.
형식이 어긋나면(`2026/08/30` 등) 버린다.

포장지에서 실제로 읽은 유통기한은 `expiry` 로 넣는다 — 구매일 + 카테고리로
짐작하는 `estimatedExpiry` 보다 정확하므로 짐작값을 덮어쓰지 않고 대신 쓴다.

응답 파서는 모델이 옛 형식(이름만 담긴 배열)으로 답해도 받아들인다.
프롬프트를 바꿔도 가끔 배열로 돌아오는 경우가 있어, 그때 통째로 실패하지 않게 했다.

#### 검토·수정 UI
인식 결과를 그냥 담거나 빼는 것만 되던 걸, **틀린 항목을 직접 고칠 수 있게** 했다.

- 항목마다 `고치기` 버튼 → 내 냉장고의 재료 추가 입력과 같은 방식의 검색 드롭다운이
  열린다. 사전(동의어 포함)에서 찾아 대표어만 후보로 보여주고, 고르면 확정된다.
  직접 입력은 받지 않는다 — 사전에 없는 말을 담으면 레시피 매칭에서 빠진다.
- **사전에 없어 못 담던 항목도 이제 담을 수 있다.** 회색으로 표시하고 `고르기` 를
  누르면 사용자가 맞는 재료를 지정할 수 있다. 예전엔 취소선만 긋고 버렸다.
- 구매일자는 화면에서 바로 고칠 수 있고, 영수증에서 읽었는지 오늘 날짜인지 표시한다.
- 포장지에서 읽은 유통기한은 항목 옆에 배지로 보여준다.
- 고칠 게 없으면 `N개 반영하기` 한 번으로 끝난다.

#### 검증 (실측)
- 거래일시가 찍힌 테스트 영수증 → **구매일자 2026-08-29 정확히 인식**,
  재료 8개 전부 사전 통과
- 우유팩(유통기한)·두부팩(소비기한) 목업 → **2026-09-12 / 2026-09-05 둘 다 정확**,
  사진이라 purchase_date 는 null
- 파서 단위 검증: 신형식 / 옛 배열형식 / 코드펜스 / 깨진 응답 4종 모두 통과
- 날짜 검증: 미래 구매일·1년 초과 과거·잘못된 형식 → 전부 버려짐,
  과거 유통기한·10년 초과 → 버려짐
- `npx tsc -b --force` 오류 18건은 전부 기존 것, 이번 변경 파일에는 없음
- `npx vite build` 통과

### 배포된 이미지 인식이 500으로 죽던 문제 수정

앨범에서 영수증을 올렸는데 "사진을 읽지 못했어요"만 나온다는 제보. 배포된
백엔드에 직접 요청해 보니 **이미지 없이 호출해도 500** 이 났다 — 검증 코드에
닿기도 전에, 라우트 안의 `import` 에서 터지고 있었다.

원인: `ingredient_vision` → `llm_ingredient_extraction` → **`update_used_ingredients_batch`**
사슬. 마지막 모듈은 import 시점에 재료 사전 CSV 를 읽고 `backend.backend.*` 까지
끌어온다. 백엔드 서버는 그 파일의 "사전 정규화" 부분만 쓰는데, 배치용 의존까지
전부 로드되면서 배포 환경에서 무너졌다.

- `llm_ingredient_extraction` 의 `_connect_db`/`_used_ingredient_token_set` import 를
  `_batch_helpers()` 안으로 옮겨 **지연 로드**로 바꿨다. 이 둘은 배치 실행(`run`)
  에서만 쓴다. → 서버는 pandas + CSV 만으로 정규화가 돌아간다.
- 라우트의 import 를 try/except 로 감싸 실패 시 **503 + 트레이스백 로그**를 남긴다.
  전에는 500 이 나가면서 원인이 응답에도 로그에도 안 남아 한참 헤맸다.

실측: 로컬에서 이미지 없이 호출 → 400(정상), 영수증 → 200 + 구매일자 인식.
배치 모듈과 `backend.backend.*` 가 더 이상 함께 로드되지 않는 것도 확인.

### 파일 선택에서 "사진 찍기" 빼기

`accept="image/*"` 이면 OS 선택 메뉴에 "사진 찍기"가 함께 나오는데, 촬영은 위쪽
타일이 맡고 있어 중복이다. `accept` 를 형식 목록으로 좁혔다. 업로드 직전에 전부
JPEG 으로 다시 뽑으므로 실제로 받는 형식은 이 목록보다 넓다.
(메뉴 자체는 OS 가 그리는 것이라 강제할 수는 없다.)

### 문서 정리

- **`INGREDIENT_RECOGNITION_FEATURE.md` 신설** — 사진으로 재료 담기 전체 동작.
  열려 있는/닫아 둔 입구, 응답 형태, 사전 정규화를 서버에서 하는 이유, 날짜 인식
  우선순위와 검증 규칙, 검토·수정 UI 설계 의도, 업로드 제한, 호출 한도, 오류 응답,
  실측 결과, 알려진 한계. **배치 모듈 import 를 최상단으로 되돌리지 말라는 경고**도
  같이 적어 뒀다.
- `PROJECT_OVERVIEW.md` — 재료 동의어 처리 절에 **정규화 3단계**와 연쇄 해소,
  사전 수정 후 돌릴 두 스크립트를 추가. 배치 한도를 5,400 → 5,280 으로 고치고
  일일 한도 소진 시 즉시 중단하는 동작을 설명. 내 냉장고 페이지에 사진 담기 추가.
- `ENVIRONMENT_SETUP.md` — `GEMINI_API_KEY_VISION`, `GEMINI_VISION_MODEL` 추가.
  사진 인식이 챗봇과 하루 한도를 공유한다는 점 명시.
- `DATABASE_SCHEMA.md` — `user_ingredients` 에 사진으로 담은 값이 어떻게 들어가는지,
  사전이 바뀌면 `migrate_user_ingredients.py` 로 맞춰야 하는 이유 추가.
- `README.md` — 문서 목록에 신설 문서 추가.

### 카카오 재로그인이 Duplicate entry 로 막히던 문제 수정

카카오 로그인 시 이 오류로 로그인 자체가 안 되던 문제.

```
(1062, "Duplicate entry 'haejin_6299@hanmail.net-kakao'
        for key 'users.unique_provider_user'")
```

원인은 **코드와 스키마의 모순**이었다.

- 유니크 키가 `unique_provider_user (email, provider)` 뿐이라 `deleted_at` 을
  구분하지 않는다.
- 그런데 `get_or_create_user()` 는 탈퇴 계정을 찾으면 *"이메일 재사용 가능"* 이라며
  같은 `(email, provider)` 로 **새 행을 INSERT** 하려 했다.

즉 재가입을 허용하려는 코드를 스키마가 막고 있었다. 해당 계정(id=8)은
2026-08-29 에 탈퇴한 상태라, 그 뒤로 카카오 로그인이 계속 실패했다.

**고친 방식**: 탈퇴 행의 `email` 을 `deleted+{id}+{원래이메일}` 로 비켜 놓아 자리를
비운 뒤 새 계정을 만든다. id 를 접두어에 넣어 같은 이메일이 여러 번 탈퇴해도
서로 부딪히지 않는다. 탈퇴 행은 그대로 남으므로 그 계정에 딸린 데이터
(재료 43·즐겨찾기 9·기록 28·완료 17)는 **새 계정에 딸려 오지 않는다** —
"탈퇴했으면 새로 시작" 이라는 뜻이고, 원래 코드의 의도(이메일 재사용)와도 맞는다.

배포되면 다음 로그인에서 자동으로 풀린다. 수동 데이터 조작은 필요 없다.

> ⚠️ **로컬(이메일·비밀번호) 가입 경로는 정책이 다르다.** 거기는 탈퇴 계정을
> **재활성화**(`deleted_at = NULL`, 같은 user_id 유지)해서 예전 데이터가 그대로
> 돌아온다(`app.py` 회원가입 분기). 이번에 OAuth 쪽만 고쳤으므로 두 경로의
> 동작이 서로 다르다 — 맞출지는 별도로 정해야 한다.

#### 검증 (실측)
- 프로덕션 DB에 대고 실제 SQL 을 트랜잭션으로 실행해 확인 후 **롤백**
  - 탈퇴 행 id=8 → `deleted+8+haejin_6299@hanmail.net` 로 비켜남
  - 새 계정 생성됨, 그 계정의 냉장고 재료 0건 (옛 데이터가 안 딸려옴)
- 롤백 후 DB 원상태 확인 (users 8행, id=8 이메일 그대로)
- `python -m py_compile` 에러 0건

### 탈퇴 후 재가입 정책을 두 로그인 경로에서 통일

직전 수정으로 소셜 로그인은 "새 계정으로 시작"이 됐는데, 로컬(이메일·비밀번호)
가입은 여전히 **재활성화**(`deleted_at = NULL`, 같은 user_id 유지)였다. 같은
이메일이어도 어떤 수단으로 들어오느냐에 따라 예전 데이터가 돌아오기도 하고
안 돌아오기도 하는 상태였다.

로컬 가입도 소셜과 같게 맞췄다 — 탈퇴 행의 `email` 을 `deleted+{id}+{원래이메일}`
로 비켜 놓고 새 계정을 만든다. 탈퇴 행과 거기 딸린 데이터는 그대로 남고 새
계정에는 딸려 오지 않는다.

`provider_id` 는 건드리지 않았다. 유니크 키가 아니고(`INDEX idx_provider_id`),
그 값으로 조회하는 쿼리도 없다. 사용자 조회는 전부 `deleted_at IS NULL` 로
거르므로 비켜 놓은 행이 끼어들지 않는다.

#### 검증 (실측)
- 프로덕션 DB에 탈퇴한 로컬 계정 + 재료 1건을 만들고 재가입을 실행 후 **롤백**
  - 탈퇴 행 → `deleted+68+align_test@example.com` 으로 비켜남, 재료 1건 보존
  - 새 계정 생성, 그 계정의 재료 0건
  - 로그인 조회(`deleted_at IS NULL`)가 새 계정을 반환하는 것 확인
- 롤백 후 DB 원상태 확인 (users 8행, 테스트 잔재 0건)

### 사진 인식이 배포에서 계속 죽던 문제 — 의존 사슬을 끊어서 해결

지연 로드까지 넣었는데도 배포된 백엔드는 여전히 503(모듈 로드 실패)이었다.
Railway 로그를 볼 수 없어 원인을 특정하기 어려웠고, 후보(pandas 실패 /
`ingredient_management` 미배포 / 그 밖)를 하나씩 확인하려면 매번 배포를 기다려야
했다.

그래서 **원인을 좁히는 대신 구조를 바꿔 후보를 전부 없앴다.**

`backend/ingredient_dictionary.py` 신설 — 재료 사전 정규화를 **표준 라이브러리
(csv)만으로** 구현하고, 웹 서버와 배치가 **함께 쓰는 단 하나의 구현**으로 삼았다.

| | 전 | 후 |
|---|---|---|
| 백엔드가 import 하는 것 | `ingredient_management.llm_ingredient_extraction` (→ pandas, pymysql, 배치 모듈) | `backend/ingredient_dictionary` (stdlib only) |
| 구현 위치 | 배치 스크립트 안 | 의존이 가장 적은 곳 |

- `ingredient_vision` 은 이제 `ingredient_dictionary` 만 가져온다.
  실측: import 후 `pandas` / `pymysql` / `ingredient_management` 셋 다 **로드 안 됨**.
- `llm_ingredient_extraction` 은 자기 구현을 지우고 이 모듈에 위임한다.
  예전 이름(`_normalize_key`, `_resolve_canonical`, `_PREP_PREFIXES`,
  `_token_fallback`, `load_alias_to_canonical`)은 별칭으로 남겨 기존 스크립트가
  그대로 돌아간다.
- `renormalize_used_ingredients.allowed_removals()` 가 모듈 상수를 갈아끼우는
  방식(`_M._INGREDIENT_CSV = ...`)을 쓰고 있었는데, 이제 `load_alias_to_canonical(path)`
  로 경로를 직접 넘긴다.

**동작이 하나도 안 바뀌었다는 걸 실측으로 확인했다.**
- 별칭 1,818개 — 기존에만 있는 키 0, 신규에만 있는 키 0, 값이 다른 키 0
- 코퍼스 고유 재료명 **13,876종 전부 resolve 결과 일치** (불일치 0)
- 배치 3종(`llm_ingredient_extraction`, `renormalize`, `migrate`, 룰베이스 배치) 정상 동작
- 로컬 엔드포인트: 이미지 없이 400, 영수증 → 200 + 구매일자 `2026-08-29` 인식

### 로그인해도 기본 재료가 안 채워지던 문제 수정

새 계정으로 로그인하면 내 냉장고가 텅 비어 있었다.

초기 재료 투입 조건이 **"DB 로드가 실패했을 때"** 로만 돼 있어서
(`dbLoadAttempted && dbLoadFailed && localStorage 비어있음`), 갓 만든 계정처럼
**"조회는 성공했는데 재료가 0건"** 인 경우가 통째로 빠졌다.

조건을 "DB 조회를 마쳤고(성공/실패 무관) 로컬에도 없으면" 으로 고쳤다.

여기에 **계정별 표시**(`myfridge_seeded_{userId}`, 비회원은 `myfridge_seeded_guest`)
를 함께 뒀다. "비었으면 넣는다"만으로는 사용자가 일부러 전부 지웠을 때 새로고침만
해도 기본 재료가 되살아나기 때문이다. 한 번 넣었으면 다시 넣지 않는다.

### "사진 찍기"는 웹에서 없앨 수 없음 (확인)

`accept` 를 형식 목록으로 좁혀 봤지만 OS 선택 메뉴의 "사진 찍기"는 그대로 나온다.
`<input type="file">` 이 이미지를 받는 한 촬영 항목은 OS 가 항상 붙이며,
`capture` 속성은 촬영을 **강제**하는 것이지 그 반대를 지정하는 표준 방법은 없다.

동작상 무해하다 — 거기서 찍어도 `mode=file` 로 들어와 자동 판별을 타므로 위쪽
카메라 타일과 결과가 같다. 문서에 이 사실과, 중복이 거슬리면 카메라 타일을 없애고
입구를 하나로 합치는 선택지가 있다는 것을 적어 뒀다.

### 재료가 0개인데 매칭률이 뜨고, 냉장고 요리가 텅 비던 문제

새 계정으로 로그인했더니 (1) 내 냉장고는 비어 있는데 레시피 카드에 매칭률이
100%/65% 처럼 뜨고 (2) 냉장고 요리 페이지에 아무것도 안 나온다는 제보.

DB 확인 결과 그 계정(id=70, 오늘 생성)의 `user_ingredients` 는 실제로 **0건**이었다.
두 증상 모두 프론트 쪽 문제였고, 원인이 서로 달랐다.

#### (1) 매칭률 — 로컬 저장소가 계정별로 나뉘어 있지 않았다

`myfridge_ingredients` 는 localStorage 한 칸에만 저장되고 **계정별로 나뉘어 있지
않은데, 지우는 건 로그아웃할 때뿐**이었다. 그래서 비회원으로 쓰다가 가입하거나
다른 계정으로 들어오면 이전 재료가 그대로 남는다.

- 내 냉장고 페이지는 DB(비어 있음)를 보여주고
- 냉장고 요리·요즘 인기·챗봇은 localStorage 를 읽는다

→ `AuthContext` 에 `clearDataOfOtherAccount()` 추가. `last_active_user_id` 와
비교해 계정이 바뀌었을 때만 냉장고 재료·마이페이지 목록·레시피 목록 캐시를
지운다. 토큰 로그인(소셜 리다이렉트 포함)과 세션 복원 양쪽에서 호출한다.
같은 계정이면 아무것도 지우지 않는다.

#### (2) 냉장고 요리가 텅 빈 것 — 렉이 아니라 필터

기본값이 `재료매칭률순` + **매칭 구간 30~100%** 였다. 재료가 하나도 없으면 모든
레시피가 0% 라서 30% 미만으로 **전부 걸러진다.** 로딩이 안 끝난 것처럼 보였다.

→ `getInitialSortBarState()` 가 냉장고가 비었는지 보고, 비었으면
`최신순 + [0, 100]` 으로 시작한다. 초기 로드에 [30,100] 을 강제하던 자리도
이 기본값을 따르도록 고쳤다.

### 사진 인식 502 — 원인을 응답으로 구분할 수 있게

의존 사슬을 끊은 뒤 배포 백엔드에서 모듈은 정상 로드됐지만(이미지 없이 호출 시
400 확인), 실제 인식은 502 였다. 배포 로그를 볼 수 없어 "사전 파일이 없다" 와
"LLM 호출이 실패했다" 를 구별할 수 없었다.

- `ingredient_dictionary` 가 사전 CSV 를 **여러 경로에서 찾는다**
  (`INGREDIENT_DICT_CSV` 환경변수 → `frontend/public/` → `backend/` → 루트).
  `chat_service` 도 같은 이유로 후보 목록을 쓰고 있었다 — 다만 거긴 실패를 조용히
  삼켜서, 챗봇이 동작한다고 해서 CSV 가 있다는 뜻은 아니었다.
- 어디서도 못 찾으면 `DictionaryUnavailable` 을 던지고, 라우트가 이를 **503 +
  "재료 사전을 불러오지 못했어요"** 로 구분해 응답한다.
- 그 밖의 실패는 502 에 `detail`(예외 클래스명)을 함께 실어 원인을 좁힐 수 있게 했다.

프로덕션 챗봇이 `provider: gemini` 로 정상 응답하는 것은 확인했으므로 키와 텍스트
모델은 살아 있다.

### 문서 정리 — 계정별 재료 로직 / 정렬 기본값 분기 / 사전 CSV 사본

오늘 고친 것들 중 "코드만 봐서는 왜 그런지 알기 어려운" 것들을 문서에 남겼다.

- **`README.md`** — 냉장고요리 필터/정렬 절에 **`0. 진입 시 기본값`** 추가.
  내 재료가 있으면 `재료매칭률순 + 30~100%`, **없으면 `최신순 + 0~100%`** 로
  갈린다는 것과, 그렇게 안 하면 화면이 텅 비는 이유(0% 가 30% 필터에 전부 걸림)를
  적었다. 렉으로 오해하기 딱 좋은 지점이라 이유까지 남겼다.

- **`PROJECT_OVERVIEW.md`** — **`내 냉장고 재료는 어디에 저장되나 (계정 전환 주의)`**
  절 신설. 재료가 DB 와 localStorage 두 곳에 있고 **페이지마다 읽는 곳이 다르다는
  것**(내 냉장고=DB, 냉장고요리·요즘인기·챗봇=localStorage), `myfridge_ingredients`
  가 계정별로 나뉘어 있지 않아 계정 전환 시 오염된다는 것,
  `clearDataOfOtherAccount()` 가 이를 막는 방식, 기본 재료 자동 투입 3조건과
  각 조건이 왜 필요한지를 정리했다.

- **`INGREDIENT_RECOGNITION_FEATURE.md`**
  - **사전 CSV 사본이 두 개인 이유**와 `scripts/sync_ingredient_dict.py` 사용법,
    탐색 순서(`INGREDIENT_DICT_CSV` → `frontend/public/` → `backend/` → 루트)
  - 오류 응답표에 **`재료 사전 못 찾음 → 503`** 추가. 원인을 응답만 보고 구분할 수
    있게 나눈 이유(배포 로그를 못 보는 상황)와, 실제로 그 덕에 원인을 한 번에
    찾았다는 것
  - **"사진 찍기"를 못 없애는 이유**를 최종 정리. `accept` 로는 안 되고, 유일한
    방법인 `accept` 제거는 **'사진 보관함'까지 잃는다**는 것. 카메라 타일을 없애는
    대안도 타일마다 프롬프트가 달라 인식 품질을 잃는 쪽이라는 것까지.

- **`ENVIRONMENT_SETUP.md`** — `INGREDIENT_DICT_CSV` 추가.

### 기본 재료의 D-표기가 안 보이던 문제 수정

기본 재료 중 달걀·감자에 구매일을 넣어 D-표기 예시를 보여주기로 해뒀는데
화면에 안 나왔다.

추정 유통기한(`estimatedExpiry`)을 채우는 effect 의 의존성이 `[categoryMap]`
뿐이었다. 그래서 **카테고리 표가 먼저 도착하고 그 뒤에 재료가 들어오면**
(기본 재료 자동 투입, 사진 인식으로 담기 등) 추정값이 영영 안 채워졌다.
`purchase` 는 들어갔지만 D-표기가 읽는 `expiry || estimatedExpiry` 는 비어 있던 것.

의존성에 재료 목록을 추가했다. `fill()` 은 바뀐 게 없으면 같은 배열을 그대로
돌려주므로 setState 가 상태를 실제로 바꾸지 않아 반복 실행되지 않는다.

### 사진 인식: 재료별 보관함 예측 + 사전 미매칭 수집

#### 재료마다 보관함이 다르다
전엔 시트 하단에서 냉장/냉동/실온을 **하나만** 골라 전부 거기 담았다. 우유는
냉장, 만두는 냉동, 소금은 실온인데 한 칸에 몰아넣게 되는 구조였다.

- 프롬프트 출력에 `storage`(fridge/frozen/room)를 추가해 **모델이 재료마다** 짐작한다.
  이상한 값이 오면 냉장으로 둔다.
- 시트에서 항목마다 드롭다운으로 보여주고, 틀리면 그 자리에서 바꾼다.
- 담을 때 보관함별로 나눠 담는다.

실측(테스트 영수증): 물엿·액젓·케첩·다시다 → 실온 / 대파·게맛살·목살·청양고추 → 냉장.

#### 사전에 없던 이름을 모아 둔다
사진 인식 품질은 모델보다 **사전이 얼마나 두꺼운지**에 더 크게 좌우된다(오늘
실측으로 확인). 무엇이 자주 걸리는지 알아야 사전을 보강할 수 있는데 그냥
흘려보내면 다시 알 방법이 없었다.

`ingredient_dictionary_misses` 표 신설 — 사진에서 읽혔지만 사전에 없던 이름을
쌓는다. 같은 이름이 또 걸리면 `hit_count` 가 올라간다. 조회는 이렇게 한다:

```sql
SELECT raw_name, hit_count, last_mode, last_seen
FROM ingredient_dictionary_misses ORDER BY hit_count DESC LIMIT 50;
```

기록에 실패해도 인식 자체는 성공시켜야 하므로 예외는 삼킨다.

#### 날짜 고르기를 앱 기존 모달로
구매일자를 고치려고 누르면 `<input type="date">` 의 **OS 기본 달력**이 떠서 닫기
버튼이 없고 버튼 양식도 앱과 달랐다. 이미 있는 `IngredientDateModal`
(우측 상단 닫기 + 취소/확인 + `CustomCalendar`)을 그대로 쓰도록 바꿨다.

#### 사전 보강
`환타`(형제=콜라), `가브리살`(형제=목살) 추가. 동의어로 `환타오렌지`·`환타포도`·
`돼지가브리살` 도 함께. 백엔드 사본도 동기화했다.

### [모두 삭제] 해도 기본 재료가 되살아나던 문제

"모두 삭제를 눌러도 삭제가 안 될 때가 있다" 는 제보. 실제로는 삭제된 뒤 **기본
재료가 곧바로 다시 들어오고 있었다.**

기본 재료 투입 effect 의 의존성에 `frozen/fridge/room` 이 들어 있다(DB 로드가
끝난 뒤 판단해야 하므로 필요하다). 그래서 재료를 전부 지우면 그 effect 가 다시
돌고, `myfridge_seeded_{userId}` 표시가 없는 계정이면 기본 재료를 또 넣었다.

- 이 화면에 있는 동안 판단이 한 번 끝났으면 다시 넣지 않도록 `seedDecidedRef` 추가.
- **재료가 이미 있으면 그 자리에서 표시를 남긴다.** 예전엔 "넣을 때만" 표시를
  남겨서, 이전에 다른 경로로 재료가 채워진 계정은 표시가 없어 삭제할 때마다
  되살아났다.

### 삭제 토스트를 되돌리기 안내로

시스템 확인창이 `정말 삭제하시겠습니까?` 를 물은 **직후에 토스트가 같은 질문을
또** 했다. 넘겨받은 `message` 는 쓰지도 않고 하드코딩된 질문을 보여주고 있었다.
게다가 하단에 낮게 깔려 잘 보이지도 않았다.

확인은 시스템 확인창이 이미 받았으므로, 토스트는 **무엇이 지워졌는지 알려주고
되돌릴 기회만** 준다: `모두 지웠어요` + `[되돌리기]` + 닫기(×).
하단 내비게이션·홈 인디케이터를 피하도록 위치를 올리고(`env(safe-area-inset-bottom)`)
그림자를 뚜렷하게 했다.

### 기본 재료에 `맛술` 이 두 개 담기던 문제

기본 목록의 `미림` 과 `맛술` 은 대표어가 둘 다 `맛술` 이라, 변환 후 같은 재료가
두 번 담겼다. 변환한 뒤 중복을 걸러 준다. `SAMPLE_PURCHASE_ITEMS`(구매일 예시를
넣는 달걀·감자) 비교도 대표어 기준으로 바꿨다.

이미 그렇게 담겨 있던 **DB 데이터도 정리**했다 — 5건(맛술 3, 고추장 2). 날짜
정보가 있는 행을 남기고 나머지를 지웠다. 정리 후 중복 0건 확인.

### [모두 삭제] 가 안 되던 진짜 원인 — 확인창이 부른 focus 경쟁

앞선 수정(기본 재료 재투입 방지) 뒤에도 여전히 되살아난다는 제보. 원인이 달랐다.

```js
// 앱으로 돌아오면 DB에서 최신화 (다른 기기에서 바꿨을 수 있으므로)
window.addEventListener('focus', refreshOnResume);
```

`window.confirm` 이 뜨면 창이 **포커스를 잃고**, 확인을 누르면 **다시 얻는다.**
그 focus 이벤트로 `refreshOnResume` 이 돌아 **DB의 옛 목록을 읽어 방금 지운 상태를
덮어썼다.** 사용자에겐 "되돌리기를 누르지도 않았는데 재료가 되살아나는" 것으로 보였다.

`hasChanges` 가드가 있었지만 못 막는다. 삭제 직후에는 아직 갱신되기 전이고,
이벤트 핸들러가 붙잡고 있는 값도 옛것이다.

**두 겹으로 고쳤다.**

1. `localEditPendingRef` — 편집하는 순간 **동기적으로** 세우는 ref. 서버 저장이
   성공하면 내린다. `refreshOnResume` 이 이 표시를 보고 건너뛴다. 상태가 아니라
   ref 라서 "아직 리렌더 전" 이나 "핸들러가 옛 값을 붙잡고 있음" 문제가 없다.
   삭제·복원·재료 추가·사진으로 담기 모든 편집 지점에 세운다.
2. `window.confirm` 제거 — 애초에 포커스를 잃을 일을 없앴다. 되돌리기 토스트(7초)가
   안전장치 역할을 하므로 확인을 두 번 받을 필요도 없다(사용자도 이 중복을 지적했다).

### [모두삭제] 확인을 앱 자체 팝업으로 정리

`window.confirm` 을 없앤 뒤 "확인 없이 바로 지우는" 상태였는데, 최종적으로
**앱 공용 `Dialog` 로 확인을 받는** 쪽으로 정리했다.

왜 확인을 남기나:
- 한 번에 최대 17개를 지우는 동작이다.
- [모두삭제] 버튼이 섹션 제목 옆의 작은 버튼(높이 24px)이라 모바일에서 잘못
  누르기 쉽다.
- 되돌리기 토스트는 7초짜리라, 못 보고 넘기면 복구할 방법이 없다.

왜 `window.confirm` 이 아니라 `Dialog` 인가:
- **포커스를 잃지 않는다.** OS 창이 뜨면 창이 blur→focus 되면서 `refreshOnResume`
  이 돌아 방금 지운 상태를 DB 값으로 덮어썼다(이번 사건의 진짜 원인).
- OS 창이라 앱 밖의 일처럼 보였고, 공용 `Dialog` 의 문서에 적힌
  "새 팝업은 반드시 이 컴포넌트를 쓸 것" 규칙에서도 벗어나 있었다.

지금 흐름: `냉장 재료를 모두 지울까요? / 17개를 지웁니다. 지운 뒤 잠깐 동안
되돌릴 수 있어요.` → [취소] [모두 지우기] → 지운 뒤 `모두 지웠어요 [되돌리기]`.

개별 재료 삭제는 그대로 확인 없이 지우고 되돌리기 토스트만 띄운다. 하나짜리
동작에까지 확인을 받으면 번거롭기만 하다 — 위험한 쪽에만 확인을 붙였다.

### 되돌리기 토스트 제거, 삭제 확인 문구 정리

"지우면 지우는 거지 되돌리는 건 또 뭐냐" — 되돌리기 기능을 아예 걷어냈다.
지우기 전에 묻고, 누른 뒤에는 그대로 지운다. 안전장치를 두 겹으로 두는 대신
**묻는 시점 한 곳으로 모았다.**

지운 것:
- `Toast` 컴포넌트, `showToast()`, `toast`/`toastTimeout` 상태
- `undoDelete()` / `handleCancelDelete()`(복원 로직)
- `DeletedInfo` / `ToastState` 타입, `TOAST_DURATION` 상수
- 삭제된 재료를 되살리려고 들고 있던 사본(`ingredients: [...prev]`)

`removeTag`/`removeAll` 은 이제 상태만 비운다. 짧은 안내 토스트(`showPrepNotice`)는
삭제와 무관한 다른 안내에 쓰이므로 그대로 둔다.

확인 팝업 문구:

```
냉장 재료를 모두 지울까요?
17개를 지웁니다.
        [취소]  [모두 지우기]
```

### 재료를 모두 지워도 매칭률이 계속 나오던 진짜 원인 — 목록 화면의 두 번째 투입 경로

"내 냉장고에서는 잘 지워지는데 냉장고 요리·요즘 인기로 넘어가면 여전히 매칭률이
나온다" 는 제보. 배포된 사이트에서 직접 재현해 원인을 잡았다.

재현 절차와 관찰:
1. 저장소를 비우고 내 냉장고 진입 → 기본 재료 30개 투입 (중복 없음 = 최신 빌드 확인)
2. [모두 지우기] ×3 → `myfridge_ingredients` 가 `{frozen:[],fridge:[],room:[]}` 로
   **정상 저장됨** (삭제 자체는 잘 되고 있었다)
3. 냉장고 요리로 이동 → **재료 30개가 되살아나 있음.** 새 타임스탬프 id 에
   `맛술` 이 두 개 — MyFridge 의 투입 경로(중복 제거 적용됨)가 아니라는 증거

원인은 **`RecipeList.tsx` 가 마운트될 때 재료가 비어 있으면
`initializeDefaultIngredients()` 로 기본 재료를 채우고 있었던 것.** MyFridge 와는
별개의 두 번째 투입 경로였고, 계정별 투입 표시(`myfridge_seeded_*`)도 중복 제거도
없었다. 그래서 냉장고를 비운 직후 목록 화면에 들어가면 매번 다시 채워졌다.

- `RecipeList` 의 마운트 effect 에서 투입을 걷어내고 `setMyIngredients(getMyIngredients())`
  만 남겼다. 비어 있으면 매칭률 0%, 정렬은 최신순으로 시작한다(의도된 동작).
- `recipeUtils.initializeDefaultIngredients()` (107줄)를 **삭제**했다. 남겨두면 언제든
  다시 불려 같은 사고가 난다. 자리에 "여기에 비슷한 함수를 다시 만들지 말 것" 이라는
  설명을 남겼다.
- 기본 재료 투입은 이제 **MyFridge 한 곳**에서만 한다. 거기에만 계정별 표시와 DB 상태
  확인이 있어 "한 번만" 넣는 판단이 가능하다.

같은 확인에서 **정렬 기본값(최신순)과 삭제 확인 팝업은 의도대로 동작**하는 것도
실측했다 — `냉동 재료를 모두 지울까요? / 3개를 지웁니다. / [취소] [모두 지우기]`.

### 재료가 없으면 레시피를 아예 안 보여주던 문제

재료를 모두 지우면 정렬이 최신순으로 시작하도록 고쳐 뒀는데, 정작 화면에는
`등록된 재료가 없습니다` 안내만 뜨고 레시피가 하나도 안 나왔다.

```js
const shouldShowNoIngredientsMessage = hasNoIngredients || (hasOnlyDefaultEgg && hasNoRecipes);
```

재료가 0개면 **레시피가 있든 없든** 안내로 화면을 덮고 있었다. 하지만 재료가
없다는 건 "매칭률이 전부 0%" 라는 뜻일 뿐, 보여줄 레시피가 없다는 뜻이 아니다.
서버도 정상적으로 응답한다 — `sort_by=date` 로 **총 44,930건, 각 `match_rate=0`**
(실측).

안내는 **보여줄 레시피까지 없을 때만** 띄우도록 조건을 바꿨다.

```js
const shouldShowNoIngredientsMessage = (hasNoIngredients || hasOnlyDefaultEgg) && hasNoRecipes;
```

이제 냉장고가 비어 있으면 최신순으로 레시피가 쭉 나오고 매칭률은 0% 로 표시된다.

### 냉장고가 비었을 때 안내 배너

재료가 없으면 모든 카드가 0% 로 뜬다. 이유를 모르면 고장난 것처럼 보이므로,
목록 위에 얇은 배너 한 줄을 둔다.

```
내 냉장고에 재료를 등록하면 매칭률순으로 볼 수 있어요.      등록하기 ›
```

- 재료가 **0개이고 보여줄 레시피가 있을 때만** 나온다. 재료가 하나라도 생기면 사라진다.
- 목록을 가리지 않는 높이로 두고, 누르면 내 냉장고로 바로 간다.

#### 검증 (실측)
로컬 미리보기(운영 빌드)에서 냉장고를 비우고 확인:
- 배너 표시 O / `등록된 재료가 없습니다` 안내 사라짐 O
- 정렬 `최신순`, 카드 30개 렌더, 매칭률 전부 `0%`

> 미리보기는 `.env.local` 때문에 API 를 `localhost:5000` 으로 보게 되어 더미 데이터로
> 그려진다(포트 4173 은 백엔드 CORS 화이트리스트에도 없다). 실제 API 는 별도로
> 확인했다 — `sort_by=date` 로 총 44,930건, 각 `match_rate=0`.

### 냉장고가 비면 정렬을 항상 최신순으로 (재진입해도 유지)

배너의 `등록하기 ›` 로 내 냉장고에 갔다가 냉장고 요리로 돌아오면, 정렬은 최신순인데
`등록된 재료가 없습니다` 안내만 뜨고 카드가 하나도 안 나오는 경우가 있었다.

원인은 **매칭 구간**이었다. 정렬만 최신순으로 바뀌고 구간은 `30~100%` 로 남아 있으면,
재료가 없어 모든 레시피가 0% 인 상태에서 **전부 걸러진다.** 세 갈래로 새고 있었다.

1. 세션 복원(`recipe_list_state`)이 예전 설정(`매칭률순` + `30~100%`)을 되살렸다.
2. 초기 로드용 기본값이 두 곳에 `[30, 100]` 으로 **하드코딩**돼 있었다
   (한 곳만 고쳐 뒀고 나머지 두 곳이 남아 있었다).
3. 재료를 지운 뒤 화면에 그대로 머무르면 아무도 구간을 되돌리지 않았다.

고친 내용:
- 세션 복원 시 냉장고가 비어 있으면 매칭 기반 정렬(`match`/`expiry`)은 `latest` 로,
  구간은 `[0, 100]` 으로 바꿔 복원한다.
- 하드코딩된 `[30, 100]` 두 곳을 `initialSortBarState.matchRange` 로 교체 —
  냉장고 상태에 따라 갈리는 값 하나만 보게 했다.
- **냉장고가 비면 정렬·구간을 다시 맞추는 effect 추가.** 재료가 생기면 건드리지
  않는다(그때부터는 사용자가 고른 정렬이 유효하다).

#### 검증 (실측)
운영 빌드 미리보기에서 빈 냉장고로:
- 첫 진입: 최신순 / 카드 30개 / 매칭률 전부 0% / 배너 O / 안내문구 X
- `등록하기 ›` → 내 냉장고 이동 확인
- 하단 탭으로 냉장고 요리 **재진입**: 최신순 / 카드 30개 / 0% / 재료 되살아나지 않음
- 세션에 `매칭률순 + [30,100]` 이 남아 있는 상태로 진입해도 최신순으로 교정되는 것 확인

### 파일 선택에서 '사진 찍기' 로 찍어도 모드를 물어본다

파일 선택을 누르면 OS 가 `사진 보관함 / 사진 찍기 / 파일 선택` 을 띄우는데, 웹에서
촬영 항목만 빼는 표준 방법이 없다. 진짜 문제는 미관이 아니라 **거기서 찍으면
타일(영수증/음식1개/음식여러개)을 거치지 않아 무엇을 찍었는지 모른 채 처리된다는
것**이었다. 타일마다 프롬프트가 다르므로 분기가 빠지면 인식 품질이 떨어진다.

촬영으로 들어온 파일은 **만들어진 시각이 방금**이다(앨범 사진은 대개 예전 날짜).
`looksJustTaken()` 이 이를 단서로 잡아, 바로 처리하지 않고 타일 화면으로 돌아가
묻는다.

```
방금 찍은 사진, 무엇인가요?
사진은 그대로 두었어요. 다시 찍지 않아도 돼요.

  [영수증]   [음식 1개]   [음식 여러 개]     ← 노란 테두리가 깜빡임

     모르겠어요 · 알아서 인식해 주세요
```

- **다시 찍게 하지 않는다.** 받은 사진을 들고 있다가 고른 모드로 보낸다.
- 고르기 싫으면 하단 버튼으로 자동 판별(`mode=file`)을 탈 수 있다.
- 판별은 시각 기반이라 완벽하지 않다(방금 찍은 사진을 앨범에서 골라도 물어본다).
  그 경우에도 한 번 더 묻는 것뿐이라 손해가 없다.

#### 검증 (실측)
운영 빌드 미리보기에서 파일 입력에 `lastModified = 지금` 인 파일을 넣어 확인:
- `방금 찍은 사진, 무엇인가요?` 표시 O / 타일 3개 모두 깜빡임 O
- 하단 버튼이 `모르겠어요 · 알아서 인식해 주세요` 로 바뀜 O
- `영수증` 타일 클릭 → **카메라가 다시 열리지 않고** 들고 있던 사진으로 인식 진행 O

### 서비스워커 캐시 버전 올림 (v1.0.1 → v1.0.2)

`sw.js` 는 파일 안에 **"배포할 때마다 이 값을 올려야 새 코드가 실제로 반영된다"**
고 적혀 있는데, 오늘 하루 여러 번 배포하면서 한 번도 올리지 않았다.

지금은 HTML 문서를 네트워크 우선으로 서빙하므로 예전만큼 치명적이진 않다. 다만
`CACHE_NAME` 이 그대로면 서비스워커 파일 자체가 바뀌지 않아 **재설치·활성화가 안 되고
`activate` 의 옛 캐시 정리도 걸리지 않는다.** PWA 로 설치해 쓰는 경우 옛 자원이
남아 있을 수 있다.

> 앞으로 프론트를 배포할 때는 `frontend/public/sw.js` 의 `CACHE_NAME` 을 함께 올릴 것.

### 촬영 후 모드를 묻는 화면이 "아무 일도 안 일어난 것" 처럼 보이던 문제

"사진 찍기로 찍으면 질문 없이 그냥 타일 화면에 남아 있다" 는 제보. 확인해 보니
**기능은 동작하고 있었다** — 타일 화면에 남는 게 곧 "무엇을 찍었는지 물어보는"
상태였다. 문제는 그게 전혀 전달되지 않는다는 것이었다. 제목 한 줄만 바뀌고,
**사진을 받았다는 표시가 아무것도 없어서** 사용자에겐 그냥 화면이 안 넘어간 것으로
보였다.

- **찍은 사진 썸네일**을 보여 준다(최대 3장, 넘으면 `+N`). "잘 찍혔고 우리가 갖고
  있다" 를 눈으로 확인시켜 주는 게 핵심이다.
- 노란 안내 박스 추가: `사진은 잘 받았어요. 다시 찍지 않아도 돼요. / 아래에서
  무엇을 찍었는지 골라 주세요.`
- 미리보기 URL 은 `useEffect` 정리에서 `revokeObjectURL` 한다.

**판별도 보강했다.** 예전엔 파일 시각이 있고 최근일 때만 물어봤는데, 시각을 못 읽는
기기에서는 아예 안 물어보게 된다. 이제 **시각이 없으면 물어보는 쪽으로 기운다** —
잘못 물어봐야 탭 한 번이지만, 안 물어보면 모드 분기가 통째로 빠지기 때문이다.
여러 장 중 하나라도 방금 찍은 것으로 보이면 묻는다(`some`).

서비스워커 캐시도 함께 올렸다(v1.0.2 → v1.0.3).

#### 검증 (실측)
운영 빌드 미리보기에서 실제 PNG 를 `lastModified = 지금` 으로 넣어 확인:
제목·안내 박스·"다시 찍지 않아도"·"무엇을 찍었는지" 문구 O, 썸네일 1장 O,
타일 3개 깜빡임 O. 화면도 눈으로 확인했다.

### 앨범/파일 경로: 사진을 고르기 **전에** 모드를 묻도록 순서를 뒤집음

파일 시각(`lastModified`)으로 "방금 찍은 사진" 을 짐작해 되묻는 방식은 실제 기기에서
동작하지 않았다. 시각이 안 실리거나, 촬영 중 화면 상태가 날아가 물어볼 기회 자체가
없어지는 경우가 있다.

**순서를 뒤집었다.** `사진 앨범/파일에서 선택` 을 누르면 바로 선택창을 열지 않고
먼저 종류를 묻는다.

```
어떤 사진을 고르실 건가요?
먼저 종류를 골라 주세요. 그다음 사진을 고르는 창이 열려요.
(거기서 바로 찍어도 괜찮아요)

  [영수증]   [음식 1개]   [음식 여러 개]     ← 깜빡임

     모르겠어요 · 알아서 인식해 주세요
```

고른 모드를 `pickerModeRef` 에 담아 두고 선택창을 연다. 그러면 OS 메뉴에서 무엇을
누르든 — 보관함이든 **사진 찍기든** — 모드가 이미 정해져 있다. 추측이 필요 없다.

`looksJustTaken()` 과 촬영 후 되묻던 화면(썸네일 포함)은 제거했다.

### 기본 재료 투입 표시를 서버로 옮김 (`users.fridge_seeded`)

"재료를 다 지우고 앱을 지운 뒤 캐시까지 비우고 다시 들어가니 기본 재료가 다시
깔려 있다" 는 제보.

표시(`myfridge_seeded_{userId}`)를 **브라우저 localStorage 에만** 두고 있었다. 앱을
지우면 표시도 함께 사라지니, 서버에는 재료가 0개이고 표시도 없어 "처음 온 계정" 으로
보였다. 계정에 딸린 사실이므로 계정과 함께 남아야 한다.

- `users.fridge_seeded TINYINT(1) NOT NULL DEFAULT 0` 추가 (기존 마이그레이션 패턴대로
  `ensure_users_table()` 에서 `ALTER`, 이미 있으면 무시).
- `GET /api/users/<id>/ingredients` 응답에 `fridge_seeded` 포함.
- `POST` 로 저장하면 `fridge_seeded = 1`. **빈 목록 저장도 "내가 정한 상태"** 이므로
  포함한다 — 전부 지운 뒤 앱을 지워도 되살아나지 않는다.
- 프론트는 로그인 사용자면 **서버 표시를 최종 기준**으로 삼는다(localStorage 표시는
  비회원용으로 유지).
- 이미 재료를 갖고 있던 계정 6개는 `fridge_seeded = 1` 로 **백필**했다. 명백히
  초기화가 끝난 계정이다.

서비스워커 캐시도 올렸다 (v1.0.3 → v1.0.4).

### 앨범/파일 종류 선택을 별도 팝업으로

앞선 방식은 시트 안에서 제목과 안내 문구가 바뀌는 형태라, 사용자에게는 "화면이
그대로인데 뭐가 바뀐 건지" 알기 어려웠다. 공용 `Dialog` 로 **따로 띄운다.**

```
어떤 사진을 고르실 건가요?        ×
   [영수증]
   [음식 1개]
   [음식 여러 개]
        취소
```

- 셋 중 하나를 고르지 않으면 선택창이 열리지 않는다. **모드 없이 처리되는 길을
  아예 없앴다.**
- `모르겠어요 · 알아서 인식해 주세요` 선택지를 뺐다(자동 판별로 새는 길이었다).
- 설명 문구도 뺐다. 제목과 버튼만으로 충분하다.
- 시트 위에 뜨므로 `nested` 로 한 단계 위 층에 올린다.

시트 본문은 원래대로(`무엇을 찍을까요?` + 타일 3개) 돌아갔고, 깜빡임 CSS 는 제거했다.

#### 검증 (실측)
운영 빌드 미리보기:
- `사진 앨범/파일에서 선택` → 팝업 표시 O / 선택지 3개 O / `모르겠어요` 없음 O /
  설명 문구 없음 O / `취소` O
- 팝업의 `영수증` 클릭 → **파일 선택창이 열리고**(카메라 아님) 팝업이 닫힘 O

서비스워커 캐시 v1.0.4 → v1.0.5.

### 팝업 본문과 하단 버튼이 붙어 보이던 문제 (`ui/Dialog`)

`어떤 사진을 고르실 건가요?` 팝업에서 마지막 선택지와 `취소` 버튼 사이 여백이 없었다.

```js
marginBottom: actions.length ? 20 : 0,
```

`actions` 를 넘기지 않으면 여백이 0 이었는데, **하단 버튼은 `actions` 가 없어도
자동 나가기 버튼으로 항상 렌더된다.** 조건 자체가 틀렸다. 항상 여백을 준다.

공용 컴포넌트라 `actions` 없이 쓰던 다른 팝업에도 같이 고쳐진다.
(실측: 마지막 선택지와 `취소` 사이 간격 0px → 19px)

서비스워커 캐시 v1.0.5 → v1.0.6.

### 종류 선택 팝업에 안내 한 줄 + 버튼 이름 정정

- 팝업에 `고른 종류가 사진 전체에 적용돼요. 같은 종류끼리 골라 주세요.` 추가.
  여러 장을 **한 번의 호출**로 처리하느라 프롬프트가 하나뿐이라, 영수증과 음식 사진을
  섞어 고르면 한쪽 인식이 나빠진다. 알려 주지 않으면 알 수 없는 제약이라 적어 둔다.
- 하단 버튼 이름을 `사진 앨범/파일에서 선택` → **`여러 장 한 번에 고르기`** 로 바꿨다.
  OS 선택 메뉴에는 `사진 찍기` 가 늘 함께 나오는데(웹에서 뺄 수 없다), 버튼이
  "앨범/파일" 이라고 단언하니 촬영 항목이 나오는 게 앞뒤가 안 맞아 보였다.
  이 버튼의 실제 차이는 **한 번에 여러 장을 담을 수 있다는 것**이므로 그렇게 부른다.

서비스워커 캐시 v1.0.6 → v1.0.7.

### 버튼 이름을 `사진 추가` 로

`여러 장 한 번에 고르기` 도 정확하지 않았다 — OS 메뉴의 `사진 찍기` 로 들어오면
한 장이기 때문이다.

이 버튼을 누르면 OS 가 **보관함 / 촬영 / 파일** 을 함께 띄우고 그 메뉴는 웹에서
손댈 수 없다. 그러니 이름은 **세 경로를 다 포괄**해야 한다.

| 이름 | 어긋나는 지점 |
|---|---|
| 사진 앨범/파일에서 선택 | 촬영이 나오는 게 앞뒤가 안 맞음 |
| 여러 장 한 번에 고르기 | 촬영으로 들어오면 한 장 |
| **사진 추가 (최대 5장)** | 세 경로 모두에 맞음. `최대 5장` 은 상한을 알릴 뿐 강요하지 않음 |

왜 이 이름인지 코드에도 주석으로 남겼다 — 나중에 다시 "앨범/파일" 로 되돌리기 쉬운
자리라서.

서비스워커 캐시 v1.0.7 → v1.0.8.

### 사진 인식 검토 시트: 구매일자 수정 · 버튼 문구 · 미매칭 재료 조회

**구매일자를 눌러도 아무 일도 안 일어났다.** 실제로는 달력 모달이 열리고 있었는데
**시트 뒤에 깔려 있었다.** `IngredientDateModal` 의 z-index 가 시트와 같은
`--z-modal`(600) 인데, 바텀시트는 Portal 로 body 직속에 붙어 더 나중에 그려진다.
같은 층위면 나중 것이 위로 올라간다. 그래서 시트를 닫는 순간에야 달력이 나타났고,
"팝업이 닫힐 때 날짜를 고치게 되어 있는" 것처럼 보였다.

- `IngredientDateModal` 에 `nested` 옵션 추가 — 켜면 `--z-modal-nested`(650) 로
  올라가고 Portal 로 body 직속에 붙는다. 시트 안에서 열 때는 반드시 이걸 쓴다.

**구매일자를 어디서 정하는지 모드에 따라 나눴다.**

| 모드 | 어디서 |
|---|---|
| 영수증 / 앨범(자동판별) | 목록 위에서 한 번, 전체 일괄 |
| 음식 1개 / 음식 여러 개 | 행마다 (`구매 09.01` 칩) |

영수증 한 장은 한 번에 산 목록이라 일괄이 맞지만, 음식 사진은 재료마다 산 날이
다를 수 있어 한 값으로 묶으면 유통기한 추정이 통째로 어긋난다.
`ConfirmedIngredient` 에 `purchase` 를 넣어 항목별로 실어 보낸다.

**`고치기` → `재료명 고치기`** (미매칭 항목은 `재료명 고르기`). 같은 줄에 보관함
셀렉트가 함께 있어서 무엇을 고치는 버튼인지 알 수 없었다. 문구가 길어진 만큼
한 행을 두 줄로 나눴다 — 윗줄은 무엇인지, 아랫줄은 어떻게 할지. 예전 한 줄
배치에서는 폭이 좁은 폰에서 재료명이 몇 글자 만에 잘렸다.

**사전에 없던 이름은 이미 모으고 있었다** (`ingredient_dictionary_misses`,
`_record_dictionary_misses`). 쌓기만 하고 꺼내 볼 방법이 없어서 조회 스크립트를
추가했다.

- `scripts/show_dictionary_misses.py` — 자주 걸린 순서로 보여 주고, **지금 사전으로
  다시 조회한 결과**를 함께 낸다(그 사이 사전을 보강해 이미 해결된 건 구분해야
  헛수고를 안 한다). `--all` / `--limit` / `--csv` 지원.
- 첫 조회 결과 8건: `진로`·`참이슬`·`카스`·`테라`(주류 브랜드),
  `된장찌개`·`비빔냉면`·`간장계란밥`(요리 이름이라 사전에서 일부러 뺀 것),
  `냉동 플루미`. **8건 모두 사전에 넣지 않는다** — 주류는 넣지 않기로 결정했고,
  요리 이름은 애초에 재료가 아니다. 이 판단을 기능 문서에 표로 남겼다(다음에 또
  같은 제안을 하지 않도록).

`expiry` 표기를 앱 표준(`yyyy.mm.dd`)으로 맞췄다. 서버는 ISO 로 주는데 그대로
저장하고 있었다.

서비스워커 캐시 v1.0.8 → v1.0.9.

### 사진 인식 시트 한 줄 정리 · 행별 유통기한 · 가이드 하이라이트 3건

**행을 다시 한 줄로.** 어제 "재료명 고치기" 로 문구를 늘리면서 한 행을 두 줄로
나눴는데, 항목이 10개 넘는 영수증에서 목록이 두 배로 길어져 훑어보기가 나빠졌다.
이름 칸만 줄어들게(`flex:1; min-width:0` + 말줄임) 두고 조작 버튼은 고정폭으로
되돌렸다. 여백(gap 8→6, 칩 padding 10→8)을 줄여 이름 칸을 확보했다 —
실측 430px 폰 149px / 375px 폰 94px / 351px 폰 70px.

**행마다 유통기한을 넣을 수 있다.** 영수증 한 장으로 산 것이라도 제품마다 기한은
제각각이라는 지적. 새 칸을 만들지 않고 **기존 유통기한 배지 자리를 그대로 버튼**
으로 썼다(값 있으면 `~09.12` 노랑, 없으면 `기한 +` 점선) — 한 줄을 지키기 위해서다.

구매일자와 유통기한의 관계는 원래 코드에 이미 정해져 있었고(`expiry` 가 있으면
그걸 쓰고, 없을 때만 구매일로 `estimatedExpiry` 를 짐작), 그 규칙을 화면 문구로
드러냈다: "기한을 넣지 않은 재료는 이 날짜로 기한을 짐작해요. 제품에 적힌 날짜는
`기한 +` 를 눌러 넣어 주세요 — 그 값이 우선해요."

**가이드 노란 하이라이트가 엉뚱한 데 찍히던 문제 3건.**

| 증상 | 원인 |
|---|---|
| 입력창 단계(2/11)에 하이라이트가 아예 없음 | 선택자가 `input[placeholder="추가할 재료명을 입력하세요"]` 였는데 문구를 "재료명을 입력하세요" 로 다듬으면서 안 맞게 됨 → `data-guide-target="ingredient-input"` 로 교체 |
| 재료 배지 단계(3/11)에 글자 "감자" 에만 테두리 | 속성이 안쪽 이름 `<span>` 에 붙어 있었음 → 배지 전체를 감싸는 바깥 `div` 로 이동 |
| 즐겨찾기·완료·공유·기록(8~11/11) 하이라이트 없음 | ① `RecipeCard` 가 썸네일 검증 전까지 `null` 을 돌려줘 **버튼이 아직 문서에 없음** ② 카드가 화면 아래에 있는데 스크롤을 안 해서 높이 0 으로 잘리고 최소 크기 상자가 화면 맨 위에 그려짐 |

`GuideOverlay` 를 이렇게 고쳤다.

- 단계가 떠 있는 동안 **계속 다시 찾고 다시 잰다**(250ms). 늦게 생기는 요소도,
  위쪽이 뒤늦게 렌더돼 자리가 밀리는 것도 따라간다.
- 재기 전에 **안전 구간(고정 헤더 아래 ~ 하단 탭 위)의 가운데로 스크롤**한다.
  `scrollIntoView({block:'center'})` 는 뷰포트 기준이라 하단 탭에 가려지는
  자리로 데려다 놓는 경우가 있어, 안쪽 스크롤 상자만 `block:'nearest'` 로 맞춘 뒤
  페이지는 직접 계산해 움직인다.
- 하단 탭 예약(80px)까지 자르면 남는 높이가 없을 때는 화면 끝까지 허용한다.
  탭에 조금 겹치는 게 상자를 아예 안 그리는 것보다 낫다.
- `settings-icon` 단계의 옛 탐색 코드(제목 텍스트로 냉장보관 섹션을 찾아 올라가는
  50줄)와 스크롤마다 찍히던 `console.log` 를 걷어냈다.
- 브라우저로 재현·확인: 늦게 생기고 화면 밖에 있는 타겟에 대해 하이라이트 사각형이
  타겟과 정확히 일치(357/200/40/26)하는 것까지 확인했다.

`.claude/launch.json` 추가 (프론트 개발 서버 미리보기용).

서비스워커 캐시 v1.0.9 → v1.1.0.

### 영수증 거래일시를 유통기한으로 읽어 담던 문제

영수증으로 담은 재료가 곧바로 `D-day` / `지남` 으로 빨갛게 뜬다는 제보. 원인은
**모델이 영수증에 큼직하게 찍힌 거래일시를 각 재료의 `expiry` 로 적어 보낸 것**이다.
그 날짜는 대개 오늘이라 `_clean_expiry()` 의 "과거는 버린다"(`parsed < today`) 검사를
그냥 통과했고, 프론트는 그걸 **사용자가 아는 실제 유통기한**으로 대접해
구매일 기반 짐작(`estimatedExpiry`)을 아예 건너뛰었다.

의도한 우선순위는 원래대로다 — 사용자가 넣은 유통기한 > 구매일 + 재료 특성 짐작.
문제는 가짜 유통기한이 1순위 자리를 차지한 것이었다.

- `backend/ingredient_vision.py`: `_clean_expiry(value, purchase_date)` 로 바꿔
  **구매일자와 같거나 그보다 이른 값은 버린다.** 산 날과 같은 날 상하는 식품은
  사실상 없으니 오독으로 본다. 이 검사를 하려면 구매일자가 먼저 필요해서
  `recognize()` 가 `purchase_date` 를 먼저 정리하도록 순서를 바꿨다.
- `IngredientRecognitionSheet`: 같은 검사(`usableExpiry`)를 행을 만들 때 한 번 더.
  서버가 옛 버전이어도 화면에 가짜 배지가 안 뜬다.
- 두 검사 모두 **사진에서 읽은 값에만** 건다. 사용자가 `기한 +` 로 직접 고른
  날짜는 (마감 임박 식품처럼 오늘이 기한인 경우가 있어) 그대로 존중한다.
- `MyFridge`: 이미 담긴 재료 중 **유통기한 == 구매일** 인 것은 그때 잘못 저장된
  값이므로 걷어내고 짐작값으로 되돌린다. 인식 쪽만 고치면 그때 담은 재료는
  계속 "지남" 으로 남기 때문.

브라우저로 확인:

| 저장돼 있던 값 | 고친 뒤 |
|---|---|
| 삼겹살 `expiry=2026.09.01`(=구매일) | `약 D-3` (돼지고기 냉장 3일) |
| 우유 `expiry=2026.09.01`(=구매일) | `약 D-7` |
| 달걀 (기한 없음) | `약 D-30` |
| 삼겹살 `expiry=2026.09.20` (사용자 입력) | `D-19` — 그대로 유지, "약" 없음 |

서비스워커 캐시 v1.1.0 → v1.1.1.

### 가이드 문구·하이라이트 범위 조정

- 1단계에서 `사진으로 한번에 파악하기 기능은 추후 추가 계획입니다.` 삭제 —
  사진 인식이 실제로 들어와서 더 이상 "추후 계획" 이 아니다.
- 2단계 문구를 `재료명을 입력해서 내냉장고에 추가할 수 있어요.` →
  `재료명을 입력하거나, 영수증/음식 사진을 찍어서 / 재료를 내냉장고에 추가할 수 있어요.`
- 2단계 하이라이트를 입력창 하나가 아니라 **입력 줄 전체**(입력창 + 입력 + 카메라)로
  넓혔다. 문구가 둘 다 말하는데 카메라 버튼이 하이라이트 밖에 남아 있었다.
  선택자 `ingredient-input` → `ingredient-add-row` (줄 컨테이너로 이동).
- 하이라이트 상자에 여유 6px. 요소에 딱 붙여 그리니 카메라 버튼의 "AI" 배지처럼
  음수 top/right 로 얹힌 장식이 상자 밖에 4px 걸쳐 오류처럼 보였다.

브라우저로 확인: 2단계 상자 안에 입력창과 카메라 버튼, "AI" 배지까지 모두 들어온다.

서비스워커 캐시 v1.1.1 → v1.1.2.

### 음식 1개 / 음식 여러 개 모드 개방 + 프롬프트를 모드별로 분리

세 모드를 나눠 둔 이유가 프롬프트가 다르기 때문인데, 정작 `food-single` 과
`food-multi` 가 **같은 `_FOOD_PROMPT` 하나**를 쓰고 있었다. 모드를 물어보고도
같은 지시를 내리면 물어본 의미가 없어서 둘로 갈랐다.

| mode | 프롬프트 | 시키는 일 |
|---|---|---|
| `receipt` | `_RECEIPT_PROMPT` | 산 목록을 읽어라. 봉투·할인·합계 제외, 브랜드·용량 제거, 결제 날짜를 `purchase_date` 로 |
| `food-single` | `_FOOD_SINGLE_PROMPT` | 사진 한 장당 **하나만**. 가장 크게 가운데 찍힌 것, 배경은 무시 |
| `food-multi` | `_FOOD_MULTI_PROMPT` | 보이는 것을 **빠짐없이**. 그릇·도마·행주 같은 비식재료 제외 |
| `file` | `_AUTO_PROMPT` | 영수증인지 음식 사진인지 스스로 판단 |

두 음식 프롬프트의 차이는 "몇 개를 뽑을 것인가" 하나뿐이라, 나머지 규칙은
`_FOOD_COMMON_RULES` 한 곳에 모아 둘이 어긋나지 않게 했다. 공통 규칙에
브랜드 제거 예시(`서울우유 1L` → `우유`)와 완성 요리 예시(김치찌개 → 김치·
돼지고기·두부)를 보탰다.

`food-single` 이 하나만 뽑는 이유: 이 모드를 고른 사용자는 특정 재료 하나를
담으려는 것이라, 배경에 우연히 걸린 물건까지 담기면 지우는 일만 늘어난다.

- `MyFridge.handleCameraCaptured` 의 "준비 중" 분기 제거 — 네 모드 모두 서버로 간다.
- 카메라 시트 배지 `영수증은 지금 바로 인식돼요 · 음식 사진은 준비 중`
  → `사진에서 재료를 찾아 담아 드려요`.
- 타일마다 한 줄 힌트 추가 (`산 것 전부` / `하나만 크게` / `펼쳐 놓고`) —
  이제 세 모드가 실제로 다르게 동작하므로 언제 무엇을 고르는지 알려 준다.

**실제 사진으로 확인** (레시피 썸네일을 내려받아 `recognize()` 직접 호출):

| 사진 | mode | 결과 |
|---|---|---|
| 불닭볶음탕면 | `food-single` | `라면` 하나만 — 의도대로 |
| 단호박빵 | `food-multi` | `달걀`, `부추` (부추는 고명 오독) |
| 고구마순 | `food-single` / `food-multi` | 둘 다 `고구마줄기` — 사진에 실제로 하나뿐이라 일치 |

음식 사진은 영수증보다 어렵다. 사진에 안 보이는 재료(버터·소금)는 못 읽고,
고명을 다른 재료로 읽기도 한다. 검토 시트에서 걸러 담는 걸 전제로 한 기능이다.

서비스워커 캐시 v1.1.2 → v1.1.3.

### 카메라 시트가 화면보다 넓어져 좌우로 흔들리던 문제

타일에 힌트 한 줄(`펼쳐 놓고`)을 더한 것이 원인이었다. 범인은 `aspect-ratio: 1/1`.

- 타일 3개는 `gridTemplateColumns: repeat(3, 1fr)` 로 놓여 있었는데, `1fr` 의
  **자동 최소 크기는 min-content** 라 칸이 내용보다 작아질 수 없다.
- 타일에 `aspect-ratio: 1/1` 이 걸려 있어서 **세로로 길어지면 그 높이가 가로
  최소 폭으로 되돌아온다.** 힌트를 더하면서 내용 높이가 102 → 125px 이 됐고,
  칸도 125px 을 요구했다.
- 3칸 합이 시트보다 59px 넓어졌다(실측 `scrollWidth` 414 vs `clientWidth` 375).
  바텀시트 본문은 `overflow-y: auto` 인데, CSS 규칙상 이러면 **가로도 자동으로
  스크롤된다.** 그래서 손가락을 움직일 때마다 시트가 좌우로 흔들렸다.

고친 방법: `repeat(3, minmax(0, 1fr))` 로 칸이 줄어들 수 있게 하고, 정사각 고정
대신 내용에 맞춰 높이가 정해지게 두었다(그리드가 세 칸 높이를 알아서 맞춘다).

확인: 320 / 351 / 360 / 375 / 414 / 430px 전부 `scrollWidth == clientWidth`.
375px 에서 타일 106×134px 3개가 나란히 들어간다.

**노랑 배지 `✨ 사진에서 재료를 찾아 담아 드려요` 제거.** 세 모드가 다 동작하게
되면서 알릴 내용이 없어졌고, 시트를 여는 버튼 자체가 이미 "AI" 배지를 달고 있어
중복이었다.

서비스워커 캐시 v1.1.3 → v1.1.4.

### 종류 선택 팝업 안내 문구 줄바꿈

`고른 종류가 사진 전체에 적용돼요. 같은 종류끼리 골라 주세요.` 를 두 줄로 나눴다.
앞 문장은 "이렇게 동작한다", 뒤 문장은 "이렇게 해 달라" 로 하는 말이 달라서,
한 줄로 이어 붙이면 부탁이 설명에 묻힌다.

서비스워커 캐시 v1.1.4 → v1.1.5.

### AI 사용량 한도를 사용자별로 (1단계)

**고치기 전:** `chat_service` 안에 프로세스 전역 카운터 하나가 있었고, 챗봇과
사진 인식이 그것을 **모든 사용자와 함께** 나눠 썼다.

| 문제 | 결과 |
|---|---|
| 사용자별이 아님 | 한 명이 250번 쓰면 **나머지 전원 차단** |
| 메모리에만 존재 | 배포·재시작 때마다 0으로 리셋 |
| 인스턴스마다 따로 셈 | 2개면 실제 한도는 500 |
| 기록 없음 | 누가 얼마나 썼는지 알 방법이 전혀 없음 |

사용자가 9명이라 안 터지고 있었을 뿐이다. 광고로 유입을 늘리기 전에 고쳐야 했다.

**설계 문서:** `USAGE_QUOTA_PLAN.md` (정책 근거·수치 산출·어드민 설계 전체)

**정책**

- **주별 한도 + 일별 상한.** 냉장고 재료 등록은 장 본 날 하루에 몰린다. 일별만
  걸면 첫 사용이 차단으로 끝나고, 주별만 걸면 첫날 한 주치를 태운다.
- 비회원 주 15 / 일 10 · 회원 주 100 / 일 40 · plus 주 400 / 일 100
- 리셋은 **월요일 00:00 KST 고정** (롤링 7일은 언제 리셋되는지 예측이 안 된다)
- 챗봇 1 크레딧 / 사진 인식 2 크레딧 — **장수와 무관**

사진을 장수만큼 매기려다 바로잡았다. `countTokens` 실측 결과 사진 5장(6,015토큰)도
1장(1,759토큰)도 **API 호출은 똑같이 1회**이고, 무료 티어 한도는 토큰이 아니라
**요청 수**로 센다. 장수만큼 매기면 권장해야 할 행동(여러 장을 한 번에 올려
호출을 아끼는 것)에 벌금을 매기는 꼴이 된다.

**구현**

- `backend/usage_quota.py` 신설 — 챗봇·사진 인식이 함께 쓰는 단 하나의 구현
- `llm_usage` 표: 호출 1건 = 1행. **카운터가 아니라 원장**이라 집계 기준을
  나중에 바꿀 수 있고 어드민 통계가 여기서 다 나온다
- `user_quota` 표: 플랜 + 수동 조정(+메모). 어드민이 결제 없이 한도를 올려 줄 수 있다
- `users.is_admin` 컬럼 추가
- `GET /api/usage` — 화면 세 곳이 같은 값을 쓰도록 한 곳에서만 계산
- 비회원은 `X-Device-Id` 로 센다. 캐시를 지우면 초기화되지만 **완벽할 필요가 없다** —
  비회원 한도는 남용 방지가 아니라 가입 유인이 목적이다
- 한도 계산이 실패해도 기능은 막지 않는다(첫 배포 등). 로그만 남기고 통과

**실DB 검증**: 비회원이 사진 5회(10크레딧)로 일 상한에 정확히 닿고 6번째가
`daily` 로 차단, 회원은 별도 집계, 리셋 시각 2026-09-07T00:00 확인.

옛 전역 카운터(`_rate_count` / `_daily_limit` / `_consume_quota`)는 삭제하고,
같은 자리에 "다시 만들지 말 것" 주석을 남겼다.

### 사용량 원장에 실제 토큰까지 기록

크레딧만 남기고 있었다. 크레딧은 **우리가 정한 환산값**이라, 그것만으로는
"사진 2크레딧이 적정한가", "유료 티어로 넘어가면 원가가 얼마인가" 를 감으로밖에
못 정한다.

- `llm_usage` 에 `model` / `images` / `prompt_tokens` / `output_tokens` /
  `total_tokens` 추가 (이미 만들어진 표에는 `ALTER` 로 채운다)
- Gemini 응답의 `usageMetadata` 를 그대로 받아 적는다. 호출 함수의 반환값을
  바꾸면 호출부를 전부 고쳐야 해서, 같은 스레드에서만 보이는 자리에 흘려 두고
  (`note_gemini_usage`) 호출이 끝난 쪽에서 원장 행에 붙인다(`attach_tokens`)
- 챗봇·사진 인식 양쪽 모두 적용. 기록이 실패해도 사용자 요청은 성공시킨다

실호출 검증 (사진 2장, `food-multi`):

| kind | credits | images | prompt | output | total |
|---|---|---|---|---|---|
| vision | 2 | 2 | 2,841 | 18 | 2,859 |

환산이 맞는지 확인하는 쿼리를 `USAGE_QUOTA_PLAN.md` 에 함께 적어 뒀다 —
`tokens_per_credit` 이 종류별로 벌어지면 그때 `CREDITS_VISION` 을 조정한다.
**감으로 바꾸지 말 것.**

### AI 사용량 화면 표시 (2단계)

`GET /api/usage` 값을 화면 세 곳에 붙였다. 자리마다 성격을 다르게 뒀다.

| 위치 | 무엇을 | 왜 |
|---|---|---|
| 카메라·챗봇 아이콘 | **20% 이하일 때만** 작은 배지 | 많이 남았을 때 숫자를 들이밀면 "아껴 써야 하나" 싶어 오히려 안 쓴다 |
| 카메라 시트 · 챗 패널 헤더 | `이번 주 0 / 15` 한 줄 | 기능을 열었을 때가 알려주기 가장 좋은 때 |
| 마이페이지 | 게이지 + 리셋 시각 | "내 계정 상태"를 보는 자리 |

- `frontend/src/utils/usage.ts` — 조회·구독·기기 식별자를 한 곳에. 표시 자리가
  셋이라 각자 불러오면 같은 화면에서 다른 숫자가 보인다. **한 번 불러온 값을
  공유하고 바뀌면 함께 갱신**한다. 프론트는 서버 값을 보여주기만 하고
  **직접 계산하지 않는다**
- `frontend/src/components/UsageMeter.tsx` — `UsageBadge` / `UsageLine` / `UsageGauge`
- 챗봇·사진 인식 요청에 `X-Device-Id` 를 실어 보내고, 응답에 실려 온 최신
  사용량을 **재조회 없이** 즉시 반영한다(`applyUsage`)
- 배지 숫자는 `min(주간 잔여, 일간 잔여)` — 사용자가 실제로 막히는 쪽

**만들면서 걸린 것 둘**

- **CORS**: `X-Device-Id` 를 `allow_headers` 에 안 넣어 브라우저가 preflight 에서
  막았다. 서버에서 직접 호출하면 CORS 가 없어 멀쩡히 되므로 찾기 어려운 종류다
- **배지가 안 보였다**: 아이콘 우상단은 이미 `.ai-fab-badge`("AI")가 쓰고 있어
  겹쳤다. DOM 에는 있는데 화면에는 없었다 → 좌하단으로 옮김

로컬 백엔드를 띄우고 브라우저로 확인: 시트 헤더 `이번 주 0 / 15 · 오늘은 10번 더`,
주간 13/15 시드 후 아이콘 배지 `2`, 마이페이지 게이지 86.7% 채움 +
`9월 7일 월요일에 다시 채워져요`. 확인 후 시드 행은 삭제했다.

"더 필요해요" 버튼은 요청 폼(4단계)이 생기면 붙인다 — 지금 붙이면 눌러도 아무
일도 안 일어나는 버튼이 된다.

서비스워커 캐시 v1.1.5 → v1.2.0.

### 어드민 `/admin` (3단계)

`920803hj@naver.com` 을 관리자로 지정했다. 그 이메일로 계정이 **둘**이라
(id=4 naver / id=7 local) 같은 사람이므로 둘 다 `is_admin=1`.

**구조 — 지금은 합쳐 두되 떼기 쉽게**

분리를 부르는 건 사용자 수가 아니라 **관리자 수와 위험도**다. 관리자가 1명이면
사용자가 10만이어도 `/admin` 하나로 버틴다. 그래서 지금은 같은 앱에 두되,
나중에 떼기 쉽도록 세 가지를 지켰다.

1. 어드민 API 는 전부 `/api/admin/*` — `backend/admin_api.py` 한 파일
2. 어드민 화면은 `lazy()` 로만 — 실측 `Admin-*.js` 13KB 가 별도 청크로 떨어져
   일반 사용자 번들에 안 실린다
3. 관리자 행동을 남긴다 — `user_quota.updated_by` + `note`

**권한**: 모든 엔드포인트에서 서버가 검사한다. 토큰만 믿지 않고 **DB 의
`is_admin` 을 매번 확인** — 권한을 뺏어도 발급된 토큰이 30일 살아 있기 때문.

**화면**

- 사용자 목록 — id / 이메일 / 닉네임 / 가입 / 경로 / **재료 수** / 이번 주
  크레딧 / **토큰** / 플랜 / 탈퇴. 검색 · 탈퇴 포함 토글
  (재료 수와 사용량은 다른 표라 조인해야 나온다. 이게 없으면 "이 사람이 실제로
  쓰고 있는지"를 알 수 없어 어드민의 쓸모가 절반으로 준다)
- 사용자 상세 — 한도 조정(플랜/주/일) + **메모 필수**, 최근 사용 이력(크레딧과
  실제 토큰을 나란히)
- 대시보드 — 가입·탈퇴·식구 그룹, 이번 주 크레딧·호출, 쿠팡 클릭(30일),
  일별 크레딧 막대, **크레딧 환산 점검**(종류별 크레딧당 토큰), 사전 미매칭 상위

탈퇴 계정의 `deleted+{id}+` 접두어는 화면에서만 벗겨 원래 이메일을 보여준다.
덮어쓰는 건 의도된 동작이라(재가입 허용) 원본을 따로 저장하지는 않는다.

**브라우저 검증**: 토큰 없음 403 / 일반 사용자 403 / 관리자 200. 사용자 8명이
재료 수까지 조인되어 나오고, 메모 없이 저장하면 막히고, 메모를 넣으면
`plan=plus 주400 일100 updated_by=4` 로 저장된 뒤 목록이 `0 / 400 · plus` 로
갱신됐다. 대시보드도 실수치(사용자 9 · 탈퇴 1 · 식구 6 · 쿠팡 클릭 1)를 냈다.
확인 후 테스트로 넣은 한도 행은 삭제했다.

서비스워커 캐시 v1.2.0 → v1.2.1.

### 한도 추가 요청 (4단계)

결제를 붙이지 않기로 했으므로(USAGE_QUOTA_PLAN.md 6절), 더 필요한 사람은
관리자에게 말하고 관리자가 손으로 올려 준다. `usage_requests` 표가 그 창구다.

부수 효과가 더 중요하다 — **실제 수요가 측정된다.** 요청이 꾸준히 들어오기
시작하면 그때 결제를 붙일 근거가 생긴다. 그 전까지 이 목록 자체가 수요 조사다.

- 마이페이지 게이지의 `더 필요해요` → 사유(선택) → 접수
- **로그인한 사용자만.** 비회원은 "가입하면 더 쓸 수 있다"가 답이라 요청받을
  이유가 없다
- **대기 중인 요청은 사람당 하나.** 여러 번 눌러도 새로 만들지 않는다(서버가
  막는다). 접수 뒤에는 버튼 대신 "요청이 접수됐어요"로 바뀐다
- 어드민에 `요청` 탭 — 처리함 / 거절 / 되돌리기
- **한도를 올리는 것과 요청을 닫는 것은 분리했다.** 섞으면 "올려주지 않고
  닫는" 경우를 표현할 수 없다. 한도 조정은 사용자 탭에서 한다

브라우저 검증: 마이페이지에서 사유를 적어 요청 → "요청을 남겼어요" → 버튼이
"요청이 접수됐어요"로 바뀜 → 어드민 `요청` 탭에 이메일·닉네임·플랜·사유와 함께
`open` 으로 도착 → `처리함` 누르니 목록에서 빠짐. 확인 후 테스트 행 삭제.

서비스워커 캐시 v1.2.1 → v1.2.2.

### 같은 이메일인데 계정이 둘로 갈리던 문제

네이버로 로그인한 계정과 그 이메일로 직접 가입한 계정이 **서로 다른 냉장고**를
갖고 있었다. 원인은 `users` 의 유니크 키가 `(email, provider)` 복합이라
같은 이메일이 제공자마다 하나씩 존재할 수 있었기 때문이다.

원래 이런 게 아니라 **구현 선택**이었다. 보통은 셋 중 하나를 쓴다 —
① 이메일 = 계정(로그인 수단만 여러 개) ② 제공자 = 별개 계정 ③ 중복 차단.
쿡매치는 ②였고, 사용자가 예상하지 못하는 동작이라 ①+③으로 바꿨다.

**새로 생기지 않게**

- `get_or_create_user()`: 같은 이메일의 다른 로그인 수단 계정이 있으면 **그리로
  들어간다**. 단 `email_verified` 일 때만 — 확인 안 된 이메일로 연결해 주면 남의
  이메일을 적어 둔 계정으로 그 사람 계정에 들어갈 수 있다. 판단은 콜백이 한다:
  구글 `verified_email` / 네이버 이메일을 실제로 받았을 때만(없으면 아이디로
  만들어 내는데 그건 진짜 이메일이 아니다) / 카카오 `is_email_verified`
- 로컬 회원가입: 같은 이메일이 소셜로 가입돼 있으면 새로 만들지 않고
  **409 + "이 이메일은 네이버 로그인으로 가입돼 있어요"** 안내

**이미 갈라진 것 합치기** — `scripts/merge_duplicate_accounts.py`

- **비밀번호를 가진 행을 남긴다.** 해시는 그 행에만 있어서 지우면 아이디/비밀번호
  로그인이 영영 안 된다. 소셜은 이메일로 찾아 붙일 수 있어 어느 행이든 상관없다
- 재료(이름 기준 중복 제외) · 즐겨찾기 · 완료 · 기록 · 사용량 원장을 옮기고
  남은 쪽은 탈퇴 처리 + 이메일 비우기
- **식구 그룹은 옮기지 않는다.** 다른 사람들과 얽힌 문제라 자동으로 정할 일이 아니다
- 미리보기가 기본, `--commit` 이어야 반영. 실행 전 백업 JSON 을 남긴다
  (비밀번호 해시가 들어 있어 `scratch_backups/` 를 .gitignore 에 추가)

실행 결과 — 중복 2쌍:

| 이메일 | 남김 | 합침 | 옮긴 것 |
|---|---|---|---|
| 920803hj@naver.com | id 7 (local) | id 4 (naver) | 재료 15 · 즐겨찾기 4 · 완료 10 · 기록 3 |
| lds1582@gmail.com | id 9 (local) | id 58 (google) | 없음 |

id 7 은 재료 45 · 즐겨찾기 4 · 완료 23 으로 합쳐졌고 중복 이메일은 0이 됐다.
검증: 네이버(확인됨) 로그인 → id 7 로 연결 / 카카오(미확인) → 연결하지 않고
새 계정. 관리자도 id 7 하나로 정리됐다.

### 계정 병합이 식구 그룹을 깨뜨린 것 수습

앞선 병합에서 식구 그룹을 "남과 얽힌 문제라 자동으로 정하지 않겠다"며 손대지
않았는데, **그게 그룹 하나를 깨뜨렸다.**

`households.storage_user_id` 는 그 그룹의 공유 냉장고가 어느 계정에 있는지를
가리킨다. 그룹 28 은 그 값이 id 4 였는데 병합에서 id 4 를 닫아 버려서, 남아 있던
활성 멤버(id 70)의 공유 냉장고가 **죽은 계정**을 보게 됐다.

병합 스크립트에 그룹 처리를 넣었다.

1. 합쳐지는 계정이 `storage_user_id` / `created_by` 이던 그룹은 **남는 계정으로 넘긴다**
2. 소속은 **활성 멤버가 있는 쪽**으로. 양쪽 다 사람이 있으면 자동으로 정하지 않고
   알리기만 한다 — 남과 얽힌 문제라 함부로 결정할 일이 아니다
3. 아무도 안 남은 그룹은 지운다
4. `--fix-orphans` — 이미 합친 뒤에 깨진 것만 손보는 모드 (재실행해도 안전)

**`merged+` 접두어를 새로 뒀다.** 닫힌 계정의 이메일을 비켜 놓을 때 쓰던
`deleted+` 를 그대로 쓰면, **사용자가 직접 탈퇴한 계정과 구분되지 않는다.**
직접 탈퇴는 "새 계정으로 시작"이라 옛 그룹을 새 계정에 끌고 오면 안 되고, 병합은
반대로 끌고 와야 한다. 구분이 없어서 `--fix-orphans` 가 진짜 탈퇴 계정(id 8)의
그룹까지 새 계정에 옮기려 했다 — 접두어를 나눠 막았다.
어드민 목록은 두 접두어 모두 벗겨 원래 이메일을 보여준다.

정리 결과:

| 그룹 | 저장 계정 | 멤버 |
|---|---|---|
| 11 | 나혜진(id 1) | 나혜진, 도아아빠(id 9 — id 58 자리를 이어받음) |
| 28 | 쿡매치(id 7 — id 4 에서 넘겨받음) | 쿡매치, NHJ(id 70) |

id 7 이 혼자 있던 그룹 16 은 사람이 없어 삭제됐다.
**저장 계정이 죽은 그룹: 0.**

### 어드민 입구를 마이페이지에 추가

`/admin` 은 배포돼 있었는데 **화면 어디에도 링크가 없어서** 주소를 직접 쳐야
들어갈 수 있었다. 마이페이지 AI 사용량 아래에 관리자에게만 보이는 버튼을 뒀다.

`useIsAdmin()` 이 `/api/admin/me` 로 확인한다. ⚠️ 이건 **화면 편의일 뿐 보안이
아니다** — 실제 권한은 서버가 `/api/admin/*` 마다 DB 의 `is_admin` 을 확인하므로,
경로를 아는 것만으로는 아무것도 볼 수 없다.

서비스워커 캐시 v1.2.2 → v1.2.3.

### 챗 헤더의 사용량 문구가 줄바꿈되던 문제

챗 패널 헤더는 아바타(36)와 버튼 두 개(40×2) 사이에 끼어 있어 남는 폭이
150px 남짓이다. 거기에 `이번 주 0 / 15 · 오늘은 10번 더 · 로그인하면 더 넉넉해요`
를 다 넣으니 여러 줄로 접혀 제목을 밀어냈다.

`UsageLine` 에 `compact` 를 뒀다. 좁은 자리에서는 **한 줄로 끝나는 것만** 남기고
(`이번 주 0 / 15`), 자세한 안내는 넓은 자리(카메라 시트 · 마이페이지)에서 한다.
넘치면 줄을 늘리는 대신 말줄임(`nowrap` + `ellipsis`).

확인: 챗 헤더 `이번 주 0 / 15` 높이 18px 한 줄 / 카메라 시트는 세 토막 그대로
나오면서 역시 18px 한 줄, 가로 넘침 없음.

서비스워커 캐시 v1.2.3 → v1.2.4.

### 어드민 화면이 관리자로 로그인해도 안 열리던 문제

두 가지가 겹쳐 있었다.

**① 토큰 키 이름을 틀렸다 (진짜 원인).**
앱은 JWT 를 **`auth_token`** 으로 저장하는데(AuthContext), 새로 만든 세 파일
(`utils/usage.ts` · `UsageMeter.tsx` · `Admin.tsx`)이 `token` 을 읽고 있었다.
그래서 **토큰이 아예 안 실려 나갔고**, 로그인해도 서버는 비회원으로 봤다.
어드민은 403, 마이페이지의 어드민 버튼도 안 나왔다.

브라우저 검증 때 내가 `localStorage.setItem('token', ...)` 으로 직접 넣고
확인해서 못 잡았다 — **테스트가 실제 로그인 경로를 안 거치면 이런 게 통과한다.**
토큰 읽기를 `getAuthToken()` 한 곳으로 모으고 `auth_token`(+ sessionStorage)을 본다.

**② 병합으로 닫힌 계정의 토큰을 들고 있었다.**
계정을 합치면서 닫은 쪽(id 4)의 토큰을 브라우저가 그대로 갖고 있었다. 서버는
그 계정을 닫힌 것으로 보므로 무엇을 해도 거부한다. 사용자는 아무것도 안 했는데
로그인이 풀리고 자기 재료가 사라진 것처럼 보인다.

`/api/auth/me` 가 **살아 있는 계정으로 넘겨준다** — 닫힌 계정의 이메일이
`merged+` 로 시작하면 원래 주소로 살아 있는 계정을 찾아 **새 토큰을 함께** 준다.
프론트(AuthContext)는 그 토큰을 저장한다. 로그아웃 없이 이어진다.

⚠️ `deleted+`(사용자가 직접 탈퇴)는 넘기지 않는다. 그건 "새로 시작"이라는 뜻이다.

검증(로컬):

| 토큰 | 결과 |
|---|---|
| id 4 (병합으로 닫힘) | 200 · `user.id=7` · `merged=true` · 새 토큰 발급 |
| 넘겨받은 토큰 → `/api/admin/me` | `is_admin: true` |
| id 8 (직접 탈퇴) | 401 — 넘기지 않음 |

서비스워커 캐시 v1.2.4 → v1.2.5.

### 사전에 없던 이름을 LLM 제안 + 관리자 승인으로 사전에 넣기

어드민에 `사전` 탭. 사진에서 읽혔지만 사전에 없던 이름을 골라 **제안 받기**를
누르면 LLM 이 셋 중 하나로 판단한다 — 기존 재료의 **동의어** / **새 재료**(분류까지)
/ **넣지 않음**. 관리자가 판단을 고칠 수 있고, **승인한 것만** 들어간다.
바로 넣지 않는 이유는 사진 인식과 같다 — LLM 은 틀리고, 사전은 모든 사용자의
레시피 매칭 기준이라 한 번 잘못 들어가면 영향이 넓다.

지어낸 값은 서버가 걸러 낸다. 동의어의 대표어가 사전에 없거나, 새 재료의 분류가
사전에 없는 조합이면 자동으로 `넣지 않음` 으로 내린다.

**후보를 글자 겹침으로만 주면 안 됐다.** 처음엔 그렇게 했는데 뜻은 같고 글자가
다른 관계를 놓쳤다 — `포항초`(시금치 품종)에 `시금치` 가 후보로 안 들어가 모델이
판단을 못 했다. 재료 대표어 **전체**(1,300여 개)를 넘기도록 고쳤더니 바로
`포항초 → 시금치`, `대저토마토 → 토마토`, `아롱사태 → 소 사태` 로 잡혔다.

**⚠️ CSV 에 바로 쓰지 않는다 — 쓸 수가 없다.**
사전 원본은 저장소의 CSV 인데, 서버가 도는 Railway 는 파일시스템이 임시라
거기 쓴 건 다음 배포에 사라지고 저장소에도 안 남는다. 그래서 승인분은
`ingredient_dictionary_additions` 표에 쌓고, 사전을 읽을 때 **CSV + DB 를 합쳐서**
쓴다(`set_extra_aliases_provider`). 반영 즉시 인식에 쓰인다.

저장소 CSV 로 접어 넣는 것은 사람이 한다 — 눈으로 보고 커밋하는 절차를 남겼다:

```bash
python scripts/apply_dictionary_additions.py --write
python scripts/sync_ingredient_dict.py --write
```

- 일부러 안 넣을 것(요리 이름·주류 브랜드)은 **목록에서 지우기**. 안 지우면 볼
  때마다 다시 판단하게 된다
- 그 사이 사전이 보강돼 이제는 잡히는 것은 취소선으로 구분한다

### 관리자 계정이 탈퇴하면 아무도 못 들어가던 문제

관리자 표시가 `users.is_admin` 한 곳에만 있어서, 그 계정이 탈퇴하면 어드민에
들어갈 수 있는 사람이 아무도 없어진다. 되돌리려면 DB 를 직접 만져야 한다.

`ADMIN_EMAILS` 환경변수(쉼표 구분)에 있는 이메일은 로그인만 하면 관리자로
승격된다. 비밀번호가 아니라 **이메일 목록**이라, 값이 새어도 그 이메일 계정에
로그인할 수 있는 사람만 통과한다.

서비스워커 캐시 v1.2.5 → v1.3.0.

### 사전 추가분을 CSV 로 옮기는 것도 자동화

어드민에서 승인한 항목은 DB 에 쌓여 **즉시 인식에 쓰이지만**, 저장소의 CSV 는
그대로였다. 서버(Railway)는 파일시스템이 임시라 거기서 CSV 를 못 고치기 때문에
"로컬에서 스크립트를 돌리세요" 로 남겨 뒀는데 — 이 컴퓨터는 매일 03:00 재료
추출을 돌리며 항상 켜져 있으므로, 같은 방식으로 자동화했다.

`apply_dictionary_additions_daily.bat` (매일 04:30, 재료 추출 뒤):

1. DB 승인분을 CSV 에 반영
2. 백엔드 사본까지 맞춤 (`sync_ingredient_dict.py`)
3. **사전이 멀쩡한지 확인** — 실패하면 되돌리고 커밋하지 않는다
4. **여기서 멈춘다.** 커밋과 푸시는 사람이 한다.

바뀐 게 없으면 3번 전에 끝난다. 대부분의 날은 여기서 끝난다.

**커밋/푸시를 배치에서 하지 않는 이유**: 푸시는 곧 배포다. 사람이 안 보는
새벽에 스스로 출시까지 해 버리면 무엇이 언제 나갔는지 모른 채 서비스가 바뀐다.
파일만 고쳐 두고, 아침에 diff --git a/apply_dictionary_additions_daily.bat b/apply_dictionary_additions_daily.bat
index c15e921d..0c65f31b 100644
--- a/apply_dictionary_additions_daily.bat
+++ b/apply_dictionary_additions_daily.bat
@@ -1,6 +1,6 @@
 @echo off
 REM ============================================================
-REM  어드민에서 승인한 사전 추가분을 저장소 CSV 로 접어 넣고 커밋한다.
+REM  어드민에서 승인한 사전 추가분을 저장소 CSV 로 옮겨 둔다.
 REM
 REM  왜 필요한가:
 REM   어드민 "사전" 탭에서 승인한 항목은 DB(ingredient_dictionary_additions)에
@@ -14,16 +14,21 @@ REM
 REM  하는 일:
 REM   1) DB 승인분을 CSV 에 반영
 REM   2) 백엔드 사본(backend/...csv)까지 맞춤
-REM   3) 사전이 정상으로 읽히는지 확인  <- 실패하면 커밋하지 않는다
-REM   4) 커밋 + 푸시 (배포에 반영)
+REM   3) 사전이 정상으로 읽히는지 확인  <- 실패하면 변경을 되돌린다
+REM   4) **여기서 멈춘다.** 커밋과 푸시는 사람이 한다.
 REM
-REM  자동 푸시를 끄고 싶으면 아래 PUSH 를 0 으로.
+REM  ⚠️ 커밋/푸시를 여기서 하지 않는 이유:
+REM   푸시는 곧 배포다. 사람이 안 보는 새벽에 스스로 출시까지 해 버리면,
+REM   무엇이 언제 나갔는지 모른 채로 서비스가 바뀐다. 파일만 고쳐 두고
+REM   변경 내용을 눈으로 본 뒤 내보내는 것이 맞다.
+REM
+REM   아침에 이렇게 확인하면 된다:
+REM     git diff -- frontend/public/ingredient_profile_dict_with_substitutes.csv
+REM     git add -A ^&^& git commit -m "재료 사전 추가분 반영" ^&^& git push
 REM
 REM  주의: 이 파일은 반드시 CRLF 개행으로 저장할 것.
 REM   LF 로 저장하면 cmd.exe 가 잘못 읽어 즉시 실패한다.
 REM ============================================================
-set PUSH=1
-
 chcp 65001 >nul
 set PYTHONIOENCODING=utf-8
 set PYTHONUTF8=1
@@ -50,7 +55,7 @@ if not errorlevel 1 (
   goto done
 )
 
-REM 4) 사전이 깨지지 않았는지 확인. 깨졌으면 되돌리고 커밋하지 않는다.
+REM 4) 사전이 깨지지 않았는지 확인. 깨졌으면 되돌린다.
 %PY% -u scriptserify_ingredient_dict.py >> %LOG% 2>&1
 if errorlevel 1 (
   echo [%date% %time%] 사전 검증 실패 - 변경을 되돌립니다 >> %LOG%
@@ -58,17 +63,8 @@ if errorlevel 1 (
   goto failed
 )
 
-if "%PUSH%"=="0" (
-  echo [%date% %time%] PUSH=0 이라 커밋하지 않고 둡니다 >> %LOG%
-  goto done
-)
-
-git add frontend/public/ingredient_profile_dict_with_substitutes.csv backend/ingredient_profile_dict_with_substitutes.csv
-git commit -m "재료 사전 추가분 반영 (어드민 승인분 자동 반영)" >> %LOG% 2>&1
-git push origin main >> %LOG% 2>&1
-if errorlevel 1 goto failed
-
-echo [%date% %time%] 반영 완료 >> %LOG%
+echo [%date% %time%] CSV 를 고쳐 두었습니다. 확인 후 직접 커밋/푸시하세요. >> %LOG%
+git diff --stat -- frontend/public/ingredient_profile_dict_with_substitutes.csv >> %LOG% 2>&1
 goto done
 
 :failed 로 확인한 뒤 내보낸다.
(처음엔 자동 푸시로 만들었다가 되돌렸다 — 자동화의 범위를 잘못 잡았다.)

**커밋 전 관문을 둔 이유** — `scripts/verify_ingredient_dict.py`:
사전은 레시피 매칭과 사진 인식의 기준이라 깨진 채 배포되면 모든 사용자에게
영향이 간다. **사람이 안 보는 사이에 나가는 변경일수록** 나가기 전에 한 번은
막아야 한다. 두 사본이 바이트까지 같은지, 별칭 수가 갑자기 줄지 않았는지,
기본 재료 8개가 여전히 잡히는지, 대표어가 서로를 가리키는 고리가 없는지 본다.

작업 스케줄러 등록: `CookMatch-DictionarySync` (일 04:30).
등록 후 즉시 실행해 `LastTaskResult: 0` 확인.

현재 등록된 자동 작업:

| 작업 | 시각 | 하는 일 |
|---|---|---|
| CookMatch-DailyLLMIngredients | 03:00 | 레시피 재료 LLM 추출 |
| CookMatch-DictionarySync | 04:30 | 사전 추가분 CSV 반영 + 커밋 |
| CookMatch-WeeklyCrawler | 07:00 | 크롤링 + 룰베이스 재료 추출 |

### 자동 푸시를 되돌림

사전 반영 배치가 커밋·푸시까지 하도록 만들었는데, **자동화의 범위를 잘못 잡았다.**
푸시는 곧 배포다. 사람이 안 보는 새벽에 스스로 출시까지 해 버리면 무엇이 언제
나갔는지 모른 채 서비스가 바뀐다.

배치는 이제 **CSV 를 고쳐 두고 멈춘다.** 검증(`verify_ingredient_dict.py`)은 그대로
돌려서 깨졌으면 되돌리고, 로그에 `git diff --stat` 을 남긴다. 아침에 확인하고
직접 커밋·푸시한다.

### 어드민 `운영` 탭 — 손으로 관리해야 하는 것들

관리자가 프로그램을 유지하려면 "무엇을 언제 손봐야 하는지" 를 알아야 하는데,
그게 저장소 여기저기 흩어져 있었다. 한 화면에 모았다.

**서버는 이 값을 만들지 못한다.** 알고 싶은 것들이 전부 개발 컴퓨터에만 있다 —
파일을 언제 마지막으로 고쳤는지는 **git 이력**이고(서버엔 git 이 없다), 크롤러가
몇 시에 도는지는 **윈도우 작업 스케줄러**다. 그래서 매일 배치가
`scripts/report_ops_status.py --write` 로 `ops_status` 표에 적어 두고 서버는 읽기만
한다. 커밋/푸시가 필요 없어 항상 최신이고, **언제 적힌 값인지**를 맨 위에 보여줘서
그 배치가 며칠 안 돌았으면 그것도 바로 보인다.

보여주는 것:

| 자료 | 지금 상태 |
|---|---|
| 재료 사전 | 2,946행 · 2일 전 |
| 대체 재료 표 | 50,138행 · **137일 전** |
| 필터 키워드 | 133행 · **247일 전** |
| 쿠팡 광고 링크 | 194행 · **링크 1개만 채움** · 7일 전 |
| 유튜브 크롤링 대상 채널 | 49행 · **131일 전** |

각 자료마다 **왜 관리해야 하는지**를 함께 적었다. 오래됐거나(120일 초과) 비어
있으면 빨갛게 표시한다.

자동 작업 셋(재료 추출 03:00 / 사전 반영 04:30 / 크롤러 07:00)의 마지막 실행 시각,
성공 여부, 다음 실행 시각도 함께 본다. 배치 로그 마지막 줄까지 붙여 둬서 실패했을
때 어드민에서 바로 원인을 볼 수 있다.

서비스워커 캐시 v1.3.0 → v1.3.1.

### 어드민 다듬기 4건

**관리자 지정/해제를 화면에서.** "관리자 계정이 탈퇴하면 어드민은 어디로 붙나"
에 대한 제대로 된 답이다. 환경변수(`ADMIN_EMAILS`)는 잠겼을 때 여는 **비상
통로**지 정상 절차가 아니다. 사용자 상세에서 다른 사람을 관리자로 지정할 수 있게
했고, **마지막 한 명은 서버가 해제를 막는다** — 실수로 잠기는 상황 자체를
만들지 않는다. 검증: 유일한 관리자를 해제하려 하니 막혔고, 다른 사람을 지정한
뒤에는 풀렸다.

**사용량 열을 오늘 / 이번 주로 나눴다.** 주 한도와 일 상한은 **따로** 걸리는데
주간만 보여주니 "주간은 남았는데 왜 막히지?" 를 설명할 수 없었다.
이제 `오늘 0 / 40`, `이번 주 0 / 100` 으로 각각 본다.

**"사용량" 과 "토큰" 이 뭔지 표 옆에 적었다.** 매번 헷갈리는 지점이다.

- **오늘 / 이번 주 = 크레딧.** 챗봇 1, 사진 인식 2. 사용자에게 보이는 숫자이고
  한도도 이 단위로 걸린다
- **토큰 = LLM 이 실제로 쓴 양(원가).** 화면엔 안 나오고, "사진 2크레딧이
  적정한가" 를 감이 아니라 근거로 판단하려고 기록만 한다.
  아직 아무도 AI 를 안 썼으면 전부 0 이다 (지금이 그렇다)

**"안 잡히는 것 모두 고르기" 를 토글로.** 고르기만 되고 풀리지 않아서, 잘못
골랐을 때 새로고침 말고는 되돌릴 방법이 없었다. 전부 골라져 있으면 `전체 해제`
로 바뀐다.

**대시보드에서 사전 미매칭 목록을 뺐다.** `사전` 탭에서 직접 고르고 반영할 수
있게 되면서 중복이 됐다. 같은 것을 두 곳에서 보는데 대시보드에서는 손댈 수 없어
"그래서 어떻게 하라는 거지" 가 된다. 개수만 알리고 사전 탭으로 보낸다.

서비스워커 캐시 v1.3.1 → v1.3.2.

### 어드민 재정리 — 탭 순서 · 활동 통계 · 모바일 · 열 정돈

**탭 순서를 바꿨다**: 대시보드 → 사용자 → 요청 → 운영 → 사전, 기본 탭은 대시보드.
어드민을 여는 이유는 대개 "지금 어떤 상태지" 이고, 사전은 가끔 손보는 것이라 맨 뒤.

**대시보드에 활동 통계를 넣었다.** "어디서 사용자가 이탈하는지, 어디서 많이
활동하는지" 를 보려면 기록이 있어야 한다.

- **어디까지 오고 어디서 멈추나** — 가입 → 재료 등록 → 레시피 반응 → AI 사용 →
  식구 그룹. 단계마다 몇 명이 남는지와 **직전 대비 얼마나 줄었는지**를 함께 본다.
  많이 줄어드는 칸이 곧 고칠 곳이다.
  (지금: 가입 6 → 재료 등록 2 **-67%** → 레시피 반응 2 → AI 사용 0 **-100%**)
- **어떤 기능을 쓰나** — 챗봇/사진 인식별 사람 수·호출·크레딧
- **사람별 활동** — 재료·즐겨찾기·완료·기록·AI·마지막 활동 시각

**⚠️ 지금 데이터로 못 보는 것을 화면에 함께 적었다.** 없는 것을 있는 것처럼
보여주면 잘못된 판단을 하게 된다:

- 어느 화면에서 나갔는지 — 화면 진입 기록이 없다
- 레시피를 열어 봤는지 — 조회 기록이 없다 (즐겨찾기·완료·기록만 남는다)
- 비회원이 무엇을 했는지 — 계정이 없어 이어붙일 수 없다

이걸 보려면 화면 진입·행동을 남기는 이벤트 기록이 따로 필요하다.

**고친 것들**

- 사용자 표의 마지막 열에 제목이 없어 `자세히` 버튼이 **탈퇴 열에 든 것처럼**
  보였다 → `관리` 로 이름을 붙였다
- 한도 조정의 메모 입력창이 `width:100%` 라 표 칸을 밀어내 **모바일에서 가로로
  늘어났다** → 최대 폭을 묶고 `box-sizing` 을 맞췄다. 상세 패널도 화면 폭에 묶었다
  (실측 375px 화면에서 입력창 315px, 가로 스크롤 없음)
- 한도 조정에 **플랜 기본값**(free 주100/일40, plus 주400/일100)을 적어 뒀다.
  숫자를 직접 적으면 그 사람에게만 적용된다는 것도 함께
- 대시보드의 사전 미매칭 영역을 완전히 제거 — 사전 탭 전용

### 레시피 재료 LLM 추출 98.8%

45,149건 중 44,586건 완료, 남은 563건. 내일 새벽 한 번이면 끝난다.
다만 크롤러가 매일 새 레시피를 가져오므로 **이 배치는 계속 돌아야 한다** —
"끝"이 아니라 신규분을 따라가는 상태로 바뀌는 것이다.

서비스워커 캐시 v1.3.2 → v1.3.3.

### 어드민에 한도 정책 표

관리자가 "이 사람 한도를 얼마로 올려 줄까" 를 정하려면 **기준이 눈앞에 있어야
한다.** 문서를 찾아보게 만들면 결국 감으로 정하게 된다. 사용자 탭 맨 위에
접었다 펴는 카드로 넣었다.

- 플랜별 주 한도 / 일 상한 표
- 무엇을 세는가 (챗봇 1 · 사진 2, 장수 무관) 와 그 이유
- 주 한도와 일 상한을 함께 두는 이유
- **주 100 이 실제로 얼마인가** — 챗봇만 100회 / 사진만 50회 /
  첫 주 전형(영수증 2 + 사진 5 + 챗봇 10) = 24 크레딧
- 올려 줄 때와 기준 자체를 바꿀 때 각각 어디를 보면 되는지

**숫자는 서버가 지금 실제로 쓰는 값을 내려준다.** 화면에 하드코딩하면 환경변수로
값을 바꿨을 때 화면만 옛 숫자를 말하게 되고, 관리자가 그걸 보고 정책을 정하면
어긋난다.

서비스워커 캐시 v1.3.3 → v1.3.4.

### 이벤트 기록 — 어느 화면을 보고 어디서 나가나

**지금 넣는 이유는 하나다: 나중에 넣으면 과거가 없다.** 광고로 사람이 들어온
뒤에 넣으면 정작 가장 궁금한 "첫 유입자들이 어디서 나갔나" 를 영영 모른다.

**작게 넣었다.** 소규모 서비스가 흔히 하는 실수가 도구를 잔뜩 붙여 놓고 아무도
안 보는 것이다. 화면 진입 + 핵심 행동만 남긴다. 그것만 있어도

- 어디서 나갔나 → 그 방문의 **마지막 화면**
- 얼마나 머물렀나 → 화면 진입 **시각의 간격**

이 나온다. 그래서 체류 시간을 따로 재지 않는다.

**개인정보**: 이름·이메일·재료 이름 같은 *내용*은 남기지 않는다. 화면 이름과
행동 이름, 누구인지 이어붙일 최소한의 식별자만.

구성

- `backend/user_events.py` + `POST /api/events` — 로그인 안 해도 받는다
  (광고로 들어온 사람은 대부분 비회원 상태로 둘러본다). 이벤트 이름은 **허용
  목록**으로 막는다 — 자유롭게 받으면 오타난 이름이 섞여 집계가 조용히 갈라진다
- `frontend/src/utils/track.ts` — 3초/10개 단위로 모아 보내고, 탭을 닫을 때는
  `sendBeacon` 으로 내보낸다. **이게 없으면 "마지막 화면"(= 이탈 지점)이 늘
  유실된다** — 정작 제일 알고 싶은 것이
- 시각은 서버가 정한다. 기기 시계가 틀릴 수 있어 "몇 ms 전" 으로 보내고 되돌린다
- `?utm_source=instagram` 을 기억해 **어디서 들어왔는지** 나눠 본다.
  안 붙였으면 referrer 로 대신한다
- 어드민 대시보드: 많이 본 화면 / **마지막으로 본 화면(= 나간 자리)** /
  재방문율 / 유입 경로

**만들면서 잡은 것 셋**

1. `caller_identity()` 가 `request.form` 을 먼저 건드려 **본문 스트림을 소비**하고
   있었다. 그래서 뒤이은 `get_json()` 이 비어 이벤트가 "성공했는데 0건" 이 됐다.
   헤더를 먼저 보고 본문은 형식에 맞는 쪽만 보도록 고쳤다
2. `React.useEffect` 를 썼는데 그 파일은 React 를 기본 임포트하지 않는다.
   **타입체크는 UMD 전역 덕분에 통과하고 런타임에만 죽는다** — 조용히 놓칠 뻔했다
3. 개발 모드(StrictMode)에서 화면 조회가 정확히 두 배로 잡혔다. 같은 화면이
   1초 안에 두 번 들어오면 한 번만 세도록 막았다

**A/B 테스트는 아직 하지 않는다.** "A안이 B안보다 낫다" 를 우연이 아니라고
말하려면 한 안당 수백~수천 명이 필요하다. 사용자가 몇 명인 지금 나누면 그 차이는
전부 우연이고, 오히려 **잘못된 결론을 확신하게 된다.** 사람이 모인 뒤에 한다.

서비스워커 캐시 v1.3.4 → v1.4.0.

### 사전 탭 — 어디로 들어가는지 보이게, 고칠 수 있게

**"동의어로 정하면 어디 하위로 들어가는지 모르잖아"** 는 정확한 지적이었다.
제안 표가 동의어일 때 분류를 `-` 로 비워 두고 있었다. 그러면 관리자는
"청상추를 상추의 다른 이름으로" 만 보고 **사전 어디에 붙는지 모른 채 승인**하게 된다.

- 동의어면 **대상 대표어가 있는 분류**를 채워 보여준다
  → `들어갈 자리 — 식재료 원물 › 채소류 › 잎채소류 › 상추류 의 상추 아래`
- 새 재료면 **분류를 직접 고른다** — 사전에 실제로 있는 조합 329개 중에서
  (없는 분류를 지어낼 수 없다)
- 동의어 대상도 바꿀 수 있다. 대표어 1,289개는 select 로는 못 고르므로
  `datalist` 자동완성. 대상을 바꾸면 **분류 표시도 따라 바뀐다**
- 표를 카드로 바꿨다. 좁은 화면에서 한 줄에 다 넣으면 정작 중요한
  "어디로 들어가는지" 가 잘렸다

### "스크립트를 돌리세요" 대신 상태를 보여준다

`scripts/apply_dictionary_additions.py --write` 를 돌리라고 안내하고 있었는데,
그건 **이미 매일 04:30 배치가 하는 일**이다(자동화한 뒤 문구를 안 고쳤다).

- 안내를 "저장소 사전 파일로 옮기는 것은 매일 새벽 4시 30분에 자동으로 진행돼요" 로 교체
- **사전에 보탠 것** 목록을 추가 — 무엇을, 어디로, 파일 반영이 `완료` 인지 `대기` 인지
  (`바로` 인식에 쓰이는 것과 `파일로 옮겨진` 것은 다르다는 걸 구분해 보여준다)

버튼을 따로 두지 않은 이유: 서버가 도는 곳은 파일시스템이 임시라 **서버가 직접
저장소 파일을 고칠 수 없다.** 버튼을 만들어도 실제로 옮기는 것은 개발 컴퓨터의
배치라, 누르면 뭔가 된 것처럼 보이지만 아무 일도 안 일어난다. 대신 언제 옮겨지는지와
지금 상태를 보여주는 편이 정직하다.

서비스워커 캐시 v1.4.0 → v1.4.1.

### 사전 탭 — 대표어 검색 드롭다운 + 분류도 LLM 이 채운다

**`<datalist>` 를 걷어내고 검색 드롭다운을 직접 만들었다.** 브라우저마다 동작이
달라 목록이 아예 안 뜨거나 앞글자가 맞아야만 걸렸다. 1,300개 중에서 고르는
일이라 "치면 바로 후보가 보이는" 게 핵심이다. 이제 부분 일치로 걸러 30개까지
보여주고, 정확히 같은 것 → 앞에서 시작하는 것 → 나머지 순으로 정렬한다.

**판단을 바꾸면 분류를 다시 물어본다.** 관리자가 "이건 새 재료야" 라고만 정하고
분류는 329개 목록에서 직접 찾게 하면 사실상 안 쓰인다. 판단(동의어냐 새
재료냐)은 사람이, 대표어·분류는 AI 가 채우는 게 맞다.

- `suggest(names, force_decision=...)` — 판단을 정해 주고 나머지만 채우게 한다
- 화면에서 판단을 바꾸면 그 한 건만 다시 물어본다 (`AI가 분류를 정하는 중...`)
- 채워진 뒤에도 드롭다운으로 고칠 수 있다

**사람이 정한 판단은 되돌리지 않는다.** 지어낸 값을 거르는 검사가 강제 판단까지
`건너뜀` 으로 내려 버리고 있었다 — 관리자는 자기가 고른 게 왜 사라졌는지 알 수
없다. 이제 못 채운 칸만 비우고 `알맞은 분류를 못 찾았어요. 직접 골라 주세요` 로
안내한다.

서비스워커 캐시 v1.4.1 → v1.4.2.

### 분류가 여전히 안 채워지던 이유 둘

**① 보여준 것과 받아들이는 것이 달랐다.** 프롬프트에는 많이 쓰이는 분류 **60개**만
보여주면서 검증은 **329개 전체**로 하고 있었다. 모델이 61번째 이후에 있는 알맞은
분류를 골라도 "목록에 없는 조합" 으로 걸러졌고, 화면에는 빈 채로 "직접 골라
주세요" 만 떴다. **모델이 이길 수 없는 구조였다.** 전체를 보여주도록 고쳤다.

**② "새 재료" 로 강제하면 모델이 분류를 비운 채 가까운 기존 이름만 돌려준다.**
(대저토마토 → "토마토", 포항초 → "시금치") 그런데 **그게 곧 답이다** — 품종·부위가
다른 새 재료는 원래 재료와 같은 칸에 들어간다. 그 자리를 빌려 쓰고 대표어 이름은
원래 이름으로 둔다.

| 이름 | 대표어 | 분류 |
|---|---|---|
| 대저토마토 | 대저토마토 | 식재료 원물 › 채소류 › 잎채소류 › 기타채소류 |
| 포항초 | 포항초 | 식재료 원물 › 채소류 › 잎채소류 › 시금치류 |
| 아롱사태 | 아롱사태 | 식재료 원물 › 육류/수산/달걀 › 육류 › 소고기류 |
| 가브리살 | 가브리살 | 식재료 원물 › 육류/수산/달걀 › 육류 › 돼지고기류 |

화면에서도 확인: 판단을 "새 재료로 추가" 로 바꾸니 분류가 자동으로 골라지고
`들어갈 자리 — 양념/조미료 › 장류(베이스) › 된장 에 새 재료 된장찌개` 가 나온다.

### 사전 추가분에 반영 시각·실패 사유·되돌리기

"승인은 했는데 실제로 들어갔나?" 를 확인할 방법이 없었다. `applied_to_csv` 가
0/1 뿐이라 **언제** 반영됐는지도, **왜** 안 됐는지도 알 수 없었다.

- `applied_at` — 사전 파일에 실제로 들어간 시각
- `apply_error` — 못 넣었으면 그 이유 (`'아이스크림' 는 이미 CSV 에 있음` 처럼)
- 화면에 `파일 반영 완료 / 반영 대기 / 반영 실패` 를 색으로 구분하고,
  승인 시각과 반영 시각을 함께 보여준다
- 실패한 것은 **원인을 고치면 다음 새벽에 자동으로 다시 시도**한다
  (`applied_to_csv` 가 0 이면 계속 대상이라 따로 재시도 버튼이 필요 없다)
- 아직 파일에 안 들어간 것은 **되돌리기**로 치울 수 있다. 이미 파일에 들어간
  것은 파일을 직접 고쳐야 하므로 안내만 하고 지우지 않는다

**CSV 에도 `추가일자` 열을 만들었다.** DB 만 보면 "이 재료가 언제 들어왔는지" 를
알려고 매번 DB 를 열어야 한다. 사전은 사람이 직접 열어 보는 파일이라 거기서도
보이는 편이 낫다. (프론트·배치 모두 열 **이름**으로 읽으므로 열을 더해도 안전하다)

목록을 표에서 카드로 바꿨다 — 좁은 화면에서 가로 스크롤이 생겨 **첫 열(이름)이
화면 밖으로 밀려** 무엇에 대한 줄인지 안 보였다.

전 과정 실행 확인 (확인 뒤 원복):

| 이름 | 결과 |
|---|---|
| 야채 | `applied_at 2026-09-02 11:49` · CSV `추가일자 2026-09-02` |
| 양복버섯 | 〃 |
| 아이스크림 | `apply_error: '아이스크림' 는 이미 CSV 에 있음` |

서비스워커 캐시 v1.4.2 → v1.4.3.

### 되돌리기는 됐는데 실패한 것처럼 보이던 문제

`되돌리기` 를 눌렀더니 `제안을 받지 못했어요: HTTPError` 가 떴다.
**되돌리기는 성공했고**(DB 에서 확인: 아이스크림이 지워짐), 화면에 남아 있던 것은
그 전에 실패한 `제안 받기` 의 오류였다.

- 동작을 시작할 때마다 지난 안내를 지운다(`clearMessages`). 안 지우면 앞선
  동작의 오류가 남아 **방금 한 일이 실패한 것처럼** 보인다
- `제안을 받지 못했어요: HTTPError` 처럼 예외 이름을 그대로 보여주면 관리자는
  무엇을 해야 할지 알 수 없다. 무엇이 잘못됐고 어떻게 하면 되는지로 바꿨다
  - 429 → `요청이 몰렸거나 오늘 한도를 다 썼어요. 잠시 뒤에 다시 눌러 주세요.`
  - 5xx → `AI 쪽이 잠시 불안정해요. 잠시 뒤에 다시 눌러 주세요.`
  - 그 밖 → 예외 이름과 함께 다시 시도하라고 안내

(당시 실패 원인은 짧은 시간에 호출이 몰린 일시적 오류였다. 키는 정상.)

### `아이스크림` 이 사전 파일 반영에 실패한 이유 — 데이터 문제

`아이스크림` 은 사전에 **이미 있다.** 다만 `대분류 = 요리이름` 으로 들어가 있다.
사전 매칭은 `재료 / 포장·제품` 행만 쓰므로 **영수증에서 읽혀도 안 잡히고**,
그래서 "사전에 없던 이름" 으로 쌓였다가, 추가하려니 같은 이름의 행이 이미 있어
중복 방지에 걸렸다.

즉 **"사전에는 있는데 재료로는 안 잡히는"** 상태다. 지금 로직으로는 계속 실패한다.
재료로 볼지 요리로 볼지는 사전 정책 문제라 임의로 바꾸지 않고 그대로 뒀다.
(같은 상태인 항목이 더 있을 수 있다 — `요리이름` 인데 영수증에 자주 찍히는 것들)

서비스워커 캐시 v1.4.3 → v1.4.4.

### 대시보드에 화면별 추이 꺾은선 (일자별 / 월별 / 년도별)

`어느 화면을 보고 어디서 나가나` 는 표만 있어서 **"지금 어디가 많나"** 에만
답했다. 정작 알아야 할 건 **"늘고 있나 줄고 있나"** 인데, 그건 시간축이 있어야
보인다. 광고를 켠 뒤 무엇이 달라졌는지도 표로는 못 읽는다.

- `GET /api/admin/activity` 에 `screen_series` / `exit_series` 추가 —
  날짜별 화면 진입 수, 날짜별 "그날 끝난 방문의 마지막 화면" (최근 400일)
- 월별·연도별은 **서버에 다시 묻지 않고** 화면에서 날짜를 접어 쓴다.
  드롭다운을 돌릴 때마다 왕복하면 느리고, 같은 자료를 두 번 세게 된다
- `화면 진입 / 나간 자리` 를 고르는 버튼 + `일자별 / 월별 / 년도별` 드롭다운

그래프를 만들 때 지킨 것:

- 계열 색은 **색각 이상(적록·청황)에서 이웃한 색이 구분되는지 검증한 조합**만
  쓴다. 순서를 바꾸거나 색을 갈아 끼우지 않는다
- 색만으로 구분하게 두지 않는다 — 계열마다 **점 모양**(원·네모·세모·마름모·도넛)이
  다르고, 이름표가 항상 붙는다. 흑백으로 뽑아도 읽힌다
- 계열은 **전 기간 합계 상위 5개로 고정**한다. 칸마다 순위로 색을 주면 단위를
  바꿀 때마다 색이 옮겨 다녀 같은 색이 어제는 홈이고 오늘은 레시피가 된다
- 기록이 없는 날도 **0으로 세운다.** 있는 날만 이으면 뜸했던 기간이 활발했던
  것처럼 보인다 — 이 그래프로 가장 하기 쉬운 착각이다
- 세로 눈금 간격을 1·2·5·10… 중에서 먼저 고른다. 꼭대기만 올려 4등분하면
  `0 / 13 / 25 / 38 / 50` 같은 눈금이 나와 볼 때마다 반올림하게 된다

### `어디서 들어왔나` 를 유입별 성적표로

전에는 `instagram 2` 같은 알약 목록뿐이었다. **방문 수만으로는 광고를 어디에 더
쓸지 못 정한다.** 3천 명이 들어와 아무도 재료를 안 담는 유입보다, 300명이 들어와
절반이 담는 유입이 낫다.

유입별로 표를 만들었다 — 방문(비중), 사람, **방문당 화면**, 재료 담기, AI 사용,
가입, 쿠팡 (뒤 넷은 방문 대비 비율 병기), **첫 화면**, 들어온 기간.

- 방문의 유입은 그 방문에서 **처음 본** `utm_source` 로 정한다. 나중 것으로 세면
  광고 효과가 엉뚱한 곳으로 옮겨 붙는다
- 출처가 없는 방문은 버리지 않고 `(직접)` 으로 묶는다. 버리면 표의 합이 전체
  방문과 안 맞아 "나머지는 어디 갔지" 를 매번 되짚게 된다
- **방문당 화면**이 1에 가까우면 첫 화면만 보고 나갔다는 뜻 — 사람은 데려오는데
  앱을 못 붙잡고 있는 유입이다
- **첫 화면**은 광고 링크가 엉뚱한 데로 떨어지고 있는지 보는 자리다

### 대시보드 카드마다 "언제부터 언제까지" 표기

카드마다 집계 구간이 다른데(누적 / 이번 주 / 최근 30일 / 화면 기록을 켠 뒤부터)
어디에도 안 적혀 있었다. 볼 때마다 "이게 언제 거지" 를 되묻게 되고, 결국
서로 다른 기간의 숫자를 나란히 놓고 비교하게 된다.

- 두 API 가 `periods` 를 함께 내려준다 (첫 가입일, 사용 기록 구간, 화면 기록
  구간, 이번 주 시작일, 30일 기준일, 서버의 지금)
- 카드 제목 아래 `기간` 줄을 붙였다
- `한눈에` 는 타일마다 구간이 다르므로 **타일마다** 적는다 — 한 줄로 뭉뚱그리면
  `이번 주 크레딧` 과 `전체 사용자` 가 나란히 놓인 걸 못 알아챈다

확인: 새 질의 4개를 실제 DB 에 돌려 봤고(오늘 방문 10건, `instagram` 유입 2건),
그래프는 표본 자료로 375px 폭에서 그려 눈금·이름표·겹침을 확인했다.
관리자 로그인이 필요한 실제 대시보드 화면은 배포 뒤 확인이 필요하다.

서비스워커 캐시 v1.4.4 → v1.4.5.

### 어드민 입구를 그룹 설정 아래로

마이페이지에서 `어드민` 이 **AI 사용량 바로 밑, 그룹 설정 위**에 있었다.
순서가 거꾸로다 — 관리자는 한 명인데 그 한 줄을 위에 두면, 모두가 쓰는
그룹 설정이 그만큼 아래로 밀린다.

`프로필 → AI 사용량 → 그룹 설정 → 어드민 → 구분선` 순으로 바꿨다.
`내 정보·설정` 묶음의 맨 끝이라, 바로 아래 구분선과 함께
"설정은 여기까지" 로도 읽힌다.

(자리를 옮겼을 뿐 권한은 그대로다. 여기서 감추는 건 보안 장치가 아니라
찾기 쉽게 하려는 것으로, 실제 검사는 서버가 `/api/admin/*` 마다 한다.)

서비스워커 캐시 v1.4.5 → v1.4.6.

### 나가는 순간의 기록만 "누구인지 모른 채" 도착하던 문제

비회원 기록이 되는지 확인하다가 찾았다. 결론부터: **비회원도 기록된다.**
다만 기록 102건 중 4건에 기기 식별자가 없었다.

원인은 `sendBeacon` 이다. 탭이 사라지는 중에는 `fetch` 가 취소되므로 마지막
기록만 beacon 으로 보내는데, **beacon 은 헤더를 붙일 수 없다.** 기기 식별자와
로그인 토큰이 둘 다 헤더로만 가고 있어서, 하필 가장 궁금한
**"어디서 나갔나" 가 익명으로 도착**했다.

- `track.ts`: 기기 식별자를 **몸통에도** 싣는다. 서버는 헤더를 먼저 보고 없으면
  이 값을 쓴다(`caller_identity` 가 이미 그렇게 되어 있었다)
- `user_events.record()`: `user_id` 가 없으면 **같은 방문의 앞선 기록에서
  물려받는다.** 방문은 한 사람의 것이다. 안 그러면 회원의 이탈 기록만
  비회원처럼 남아 **한 사람이 둘로 세어진다**
  - 로그인 토큰을 몸통에 실어 보내는 방법도 있지만, 자격 증명을 새로운 곳에
    두지 않고 서버에서 이어 붙이는 쪽을 택했다
- 이미 쌓인 것도 같은 규칙으로 메웠다(빈 값만 채움, 5행). 지금은 누구인지
  모르는 행이 0건이다

기억해 둘 것: `blind_spots` 에 적어 둔 **"앱을 지우고 다시 깐 비회원은 다른
사람으로 보인다"** 는 여전히 그대로다. 기기 식별자가 새로 만들어지기 때문이고,
이건 고칠 수 있는 종류의 문제가 아니다.

서비스워커 캐시 v1.4.6 → v1.4.7.

## 2026-09-03

### 지난 대화 목록을 바텀시트로

인라인 아코디언은 **대화 위 공간을 늘 먹었다.** 그리고 한 손으로 쥐면 화면
위쪽 모서리는 손이 안 닿는데, 이 화면은 이미 손이 아래 입력창에 있다.

모바일에서 쓰이는 방식은 넷이다 — 좌측 드로어(ChatGPT·Claude), 전체 화면
목록(메신저), 바텀시트, 인라인. **바텀시트**를 골랐다: 엄지가 닿는 자리이고,
앱에 `Sheet` 부품이 이미 있어 새 패턴을 만들 필요가 없다. 좌측 드로어는
이 앱에 없는 패턴이라 제스처 처리까지 새로 만들어야 한다.

- 머리에 `지난 대화 N` 단추 → 시트로 열린다
- 각 줄에 **그때 처음 말한 조건** + 날짜 + `장보기 N개로 M일치`.
  마지막 것만으로는 어떤 대화였는지 안 떠오른다
- 비어 있을 때도 무엇을 하면 쌓이는지 말해 준다

### `자세히 보기` 가 본문보다 눈에 띄던 것

요리 캘린더 월 목표 카드의 `자세히 보기` 가 테두리 있는 알약 배지였다.
거기 담기는 건 부수적인 안내(매월 1일 초기화 여부)라 그만한 무게를 가질
자리가 아니다.

- 테두리·배경을 빼고 **흐린 글자**로. 찾는 사람만 찾으면 된다


### 완료·기록·즐겨찾기를 토글처럼

켜졌을 때 **흐려지고** 있었다(`opacity .5` + `brightness .6`). 그건 "못 누른다"
로 읽힌다 — 토글은 켜졌을 때 도드라져야 한다.

- 켜진 것은 **노란 원**(`#FFD600`) + 검은 아이콘, 꺼진 것은 어두운 원 + 흰 아이콘
- 즐겨찾기 별은 켜지면 **채운다**
- `aria-pressed` 로 보조기술에도 눌린 상태를 알린다

### `장은 적게` 가 지출로 읽히던 것

옆의 무료 버튼이 `냉장고 재료로 · 무료` 인데 AI 쪽이 `장은 적게 · 조건대로` 라,
"장을 본다" 로 읽혀 오히려 돈이 더 드는 것처럼 보였다.

**값을 그대로 밝힌다** → `말한 대로 · 크레딧 2`. 무료 / 크레딧 2 로 나란히 놓이면
무엇이 다른지 한눈에 갈린다.

### `내 요리는 빼고 보기` 가 안 먹는 것처럼 보이던 것

식구 것이 하나도 없으면 눌러도 아무 일이 안 일어난다. 눌리기만 하고 화면이
안 바뀌면 **고장으로 읽힌다.**

- 뺄 것이 없으면 **못 누르게** 하고 `· 식구들이 한 게 아직 없어요` 라고 적는다
- 있으면 `· 식구들 것 N건` 으로 몇 건이 남을지 미리 밝힌다

### 지난 대화를 꺼내 볼 수 있게

`새 대화` 를 누르면 지금 대화가 **그냥 사라졌다.** 크레딧을 쓴 결과인데.

- `archiveChat()` — 지우는 대신 **지난 목록으로 밀어 넣는다**
  (`cookmatch_ai_chat_past`, 최근 10개). 내가 말한 게 없는 대화는 버린다
- 화면 위에 `지난 대화 N개 [보기 ▾]` — 목록에는 **그 대화에서 내가 처음 말한
  조건**과 날짜가 뜬다
- 꺼내 오면 지금 대화는 다시 목록으로 밀려 들어간다. 덮어쓰지 않는다
- 하나씩 지울 수 있다
- 확인: 꺼내 온 대화에 결과·장바구니·쿠팡 링크까지 그대로 복원됨


### 캘린더 반영 팝업의 글이 버튼 밖으로 튀어나가던 것

`height: 44` 버튼 안에 제목 + 설명 두 줄을 넣었다. 넘칠 수밖에 없었다.

- **버튼 이름만으로 뜻이 통하게** 짓고 설명은 걷어냈다
  - `새로 짠 것으로 바꾸기` + `겹치는 7일이 새 요리로 바뀌어요` → **`겹치는 날도 새로 바꾸기`**
  - `비어 있는 날에만 넣기` + `이미 정해 둔 날은 그대로 둬요` → **`빈 날에만 넣기`**
- 확인: 두 버튼 모두 44px 안에 한 줄로, 넘침 없음

### 설명글 전반 축약

| 화면 | 전 | 후 |
|---|---|---|
| 냉장고요리 안내 | 버튼을 누르면 레시피를 완료 상태로 저장해요. 완료 레시피는 **마이페이지**에서 확인할 수 있어요. | 만든 요리로 기록해요. **요리 캘린더**에서 볼 수 있어요. |
| 〃 | 버튼을 누르면 레시피를 기록 상태로 저장해요. 기록한 레시피는 마이페이지에서… | 메모를 남겨요. 요리 캘린더에서 볼 수 있어요. |
| 〃 | 버튼을 누르면 레시피 링크를 복사해서 다른 사람과 공유할 수 있어요. | 레시피 링크를 복사해요. |
| 〃 빈 상태 | 등록된 내냉장고 재료가 부족하거나 / 검색·필터 조건이 너무 좁습니다. / 재료를 추가하거나 조건을 넓혀주세요. (3줄) | 조건에 맞는 레시피가 없어요. / 재료를 추가하거나 조건을 넓혀 보세요. (2줄) |
| 첫 방문 | 3줄 | 2줄 |
| 유통기한 알림 | 약이 붙은 건 구매일과 재료 종류로 짐작한 날짜예요. 포장지에 적힌 날짜를 넣으면 정확해집니다. | 약은 짐작한 날짜예요. 포장지 날짜를 넣으면 정확해져요. |
| 조리 순서 | 조리 순서는 원문을 요약한 것이에요. 사진이나 더 자세한 설명은 원문에 있어요. | 원문을 요약한 거예요. 사진과 자세한 설명은 원문에. |
| 공유 요청 | …공유해 달라고 해요. 수락하면 그룹원이 볼 수 있어요. | …보고 싶어 해요. |
| 그룹 참여 | 그룹에 참여했어요. 냉장고 재료가 하나로 합쳐졌어요. | 참여했어요. 재료가 하나로 합쳐졌어요. |
| 설치 안내 | 장점 5줄 | 4줄 (중복 삭제) |

**사실이 달라져 있던 문구를 함께 바로잡았다** — 완료·기록을 요리 캘린더의
`내 요리` 로 옮겼는데 안내는 아직 "마이페이지에서 확인" 이라고 말하고 있었다.


### 버튼 위 숫자 배지 제거

챗봇 FAB 과 카메라 버튼에 남은 사용량이 숫자로 붙어 있었다(`UsageBadge`).
버튼은 **무엇을 하는 자리**지 계기판이 아니다. 남은 양은 그 기능을 여는 순간
(`UsageLine`)과 마이페이지에서 말한다.

### `남은 5` 가 무엇의 5인지

채팅 하단이 `한 번 물을 때 크레딧 2을 써요 · 남은 5` 였다. 숫자만 있으면
무엇의 숫자인지 다시 묻게 된다 → **`남은 크레딧 5 · 한 번에 2`**

### 문구 축약

자잘한 안내가 너무 많았다. AI 화면 기준 화면 글자 수 **108자**까지 줄였다.

| 전 | 후 |
|---|---|
| 냉장고 재료로 이번 주 식단을 짜 드릴게요. / 장을 가장 적게 보는 조합으로 고르고, / 원하는 조건이 있으면 그에 맞춰 드려요. (3줄) | 장을 가장 적게 보는 조합으로 짜 드려요. / 원하는 조건이 있으면 말씀해 주세요. (2줄) |
| 아래에서 요일을 옮기거나 캘린더에 담을 수 있어요. | (삭제 — 바로 아래에 그 버튼이 있다) |
| 쿠팡 파트너스 활동으로 일정액의 수수료를 받을 수 있어요. | 쿠팡 파트너스 · 수수료를 받을 수 있어요 |
| 위 식단에 필요한데 냉장고에 없는 재료예요. 여러 요리에 쓰이는 것부터 놓았습니다. | 냉장고에 없는 것. 많이 쓰이는 순서. |
| 곧 상해요 · 먼저 씁니다 / 오래됐어요 · 빼 뒀어요 | 곧 상해요 / 오래됐어요 |
| 바꾸기는 크레딧을 안 써요 | 바꾸기는 공짜 |
| 눌러서 빼거나 다시 넣으세요. | 눌러서 빼고 넣어요. |
| 체크를 끄면 그 날은 빼고 담아요. | 체크를 끄면 그 날은 빼요. |

### 찾은 것 — 설치 배너가 크레딧 쓰기 직전에 끼어들었다

`HomeInstallPrompt` 는 **화면 전체를 덮는 모달**(`inset: 0`, `z-toast`)이다.
식단 화면에서 뜨면 아래 고정 입력창을 덮고, 특히 AI 쪽은 조건을 적다가
크레딧을 쓰기 직전인데 그 흐름이 끊긴다.

- `/plan` 에서는 띄우지 않는다 (로그인·가입 화면과 같은 대우)
- 확인: 배너 없음, 입력창 126px · 아래 여백 170px 로 마지막 말풍선을 안 가림


### 턴마다 결과를 그 말풍선에 · 대화 이력 저장

결과를 대화 **밖**에 하나만 두고 있었다. 그러면 새로 물을 때마다 지난 답이
덮여서, "아까 뭐라고 답했더라" 를 되짚어 볼 수가 없다.

- `ChatMsg` 에 `result`(식단 + 장바구니)를 달았다. 턴마다 그 말풍선 안에 그린다
- 결과가 든 말풍선은 폭 **100%** (보통은 82%) — 좁으면 카드가 뭉개진다
- **마지막 턴만** 아래 편집 화면(요일 옮기기·요리 추가·캘린더 반영)과 이어진다.
  지난 턴은 그때 무엇을 받았는지 보여 주는 기록이라 읽기만 한다 (`TurnResult`)
- `askAi` 가 결과를 **돌려주도록** 고쳤다. 전에는 상태에만 넣어서 어느 턴의
  것인지 알 수 없었다

### 대화 이력 (`utils/aiChat.ts`)

크레딧을 쓴 결과다. 화면을 나갔다 오면 사라지는 게 맞지 않다 — 다시 보려면
크레딧을 또 써야 한다.

- `localStorage.cookmatch_ai_chat`, 최근 **40개 · 60일**
- **서버가 아닌 이유**: 비회원도 체험 크레딧으로 AI 를 쓴다. 서버에 두면 그
  사람의 이력이 갈 곳이 없다
- 레시피 전체가 아니라 **다시 그릴 만큼**(제목·썸네일·링크·이유)만 담는다
- 이력이 있으면 인사부터 다시 하지 않는다
- 머리에 **`새 대화`** — 지울 길이 없으면 이력이 오히려 짐이 된다

확인: 두 턴을 주고받은 뒤 새로고침 → 대화·결과·장바구니가 그대로 살아 있고
인사말은 한 번만. 실패한 턴("조건에 맞는 걸 못 찾았어요")도 기록으로 남는다.


### 왜 이렇게 골랐는지를 말한다

날짜별 `why` 는 있는데 **"이 일곱 개를 왜 이 조합으로 골랐나"** 가 없었다.
그게 없으니 사용자는 무료 추천과 무엇이 다른지 알 수가 없다 — 결과만 보면
둘 다 그냥 목록이다.

- 프롬프트에 `summary` 추가 (60자 안쪽). "맛있는 식단이에요" 같은 말은 금지하고
  **실제로 판단한 내용**을 적게 했다
- `_parse` 와 `suggest` 가 `(plan, summary)` 를 함께 돌려준다
- 그 문장이 AI 말풍선이 된다

실제 응답: `아이가 먹기 좋게 맵지 않은 반찬과 간식 위주로 고르고 감자를
여러 날에 나눠 썼어요.`

### 채팅 화면 정리

- **챗봇 FAB 를 숨긴다** — 이 화면 자체가 채팅인데 오른쪽 아래 버튼이 입력창을
  가리고, 채팅이 두 개인 것처럼 보였다
- **무료 안내 상자 제거** — AI 추천을 누르고 들어온 자리에 "그냥 무료로 볼게요"
  를 크게 둘 이유가 없었다 (게다가 눌리지도 않았다)
- **장바구니에 `장보기 목록으로 보기` 버튼.** 장을 보는 건 식단을 본 **직후의
  행동**이라 여기 없으면 맥락이 끊긴다. 캘린더로 보내는 것보다 이쪽이 맞다

### 같은 기능이 두 UI 로 있던 것

무료 화면 한가운데에 큰 `AI 추천받기` 카드가 또 있어서, "위에서 누른 AI 와
이건 다른 건가" 가 됐다. **한 줄 안내**로 바꿨다 —
`[AI] 조건 말하고 추천받기 · 장보기도 줄여 드려요 ›`

### 예시 하나가 제약처럼 읽히던 것

노란 버튼 부제가 `"아이 먹을 것 위주로"` 였다. 예시 하나만 박아 두면 **그것만
되는 기능**으로 읽힌다.

- 부제를 **`장은 적게 · 조건대로`** 로 — 이 기능이 실제로 해 주는 일
- 무엇을 말할 수 있는지는 **인사말에서** 알려 준다:
  `장을 가장 적게 보는 조합으로 고르고, 원하는 조건이 있으면 그에 맞춰 드려요.`


### 장보기 최소 조합 — AI 식단이 무료보다 나은 지점

지금까지 장보기 목록은 **결과**였다. 매칭률 높은 7개를 먼저 고르고 나서 "그중
냉장고에 없는 것" 을 세었다. 실측해 보니 **여든네 개**를 사야 했다.

거꾸로 푼다 — **사야 할 재료의 합집합이 가장 작아지도록** 요리를 고른다
(`backend/shopping_plan.py`, 그리디).

| | 장보기 |
|---|---|
| 매칭률 순 상위 7 (전) | **84개** |
| 최소 조합 (후) | **7개** |
| `아이 먹을 것 위주로` | **8개** |

부수 효과 — 매칭률 순 상위가 전부 `전골 10가지`, `반찬 20가지` 같은 **다중 레시피
글**이었다(재료가 많으니 매칭률이 높다). 최소 조합은 재료가 적은 **단일 요리**를
자연스럽게 고른다.

#### 재 보고 정한 값들

- **그리디에게 딱 필요한 만큼만.** 7일치인데 12개를 고르라 하면 장바구니가
  7 → 16개로 뛴다
- **LLM 에게는 그 7개만.** 후보를 여섯 개 더 얹었더니 그쪽으로 새어 나가
  7 → 19개가 됐다. 프롬프트로 타이르는 것보다 **줄 수 있는 것을 줄이는 쪽**이 확실
- **모자란 날은 그 7개에서 채운다** — LLM 이 일곱을 달라고 해도 셋만 줄 때가 있다
- **장바구니는 최종 식단에서 센다** — 미리 좁힌 것에서 세면 "7개" 라 적어 놓고
  실제로는 열셋을 사야 하는 일이 생긴다
- 후보 60 → 300 (겹치는 조합을 찾을 여지가 있어야 한다)

#### 화면 · 쿠팡

- `장보기 7개면 7일치가 돼요` 를 노란 카드로 못 박고, 재료마다 **`사러가기 ↗`**
  (`resolveCoupangUrl`) + 파트너스 고지
- 장바구니가 짧고 구체적이라 **살 것이 분명하다** — 광고가 자연스럽게 붙는 자리다

### 고친 버그 — AI 결과가 화면에 안 올라오던 것

`askAi` 가 아직 옛 자료 구조(`slot.recipe`)에 결과를 넣고 있었다. 하루 여러 끼
구조(`slot.meals`)로 바꾸면서 이 한 줄을 빠뜨려서, **AI 가 짜 준 식단이 화면에
전혀 반영되지 않고 무료 추천이 그대로 남아 있었다.**


### 시스템 달력을 앱 달력으로 (전수 조사)

`<input type="date">` 를 그냥 쓰면 안드로이드·iOS·크롬이 **각자 다른 달력**을
띄운다. 재료 유통기한은 우리 달력(`CustomCalendar`)으로 고르는데 식단 기간은
시스템 달력이 뜨니, 같은 앱에서 날짜를 고르는 방법이 두 가지였다.

조사 결과 **다섯 자리**가 시스템 달력을 쓰고 있었다:

| 자리 | 무엇 |
|---|---|
| `CookingCalendar` ×2 | 목록 기간 직접 지정 |
| `CookingCalendar` ×1 | 완료 날짜 고치기 |
| `PlanThisDay` ×1 | 레시피 시트의 `다른 날짜` |
| `Admin` ×2 | 대시보드 기간 |

- **`DatePickerField` 신설** — 버튼처럼 생긴 자리를 그리고 누르면 우리 달력을
  띄운다. 값은 `YYYY-MM-DD` 로 주고받는다
- 다섯 자리 전부 교체. 이제 앱에 남은 `type="date"` 는 없다
  (`IngredientRecognitionSheet` 의 것은 주석이지 실제 입력이 아니다)

### 기간 직접 지정 — 적용 버튼

- `기간` 라벨과 **`[적용]`** 버튼 신설. 고른 값은 초안(`draft`)에 담고
  누를 때 반영한다 — 시작일만 고른 순간 "그날부터 오늘까지" 로 한 번 조회되는
  중간 결과는 아무도 원한 적이 없다
- 미적용 상태에는 `아직 적용 안 됐어요`

### 목록 스크롤 아래 여백

마지막 카드가 스크롤 경계에 딱 붙어 있어 더 있는지 없는지 알 수 없고 집기도
어려웠다 → `paddingBottom: 14`

### AI 화면이 무료 결과를 먼저 깔던 것

들어오자마자 무료 추천 카드를 보여 주니 **"왜 크레딧을 써야 하지"** 가 됐다.
이 화면은 조건을 받으러 온 자리다.

- 조건을 **들은 뒤에만** 식단을 보여 준다 (`chat.some(m => m.who === 'me')`)
- 그 전에는 무엇을 말할 수 있는지 안내하고, 마음이 바뀌면 나갈 길을 준다
  (`그냥 재료로만 추천받을게요 (무료) ›`)
- 채팅 화면에는 위에 접힌 `쓸 재료` 가 이미 있으므로 ① 영역은 감춘다
  (같은 것이 두 번 나왔다)


### AI 식단을 **채팅**으로

무료 화면과 같은 폼에 시트만 얹었더니, 시트를 닫는 순간 무료 화면과 구분이
안 됐다. 조건을 주고받는 일은 **대화**다.

- `/plan?ai=1` 은 채팅 화면 — AI 말풍선(왼쪽·흰색) / 내 말풍선(오른쪽·먹색),
  아래 고정 입력창(둥근 44px + `↑` 보내기), 그 위에 자주 쓰는 조건 알약
- **쓸 재료는 맨 위에 접어 둔다** (`쓸 재료 30개 [보기 ▾]`). 무엇으로 짜는지는
  늘 보여야 하지만 대화보다 앞설 일은 아니다
- 조건을 보내면 내 말풍선이 남고, 답이 오면 `이렇게 짜 봤어요` 가 붙는다.
  기다리는 동안 `고르는 중이에요...` 말풍선
- 아직 아무 말도 안 했는데 결과가 있으면 AI 가 짜 준 것으로 오해한다 →
  `아래는 냉장고 재료만으로 골라 본 거예요` 라고 밝힌다
- `조건 바꿔서 다시` 버튼은 뺐다 — **아래 입력창이 곧 그 일**이다.
  같은 일을 하는 버튼이 둘이면 어느 쪽이 크레딧을 쓰는지 헷갈린다
- 하단에 `한 번 물을 때 크레딧 2를 써요 · 남은 5`

### 버튼 순서 — AI 가 먼저

식단은 베네핏이 큰 기능이라 `이번 주 AI 식단 추천` 을 **왼쪽 첫 자리**로.
(확인: AI x 14 `shimmer, breathe` / 무료 x 179 `animation: none`)

### `내 요리는 빼고 보기` 가 기록에서 안 먹던 것

**찾은 것**: `기록` 목록은 늘 `/api/users/{id}/recorded-recipes`(**내 것만**)를
쓰고 있었다. 그래서 `우리 식구 요리` 탭에서 기록을 눌러도 내 기록만 보였고,
체크박스는 완료에만 걸려 기록에서는 아무 효과가 없었다.
그룹용 엔드포인트(`/api/households/me/recorded-recipes`)가 이미 있는데 안 썼다.

- 식구 탭에서는 그룹 기록을 쓴다. 각 줄에 **누가 기록했는지**(`acted_by`)를 적는다
- `내 요리는 빼고 보기` 는 **오직 나만 기록한 것**을 뺀다 —
  나와 식구가 같이 기록한 레시피는 남긴다 (기록은 레시피 한 장에 여러 사람이
  묶여 오므로 `user_id` 가 아니라 `acted_by` 로 판단한다)
- 체크박스를 기록에서도 보이게 하고, 옆의 `(식구들 것 N건)` 도 기록 기준으로 센다

### 목록 탭 정리

- **인원별 색 집계 제거** — 줄마다 누구인지 점과 이름으로 이미 보인다.
  위에 또 요약을 두면 같은 말을 두 번 한다. 달력에만 남긴다
- **기간 직접 지정** 추가 (`전체 / 1년 / 3개월 / 직접`).
  퀵 버튼으로 안 되는 구간(작년 여름 같은)이 있다


### AI 입구가 무료 화면과 똑같던 것 — 조건부터 묻는다

카드 순서만 바꿔서는(위/아래) "다른 화면" 이 아니다. `이번 주 AI 식단 추천` 을
누른 사람은 **조건을 말하러 온 것**이다.

- `?ai=1` 로 들어오면 **조건 묻는 시트**가 먼저 뜬다 (`어떤 식단으로 드릴까요?`)
- 자주 적는 조건 여섯을 알약으로 — 빈 칸만 주고 "적어 보세요" 하면 대부분
  그냥 넘긴다. 눌러서 고를 수 있으면 **무엇을 말할 수 있는 자리인지**가 보인다
- 버튼은 `AI 추천받기 · 크레딧 2` — 얼마가 나가는지 누르기 전에 적는다
- **닫을 수 있다** (`그냥 무료 추천 볼게요`). 크레딧이 없거나 마음이 바뀌었을 때
  화면이 막히면 안 된다
- `askAi(text?)` 가 글을 인자로 받는다 — 시트에서 방금 고른 조건은 아직 상태에
  안 들어가 있을 수 있다

### `조건 바꿔서 다시` (크레딧)

AI 로 들어온 화면에만 둔다. 무료로 굴려 보다가 "이게 아닌데" 싶을 때 조건을
바꿔 다시 부르는 **계단**이다.

- `↻ 다시 추천` 은 **그대로 무료**로 남긴다. 이 버튼의 값어치가 바로 "공짜" 다 —
  크레딧을 물리면 누르기를 주저하고, 그러면 식단을 완성하지 못한 채 나간다
- 확인: `↻ 다시 추천` `animation: none`, `조건 바꿔서 다시` `shimmer, breathe`

### 워딩

카드 안 버튼 `추천받기` → **`AI 추천받기`**


### 한 화면에 기준이 둘이던 것

요리 캘린더 위쪽 **월 목표 카드**(인원별 `N회`, 달성률, 절약액)는 **보고 있는 달**을
세는데, 그 아래 `내 요리` / `우리 식구 요리` 목록은 **전 기간**이었다. 같은 화면에
두 기준이 섞여 있으니 어느 숫자가 무엇인지 알 수가 없었다.

- 월 목표 카드는 **달력 탭에서만** 보인다. 그건 달력의 이야기다
- 목록 탭에는 **그 목록과 같은 기준**의 인원별 집계를 둔다
- 기준을 글로도 밝힌다 — `전 기간 기준이에요. 위 달력의 월 목표와는 다른 숫자예요.`

### 식구 수·한 끼 추정액 — 바깥을 눌러도 저장된다

노란 체크 버튼을 찾아 눌러야만 반영됐다. 고쳐 놓고 딴 데를 누르면 말없이 사라졌다.

- `onBlur` 로 저장. `Esc` 로 취소
- 체크 버튼은 그대로 두되 `onMouseDown` 에서 기본 동작을 막는다 —
  blur 가 먼저 나가면 버튼이 사라져 클릭이 씹힌다

### 워딩

`조건 넣어 추천` → **`이번 주 AI 식단 추천`** (넘침 없음 확인)


### 두 입구가 같은 화면을 열던 것 + `짜기` 를 걷어냄

버튼만 둘로 나눠 놓고 **둘 다 같은 화면**으로 보냈다. 그러면 나눈 의미가 없다.
`조건 넣어 추천` 을 누른 사람은 조건을 말하러 온 것이다.

| | `/plan` | `/plan?ai=1` |
|---|---|---|
| 제목 | 이번 주 식단 추천 | **AI 식단 추천** |
| 맨 위 | 쓸 재료 (AI 칸은 y 243) | **조건 넣는 칸** (y 128, 커서까지) |

- AI 카드를 `aiCard` 로 떼어 내고, `?ai=1` 이면 **① 재료보다 위**에 꽂는다
- 화면 제목도 `AI 식단 추천` 으로 바뀐다

#### 버튼 배치

가로로 긴 줄을 위아래로 쌓으니 둘 다 "주요 버튼" 처럼 무거워지고, 정작
무엇이 다른지는 안 보였다. **나란히** 둔다 — 평범한 것과 노란 것이 한눈에 갈린다
(확인: x 14 / x 205, 같은 높이 62, 일반 `animation: none` · AI `shimmer, breathe`).

#### `짜기` → `추천`

식단을 **짜는** 건 앱이 하는 일이고, 사람이 하는 건 **추천을 받는** 것이다.
화면 제목도 원래 `이번 주 식단 추천` 이었다.

| 전 | 후 |
|---|---|
| 이번 주 식단 짜기 | **이번 주 식단 추천** |
| AI 로 짜기 | **조건 넣어 추천** |
| 조건을 넣어 짜기 | **조건 넣어 추천받기** |
| AI로 짜기 (버튼) | **추천받기** |
| 짜는 중... | **고르는 중...** |
| ↻ 다시 짜기 | **↻ 다시 추천** |
| 어떻게 짜 드릴까요? | **어떤 걸로 드릴까요?** |
| AI 가 새로 짰어요. | **AI 가 새로 골랐어요.** |


### 식단 입구를 둘로 — 노란색이 거짓말을 하고 있었다

요리 캘린더의 `이번 주 식단 짜기` 는 노란색 + `AI` 배지 + 반짝임을 달고 있었는데,
눌러 봐야 **그냥 페이지가 열렸다.** AI 는 그 안에 있었을 뿐이다.
이런 게 쌓이면 노란색이 "여기서 돈이 나간다" 를 뜻하지 않게 되고,
정작 크레딧을 쓰는 자리에서도 사람이 안 멈춘다.

| 버튼 | 옷 | 하는 일 |
|---|---|---|
| `이번 주 식단 짜기` | 평범한 흰 버튼 | `/plan`. 무료 |
| `AI 로 짜기` | 노란 그라디언트 + 반짝임 + `AI` 배지 | `/plan?ai=1`, AI 칸을 열어 준다 |

**AI 버튼도 누르는 순간 크레딧을 쓰지는 않는다.** 냉장고를 보기도 전에,
조건을 적기도 전에 돈이 나가면 결과가 마음에 안 들 때 그대로 손해다.
데려다 주기만 하고 크레딧은 그 칸에서 나간다.

- `/plan?ai=1` 로 들어오면 AI 칸으로 스크롤하고 입력창에 커서를 놓는다.
  왜 여기로 왔는지 알 수 있게 그 칸에 노란 테두리를 두른다
- 확인: 일반 버튼 `background: none / animation: none`,
  AI 버튼 `linear-gradient + ai-fab-shimmer, ai-fab-breathe`.
  `?ai=1` 진입 시 테두리 `rgb(255,214,0) 2px` · 입력창 포커스 · 화면 안(y 243)
- `MEAL_PLAN_FEATURE.md` 에 "입구가 둘인 이유" 로 남김


### 범례가 잘려 건수와 붙어 보이던 것

`즐겨찾기만` 을 결과 줄에 넣으면서 한 줄에 셋(범례 243px + 건수 + 버튼)을
욱여넣었다. 375px 화면에 안 들어가서 범례가 198px 상자에 갇혔고,
`보유 재료` 가 반쯤 잘린 채 건수와 붙어 보였다.

- 범례가 **제 줄**을 갖고, 건수와 `즐겨찾기만` 이 아랫줄을 나눠 쓴다
  (확인: 범례 오른쪽 끝 263 < 화면 375, 잘림 없음)
- 즐겨찾기만 볼 때는 건수 대신 `즐겨찾기에 담아 둔 요리` 라고 적는다 —
  서버 건수는 그때 안 맞는 숫자다

### 요리 캘린더 탭·목록 다듬기

- **탭이 폭을 나눠 갖지 않는다.** 셋으로 쪼개 늘리니 글자보다 밑줄이 훨씬
  길어져 둔해 보였다. 밑줄은 **글자 밑**에만 (`left/right: 14`)
- **기간 좁히기** 추가 — `전체 / 1년 / 3개월`. 달력의 `< 2026년 9월 >` 은
  가져오지 않았다. 그건 한 달씩 **넘기는** 장치라 "여태 만든 것" 을 보러 온
  자리와 안 맞는다. 넓은 쪽에서 좁히는 방식이 맞다
- `내 요리는 빼고 보기` 를 완료·기록 칩 **아래**로 옮기고, 기간 칩은 그 줄
  오른쪽 끝으로. 왼쪽은 *무엇을* 보는지, 오른쪽은 *얼마나 넓게* 보는지다
- **`우리 식구 요리` 에서도 `기록` 을 볼 수 있게** 되돌렸다 (감췄던 것을 복구)
- 목록을 **`58vh` 안에서만 스크롤**한다. 줄이 끝없이 이어지면 이 화면을
  벗어나는 데만 한참 걸린다. 머리(고르개)는 고정된다

### `내 요리는 빼고 보기` 를 누르면 다 사라지던 것

동작 자체는 맞았다 — 완료가 **전부 본인 것**이면 본인을 빼면 0건이 된다.
문제는 그걸 화면이 말해 주지 않아 고장으로 보였다는 것이다.

- 체크박스 옆에 **`(식구들 것 N건)`** 을 미리 적는다. 눌러 놓고 텅 비면
  고장으로 읽힌다
- 비었을 때 문구를 따로 둔다 — `식구들이 만든 요리가 아직 없어요.
  지금까지의 완료는 전부 내가 한 거예요.`


### `내 요리` 가 식구 것까지 합쳐서 보여 주던 것

서버(`completed-calendar`)는 **그룹 전체**를 내려 준다. 그걸 그대로 그려서
`내 요리` 인데 식구 것이 다 섞여 있었다.

- 탭을 셋으로 — `달력` / `내 요리` / `우리 식구 요리`(그룹에 속했을 때만)
- `내 요리` 는 `user_id === 나` 만. `우리 식구 요리` 는 전부이되
  **`내 요리는 빼고 보기`** 체크박스로 나를 뺄 수 있다
- 기록은 아직 **내 것만** 모은다. 식구 탭에서 내 기록을 보여 주면 식구 것으로
  오해하므로 그 탭에서는 `기록` 칸 자체를 감춘다

### 탭을 탭처럼

알약으로 뒀더니 **바로 아래 일/주/월 알약과 같아 보여서**, 화면을 바꾸는
것인지 결과를 좁히는 필터인지 구분이 안 됐다.

- 밑줄 탭(높이 44, 선택된 것에 2px 밑줄)으로. 탭은 밑줄로 "지금 여기 있다" 를 말한다

### 인원별 범례의 `? 3회`

내가 만든 버그다. `mergeLocalDone()` 이 기기 기록에 `user_id: 0, nickname: ''`
을 붙였는데, 닉네임 표는 `nicknameById.get(uid) || '?'` 라 물음표가 됐다.

- 기기에 남은 완료는 **이 사람 것**이므로 로그인한 사람의 id·이름을 붙인다
- 닉네임 표에 **빈 이름은 넣지 않는다** — 넣어 두면 `|| '?'` 가 안 걸려
  빈칸으로 보인다. 내 id 는 항상 넣어 둔다

### 날짜 고르개가 검색창처럼 보이던 것

가로로 긴 `input[type=date]` 을 따로 한 줄에 뒀더니 검색창으로 읽혔다.

- **알약 줄 끝에, 같은 크기(32px)의 `다른 날짜` 칩**으로. 투명한 날짜 입력을
  칩 위에 덮어 눌렀을 때 달력이 뜬다 (확인: 82×32, 다른 알약과 같은 높이)
- 점선 테두리로 "여기서 더 고를 수 있다" 를 말한다

### `즐겨찾기만` 자리

화면 맨 위에 혼자 떠 있어서 무엇에 걸리는 버튼인지 알 수 없었다.
**결과를 좁히는 일**이므로 결과 줄(`총 N건`) 옆으로 옮겼다 (y 249).
즐겨찾기만 볼 때는 서버 건수를 감춘다 — 그 숫자는 서버 결과의 것이라 안 맞는다.

### 마이페이지 → 캘린더

`만든 요리 돌아보기` 로 들어가면 **`내 요리` 탭이 열려 있게** 했다.
달력이 열리면 방금 누른 것과 다른 화면이 나와서 한 번 더 눌러야 했다.


### 냉장고요리에서 누른 완료가 캘린더에 안 뜨던 것 — 서버에 안 올라가고 있었다

레시피 카드에서 완료를 눌러도 요리 캘린더의 **월·주·일·목록 어디에도** 안 떴다.
원인은 `RecipeList` 가 완료·기록·즐겨찾기를 **기기(localStorage)에만** 저장하고
있었기 때문이다. 요즘인기(`Popular`)는 서버에도 보내는데 이쪽만 빠져 있었다.
캘린더는 서버를 읽으니 아무것도 없었다.

- `syncToServer(type, id, remove?)` 추가 — 담을 때 `POST`, 취소할 때 `DELETE`.
  실패해도 막지 않는다(기기 저장은 이미 끝났고 화면은 그걸로 그린다).
  비회원은 서버에 계정이 없으니 기기 저장이 전부다
- 완료·기록·즐겨찾기 **세 곳 모두**와 취소 경로에 붙였다

#### 이미 기기에만 남아 있는 것도 보이게

로그인 전에 눌렀거나 서버 반영이 실패한 완료는 기기에만 있다. 서버 것만 그리면
분명히 눌렀는데 목록이 비어 보인다.

- `mergeLocalDone()` — 서버 기록에 기기 기록을 합친다(같은 날·같은 레시피는 한 번)
- **달력(월·주·일)에도** 합친다. 보고 있는 달 밖의 것은 걸러 낸다
- 목록에도 합친다

### `목록` → `내 요리`

`목록` 은 **무엇의** 목록인지 말하지 않는다. 여기 담기는 건 내가 완료했거나
기록한 요리다. 위층은 `달력 / 내 요리`, 그 안에서 `완료 / 기록` 을 가른다.


### 요리 캘린더 목록이 "0건" 이던 것

목록을 달력이 쓰는 `entries` 로 그렸는데, 그건 **보고 있는 달만** 담는다.
이번 달에 완료한 게 없으면 0건이 됐다 — 여태 만든 것을 보러 온 화면인데.

- 목록을 열 때 **전 기간**을 따로 한 번 불러온다(`allEntries`)
- **`목록` 을 일/주/월에서 떼어 냈다.** 그 셋은 **기간**이고 목록은 기간이 아니다.
  같은 줄에 두니 "목록이라는 기간" 처럼 읽혔다.
  이제 `달력 / 목록` 을 먼저 고르고, 기간(일·주·월)은 **달력일 때만** 나온다.
  목록에는 이전/다음 넘기기도 없다 — 넘길 것이 없다
- 목록 안에서 **`완료` / `기록` 을 갈라 본다.** 완료만 옮기면 기록은 여전히
  마이페이지 카드 안에서만 볼 수 있었다. 둘 다 "내 요리 이력" 이지만 다른
  것이다 — 완료는 만든 사실, 기록은 남긴 메모.
  기록은 서버가 안 되면 기기에 있는 것으로라도 보여 준다

### 레시피에서 날짜 잡기 — 달력 추가, 노랑 뺌

- **직접 고르는 달력**(`input[type=date]`) 추가. 알약 열넷은 **가까운 날**을 빨리
  누르라고 있는 것이고, 그보다 먼 날이나 정확한 날짜는 달력이 빠르다
  (확인: 20일 뒤 `9/23` — 알약 범위 밖 — 을 지정해 계획에 들어감)
- `날짜 고르기` 버튼에서 **노랑을 뺐다.** 노랑은 이 앱에서 AI 와 주요 실행을
  뜻한다. 날짜를 고르는 건 그만큼 무거운 일이 아니다

### 즐겨찾기 토글

- **별표를 뺐다.** 카드 위 즐겨찾기 버튼의 별과 뜻이 겹쳐서, 이 버튼이
  "별표를 매기는" 것인지 "별표만 보는" 것인지 헷갈렸다
- 켜졌는지는 다른 필터 칩과 **같은 방식**(먹색 바탕 + 흰 글자)으로 말한다
  (확인: 꺼짐 `#FFFFFF` → 켜짐 `#1A1A1E`)

### 크레딧 안내의 괄호

`월요일마다 5 크레딧씩 채워져요 (9월 8일 월요일)` 에서 괄호를 뺐다.
"월요일마다" 라고 이미 적혀 있는데 다음 월요일이 며칠인지까지는 알 필요가 없다.


### 완료·기록·즐겨찾기를 쓰는 자리로 옮김

셋을 마이페이지에 다 펼쳐 두니 화면이 계속 길어졌는데, 정작 필요한 순간에는
그 목록이 다른 화면에 있었다. 셋은 성격이 다르다.

| | 성격 | 새 자리 |
|---|---|---|
| 완료·기록 | **지나간 사실** | 요리 캘린더의 `목록` 보기 |
| 즐겨찾기 | **앞으로 할 것** | 냉장고요리의 `즐겨찾기만` 보기 |

- **요리 캘린더에 `목록` 보기 신설** — 달력은 "이번 달" 만 보여 줘서 여태 만든
  것을 훑으려면 달을 계속 넘겨야 했다. 같은 자료라 보기만 바꾼다.
  같은 날이 이어지면 날짜를 한 번만 찍는다
- **냉장고요리에 `즐겨찾기만` 토글** — 서버에 다시 묻지 않고 기기에 담아 둔 것을
  그대로 그린다. 서버 결과에서 걸러 내면 지금 페이지에 없는 즐겨찾기가 빠진다
  (확인: 카드 30개 → 2개)
- **마이페이지에서 세 목록 제거.** 요약 숫자는 그대로 두고(입구), 그 아래
  `즐겨찾기로 요리 고르기` · `만든 요리 돌아보기` 두 줄을 붙였다 —
  숫자만 있으면 "그래서 어디서 쓰지" 가 남는다

### 레시피를 보다가 바로 "언제 해먹을까" (`PlanThisDay`)

계획은 `이번 주 식단 추천` 을 거쳐야만 세울 수 있었다. 그런데 사람이 요리를
정하는 순간은 그 화면이 아니라 **레시피를 보고 있을 때**다. 냉장고요리를 넘기다
"이건 금요일에 해먹자" 가 되는데 담아 둘 자리가 없어 그 생각이 그냥 사라졌다.

- 조리 순서 시트(`CookModeSheet`) 안, 원문 버튼 위에 붙였다.
  **카드가 아니라 시트인 이유**: 카드는 그리드에 촘촘히 놓여 훑는 자리이고,
  훑는 중에는 아직 정하는 중이 아니다. 시트를 연 순간이 판단하는 자리다
- 오늘부터 14일 중에 고른다. 그보다 먼 계획은 세워도 안 지킨다
- 이미 잡아 둔 날은 켜진 채로 보이고, 다시 누르면 뺀다
- 그 날 다른 요리가 이미 있으면 **건드리지 않는다**(`savePlan(..., 'fill')`) —
  하루에 여러 끼를 할 수 있고, 하나 더하는 것이 남의 계획을 지울 이유가 없다
- 즐겨찾기와 역할이 갈린다: 즐겨찾기는 **언젠가**, 계획은 **언제**

### 크레딧 단위

`남은 크레딧 37개` 는 무엇이 37개인지 다시 묻게 만든다. **크레딧이 곧 단위다.**

- 머리에서 `개` 제거 → `남은 크레딧 37`
- 쓰는 곳 칩에 단위를 붙임 → `재료 담기 2 크레딧`. 숫자만 있으면 무슨 숫자인지 모른다
- `월요일마다 5개씩` → `월요일마다 5 크레딧씩`, `가입하면 30개` → `30 크레딧`
- 일 상한만 `번` 을 쓴다 — 그건 크레딧이 아니라 **횟수**다


### AI 식단이 "아이 먹을 것" 을 못 알아들은 이유 — 요청이 후보 선정에 안 쓰였다

`/api/plan/suggest` 는 냉장고 재료 매칭만으로 후보 40개를 뽑아 LLM 에게 넘겼다.
`request_text` 는 프롬프트에만 들어가고 **후보를 고르는 데는 전혀 안 쓰였다.**
그 40개 안에 아이가 먹을 만한 게 없으면 LLM 이 아무리 잘해도 나올 수가 없었다.

- **`backend/plan_intent.py` 신설** — 적은 말을 레시피에서 찾을 수 있는 말로 바꾼다.
  앱의 레시피 필터가 쓰는 분류(효능·대상·TPO·난이도·스타일)를 그대로 옮겨,
  버튼으로 고르든 글로 적든 같은 말로 찾게 했다
  - `아이` → `아이/아이들/유아/어린이/키즈/아기` + 지침 "맵지 않고 자극이 적어야"
  - `국물 요리는 빼고` 처럼 **뒤쪽에 오는 부정**도 읽는다(뒤 12글자 안).
    `국물 좋아하는데 매운 건 싫어` 는 국물을 빼지 않는다 — 오탐 확인함
- 후보 질의에 `want_hit`/`avoid_hit` 을 넣어 **요청에 맞는 것을 위로** 올린다.
  걸러 내지는 않는다 — 딱 맞는 게 없을 때 후보가 0이 되면 아무것도 못 짠다.
  후보 수도 40 → 60
- 프롬프트에 상황별 해석 지침 추가 — 제목의 말을 그대로 믿지 말고 요리 자체를
  보라고. 아이/다이어트/담백/간단/손님상 각각 무엇을 빼야 하는지 적음

확인 (LLM 없이 SQL 만):

| 요청 | 전 (매칭률만) | 후 |
|---|---|---|
| (없음) | 전골 10가지 · 백반집 한 상 10가지 | 그대로 |
| 아이 먹을 것 | 〃 | 아기등갈비 · 아기 목살 수육 · 아이죽 · 유아간식 |
| 다이어트·담백 | 〃 | 혈당 걱정 없는 케이크 · 다이어트 샌드위치 · 연두부계란탕 |
| 간단한 걸로 | 〃 | 초간단 호박죽 · 초간단 샌드위치 |

### 한 글에 요리가 여러 개인 것도 지운다

식단은 "이 날 이걸 만든다" 를 정하는 것인데, 한 글에 반찬이 넷이면 그 날 무엇을
만들지가 안 정해지고 재료·조리 순서가 네 요리 것으로 섞여 매칭률이 엉킨다.
요청 없이 뽑은 상위 후보가 전부 `전골 10가지`, `반찬 20가지` 였던 게 그 증거다.

- `MULTI_RECIPE` 판정 신설 (`llm_ingredient_extraction.py`). 지우는 것은
  `NOT_RECIPE` 와 같지만 **왜 지웠는지**를 로그에서 갈라 본다
  (`not_recipe` 자리에 이유 문자열을 싣는다 — 빈 문자열은 거짓이라 기존 조건문이
  그대로 동작한다)
- 프롬프트 두 곳(단건·묶음)에 규칙 추가. 곁들임 소스·밑간을 함께 설명하는 것과
  같은 요리의 변형(매운맛/순한맛)은 **한 요리로 본다**
- **묶음 파서의 구멍 하나**: 한 건짜리 묶음에서 모델이 번호 없이 `"MULTI_RECIPE"`
  한 줄만 답하면 `batch response missing this index` 오류가 되어, 정작 지워야 할
  글이 그대로 남았다. 낱개 판정도 받도록 고침
- 확인: `메인 하나에 반찬 셋 — 조합 10가지`, `일주일 밑반찬레시피 주간밥상` →
  `multi_recipe`. 재료가 11~16개인 **단일 요리**(라따뚜이·가지볶음·동그랑땡)는
  보존 — 오탐 없음
- 어드민 운영 탭의 이 배치 설명에 **지우는 글 네 가지**와 ②를 지우는 이유를 적음

### 어드민 사전 탭 — "계속 300개가 나온다" 의 정체

목록 질의가 `LIMIT 300` 이다. 처리하면 뒤에 있던 301~600번째가 올라온다.
그리고 **딱 한 번만 나온 이름이 전체의 3분의 2**였다(9,410 / 14,223).

- 한 번만 나온 9,410행을 `dismissed = 1` 로 치웠다. 물리 삭제가 아니다 —
  지우면 매일 도는 배치가 같은 글을 다시 훑다 되살려 놓는다
- **빈도 하한**(`min_hits`, 기본 2회) 신설 + `2·5·10·50회` 고르개
- 목록 위에 규모를 적는다: `치우지 않은 것 4,778개 · 이 기준에 드는 것 N개 ·
  지금 보는 것 300개 (많이 나온 순 300개씩. 치우면 다음 300개가 올라와요)`
- `사전에 보탠 것` 도 정해진 높이(480px) 안에서만 스크롤

(이미 되어 있던 것: `목록에서 지우기` 는 `dismissed=1` 을 세우고 목록 질의가
`COALESCE(dismissed,0)=0` 으로 거른다. 한 번 치운 이름은 다시 안 나온다.)

### 마이페이지 크레딧 카드

- 남은 양을 **20px 로 크게** — 제목과 같은 크기라 이름만 읽히고 숫자는 안 읽혔다
- **쓰는 곳 세 군데를 다 적는다** — `재료 담기 2 · 요리 챗봇 1 · 식단 짜기 2`.
  식단 짜기가 빠져 있어서 크레딧이 왜 줄었는지 알 수 없는 경우가 있었다.
  값은 서버가 준 것을 그대로 쓴다
- 아래 설명을 한 줄로, 비회원 안내 중복 줄 삭제


### 하루에 여러 끼 — 요일을 옮기면 남의 요리가 끌려오던 것

토요일 것을 일요일로 옮기면 **멀쩡하던 일요일 요리가 토요일로 밀려났다.**
`moveTo` 가 두 칸의 요리를 맞바꾸고 있었기 때문인데, 그건 자료 구조가
`Slot { recipe: PlanRecipe | null }` 로 **하루 한 끼만** 담게 돼 있어서
빈 칸을 안 만들려면 맞바꾸는 수밖에 없었던 것이다. 실제로는 하루에 두세 개를
해 먹을 수도, 아무것도 안 할 수도 있어야 한다.

- `Slot { date, meals: Meal[] }` 로 바꾸고 `Meal { recipe, on }` 신설
  (담기 여부를 **끼니마다** 따로 끈다)
- `moveTo(from, at, to)` 는 이제 **옮긴다** — 맞바꾸지 않는다. 옮긴다는 말은
  옮긴다는 뜻이다
- `addTo(day)` / `removeAt(day, at)` / `swapOne(day, at)` / `toggleAt(day, at)`
- 화면은 **하루 한 묶음**: 날짜 머리줄(+ `2끼` 표시) + `+ 요리 추가` 버튼,
  그 아래 끼니 카드들. 옮기고 나서 빈 날이 생기면 `이 날은 비워 뒀어요`
- `mealPlan.ts` 의 `planByDate()` → `Map<string, PlannedMeal[]>`,
  `clearPlanMeal(date, recipeId)` 추가
- 요리 캘린더 주 보기는 `만들기로 한 요리 외 N개`, 일 보기는 그 날 계획을 전부,
  `PlannedList` 는 같은 날 두 번째 끼니부터 날짜를 비워 찍는다

#### 곁다리로 잡은 버그 — 늦게 온 응답이 편집을 되돌렸다

옮겨 놓고 몇 초 있으면 **제자리로 돌아갔다.** `usablePool` 이 바뀔 때마다 무조건
다시 채우게 해 뒀는데, 같은 조건으로도 요청이 여러 번 나가서 늦게 도착한 응답이
사용자가 방금 옮긴 식단을 덮어썼다.

- `planKey`(고른 재료 + 우선 재료)를 `useRef` 로 기억하고, **조건이 그대로면서
  이미 짜 둔 게 있으면** 다시 채우지 않는다

### 타입 검사가 실제로는 안 돌고 있었다

`npx tsc -p tsconfig.app.json` 을 저장소 루트에서 돌려 왔는데
`Cannot find module ...	ypescriptin	sc` 로 **즉시 실패**하고 있었다.
출력이 비어 있으니 `grep` 은 아무것도 못 찾았고, 그걸 "통과" 로 읽었다.

- `frontend/` 에서 돌려야 한다. 실제로 돌려 보니 **21개** — 그중 4개가 이번
  변경(`planByDate` 다중화)에서 난 것이라 고쳤고, 나머지 17개는 기존 것


### AI 버튼이 안 움직인 진짜 원인 — 내가 넣은 감소-모션 규칙

네 번째 시도 만에 찾았다. 앞선 세 번은 전부 **내 브라우저에서만** 확인했는데,
거기는 "동작 최소화"가 꺼져 있어 증상이 재현되지 않았다.

`index.css` 의 전역 감소-모션 블록은 `.ai-fab-button` 과 `.ai-fab-glow` 를
**예외로 남겨** 계속 움직이게 한다. `.ai-action` 은 그 목록에 없었고, 게다가
내가 나중에 `.ai-surface, .ai-action { animation: none !important }` 을 따로
넣어 두었다. 그래서 윈도우에서 애니메이션 효과를 끈 사람에게는

| 클래스 | 감소 모션일 때 |
|---|---|
| 챗봇 FAB · 카메라 버튼 (`.ai-fab-button`) | 계속 움직임 ✓ |
| AI로 짜기 · 이번 주 식단 짜기 (`.ai-action`) | **완전히 멈춤** ✗ |

정확히 "동그란 버튼만 예쁘게 되고 네모난 버튼은 안 된다" 는 증상이 된다.

- `.ai-action` 을 전역 규칙의 예외 목록에 추가하고, FAB 과 **같은 값**으로
  `!important` 재선언 (`ai-fab-shimmer` + `::after` 의 `ai-fab-sheen`)
- 이동(scale·translate)은 빼고 **빛만** 움직인다 — 움직임을 줄여 달라는 요청의
  뜻은 화면이 들썩이지 않는 것이지 아무것도 안 변하는 게 아니다
- 크레딧이 없어 비활성인 버튼은 그대로 멈춘다 (`:not(:disabled)`)
- 검증: 감소-모션 규칙을 주입한 상태에서 고치기 전 `animation: none` / 빛줄기
  `display: none` → 고친 뒤 `ai-fab-shimmer infinite` / `block` 확인

### 식단 반영 — 말없이 덮어쓰던 것을 묻는다

며칠에 걸쳐 고쳐 둔 계획이 버튼 한 번에 사라지는데, 사라졌다는 사실조차
화면에 안 나왔다.

- `mealPlan.ts` 에 `conflictingDates()` 와 `savePlan(meals, mode)` 추가
  (`overwrite` / `fill`)
- 겹치는 날이 있을 때만 창을 띄우고 **새로 짠 것으로 바꾸기 / 비어 있는 날에만
  넣기 / 그만두기** 중에 고르게 한다. 안 겹치면 예전처럼 바로 담는다
- 확인: 9/5 를 손으로 정해 둔 상태에서 `비어 있는 날에만` → 9/5 는 그대로,
  나머지 6일만 채워지고 토스트도 "6끼를 담았어요"

### 어드민 운영 탭 — 자동 작업이 무슨 일을 하는지

표에 이름·시각만 있어서 `CookMatch-DailyLLMIngredients 성공` 이 무슨 뜻인지,
실패했을 때 큰일인지 아닌지 알 수가 없었다.

- **무슨 일을 하나** 열 신설 — 하는 일과 **안 돌면 무슨 일이 생기나**를 함께 적음
- 작업 이름 밑에 실행 시각(`매일 03:00`)을 붙이고, 셋이 어떻게 물려 있는지
  카드 머리말에 설명 (크롤러 07:00 → 다음 날 새벽 AI 재료 추출 03:00 →
  사전 파일 반영 04:30)
- 결과 코드 해석 추가 — `267014` 시간 제한 중단, `3221225786` Ctrl+C·창 닫힘,
  `267009` 실행 중, `267011` 아직 안 돎

### 배치가 죽던 두 건

- **크롤러** `267014` — `ExecutionTimeLimit = PT6H` 에 걸려 `used_ingredients`
  배치 직전에 잘렸다 → `PT10H`
- **LLM 재처리** `3221225786`(`0xC000013A`) — 09-04 03:00 에 시작해 90/5280 건에서
  `^C`. 사용자가 새벽에 콘솔 창을 닫은 것이 원인
- 세 작업 모두 **창 숨김(Hidden)** 으로 바꿔 실수로 닫을 창 자체를 없애고,
  **15분 간격 2회 재시도**를 걸었다. 중단돼도 다음 회차가 남은 것부터
  이어서 처리하므로 기록은 사라지지 않는다


### 가로로 긴 AI 버튼에 FAB 값을 그대로 베낀 게 문제였다

세 번 고쳐도 안 되던 이유. FAB 는 **56px 원**이고 이 버튼들은 **가로로 긴 네모**다.
같은 `linear-gradient(120deg …)` 를 `260% 260%` 로 늘리면

1. 대각선 축이 짧은 변을 가로질러 **굵은 띠**로 갈라진다 — 진한 쪽 `#F2C200` 이
   "노란색이 엄청 진하다 / 레이어가 두 개 같다" 로 보인다
2. `background-position` 은 좌우로 흐르는데 그라데이션 축은 대각선이라
   **정작 색이 거의 안 바뀐다**

- `.ai-action` 을 모양에 맞춤: 축을 거의 수평(`100deg`), 세로는 안 늘리고
  가로만 3배(`300% 100%`), 밝은 쪽에서 시작해 어두운 띠가 안 생기게
- 빛줄기도 납작한 네모에 맞춰 조정 (`width 45%→38%`, 불투명도 `.75→.95`)

(브라우저 창이 애니메이션을 스로틀해서 `currentTime` 이 FAB 까지 0 으로 멈춰
있었다 — 이 환경에서는 진행도를 못 잰다. 모양 계산으로 원인을 좁혔다.)

### 로그인 전 요리 캘린더 — 식단 버튼이 탭에 가려 안 보였다

`min-h-screen` + `flex-1` 로 로그인 안내가 화면을 꽉 채우고, 그 아래 있던
`이번 주 식단 짜기` 는 하단 탭(84px)에 가린 채 **스크롤도 안 되는** 자리에 있었다.
로그인 없이 쓸 수 있는 유일한 기능인데 도달이 불가능했다.

- 식단 블록을 **로그인 안내 위로** 이동, 안내 쪽에 `paddingBottom: 100`

### 화면 문구 축약 (전반)

| 화면 | 전 → 후 |
|---|---|
| 식구 그룹 | 78자 → **"식구끼리 냉장고를 함께 씁니다. 즐겨찾기·완료·기록도 원하면 공유할 수 있어요."** |
| 요리 캘린더 목표 | 83자 → **"매월 1일에 0%로 돌아가요. 그룹 공용 목표라 누가 바꿔도 모두에게 적용돼요."** |
| 매칭률 도움말 | 64자 → **"레시피 본문에서 뽑아낸 재료와 내 냉장고를 비교한 값이에요. 글쓴이가 안 적은 재료는 빠질 수 있어요."** |
| 유통기한 도움말 | 60자 → **"구매일만 넣은 재료도 기한을 짐작해 함께 계산해요. 짐작한 값은 약 D-00 으로 보여요."** |
| 냉장고 입력 안내 | 62자 → **"가진 재료를 넣어 두면 더 잘 맞는 레시피를 골라 드려요."** |
| 공유 요청 팝업 | 58자 → **"…공유해 달라고 해요. 수락하면 그룹원이 볼 수 있어요."** |
| 구매일자 안내 | 58자 → **"구매일만 넣어도 기한을 짐작해 드려요. 실제 표기와는 다를 수 있어요."** |
| 내냉장고 온보딩 3개 | 58/35/29자 → 각각 절반 이하 |

어드민 화면은 그대로 뒀다 — 만든 사람이 **숫자를 보고 판단**하는 자리라,
"왜 이 값인지" 를 적어 두는 게 길이보다 중요하다.


### AI 버튼이 안 반짝인 진짜 이유 — 애니메이션은 돌고 있었다

세 번 지적받고서야 원인을 쟀다. 브라우저에서 재 보니 **FAB 와 완전히 같은
애니메이션이 같은 속도로 돌고 있었다** (`ai-fab-shimmer` 4.2s + `ai-fab-breathe`
2.8s + `::after` 의 `ai-fab-sheen`, 둘 다 `running`).

안 보인 이유는 **노란 버튼이 노란 패널 위에 있었기 때문**이다. FAB 가 반짝여
보이는 건 흰 화면 위 노란 원이라서다. 거기에 후광(`ai-glow`)과 바깥 그림자까지
겹쳐 버튼 둘레가 번지니, 표면을 스치는 빛줄기가 배경에 묻혔다.

- `.ai-surface` 를 **중립 카드**로 (노란 그라디언트 + 자체 shimmer/sheen 제거).
  이 자리가 AI 임은 배지와 버튼이 말한다
- `.ai-action` 의 **바깥 그림자 제거** — 안쪽 하이라이트만 남긴다
- **후광 `.ai-glow` 를 통째로 삭제** (식단 화면·요리 캘린더 두 곳 모두).
  CSS 규칙도 지웠다 — 쓰는 데가 없다
- 중복 정의돼 있던 `.ai-surface` 두 벌을 한 벌로 (한쪽만 고치면 서로 달라진다)

### 영역 구분에서 배지를 걷어냄

번호(①②③)를 달았다가 아이콘 배지로 바꿨는데, **둘 다 이 화면만 다른 화면과
다르게 보이게** 만들었다. 영역을 나누는 건 제목과 카드 경계가 이미 하는 일이다.

- `SectionHead` 를 앱의 보통 제목(`h2` 15px/700)으로 — 장보기 목록과 같은 생김새
- 아이콘 3개(`IngredientsGlyph`/`AiGlyph`/`PlanGlyph`) 삭제

### 워딩 축약

| 전 | 후 |
|---|---|
| 이 재료로 짜요 | **쓸 재료** |
| 냉장고 재료 30개 중 29개를 써요 · 14일 넘게 지난 3개는 빼 뒀어요 | **냉장고 30개 중 29개 · 오래된 3개는 뺐어요** |
| 먼저 쓸 재료 · 5일 이내 | **곧 상해요 · 먼저 씁니다** |
| 그 밖의 재료 · 기한이 남았거나 안 적은 것 — 모두 식단에 써요 | **아직 괜찮아요** |
| 14일 넘게 지났어요 · 기본으로 뺐어요 — 쓰실 거면 누르세요 | **오래됐어요 · 빼 뒀어요** |
| 원하는 대로 짜 드려요 | **조건을 넣어 짜기** |
| AI 식단 짜기 | **AI로 짜기** |
| 이번 주 식단 · 내일부터 7일 / 바꾸기·다시 짜기는 크레딧을 쓰지 않아요 — 마음에 들 때까지 누르세요 | **이번 주 식단** / 내일부터 7일 · 바꾸기는 크레딧을 안 써요 |
| 누르면 이번 주 식단에서 빠져요. 냉장고에서 지워지는 건 아니에요. | **눌러서 빼거나 다시 넣으세요.** |

### 요리 캘린더 — 계획 표시

- 기울인 타원("찌그러진 동그라미") → 날짜 뒤에 깔리는 **정원**(`#FFF0A8`)
- 범례 견본도 같은 모양으로, 문구는 `요리 계획 있는 날` 한 줄


### 식단 카드 그리드 다시 — 두 기둥으로

- 왼쪽 한 기둥에 **날짜 + 썸네일**, 오른쪽에 제목·조리 순서·바꾸기.
  전에는 버튼 줄에만 `paddingLeft: 70` 을 줘서 왼쪽에 큰 빈 자리가 생겼다.
  이제 왼쪽 폭이 썸네일(84px)로 정해져 빈 자리가 안 생긴다
  (확인: 카드 x=14 / 날짜 x=27 / 썸네일 x=27 — 같은 기둥)
- 날짜 `<select>` 를 **직접 그리는 고르개(`DayPicker`)로 교체**.
  안드로이드 웹뷰에서 네이티브 폼 컨트롤이 고정된 하단 탭 위로 그려져서,
  페이지를 내리면 날짜 칸이 GNB 를 덮었다. z-index 로는 못 이긴다 —
  네이티브 위젯이라 우리 쌓임 맥락 밖이다 (지금은 우리 목록, `--z-dropdown` 300)

### 반영 알림 · 도움말

- 초록 버튼(`✓ 요리 캘린더에 반영했어요`)을 **먹색 알약 토스트**로 교체.
  버튼 색을 바꿔 두면 그 색이 계속 남아 "뭔가 켜져 있다" 처럼 읽혔다.
  `요리 캘린더에 7끼를 담았어요 [보기]` 가 아래에서 올라왔다 3.6초 뒤 사라진다
  (`.plan-toast`, `prefers-reduced-motion` 대응)
- 도움말 `체크를 풀면 반영·장보기 목록에서 함께 빠져요. 요리를 누르면 조리
  순서가 나옵니다.` → **`체크를 끄면 그 날은 빼고 담아요.`**

### 요리 캘린더 — 계획 목록을 달력 안으로

같은 내용이 위에는 줄 목록, 아래에는 달력으로 두 번 있었다. 목록을 읽고 나면
정작 달력은 화면 밖으로 밀렸다.

- 로그인 화면의 `PlannedList` 제거 — 계획은 달력 안에서 보여 준다
  (로그인 전 화면에는 달력이 없으므로 거기서는 목록을 그대로 둠)
- **월 보기**: 테두리만 있던 6px 점 → 날짜 위에 비뚤게(-13°) 찍히는
  **노란 도장**. 점 하나로는 무슨 뜻인지 알 수가 없었다
- **범례 추가**: `도장이 찍힌 날은 만들기로 한 요리가 있어요 · 누르면 무엇인지
  보여요`. 인원별 색 범례와는 무관한 표시라 설명이 없으면 계속 "이 노란
  동그라미는 뭐지" 로 남는다
- **주 보기**: 계획한 날은 `계획 · 제목` 한 줄이 아니라 **썸네일 + 제목 카드**로,
  테두리는 **점선**(아직 안 한 것) — 완료 기록의 실선과 눈으로 바로 갈린다


### 이번 주 식단 추천 — 세 영역으로 나누고, 재료를 직접 고르게

한 화면에 성격이 다른 세 가지가 제목 없이 쌓여 있어서, 스크롤하는 사람에게는
그냥 카드 세 장이었다. 정작 결과인 식단 카드는 두 영역 밑에 깔려 안 보였고,
그래서 이 화면이 "이번 주 식단 추천" 이라는 게 인지되지 않았다.

- `WeeklyPlan.tsx`: **① 이 재료로 짰어요 · ② 원하는 대로 짜 드려요(AI) ·
  ③ 이번 주 식단** 으로 번호를 붙인 영역 제목(`SectionHead`) 신설
- ① 을 접을 수 있게 하고(기본 접힘), ② 의 설명 두 줄을 힌트 한 줄로 줄이고,
  따로 떠 있던 크레딧 안내 상자를 ③ 제목 밑줄로 흡수 —
  식단 카드 시작점이 **431px** 로 올라와 첫 화면에서 바로 보인다

#### 기준이 모호했던 것을 고침 — 실제로는 안내와 다르게 돌고 있었다

"14일 넘게 지난 재료는 식단에 쓰지 않았어요" 라고 적어 놓고, **매칭에는 그 재료를
그대로 넣고 있었다.** `my_ingredients` 는 냉장고 전체를 보냈고 오래된 재료는
`applied_expiry_ingredients`(우선순위)에서만 빠졌다. 152일 지난 소고기가 안 쓰인다고
적힌 화면에서 그 소고기로 요리를 고르고 있었던 셈이다.

- `planIngredients` 하나로 **매칭과 우선순위를 같은 목록에서** 정하도록 통일
- ① 에 재료 칩 토글 추가 — 누르면 이번 주 식단에서 빠진다
  - `곧 상해요`(5일 이내) / `그 밖의 재료` / `14일 넘게 지났어요` 세 묶음
  - 오래 지난 것은 **기본 꺼짐**이지만 목록에 남겨 둬서, "지났지만 이번 주에
    쓸 건데" 인 경우 눌러서 도로 넣을 수 있다
- 뺀 재료가 **들어간 요리는 후보에서 실제로 제거**(`usablePool`). 질의에서 이름만
  빼면 매칭률만 낮아질 뿐이라, "감자" 를 뺐는데 감자채전·감자채볶음이 그대로
  남아 있었다 (확인: 감자 요리 2개 → 0개)
- 후보 수 40 → 60 (걸러 내고도 7칸을 채우기 위해)
- 재료를 너무 많이 빼서 만들 게 없어진 경우를 따로 안내하고, 버튼이 ① 을 열어 줌

### 어드민 — 고른 기간이 안 걸리던 카드들

기간을 좁혀도 아래쪽 숫자가 꿈쩍 않아서, 필터가 안 먹은 것처럼 보였다.

- **직접 지정에 `[적용하기]` 버튼** 신설. 전에는 날짜를 고칠 때마다 바로 질의해서,
  시작일만 고른 순간 원한 적 없는 중간 결과로 한 번 조회됐다. 미적용 상태에는
  "아직 적용 안 됐어요" 를 띄운다
- **한눈에** 타일 6개가 누적·이번 주·30일로 제각각이었다 → 기간을 골랐으면
  전부 그 기간을 센다 (탈퇴만 `deleted_at` 기준 — 가입한 때로 세면 "이 기간에
  몇 명이 떠났나" 를 못 본다)
- **퍼널**(가입·재료 등록·레시피 반응·AI 사용·식구 그룹)이 전체 기간 누적이었다 →
  **그 기간에 가입한 사람**을 따라가는 코호트로 변경.
  확인: 가입 전체 9명 → 최근 7일 3명, 퍼널 2 → 1 → 0 → 0 → 1
- 쿠팡 클릭에 종료일 조건 누락돼 있던 것 보완

### 어드민 사전 탭 — 취소선 줄을 치울 수 있게

그 사이 사전이 보강돼 이제는 잡히는 이름은 취소선이 그어지는데, 선택 자체를
막아 둬서 손댈 방법이 아예 없었다. 다 끝난 일인데 목록에 영영 남아 매번 눈에 걸렸다.

- `이제 잡히는 것 N개 치우기` 버튼 신설 (`DELETE /api/admin/dictionary/misses`)
- 설명 줄에 취소선의 뜻과 처리 방법을 명시

### 크롤러가 스스로 죽던 것 — 6시간 실행 제한

`CookMatch-WeeklyCrawler` 의 마지막 결과가 `267014`(`0x41306`,
SCHED_S_TASK_TERMINATED)였다.

- 09-03 **07:00:01 시작 → 13:09:55 종료**, 설정값 `ExecutionTimeLimit = PT6H`
- 크롤링은 정상 완료했고 `used_ingredients 배치 처리 시작` 직후에 잘렸다 —
  배치가 **한 번도 못 돈 날**이었다 (09-02 까지는 12:53 에 끝나 안 걸렸음)
- 시간을 다 쓴 건 `네이버(인플루언서핫토픽)` — 07:59 → 13:09 로 혼자 5시간 10분
- **`PT6H` → `PT10H`** 로 상향


### 앱 배포 착수 — 안드로이드 프로젝트 생성 (Capacitor)

`capacitor.config.ts` 와 안내 문서만 있고 **시작은 안 되어 있었다.** 패키지도
안 깔려 있었고 `android/` 도 없었다.

- `@capacitor/core · cli · android · app · splash-screen · status-bar` 설치
- `npx cap add android` — `frontend/android/` 생성 (appId `com.cookmatch.app`,
  앱 이름 `쿡매치`)
- 아이콘·스플래시 100장 생성. 원본 `cookmatch_icon.png` 가 1024x1022 라
  `@capacitor/assets` 가 요구하는 정사각(1024x1024)으로 맞춰 `frontend/assets/`
  에 원본을 두고 거기서 뽑는다
- `npm run app:sync` (빌드 + 동기화), `npm run app:open` (안드로이드 스튜디오)

앱에서만 깨질 것 두 가지를 미리 막았다. 둘 다 **웹에서는 멀쩡하고 앱에서만**
틀어지는 종류라 나중에 찾으면 원인을 짚기 어렵다.

- **CORS**: 앱은 화면을 기기 안에서 띄우므로 웹 주소가 없다. 안드로이드는
  `https://localhost`, iOS 는 `capacitor://localhost` 라는 고정 출처를 쓴다.
  허용 목록에 없으면 **앱의 모든 API 호출이 preflight 에서 막힌다**
- **서비스워커**: 앱은 화면 파일을 이미 들고 있어 캐시가 필요 없다. 그 위에
  또 캐시를 얹으면 **앱을 새 버전으로 갱신해도 옛 화면이 계속 뜬다.**
  스토어 심사를 통과한 새 버전이 사용자에게 반영 안 되는 상태가 된다.
  `isNativeApp()` 으로 앱에서는 등록을 건너뛴다
- `gradlew` 는 유닉스 셸 스크립트라 CRLF 로 바뀌면 실행이 깨진다.
  `.gitattributes` 에 `eol=lf` 를 박았다

아직 빌드는 못 한다 (이 컴퓨터 기준):

- **JDK 17 필요** — 지금 11 이다. Android Gradle Plugin 8.x 가 17 을 요구한다
- **Android SDK 필요** — 안드로이드 스튜디오를 깔면 같이 온다
- **iOS 는 이 컴퓨터에서 불가능** — 맥이 필요하다. 안드로이드부터 간다

### 확인해 둔 것 — 쿠팡 링크는 대부분 이미 걸려 있다

`coupang_link` 가 2,948행 중 1행만 차 있어서 "링크가 없다" 고 생각했는데,
`resolveCoupangUrl` 에 **3단 폴백**이 있고 3단계가 파트너스 검색 딥링크다.
배포본(`refrigerator-code.vercel.app`)을 직접 받아 확인한 결과 파트너 ID 가
번들에 들어 있다 — **비어 있어도 모든 재료에 링크가 나가고 있다.**

정말 손볼 것은 `coupang_ads.csv` 194행인데, 이미 레시피 등장 횟수로 정렬되고
A/B/C 우선순위가 매겨져 있다(1위 다진마늘 11,678회). 상위 20~30개만 실제
상품으로 고정하면 되고, 나머지는 검색 딥링크가 낫다 — 고정 링크는 품절·단종
때문에 유지보수가 계속 든다.

### 스크린샷에서 찾은 버그 두 개

**1. 파일 반영이 승인보다 먼저 찍히던 문제 (시간대)**

화면에 `승인 2026-09-02 20:42 · 파일 반영 2026-09-02 19:30` 이 떴다.
반영이 승인보다 1시간 12분 **먼저**다.

Railway MySQL 서버 시계는 UTC 다. 그래서 `backend/app.py` 의 `get_db()` 는
연결할 때 `SET time_zone = '+09:00'` 으로 세션을 KST 에 고정해 둔다. 그런데
`scripts/` 의 스크립트들은 **각자 연결을 따로 만들면서 이걸 안 했다.**

결과: 앱이 쓴 `created_at`(승인)은 KST, 배치가 쓴 `applied_at`(반영)은 UTC.
같은 표의 두 열이 9시간 어긋나 앞뒤가 뒤집혔다.

- `pymysql.connect` 를 쓰는 `scripts/*.py` **11개 전부**에 KST 고정을 넣었다
- 이미 잘못 찍힌 2행은 +9시간으로 보정했다
  (`04:30` — 배치가 실제로 돈 시각과 맞는다)

**2. "사전에 없던 이름" 에 응답 덩어리가 통째로 쌓이던 문제**

`{'purchase_date': '2026-04-01', 'ingredients': [{'name': ...` 이 재료 이름
하나로 저장되어 있었다. 3건, 모두 영수증 모드, 모두 255자에서 잘림.

`_parse_response` 가 배열 응답을 **한 가지로만** 봤다. 배열은 두 가지로 온다:

- 옛 형식 — 이름만: `["물엿", "대파"]`
- **사진마다 객체 하나씩**: `[{"purchase_date":…, "ingredients":[…]}, …]`

뒤엣것이 오면 원소를 통째로 `str()` 해서 이름으로 썼다. 그래서 응답 전체가
재료 이름 하나가 됐다(작은따옴표인 게 그 증거 — 파이썬 dict 를 문자열로 만든 것).

- 배열 안에 `ingredients`/`purchase_date` 를 가진 객체가 있으면 **합쳐서** 읽는다
- 이름을 만드는 규칙을 `_items_from()` 한 곳으로 모았다. 갈래마다 따로 만들다
  생긴 문제라, 갈래를 늘리는 대신 규칙을 하나로 뒀다
- 들어오는 자리에도 방어막: 40자를 넘거나 `{ } [ ] "` 가 섞인 것은 미매칭으로
  기록하지 않는다. 한 번 저장되면 사람이 하나씩 지워야 한다
- 이미 쌓인 3건은 지웠다 (21 → 18건)

### 레시피 본문에서 버려진 이름도 어드민 '사전' 탭으로

LLM 은 레시피 본문에서 재료를 자유롭게 뽑고(사전을 주지 않는다), 그 뒤에 사전으로
정규화한다. **사전에 없으면 버렸는데**, 그 목록이 미리보기 CSV 에만 남았다.
로컬 파일이라 화면에서는 안 보였고, 그래서 "사전에 없던 이름" 에는 사진에서 읽힌
18건만 떴다. 정작 물량은 레시피 쪽이다.

- `ingredient_dictionary_misses` 에 `recipe_hits` 칸 추가. 사진에서 3번 읽힌 것과
  본문 1,799건에서 나온 것은 **무게가 전혀 다르다** — 한 칸에 더하면 못 가린다
- 매일 도는 배치가 스스로 쌓는다. 건마다 쓰지 않고 세어 뒀다 끝에 한 번에 보낸다
- 지나간 45,334건은 `scripts/backfill_recipe_misses.py` 로 옮겼다
  (**14,415종 / 85,956회**. 다시 돌려도 부풀지 않도록 더하지 않고 덮어쓴다)

이제 표에 14,418건이 있다. 상위권은 쓰레기가 아니라 **진짜 재료**다 —
물 15,100 · 밥 2,157 · 물엿 1,799 · 깨 1,082 · 생수 1,022 · 기름 813.
`물엿` 은 사진 인식 프롬프트의 예시로 쓰는 재료인데 1,799번 버려지고 있었다.
`고추가루`/`후추가루` 는 맞춤법 변형이니 동의어로 넣으면 되고,
`다진 생강`/`다진생강` 은 접두어 제거 폴백이 있는데도 안 잡히고 있다.

### 사전 후보를 알약에서 표로

알약으로 늘어놓으면 **몇 번 나온 것인지가 안 읽힌다.** 숫자가 이름 뒤에 붙어 줄이
안 맞고, 15,100번짜리와 1번짜리가 같은 크기로 나란히 있어 무엇부터 볼지 판단이
안 된다. 후보가 18건일 땐 견뎠지만 14,418건이면 못 쓴다.

- 표로 바꾸고 **많이 나온 순**으로 정렬. 숫자 옆에 막대를 둬서 크기를 눈으로 잰다
- `어디서` 칸에 출처를 나눠 적는다 (레시피 1,799 · 사진 6)
- 고르기는 체크박스 + 줄 전체 클릭

### 사진 읽는 동안 멈춘 것처럼 보이던 문제

AI 호출은 사진 한 장에 5~10초, 여러 장이면 십몇 초 걸린다. 그동안
`사진에서 재료를 찾고 있어요...` 글자 한 줄만 떠 있어서 **멈춘 것처럼 보였다.**

기다림 자체는 못 줄이니, 무슨 일이 일어나는 중인지 보이게 했다.

- **훑는 애니메이션** — 사진 모양 틀 위로 빛줄기가 위아래로 지나간다
- **바뀌는 단계 문구** — 사진을 보내는 중 → 글자와 재료를 훑어보는 중 →
  재료 이름을 골라내는 중 → 사전과 맞춰 보는 중. 14초가 지나면 "거의 다 됐어요"
- **결과와 같은 모양의 스켈레톤** — 곧 무엇이 나올지 미리 알려 주고, 실제 결과로
  바뀔 때 화면이 덜 튄다
- 사진이 여러 장이면 `사진 3장을 함께 읽고 있어요` 라고 장수를 말해 준다

가짜 진행바는 쓰지 않았다. 남은 시간을 모르면서 아는 척하면 90% 에서 멈춰 있는
순간 오히려 더 고장 난 것처럼 보인다. 단계 문구는 **경과 시간** 기준이고, 서버가
중간 상태를 알려주지 않으므로 그 이상은 정직하게 말하지 않는다.

`prefers-reduced-motion` 에서는 위아래로 움직이는 대신 밝기만 변한다.

서비스워커 캐시 v1.4.7 → v1.4.8.

### 레시피가 아닌 글 걸러내기 — LLM 이 본문을 보고 판정한다

레시피 카드에 보관법·제품 후기·맛집 방문기·광고 글이 섞여 나왔다.

제목 규칙으로는 못 가린다. 실측해 보니 제목만으로 의심되는 글이 2,509건(5.6%)
인데 그중에 진짜 레시피가 섞여 있다 —
`김진순 김치 비빔국수 레시피 식당 검증된 황금 양념장` 은 '식당' 이 들어 있지만
멀쩡한 레시피다. 제목으로 지우면 이런 글이 같이 날아간다.

그래서 **본문을 이미 읽고 있는 LLM 에게 같이 물어본다.** 호출은 안 늘어난다.

- 프롬프트에 판정 규칙 추가 — 요리 글이 아니면 재료 대신 `"NOT_RECIPE"`
  - 아닌 예: 재료 보관법·손질법, 제품 후기·광고·공구, 맛집·카페 방문기,
    효능·칼로리 글, 식당 메뉴 소개, 일상 브이로그
  - **본문에 실제로 만드는 과정이 있으면** 맛집 얘기가 섞여도 요리 글로 본다
  - 애매하면 요리 글로 본다 — 지우는 쪽이 되돌리기 어렵다
- 빈 배열로 두지 않고 별도 표식을 쓴 이유: 빈 배열은 **"레시피인데 재료를 못
  찾았다"** 와 구분이 안 된다. 앞엣것은 지워야 하고 뒤엣것은 남겨야 한다
- 판정된 글은 사전 정규화에 넣지 않는다. 넣으면 표식이 "사전에 없는 이름" 으로 쌓인다

### 재료가 하나뿐인 레시피 삭제

재료가 하나로 나온 글은 레시피가 아니라 **그 재료에 대한 글**이다. 표본:
`새송이버섯 보관법`, `밤보관방법`, `아보카도 오일 활용법`, `CU 신상 후기`.

진짜 레시피인데 추출이 실패해 하나만 남은 것도 섞이지만 그래도 지운다 —
**재료가 하나면 어차피 매칭에 못 쓰인다**(재료 3개 이하는 추천에서 빠진다).
남겨 두면 자리만 차지하고 매일 배치가 계속 다시 훑는다.

- 이미 쌓인 **281건 삭제** (`scripts/delete_single_ingredient_recipes.py`,
  지우기 전 CSV 백업)
- 앞으로는 배치가 자동으로. 2개는 안 건드린다 — `계란 + 소금` 처럼 진짜 간단한
  레시피가 섞여 애매하다

### 누적 44,868건 재판정 예약

`scripts/requeue_recipes_for_llm.py --write` 로 `llm_ingredients_done` 을 되돌렸다.
**나머지는 기존 배치가 알아서 한다** — 매일 새벽 3시에 무료 한도만큼 처리하고,
요리 글이 아니면 그 자리에서 지운다. 약 8~9일 걸린다. 그동안 앱은 정상 동작한다.

지워진 글은 그날 미리보기 CSV 에 `NOT_RECIPE` / `DELETED_1ING` 라벨과 제목·링크가
남으므로 나중에 확인할 수 있다.

### 무료 한도를 주 100 → 30 으로

**실제로 요리하는 사람의 한 주**에서 거꾸로 세어 보니:

| | 빈도 | 크레딧 |
|---|---|---|
| 요리하며 챗봇에 묻기 | 주 3~4회 × 2~3번 | 9~12 |
| 장 보고 영수증 찍기 | 주 1회 × 1~2장 | 2~4 |
| **보통 사용자** | | **11~16 / 주** |

주 100 은 그 **6~9배**다. 아무도 한도를 느끼지 못했고, 한도를 못 느끼면
**유료로 올릴 명분 자체가 없다.**

지금 줄이는 또 다른 이유 — 사람들이 100 에 익숙해진 뒤에 줄이면 **쓰던 걸
뺏는 것**이 된다. 아직 익숙해진 사용자가 없을 때 정해 두는 편이 낫다.

| 구분 | 주 한도 | 일 상한 | |
|---|---|---|---|
| 비회원 | 15 → **10** | 10 → **6** | 맛보기 |
| 무료 | 100 → **30** | 40 → **15** | 보통 사용자의 2배 |
| 플러스 | 400 → **150** | 100 → **50** | 무료의 5배 |

400 은 과했다 — **아무도 도달 못 하는 한도는 없는 한도와 같다.** 유료를 사도
뭐가 좋아졌는지 체감이 안 된다. 일 상한은 주 한도의 절반이다(주 초에 몰아 쓰고
남은 6일을 못 쓰는 상태를 막는다).

전부 환경변수로 덮을 수 있다(`QUOTA_FREE_WEEKLY` 등). 실제 사용량이 쌓이면
대시보드의 주별 크레딧을 보고 다시 조정한다 — 감이 아니라 그 값을 보고.

### 조리 순서를 본문에서 뽑아 저장한다

**요리하는 중에 원문 링크를 여는 건 사실상 못 할 짓이다.** 손에 물이 묻어 있고,
블로그 글은 위아래로 한참 스크롤해야 하고, 중간에 광고와 잡담이 섞여 있다.

그래서 재료만 뽑던 것을 **재료 + 조리 순서**로 바꿨다.

- 같은 LLM 호출에서 함께 받는다. **호출 수는 안 늘어난다** — 이미 본문을 읽고 있다
- `recipes.cook_steps` 에 줄바꿈으로 이어 붙여 저장. 별도 표로 빼면 조인이 늘고,
  단계는 항상 그 레시피와 통째로만 쓰이므로 나눌 이유가 없다
- **원문을 그대로 베끼지 않고 요약해 다시 쓰게 했다.** 남의 블로그 글이고, 소리로
  읽어 줄 것이라 군더더기가 없어야 한다. 다만 불·시간·양처럼 결과를 좌우하는
  수치는 반드시 남긴다
- 최대 12단계, 한 단계 120자

**지금 넣은 이유**: 어제 예약한 44,868건 재처리가 이미 도는 중이라 **한 번에 같이
끝난다.** 나중에 넣었으면 45,000건을 또 돌려야 했다.

응답이 길어져 한 번에 묶는 본문 수를 12 → 8 로 줄였다(하루 물량 5,280 → 3,520).

실측 결과:

```
[1288] 달걀로 만두를 어떻게 만들지??
  1. 당면이 잠길 정도의 물을 넣고 8분간 삶는다
  2. 청양고추, 당근, 대파를 잘게 다진다
  ...
  7. 팬에 식용유를 두르고 중약불에서 달걀 반죽을 둥글게 부친다
  8. 가장자리가 익으면 반으로 접어 반달 모양으로 만든다
```

### 응답 모양을 하나로 통일

값이 세 가지로 온다 — `"NOT_RECIPE"` / 옛 배열 `["김치"]` / 지금 객체
`{"ingredients": [...], "steps": [...]}`. `_as_result()` 하나에서 전부 받아
`{ingredients, steps, not_recipe}` 로 통일한다.

옛 배열을 계속 받는 이유: 프롬프트를 바꿔도 모델이 가끔 예전처럼 답한다. 그때
통째로 실패시키면 **그 묶음 8건이 다 날아간다.**

### 고친 것

- `record_recipe_misses` 가 `_connect_db` 를 못 찾아 사전 후보 기록이 매번
  조용히 실패하고 있었다(`사전 후보 기록 실패(무시)`). 이 함수는 모듈 최상단에
  없고 `_batch_helpers()` 안에 감춰져 있다 — 무거운 import 사슬을 늦게 끌어오기
  위해서다. 같은 길로 가져오도록 고쳤다
- `recipes` 에 칸을 더할 때 표를 다시 쓰다가 `post_time = '0000-00-00'` 인 옛 행에서
  막혔다. 그 연결에서만 엄격 모드를 풀어 통과시켰다
  (FULLTEXT 인덱스가 있어 `ALGORITHM=INSTANT` 는 못 쓴다)

### 조리 순서에서 분량이 날아가던 문제 — 27% → 95%

한 건만 보고는 몰랐다. 원문의 수치 표현을 전부 긁어 세는 도구를 만들어
15건을 대조하니 패턴이 뚜렷했다.

```
                    고치기 전        고친 후
개수                   5%     →      85%
분량(무게·부피)         14%     →     100%
길이·두께              22%     →     100%
분량(계량도구)          42%     →      98%
불세기                57%     →     100%
시간                  83%     →      83%
온도                 100%     →     100%
──────────────────────────────────────────
전체                  27%     →      95%
```

**시간·온도는 지키면서 분량만 버리는** 편향이었다. 원인은 내가 쓴 프롬프트다 —
"불·시간·양처럼 결과를 좌우하는 수치는 반드시 남긴다" 라고 쓰면서 예시를
`"중불에서 5분"` **하나만** 들었다. 모델이 그 예시를 좁게 해석해 시간·불세기만
수치로 봤다.

가장 나빴던 예: `보쌈무김치` 는 원문 수치 20개 중 **1개**만 남았다. 절임 비율이
사라진 김치 레시피는 아무 쓸모가 없다.

고친 것:

- **종류별로 예시를 다 박았다** — 분량 / 시간·불세기 / 크기·두께 / 온도·개수
- **틀린 예를 같이 보여준다** —
  `× "고추장, 식초, 설탕을 섞는다" — 이러면 요리를 못 한다`
- **`ingredients_detail` 칸 신설** — `["고추장 1과 1/2큰술", "양조식초 2큰술"]`
  처럼 분량이 붙은 재료 목록을 따로 받는다. `used_ingredients` 는 사전 대표어라
  분량이 없다. 단계에서 놓쳐도 여기서 건진다
- **"문장만 짧게 다듬고 정보는 줄이지 마라"** — 요약 압력을 문장 길이에만 걸고
  내용에는 안 걸리게
- 한 단계 길이 120 → 200자, 최대 12 → 14단계

남은 5% 는 대부분 `1인분` `2인분` 같은 **레시피 전체의 속성**이라 단계에 안 들어가는
게 맞다. 실제 손실은 시간 2건뿐이다.

### 품질 측정 도구를 남겼다

`scripts/check_cook_steps_quality.py` — 원문에서 `숫자+단위` 를 전부 긁어
단계·재료상세에 살아남았는지 세고, 종류별 보존율과 놓친 값의 예시를 뽑는다.

**프롬프트를 만질 때마다 전후를 숫자로 비교할 수 있다.** 한두 건을 눈으로 보고
"괜찮은 것 같다" 로 넘어가면 이번 같은 편향을 못 잡는다.

### 매주 채워 주던 한도 → 잔액(충전) 모델

**다 쓸 일이 없으면 부족함을 못 느끼고, 그러면 결제할 이유가 영영 안 생긴다.**
매주 30개가 그냥 새로 생기는 구조가 그랬다. 잔액은 실제로 **소진된다** — 부족한
그 순간이 오고, 그때가 팔기 가장 좋은 순간이다.

| | 전 | 후 |
|---|---|---|
| 방식 | 매주 한도 리셋 | **잔액** (받은 것 − 쓴 것) |
| 가입 | — | **30 크레딧** (한 번) |
| 매주 | 30 리셋 | **5 크레딧 추가** |
| 부족하면 | 월요일까지 대기 | 충전 (결제는 나중) |
| 비회원 | 주 10 | **AI 사용 불가** |

매주 5개라도 주는 이유: **잔액이 0이면 앱을 아예 안 열게 된다.** 앱을 안 열면
결제도 안 한다. 소액이라도 계속 흐르게 두고, 모자라면 충전하게 만든다.

- `credit_grants` — 지급 원장. `UNIQUE (user_id, reason, period_key)` 가 중복
  지급을 막는다. 이게 없으면 화면을 새로고침할 때마다 주간 지급이 쌓인다
- 주간 지급은 배치 없이 **화면을 열 때** 채운다. 안 쓰는 사람에게까지 매주
  지급 행을 쌓을 이유가 없다
- 일 상한(free 15 / plus 50)은 남긴다. 잔액이 있어도 하루에 몰아 쓰면
  "어제 다 써서 오늘 못 쓴다" 가 되는데, 그 경험이 한도 자체보다 더 나쁘다
- 어드민은 이제 한도를 **설정**하지 않고 크레딧을 **지급**한다. 숫자를 덮어쓰게
  두면 실수로 남의 잔액을 깎을 수 있어서 보태기만 되게 했다

### 비회원은 AI 를 못 쓴다

전에는 비회원도 주 10 크레딧을 줬다. 지금은 0이다 — 가입 유인을 크레딧 자체에
걸었기 때문이다. 서버에서 막고(`QuotaDenied("guest")`), 화면에서도 **누르기 전에**
알려 준다. 누르고 나서 실패하는 것보다 낫다.

### 탈퇴 후 재가입 · 이메일 바꿔 재가입 막기

`credit_identity_claims` — 가입 크레딧을 **한 사람에게 한 번만** 주기 위한 지문표.

- `p:{provider}:{provider_id}` — 소셜은 이게 제일 단단하다. 이메일을 바꿔도
  provider_id 는 그대로다
- `e:{sha256(정규화된 이메일)}` — **점·플러스 별칭을 정규화**한다.
  `920803hj+abc@gmail.com` 과 `920803hj@gmail.com` 은 같은 사람이다
- `d:{device_id}` — 같은 기기에서 계정을 바꿔 가며 가입하는 경우

실측: 같은 소셜 계정 재가입 **차단**, 이메일에 `+별칭` 붙여 재가입 **차단**.

완벽하게는 못 막는다 — 앱을 지우고 다른 이메일로 가입하면 뚫린다. 목표는
"쉽게는 못 하게" 이고, 무료분이 30개라 그 수고를 들일 사람은 적다.

기존 회원 6명 중 5명에게 가입분을 소급 지급했다(1명은 이미 받은 지문에 걸림).

서비스워커 캐시 v1.4.8 → v1.4.9.

### 일별 크레딧 막대그래프에 숫자가 없던 문제

막대만 그리고 값은 `title`(마우스 올림)로만 보여 줬다. **폰에는 마우스가 없어서
숫자를 볼 방법이 아예 없었다.** "몇인지를 모르겠다" 는 지적이 정확했다.

- **세로 눈금 + 막대 위 숫자**를 둘 다 그린다. 어느 쪽으로도 읽힌다.
  노란 막대는 흰 배경과 대비가 약해서 숫자가 없으면 크기를 눈으로만 가늠해야 한다
- 눈금 간격은 1·2·5·10… 중에서 고른다 (`0 / 13 / 25` 같은 눈금이 안 나오게)
- **기록이 있는 날만 그리던 것을 오늘까지 연속 14일로** 바꿨다. 있는 날만 이으면
  뜸했던 기간이 사라져 활발했던 것처럼 보인다. 0인 날은 숫자를 안 적는다
- 기간 합계를 머리말에 적는다. 막대를 눈으로 더할 필요가 없다
- 손가락·마우스 어느 쪽으로도 짚으면 그날 값이 뜬다

서비스워커 캐시 v1.4.9 → v1.5.0.

### 요리 모드 — 원문으로 나가지 않고 앱 안에서 (6·7·8)

레시피 카드를 누르면 원문이 새 창으로 열렸다. 그런데 **요리하는 중에 블로그
원문을 보는 건 사실상 못 할 짓이다** — 손에 물이 묻어 있고, 위아래로 한참
스크롤해야 하고, 중간에 광고와 잡담이 섞여 있다.

- `GET /api/recipes/<id>/cook` — 재료(분량 포함)와 조리 순서만.
  **목록 API 에 안 싣는 이유**: 조리 순서는 한 건에 수백 자다. 목록에 실으면
  20건만 불러도 응답이 몇 배가 되는데 대부분은 열어 보지도 않는다. 열 때 가져온다
- `CookModeSheet` — 재료 목록 + **번호 붙은 조리 순서**. 냉장고에 있는 재료는
  체크 표시로 구분한다
- 원문 링크 버튼을 항상 크게 둔다. **요약은 요약이다** — 사진이 필요하거나 더
  자세히 보고 싶으면 원문으로 갈 수 있어야 한다
- 출처를 함께 적는다

**소리로 읽어 주기** — 손이 젖어 있으면 화면을 못 만진다.

- 브라우저 내장 음성(`speechSynthesis`). API 비용이 없고 앱에서도 그대로 된다
- 단계마다 발화를 따로 만들고 `onend` 로 잇는다. 전체를 한 덩어리로 넘기면
  **지금 어느 단계인지 알 수 없어** 화면에서 짚어 줄 수가 없다
- 읽고 있는 단계를 노랗게 짚어 준다. 소리만 나오면 한눈 판 사이 어디까지 왔는지
  모르고 처음부터 다시 들어야 한다
- 단계를 누르면 거기서부터. 멈출 때까지 이어서 읽는다
- 시트를 닫거나 원문으로 나가면 반드시 멈춘다 (안 그러면 계속 떠든다)

**조리 순서가 없는 글** — 유튜브처럼 영상에서만 설명하는 경우다. 없는 걸
지어내지 않고 "본문에 만드는 과정이 안 적혀 있어요" 라고 말한 뒤 영상 링크로 보낸다.

시트는 앱에 **하나만** 둔다(`CookModeHost`). 레시피 카드는 목록·인기·내냉장고
등 여러 화면에 흩어져 있어서, 화면마다 시트를 두면 같은 코드를 여섯 군데에 적게
된다. 카드는 `openCookMode()` 로 말만 한다.

곁들여: 레시피를 열면 `recipe_open` 이벤트를 남긴다. 어떤 레시피가 실제로
열리는지 알아야 추천을 고칠 수 있다.

> **지금은 조리 순서가 15건에만 있다.** 나머지 44,964건은 재처리가 도는 8~9일에
> 걸쳐 채워진다. 그동안 대부분의 레시피는 재료와 원문 링크만 보인다.

### 크레딧 그래프에도 일자별·월별·년도별

화면 추이 그래프에만 있던 단위 선택을 크레딧 그래프에도 넣었다. 두 그래프는
**따로** 고른다 — 한 상태로 묶으면 한쪽을 월별로 보려다 다른 쪽까지 바뀌어
비교하던 걸 놓친다.

`usage_daily` 를 30일에서 400일로 넓혔다. 30일치만 주면 월별은 한두 칸,
연도별은 한 칸밖에 안 그려진다.

서비스워커 캐시 v1.5.0 → v1.5.1.

### 유통기한 알림 · 이번 주 식단 · 장보기 목록 (9·10·11)

**유통기한 알림** — 유통기한은 원래도 재료마다 들고 있었지만 재료를 하나씩
눌러 봐야 보였다. **찾아 들어가야 보이는 정보는 없는 것과 같다.**

- 내냉장고를 열면 곧 상하는 재료가 맨 위에 뜬다 (기본 3일 이내 + 이미 지난 것)
- 적힌 유통기한이 있으면 그걸 쓰고, 없으면 구매일+재료 종류로 짐작한다.
  짐작값에는 **`약`을 붙인다** — 정직하게 말하지 않으면 멀쩡한 재료를 버리게 된다
- 알림 권한을 주면 **하루 한 번** 알린다. 열 때마다 알리면 금방 꺼 버리고,
  한 번 꺼지면 정작 필요할 때 못 알린다
- 앱을 안 켜도 오는 진짜 예약 알림은 설치형 앱의 로컬 알림으로 붙인다.
  지금은 **확실히 되는 것만** 한다

**이번 주 식단 + 장보기 목록** (`/plan`) — 한 화면에 둔다. 장보기 목록은
식단에서 **나온다.** 무엇을 만들지 정해야 무엇이 부족한지 나온다. 두 화면으로
나누면 사용자가 같은 걸 두 번 정하게 된다.

- **곧 상하는 재료를 쓰는 레시피를 앞으로** 올린다. "뭐 해 먹지" 에 답하는 앱은
  많지만, **가진 재료 중 곧 상하는 것**을 아는 건 이 앱만 할 수 있다
- 매칭률만 보고 위에서부터 자르면 **비슷한 요리가 줄줄이 나온다**(김치찌개,
  김치볶음밥, 김치전…). 이미 뽑은 요리와 재료가 절반 넘게 겹치면 건너뛴다
- 요일을 붙여 5개. 체크를 풀면 장보기 목록에서도 빠진다
- 장보기 목록은 **여러 요리에 쓰이는 것부터**. 쿠팡 링크는 기존 3단 폴백을 쓴다
- 식단 입구는 내냉장고에 둔다. 하단 네비에 탭을 더 다는 대신 — 식단은
  **재료를 넣은 다음**에 의미가 생기는 기능이라 재료 화면에서 이어지는 게 맞다

### 고친 것 — 타입체크가 아무것도 검사하지 않고 있었다

`npx tsc --noEmit` 이 계속 통과했는데, 루트 `tsconfig.json` 이
`{"files": [], "references": [...]}` 라 **검사 대상이 0개였다.** 그래서
`<Sheet isOpen=...>` (실제 prop 은 `open`) 같은 오류가 그대로 지나갔고,
시트가 열리지 않는 채로 커밋될 뻔했다.

앞으로는 `npx tsc --noEmit -p tsconfig.app.json` 으로 확인한다.
돌려 보니 **기존 파일에 18건**이 쌓여 있다(이번 작업과 무관한 것들, 별도로 정리 필요).

실측 확인: 내냉장고에서 `우유 1일 지났어요 / 양파 2일 남음` 배너, `/plan` 에서
식단 3개 + 장보기 4개, 요리 모드에서 분량·냉장고 재료 체크(✓)·번호 8단계·
읽어 주기 버튼까지 모두 동작.

서비스워커 캐시 v1.5.1 → v1.5.2.

### 자리 바로잡기 — 무엇이 이 화면의 주인공인가

**내냉장고**: 유통기한 알림과 식단 버튼을 입력창 **위**에 뒀더니, 이 화면에서
가장 먼저 하는 일(재료 추가)이 아래로 밀리고 알림이 주인공처럼 보였다.

- 순서를 `재료 추가 → 입력창 → 유통기한 알림 → 재고 관리` 로 바로잡았다.
  **알림은 거들 뿐이고 주인공은 입력창이다**
- `이번 주 식단 짜기` 버튼은 내냉장고에서 뺐다

**요리 캘린더**로 식단 입구를 옮겼다. 캘린더는 "무엇을 언제 먹었나/먹을까" 를
보는 화면이고 식단은 그 앞날을 정하는 일이다. 내냉장고에 두면 **재료를 넣으러
온 사람 앞을 가로막는다.**

- 로그인 안 한 화면에도 같은 버튼을 둔다. 식단은 냉장고 재료(로컬)만 있으면
  되는 기능이라 **로그인 벽 뒤에 가둘 이유가 없다.** 로그인이 필요한 건
  캘린더(내 요리 이력)뿐이다

**어드민 크레딧 그래프**: `기간` 줄과 단위 선택(일자별/월별/년도별)을 한 줄에
붙였다. 아래위로 나누면 두 줄을 잡아먹는데, 둘은 "지금 무엇을 보고 있나" 하나를
말하는 정보다.

서비스워커 캐시 v1.5.2 → v1.5.3.

### 유통기한 알림도 요리 캘린더로 — 내냉장고에선 같은 말을 두 번 하고 있었다

내냉장고 재고 목록은 **이미 재료마다 `D-10` `지남` `D-2` 를 달고 있다.** 그 위에
"유통기한이 지난 재료 1개" 배너를 또 얹으면 같은 말을 두 번 하는 것이고,
정작 이 화면에서 할 일(재료 추가)만 밀려난다.

배너의 값어치는 **냉장고를 안 보고 있을 때** 알려 주는 데 있다. 그건 다른
화면에서 할 일이다.

- 곧 상하는 재료 + 이번 주 식단을 **한 묶음**으로 요리 캘린더에 뒀다.
  둘은 하나의 이야기다 — "이게 곧 상해요 → 그럼 이걸로 식단을 짜요".
  떨어뜨려 놓으면 알림은 잔소리로만 남고, 식단은 왜 지금 짜야 하는지 이유가 없다
- 로그인 벽 **앞**에도 둔다. 둘 다 냉장고 재료(로컬)만 있으면 되는 기능이고,
  로그인이 필요한 건 캘린더(내 요리 이력)뿐이다

서비스워커 캐시 v1.5.3 → v1.5.4.

### 식단 화면 — 뒤로가기가 헤더에 깔려 있었다

`padding-top` 을 16으로 뒀는데 앱 상단에 **고정 헤더가 72px** 있다. 뒤로가기
버튼이 그 밑에 깔려 안 보였다. 다른 화면들이 쓰는 값(72)으로 맞췄고, 버튼도
글자 `‹` 대신 아이콘 + **44px 터치 영역**으로 바꿨다.

### 기다림의 생김새를 한 곳으로 (`StepLoading`)

사진 인식에서 쓰던 로딩 화면(훑는 애니메이션 + 바뀌는 단계 문구 + 결과 모양
스켈레톤)을 공용 부품으로 뺐다. **같은 앱 안에서 기다림은 항상 같은 모양이어야
하고, 두 벌로 두면 한쪽만 고쳐져 서로 달라진다.**

식단 화면도 `짜는 중이에요...` 한 줄에서 같은 화면으로 바꿨다 —
`냉장고 재료를 살펴보는 중 → 곧 상하는 재료를 먼저 챙기는 중 → 만들 수 있는
요리를 고르는 중 → 비슷한 요리를 걸러내는 중`.

### 식단은 AI 크레딧을 쓰지 않는다 — 그렇다고 적었다

"이것도 LLM 붙인 거 아니냐" 는 물음에 코드를 확인했다. `/api/recipes/filter` 에
`consume()` 호출이 **0건**이고 LLM 호출도 없다. 레시피마다 **미리 뽑아 둔 재료**를
SQL 로 맞춰 보는 것뿐이다.

그런데 화면에 아무 말이 없으면 "AI 가 짜 주는 것" 으로 보이고, 그러면 크레딧이
닳는 줄 알고 아껴 쓰게 된다. **안 쓴다는 사실을 적어 두는 것도 정보다.**

### 당겨서 새로고침 — 되는 건지 알 수가 없었다

화살표는 돌고 있었지만 두 가지가 없었다.

- **글자가 없었다.** 아이콘 하나에 "당기라는 건지 / 되고 있다는 건지 / 끝났다는
  건지" 세 가지 뜻을 다 실을 수는 없다.
  `당겨서 새로고침 → 놓으면 새로고침돼요 → 새로고침 중... → 새로고침 완료`
- **너무 빨리 끝났다.** 요청이 100ms 만에 끝나면 화살표가 한 바퀴 돌기도 전에
  사라져 아무 일도 안 일어난 것처럼 보인다. **최소 500ms** 는 보여 주고,
  끝나면 초록 체크로 **완료를 0.9초** 알린다

### 어드민 사람별 활동에 가입일

마지막 활동만 보면 **어제 가입해서 어제 쓴 사람**과 **반년 전에 가입해 계속
쓰는 사람**이 똑같아 보인다. 가입일이 있어야 "언제 들어온 사람이 아직 쓰고 있나"
를 읽을 수 있다.

서비스워커 캐시 v1.5.4 → v1.5.5.

### 식단을 **고칠 수 있게** — 그리고 AI 식단(유료 전환 구멍)

식단이 "추천 하나 던지고 끝" 이었다. 마음에 안 들어도 할 수 있는 게 없었다.

- **뒤로가기를 공용 `BackButton` 으로.** 이 화면만 다르게 그려 놨었다
- **내일부터 7일.** 오늘을 넣으면 이미 저녁이 지난 경우가 많아 첫 칸이 버려진다.
  수요일에 짜면 **목~수**가 된다
- **요일 바꾸기** — 드롭다운에서 다른 날을 고르면 그 날 요리와 **맞바뀐다.**
  밀어내지 않고 자리를 바꾸므로 빈 칸이 안 생긴다
- **다른 요리로 바꾸기** (칸별) / **↻ 다시 짜기** (전체). 둘 다 무료
- 체크를 푼 날은 장보기 목록에서 빠진다

**AI 식단** (`POST /api/plan/suggest`, 크레딧 `plan` = 2)

규칙으로는 **"담백하게", "아이가 먹을 것 위주로", "국물은 빼고"** 같은 말을 받을
수 없다. 사람이 식단을 짤 때 실제로 하는 생각이 그건데.

- **후보는 우리가 고르고 LLM 은 배치만 한다.** 레시피를 지어내게 두면 우리 DB 에
  없는 요리를 추천하게 되고, 그러면 조리 순서도 쿠팡 링크도 못 붙인다
- 날마다 **왜 이걸 골랐는지** 한 줄이 붙는다 ("곧 상하는 양파를 먼저 씁니다")
- 크레딧을 **먼저** 깎는다. 호출해 놓고 나중에 깎으면 한도를 넘겨 쓰게 된다
- `temperature 0.7` — 매번 똑같으면 "다시 짜기" 가 의미가 없다

**왜 이게 유료 전환 구멍인가**: 재료 매칭은 한 번 써 보고 마는 기능이지만,
**주마다 식단을 다시 짜는 건 계속 돌아오는 일**이다. 무료 기능(다시 짜기·바꾸기)과
유료 기능(내 말을 반영한 식단)의 차이가 화면에서 바로 보이게 적어 뒀다.

실측: 로그인 사용자로 `"국물 요리는 빼고 담백하게"` 요청 → 7일치가 나오고
잔액 35 → 33 (정확히 2 차감). 비회원은 `로그인하면 AI 기능을 쓸 수 있어요` 로 막힘.

서비스워커 캐시 v1.5.5 → v1.5.6.

### AI 식단이 후보를 **첫 재료 하나로만** 좁히고 있었다

"AI 식단이 어떻게 도는 거냐" 는 물음에 코드를 다시 읽다가 찾았다.

```sql
WHERE used_ingredients <> ''
  AND FIND_IN_SET(%s, ...) > 0     -- have[0] 하나만 묶여 있었다
```

재료를 20개 넣어도 **첫 번째 재료가 든 레시피만** 후보가 됐다. `HAVING hit > 0`
으로 바꿔 **하나라도 겹치는 레시피**를 다 후보로 삼는다.

### 비회원에게 체험 크레딧 5

전에는 비회원에게 AI 를 아예 안 줬다. 그런데 **"로그인하면 쓸 수 있어요" 만으로는
가입할 이유가 안 된다** — 무엇이 좋은지 본 적이 없으니까.

5는 우연한 숫자가 아니라 **세 기능을 한 번씩 써 볼 수 있는 양**이다:
사진 인식 2 + 식단 2 + 챗봇 1.

- 기기당 한 번. 다시 채워지지 않는다 (`credit_grants` 는 회원용이라, 비회원은
  정해진 체험분에서 쓴 만큼 빼는 방식)
- 하루 상한 3 — 한 번에 다 태우지 않게. 사진 한 번(2) + 챗봇 한 번(1)이 그날
  몫이고, 다음 날 식단을 써 볼 여지가 남는다
- 다 쓰면 **"체험 5회를 다 쓰셨어요. 가입하면 30개를 바로 드려요."**
  "로그인하세요" 가 아니라 **얼마를 주는지**로 말한다. 체험을 다 쓴 그 순간이
  가입 의사가 가장 높은 때다
- 앱을 지우면 기기 식별자가 새로 생겨 다시 받는다. 막을 수 없고, 5개 때문에
  그 수고를 할 사람도 적다

실측: 새 기기로 잔액 5 → AI 식단 7일치 성공 → 잔액 3 → 하루 상한에 걸려
`오늘 체험할 수 있는 만큼(3)을 다 쓰셨어요. 가입하면 30개를 바로 드려요.`

서비스워커 캐시 v1.5.6 → v1.5.7.

### 식단을 짜고 **끝나지 않게** — 캘린더에 남긴다

`[이번 주 식단 계획 반영하기]` 를 누르면 계획이 요리 캘린더에 남는다.
짜고 끝나면 아무 데도 안 남아서, 다음 날 "뭐 해 먹기로 했더라" 를 다시 물어야
했다. 그러면 식단을 짠 의미가 없다.

- 계획은 **기기에 저장한다**(`cookmatch_meal_plan`). 서버에 표를 하나 더 만들
  무게가 아니고, 무엇보다 **비회원도 식단을 짤 수 있어야** 하는데 서버에 두면
  로그인 벽 뒤로 들어간다
- **완료 기록과 섞지 않는다.** 캘린더의 기존 항목은 실제로 만든 것이고 계획은
  아직 아니다. 같은 목록에 넣으면 "만들었다" 는 기록이 오염된다
  - 월 보기: 계획은 **테두리만 있는 점**, 완료는 채워진 점
  - 주 보기: `계획 · 감자전 레시피`
  - 일 보기: `오늘 만들기로 한 요리` 카드 (눌러서 조리 순서)
- 로그인 벽 **앞에도** 계획 목록을 둔다. 안 그러면 비회원이 반영해 놓고 볼 곳이 없다
- 지난 날짜는 저장할 때 함께 지운다. 안 지우면 지난달 계획이 쌓여 어지럽다
- 식단을 고치면(다시 짜기·바꾸기·요일 옮기기·AI) **반영 표시가 풀린다.**
  안 맞는 상태를 "반영했다" 고 말하면 안 된다

### 식단 화면 다듬기

- 제목을 **이번 주 식단 추천**으로. 추천이라는 걸 이름에서 알 수 있어야 한다
- **썸네일**을 카드에 넣었다
- **`조리 순서 보기 ›`** 를 붙였다. 제목만 있으면 **눌러도 되는지 알 수가 없다** —
  실제로 "제목이 클릭되는 건지 모르겠다" 는 말을 들었다
- AI 카드에 **반짝임**(`.ai-surface`)과 **AI 배지**를 넣었다. 챗봇 FAB·카메라
  버튼과 같은 시각 언어다 — 같은 앱에서 "AI 가 하는 일" 은 항상 같게 보여야 한다
- 남은 사용량을 **공용 `UsageLine`** 으로 바꿨다. 화면마다 다르게 적으면
  사용자가 매번 다시 읽어야 한다

`.ai-surface` / `.ai-action` 을 공용 CSS 로 뺐다. FAB 은 동그란 떠 있는 버튼이라
규격을 그대로 못 쓰고, 네모난 카드·버튼용이 따로 필요했다.
움직임 최소화 설정에서는 반짝임을 멈춘다.

서비스워커 캐시 v1.5.7 → v1.5.8.

### 썸네일이 안 뜨던 진짜 이유 두 가지

**하나 — 핫링크 차단(403).** 네이버 이미지를 그대로 부르면 막힌다. 앱에는 이미
`getProxiedImageUrl` 이 있는데 안 거쳤다. `onError` 로 조용히 숨겨져서
"썸네일이 아예 안 나온다" 로만 보였다. 식단 카드와 캘린더 양쪽에 적용했다.

**둘 — 7칸 중 3칸이 비어 있었다.** 겹치는 요리를 거르는 규칙(`pickDistinct`)만
돌리니 후보가 적을 때 7일을 못 채웠다. 이제 **1차로 겹침을 피해 고르고, 모자라면
2차로 남은 후보를 채운다.** 비슷한 요리가 하루 더 들어오는 게, 그 날 칸이 텅
비어 있는 것보다 낫다.

썸네일이 아예 없는 레시피도 있다. `src=""` 로 두면 브라우저가 **페이지 자체를
다시 요청**하므로 자리표시자를 그린다 — 크기를 맞춰 둬야 카드마다 글자 시작점이
안 어긋난다.

실측: 7칸 · 썸네일 7개 · 빈 칸 0.

### 반짝임이 "명암만 있고 지나가지 않던" 이유

`ai-fab-sheen` 은 **56px 짜리 동그란 FAB 용**이라 이동폭(±130%)이 자기 너비
기준이다. 폭이 넓은 카드·버튼에 그대로 쓰면 가운데서만 깜빡이고 끝까지 지나가지
않는다. 넓은 자리를 가로지르는 `ai-sweep` 을 따로 뒀고, 버튼에는 FAB 처럼
아주 살짝 숨쉬는 움직임(`ai-fab-breathe`)도 얹었다 — 노란 버튼 위 노란 스침은
대비가 낮아 스침만으로는 안 보인다.

### 그 밖

- 버튼 이름을 **`AI 식단 짜기`** 로. `AI로 짜기 2` 는 2가 뭔지 알 수 없었다.
  숫자는 아래 안내 줄에서 말한다
- 버튼 우상단에 **AI 배지**. 배지는 버튼 **밖**에 둬야 한다 — 안에 넣으면
  `overflow: hidden` 에 잘린다
- 크레딧 안내를 **식단 목록 위로** 옮겼다. 아래에 뒀더니 버튼을 다 눌러 본
  다음에야 읽게 됐다. 문구도 **왜** 안 쓰는지로 바꿨다 —
  `AI 없이 냉장고 재료만 맞춰 보는 것이라, 마음에 들 때까지 눌러도 됩니다`

서비스워커 캐시 v1.5.8 → v1.5.9.

### AI 버튼이 "안 돈다" 던 것 — 돌고 있었는데 안 보였다

브라우저에서 직접 재 보니 세 애니메이션이 전부 `running` 이었다. 그러니까
문제는 "안 도는 것" 이 아니라 **너무 약해서 안 보이는 것**이었다.

숫자로 보니 이유가 분명했다:

```
스침    3.2초 중 1.8초가 정지 상태  (0~45% 만 이동, 나머지는 대기)
빛줄기  24px / 버튼 94px           → 버튼 폭의 26%
숨쉬기  4.5%                        → 94px 버튼에서 약 4px
```

흘깃 보면 아무 일도 안 일어나는 것처럼 보인다.

**챗봇 FAB 이 눈에 띄는 건 스침이 아니라 후광(`ai-fab-glow`) 때문이다.**
버튼에는 그게 없었다. 같은 것을 네모용으로 만들어 붙였다.

- `.ai-glow` — 버튼 뒤에서 숨쉬는 노란 후광 (2.4초 주기)
- 스침을 굵고 자주: 폭 26% → 38%, 주기 3.2초 → 2.4초,
  정지 구간 55% → 28%
- 숨쉬기 주기 3.4초 → 2.4초

후광은 버튼을 감싸는 자리에 건다 — 버튼 안에 넣으면 `overflow: hidden` 에 잘린다.

실측: 600ms 동안 애니메이션이 정확히 600ms 진행. 390×844(폰 크기)에서
챗봇 FAB 과 겹치지 않음.

> 그래도 안 보이면 **기기의 "동작 줄이기"**(iOS 손쉬운 사용 / 안드로이드
> 접근성)가 켜져 있는지 봐야 한다. 켜져 있으면 이 앱은 일부러 반짝임을 멈춘다.

서비스워커 캐시 v1.5.9 → v1.6.0.

### AI 반짝임을 챗봇 FAB 과 **같은 값**으로 통일

"그냥 통일해 주면 되는 거 아니야?" 가 맞았다. 폭이 넓으니 이동폭도 키워야 한다고
보고 따로 만들었는데, **그게 오히려 스침을 흐리게 했다** — 정지 구간이 55% 로
길어져 흘깃 보면 아무 일도 안 일어나 보였다.

`ai-fab-sheen` 의 이동폭(±130%)은 **빛줄기 자신의 너비** 기준이라, 너비를 45% 로
두면 어떤 폭에서든 끝에서 끝까지 지나간다. 따로 만들 이유가 없었다.

- `ai-sweep` / `ai-halo` 를 버리고 FAB 의 `ai-fab-sheen` / `ai-fab-pulse` /
  `ai-fab-shimmer` / `ai-fab-breathe` 를 그대로 쓴다
- 실측으로 확인: 버튼과 FAB 의 키프레임·주기가 완전히 같다
  (`ai-fab-shimmer, ai-fab-breathe` 4.2s / 2.8s, `::after` `ai-fab-sheen` 4.2s)
- 같은 것을 두 벌로 두면 한쪽만 손보게 되고 결국 서로 달라진다

### AI 배지가 세로로 늘어나던 문제

`.ai-glow > * { position: relative }` 가 **절대배치된 자식(배지)까지 덮어썼다.**
배지가 flex 항목이 되면서 `inline-flex` 의 기본 `align-items: stretch` 에
세로로 늘어났다. 버튼은 `.ai-action` 이 이미 `position: relative` 라
`z-index` 만 주면 된다. (24×17 로 복구 확인)

### 식단 카드 그리드 정리

`다른 요리로 바꾸기` 버튼만 카드 맨 아래 왼쪽에 혼자 떨어져 있어서, **날짜 칸
밑에 붙은 것처럼 보이고 줄이 어긋났다.**

- 썸네일·제목·행동 버튼을 **가운데 한 칸 안에** 모았다. 한 칸에 넣으면 왼쪽
  끝이 저절로 맞는다
- `조리 순서 보기 › | 다른 요리로 바꾸기` 를 **한 줄에** 나란히. 하나만 아래
  왼쪽에 혼자 있으면 카드가 어긋나 보인다
- 날짜 칸 너비를 92px 로 고정 — 요일마다 글자 폭이 달라 칸이 들썩였다

실측: 7개 카드 전부 `조리 순서 보기` 201px, `다른 요리로 바꾸기` 305px 로 정렬.

서비스워커 캐시 v1.6.0 → v1.6.1.

### 유통기한 기준을 못 박았다 — 152일 지난 재료로 식단을 짜고 있었다

기준이 어디에도 안 적혀 있어서 `소고기 152일 지났어요` 가 식단의 최우선 재료로
쓰이고 있었다. 그 결과 `코스트코 고기 할인`, `매실청 담그기` 같은 글이 식단에
올라왔다. `frontend/src/utils/expiry.ts` 한 곳에 기준을 뒀다.

| 상태 | 조건 | 쓰임 |
|---|---|---|
| **곧 상해요** | 남은 날 5일 이내 ~ 지난 지 14일 이내 | 알림 · 식단 우선순위 |
| **정리하세요** | 지난 지 14일 넘음 | 안내만. **식단에 안 씀** |
| (표시 안 함) | 6일 이상 남음 | — |

**왜 5일**: 장은 보통 주 1회 본다. 5일이면 "이번 주에 써야 하는 것" 과 같다.
더 길게 잡으면 냉장고 절반이 목록에 올라와 정작 급한 게 묻힌다.

**왜 14일**: 두 주 넘게 지난 재료는 냉장고에 있는 게 아니라 **앱에서 안 지운
것**일 가능성이 훨씬 높다. 그렇다고 조용히 숨기면 그 줄은 목록에 영영 남는다 —
식단 기준에서만 빼고 **"정리하세요" 로 따로 말하고, 내냉장고로 보낸다.**

알림에서도 뺀다. 152일 지난 재료를 매일 알리면 **알림 자체를 꺼 버린다.**

곁들여 명확히 한 것:

- 유통기한이 **하나도 없어도 식단은 정상으로 짜인다** — 냉장고 재료 전체로
  매칭한다. 다만 무엇을 먼저 쓸지는 못 정하므로 그렇게 말해 준다
  (`냉장고 재료 12개로 만들 수 있는 것들이에요`)
- 화면에 `(5일 이내에 먹어야 하는 것)` 이라고 기준을 적었다.
  숫자를 안 적으면 왜 이건 뜨고 저건 안 뜨는지 알 수 없다

### 문서 — `MEAL_PLAN_FEATURE.md`

새 기능들이 `CHANGELOG.md` 에만 있었다. 그건 **"언제 무엇을 왜 바꿨나" 의
기록**이지 **"지금 어떻게 동작하나" 의 설명**이 아니다.

유통기한 기준 · 식단 추천(무료/AI) · 장보기 목록 · 요리 모드 · 조리 순서가
어디서 오는지를 한 곳에 적었다. `PROJECT_OVERVIEW.md` 와 `README.md` 에서
연결했다.

서비스워커 캐시 v1.6.1 → v1.6.2.

### 대시보드 — 기간 고르개와 "내 활동 빼고 보기"

**하나. 카드마다 기간이 제각각이었다.**

누적 / 이번 주 / 최근 30일 / 기록을 켠 뒤부터 — 카드마다 기간이 다르면 숫자를
나란히 놓고 비교할 수가 없다. 화면 위에 고르개를 하나 두고 **모든 카드가 같은
기간을 본다.**

`전체 · 오늘 · 최근 3일 · 최근 7일 · 최근 30일 · 직접 지정(달력)`

기간을 바꾸면 `dashboard` 와 `activity` 를 **함께** 다시 부른다. 하나만 갱신하면
같은 화면에 서로 다른 기간의 숫자가 섞인다.

**둘. 만든 사람 본인 계정이 통계를 통째로 덮고 있었다.**

계정이 6개다(나혜진 / 카카오사용자 / 네이버nhj / 쿡매치 / NHJ카카오 / NHJ,
탈퇴 포함). 실측하면 이렇다:

```
제외 전   방문 43 · 사람 6 · 가입 6
제외 후   방문  7 · 사람 5 · 가입 2
```

**데이터는 지우지 않는다.** 지우면 되돌릴 수 없고, 나중에 "그때 그 세션이
뭐였지" 를 못 묻는다. `users.exclude_from_stats` 표시만 해 두고 **볼 때 뺀다.**

- 사용자 탭 → 자세히 → **대시보드 집계** 에서 계정마다 켜고 끈다
- 화면의 `내 활동 빼고 보기` 체크를 풀면 표시를 그대로 둔 채 다 보인다
  (`?keep_excluded=1`)
- **기기까지 뺀다.** 로그인 전에 남은 기록은 `user_id` 가 없어서, 계정만 빼면
  그 사람이 비회원으로 돌아다닌 기록이 그대로 남는다

조건을 질의마다 손으로 이어 붙이면 한 군데를 빠뜨리게 되고, 그러면 어떤 카드만
필터가 안 걸려 **같은 화면의 숫자끼리 앞뒤가 안 맞는다.** `_stats_range()`,
`_excluded()`, `_where()` 세 도우미로 조각을 만들어 20개 질의에 같은 것을 넣었다.

서비스워커 캐시 v1.6.2 → v1.6.3.

### 사전 반영을 푸시까지 자동으로 — 승인은 이미 어드민에서 끝났다

새벽 배치가 CSV 만 고쳐 두고 멈췄고, 사람이 아침에 확인해 커밋·푸시했다.
그런데 **승인은 이미 어드민 "사전" 탭에서 끝난 것**이다. 관리자가 반영을 누른
순간이 승인이고, 그 뒤에 또 확인하라고 하면 같은 판단을 두 번 하게 된다.
실제로 승인해 둔 항목이 며칠씩 저장소에 안 올라간 채 남아 있었다.

대신 **자동으로 올려도 안전하도록** 앞에 검증을 뒀다:

- 사전이 정상으로 읽히는지 확인하고, **깨졌으면 되돌리고 푸시하지 않는다**
- 커밋 대상은 **사전 CSV 두 개뿐**이다. `git add -A` 를 쓰지 않는다 —
  작업하던 다른 파일이 딸려 올라간다 (`scratch_backups/` 에는 비밀번호 해시가 있다)
- 푸시가 실패해도 커밋은 남는다. 로그에 이유를 적는다

### 곁들여 고침 — 배치가 매일 밤 조용히 실패하던 곳

로그에 이런 줄이 매일 남고 있었다:

```
can't open file '...\scriptseport_ops_status.py': [Errno 2] No such file
```

`.bat` 파일 안에 `scripts
eport_ops_status.py` 대신 **`scripts` + 캐리지리턴 +
`eport_ops_status.py`** 가 들어가 있었다. 어드민 '운영' 탭이 읽는 상태 기록이
**한 번도 갱신되지 않고 있었다.** 파일을 다시 쓰면서 고쳤고, 돌려서 확인했다
(`DB 에 기록했습니다 (ops_status)`).

### 사전에 야채·양복버섯 추가

어드민 승인분이 CSV 로 옮겨졌다 (2,946 → 2,948행, `추가일자` 열 신설).

- 야채 — 재료 › 식재료 원물 › 채소류
- 양복버섯 — 재료 › 식재료 원물 › 버섯류

### LLM 재료 추출 배치에 "요리명" 추출·정규화 추가

매일 새벽 3시 도는 `llm_ingredient_extraction.py`(재료·조리단계 추출)가 이제
그 레시피의 **요리명**도 같이 뽑는다. 블로그 제목 그대로가 아니라 본문 내용을
보고 판단한, 짧고 깔끔한 음식 이름만 뽑도록 프롬프트에 지시했다(제목이
"닭가슴살과 라면만 가지고 초계국수 맛 나는~ 완성입니다" 여도 요리명은
"초계라면"으로 나옴).

뽑은 요리명은 재료 정규화와 똑같은 방식으로 한 번 더 다듬는다 —
`ingredient_profile_dict_with_substitutes.csv`에 이미 있던 "요리이름"
분류(994행, Popular.tsx의 "인기 급상승 요리" 랭킹이 키워드+동의어로 쓰던
그 사전)에 대고 매칭해서, 사전에 있으면 대표 이름으로 통일하고 없으면 LLM이
뽑은 원문을 그대로 쓴다(사전에 없다고 지우면 화면에 보일 이름이 없어지므로).

DB에 `recipes.recipe_name` 컬럼 신설. 이 컬럼 추가 과정에서 `post_time`에
잘못된 값(`'0000-00-00'`, naver 크롤러 쪽 버그로 추정) 1,127건이 있어 테이블
재구성이 막혔던 것을 발견 — 전부 `NULL`로 정리한 뒤 컬럼을 추가했다.

#### 검증 (실측)
- 실제 배치와 같은 조건으로 5건 미리보기 실행(`--limit 5`, DB 미반영) —
  "비빔수제비라고 아시나요?" → `비빔수제비`, "달걀로 만두를 어떻게
  만들지??" → `달걀만두`, "5분 안에 만드는 초간단 비빔밥 만들기!" →
  `전자레인지 비빔밥` 등 제목의 홍보 문구/물음표가 다 빠지고 본문 기준
  요리명만 남는 것을 확인
- 사전 매칭 단위 테스트: `토마토계란볶음`/`토달볶`(동의어) → `토마토달걀볶음`
  (대표어)로 정규화, 사전에 없는 `전자레인지 비빔밥`은 원문 그대로 통과
- `python -m py_compile` 에러 0건
- 매일 새벽 3시 스케줄(`CookMatch-DailyLLMIngredients`)이 돌지 않는 시간에
  작업해 진행 중인 배치를 건드리지 않았고, `--pending-only` 재시작 방식이라
  내일 새벽 실행부터 그냥 반영됨(수동 개입 불필요)

### UI 다듬기 — AI 반짝임 통일, 캘린더 도장, 식단 화면 워딩/색

- **AI 식단 짜기 진입 버튼**(요리 캘린더의 "이번 주 식단 짜기" 카드)에
  챗봇 FAB·카메라 버튼과 같은 반짝임 애니메이션(`.ai-action`/`.ai-glow`/
  `.ai-fab-badge`) 적용 — 지금까지 이 버튼만 그 규칙에서 빠져 있었다.
- **월별 캘린더 "계획 있음" 표시**: 찌그러진 타원(30×25, -13도 기울임) →
  정원(26×26, 기울임 없음)으로. 범례 문구도 "도장이 찍힌 날은 만들기로
  한 요리가 있어요 · 누르면 무엇인지 보여요" → "요리 계획 있는 날" 한
  줄로 줄임.
- **이번 주 식단 화면(`WeeklyPlan.tsx`)**:
  - 영역 제목의 1·2·3 숫자 배지 → 영역별 아이콘(재료 장바구니/AI 반짝임/
    캘린더)으로 교체. 순서가 아니라 성격이 다른 영역을 나누는 자리라
    숫자는 오히려 "단계"처럼 오해됐다.
  - 워딩: "이 재료로 짰어요"(아직 짜지도 않았는데 과거형이라 어색함) →
    "이 재료로 짜요", "원하는 대로 짜 드려요" → "원하는 대로 짜기",
    "그 밖의 재료" → "나머지 재료"로 축약, 부연설명 문장들도 전반적으로
    줄임(예: "기본으로 뺐어요 — 쓰실 거면 누르세요" → "기본 제외").
  - 재료 칩 색을 3단계로 분리: 곧 상해서 먼저 써야 하는 것(진한 amber)
    > 보통(연한 yellow) > 너무 지나서 기본 제외된 것(pink). 전엔 "먼저
    쓸 재료"와 "나머지 재료"가 펼쳤을 때 똑같은 연한 yellow라 뭐가
    급한지 색만 보고는 구분이 안 됐다.

#### 검증 (실측)
- `npx tsc --noEmit` 에러 0건
- 로컬(`localhost:5178`)에서 `/plan`, `/cooking-calendar`(로그아웃 상태)
  화면을 직접 열어 아이콘·워딩·AI 반짝임 버튼이 실제로 반영된 것을
  스크린샷으로 확인. 로그인 세션이 없어 캘린더 그리드의 도장 모양과
  재료 우선순위 색상(유통기한 데이터 필요)까지는 라이브로 확인 못 함 —
  코드 리뷰로만 검증됨

### "AI 식단 짜기" 버튼의 반짝임이 안 보이던 진짜 원인 수정

바로 위에서 다른 버튼(요리 캘린더 진입 카드)에 반짝임을 새로 붙였는데도
"여전히 애니메이션이 안 된다"는 지적을 받고 다시 봤더니, `WeeklyPlan.tsx`의
실제 "AI 식단 짜기" 버튼은 처음부터 `.ai-action`/`.ai-glow`가 붙어 있었다 —
문제는 **크레딧이 없거나(`canAi` false) 사용량 정보가 아직 안 불러와진
순간에 버튼이 `disabled` 처리되는데, `.ai-action:disabled { animation: none;
}` 규칙이 애니메이션을 통째로 꺼버리는 것**이었다. 챗봇 FAB·카메라 버튼은
이런 상태 의존이 없어 항상 반짝이는데 이 버튼만 크레딧 상태에 따라 죽어
있었다.

`disabled`를 `asking || !canAi` → `asking`만으로 바꾸고, `ai-glow`/`AI` 배지
조건도 `canAi && !asking` → `!asking`으로 바꿔 크레딧 유무와 무관하게 항상
반짝이게 했다. 클릭 시 실제 동작은 `onClick` 안의 `if (canAi)` 가드가 그대로
막아 주므로 크레딧 없이 실행되는 일은 없다.

#### 검증 (실측)
- `npx tsc --noEmit` 에러 0건
- 로컬에서 버튼 DOM을 직접 조회해 `disabled: false`, `className: "ai-action"`,
  래퍼 `className: "ai-glow"`, AI 배지 존재를 확인

### 어드민 "사전" 탭: "넣지 않음"이 계속 되살아나던 버그 + 무한 스크롤 수정

**버그 1 — "넣지 않음"으로 고른 이름이 사전 반영 후에도 계속 다시 뜸.**
`제안 — 승인할 것만 남기세요` 화면에서 `decision`을 "넣지 않음"으로 바꾼
항목은 `사전에 반영` 버튼을 눌러도 **사전에도 안 들어가고 목록에서
지워지지도 않았다** — `applyApproved()`가 `decision !== 'skip'`인 것만
골라 `/apply`로 보내고, skip 항목은 그냥 버려서 원래 있던 자리에 그대로
남아 있었을 뿐이다(되살아난 게 아니라 애초에 안 지워짐).

**버그 2 — 별도로 "목록에서 지우기"를 눌러 지운 이름도 며칠 안에 다시 등장.**
이건 진짜로 되살아나는 문제였다: 지우기 버튼이 `ingredient_dictionary_misses`
행을 물리적으로 **DELETE**했는데, 매일 새벽 배치가 레시피 수만 건을 새로
훑다 같은 이름을 또 만나면 `record_recipe_misses`의
`INSERT ... ON DUPLICATE KEY UPDATE`가 **새 행으로 되살렸다.** 물량이
많은 흔한 이름(요리 이름·주류 브랜드 등)일수록 며칠 안에 반드시 다시
걸렸다 — "300개씩 자꾸 새로 생긴다"는 게 이 현상.

**수정**: `ingredient_dictionary_misses`에 `dismissed` 플래그 컬럼을
추가. "지우기"는 이제 물리적 DELETE 대신 `dismissed=1`만 세운다 — 같은
이름이 나중에 다시 잡혀도 `ON DUPLICATE KEY UPDATE`는 hit_count/last_seen만
건드리고 dismissed는 그대로 두므로 목록에 다시 안 뜬다. 목록 조회
쿼리에도 `WHERE dismissed=0` 추가. 그리고 `applyApproved()`가 승인 항목을
`/apply`로 보낸 **뒤에** "넣지 않음" 항목도 같이 지우기(dismiss) 처리하도록
고쳐서, 반영 버튼 한 번으로 승인/제외가 둘 다 끝나게 했다.

**버그 3 — 목록이 최대 300건까지 그대로 펼쳐져 페이지 전체가 끝없이 길어짐.**
표를 `maxHeight: 480` + 세로 스크롤로 가두고, 스크롤해도 헤더(이름/나온
횟수/어디서)가 안 사라지도록 `position: sticky`를 줬다 — 페이지 자체의
스크롤 길이는 그대로 두고 이 표 안에서만 스크롤되게 했다.

#### 검증 (실측)
- 프로덕션 DB에서 실제 재현: 더미 이름을 miss로 INSERT → dismissed=1로
  치움 → `WHERE dismissed=0` 목록에서 안 보임 확인 → 배치가 같은 이름을
  다시 만난 것과 같은 INSERT ... ON DUPLICATE KEY를 재실행 →
  dismissed가 여전히 1로 유지되고 목록에서도 계속 안 보이는 것을 확인
  (트랜잭션 롤백으로 진행, 실제 데이터는 안 남김)
- `python -m py_compile`, `npx tsc --noEmit` 에러 0건
- 어드민 로그인 세션이 없어 화면 자체는 라이브로 확인 못 함

## 2026-09-05

### AI 식단 추천 화면을 진짜 대화처럼

**① 들어올 때마다 새 대화.** 지금까지는 이력을 그대로 이어서 열었다. 그러면
지난번에 물어본 조건이 위에 남아 있어서 지금 묻는 것과 섞여 보이고, 어디까지가
이번 이야기인지 알 수 없었다. 이제 `?ai=1` 로 들어오는 순간 앞의 대화는
`지난 대화` 로 넘기고 빈 자리에서 시작한다 — 넘기지 않고 지우면 크레딧을 쓴
결과가 사라지므로, 넘긴다. (챗봇 위젯이 이미 쓰던 방식과 같아졌다.)
`지난 대화 N` 버튼은 `새 대화` 옆 헤더에 둔다.

**② 답이 오면 그 말풍선의 머리로 데려간다.** 늘 맨 아래로 보내고 있었는데,
결과가 담긴 말풍선은 길어서 정작 답의 첫 줄이 화면 위로 넘어갔다. 내가 방금
말한 직후와 고르는 중에는 맨 아래가 맞다 — 다음 것이 거기서 나오기 때문이다.
고정 헤더(56px) 밑으로 내려 붙인다.

**③ `다시 추천`·`다른 요리로` 를 뺐다.** 조건을 바꾸고 싶으면 새로 말하면
된다. 같은 일을 하는 길이 둘이면 어느 쪽이 크레딧을 쓰는지 헷갈린다.

**④ 편집을 말풍선 **안**으로.** 말풍선에 결과를 넣어 놓고 그 밑에 편집
화면을 또 뒀더니, 같은 식단이 두 번 나와서 어느 쪽이 진짜인지 알 수 없었다.
이제 살아 있는 턴의 말풍선 안에서 **날짜 옮기기와 빼기**만 할 수 있고,
`요리 캘린더에 담기` 도 거기 있다. 지난 턴(지난 대화에서 꺼내 온 것 포함)은
그때 무엇을 받았는지 **보여 주기만** 한다 — 지금 `slots` 와 다른 식단을
고치는 자리로 두면 엉뚱한 것을 옮기고 빼게 된다. 어느 턴이 살아 있는지는
`liveAt`(그 결과를 내놓은 턴의 시각)으로 가른다.
채팅 화면 아래 있던 로딩 표시와 "재료가 없어요" 안내도 걷어냈다 — 말풍선이
이미 같은 말을 한다.

**⑤ 답을 한 글자씩 내보낸다.** 다 만들어진 문단이 통째로 튀어나오면 기다린
뒤라 더 갑작스럽다. 13ms/글자로 흘려보내고 뒤에 깜빡이는 막대(`.type-caret`)를
붙였다. 결과(식단·장바구니)는 말이 끝난 뒤에 편다 — 글과 카드가 같이 자라면
눈이 둘 데가 없다. 몇 번 돌았는지가 아니라 **흐른 시간**으로 세는데,
화면을 덮어 두면 브라우저가 타이머를 초당 한 번으로 늦춰서 글자가 기어가기
때문이다. `prefers-reduced-motion` 이면 통째로 보여 준다.

### 요리 캘린더 — 절약액

- 계산식 줄(`외식·배달 대비 1인 한 끼 8,000원 × 6회 × 식구 2명`)과 값 고치는
  칩 두 개를 `자세히 보기` 안으로 접었다. 매번 읽을 문장이 아니고, 한 끼
  추정액·식구 수는 한 번 정해 두면 다시 손댈 일이 드물다. 늘 펴 두면 정작
  금액보다 이 줄이 길어서 눈이 그쪽으로 갔다.
- **목표 횟수 기준 절약액**을 더했다. 지금까지 한 것만 보여 주면 "이번 달
  6회 = 9만 6천원" 에서 끝난다. `목표 12회를 채우면 약 192,000원` 이 보여야
  남은 횟수를 채울 이유가 생긴다. 아직 한 번도 안 한 달에는 이 줄만 크게
  보여 준다 — 0원만 띄우고 마는 건 아무 말도 안 하는 것과 같다.

#### 검증
- `npx tsc --noEmit -p tsconfig.app.json` (frontend/ 에서 실행) 기존 17건 그대로,
  새로 늘어난 오류 0건 / `npm run build` 통과
- 브라우저 실측: `/plan?ai=1` 재진입 시 대화가 비고 앞의 것이 `지난 대화 1` 로
  넘어감 / `다시 추천`·`다른 요리로` 사라짐 / 지난 대화를 꺼내면 그때 받은
  장바구니·요리 목록이 읽기 전용으로 뜨고 편집 버튼·반영 버튼은 안 나옴 /
  `.type-caret` 이 붙은 채 글자가 늘어나는 것 확인 / 무료 화면(`/plan`)은
  그대로이고 AI 대화도 안 건드림
- 요리 캘린더는 로그인이 필요해 화면으로는 확인 못 함 (구조·타입만 확인)

### 어드민

- **자동 작업 세 개가 사라진 것** — 작업은 멀쩡했다. 새벽 배치가 부르는
  `report_ops_status.py` 가 `Get-ScheduledTask` 를 60초 안에 못 읽으면 오류 한
  줄을 만들어 **멀쩡한 상태 위에 덮어썼다**(LLM 배치 03:00~04:43 과 사전 배치
  04:30 이 겹치는 날이 그렇다). 이제 못 읽으면 **지난 값을 그대로 두고**
  `이번엔 못 읽어서 지난 값이에요` 라고 적는다. 대기 시간도 150초로 늘렸다.
  DB 값은 다시 기록해 복구했다.
- 대시보드의 `일자별/월별/년도별` 이 GNB 를 덮던 것 — 네이티브 `<select>` 는
  안드로이드 웹뷰에서 목록을 **화면 맨 위 레이어**에 그려서 z-index 로는 못
  이긴다. 고를 것이 셋뿐이라 세그먼트로 펼쳐 뒀다(옆의 `조회수/이탈` 과 같은 모양).

### 마이페이지

- `더 필요해요` 를 **남은 크레딧 옆**으로. 모자란 걸 아는 순간이 그 숫자를 볼
  때인데, 버튼이 카드 맨 아래라 사이에 설명이 세 줄 끼어 있었다.
- 크레딧 설명을 고쳤다. `남은 크레딧 00 / 한 번에 0` 은 오늘 것인지 이번 주
  것인지, `한 번에` 가 무슨 뜻인지 알 수 없었다 →
  `남은 크레딧은 쌓아 두는 잔액이에요. 월요일마다 N 씩 채워져요` +
  `하루에 쓸 수 있는 양은 따로 있어요 — 오늘은 N 남았어요`.
  식단 화면 아래도 `이번 추천에 N 써요` 로.
- **식단 짜기 크레딧 2 → 3.** 후보 300개를 추려 그리디로 좁히고, 그 목록을
  통째로 프롬프트에 실어 보내고, 일곱 끼 + 날짜별 이유 + 요약을 받는다.
  사진 한 장(2)이나 짧은 한 턴(1)과 같은 값을 매길 이유가 없었다.
- `우리 식구 모두 / 나의 것만` 을 요리 캘린더와 같은 **상단 탭**으로.
- `내 것은 빼고 보기` 를 여기에도 추가.
- 그룹 설정 설명 축약.

### 요리 캘린더

- `완료 / 기록` 을 알약 두 개에서 **한 상자 안에서 미끄러지는 토글**로.
- `이번달 예상 절약액` → `이번달 절약액`. "예상" 이 목표치를 뜻하는 것처럼
  읽혔다. 자세히 안에서는 **지금까지**(실제 완료 N회)와 **목표까지**(목표 N회)를
  각각 계산식으로 나눠 적는다 — `× 6회` 만 있으면 어느 쪽인지 알 수 없었다.
- 목표 카드가 `내 요리`·`우리 식구 요리` 탭에서 통째로 사라지던 것 — 항상 보인다.
- 달력 아래 **`이번 주 장보기`** 카드 추가. 그 주에 계획한 요리의 재료 중
  냉장고에 없는 것을 모아 쿠팡 링크와 함께 보여 준다. 계획을 세우고 나면 다음
  물음은 늘 "그럼 뭘 사지" 인데, 그 답이 AI 화면에만 있었다.
  (`POST /api/recipes/ingredients` 신설 — 레시피 여러 개의 재료를 한 번에)
- 주 보기 맨 아래 카드가 상자 테두리에 붙어 있던 것 → 아래 여백.
- **당겨서 새로고침 글자가 화면에 겹쳐 깔리던 것** — 표시는
  `justDone ? 64 : pullDistance`, 내용은 `pullDistance` 로 **따로** 계산하고
  있었다. 끝나는 순간 내용만 제자리로 올라오고 표시는 64px 아래 남았다.
  하나(`offset`)로 묶었다.

### AI 식단 추천

- **`조건 말하고 추천받기` 가 눌려도 아무 일도 없던 것** — `wantAi` 를
  `useMemo(..., [])` 로 마운트 때 한 번만 읽고 있었다. 이 버튼은 같은 화면의
  주소만 `?ai=1` 로 바꾸므로 값이 다시 계산되지 않았다. `useLocation` 으로
  주소를 구독한다. 새 세션 여는 일도 효과로 옮겼다.
- **한 대화에 한 번만 묻는다.** 이어서 또 물으면 위에 있는 식단이 어느 조건으로
  짠 것인지 흐려지고 크레딧이 언제 나갔는지도 헷갈린다. 답이 오면 입력창은
  `새 대화로 다시 물어보기` 로 바뀐다.
- 지난 대화는 **읽기 전용**. 다만 그때 받은 식단은 `이 식단으로 계획 담기` 로
  지금 계획에 담을 수 있다(날짜는 내일부터 다시 센다 — 그때 날짜는 이미 지났다).
- 지난 대화 목록: `×` → **휴지통**, 카드 **안** 오른쪽 끝으로. 밖에 두니 카드
  높이와 안 맞아 줄이 어긋났고 `×` 는 닫기로 읽혔다.
- `고르는 중이에요...` → 앱의 **문서 애니메이션**(`StepLoading`). 무엇을 하고
  있는지 네 단계로 말한다.
- AI 답변이 **왜 이 조합인지**를 말하도록 프롬프트를 고쳤다: 냉장고의 무엇을
  주로 썼는지 / 새로 살 것이 무엇이고 그걸 몇 번 나눠 쓰는지 / 조건을 어떻게
  반영했는지 셋을 반드시 담게 했다.
- **냉장고가 비었을 때** — 크레딧을 쓰기 **전에** 말한다. 쓸 것이 없으면 전부
  사야 해서 장보기를 줄여 줄 수가 없다. 입력을 막고 냉장고로 가는 버튼을 준다.
- 인사말이 `냉장고에 있는 N가지로 …` 로 지금 무엇을 쓰는지 말한다.

### 그 밖에

- `/api/users/<id>/completed-recipes` 가 **행동 단위**(같은 레시피를 두 번
  완료하면 두 줄)로 주고 있었다. 그룹 목록은 `GROUP BY r.id` 라, 두 목록의
  세는 단위가 달라 `우리 식구 모두 25 / 나의 것만 26` 처럼 비교가 안 되는
  숫자가 나왔다. 레시피 단위로 맞췄다.

### 요리 챗봇 — 무엇을 묻는지부터 가른다

**무엇이 문제였나.** 이 대화창은 생김새는 챗봇인데 실제로는 레시피 검색기였다.
요리하다 막혀서 묻거나("국이 짠데 어떡해") 앱 사용법을 물으면("사진으로 재료
넣는 거 어떻게 해") 엉뚱한 레시피 목록이 떴다. 두어 번 헛물을 켠 사람은 다시
안 연다.

**무엇을 했나.** LLM 이 답을 쓰기 전에 `intent` 를 먼저 고른다.

| intent | 무엇 | 어떻게 답하나 |
|---|---|---|
| `recipe` | 뭘 해먹을지 / 직전 결과 수정 | 지금까지와 같다 (기본값) |
| `cooking` | 요리·재료 지식, 지금 막힌 것 | LLM 이 바로 답한다. 검색 안 함 |
| `app` | 쿡매치를 어떻게 쓰나 | **서버가 정해진 답으로 바꿔치운다** |
| `other` | 요리와도 앱과도 무관 | 못 한다고 한 문장 + 할 수 있는 일 한 줄 |

**앱 사용법만 LLM 에게 안 맡기는 이유** — 실측이 그대로 근거다. 같은 질문에
LLM 이 쓴 답은 셋 다 틀렸다:

- "홈 화면 상단의 카메라 아이콘" → 실제로는 **내 냉장고** 화면에 있다
- "매일 무료 크레딧이 충전" → 실제로는 **월요일마다**
- "설정 메뉴의 '가족/식구 관리'" → **그런 메뉴가 없다** (마이페이지 > 그룹 설정)

요리 이야기는 틀려도 취향 문제지만, 앱 사용법이 틀리면 사용자는 그 자리에서
막히고 "말한 대로 안 되는 앱" 이 된다. 그래서 LLM 은 **어느 주제인지만**
고르고(`topic` 한 낱말), 문장은 `backend/app_help.py` 에 적힌 것을 그대로
내보낸다. 기능이 바뀌면 그 파일만 고치면 된다.
주제: fridge · photo · plan · calendar · save · household · credit · search
(못 고르면 "쿡매치가 하는 일" 개요로 떨어진다)

말풍선에는 **그 화면으로 바로 가는 버튼**이 붙는다. 말로만 알려 주면 그 화면을
다시 찾아 헤맨다.

#### 검증 (실제 LLM 호출)
아홉 가지 물음 전부 의도를 맞게 골랐다 — 레시피 1, 요리 지식 2(짠 국 · 대체재),
앱 4(photo · credit · household · 개요), 그 밖 2(날씨 · 파이썬).
크레딧은 예전과 같이 1이다(같은 호출 한 번).

### 크레딧을 어디서나 두 가지로 말한다

크레딧은 한 종류가 아니다 — **쌓아 둔 잔액**(월요일에 채워짐)과 **하루에 쓸 수
있는 양**(자정에 돌아옴)이 따로 있고, **둘 중 하나만 바닥나도 못 쓴다.** 지금까지
자리마다 한쪽만 보여 줘서 "37 남았다는데 왜 안 되지" 가 됐다.

- `UsageLine` 을 다시 썼다. AI 가 붙은 세 자리(카메라 시트 · 챗 패널 · AI 식단)가
  모두 이 한 부품을 쓰므로 문구가 저절로 같아진다.
  - 보통: `남은 크레딧 37 · 오늘 8/10 · 이번에 3 써요`
  - 좁은 자리(챗 헤더): `크레딧 37 · 오늘 8`
  - 잔액이 0: `크레딧을 다 썼어요 · 월요일에 5 채워져요`
  - 오늘 몫이 0: `오늘 쓸 수 있는 양을 다 썼어요 · 자정에 돌아와요`
    (**어느 쪽이 바닥났는지**를 말한다. 예전엔 둘 다 "다 쓰셨어요" 였다)
  - `cost` 를 받아 그 자리에서 얼마가 나가는지도 적는다.
- 마이페이지 게이지에 **오늘 한도 막대**를 위에 추가. 아래 잔액 막대와 색을
  달리해서(오늘=검정, 잔액=노랑) 다른 것을 센다는 게 보이게 했다.
- 게이지 아래 설명은 두 막대가 못 하는 말 하나만 남겼다 —
  `월요일마다 5 채워져요 · 오늘 몫은 자정에 돌아와요`.

### 즐겨찾기·완료·기록 건수가 화면마다 다르던 것

**무엇이 문제였나.** 같은 값을 두 군데가 따로 들고 있는데 맞춰 주는 사람이 없었다.
  - 냉장고 요리(레시피 목록)는 **기기(localStorage)** 를 센다
  - 마이페이지·요리 캘린더는 **서버** 를 센다

그래서 셋이 어긋난다:
  1. **로그인 전에 누른 것은 서버로 영영 안 간다** — `syncToServer` 가
     `if (!isLoggedIn) return` 으로 빠져나가고, 나중에 로그인해도 올려 주는
     자리가 없었다.
  2. 서버 반영이 실패하면 조용히 삼킨다. 그 한 건은 기기에만 남아 영영 어긋난다.
  3. 다른 기기에서 누른 것은 이 기기 목록에 안 들어온다.

**실측.** 이 계정(user 7)의 서버 완료는 26건(레시피 25)인데, 2026-09 한 달치만
3건이다 — 화면에 뜬 `완료 3` 과 정확히 맞는다. 기기에는 최근에 누른 것만 있고
서버에는 전부 있는, 위 1~3 그대로의 상태다.

**무엇을 했나.** `utils/recipeSync.ts` — 로그인한 사람이 정해지면 세 목록을
**합집합**으로 맞춘다. 기기에만 있는 것은 서버로 올리고, 서버에만 있는 것은
기기로 내린 뒤 `localStorageChange` 를 쏴서 화면들이 다시 세게 한다.
합집합인 이유: 지운 것이 되살아날 위험보다 **누른 것을 잃을 위험**이 크다.
지우기는 로그인 상태에서 서버에도 같이 나가므로 양쪽에서 사라진다.
`/api/users/<id>/completed-recipes` 도 레시피 단위로 맞춰 뒀으므로(위 참고),
이제 세 화면이 같은 수를 센다.

### 요리 캘린더 — 달마다 누가 얼마나 했나

목표 카드는 `이번 달` 하나만 말했다. 지난달엔 얼마나 했는지, 식구 중 누가 얼마나
했는지는 아무 데도 안 남아서 목표를 세워 둔 보람이 없었다.

- `2026년 9월 목표` 옆에 **`기록`** 버튼. 누르면 바텀시트로 최근 12개월이 뜬다.
- 달마다: 총 횟수 / 목표 / 달성률, 달성하면 초록 `달성`,
  **누가 얼마나 했는지 한 줄 막대에 색으로 나눠** 담고 아래에 이름·횟수.
- `GET /api/households/me/monthly-progress?months=12` 신설.
- **지난달 목표는 저장하지 않는다**(목표는 지금 값 하나뿐). 그래서 지난 달들도
  지금 목표 기준으로 셈했다고 시트 아래에 그대로 적었다.

### AI 식단 — "0개짜리만 엄선했어요" 라고 해 놓고 장보기 6개가 뜨던 것

**원인.** 후보마다 프롬프트에 적어 주는 `추가 장보기 N개` 를 이렇게 셌다:

```python
extra = set(c['ingredients']) - have_set - basket_set   # basket_set = 좁혀 둔 7개 전부의 장바구니
```

`basket` 은 **그 일곱 개 전부의 장바구니**다. 그걸 빼면 후보가 **반드시 전부 0**
이 된다. LLM 은 본 대로 "0개짜리만 엄선했다" 고 정확히 보고한 것이고, 화면의 6은
최종 식단에서 따로 센 진짜 숫자였다.

**고침.**
- `buy_extra` 를 그 요리 **자기 몫**(`- have_set` 만)으로 센다. 이름까지
  프롬프트에 적어 준다 — 개수만 적으면 어느 것이 겹치는지(=하나 사서 여러 날
  쓰는지) 알 수가 없다. 프롬프트에도 "이미 고른 요리가 사는 것과 겹치는 쪽을
  골라라" 를 넣었다.
- **요약의 숫자는 서버가 쓴다.** LLM 은 요리를 고르는 시점에 답을 쓰는데
  장바구니는 그 뒤에 정해진다 — 구조상 LLM 이 숫자를 말하면 반드시 어긋난다.
  이제 나눠 맡는다:
  - 무엇을 썼고 무엇을 사야 하나 → 서버가 센다 (`_plan_summary`)
  - 조건을 어떻게 읽었나 → LLM 이 한 문장, **숫자 금지**
  숫자가 섞인 LLM 문장은 버린다(안전장치).

**이제 나가는 말** (실제 후보로 돌려 본 것):
> 냉장고의 간장·대파·양파·후추를 주로 썼어요. 곧 상하는 두부·애호박을 먼저
> 쓰도록 앞쪽에 뒀고요. 새로 살 건 다진마늘·달걀·… 모두 16개예요. 그중 달걀은
> 4끼·참깨는 3끼에 나눠 쓰도록 골라서, 하나 사면 여러 날 가요. 말씀하신 조건에
> 맞으면서 새로 살 재료가 가장 적어지는 조합으로, 유통기한이 임박한 것을 먼저
> 쓰는 쪽으로 골랐어요. 아이가 먹기 좋게 맵지 않은 것만 담았어요.

조사(을/를·은/는)를 받침에 맞춰 고르는 `_josa` 를 넣었다 — 코드가 문장을 쓰므로
"애호박 를", "달걀는" 이 그대로 나갔다.

### 유통기한을 **실제로** 본다

요약에 "유통기한도 봤다" 고 쓰려면 실제로 봐야 한다. 지금까지 유통기한은 LLM 의
날짜 배치에만 영향을 줬고, 후보를 좁히는 그리디는 안 봤다.
`shopping_plan.choose(soon=...)` 를 더해 **같은 값이면 곧 상하는 것을 쓰는 쪽**을
고른다. 순서는 `새로 살 것 적은 순 → 조건 → 곧 상하는 것 → 매칭률` — 장보기를
줄이는 것보다는 뒤다(상해서 버리는 것보다 안 사는 쪽이 크다).
요약도 **실제로 그 식단에 들어간** 임박 재료만 이름으로 부른다.

### 재료 개수가 두 군데서 다르게 보이던 것

머리말은 `쓸 재료 33개`, 인사말은 `냉장고에 있는 38가지`. 인사말은 한 번 만들어
대화에 그대로 남는데, 쓸 재료 수는 그 뒤에도 바뀐다(분류표가 늦게 오거나 사용자가
몇 개를 뺀다). **숫자를 인사말에 박지 않는다** — 개수는 늘 다시 그려지는 머리말이
맡고, 인사말은 `위에 있는 재료로` 라고만 한다.

### 이번 주 식단(무료) 화면

- 일곱 날이 그냥 아래로 이어지던 것을 **한 상자**에 담고 날 사이를 선으로 갈랐다.
- 부제를 `냉장고 재료로 · 곧 상하는 것부터 · 바꾸기는 공짜` 로 — 이 화면이 무엇을
  하는 곳인지 한 줄로 말한다.
- AI 입구의 부제를 `장보기도 줄여요` → `아이·다이어트 같은 조건` 으로. 이 화면이
  **조건을 못 받는다는 것**을 알아야 "왜 매운 걸 빼 달라고 못 하지" 를 안 겪는다.
- 식단 바로 아래 **장보기 카드**를 AI 쪽과 같은 생김새로 추가(쿠팡 링크 포함).
  전에는 한참 아래 목록에만 있어서, 식단만 보고 나가면 뭘 사야 하는지 몰랐다.

### AI 가 붙은 자리마다 남은 양과 이번에 나갈 값

`USAGE_QUOTA_PLAN.md` 의 정책을 화면이 다 말하고 있지 않았다. 특히 요리 캘린더의
AI 입구는 **`크레딧 2` 가 손으로 적혀 있어서**, 식단이 3 이 된 뒤에도 2 라고
말하고 있었다. 값은 서버가 정하므로 화면은 서버가 준 값만 쓴다.

| 자리 | 무엇이 보이나 |
|---|---|
| 요리 캘린더 · AI 식단 입구 | `말한 대로 · 크레딧 {서버값}` |
| 이번 주 식단 · AI 입구 | `아이·다이어트 같은 조건 · 크레딧 {서버값}` |
| AI 식단 화면 · 입력창 아래 | `남은 크레딧 N · 오늘 M/K · 이번에 3 써요` |
| 챗 패널 · 머리말 | `크레딧 N · 오늘 M` (폭이 좁아 두 숫자만) |
| 챗 패널 · **입력창 위** | 전체 줄 + `이번에 1 써요` ← 새로 추가 |
| 카메라 시트 | 전체 줄 + `이번에 2 써요` |
| 마이페이지 게이지 | 막대 두 개(오늘/잔액) + 기능별 값 |

FAB 버튼 자체에는 숫자를 안 붙인다 — 배지로 달았다가 "다른 화면 형식과 안 맞고
혼자 튄다" 는 지적을 받았다. 대신 **누르기 직전 자리**(입력창 위, 시트 안)에서
말한다. 그때가 알아야 할 때다.

### 하루 상한이 잔액을 따라가지 않던 것

관리자 계정에 크레딧을 넉넉히 넣어 놓고도 **하루 상한(free 15)에 막혀** 못 썼다.
잔액과 하루 상한이 서로 모르는 값이라, 크레딧을 아무리 줘도 하루에 15개까지밖에
못 쓴다 — 300개를 주면 다 쓰는 데 20일이 걸린다. 준 걸 못 쓰게 하는 셈이다.

- `usage_quota.effective_daily_cap()` 신설. 하루 상한 =
  **max(플랜 기본값, ⌈잔액 ÷ 7일⌉)**. 7일인 이유는 크레딧이 매주 월요일에
  채워지기 때문이다 — 리셋 주기와 같은 길이로 두면 "한 주 안에는 가진 걸 다 쓸
  수 있다" 가 된다. `max` 라서 잔액이 적은 사람의 상한을 **깎지는 않는다**.
  기간은 `QUOTA_DAILY_BURN_DAYS` 로 바꿀 수 있다.
- 어드민이 `user_quota.daily_cap` 에 숫자를 직접 넣었으면 **그 값이 이긴다.**
  사람이 정한 숫자를 계산이 덮으면 어드민 화면이 거짓말이 된다.
- `/api/usage` 와 `/api/admin/users` 가 같은 함수를 쓴다. 전에는 어드민 목록이
  플랜 기본값을 보여줘서, 크레딧을 많이 준 계정일수록 서버와 다른 숫자를 말했다.
  `daily_cap_fixed`(고정인지 자동인지)도 함께 내려준다.
- 어드민 한도 편집칸의 기본값을 **빈칸(자동)** 으로. 전에는 계산된 값이 칸에
  미리 채워져 있어서, 저장을 누르는 순간 그 숫자가 고정돼 잔액이 늘어도 상한이
  안 따라왔다. placeholder 로 `자동 N` 을 보여준다.

### 지난 대화 · 완료일자 — 눌러도 뭐가 되는지 모르던 자리들

- **지난 대화 목록의 휴지통**: 누르면 바로 지워졌다. 되돌릴 수 없고 크레딧을
  써서 받은 식단이 그 안에 들어 있는데, 목록을 열려다 옆을 누르기도 쉽다.
  → 확인 팝업을 띄운다(시트 위에 뜨므로 `nested`).
- 그 휴지통의 **세로 테두리를 없앴다.** 칸을 가르는 선이 있으니 카드 안의 보조
  동작이 아니라 따로 얹힌 버튼처럼 보였다.
- **`지난 대화` · `새 대화` 버튼을 제목 아래 줄로 내렸다.** 제목과 같은 줄
  오른쪽 끝에 절대배치돼 있어서, 둘이 함께 나오면 가운데 `AI 식단 추천` 에
  맞닿아 한 덩어리로 읽혔다.
- **요리 캘린더 완료일자 수정**: 저장 버튼이 26px 노란 사각형에 11px 체크
  아이콘 하나뿐이라 **아무 말도 하지 않았다.** `✓ 저장` 으로 글자를 넣고,
  같이 `취소` 를 뒀다 — 저장 말고 빠져나올 길이 없으면 고칠 마음이 없어도
  아무 날짜나 저장하게 된다. 같은 화면의 `1인 한 끼` · `식구 수` 저장 버튼도
  같은 모양이라 함께 고쳤다.

### (이어서) 하루 상한이 그래도 안 풀리던 것

어드민이 `user_quota.daily_cap` 에 넣은 숫자가 계산을 **이기게** 해 뒀는데, 그게
바로 이 기능을 무력화했다. 어드민 편집칸이 그 자리에 기본값을 미리 채워 두고
있었으므로, 크레딧을 지급하면서 저장을 한 번이라도 누른 계정에는 `15` 가 그대로
박혀 있다. 그리고 그 계정이 곧 크레딧을 많이 받은 계정이다.

- 하루 상한 = **max(플랜 기본값, 어드민이 넣은 숫자, ⌈잔액 ÷ 7일⌉)**.
  넣은 숫자는 **올리는 데만** 쓴다. 내려서 묶는 용도는 어차피 없다 — 크레딧은
  잔액이라 안 주면 그만이고, 상한은 몰아쓰기를 막는 장치이지 이미 준 걸 못 쓰게
  하는 벽이 아니다. 저장된 값이 무엇이든 잔액만큼은 쓸 수 있다.
- `daily_cap_fixed` → `daily_cap_manual` 로 이름을 바꿨다. 더 이상 "고정" 이 아니다.
- 삭제 확인 팝업 문구를 줄였다 — `되돌릴 수 없어요.` 한 줄. 팝업에서 두 줄이 넘게
  설명하면 안 읽고 누른다.

### 완료일자 편집칸 때문에 닉네임이 줄바꿈되던 것

`저장` · `취소` 글자를 넣으면서 편집칸이 넓어졌고, 그만큼 가운데 칸(제목·닉네임)이
눌려 **`오후 7:24 · 닉네임` 이 두 줄로 갈라졌다.**

- 항목 줄에 `flexWrap` 을 주고 편집칸을 `flexBasis: 100%` 로 — 고치는 중에는
  날짜칸·저장·취소가 **통째로 아랫줄**을 쓴다. 위 칸은 건드리지 않는다.
- 시간·닉네임 줄은 `nowrap` + 말줄임으로. 칸이 좁아져도 두 줄이 되지 않는다 —
  시간과 이름이 위아래로 갈라지면 한 사람의 정보로 안 읽힌다.

### "고기 없이" 라고 해도 고기 요리가 나오던 것

**세 군데가 다 문제였다.**

1. **말 자체를 못 읽었다.** `plan_intent` 는 분류(아이·다이어트·국물 …)만 안다.
   그런데 사람들이 실제로 가장 많이 적는 건 **재료 이름**이다. 실측:

   | 적은 말 | 예전 결과 |
   |---|---|
   | `고기 없이` | 아무것도 안 걸림 |
   | `고기는 빼줘` | 아무것도 안 걸림 |
   | `우유 빼고` | 아무것도 안 걸림 |
   | `고기 빼고` | keywords=`[채식, 비건]` (우연히 맞음) |

   "X 없이/빼고/말고/제외" 를 일반적으로 읽는 `bans()` 를 넣었다.

2. **한 이름이 여러 이름을 뜻한다.** 레시피 재료에 `고기` 라고 안 적혀 있다 —
   `삼겹살`·`베이컨`·`스팸`이라고 적혀 있다. 글자만 맞춰 지우면 삼겹살이 그대로
   남는다. `BAN_EXPAND` 로 넓힌다(고기·해산물·생선·유제품·계란·견과·밀가루·매운맛 등).

3. **거르지 않고 뒤로 미루기만 했다.** SQL 이 `avoid_hit ASC` 로 순서만 바꿨는데,
   **그 뒤의 그리디는 순서를 안 본다** — 장바구니 크기만 보고 고른다. 게다가
   냉장고에 돼지고기·스팸이 있으면 고기 요리가 "새로 살 게 가장 적은" 후보라
   그리디가 **적극적으로 골랐다.** 이제 후보에서 아예 뺀다(다 걸러지면 그대로 둔다 —
   빈 화면보다 낫다).

**실측** (냉장고에 돼지고기·스팸이 있는 상태, `고기 없이`):
후보 300개 → 227개가 걸러지고 73개. 고른 7개 중 고기 요리가
**전 7개 → 후 0개.**

### 완료를 누르면 "쓴 재료를 냉장고에서 뺄까요?"

요리를 만들었으면 그 재료는 거의 다 쓴 것이다. 그런데 냉장고 목록은 그대로
남아서, 안 지우면 **없는 재료로 계속 식단을 짜 주고 유통기한 알림도 계속
울린다.** 완료를 누른 그 순간이 기억이 가장 정확한 때다.

- 그 레시피 재료 중 **내 냉장고에 실제로 있는 것**만 칩으로 보여 준다.
- 자동으로 안 지운다 — "감자 한 알 썼는데 세 알 남았다" 가 흔하다. 있는 것을
  없다고 하는 쪽이 남겨 두는 것보다 나쁘다. **묻고 고르게** 한다.
- 기본으로 켜 두는 건 **식재료만.** 간장·소금은 한 끼로 안 없어진다 — 전부
  켜 두면 매번 꺼야 해서 이 창이 귀찮은 것이 된다.
- 물어볼 게 없으면(냉장고가 비었거나 겹치는 재료가 없으면) 창을 안 띄운다.

### 크레딧 문구

`남은 크레딧 14 · 오늘 1/15 · 이번에 3 써요` — 셋이 나란히 붙어 있어서 어느
것이 무엇인지 알 수 없었다. 서로 다른 것을 센다. 줄을 바꾸고 이름을 붙였다.

```
전체 남은 크레딧 14
오늘 더 쓸 수 있는 건 1 (하루 15까지)
이번에 물어보면 3 나가요
```

### 요리 캘린더

- 월별 기록 시트 제목 `달마다 얼마나 했나` → **`월별 기록`**.

### 크레딧 문구를 **이름 + 숫자**로

서술형으로 풀어 썼더니 오히려 안 읽혔다. 끊어서 적는다.

```
전체 잔여 크레딧 14
금일 잔여 크레딧 1 · 하루 최대 15
1회 질문당 3 크레딧
```

소진 문구도 같은 말투로 — `크레딧 소진 · 월요일에 5 충전돼요`,
`금일 크레딧 소진 · 자정에 초기화돼요`. 마이페이지 게이지의 두 막대 라벨도
`전체 잔여 크레딧` / `금일 잔여 크레딧` 으로 맞췄다.

그리고 **왜 5뿐인지**를 말해 준다. 숫자만 적어 두면 "이게 왜 이거지" 로 끝나고
더 받는 길이 있다는 것도 안 보인다 —
`더 쓰시려면 위 [더 필요해요] 로 알려 주세요 — 쓰임새를 보고 늘려 드려요.`

### 냉장고에서 뺄 재료 — 판단 기준을 사전에 못 박았다

감으로 정하면 사람마다 다르고 나중에 고칠 수도 없다. 재료 사전의 **중분류**
하나로 정한다.

| 기본 | 중분류 | 수 |
|---|---|---|
| **켠다** | 식재료 원물 / 유제품 / 즉석식·간편식 | 759 / 55 / 80 |
| **끈다** | 양념·조미료 / 베이킹·제면·디저트용 / 음료·주류 / 빙재료 | 296 / 136+6 / 23 / 3 |

간장 한 스푼 썼다고 간장이 없어지지 않는다. 밀가루·설탕도 같다.
사전에 없는 이름은 **켠다** — 흔한 양념은 사전이 거의 다 담고 있으므로 모르는
이름은 특정 부위·품종 같은 원물일 가능성이 높다. 어차피 사용자가 보고 누르는
것이라 켜 두는 쪽이 손이 덜 간다.

### 식단은 토큰이 하나도 안 쌓이고 있었다

원장(`llm_usage`)을 보니 `plan` 30건이 전부 토큰 NULL 이었다.
`meal_plan.suggest` 는 이미 벗겨 낸 `usageMetadata` 를 돌려주는데, 그걸
`note_gemini_usage` 에 넘기면 안에서 `usageMetadata` 를 **한 번 더** 찾다가 빈
값이 된다. 다시 씌워서 넘긴다. 크레딧 환산이 맞는지 확인하려면 이 값이 있어야
한다 — 어드민의 `크레딧 환산 점검` 이 식단만 빈칸이었던 이유다.

### 사진 인식 — 장수와 크레딧을 헷갈리게 적고 있었다

`1회 질문당 2 크레딧` 이라고 적혀 있었다. 두 가지가 틀렸다 — 사진은 "질문" 이
아니고, **장수와 무관**하다. 다섯 장을 한 번에 올려도 LLM 호출은 한 번이라
크레딧도 한 번치다(원장 실측: vision 7건 / 14 크레딧, 전부 2씩).

장당 나가는 줄 알면 **한 장씩 올려서 오히려 손해를 본다** — 권장해야 할 행동
(몰아서 올리기)에 벌금을 매기는 것처럼 읽힌 셈이다. `UsageLine` 에 `costUnit`
을 더해 자리마다 단위를 맞춘다:

- 챗봇·식단 → `1회 질문당 N 크레딧`
- 사진 인식 → `사진 몇 장이든 1회 2 크레딧`

### 무료 주간 충전 5 → 6

식단 짜기가 2 → 3 이 되면서 주 5 로는 **식단 한 번 반**밖에 안 됐다. 6이면 딱
두 번이다(또는 사진 세 번, 챗봇 여섯 번). 매주 무엇을 할 수 있는지가 떨어지는
숫자라야 "이번 주엔 뭘 하지" 가 성립한다.

`USAGE_QUOTA_PLAN.md` 의 환경변수 표도 고쳤다 — **주 한도** 방식이던 시절의
`QUOTA_*_WEEKLY` 가 그대로 남아 있었는데, 지금은 한도가 아니라 잔액이라 그런
변수는 없다. 코드가 실제로 읽는 것만 남기고, 유료 전환 전에 확인할 것(비용이
아니라 Gemini 무료 티어의 요청 수 제한)을 부록으로 적었다.

### 유료(plus) — 매주 60

`더 필요해요` 로 들어온 요청을 어드민이 **`유료로`** 한 번 누르면 plus 로 올라가고
요청도 함께 닫힌다. 예전엔 요청만 닫고 실제로 올리는 건 사용자 탭에서 따로 해야
했는데, 두 화면을 오가다 한쪽을 빠뜨리면 **요청은 닫혔는데 그대로 무료인 사람**이
남는다. 가장 흔한 처리 하나만 합쳤고, 그 밖의 조정(크레딧 얹기·일 상한)은 여전히
사용자 탭에서 한다.

**주간 충전은 더해지는 게 아니라 대신 들어간다.** 유료로 올려도 무료 몫 6이 따로
또 들어오면 66이 되는데, 그건 "유료는 60" 이라는 약속과 다르다. `_ensure_weekly`
가 **이번 주에 이미 받은 만큼을 빼고 모자란 것만** 채운다.

실측 (롤백 트랜잭션 안에서 실제로 돌려 봄):

```
① 월요일 무료 지급        → 누적  6
② 같은 날 또 열어 봄      → 누적  6   (안 늘어남)
③ 수요일에 유료로 전환    → 누적 60   (차액 54 만 들어감)
④ 유료로 또 열어 봄       → 누적 60   (안 늘어남)
⑤ 다시 무료로 내림        → 누적 60   (이미 받은 건 안 깎는다)

원장:  +6  2026-W36:6   주간 지급 (free 6)
      +54  2026-W36:60  주간 지급 (plus 60)
```

주중에 돈을 냈는데 다음 월요일까지 기다리는 일도, 66을 받는 일도 없다.

화면에는 plus 배지와 함께 `매주 월요일 60 크레딧 충전` 이라고 적고, 무료인
사람에게는 `더 필요해요 — 매주 60 크레딧으로 올려 드려요` 로 **올리면 얼마인지**를
숫자로 보여 준다.

### 요즘인기 카드 — 사진과 제목에 자리를 돌려줬다

**먼저 쟀다.** 카드 하나가 300×280px 인데:

| | 높이 | 비중 |
|---|---|---|
| 사진 | 100px | 36% |
| **제목** | **24px** | **9%** (한 줄, 잘림) |
| 나머지 | 156px | 56% |

훑을 때 필요한 건 제목 하나인데 그게 9% 였다. 나머지를 순위 배지·매칭률
오버레이·좋아요/댓글·아이콘 3개·재료 칩 여러 줄·대체 가능 줄이 먹고 있었고,
한 카드 안에 글자 크기가 **다섯 종류**(12/13/14/15/16px)라 위계도 없었다.

`RecipeCard` 에 `variant` 를 더했다. 냉장고요리는 매칭률이 곧 정렬 기준이므로
**그대로 두고**(`match`), 요즘인기만 `browse` 로 바꾼다.

- **사진 100 → 160px.** 280×100 은 3:1 띠라 음식이 뭉개진다. 사람을 끌어당기는
  유일한 요소인데 죽이고 있었다.
- **매칭률 오버레이를 사진에서 뺐다.** 제일 크게 읽히는 것이 `0%` 라, 카드마다
  "너는 이거 못 만들어" 를 먼저 말했다. 요즘인기는 **내 냉장고와 상관없이 요즘
  뭐가 유행하나**를 보는 자리다.
- **제목 두 줄, 15px.** 카드에서 제일 큰 글자가 됐다.
- **통계 줄을 `댓글 22 · 내 재료 0/5` 로.** 같은 정보인데 **가진 것**을 센다.
  `좋아요 0` 은 뺐다 — 네이버 블로그는 대부분 0이라 늘 노이즈였다.
- **재료 칩을 접었다.** 다섯 개가 두 줄이면 제목보다 크고, 점선 칩은 전부 없는
  재료라 카드 절반이 없는 것 목록이었다. `재료 5개 보기 ▾` 로 궁금한 사람만 편다.
- **범례의 색 설명을 껐다.** 칩이 접혀 있으니 안 보이는 것을 설명하는 꼴이었고,
  섹션마다 반복돼 화면이 범례로 덮였다. `총 20건` 만 남긴다.

칩을 접어 번 자리로 사진을 키웠으므로 **카드 높이는 280 그대로**다.

### `더 필요해요` — 무슨 일이 벌어지는지 먼저 말한다

빈 칸에 `어떻게 쓰시는지 (선택)` 만 있으면 이걸 적으면 뭐가 되는지 알 수 없어
대부분 그냥 닫는다. 지금은 시험 기간이라 요청하면 실제로 바로 올려 주므로,
그 사실을 그대로 적는다.

```
지금은 시험 기간이라 요청하시면 유료 계정으로 바꿔 드려요.
매주 60 크레딧이 충전되고, 하루 한도도 함께 올라가요.

[ 어떻게 쓰실지 한 줄만 알려 주세요 (안 적어도 돼요) ]

[   유료 계정 전환 요청 보내기   ] [ 취소 ]
```

### 대체 재료 표가 4개월 동안 안 만들어지고 있었다

어드민에서 사전에 재료를 넣으면 그 재료의 **"대체 가능"** 은 어디서 생기나 —
`ingredient_management/generate_substitutes.py` 가 사전의 Feature·분류를 보고
유사도를 계산해 `ingredient_substitute_table.csv` 를 만든다. 그런데 이건 **손으로
돌리는 스크립트**였고, 사전 배치는 사전 CSV 만 커밋했다.

실측:

| 파일 | 최종 수정 |
|---|---|
| `ingredient_profile_dict_with_substitutes.csv` | 2026-09-05 (매일) |
| `ingredient_substitute_table.csv` | **2026-04-18** |

4개월 반 동안 안 돌았고, 그 사이 들어온 재료를 포함해 **57개가 대체표에 한 번도
안 나왔다** — 화면에서 `대체 가능` 이 영원히 안 뜨는 재료들이다.
(9/3~9/4 승인분 11개가 그 안에 있었다)

**고침.** 매일 도는 사전 배치(`apply_dictionary_additions_daily.bat`)의 2.5단계로
넣었다. **4.4초**면 끝나므로 사전이 바뀌든 말든 매일 돌린다. 커밋 대상에도
대체표를 더했다(여전히 `git add -A` 는 쓰지 않는다 — 파일 세 개만 지정).

돌린 결과: 50,138 → **52,003행**, 빠진 재료 57 → **29개**.
남은 29개는 대부분 `소금 · 간장 · 다시다` 같은 양념이라 **대체재가 없는 게 맞는
답**이다(소금 대신 쓸 것이 없다).

### 유료는 기간이 있는 것

`plan` 만 있고 **언제까지인지가 없었다.** 그러면 영원히 유료인 계정이 쌓이고,
결제를 붙일 때 그 사람들을 어떻게 할지가 문제가 된다.

- `user_quota.plan_until` 추가. 어드민 요청 목록에서 **`1개월 / 3개월 / 6개월 /
  12개월`** 중 하나를 눌러 올린다(시험 기간용 `무기한` 도 남겨 뒀다).
- 만료는 **읽을 때 판단한다.** 배치를 따로 두면 그게 하루 안 도는 날 공짜로
  유료가 된다. `_plan_of` 가 기한이 지났으면 `free` 로 돌려준다 — 표의 값은
  안 지운다(언제까지 유료였는지가 남아야 나중에 알 수 있다).
- 개월 수를 더할 때 `+30일 × n` 을 쓰지 않는다. 월말·윤년에서 어긋난다.
  실측: `1/31 +1개월 → 2/28`, `12/31 +6개월 → 6/30`, `2/29 +12개월 → 2/28`.
- 화면에 `2026-12-05 까지 유료예요. 그 뒤에는 무료로 돌아가요.`

**승급을 알린다.** 지금까지는 어드민이 올려도 아무 알림이 없어서, 요청하고
기다리던 사람이 마이페이지를 다시 열어야 알았다. 전역 토스트를 띄운다:

```
[PLUS]  유료 계정이 됐어요.
        매주 60 크레딧을 받아요 · 2026-12-05 까지.
```

기기에 `본 표시`를 남겨 한 번만 뜨게 하고, 기간이 바뀌면(연장) 다시 알린다.

### 냉장고요리 카드도 같은 모양으로

요즘인기에서 한 것을 세로 목록에도 적용했다.

- **사진 120 → 160px**, 제목 두 줄 15px.
- **매칭률 배지를 사진에서 뺐다.** 어디서든 뺀다 — 사진은 사람을 끌어당기는
  유일한 요소인데 숫자로 가리고 있었다.

**그런데 냉장고요리는 매칭률이 곧 정렬 기준이다.** 숫자를 아예 없애면 "왜 이
순서지" 를 알 수 없다. 그래서 없애지 않고 **자리를 옮겼다** — 제목 아래 줄에서
`내 재료 3/5 (60%)` 로 말한다. 같은 값인데 **가진 것**을 먼저 세고, 70% 이상이면
초록으로 물들어 훑을 때 눈에 걸린다. 요즘인기는 인기순이라 정렬과 무관하므로
개수만(`내 재료 0/5`) 센다.

- `좋아요 0` 은 뺐다. 네이버 블로그는 대부분 0이라 늘 노이즈였다.

### 재료 칩 · 대체 가능

- **접는 기준을 6 → 10개로.** 재료가 7~8개인 레시피가 흔해서 한두 개 때문에
  `+2` 가 붙는 일이 잦았다. 그 한두 개를 보려고 누르게 만드는 건 이득이 없다.
  15개짜리에서 칩이 네 줄로 늘어나는 것은 또 다른 문제라 상한은 남긴다.
- **`대체 가능` 을 눈에 띄게.** 이게 이 앱의 핵심 기능인데 회색 12px 로 카드에서
  제일 조용했다. 라벨을 노란 배지로, 대체 재료 칩도 노란 테두리로 바꿨다.
  "없는 재료를 무엇으로 바꾸나" 는 찾아 읽는 게 아니라 눈에 들어와야 한다.
- 요즘인기의 접기 버튼 문구를 `재료 5개 · 대체 가능 보기 ▾` 로 — 펴면 무엇이
  나오는지 이름을 적었다. 전에는 `재료 5개 보기` 라 대체재가 거기 있는 줄 몰랐다.

### 동의어와 대체재 — 어드민에서 보이게

**동의어는 매일 잘 붙고 있다.** 어드민에서 `계란` 을 `달걀` 의 동의어로 승인하면,
새벽 04:30 배치가 그 행의 `synonyms` 칸에 보탠다. 9/5 하루만 봐도
`달걀 ← 계란·노른자`, `사과 ← 홍옥·부사`, `멸치 ← 국물용멸치·다시멸치` 가 들어갔다.
지금 재료 1,360개에 동의어 627개.

**그런데 `Feature` 는 안 채워진다.** 새 대표어를 만들 때 분류와 상위어만 들어오고
(`apply_dictionary_additions.py`), 대체재 유사도는 **Feature 로 계산한다.**
그런 재료는 맛·식감이 아니라 **분류가 비슷하다는 이유로만** 묶인다.

- `generate_substitutes.py` 가 Feature 가 비면 **상위어에서 물려받는다.**
  상위어는 바로 이런 쓰임을 위해 받아 둔 값이다 — `닭다리` 의 상위어가 `닭고기` 면
  닭고기의 특징을 물려받는 것이 맞는 첫 근사다. 사람이 나중에 적어 넣으면
  그때부터 그것을 쓴다(빈 것만 채운다). 지금 4개가 물려받는다.
- 그래도 안 되는 5개(`야채·양복버섯·생강술·황기·적채` — 상위어가 `채소`·`주류`
  같은 분류어라 사전에 재료로 없다)는 **어드민 운영 탭에 이름까지 적어 둔다.**
  배치 로그에도 `[WARN]` 으로 남는다.

어드민 `운영` 탭에 한 줄을 더했다:

```
재료 1,360개 · 동의어 627개 · 대체 재료 표는 매일 04:30 사전에서 다시 만듭니다
Feature 가 없어 분류로만 묶이는 재료 5개 — 야채, 양복버섯, 생강술, 황기, 적채
```

### 냉장고 요리가 느렸던 것 — 재 보고 고쳤다

**먼저 어디가 느린지 쟀다.**

| | |
|---|---|
| `/api/recipes/filter` 한 번 | **2,347~2,715 ms** (돌려주는 건 9KB) |
| 그중 네트워크 | 아님 — 서버에서 세는 시간 |
| 왜 | 레시피 **44,707행**마다 `FIND_IN_SET` 을 재료 수만큼 돌려 매칭률을 만들고, 그 **계산된 값으로 정렬**한다. `used_ingredients` 에는 인덱스가 없다(제목·본문 FULLTEXT 뿐) |
| 재료 3개로 줄이면 | 1,761 ms — 재료 수에 비례한다 |

매칭률은 그 사람의 냉장고에 따라 달라져서 **미리 계산해 둘 수 없다.** 쿼리를
빠르게 만드는 것으로는 못 없앤다.

**① 미리 받아 둔다** (`utils/recipePrefetch.ts`).
앱을 여는 순간 첫 화면 요청을 걸어 둔다. 냉장고 재료는 이미 기기에 있으므로
탭을 누르기 전에 시작할 수 있다. React Router 는 화면만 갈아 끼우고 앱은 계속
살아 있으므로, 모듈에 담아 두면 탭을 옮겨 다녀도 남는다 — "다른 데 갔다 오면
완성해 둘게요" 가 실제로 그렇게 된다. 화면을 덮었다 돌아올 때도 다시 본다.

한 번 막혔다: `getMyIngredientsAsKeywords()` 는 **동의어 사전이 올라와 있어야**
동작하는데 앱을 막 열었을 때는 없다. 기다리지 않으면 빈 배열이 나와 미리 받기가
조용히 아무 일도 안 했다(실제로 그렇게 안 돌고 있었다). 사전을 먼저 기다린다.

**② 12MB 짜리를 두 번 받고 있었다** (`utils/csvOnce.ts`).
`ingredient_substitute_table.csv` 가 **두 번**, `ingredient_profile_dict...csv` 가
**네 번** 내려왔다. 화면마다 각자 `fetch` 하는데 localStorage 캐시는 **다 받은
뒤에야** 써지므로, 처음 들어갈 때는 아무도 캐시를 못 본다. 개발 컴퓨터에서는
82ms 라 안 보였지만 휴대폰에서는 그대로 몇 초다.
받는 중인 약속을 주소별로 들고 있다가 같은 것을 또 달라고 하면 그 약속을
돌려준다. (12MB 는 localStorage 에 안 들어가므로 화면들이 각자 **파싱한 결과**를
캐시하는 것은 그대로 두고, **받는 일**만 합쳤다)

**③ 기다리는 동안 무슨 일인지 말한다.**
뼈대만 있으면 얼마나 더 기다릴지 모른다. 지금 무엇을 하는 중인지와,
**기다릴 필요가 없다는 것**을 적었다:
`냉장고 재료로 만들 수 있는 요리를 고르는 중이에요 / 다른 화면을 보고 오셔도 돼요`
(도는 고리 키프레임이 `PullToRefresh` 안의 `<style>` 에만 있어서 다른 화면에서는
**멈춘 채로** 그려졌다 — `.cm-spinner` 로 전역에 뺐다)

**④ 1페이지에서 같은 스캔이 두 번 돌던 것.**
COUNT 분기 조건에 `or need_match_rate` 가 붙어 있었다. 그건 "결과에 매칭률을
실어 달라" 는 뜻이지 **몇 건인지가 달라진다는 뜻이 아니다.** 매칭률 하한·상한이
없으면 세는 결과는 `WHERE` 만으로 정해진다. (다만 실측해 보니 MySQL 이 이미
최적화하고 있어 **눈에 띄는 절약은 없었다** — 논리적으로 맞게 고쳐 둔 것이다)

#### 결과 (실측, 캐시 비운 상태에서 탭을 눌러 카드가 뜰 때까지)

| | 전 | 후 |
|---|---|---|
| 탭 누른 뒤 카드까지 | **2,996 ms** | **1,005 ms** |
| 대체표 내려받기 | 2회 · 24.4MB | **1회 · 12.2MB** |

### 나머지 CSV 도 한 번만 받도록

`utils/csvOnce.ts` 를 쓰는 곳을 전부 옮겼다. 파일마다 부르는 모양이 달라
(`fetch().then(r=>r.text())` / `await fetch()` + `await res.text()` / `res.ok` 검사)
한 번에 바꾸려다 타입이 깨진 적이 있어, 이번에는 **한 곳씩 모양을 보고** 고쳤다.

옮긴 곳: `FilterModal` · `RecipeSortBar` · `CompletedRecipeListPage` ·
`RecordedRecipeListPage` · `MyFridge` · `Popular` · `RecipeList` ·
`utils/recipeUtils`(2곳) · `utils/shelfLife`

#### 결과 (캐시 비운 채로 앱을 열고 냉장고요리 탭까지)

| 파일 | 전 | 후 |
|---|---|---|
| `ingredient_substitute_table.csv` (12.2MB) | 2회 | **1회** |
| `ingredient_profile_dict_...csv` (0.3MB) | 7회 | **1회** |
| `Filter_Keywords.csv` | 2회 | **1회** |
| 내려받은 CSV 합계 | 24.4 + 2.1 + α ≈ **26.6MB** | **12.5MB** |
| 탭 누른 뒤 카드까지 | 2,996 ms | **986 ms** |

휴대폰에서 이 차이가 그대로 로딩 시간이다.

### 요즘인기 — 급상승 표 두 개를 한 자리로

**먼저 쟀다.** `인기 급상승 요리` 와 `인기 급상승 테마` 가 위아래로 따로 있었다.

| | |
|---|---|
| 표 크기 | 각 **280 × 338px** |
| 두 표 사이 | 437px |
| 합쳐서 차지하는 세로 | **846px** |
| 휴대폰 한 화면 | 812px |

**둘 다 보려면 한 화면을 꽉 채우고도 더 스크롤해야 했다.** 게다가
`max-w-[280px] mx-auto` 라 375px 화면에서 좌우 95px 이 빈 채로 좁은 표만
가운데 있었다.

나란히 놓는 것도 생각했지만, 375px 을 둘로 나누면 **187px** 이라
`순위 · 이름 · 레시피 수` 에 `▴2배` 배지까지 넣을 자리가 안 나온다.

**탭으로 한 자리에 겹쳤다.** 제목은 `인기 급상승 TOP 10` 하나, 그 아래
`요리 / 테마` 세그먼트(요리 캘린더의 완료·기록과 같은 모양 — 같은 일은 같게
보여야 한다), 표는 하나만 그린다.

- 세로 **846px → 382px**
- 표 폭 **280px → 335px** (화면 폭을 다 쓴다)
- 이름 칸을 왼쪽 정렬로. 가운데 정렬은 길이가 제각각인 이름이 들쭉날쭉해 보인다
- 숫자와 배지를 **오른쪽에 나란히** 붙였다. 전에는 칸 안에서 절대배치로 겹쳐
  놔서, 폭이 좁아지면 배지가 숫자를 덮었다

### 요즘인기도 미리 받아 둔다

`prefetchPopular()` — 이쪽은 **냉장고와 상관없는 목록**이라 누구에게나 같은
답이고 재료가 필요 없어서, 앱을 여는 즉시 부를 수 있다(사전을 기다릴 일도 없다).

냉장고 요리와 따로 둔 이유: 두 요청이 **동시에** 나가면 서로 느려진다. 조건이
없어 가벼운 이쪽을 먼저 걸고, 무거운 냉장고 쪽은 그대로 둔다.
실측 — 홈에서 430ms 에 미리 받아 둔다.

### 요즘인기 — 유튜브·네이버도 한 자리로, 표는 가운데로

**두 목록을 겹쳤다.** 유튜브와 네이버가 각각 `제목 + 범례 + 가로 목록` 으로 따로
있었다. 둘 다 "요즘 인기" 라는 **같은 이야기**인데 세로로 줄줄이 놓이니 화면을
한참 내려야 아래 것이 보이고, 위아래를 견줘 볼 수도 없다. 급상승 표와 같은
방식으로 묶었다 — 제목은 `인기 레시피` 하나, `네이버 / 유튜브` 세그먼트,
목록은 하나만 그린다.

**네이버가 기본이다.** 건수가 훨씬 많고(100건 vs 22건), 냉장고 재료와 겹치는
것도 그쪽이 많다.

`총 N건` 은 고른 쪽 것으로 세그먼트 옆에 붙였다(범례 자리에 있던 것을 옮겼다).

**표를 가운데로.** 화면 끝까지 늘렸더니 요리명과 레시피 수 사이가 휑하게
벌어졌다(375px 에서 가운데 130px 이 빈 칸). 내용에 맞는 폭(320px)으로 가운데
두고, 순위·이름도 가운데 정렬로 맞췄다.

#### 결과 (실측)

| | 전 | 후 |
|---|---|---|
| 섹션 수 | 6개 | **4개** |
| 문서 전체 높이 | 2,501px | **1,764px** |

`firstSectionKey`(맨 위 섹션에는 구분선을 안 그린다)도 합쳐진 구조에 맞게
고쳤다 — 유튜브·네이버가 한 섹션이므로 둘 중 하나만 있어도 그 자리가 첫 섹션이고,
급상승도 요리·테마가 한 섹션이다.

### 어드민 운영 탭 — 자동인 것과 손볼 것을 갈랐다

`Feature 가 없어 분류로만 묶이는 재료 5개` 안내를 **`자동으로 도는 작업`** 카드에
뒀었는데, 하는 말은 **"사람이 적어 넣으세요"** 였다. 자리가 맞지 않았다.

- **손으로 관리하는 자료** 쪽으로 옮겼다. 고칠 파일이 바로 그 표의 `재료 사전`
  이므로 그 옆이 맞다. 예시(`감칠맛, 짠맛, 액상`)와, 상위어에 Feature 가 있으면
  자동으로 물려받으니 손댈 필요 없다는 것도 함께 적었다.
  빠진 재료가 없으면 `Feature 가 빠진 재료 없음` 으로 초록색.
- **자동으로 도는 작업** 설명에는 빠져 있던 한 줄을 넣었다 —
  `04:30 에는 사전을 옮긴 뒤 대체 재료 표도 그 사전으로 다시 만듭니다. 새 재료의
  '대체 가능' 이 여기서 생깁니다.` 전에는 04:30 이 "사전을 옮긴다" 로만 적혀
  있어서, 대체 재료 표를 누가 언제 만드는지 화면 어디에도 없었다.

### 어드민 요청 탭 — 사유 칸이 세로로 늘어지던 것

`minWidth: 640` 인 표에서 사유 칸만 `maxWidth: 260` 이었다. 그런데 **표에서
`maxWidth` 는 거의 무시된다** — 다른 칸이 다 차지하고 남는 만큼만 가져간다.
기간 버튼(1·3·6·12개월·무기한)을 넣으면서 동작 칸이 넓어지자 사유 칸이 눌려
**글자가 세로로 한 줄에 한두 자씩 쌓였다.**

- `table-layout: fixed` + `colgroup` 으로 칸마다 폭을 정했다(사유 240px).
  표가 좁아지면 칸을 줄이는 대신 **가로로 스크롤**한다(바깥에 이미 걸려 있다).
  `minWidth` 도 640 → 980 으로.
- 사유는 **두 줄까지**만 보이고 나머지는 잘린다. 길게 적어 온 사람 하나 때문에
  줄 높이가 다 늘어나면 목록을 훑을 수가 없다. 전문은 마우스를 올리면 뜬다.
- 이메일·닉네임도 넘치면 말줄임(전문은 마우스 올리면).
- 동작 칸의 버튼이 줄바꿈되지 않게 `flexWrap: nowrap`.

실측: 같은 마크업으로 재 보니 칸 폭이 84/180/96/56/**240**/64/260 으로 지켜지고,
사유에 긴 글을 넣어도 줄 높이가 **57px** 로 평평하다.

### GNB 에 유료/무료 배지

지금까지는 마이페이지를 들어가야만 유료인지 알 수 있었다. 유료로 바뀐 순간엔
토스트가 뜨지만, 그 뒤로는 "지금 내가 유료였나" 를 확인할 자리가 없었다.

닉네임 옆에 배지를 붙였다 — `TopNavBar` 는 모든 화면에 떠 있는 GNB 라 어디서든
보인다.

- **PLUS** — 검정 바탕에 노랑. 다른 화면의 PLUS 배지(`UsageMeter.tsx`,
  `PlanUpgradeToast.tsx`)와 같은 값을 그대로 썼다.
- **FREE** — 흐린 외곽선. 기본 상태라 눈에 띌 필요는 없지만, "지금 무료 맞다"
  는 확인이 되어야 한다.
- 체험 중인 비회원에게는 아직 플랜이랄 게 없어 배지를 안 보여 준다.

실측(같은 스타일로 만든 자리 재기): 375px 화면에서 닉네임이 길어도(`이대식
(씨맨스클럽)` = 119px) 로고 · 닉네임 · 배지(40px) · 로그아웃 이 안 넘친다.

### 긴급 — 비회원 `/api/usage` 가 전부 503 이었다

어제 유료 기간(`plan_until`) 커밋에서 `_plan_of()` 를 고치며, **비회원
분기만 2개짜리 튜플**(`return "guest", None`)을 그대로 뒀다. 회원 분기와
만료 분기는 전부 3개(`plan, daily, until`)를 돌려주는데, 부르는 쪽
(`status()`)은 항상 `plan, daily, until = _plan_of(...)` 로 셋을 받는다.

**비회원이 이 함수를 부를 때마다 `ValueError: not enough values to unpack`
가 났고, `/api/usage` 라우트의 catch-all 이 그걸 삼켜 503 을 돌려주고
있었다.** 로그인 안 한 사람은 크레딧을 아예 못 봤고, `canAi` 가 늘
false 라서 챗봇·사진 인식·AI 식단 **버튼이 눌러도 반응하지 않았다** — 이
세션에서 실제로 재현했다(같은 기기로 반복 테스트하다 체험 크레딧을 다
쓴 뒤, 기기 식별자를 새로 발급받아도 여전히 막혀 있어서 발견했다).

로그인한 사용자는 영향이 없었다(그 분기는 처음부터 3개를 돌려주고 있었다).

```python
if user_id is None:
    return "guest", None, None   # 전: return "guest", None
```

실측: 고친 뒤 `status(user_id=None, ...)` 가 정상적으로 `balance: 5,
daily_remaining: 3, ...` 를 돌려주는 것 확인.

### "고기 없이" — 이번엔 부위 단위까지

지난번 고침(`6f359d26`)은 손으로 적은 목록(BAN_EXPAND)에 없는 부위를
놓쳤다. 실측: "고기 없이" 라고 해도 **닭봉·닭날개·닭똥집·닭발·닭안심살**이
든 요리가 그대로 나왔다 — 이 부위들이 손으로 적은 목록에 없었을 뿐, 사전
에는 이미 `세분류=육류` 로 분류돼 있었다.

**사전의 분류(세분류·소분류·hyperonym)를 직접 본다.** "고기"·"육류" 는
`세분류 in (육류, 가공육)` 전체(122개)로, "닭"·"돼지고기"·"소고기"·"양고기"
는 그 뿌리 + hyperonym 자식(사전에 이미 있는 관계, 예: 닭봉→hyperonym=
닭고기) + 이름에 핵심 글자가 있고 같은 분류인 것(훈제닭고기처럼 hyperonym
이 안 달린 예외)으로 넓힌다. 손으로 목록을 다시 안 적어도, **사전에
재료가 늘면 다음 요청부터 같이 커버된다.**

곁다리로 잡은 두 가지 버그:
- `"닭 없이"` 가 아예 안 걸렸다 — 한 글자 단어는 잡음으로 보고 걸러 내는
  guard(`len(word) < 2`)가 "닭" 처럼 실제로 한 글자인 재료명까지 막고
  있었다. **알려진 분류 표제어일 때만** 한 글자를 통과시킨다.
- `bans()` 가 결과를 40개로 잘랐다. 손으로 적은 목록은 20~30개라 문제
  없었는데, "고기" 하나가 이제 122개를 돌려주면서 **앞 40개 안에 우연히
  안 든 재료(돼지고기·베이컨 등)가 걸러지지 않고 통과했다.** 400개로 올렸다.

실측(실제 후보 300개, 냉장고에 돼지고기·닭다리·닭날개가 있는 상태):
`고기 없이` → 297개 걸러지고 3개 남음, 고른 7개 전부 고기 무검출.
`닭 없이` → 부위 8종 전부(닭봉·닭날개·닭똥집·닭발·닭껍질·닭안심살·닭다리살·
훈제닭고기) 정확히 잡힘.

### AI 버튼 문구 — 무엇에 "말한 대로" 인지

`말한 대로 · 크레딧 3` 만으로는 옆의 무료 버튼(`냉장고 재료로 · 무료`)과
무엇이 다른지 안 풀렸다. 이 버튼이 실제로 하는 일은 셋이다 — 냉장고
재료(둘 다 본다) + 내가 적은 요청(AI만 본다) + 장보기를 최소로 짜는
계산(AI만 한다). 버튼을 62 → 74px 로 키워 두 줄로 적는다:

```
재료+요청 반영해 장보기 최소화 · 크레딧 3
```

실측(같은 폭·글자 크기로 만든 자리 재기): 168px 폭에서 정확히 2줄,
겹치지 않는다.

### 냉장고요리 — 매칭률·정렬·임박재료가 첫 화면 프리페치에 안 걸려 있었다

어제 넣은 "냉장고요리 첫 화면 미리 받기"(`recipePrefetch.ts`) 가 조건을 너무
느슨하게 봤다. 실제로 있었던 구멍 둘:

1. **매칭률 구간을 안 봤다.** 냉장고에 재료가 있으면 첫 화면 기본값이
   `30~100%` 인데(`getInitialSortBarState`), 미리 받아 둔 것은 그 하한 없이
   받아 온 것이었다. 사용자가 매칭률 구간을 바꿔도 — 그 값이 재사용 조건에
   없었으니 — 여전히 하한 없는 옛 캐시가 나갔다.
2. **임박재료 우선을 안 봤다.** "곧 상하는 재료 우선" 모드는 서버에 보내는
   정렬 기준(`sort_by`)이 평소와 똑같이 `match_rate` 다(임박재료는 그 안에서
   우선순위로만 반영된다) — 그래서 정렬 기준 검사만으로는 안 걸러지고,
   임박재료 조건 없이 받아 둔 캐시가 그대로 나갔다.

**고쳤다.**
- 미리 받을 때도 실제 화면과 같은 매칭률 하한(`recipePrefetch.defaultMatchRateMin()`
  — 재료 있으면 30, 없으면 0)을 넣어 요청한다.
- 화면 쪽 재사용 조건(`canUsePrefetch`)에 매칭률 구간과 임박재료 조건을
  둘 다 추가했다 — 하나라도 기본값과 다르면 미리 받은 것을 안 쓰고 제 조건으로
  다시 받는다.

### 매칭률을 바꾸고 응답 오기 전에 다른 화면 갔다 오면 원복되던 것

이 화면의 조회 하나가 2.5초 넘게 걸린다. **조건을 바꾼 시점**과 **그 결과가
와서 저장되는 시점** 사이에 간격이 있는데, 지금까지 sessionStorage 저장은
`결과가 채워진 뒤에만` 함께 저장됐다(`cachedFilteredRecipes.length > 0` 조건).
그 사이에 다른 화면에 갔다 오면 — 새 결과가 아직 저장된 적이 없으니 — 저장돼
있던 건 여전히 **바꾸기 전** 값이었고, 돌아왔을 때 그걸 복원해서 "바꾼 게
없어졌다" 로 보였다.

**고른 조건만 따로, 즉시 저장한다**(`STORAGE_KEY_RECIPE_SETTINGS`) — 응답을
기다리지 않는다. 복원할 때는 이 조건을 결과 캐시보다 우선해서 적용하고,
캐시가 그 조건과 안 맞으면(자연히 필터 해시가 달라지므로) 필터 변경 감지
로직이 알아서 새로 받아 온다 — 옛 캐시를 잠깐 보여 준 뒤 조용히 바뀐다.

### GNB 닉네임 팝업에도 유료/무료 상태 + 전환 버튼

GNB 배지만으로는 "지금 며칠 남았지" 까지는 안 보였다. 닉네임을 누르면 뜨는
"내 정보 수정" 팝업 맨 위에 크레딧 카드(`UsageGauge`)를 그대로 넣었다 —
마이페이지 본문에 있는 것과 **같은 부품**이라 문구가 저절로 같아진다.

- PLUS/FREE 배지 + (유료면) 언제까지인지
- 무료면 그 자리에 그대로 있던 **`더 필요해요`**(유료 전환 요청) 버튼

배지가 카드 안에 두 번(위쪽 새로 넣은 것 + 아래 문장 속 것) 나오고 있던 것도
겸사겸사 정리했다 — `PlanBadge` 를 `UsageMeter.tsx` 한 곳에서만 정의하고
`TopNavBar.tsx` 도 그걸 가져다 쓰게 했다(전에는 각자 따로 정의해 색이 어긋날
여지가 있었다).

### 팝업 속 크레딧 카드 위치 · 식단 버튼 두 줄 맞춤

**크레딧 카드를 "내 정보 수정" 팝업 맨 위 → 비밀번호 변경 아래로.**
위쪽은 *고치러 온 칸*(닉네임·비밀번호)이고 크레딧은 고치는 것이 아니라
*지금 상태를 보는 것*이다. 입력란보다 앞에 두니 폼이 시작되기도 전에
끊겼다. 취소/적용 버튼 바로 위, 구분선을 하나 두고 앉힌다.

**식단 추천 버튼 두 개의 부제를 같은 두 줄로.**
전에는 AI 쪽만 한 문장을 넣고 줄바꿈을 브라우저에 맡겼다 — 폭에 따라 1줄이
됐다 2줄이 됐다 하는데 옆의 무료 버튼은 늘 1줄이라 글자 아랫단이 어긋났다.
이제 둘 다 `무엇을 보고 / 무엇을 해 주고 얼마` 두 줄을 **직접 나눠** 적고
`nowrap` 이라 폭이 변해도 줄 수가 안 변한다. 부제 글자 크기도 11 로 통일
(무료 쪽만 11.5 였다).

- AI: `냉장고 재료 + 내 요청` / `장보기 최소화 · 크레딧 3`
- 무료: `냉장고 재료만 보고` / `일주일 식단 · 무료`

375px·360px 에서 실제로 띄워 확인했다 — 두 버튼의 네 줄이 같은 높이에서
끊긴다. 320px 에서는 AI **제목**이 두 줄로 접히는데 이건 전부터 그랬고
(제목 길이·글자 크기를 안 건드렸다) 이번 건과 별개다.

### 룰베이스 재료 매칭 — 긴 이름이 이기게, `가지` 수량사 차단

레시피 카드의 재료가 이상하다는 제보. 실제로 재 보니 **본문에 없는 재료가
꾸준히 섞여 있었다.** 원인은 두 가지였고 둘 다 `extract_ingredients()` 다.

**1. 짧은 이름이 긴 이름에 덤으로 따라붙었다.** 사전의 모든 이름을
`if 이름 in 줄` 로 따로 넣다 보니 `고추장` 한 줄이 `고추장` 과 `고추` 를 둘 다
만들었다. 같은 본문을 LLM 이 읽은 결과와 견주면 (룰 / LLM):

| 짧은 쪽 | 어디서 왔나 | 룰 | LLM |
|---|---|---|---|
| 고추 | 고추장·고춧가루·청양고추 | 26.4% | 2.6% |
| 참치 | 참치액·참치액젓 | 11.9% | 1.5% |
| 버섯 | 표고버섯·새송이버섯 | 8.4% | 0.6% |
| 올리브 | 올리브유 | 7.6% | 0.7% |
| 매실 | 매실청 | 5.5% | 0.3% |
| 다리 | 닭다리·앞다리살·코다리 | 4.5% | 0.03% |

가장 심한 건 **식용유**였다. 사전에서 `기름`·`오일` 이 식용유의 동의어라
`참기름`·`들기름`·`올리브오일` 이 전부 식용유를 하나씩 더 만들었다 —
표본 2,000건 중 437건.

이제 **자리(위치)까지 보고** 더 긴 이름에 완전히 덮이는 것은 버린다.
한 줄에 `고추장 1T, 고추 2개` 처럼 진짜로 둘 다 있으면 자리가 달라 둘 다 남는다.

**2. `가지` 는 채소이면서 "종류"를 세는 말이다.** `"여러 가지 방법"`,
`"밑반찬 10가지"`, `"가지고 있는"` 이 전부 가지로 잡혔다. 44,205건 중
4,309건에 가지가 붙어 있었고 그 중 3,116건은 제목에 가지가 아예 없었다 —
`믹스커피말차라떼` 의 재료가 `얼음,우유,원두,가지,말차` 였다.
수량 표현 뒤·`가지고`·`가지런히` 자리만 떨어뜨린다. `"간단한 가지무침"` 의
`한` 처럼 낱말 끝 글자가 우연히 수량사와 같은 경우를 살리려고, 수량사는 그
앞이 한글이 아닐 때만 인정한다.

같은 블록에 옛/새 매칭만 갈아 끼워 최신 2,000건으로 확인:
- 1,523건이 달라짐, 재료 평균 **9.49 → 7.92개**
- **새로 생긴 재료 0건** (순수하게 덜어내기만 한다)
- 4개 이상 → 3개 이하로 떨어진 것 128건 (원래 그만큼 없었던 것)

### 매일 도는 LLM 배치를 최신 글부터 (`--order recent`)

조리 순서 요약(음성으로 읽어 주는 부분)이 최신 글에 하나도 없다는 제보.
기본값이 `id` 오름차순, 즉 **오래된 것부터**였다. 앱은 최신순으로 보여 주는데
처리된 것은 id 88,045 까지였고 가장 최근 글은 170,704 였다 — **최신 500건 중
`cook_steps` 가 있는 것이 0건.**

밀린 34,323건을 다 끝내려면 하루 5,280건씩 7일이 걸리는데, 순서를 뒤집으면
총 걸리는 시간은 같고 **보이는 쪽부터** 맞춰진다. 다 따라잡은 뒤에는 매주 새로
크롤링된 것이 다음날 바로 처리된다.

참고로 지금 LLM 이 본 것은 9,890 / 44,210 건(22%)이다. 나머지 34,320건은
`recipe_name` 이 비어 있어 **한 번도 LLM 을 거치지 않았다** — 위 `가지` 오탐
4,309건 중 4,040건이 이 미처리분에 있다(LLM 이 본 269건은 대부분 진짜
가지무침·가지볶음이었다).

### 레시피 카드 테두리가 안 보이던 것

카드도 흰색, 뒤 배경도 흰색인데 선이 `0.5px rgba(0,0,0,0.06)`(≒`#F0F0F0`)
이라 **어디까지가 한 장인지** 안 보였다. 0.5px 이라 배율에 따라 아예 안
그려지는 화면도 있었다. 기본 구분선(`--line-200`) 1px + 옅은 그림자로 바꿨다.
선만 진하게 하면 표처럼 딱딱해지는데, 그림자가 같이 있으면 얇은 선으로도
경계가 읽힌다.

### 8월 LLM 사이클 결과가 룰베이스에 덮인 사고 — 원인과 복구

**무슨 일이 있었나.** 8/26~9/3 에 LLM 이 누적 44,868건의 재료를 한 바퀴 다시
뽑아 사이클을 끝냈다(9/3 에 남은 것이 563건뿐이었다). 그런데 지금 DB 를 보면
78% 가 룰베이스 값이다. 9/3 배치의 미리보기 CSV 에는 제대로 뽑힌 값이 남아
있는데 DB 에는 옛 값이 있다:

```
id=170349  "I Put CHEESE CAKE Inside Japanese Monaka"
  CSV(9/3 LLM): 달걀,말차가루,생크림,설탕,소금,옥수수전분,크림치즈,팥앙금,화이트초콜릿
  DB:           가지,검정깨,말차,치즈            ← 룰베이스 값
```

**원인은 셋이 겹친 것이다.**

1. `used_ingredients` **한 칸을 룰베이스와 LLM 이 같이 쓴다.** 나중에 쓴 쪽이
   이기고, 어느 쪽이 쓴 값인지 구분할 방법이 없었다.
2. 룰베이스 배치의 안전장치가 **`llm_ingredients_done = 0`** 이었다. 이 플래그는
   뜻이 두 개다 — "LLM 이 값을 넣어 뒀다" 와 "다시 처리할 필요가 없다".
   재처리 예약은 두 번째 뜻으로 0 을 넣는데, 룰베이스는 첫 번째 뜻으로 읽었다.
3. 그 배치는 "신규 수집분 임시 채움" 이라면서 실제로는 **`--limit` 없이 전량
   재계산**이다. 조건만 풀리면 즉시 카탈로그 전체를 덮는다.

**방아쇠.** 9/3 오전 "레시피 아님" 판정을 넣으며 44,868건을 재예약 → done 이
전부 0 → 그날 13:09 부터 크롤러 뒤에 붙은 룰베이스 배치의 대상이 전량이 됨 →
**9/5 13:50~18:13 (4시간 22분)** 에 완주하며 9일치 LLM 결과를 덮어씀.
(같은 배치가 9/2 에는 12:48~12:53, **5분**이었다. 4시간이 걸린 것 자체가
전량을 건드렸다는 증거다.)

살아남은 것은 9/5·9/6 새벽 LLM 배치가 다시 처리한 id ≤ 88,062 뿐이다:

| id 구간 | 가지 | 고추 | 식용유 |
|---|---|---|---|
| ≤88,062 (재처리 완료) | 2.8% | 2.8% | 21.6% |
| 88,063~170,349 (덮어써짐) | 11.7% | 26.5% | 37.3% |

**고친 것.**

- `recipes.llm_ingredients_at` 열을 새로 뒀다 — **LLM 이 실제로 값을 쓴 시각**.
  재처리 예약이 건드리지 않는다. 룰베이스 배치의 조건을
  `llm_ingredients_done = 0` → **`llm_ingredients_at IS NULL`** 로 바꿨다.
  이제 재예약을 몇 번 하든 LLM 값은 안 덮인다.
- 미리보기 CSV 에 **44,367건의 LLM 원본이 전부 남아 있다.**
  `renormalize_used_ingredients.py --restore` 로 LLM 호출 0회로 되돌린다.
  덤으로 CSV 우선순위 버그도 고쳤다 — 파일명 정렬이라 `_5400_20260826` 이
  `_5280_20260906` 보다 뒤에 와서 **옛 원본이 새 원본을 덮고** 있었다.
  이제 만든 시각으로 정렬한다.
- `scripts/mark_llm_restored.py` — 값이 우연히 같아 UPDATE 를 건너뛴 행에도
  표식을 채운다. 표식이 없으면 앱이 숨기고 룰베이스가 다시 덮는다.

### 재료가 다 채워지기 전에는 앱에 안 띄운다

크롤러는 LLM 을 기다리지 않는다 — 룰베이스로 훑은 임시 재료를 붙여 바로
넣는다. 그 값이 그대로 보이니 사람이 카드에서 **본문에 없는 재료**를 봤다.
매칭률도 그만큼 틀렸다.

이제 조회에 `llm_ingredients_at IS NOT NULL` 을 건다 — 냉장고요리 필터,
요즘인기, 검색, 목록, AI 식단 후보. 새로 크롤링된 글은 LLM 배치를 지나야
목록에 나타나고, 그때는 재료·조리 순서·요리명이 다 채워진 **완성된 카드**다.
크롤러가 마지막에 신규분 LLM(`--limit 450`)을 바로 돌리므로 보통 같은 실행
안에서 공개된다.

저장한 레시피·완료한 요리·즐겨찾기와 상세 페이지는 뺐다 — 이미 본 것이
사라지면 안 된다.

### 카드 여백·펼침, 버튼 정렬, 모달 여백

- **레시피 카드 안쪽 여백을 사방 같게.** `3px 8px` 이라 테두리까지 합쳐
  좌우 9px, 위아래 4px 였다 — 썸네일이 위에만 바짝 붙어 위로 쏠려 보였다.
  사방 9px 로 맞추면서 카드가 277 → 287px 이 돼, 가로 목록의 고정 높이도
  280 → 290 으로 올렸다(안 올리면 잘린다).
- **가로 카드에서 `재료 N개 · 대체 가능 보기 ▾` 가 잘리던 것.** 모든 카드를
  같은 규격으로 맞추려고 높이를 고정하고 넘치는 것을 잘라 냈는데, 펼친
  내용까지 잘려 누른 보람이 없었다. 접혀 있는 동안만 고정한다.
- **식단 추천 버튼 두 개의 설명을 왼쪽 정렬.** `<button>` 이 기본으로 가운데
  정렬이라, 칸은 왼쪽에 붙어도 그 안에서 두 줄이 서로 가운데로 맞춰져 짧은
  줄이 들여쓴 것처럼 보였다.
- **내 정보 수정 팝업**: 아래 여백을 `pb-8` 로 넉넉히(회원탈퇴 글씨가 끝에
  붙어 보였다). 취소 버튼의 브라우저 기본 포커스 테두리(파란 선)를 껐다 —
  옆의 `변경 적용` 과 회원탈퇴는 이미 끄고 있어서 취소만 파랗게 튀었다.

### 챗봇 크레딧 표시

응답에 실려 오는 사용량을 즉시 반영하는 코드는 이미 있었고, 프로덕션에서
실제로 확인했다 — 질문 하나에 헤더가 `크레딧 5 · 오늘 3` → `크레딧 4 · 오늘 2`
로 창을 나가지 않고 바로 바뀐다. 다만 서버가 사용량을 안 실어 보내는 길이
있다(광범위한 질문은 LLM 을 안 불러 차감이 없고, 네트워크가 끊기면 응답이
없다). 그런 때 옛 숫자가 남지 않도록 보낸 뒤 한 번 더 확인한다.

### 복구 결과 (2026-09-06)

`renormalize --restore` 로 **32,927건**을 LLM 원본에서 되돌렸다(LLM 호출 0회,
약 40분). 값이 우연히 같아 UPDATE 를 건너뛴 509건은 `mark_llm_restored.py` 로
표식만 채웠다.

오탐이 그대로 사라졌다:

| 재료 | 복구 전 | 복구 후 |
|---|---|---|
| 고추 | 9,067 | 1,136 |
| 참치 | 4,097 | 842 |
| 가지 | 4,309 | 1,367 |
| 버섯 | 2,880 | 578 |
| 올리브 | 2,590 | 334 |
| 매실 | 1,874 | 167 |
| 다리 | 1,548 | 70 |

제목에 `가지` 가 없는데 재료에 가지가 든 것: **3,116건 → 495건.**

남은 495건과 `가지 1,367건` 은 8월 사이클 때 LLM 이 실제로 뽑은 값이다.
룰베이스 오탐과는 성격이 다르므로 여기서 더 건드리지 않는다 — 매일 도는
배치가 새 프롬프트로 다시 판정한다.

**앱에서 숨는 것 932건** (전체의 2.1%). 원본이 안 남은 행 930건 + 신규분이다.
`llm_ingredients_at` 이 비어 있어 목록에 안 나오고, 매일 새벽 배치가 처리하는
대로 하나씩 다시 나타난다(이제 최신 글부터 돈다).
